各位同学大家好，在这节课我们要探讨带符号整数补码的嗯，除法运算原理，那么在这节课当中我会给大家设计五个例子。这五个例子可以涵盖带符号，整数除法的呃，大部分情况。由于除法运算的底层原理过于复杂，所以在考试当中，通常考察的是呃，除法运算的这个基本逻辑而不会深入考察它底层每一步二进制如何运算的这些细节。好，所以我们在学习除法电路的时候啊，我们重点关注除法电路的开始状态。结束状态以及如何判断除法的异常，

那除法异常有这样的两种，分别是除以零以及啊商溢出这两类异常。那这节课当中，我也会用具体的例子呃，带大家看见这两种异常的发生。那关于带符号整数补码除法的呃，最重要的东西我都总结在了这张思维导图里，其中我标红的这三大部分更有可能作为出题点。好，接下来我们先看第一个例子。这是一个带符号，整数除法的嗯，电路结构可以看到它和无符号整数除法的电路一模一样。只不过控制逻辑这个部分在发出控制信号的时候会和无符号整数除法电路有一些区别。好，

那么这个除法电路，它可以支持2n比特的呃带符号整数除以n比特的带符号整数。最终可以得到n比特的熵和。n比特的余数在右边，这个电路图当中所谓的n就是四。可以看到aru可以支持四位的加法和减法运算，有三个寄存器rqy。它们的位数分别也都是四个比特，以及在控制逻辑的内部，这个地方有一个东西叫计数器。好，接下来我们尝试着让右边这个电路去进行负七÷3这个除法运算。那么，负七用四比特的补码表示应该是幺零零幺。而除数三用四比特补码应该是零零幺幺，

我们可以先手算推出我们期待的这个呃n比特商和n比特余数，最终的结果应该是多少？呃，显然负七÷3，最终熵值应该等于负二余数，应该等于负一，对吧？那同样的把负二和负一，它的二进制比特呃先写出来，那这两个东西就是我们呃期待得到的最终结果。好，现在为了实现负七÷3这个逻辑，首先呃，在正式开始除法运算之前，需要对这个除法电路。

进行初始化。首先是被除数的初始化，被除数负七幺零零幺，应该把它放到这个呃寄存器q里边。那之前我们说这个电路，它原本是可以支持2n比特，除以n比特对吧？现在这个被除数，它的比特数只有n比特，那么我们需要把这个被除数。拓展为2n比特的长度，同时保持它的真值不变。那通过之前的学习，我们知道，如果要把补码的位数呃拓展，

那么我们会采用符号拓展的，这种逻辑也就是观察原本的这个补码，它的符号位如果等于一。那么，更高的拓充的这些位，我们也补一，而如果原本的符号位等于零，那么更高的这些拓充位，我们也把它补零，那进行这种符号拓展之后。恩比特的补码变成了二恩比特的长度，但与此同时，这个补码的真值不会发生任何变化。好，那这是被除数的初始化，

用这两个四比特寄存器去保存2n比特的，这个拓展的被除数。好，接下来除数恩比特嗯零零幺幺，应该把它放在寄存器y这个地方。零零幺幺最后还需要对计数器CN，也就是控制逻辑里边的这个计数器对它进行初始化，把它的初始值置为n。那么，在这个例子当中n=4，所以CN=4，表示接下来还需要进行四轮的处理，每一轮的处理都会确认一比特的熵。那最终，我们进行四轮处理之后，

就可以得到四比特的熵。在接下来的每一轮处理当中，首先都要进行左移操作，把寄存器r和q的这些比特通通左移。一位。左移一位，那么更高移出的这一位，我们就把它丢弃更低的这一位，就会空出来，那这一位就是本轮处理当中要上熵的位置。上升的规则比较复杂。首先，第一步要进行加法或者减法运算。这个控制逻辑会检查当前的这个余数r，这四个比特的余数以及除数y。

它们俩之间符号位是否相同？根据符号位相同或者不同去决定本轮到底要进行加法还是减法？那像这个例子当中余数的这个符号等于呃一，然后除数的符号位等于零，那二者的符号位相异。这种情况下就说明，本轮应该进行加法运算。那相应的，如果二者的符号位相同，那本轮就应该进行减法运算。加法和减法的两个数，观察这个电路分别来自于r。和y这两个寄存器，所以如果进行加法的话，就是r+y，

如果本轮进行减法的话，就是r-y，就这个逻辑。好，那么由于这一轮处理当中要进行加法，那四个一加上零零一一。相加的结果应该等于零零一零。相加或者相减的这个运算结果。会作为新的余数r写到这个余数寄存器当中。接下来，这个控制逻辑会检查这一次得到的这个新余数，它的符号位。和以前的这个老余数相比，呃符号位有没有发生变化？如果符号位发生变化。

那么，本轮的处理当中，这一位商就应该上零。那在这个例子中，老的这个余数符号位为一新的这个余数符号位为零，对吧？那符号发生了变化，所以本轮商零就是这个意思。好那么余数的符号改变，除了上商零之外，还需要做恢复余数的动作。刚才我说过这样的一句话，就是本轮处理得到的这个新余数。呃，会被写到这个寄存器r当中对吧？

但是现在这个控制逻辑发现哎，它的这个符号位发生了变化，那在这种情况下控制逻辑就会把这个余数。恢复成老的这个样子。也就是说，经过刚才这一轮处理，兜兜转转之后，这个余数寄存器r里边的值最终又会停留在幺幺幺这个值。那至于为什么符号改变的时候要恢复余数这个问题，我们就不再深究，有兴趣的同学可以去参考袁春峰老师的教材，里边有更详细的讲解。但是可能会耗费比较多的时间，对于考研来说，可能性价比并不高好，

那么回到刚才的这一轮处理。现在我们已经确定了本轮的熵等于多少，以及这个余数最终的值应该恢复原样，还是说应该把新余数写进来？所有这些处理都啊，完成之后控制逻辑会让计数器的值减1 CN减减。于是，计数器的值从四来到了三，表示接下来还需要进行三轮。刚才呃，同样的运算。好，那么接下来一轮的运算也是一样的呃，整体往左移最高位丢弃最低位呃空出来。本轮处理上熵就上在这个位置。

上上的规则还是一样的，控制逻辑会检查当前的中间余数r和除数y之间，它们的符号位是否相同？如果说两者是异号的，那么本轮就要进行加法运算，也就是r。加y。这个加法运算是由中间的这个aru完成的，那么幺幺幺零加上零零幺幺本轮运算的结果应该是零零零幺。接下来，本轮加减运算得到的这个新余数和以前的这个老余数进行对比。符号位发生了变化，那么说明本轮应该上商零。与此同时，符号位发生变化，

那说明这个余数寄存器r的值应该保持原样。而不是替换为新余数保持原样即可。当然，所谓的保持原样，它在背后是先把新余数写到这个寄存器呃当中，然后再用加法或者减法的逆运算，再把它恢复成原本的这个余数幺幺幺零。那背后的过程，我们就不再深究好，本轮的熵也确定了，接下来就进行计数器CN减减的操作。从三来到了二，意味着接下来还需要进行两轮。同样的，这种操作，

那后面的两轮运算，我们就不再做详细的探讨。最终，总共经过四轮的这个处理之后，我们的呃除法运算器。它会来到这样的一个状态。就是CN的值会减到零。除法运算结束，此时余数寄存器里的值应该是四个一，而熵寄存器里的值和我们期待的这个幺幺幺零还不太一样。好，那么对于这个例子来说，呃，对这个熵q还需要进行额外的一步处理控制逻辑。会检查，

如果说初始的这个被除数和除数，它们之间是异号的。那么，最终得到的这个熵q还需要进行额外的一步运算，就是取补所谓的取补就是。全部为按位取反，末位加一那么零零幺零，全部位按位取反应该是幺幺零幺，对吧？末位再加一之后，最终应该得到的是幺幺幺零这样的一个值好，因此进行最后的这步操作之后。这个寄存器q里边的值最终就停在了幺幺幺零，这个值那这正是我们期待的最终结果。好，

那么这是第一个例子，我们看到了两个呃异号的数相除呃，最终的一个效果。好，接下来看第二个例子。在第二个例子中，我们会看到两个同号的数相除，它的初始状态以及最终状态应该是什么样？假设被除数等于六，除数等于三，那么写出四比特的补码表示，我们可以手算推演，最终得到的商应该是二余数等于零。这除法电路正式开始运算之前，需要进行数据的初始化，

那被除数六。零幺幺零先写到这个寄存器q里边。我们需要把n比特的被除数拓展为2n比特，那么对于补码来说，为了确保拓展之后它的真值不变，我们需要进行的是。符号扩展对吧？原本的符号位等于零，那么更高的这n个比特也全部用零来拓充。好，这是被除数的初始化。接下来，除数啊，直接把它零零幺幺写到寄存器y里边，最后计数器CN也需要初始化，

把它的值设为四。那接下来就是进行四轮的处理，每一轮的处理都需要先进行左移。然后上熵最终计数器还要减一。这中间的过程，我们就把它略过，那最终这个除法电路经过四轮的运算之后。各个寄存器的值会停在这样的一个状态。余数寄存器r应该等于四比特的全零对应余数的真值，而熵寄存器呃q里边的值应该对应的是二，也就是零零幺零。除数寄存器的值保持原样，保持初始值不变，计数器最终停留在了零这个值。好，

那么这是第二个例子，我们看到了两个同号的数相除，最终的一个结果。接下来第三个例子，从这个例子开始，我们会看到除法运算的一些特殊情况，假设被除数等于三。除数等于负七，那按照我们小学数学的这个经验来看，三除以负七它的呃。商应该等于零对吧，而最终的这个余数应该等于三，也就是等于被除数，它自身好，所以这个例子它体现的是除法运算的一种比较特殊的情况。

如果被除数小于除数，那么这个除法运算的n轮处理其实就没必要进行了，可以一步跳到最终的结果。可以确定最终的商肯定等于零，以及最终的余数肯定等于被除数自身。好，所以对于这个例子来说，首先这个除法电路进行被除数除数的初始化，被除数需要把n比特扩展为2n比特。计数器初始值设为四好，初始化完成之后，这个控制逻辑就会检查被除数的绝对值是否小于除数的绝对值。那如果满足这个条件，那OK接下来的四步运算就直接跳过一步到位，直接得出最终的熵和余数。

商q=4个零余数等于这个被除数，它原本的值也就是零零幺幺。那这样的特殊处理可以提高除法器的运算效率。好，这是第三个例子。接下来看第四个例子，在这个例子当中，我们可以看到除以零这种异常情况。除法电路是怎么去发现的？好假设被除数等于三，除数等于零，那么一个实数除以零，这个运算是不被允许的，对吧？好，

那么对于这个除法电路来说，呃，它同样的先进行被除数和除数，以及这个计数器的初始化。初始化完了之后，它会检查这个除数寄存器里的值是否为零，如果是，那么就直接抛出除数为零的这种异常。从而会调出操作系统的这个呃异常处理程序。那相应的，这个除法显然不应该让它继续下去，对吧？直接把这个除法停止就行了。好，那这是除以零的异常。

除法电路的控制逻辑能够检测出这种非法的情况，并且及时的停止这个除法运算。好，这是第四个例子，接下来最后一个例子。在除法运算当中，有可能会发生溢出，那对于两个恩比特补码相除的这种呃运算来说。最终，我们会用恩比特来保存熵，那有且只有一种特殊的情况，可能会让呃，这个运算的结果也就是熵溢出。如果用绝对值最大的负数除以负一，就可能会发生这种情况好结合这个例子来理解，

在这个例子当中n=4。那么，四比特的补码可以表示的合法范围应该是负八到正七，对吧？好，那么假设用这个除法电路完成这个负八除以负一这种运算，那最终的运算结果商的真值应该等于八对吧？但是我们说这个除法电路，它最终只会用四个比特来保存熵的值，那显然四比特的这个熵无法去表示正八这个值，它最大值能表示到正七。好，所以对于这种特殊的情况就是绝对值最大的负数，除以负一这个除法最终会触发商溢出的异常。那么，

这个电路是怎么检测到这种异常的呢？首先第一步，同样的先把被除数和除数进行初始化。当然，计数器也要初始化，那么被除数这个部分呃，需要进行符号拓展好初始化完成之后。控制逻辑会检查当前的这个被除数和除数是不是刚好就是这种绝对值最大的负数，除以负一的这种组合？如果是这种组合，那OK后面的四轮运算就不用再算了，直接抛出商溢出异常，并且停止后续的几轮处理。好，所以商议出异常并不是在n轮处理结束之后才发现的，

而是在初始化完成之后刚开始的时候就可以被这个控制逻辑发现，并且及时的处理。好，那么这是除法电路可能遇到的第三种特殊的情况，好的，那么在这个视频中我们介绍了带负号整数补码的除法运算。对于考试来说，我们重点是要能够结合手算的方式去了解这个除法电路的初始状态以及结束状态。当然，除法电路可能会遇到的几种特殊情况，我们也需要重视。而至于中间的n轮处理。特别是上熵的这个规则，有兴趣有能力的同学可以结合袁春峰老师的教材来加强学习。那大家可以再结合这一页思维导图进行回顾，

好的，那以上就是带符号整数除法的所有内容。
