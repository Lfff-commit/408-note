各位同学大家好，通过之前的学习，我们已经了解到呃无符号整数的乘法是如何用计算机硬件去实现的？那在这个视频中，我们要继续探讨带符号整数的乘法运，算它的实现原理。那我们首先会介绍带符号整数的呃，这个乘法电路的结构。以及会用一个例子带大家感受呃，这个电路的工作过程，最后我们会介绍带负号整数乘法的溢出判断是怎么进行的？那首先我们引入一个呃，大家熟悉的C语言的例子，在C语言当中无符号整数。我们会使用unsigned这个呃关键字去修饰，

比如说unsigned int这样的一个无符号整数，它就是占32个比特。我们可以让两个无符号整数相乘，乘积的结果再付给另外一个无符号整数，那这样的乘法运算可以用之前学习的无符号整数乘法电路去实现。那再来看带符号整数的例子，如果int前面没有加unsigned这个关键字，那么呃，这样的一个变量，它就是一个带符号整数。并且在计算机当中，这种带符号整数，它在计算机硬件的底层都是用补码来表示的。比如说在这个例子中，我们定义了xy这样的两个带负号整数呃，

分别是负的九八五和正的二幺幺，我们可以把两个带负号整数相乘。乘积的结果再付给另外一个带符号整数，那这样的乘法运算就是典型的带符号整数乘法。那对于计算机硬件来说，带符号整数的乘法运算和无符号整数的乘法运算有什么区别呢？这就是本节课探讨的内容。首先，回顾无负号整数乘法。当两个无负号整数相乘的时候，我们是这么做的，我们会用乘数的逐比特和呃被乘数。进行相乘。呃主比特相乘，再错位相加，

最终就可以得到乘积的结果。对于上面这个例子来说，我们代入十进制进行验算啊，那最终可以看到这个计算的结果是正确的。说明无负号整数的乘法可以采用这种逐位相乘，错位相加的思路呃进行处理。那对于计算机硬件来说，实现无符号整数的乘法，本质上就是用硬件去模拟我们呃手算的这个过程。主谓相乘，错位相加。好，现在我们需要思考，对于带符号整数的乘法来说。这种无负号整数的乘法电路是否同样适用呢？

那我们不妨代入十进制去验算一下，假如我们把这两个呃二进制串把它视为。带负号整数，也就是把它们视为四比特补码的话。那这个被乘数x，它对应的真值应该是十进制的负三。而乘数y，它对应的真值应该是十进制的负五。好，那如果我们把这两个带符号整数，把它丢给无符号整数的这个乘法电路进行运算的话。那最终运算的结果肯定和上面这个二进制串是一样的，对吧？那可以看到一零零零一一一这个乘积的结果，如果我们按照带符号整数，

也就是补码的这种方式去呃解读的话。它对应的应该是十进制的负幺幺三，那显然负三乘以负五，它不可能等于负的幺幺三。因此，这就说明，如果我们采用无符号整数的乘法电路去计算两个带符号整数的相乘，那么最终这个结果肯定是错误的。因此，对于带符号整数的乘法运算，我们需要设计另外一套电路。那不卖关子，我们直接把带负号整数的乘法电路给抛出来。观察这两个乘法电路可以看到呃，有很多相似的地方，

接下来我们重点关注二者的差异。首先可以看到，带符号整数的这个乘法电路，我们在乘数寄存器y的后面增加了这样的一个呃单独的位。我们不妨把它称为辅助位，黄色的这个小方框可以存放一比特的信息，那么这个辅助位的作用我们会用之后的例子来进行感受。好，另外在这个地方可以看到，对于带符号整数的乘法电路来说，控制逻辑，每一次会取末尾的两个比特，也就是乘数寄存器y的最低。一个比特以及刚才我们提到的这个辅助位，根据这两个比特去决定当前这一轮处理到底要进行加法。

还是减法运算。那这两个比特是如何影响控制逻辑的？这点会结合之后的例子来感受好，另外我们看到无符号整数的乘法电路。呃，在每一轮处理的这个加法运算当中，我们会把进位就是最高位产生的进位信息给保存下来。但是在呃带负号整数的这个乘法电路当中，我们是不保存最高位产生的进位信息的。那为什么这么设计？我们先不深究，我们先关注这两个电路之间的呃，显著的区别。好，接下来我们会用两个四比特的带负号整数为例呃，

去看一下这个电路是怎么工作的？我们说计算机内部的带负号整数都是用补码来表示的，所以这两个四比特数呃，我们按照补码的规则去解释的话。它们的真值应该对应负三和六，那么三六一十八最终乘积的结果应该是负18对吧？那负18如果用2n个比特去保存的话，应该是这样的一串二进制。所以这串二进制是我们最终期待的运算结果。那接下来我们会用这个电路完成四比特带符号整数的乘法运算，最终的乘积结果我们会用2n个比特，也就是八个比特来进行保存。那需要说明的是，由于带负号整数在计算机内部是用补码来表示的，而补码的这个乘法运算的数学原理。

要比无符号整数的这个呃数学原理解释起来会更复杂很多，我们得结合补码的特性去理解。但数学原理并不是我们考研的重点，所以为了降低大家的学习负担，我们会聚焦于考研的重点。接下来，我们只介绍这个呃带负号整数的乘法电路，它的工作过程。而不去探讨它的数学原理，也就是说接下来我们只解决how怎么做的问题，不解释WHY为什么这么做的问题。如果想要探究y的问题，大家可以去参考袁春风老师或者唐硕飞老师的教材。那对于计算机考研来说，我们理解how啊，

这个电路是如何工作的就行了。好，这是第一个值得说明的东西，第二个要说明的是带符号整数的乘法运算，我们会让符号位参与到运算当中。好，接下来我们让这套电路开始工作。那这套乘法电路是CPU内部的一个小电路，一个子电路。当CPU执行乘罚指令的时候。这条乘法指令首先需要指明呃被乘数。x乘数y分别是什么值？然后CPU执行这条乘法指令，本质上就是激活这一套呃乘法运算电路。让这一套电路去实现这个乘法指令所要求完成的这些事情。

好，那首先这套电路，它会把被乘数和乘数分别放在寄存器x和寄存器y当中。那被乘数是幺幺零幺，乘数是零幺幺零。好，除此之外，还需要把沉积寄存器p置为全零，以及把辅助位，也就是我们标黄色的这位也置为零。那结果就是这样的。好，接下来第三步还需要把计数器CN。置为n。这儿的n指的是乘数的位数，

那在这个例子中，我们假定被乘数和乘数都只有四个比特。所以CN的初始值应该设为四。好，这是乘法电路的初始化工作。那这儿补充一句。我们知道，零乘以任何数都等于零，对吧？所以如果被乘数或者乘数当中，也就是x或者y这两个寄存器当中，但凡出现一个全零的。啊，这这种数值。那么结果就可以直接确定为零，

我们就不需要再进行呃，后续的繁琐的这种运算了。那对于我们这儿的例子当中，被乘数和乘数都不是全零，所以这个乘法电路还得完成后面的工作。接下来和无符号整数的乘法类似。这套电路会进行n轮的处理，每一轮的处理需要进行加法或减法。以及一位运算。每经过一轮处理计数器CN的值都会减一，当CN的值减到零的时候，乘法运算结束。好，接下来我们开始第一轮的处理。首先，

我们需要把乘数寄存器y它的最低位以及辅助位。把这两个比特，这两个比特送到控制逻辑当中，控制逻辑会根据这两个比特的值。去决定在本轮处理当中到底应该加上x还是减掉x还是加上零？那由于是采用两个比特进行判断，所以有可能出现零零零一一零和一一这四种情况。这四种情况分别对应的处理，我在这张表格当中给大家罗列了。当这两个比特等于零零或者一一的时候，本轮只需要加零就可以。那加零就相当于什么都不加，对吧？那如果这两个比特等于零一，本轮就需要加上x。

如果这两个比特等于一零，本轮就需要减掉x。那观察这个电路的连接。中间这个核心部件是aru，可以支持加法减法，这些基本运算。aru的左边这一端输入它，连接的是寄存器p。连接的是寄存器p的这四个比特。而右边这个输入连接的是被乘数x。那刚才提到的加零+x-x就就是这些操作，我们指的是要把寄存器p。寄存器p的值加零或者加上x或者减掉x就是有可能出现这样的三种情况。所以，基于这样的一个电路连接，

如果我们要让寄存器p+x的话，那是不是控制逻辑？向呃aru发出加法的控制信号，就可以实现这个加法运算了。那如果是减掉x的话就是。发出减法控制信号嘛，而如果是要加上零的话，那控制逻辑既不需要发出加法信号，也不需要发出减法信号，因为加上零本质上就是什么都不加。对不对？让寄存器p的值保持原样即可。好，那回到这个具体的例子，现在我们进行第一轮的处理，

末尾的两个比特是零零。那零零这种情况，本轮应该加零，本轮应该进行加零的操作，那刚才我们说了加零本质上就是什么都不加，对吧？让寄存器p的值保持原样即可。因此，我们什么都不做，就可以实现加零的一个效果。那现在我们完成了第二个子步骤，接下来进行第三个子步骤，我们会把寄存器p。y以及辅助位视为整体。把这九个比特视为一个整体，

对它们进行算术右移移位的操作，注意是算术右移，而不是逻辑右移。那算术右移的效果就是低位直接移出丢弃，而高位空出来的这位，我们用原先的符号位。去填补那原本最高的这个符号位等于零，所以这次算术右移，我们在高位补零。好，那完成这一步之后，接下来计数器CN要减一，那么CN的值就从四来到了三。那接下来我们要进行第二轮的处理。首先把末尾的两个比特送到控制逻辑。

由于这两个比特的组合是一零，因此本轮处理需要让沉积寄存器p。减掉x。因此，控制逻辑向aru发出减法的控制信号。那aru会完成寄存器p减掉x这样的一个运算。那我们不妨用手算的方式模拟一下寄存器p减掉x这个过程。也就是用零零零减掉一一零一。那补码的减法运算可以转换成与之等价的加法运算。那我们的做法是把减数全部为按位取反，末位加一，那全部位按位取反就应该是零零一。一零对吧？末位再加一就应该是零零一一。好，

所以上面这个减法运算就等价于加上零零一一。那这次加法运算结果就是零零一一。那刚才提到的这个过程，减法转换成等价的加法，然后得到最终的运算结果呃，这些过程都是aru。在收到减法控制信号之后，他在背后默默完成的一些事情，总之最终这次减法运算的结果就是零零一一。好得到运算结果之后，我们需要把这四个比特写回到呃寄存器p当中，对吧？所以此时控制逻辑。会向寄存器p发出写矢能的控制信号，让寄存器p允许被写入。

于是刚才的计算结果。零零一一就可以顺利的写回到寄存器p当中。好，现在我们完成了一二这两个子步骤。接下来要进行第三个子步骤。我们把寄存器py辅助位视为一个整体。算术右移一位，那算术右移会用原本的符号位去补足空出来的这个高位。因此，高位补零。最后还需要把呃CN的值减一，那么CN的值就从三来到了二。那这样我们就完成了第二轮处理的所有的事情。接下来，来到了第三轮处理。

在本轮处理当中，用末尾的两个比特送到控制逻辑。由于这两个比特是一一的组合，所以本轮处理应该把寄存器p。加上零那么加零就是什么都不加。所以，控制逻辑既不需要发出加法信号，也不需要发出减法信号。所以第二个子步骤天然的什么都不用做。接下来进行第三个子步骤整体算术右移一位。算术右移，把最低位移出。空出来的高位，我们用原本的符号位去补充。原本符号位等于零，

因此高位应该补零。那这样，我们完成了第三步操作。最后，第四个子步骤我们需要把CN的值减一，那CN就从二来到了一。那接下来就可以开始第四轮，也就是最后一轮的处理。根据末尾的两个比特去判断，本轮应该进行的处理是让p+x。因此，控制逻辑需要向aru发出加法，控制信号。那么，当前寄存器p的值是全零。

让它加上被乘数x。显然，这个加法运算它的结果应该是一一零一。aru完成加法运算的同时，控制逻辑需要向寄存器p发出写矢能控制信号。让寄存器p允许被写入。好于是本轮加法运算的结果顺利的写到了寄存器里边。那截至目前，我们完成了一二两个小步骤。接下来进行第三步。整体算术右移，一位最低位丢弃而空出来的这个高位，我们需要用原本的符号位去进行填充。因此，本轮算术右移，

我们需要在高位补一。完成右移之后，还需要把计数器的值呃减一，那么计数器的值就从一来到了零。当计数器的值为零的时候，整个乘法运算结束，那可以看到目前寄存器p和寄存器y当中。保存的就是我们期待的最终结果。把这个巴比特二进制串，按照补码去解释，它应该对应负18这个真值，那负三×6=-18。结果正确。那可以看到，在乘法运算结束的时候，

我们是用2n个比特暂存了这个乘法的运算结果，也就是用寄存器p和寄存器y。把它们视为一个整体。那这个整体表示的乘法运算结果一定是正确的，然而在很多计算机架构当中。我们最终只会保留末尾的这n个比特，也就是只保留寄存器外，把它作为最终的沉积结果。那显然，我们把八比特的呃带符号整数强行截断为只有四个比特。那这个过程是不是就有可能发生啊？这个数值的错误啊？因此，在这个过程当中，就有可能发生溢出。

那对于带符号整数的乘法运算，计算机怎么判断它有没有发生溢出呢？好是这么做的。首先，两个恩比特的带负号整数相乘。我们用2n个比特暂存这个沉积的结果，2n个比特是一定能够存得下的，对吧？溢出发生在我们把2n个比特强行的只保留末尾的n个比特，这个过程就有可能发生溢出。好，那计算机的做法是对于这2n个比特来说，它会判断更高的n+1位。是否完全相同？如果更高的n+1位不完全相同说明会发生溢出。

而一旦检测发生溢出，就需要把of标志位置为一。好，这儿给了大家三个例子。第一个例子当中，负三×6=-18，那负18用2n个比特表示的话，应该是一一一。一零一一一零。如果最终我们只截取末尾的n个比特，也就是末尾的四个比特来作为乘积的话，那显然。四个比特的带符号整数补码可以表示的范围。只有负八到正七这个区间。因此，

显然只截取末尾的这四个比特是无法表示负18这个值的，那对于计算机硬件来说。他判断，呃，是否发生溢出的方法，就是看更高的这n+1个比特是否完全相同。那显然，这个例子当中，更高的n+1个比特不完全相同，对吧？因此，说明发生了溢出。好，再看第二个例子，用负三乘以负二，

结果等于负六。那么，我们从人类视角来看，最终如果只截取末尾的四个比特，那四个比特的补码只能表示负八到正七这个范围。那负六这个运算结果并没有超出这个范围，对吧？所以从人类视角来看，保存末尾的四个比特并不会导致这个运算结果的错误，也就是说不会发生溢出。那再来看计算机的视角，它检查更高的，这n+1个比特，由于这些比特完全相同。因此，

即便只保留末尾的n个比特作为结果，也不会发生溢出。好，最后一个例子。四×2=8。从人类视角来看，八这个值已经超出了四比特带符号整数补码所能表示的范围。所以从人类视角来看，这次运算如果只保留更低的n位作为结果的话，那肯定会发生溢出，对吧？好，再来看机器视角。计算机检查更高的n+1个比特，那由于这些比特不完全相同，

因此计算机可以判断。如果只保留末尾的n个比特，那么一定会发生溢出。计算机判断的结论和我们人脑判断的结论是一致的。好，那这三个例子可以辅助大家理解计算机判断溢出的呃逻辑。一旦检测到溢出，就需要把of标志位置为一。对了，这地方有必要解释一下，当我们在介绍无符号整数乘法运算的时候，我们说如果两个无符号整数乘法发生了溢出。那么CPU也会把of标志位置为一。有很多同学有这样的疑问，无负号整数的运算是否发生溢出，

不是使用CF标志位去记录的吗？为什么乘法运算使用的是of标志位？这个问题是很多同学会问到的。好来解释一下CF标志位通常是用于记录无符号整数的加法。减法运算是否发生溢出？而乘法运算是否发生溢出，无论是无负号整数还是带负号整数的乘法，我们一般都是用of标志位。去进行记录的当然在无符号整数乘法运算发生溢出的时候of标志位置为一的同时。CF标志位也会被置为一。但是程序员一般会用of标志位去判断无符号整数的乘法有没有发生溢出，而不会采用CF标志位。因此CF标志位一般来说只用于记录无符号整数加减法的溢出。而乘法运算通常都是用of标志位去记录的，好这个地方叉开一句解决大家的困惑。

接下来，我们回到带负号整数的溢出处理。现在我们知道了如何判断是否发生溢出。一旦发生溢出处理的方法和无符号整数是一样的，首先第一种方法就是直接摆烂，就是直接忽略这种溢出。只不过最终我们拿到的这个乘法运算结果呃，会是错误的，对吧？而如果不想忽略这个溢出的话，我们可以在乘法指令的后面。加入一条溢出字限指令。比如说叉八六架构下的into这个指令。这条指令的执行原理是这样的，它会检查of标志位是否为一，

如果of=1。那么说明发生了溢出，此时就会执行操作系统的异常处理程序来主动的处理乘法溢出。好，接下来我们看一道真题，一零年的13题。大家可以先暂停做一下。题目给了四个整数，用八位补码表示，那八位补码意味着它一定是带符号整数，对吧？r1，r2，r3，r4给了四个数。

都是用16进制表示的，我们可以把每一个数展开成八比特的形式题目，说这几个数进行运算啊，这个运算的结果。最终会存在一个八位的寄存器当中。问下列运算当中会发生溢出的是？好观察abcd几个选项。他们都是乘法运算，对吧？所以这道题目它考察的就是带符号整数乘法运算的溢出判断。八比特乘以八比特，最终我们用八个比特来保留乘积运算的结果。好，那经过之前的学习，这道题目应该信手拈来，

对吧？在考场上，你灵机一动，这道题目太简单了，我只需要画出一个呃巴比特乘巴比特的电路图。然后用这节课，我们呃模拟的这个过程分别去模拟abcd四次乘法运算的结果。每一个选项都会得到16比特的乘积结果。我们只需要根据这16比特当中的更高的呃，九个比特是否完全相同？是不是就可以判断这四次乘法有没有发生溢出，对吧？好，这个想法确实很可爱，但是如果你在考场上这么去做的话，

你的内心一定会很快就崩溃。你会发现，如果你用计算机的方法去计算，这四次乘法运算，那等你把这道题这道题只有两分哈，等你把这道题做完的时候呃，基本上考试也已经结束了。于是你撕毁了你的四零八卷子，开心的走出了考场。那显然，这个题目我们用计算机的方法去模拟乘法运算，肯定是不合理的，对吧？那有没有更快的解决方法呢？其实很简单，

我们可以用人类的视角去呃快速判断，我们知道巴比特巴比特的这个最终结果。它是用补码表示的嘛，那巴比特能表示的合法范围不就是负的128到正的127吗？那我们可以把r1r2。r3r4这四个数分别把它转换成十进制的形式，然后用十进制去算一下哪些数的相乘。会超出这个呃巴比特的合法范围，那显然绝对值更大的两个数r2r3，他们俩相乘的结果更有可能超出这个范围，对吧？那实际计算一下，也确实r2×r三，它的真值应该是等于1568已经远远超越。合法范围了，

所以r2r3这两个带负号整数的乘法运算必定会发生溢出，因此这个题目应该选择b选项。所以我们用人类的方法快速的得到了这道题的答案，那对于计算机来说。r2r3这两个八比特数呃丢到乘法器当中进行运算，那最终的运算结果会先用16个比特暂存。最终只会截取末尾的八个比特作为运算结果，因此有可能会发生溢出。那么，计算机判断溢出的方法是看更高的，这n+1个比特对于这个题目来说就是更高的九个比特。是否完全相同？由于它们不完全相同，说明发生了溢出。那这是计算机的判断方法，

和我们人类的这种判断方法得到的结论是完全一致的。好，这是一零年的13题。再来看一道题。一九年的45题，这是一道大题。这题目看起来还比较恐怖哈，他给了一个程序，然后给了这个程序对应的汇编语言指令呃以及机器及代码。我们来看一下这个题目的第五个小问。问，我们第19行。这个地方第19行的imul。这条指令，这条指令，

它是带符号整数的乘法运算，它的功能是把eax寄存器和ecx寄存器当中的呃两个数。相乘乘积的结果再写回到eax寄存器当中。好，这是一台32倍的计算机，那eax和ecx它们都是32比特的呃长度。也就是说，两个32比特的带符号整数相乘，乘积的结果又会保存在32比特的这个寄存器当中。问，我们当乘法器输出的高低呃32位的乘积之间满足什么条件的时候，溢出标志of=1？以及要使CPU在发生溢出时转向异常处理，那么编译器应该在这条呃带符号整数乘法指令后面。增加一条，

什么指令呢？好，这个题目其实就是我们刚才提到的这个东西，对吧？判断溢出的方法就是根据高n+1位是否完全相同去判断。那如果要处理这个溢出，那我们可以在惩罚指令的后面执行一条溢出自限指令。用这条指令去检查of标志位是否为一，如果of标志位等于一，那么就会转向操作系统的异常处理程序。好，那这就是一九年的45题。这题目考的还是很细很深的。前面这道题我们不知道，计算机判断溢出的方法，

用人类判断溢出的方法也可以拿到分数，对吧？但是一九年的这道题，我们就必须知道计算机硬件，它在底层是如何判断溢出的，以及在发生溢出的时候，我们一般要怎么去处理？要深入了解这些细节，才可以拿到分数。好的，那么这是一道小题和一道大题，带大家感受一下乘法运算这个部分它会怎么考？那最后这页课件给大家标注了32位的带负号整数乘法运算电路，它的结构长什么样？其实和前面那个四位的呃乘法运算电路没有任何区别，

只不过这些寄存器的位数，还有这个aru的位数呃都扩展为了32而已。使用这一套电路，就可以实现C语言当中两个32比特的带符号整数相乘。就可以实现这个逻辑。好的，在这个视频中，我们探讨了带负号整数的乘法运算是怎么实现的，以及在乘法运算结束之后，由于我们只会保留更低的n个比特作为。最终结果因此有可能会发生溢出，那么带符号整数的乘法溢出，判断它的逻辑和无符号整数的溢出判断是不一样的。这点大家可以对比学习。但是无论是带负号整数还是无负号整数，

一旦发生了乘法溢出，这个of标志位都会被置为一。那如果我们想要处理这种溢出的话，可以在惩罚指令的后面呃加入一条溢出自限指令。那值得注意的是，带符号整数的乘法电路相比于无符号整数的乘法电路来说，它在寄存器外的后面增加了一个辅助位。这个辅助位的初始值设为零，然后每一轮的处理当中，我们需要根据啊，寄存器y的最低位以及辅助位这两个比特去判断。本轮到底是加x还是减x还是加零？那么根据这两个比特的组合不同。我们在本轮的处理也会有一些区别，好的，

那以上就是这个小节的全部内容，那最后我们再补充一个小小的术语。啊，万一在考试当中出现了，你要知道它指的是什么？在这节课当中，我们介绍了补码的乘法运算如何实现，那如果忽略这个辅助位的话，我们每一轮的处理其实只处理了呃。乘数的一个比特。因此，我们可以把这节课介绍的方法称为补码的移位乘法。那由于这种方法是由一个叫做boost的科学家提出的。所以这种方法也被称为boos乘法。这个术语万一在题目当中出现了，

那你要知道它指的就是我们这节课介绍的补码乘法。好的，那以上就是这个视频的所有内容。
