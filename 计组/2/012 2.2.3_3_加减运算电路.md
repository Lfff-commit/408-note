各位同学大家好，在这个视频中，我们会探讨怎么用电路去实现补码的加减运算，在之前的学习中。我们知道了n比特加法器它的一个实现原理，给加法器输入n比特的被加数a以及n比特的加数b。同时，加法器还可以接收来自于更低位的进位信息。那呃，经过加法器的电路处理，就可以得到n比特的加和结果。当然，加法器也有可能向更高位产生一个敬畏信息，那忘了的同学可以结合这儿给出的两个例子来进行回忆。我们这给了AB两个数，

分别都是四比特。假设a等于一三个0b等于零三个一，那么当更低位的进位cin=0的时候。加法器的运算结果就应该是四个一同时往更高位的进位信息等于零。这是第一个例子。那第二个例子a和b的值不变，改变了来自于更低位的进位信息CN=1。那在这种情况下，呃a+b再加上来自于更低位的进位，一得到的加和结果就应该是四个零，同时往更高位产生了进位一。好，那这是两个简单的例子，可以帮大家回忆起加法器的原理。好，

接下来我们对这个加法器进行一些拓展，让它能够支持补码的加法和减法运算。那拓展之后，得到这样的一个电路，这个电路就可以支持补码的加法和减法运算。好来解释一下，首先这个地方有个很显眼的东西，它是多路选择器，它有两个输入，一个输出。那看一下多路选择器的这个控制信号叫sub。它是substruction的缩写，这是减法的英文单词，也就是说这个控制信号为零还是为一，表示当前要进行的是加法还是减法？

如果当前进行的是加法运算，那么sub这个控制信号就等于零，当这个信号等于零的时候，意味着右边这条线路。会通过多路选择器作为加法器右端的这个输入。那结合右边这个例子来看，假设被加数和加数都是四比特被加数等于三加数等于四。他们的补码分别是零零一一和零一零零，现在要计算补码x，加上补码y的值。那由于是加法运算，所以sub这个控制信号等于0y的值零一零零，通过这条线路。直接从多路选择器这边输出，所以加法器的右端，

它接收到的数据就是零一零零。而加法器的左边这个输入端接收到的就是x的值零零幺幺，那加法器会把这两个补码从低位到高位逐位相加，最终输出加和的结果。那这两个数加和的结果应该是零一一一，如果把这个补码翻译成十进制，那就应该是十进制的七。三+4=7得到了正确的结果。好，这是进行加法的一个例子。好，接下来看一下这个电路是怎么实现补码的减法运算的？那经过之前的学习，我们知道补码的减法运算。我们最终需要把它转变成等价的加法，

具体的做法就是要把减数y全部位按位取反，末位加一。经过这样的处理，就可以得到户外的补码。紧接着我们就可以把减法转变成与之等价的加法。好，所以补码的减法运算也可以用加法器来实现，关键在于我们怎么用电路把减数y全部位按位取反末位加一。好来看一下这个电路是怎么做的？如果当前进行的是减法运算，那么sub这个控制信号的值就是一。那对于多路选择器来说，控制信号等于一意味着左边这条线路会被选通，而右边这条线路会被拦截。那左边这条线路看一下，

它接的是一个非门，同时它的输入端接的是呃减数y。那对于这个例子来说，减数y的值应该是零一零零好，那由于非门的作用是零变一一。变零所以经过非门的处理之后，就可以实现减数y全部位按位取反的一个效果。那这些信号通过多路选择器之后，输入到加法器的右端一零一一，那这就是减数y全部为按位取反的一个值。好，接下来把目光放到右边这条线路，我们说加法器可以接收，来自于更低位的一个进位信息，那么这条线路观察一下，

它是连着sub这个信号。也就是说，如果当前进行的是减法运算，那么sub等于一一这个信号会通过这条线路输入到呃加法器这。好，那再来看加法器的左边直接接的是xx的值是零零一一输入到加法器的左边这一端。好，接下来这个加法器做的运算就是零零一一加上。一零一一再加上来自于更低位的进位信息，一那最上面这个数是x的补码。中间的这个数是减数y按位取反的一个值，那最后这个进位信息一是被加到了末位。所以这个效果就相当于末尾加了一个一。因此，当sub=1的时候，

这个电路就实现了减法运算。那这个减法运算的结果是四个一。把它翻译成十进制，就刚好是负一三减四等于负一，这是一个正确的结果。好的，那这就是实现补码加减运算的电路。在之前的学习中，我们说过无符号数的加减运算，它在底层的运算逻辑和补码的加减运算是一模一样的。无符号数的加法运算和补码的加法运算一样，只需要从低位到高位按位相加就可以，而两个无符号数相减的运算也和补码的减法运算。一模一样，我们需要把减数全部位按位取反末位加一把减法转变成等价的加法。

因此，刚才我们设计的这个补码加减运算电路，它也可以用于实现无符号数的加减运算。好，那在这个地方我也给了一个例子啊，用四比特表示无符号数八和七当两个数相加和相减的时候啊，分别等于什么样的一个值，大家可以结合这个电路图自己。推一下，那这就不再追述。好，那在刚才这个电路中，我们基于以前学过的简单的加法器，实现了加法和减法的呃运算逻辑。我们回顾一下在之前的学习当中，

我们基于简单的加法器进行了一些拓展，得到了一个带标志位的加法器。接下来，我们把这个带标志位的加法器使用到补码的加减运算电路当中，也就是说，这个加法器除了得到加和的结果以及产生往更高位的净位信息之外。还会输出ofsfzf和CF这样的四个标志位的信息，那我们来复习一下of是溢出标志用于判断带符号数加减运算是否溢出。当of=1的时候，溢出of=0的时候，没有溢出，那回顾一下之前我们讲到标志位的生成的时候，我们说of这个标志位的信息。它是用最高位产生的进位和次高位产生的进位进行异或运算来确定of的值。为什么这个值可以反映带符号数的加减运算是否发生了溢出呢？

那我们知道计算机内部所有的带符号整数都是用补码来表示的，对吧？那之前我们学习补码加减运算的时候，我们提出过这样的一种判断呃，溢出的方法。我们可以用次高位产生的进位和最高位产生的进位信息进行异或运算。如果这个异或的值等于一，那么就说明有溢出，如果异或的值等于零，说明没有溢出。这是判断补码加减运算是否溢出的一个呃方法，那再回到这个带标志位的加法器。of这个标志位，刚好就是用最高位产生的进位和次高位产生的进位进行异或运算来得到of的值。那不就是我们之前学习的这个逻辑吗？

当of=1的时候，表示这个带符号数的呃加减运算发生了溢出of=0的时候，表示没有溢出。好，那这是of这个标志位。好，接下来第二个标志位是sf符号标志，它反映了带符号数的加减运算到底为正还是为负sf=1的时候，为负sf=0的时候，为正。那回到之前的这个电路图，我们可以看到sf这个标志位，直接就是取了嗯，最高位的本位和。那我们知道带符号数是用补码来表示的，

而补码的呃，最高位肯定就是符号位当最高位等于一的时候表示这是一个负值当最高位等于零的时候表示这是一个正。好，那这就是sf这个标志位的一个由来以及原理。好，接下来第三个标志位zf，它表示加减运算的结果是否为零？那回顾之前这个电路图，可以看到zf这个标志位是把呃加法器输出的，这n比特的加和。每一位都进行或非运算。只有当n个比特全为零的时候zf或非的值才等于一，此时说明运算的结果等于零。那相反的，当zf=0的时候，

就说明加法器运算的结果是非零。好，这是zf标志位最后看CF标志位之前我们说过CF标志位表示的是无符号数的加减运算当中是否发生了进位或者借位？当两个无符号数进行加法运算的时候，如果最高位产生了进位，那说明此次的加法运算肯定发生了溢出，而两个无符号数进行减法运算的时候，如果最高位。向更高位产生了借位，那么说明减法运算发生了溢出，总之当CF=1的时候表示无符号数的加减运算发生溢出CF=0的时候表示没有发生溢出。好，那再来回顾一下CF标志位的一个逻辑，可以看到CF标志位是把加法器的呃，最高位的这个进位信息，

以及来自于更低位的这个进位信息。进行了易货。那这是什么原理呢？回顾一下之前我们说过，无论是无符号数的加法还是无符号数的减法，在判断溢出的时候，我们都需要关注最高位产生的进位信息。那对于加法器来说，就是要关注c out这个信号。对于加法运算，当c out=1的时候发生溢出。对于减法运算，当c out=0的时候，说明发生溢出。那回到CF这个标志位的生成逻辑，

它是把c out和cin这两个信号进行异或。那如果结合之前这个呃加减运算的电路图来看。cin这个信号，它和sub这个控制信号是相同的，如果此时进行的是加法，那么cin刚好就等于零，如果此时进行的是减法，那么cin就等于一。好，所以进行加法运算的时候cin这个信号肯定等于零，我们把cin和c out进行异或运算。那只有c out=1的时候，这个异或运算的值才等于一。对于减法运算也一样进行减法的时候cin肯定等于一，那cin和c out进行异或运算。

只有c out=0的时候，这个运算的结果才等于一。好，所以CF这个标志位，它反映了无符号数的加法和减法是否发生了溢出？CF=1的时候发生了溢出？CF=0的时候没有发生溢出。好，那这就是CF这个标志位的一个呃，实现原理，大家可以再结合之前的这个电路图来回顾一下。好的，在这个视频中，我们学习了如何用电路去实现补码的加减运算，那补码的加减运算电路呃，

既可以支持带符号数补码的加减法，也可以支持。无符号数的加减法。对于硬件来说，实现的原理是一样的，那除了理解这个电路的原理之外，大家还需要重新去理解和体会ofsfzf和CF。这几个标志位的一个含义，以及各个标志位，它是怎么确定怎么生成的？好的，那以上就是这些视频的全部内容。
