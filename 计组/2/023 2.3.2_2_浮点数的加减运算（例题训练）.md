好，继续学习浮点数的加减运算。接下来看一个实例，我们会看到浮点数的加法是如何实现的。浮点数的加减运算通常会基于float型呃来进行考察，因为double型和float型它的原理完全一致。所以如果作为考题的话，其实float型完全能够考察出啊，大家对浮点数加减运算原理的理解了。就没有必要为了复杂而复杂故意的去考大模型，对吧？那在历年真题当中也体现了这个特点。所以接下来的加减运算，我们主要基于float型啊来进行探讨好，首先看这个例子有xy两个float型的浮点数符合呃七五四标准。

它们的十进制真值分别是一点五和零点五，然后让我们分析呃x+y=z这个过程。好，首先我们把这两个数转换成float型二进制的形式，大家可以先暂停练练手。那转换的结果应该是这样的。float型由一比特的符号位，八比特的阶码和23比特的尾数组成。其中，接码这个部分的偏置值是127。那前面这个部分呃，我是用二进制科学计数法来记录的，我们只要先写出二进制科学计数法，然后就可以推出float型的二进制表示。那数值转换这个部分之前已经练过很多，

这儿就不再赘述。好，接下来我们把符号位和尾数啊，单独的提取出来。我们知道规格化的呃，这个七五四浮点数，它在尾数的小数点前面还隐含了一个有效位一对吧？所以为了让接下来这个尾数的加减运算呃，保持正确性，我们把这个小数点前面隐含的一给补上。那另外y这个数同样的在小数点前面补一个一，与此同时，前面的这个符号位是正还是负？我们也把它给写上。因为加减运算，

我们是需要考虑符号位的好，那这样我们就得到了呃，加数被加数的尾数的原码表示。呃，包括隐含位和符号位。好，接下来正式开始加法运算，那我们说需要经过五个步骤，分别是对接尾数加减。尾数规格化，尾数的舍入和溢出判断，这样的五个步骤首先第一个步骤对接。我们说要让小阶向大阶靠齐，那计算机是怎么判断两个数的阶数谁大谁小的呢？很简单。

只需要把两个数的阶码部分相减就行了。那x的解码减掉y的解码呃等于一说明x的解码比y的解码更大一。从左边这个科学计数法，这我们也可以很直观的看到这一点x的解码，更大y的解码更小，并且它们的差值是一。好，那么既然y的阶码更小，那我们让小阶向大阶靠齐，就得让y的尾数向右移一位。与此同时，让接码加一那这样就可以让二者的接码对齐。好，所以x的尾数这个部分保持不变y的尾数要右移一位。就得到了这样的一个值。

小数点固定不变，尾数整体右移，那么右移多出来的这一位，我们先把它保留下来。与此同时，更高的这一位高位空出来的这一位，我们补零。符号位保持不变好，那这个地方值得一提的是I triple e七五四的标准，它会规定哈，就是在这个尾数右移的过程当中。右移移出的这个比特，我们至少要额外的保留三位。那现在我们只移出了一位，对吧？

所以这个比特我们要把它保留下来。思考一下，保留下来有什么作用？可以提高运算的精度嘛，对吧？像这个例子当中，我们移出的这一位是零，这个零保留或者不保留其实无所谓。但是如果说最后的这一比特原本是一移出的，是一个比特的一，那么这个一我们把它先保留下来。参与接下来的尾数加减运算，这就会呃比较有意义，可以使得加减运算的精度更高，对吧？

好，所以I triple e有这样的规定，就是右移一。提出的比特至少要额外的保留三位。好那么y的尾数右移一位之后，相当于x和y的解码就对齐了，解码都统一为了零这个值。小接向大接看齐好，那么接码对齐之后，接下来就可以进行尾数的加减运算。那由于我们要实现的是加法，所以接下来只需要让这两个尾数进行相加，那我们说两个浮点数，它们的尾数是用原码的规则去呃表示的。计算机硬件实现源码的加减运算，

本质上是把源码先转换成补码。用补码实现了加减之后再转回原码，这是计算机硬件实现原码加减运算的原理。好，那么我们用手算的方式来计算，观察两个数都是正值，那我们不妨把呃每一个比特对应的比特位全部相加，最后符号保持为正就行了，对吧？那后面的这些比特全部是零，全部是零，那这两个比特一+1=0往更高位进一个一对吧？那么小数点前面一+0再加一=0往更高位进一个一，所以两个数相加的结果就是正的一零点零零零，后面全部是零。

得到这样的一个加法，结果好，这是我们手算得到的结果，计算机机算得到的结果和我们手算的结果是完全一致的。如果不信的话，大家可以自己试一下啊，把这两个源码先转换成补码，就是把符号位变成零。然后用补码相加的规则看一下，最后得到的结果是不是和我们一样OK？那到这一步，我们实现了两个尾数的。相加那现在得到的这个尾数，显然它并不符合规格化的规定，对吧？

规格化的尾数必须是第一个有效数值为。让它在小数点的前面一个位置，那显然对于这个尾数来说，我们需要对它进行右归。于是来到了第三步，让小数点固定不变，尾数向右移。一位那尾数右移，一位就得到。这样的一个值正的一点零零零，那尾数每又归一位相应的，这个阶码就应该加一。那由于前面对接的这个步骤，我们已经让小解码向大解码看齐了，二者的解码此时都是统一为零，

对吧？和大解码保持一致，那么在这个步骤尾数右移一位的时候，我们就需要把原本已经统一的这个大解码。让它再加一，于是可以看到更大的这个阶码，加一得到一零零零。好，那这样我们就完成了规格化，接下来第四个步骤尾数的舍入处理。多余的尾数位，我已经用这种灰色字体下划线给它呃标注出来了，那在二进制浮点数的舍入这个动作，这呃我们通常采用的是零舍一入。可以简单的记为零舍一入的呃，

这样的一个原则。就是要观察被舍弃的这个更高位，它如果是零，那么就直接舍掉就行了，如果是一，那么就要考虑往更高位入一个一。那在这个例子当中，我们不需要往更高位进一，所以直接保留下24比特的有效尾数位。这样就完成了舍入的动作好，那到这一步我们就得到了浮点数的每一个呃部分。首先，尾数加减之后得到的运算结果为正，所以最后的运算结果符号位应该等于零。好，

另外尾数这个部分我们保留了一+23比特的有效尾数位，那小数点前面的这个一。可以隐含的表示，小数点后面的这23比特呃需要完整的保存到float型变量当中。所以你会看到23比特的零，那与此同时接码这个部分，刚才我们进行尾数的规格化之后，接码的值也进行了相应的调整，对吧？尾数右移，一位接码加了一，那么接码最终的值就会变成一个一，后面七个零。那这就是x+y的呃，运算结果大家可以先暂停把这个float形数转换成十进制。

看一下是不是等于二点零这个值？好，那大家可以先暂停转换一下这个数值好，那么得到这个运算结果之后，对于计算机硬件而言。它还需要进行溢出判断，看一下刚才这个加法或者减法运算有没有超出float型，可以表示的范围。通过之前的学习，我们知道规格化的浮点数，它的阶码呃不全为零，也不全为一，对吧？如果解码权为一，那么整个浮点数的值可能表示的就是正无穷或者负无穷，

那此时说明发生了上亿。而如果阶码全为零，阶码全为零，那么就说明已经超出了规格化浮点数。可以表示的范围。此时，这个浮点数就会下移到这个非规格化浮点数的区间。所以，计算机判断是否发生溢出，主要就是看运算结果的这个解码部分，如果不全为一也不全为零，那么就说明既没有发生上移，也没有发生下移。那对于这个例子而言，显然没有发生溢出，

对吧？好，那么这个二进制浮点数把它转换成十进制真值，对应的就是二。和我们期待的运算结果是完全一致的。好，大家可以再结合这些课件感受一下这五个步骤分别做了什么？好，接下来再看一个例子，浮点数的减法。给出了xy的真值分析x-y的过程，同样分为一二三四五这样的五个步骤。好第一步，首先大家暂停把这两个数转换成float型的二进制形式，三秒钟之后公布答案。

三二一。转换的结果应该是这样的，其中左边这个数我是用二进制科学计数法来表示的好，接下来我们把两个数的尾数给提取出来。注意23比特的尾数呃，前面应该隐含一个一对吧？与此同时，最高位这个比特表示的是呃尾数的正负性，所以我们用正号去表示。那这样我们得到了两个数的尾数源码，接下来正式开始减法运算。首先第一步同样的是对接，我们要让小接向大接靠齐。那我们说过，计算机判断阶数大小呃方法很简单，

把二者的阶码相减。x的解码减y的解码等于负二，大家可以计算一下啊，这两串这两个数把它进行相减等于负二。那说明x的接码要更小，对吧？并且接码的差值是二，于是我们需要把。解码更小的这个呃数x，它的尾数右移两位，这样就可以对齐解码好，那么x的尾数右移两位之后得到这样的一个值。右移多出来的，这两个比特，我们把它保留，

因为我们说过I trip e规定右移移出的这些比特至少要额外的保留三位。那现在只移出了两位，对吧？这两位我们都把它保留下来。与此同时，右移空出的高两个比特补零就行了，符号位保持不变，好y的尾数保持不变。那经过这样的操作x和y的解码，就对齐到了负一这个值。好解码对齐之后就可以进行尾数的加或者减，那这道题当中我们需要用x的尾数减掉y的尾数。那对于计算机而言，这两个尾数源码的减法运算，计算机会先把源码转补码补码加减之后再转回源码。

那我们可以模仿一下计算机的做法呃，这画一条横线，右边的这些位全部都是零，所以我们就不模拟了哈，我们只考虑前边的这些比特。把符号位正号变成零，那么x的补码就是零零点零一。外的补码就是零一点零零最高的，这个比特是符号位。x要减掉y。那对于计算机硬件而言，这个补码的减法运算就是把减数全部为按位取反，末位加一，那全部为按位取反呃，得到的就是。

一零。点一一对吧？末尾再加一个一末尾再加一，那这样的话，减法就可以变成加法。好，那最后的这三个比特一+1，再加一本位留一往更高位，进一个一零+1，再加一本位留零往更高位，进一个一。零+0再加一等于一零+1=1好，那这样的话就得到了x-y的值。但是现在这个得到的结果是补码表示，对吧？

我们还需要把补码转换成源码。负数的补码转圆码很简单呃，这个符号位一保持不变，然后呃从右往左找到第一个一这个一的右半部分保持不变，左半部分零变一。一变零，所以就变成了一零点一一，对吧？那最高位符号位等于一表示它是一个负的。所以x-y的运算结果就应该是负的，零点一一后面全部都是零。这个是源码哈，我们已经把补码转换成了源码好，那这是尾数相减得到的一个运算结果。没跟上的同学可以暂停动手算一下，

总之通过这个例子，我们就能感受到计算机硬件做这种源码的减法。本质上就是把源码先转成补码，加减之后先得到补码的运算结果，然后再把补码转换回原码。好，那么目前得到的这个尾数显然没有规格化，对吧？规格化的尾数的特点就是小数点的前面有且只有一个一。那对于现在这个尾数，我们应该进行左归小数点固定，不变尾数整体左移一位。那左归一位得到的就是负的一点一，后面全部都是零。好那尾数左归一位，

与此同时，解码就应该减一。之前x和y的解码已经对齐到了负一，这个值对吧？也就是对齐到了更大的这个解码，所以这儿的解码减一就是在原本的这个大解码的基础上。进行减一的动作好，那原本的大解码减一就等于这样的一个值。这种简单的减法运算，我们就不再赘述。好，那这样我们就完成了尾数的规格化，注意尾数的规格化这一步呃，在对尾数进行左移或者右移的同时，呃，

接码的值也要相应的减。或者加。好，接下来到了第四步，尾数的舍入，我们只保留一+23比特的有效尾数。那多余的这些比特就把它舍弃，采用零舍一入的原则，那由于舍弃的这些都是零，那直接把它干掉就行了，不需要往更高位进一。那最终尾数就留下了这样的24比特。好，最后我们把运算的结果拼接起来，由于运算结果是一个负值，

所以符号位是一。而运算结果的解码应该是这样的一个值，那尾数部分我们把小数点前的这一个比特隐含。只把更低的23比特呃保存到float型变量当中，那这样就得到了运算的结果。大家可以暂停尝试一下，把这个二进制串转换成十进制，真值等于多少？三秒钟之后公布答案。那结果应该是对应负的零点三七五和我们这期待的x-y的结果是能够对应上的。好，那这是减法运算的例子。接下来再来训练一个例子呃，希望大家可以先暂停自己把一二三四五这几个步骤先完整的呃梳理一遍。好，

那十秒钟之后，我再公布答案。愣着干嘛？动手呀。哈哈哈。好，先把xy两个数转换成float型。先转换成二进制科学计数法，再把科学计数法转换成float型。这是第一步，这步就搞错的同学，请把不愧是我打在公屏上，好，接下来我们把两个数的尾数提取出来。注意小数点的前面隐含了一个一把它补上。

与此同时，符号位也把它补上。好，接下来开始对接。从人类视角可以很简单的看出来x的接码要比y的接码小三对吧？所以小接向大接看齐，我们就需要把x的尾数。右移三位，我们没必要把这个二进制的解码代进去做减法运算，这是计算机做的事情。我们是人不是人机，所以直接从人类视角就是二进制科学计数法的这个视角去看就行了。二者的差值是三，那么我们把x的尾数右移三位，你看x的尾数右移三位，

那多出来的这三位先把它保留下来。更高的三位补零。好y的尾数保持不变。好，现在解码对齐了，下一步要进行尾数的减法，对吧？两个二进制的源码。进行减法运算怎么减？计算机的做法是把源码转补码，然后用补码完成加减运算，最后再转回源码好，那么这两个源码的减法运算结果。x-y应该等于负的。一点哒哒哒得到这样的一个值做错的同学，

抽自己一鞭子啊。好那么减法运算得到的这个尾数，天然的就已经规格化了，对吧？所以接下来第三步规格化的这个动作，我们就可以直接跳过。尾数既不需要左移，也不需要右移，所以阶码呃也可以保持不变。就是保持为原本更大的这个解码就行了，保持不变好，接下来第四步尾数的舍入。尾数我们只保留有效的一+23比特，对吧？那更多余的这三个比特就全部把它舍弃，

那么采用零舍一入的原则，由于舍弃的最高位等于零。所以不需要向更高位进一，于是保留这样的一个尾数。总共是一+23比特好，最后就得到了运算结果，运算结果为负，负号位等于一。呃，接码是这样的一个值。好，最后尾数23比特，我们把小数点前的这个一隐含小数点后面的23比特保留下来。那这样就得到了我们的float型。如果把它转换成十进制真值，

那么对应的应该是负的幺幺零点幺二五。那大家可以用计算器算一下x-y的值，刚好就是这样的一个值。好的，那这是浮点数的减法运算。其实理解这个部分最好的方式就是自己动手呃，算几个例子。
