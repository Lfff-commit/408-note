好，继续来看七五四浮点数的表示范围和几种特殊状态。之前我们已经分析出规格化浮点数，可以表示的最小的正数以及绝对值最小的负数分别等于多少。那如果说浮点数运算的结果落在了这两个区间，我们就说此时发生了正下移或者负下移。那正下移负下移，统称为下移。英文术语对应on the floor。那么，当下意发生的时候。七五四规定需要这么去处理，首先如果一个数值太小，这个规格化的浮点数已经无法表示。那么，

在规格化浮点数的呃下面，就是更小的这个值，我们可以用非规格化浮点数去表示。而非规格化浮点数，它的表示范围也是有限的，如果这个值还要更小，真值非常逼近于零，那么此时这个运算的结果会直接把它存储为。增值零。好，那接下来我们先介绍更简单的，这个真值零应该怎么用浮点数去表示，然后我们再介绍非规格化的浮点数呃，它的格式是什么样的？七五四标准规定真值零有两种表示状态，

分别是正零和负零。那逻辑上，正零和负零它们是等价的。首先，真值零的阶码是全零，尾数也全零，那正零和负零的区别仅在于符号位到底是零还是一？好，所以float型的正零应该是呃32比特全为零，而负零应该是呃符号位为一，剩下的阶码尾数全为零。那这是真值零的表示。那double型的真值零啊，规则也是一样的，编码尾数必须全为零。

好，接下来再看呃，非规格化的浮点数，怎么表示它的特点是阶码全为零？而尾数不全为零。此时，我们应该按照非规格化浮点数的这种呃规则去解读它。好比如这个地方，我给了一个非规格化的正数。好，那么我们在解读它的真值的时候，需要注意在这个尾数的小数点前面。我们默认隐含的是零而不是一。好，所以尾数部分这串尾数应该是零点零一。

这是对尾数的解读。然后对解码的解读，这个解码全零的状态，我们固定的把它解读为负的126。注意啊，我们不能按照规格化浮点数的那种方式去解读解码，假如我们现在错误的用规格化浮点数的这种方式去解读的话，会发生什么呢？先把这个阶码按照无符号整数去解读，那当然对应的就是无符号数零那零再减掉这个偏置值127。最终，我们会算得呃真值等于负的127。但是这个地方我们并不是按负127去呃去解读这个解码的真值，而是负的126。所以，

规格化浮点数的这种解码的解读方法，在这个地方是不适用的。好，那么这是非规格化浮点数的尾数和阶码的解读。接下来，我们尝试分析非规格化浮点数所能表示的最小的正值。好来看一下非规格化浮点数，它的这个呃解码部分已经固定了。肯定是全零，它的真值对应的就是负的126，然后为了让它的这个绝对值尽可能的小，那我们应该让这个尾数部分。它的值也尽可能的小，对吧？尾数部分可不可以取23比特全零呢？

不可以，因为如果解码全零尾数也全零的话，那这个值就退回到零了。所以，非规格化浮点数的尾数，它的最小值只可能取到呃，前面全部是零，最后一个比特是一。那么，这个尾数的小数点前面补一个零用二进制科学计数法来表示，就应该是尾数乘以二的负126次方。那这个数值化简之后，就应该等于二的负149次方好，所以这就是非规格化的浮点数所能表示的最小的一个正数。好，

接下来再探讨绝对值最大的非规格化帧数。那解码全零已经确定了，要让这个非规格化正数的绝对值尽可能的大，那我们就需要让尾数。尽可能的大。那23比特权一肯定就是最理想的一个状态，那尾数的小数点前面补一个零，然后再乘以二的负126次方。这个值就是最大的非规格化正数。把它进行化简，应该是等于二的负126次方减掉二的负149次方。那化简的问题就不再赘述。这个数值反映的是什么呢？反映的是这条线对吧？非规格化正数所能表示的最大值，

你看规格化的浮点数。可以表示的最小值只能到二的负126次方，如果要比这个值更小，那么就落在了非规格化正数的这个区间。那刚才我们说非规格化正数的呃，这个这个区间的右侧右侧的这个边界应该是二的负126次方减掉二的负149次方。这些数值边界都是重要的命题点。好，那刚才我们分析的是非规格化正数，那对于非规格化负数来说，分析的方法一模一样。无非就是把这个符号位从零变成一而已。那这个数值的边界，同样的只需要在前面把正号变成负号即可，那没跟上的同学也可以暂停自己分析一下。

那么，回到我们之前探讨的下溢的问题。现在我们已经知道了非规格化浮点数，可以表示的范围也是有限的。如果浮点数的运算结果已经超出了非规格化浮点数所能表示的范围，那我们就会按照机器零去啊存储。而一旦这个呃运算结果下移到了机器零这个区间，那此时浮点数的运算部件floating point unit。就会把浮点数下移异常这个标志位呃，在叉八六里边就是u异这个标志位。给它置为一。注意啊，只有下移到机器零这个区间才算是下移异常，而如果下移到非规格化呃浮点数这个区间，它是不会引发下移异常的。

那七五四标准规定呃，默认不响应，下一异常，也就是不会终止程序的运行，除非程序员手动的去呃处理这种异常。好，接下来看一个真题，二三年的14题，大家先暂停自己动手，三秒钟后公布答案。三二一这个题目选择a选项。好来看一下怎么处理啊？首先题目给了float型变量的机器数，那我们肯定先要把这个机器数16进制展开成二进制嘛。紧接着，

按照float型的这个结构，就是一个符号位八个阶码。以及23个尾数。全部按结构拆分，那由于阶码是全零尾数不全为零。这就表示，我们需要按照非规格化浮点数的规则去解读这个数。那尾数部分，我们需要在小数点的前面补零，而解码部分固定的把它解读为负的126。再结合符号位一表示负数，那么把这个数值进行化简，就应该是这样的一个值，也就是a选项。那我们利用这道题引出最后一种浮点数的特殊状态，

也就是非数nan。nan是not a number的缩写。表示运算结果不是一个实数。那这种特殊的状态，它的特征是呃阶码全为一，而尾数不全为零。好，举个例子。float型符号位零或者一都无所谓啊，接码全为一，然后尾数部分不全为零。此时表示，这不是一个数not a number。什么情况下可能会出现这样的运算结果呢？比如说我们用零÷0，

那么运算的结果就是nan。或者比如你用负数开根号，那么运算的结果也会被记为nan。以及如果用无穷减无穷的话，结果也是nan。那开根号这个函数呃，它是包含在max点h这个头文件里面的。大家可以去试一下啊，那这个地方值得一提的是I trip e七五四规定啊，如果用一个非零数值除以零的话。运算的结果是无穷，而不是nan。比如说用一点零÷0，那大家可以去试一下它的运算结果是正无穷而负的一点零÷0运算结果是负无穷。如果出题老师想考的变态一些，

那我觉得这是一个挺不错的，很变态的一个出题点啊。问你非零数值，除以零它的运算结果是什么？或者零÷0它的运算结果是什么？那这也给大家准备了两个程序的示例。有兴趣的同学可以看一下左边这个程序，就是用零÷0。运算结果用float型变量result去记录，然后这句。我们打印出了运算的结果，那可以看到打印出来的结果就是nan对吧？而右边这个例子，我们是用一点零÷0。同样的结果，

用result变量去保存。那打印出来的呃，结果就是inf infinity。意味着这个运算的结果是无穷。你可以用result呃大于零还是小于零去判断。它到底是正无穷还是负无穷？有兴趣的可以试一下。好，然后左边这个程序啊，这有个if判断if is nan这个函数被包含在m点h啊，这个头文件里边呃，就是用于判断。这个float型变量，它是不是nan？那类似的，

如果要判断一个float型变量是不是无穷的话，就是用啊is infinity用这个函数去判断。如果是无穷，那么就会返回true，如果不是无穷，就会返回FALSE。那从这两个例子当中可以看到，I triple e七五四对于浮点数的这种啊，这种设计可以使得浮点数的这个运算过程容错性非常的高。哪怕是一÷0或者零÷0，这种很变态的情况都可以让这个程序继续往下执行，而不会被中断。当然，程序员也可以手动的去处理这种呃异常的情况。好的，

那么在这一小节中，我们介绍了七五四浮点数的表示范围。七五四标准规定规格化浮点数，它的特征是解码不全为零，也不全为一。那对于规格化浮点数，我们解读解码真值的时候，就是先按照无符号数去解读这个解码，然后再减掉偏置值。而规格化浮点数的尾数呃，我们解读的时候需要在小数点前面就是呃，补上一个隐含的一。那相比之下，非规格化浮点数它的特点是阶码全为零，而尾数不全为零。

非规格化浮点数的解码真值解读方法不一样，应该用一减掉偏置值。对于float型来说，呃，非规格化浮点数的这个呃解码的真值就固定的解读为一。减掉127，也就是等于负的126，而double型的这个非规格化浮点数，它的解码真值就固定的解读为一减掉一零二三。也就是等于负的一零二二。好，另外非规格化浮点数的尾数解读方法也不一样，我们在小数点前面呃要补上一个隐含的零，而不是一。非规格化浮点数可以用于解决啊，

规格化浮点数的下一问题。如果浮点数的运算结果落在了非规格化区间，那么首先会尝试着用非规格化浮点数去存储运算结果。而如果运算结果实在是太小。已经超出了非规格化浮点数可以表示的范围，那此时就会用机器零去存储。那I triple e七五四规定了机器零或者说真值零有两种状态，分别是正零和负零。正零和负零唯一的区别就是符号位到底是零还是一？好，那正零负零以及非规格化浮点数，它们的共同特点是阶码等于全零。非规格化浮点数的尾数部分不全为零。那如果解码权为一，而尾数全为零的话，

我们可以表示正无穷和负无穷。如果解码权一尾数不全为一，那表示这是一个呃无定义的数not a number。那么，在这个部分呃，无论是过去还是未来，一个可预测的命题点就是这个题目可能会考察你呃这个数轴上面的各个数值的边界，比如说规格化浮点数的最大值。最小值或者非规格化浮点数的，最大值，最小值。以及它可能会考你真值零，怎么表示正无穷负无穷？怎么表示？这种命题角度过去很常考，

未来肯定也会很常考。所以大家要能够自己去分析。好的，那大家可以再结合这一页课件回顾一下。最后给大家留个练习，请大家动手推演一下呃double型变量的这种表示范围。包括规格化的双精度，浮点型可以表示的最大值，最小值以及非规格化的double型。可以表示的最大值最小值。当然还有复数的，这个区间也全部推演一遍。那请暂停动手，三秒钟之后给出答案三二一。大家核对一下，

有没有做对？好，另外也请大家写出double型的正零负零呃，正无穷负无穷以及nan。分别长什么样子？三秒钟之后给出答案三二。一是这样的。好，以上就是这个小节的全部内容，把这个数轴的各个数值边界搞明白了，以及把各种呃特殊情况搞明白了。那这个部分的考题绝对难不住你。好的，那大家辛苦了。
