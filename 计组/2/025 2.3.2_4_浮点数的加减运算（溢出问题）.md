好的，继续来看浮点数加减运算的最后一个问题就是溢出判断，我们说进行浮点数加减的时候需要进行五个步骤。第一个步骤叫对接原则是小接向大接看齐，第二个步骤是尾数加减。第三个步骤尾数的规格化可能需要进行左归或者右归。确保尾数是规格化的，那所谓规格化的尾数就是一点叉叉叉，这样的一个形态，那第四个步骤是舍入。把多余的尾数位给舍弃，那最后第五个步骤就是溢出判断了，在这节课我们主要探讨的就是第五个问题。好，首先给大家写了一个程序，

在这个程序当中可以看到加减运算发生上亿的情况。在这儿定义了一个呃变量x这一长串数字，它的值就是二的127次方。下面y的值对应的是二的127+2的126次方，紧接着z=x+y。最后打印出xyz的值，那可以看到z的运算结果是infinity，也就是无穷。如果我们监测xyz的这个二进制表示，那么可以看到xyz分别是这样的值。重点关注z它的解码部分是巴比特权一。以及尾数部分是23比特的全零。另外，它的符号位等于零。那之前我们学过单精度浮点数，

如果符号为零，解码全一，而尾数全零的话，表示这是一个正无穷大。好，所以在这个例子中x+y的值超出了单精度浮点数，可以表示的最大范围，因此发生了上亿。运算结果记录为正无穷。那我们快速回顾一下上亿的概念，我们说规格化的浮点数可以表示的范围是有限的。如果说超出了正的规格化浮点数，可以表示的最大范围，那此时我们称发生了正上移。另外，

如果负值太大，那么就会发生负上移正上移的运算结果，通常会记为正无穷负上移的运算结果，通常会记为负无穷。那正上一和负上一统，称为上一，有时也会简单的翻译为一出。发生溢出的时候，运算结果会设置为正无穷或者负无穷。与此同时，浮点数溢出异常这个标志位。会被置为一，但是I triple e七五四规定，默认情况下，我们不响应浮点数溢出异常。

发生这种异常的时候不中断程序。除非程序员手动的响应，这个溢出异常。好，接下来我们从二进制的视角分析一下x+y呃，它的背后发生了什么？大家先暂停自己动手算一遍。三秒钟后公布答案。三二一好，那首先把x和y的二进制给写出来。它们的解码部分都是呃七个一一个零二者的解码相等尾数部分x是呃23比特全0y是一个一+22比特的全零。符号位都为正好，接下来把符号位和尾数呃补全。在小数点的前面，隐含了一个一把这个一给补上好，

接下来正式开始加法运算。第一步，对接。用x和y的阶码相减等于零，说明二者的阶码相等，所以无需对接。于是我们可以把x和y的尾数直接进行呃加减运算，那在这个题目当中，我们是要进行加法运算。把这两个尾数相加，那么相加的结果就是这样的一串正的幺零点幺零零零。好，现在对于这种情况，显然我们需要进行右归，对吧？

向右移动移位。完成尾数的规格化，那右归的同时需要把阶码加一。原本这个解码值是七个一一个零，再加一之后运算结果就等于八比特权一。那之前我们说过，对于单精度浮点数来说。规格化浮点数的解码的合法范围是呃零零零一。一直到一一一零。这是规格化浮点数的阶码的范围，但现在又归之后，阶码来到了八比特权一这个值。那么，计算机硬件检测到这种情况的时候，它就可以知道此时发生了溢出。

具体来说，是发生了正上移，因为这个尾数的呃运算结果是正的，对吧？与此同时，阶码是八比特权一，所以发生了正上移。那在这种情况下，这个加法运算的呃结果。就是尾数部分会被强行的置为23比特的全零。我们知道，在没有发生上亿的时候，尾数舍入的这个环节，通常来说，我们是把小数点后面。

的23比特保留下来，对吧？多余的比特位把它砍掉，采用零舍一入，然后幺零零特殊处理，一般来说是采用这样的原则，但是一旦发生了这种接码的上亿，那么这个。尾数的运算结果，我们会强行的把它置为全零。你看这儿原本有一个一对吧，现在被我们置为了全零。那符号位为零，阶码为全一，而尾数又是全零，

这个浮点数在七五四标准当中，对应的就是正无穷这样的一个真值。那我们可以得到这样的结论，这个结论也是选择题或者简答题当中比较呃常考的一个点。浮点数的溢出并不是用尾数的溢出来判断的，比如说x和y的尾数相加之后得到的这个新的尾数，你看。最高位这已经溢出了，对吧？但是这种尾数的溢出，我们可以通过右归，通过右归来进行纠正。尾数每又归一位解码，相应的就要加一。那什么时候浮点数真正的发生了溢出呢？

就是指数这个部分，也就是解码这个部分发生了上亿，变成了权益。所以浮点数的上亿是通过指数是否上亿，是否来到了八比特权一来进行判断的。一旦发生了上亿，那么就把这个运算结果记为无穷。好，那么这是第一个例子，我们可以看到上亿是怎么发生的，以及计算机硬件是怎么去检测上亿的？接下来第二个例子，我们会看到加减运算发生下溢的情况，同样给大家写了一段程序。在这段程序当中，

调用了这样的一个函数呃，大家如果有兴趣自己去写一下的话，这个函数被包含在了max点h这个头文件当中。所以大家需要include max点h。那这个函数它计算的就是呃一点零呃乘以二的负126次方。那右边这个呢，就是一点零×2的负149次方。所以在我这x的值就是二的负126，加上二的负149，那么y的值呢？它是等于负的二的负126次方。那z的值我是要把x和y相加，相加之后真值应该是等于二的负149次方。那由于这个小数非常小哈，所以在打印呃运算结果的时候，

我在这个地方小数点后面打印了60位。这样我们就可以看出xyz的值用，我们熟悉的十进制的表示。那么，如果监控xyz的这个二进制形式，我们可以看到z的二进制表示是零。八比特全零，然后零零零最后有个一。阶码全零而尾数不全为零。这在浮点数的标准当中表示，这是一个非规格化的浮点数，那由于符号为正，所以这是非规格化的正数。此时，这个浮点数的真值应该解读为二的负126次方乘以零点f。

这个f就是我们的23比特的尾数。那大家可以自己验证一下，这个真值对应的就是二的负149次方。好，我们回顾一下夏邑是怎么回事？我们说规格化的浮点数可以表示的范围是有限的，如果一个浮点数，它的绝对值很小。已经低于规格化浮点数可以表示的最小范围，那么呃，假如它是正数的话，此时就发生了正下移。而如果是负数，那么就发生负下移。发生下移的时候，

如果可以用非规格化的浮点数表示，那么这个运算结果就会用非规格化的浮点数。而如果非规格化浮点数，还不能表示还要更小，那么此时就会被当做机器零来处理。那按epe规定发生这种浮点数下一异常的时候呃，通常来说也不中断程序。不会特意的去处理这种异常，除非程序员手动的去响应这种下一异常。好，接下来我们从二进制的呃视角看一下x+y这个过程发生了什么？大家先暂停自己练习一下，三秒钟后公布答案。三二。一好应该是这样的，

首先把x和y的二进制给写出来。编码都是呃七个零一个一。尾数部分x这是22比特的零，最后有一个1y的尾数部分是23比特全零。好，那我们把这个尾数和符号位给它呃写出来。并且补上小数点，前面隐含的这个一。接下来进行第一步对接，由于二者的解码相减等于零，说明解码相等无需对接。于是我们可以直接对x和y的这个尾数进行加法运算。那y的尾数是一个负值。所以x+y相当于x的尾数减掉y的，这个尾数的绝对值。

我们用手算的方式可以逐位相减，最后一个比特一减零等于一前面这些比特都是零减零等于零，对吧？都是零。小数点前面这一减掉一同样等于零。好，所以x的尾数加上y的尾数呃，最终的运算结果就应该是正的零点零零零，最后还剩一个一。好，那么我们把这个运算结果写出来。现在由于小数点的前面这一位不是一，所以按照常规的流程，我们需要进行尾数的规格化，对吧？

这种情况我们呃通常需要用左归的方式去处理，尾数每左移一位。解码就要减一，然而现在问题发生了，当前这个解码就是规格化浮点数呃的最小的一个解码值。如果尾数继续左移，然后接码继续减一，那么接码就会落到零零零这样的一个值。而一旦解码变成全零。那么，这个浮点数就变成了一个非规格化浮点数，我们就要用另一种规则去解读它。好，所以现在我们对尾数进行左归，这个动作其实有点矛盾的，

对吧？我们要对尾数进行规格化，但是当尾数。左归一位的时候，整个浮点数就会变成非规格化的浮点数。好，所以此时我们不能按照普通的左归方式去处理。那计算机的处理方法是这样的，如果当前的这个解码值已经是零零零一了。但是此时这个尾数还需要继续左移，才有可能实现规格化，如果发生这种情况。那么，计算机会直接把解码置为全零，并且这个尾数不一位。

注意尾数不移位保持原样。阶码虽然置为了全零，但是尾数保持不变。然后直接把这个尾数就是小数点，后面的这23位保存到呃float型浮点数的呃里边。好此时符号位为零，解码全零尾数是呃22比特的零，再加上一个一。当解码变成全零的时候，说明发生了下一。而当前这个呃尾数不全为零，说明此时是下移到了非规格化浮点数的区间。如果解码全零并且留下的这些尾数位也是全零，那么就是直接下译为了机器零。好，

那么对于这个例子，我们尝试着用非规格化浮点数的呃，这个规则去解读一下它的真值，应该是对应正的。二的负126次方乘以零点一。那最终对应的结果就是二的负149次方。那这样我们就理解了前面这个例子。x+y的运算结果下移到了非规格化浮点数的区间。好的，那么这就是浮点数加减运算的溢出判断问题。我们通过两个例子感受了上亿。以及下溢这两种情况。计算机硬件对浮点数溢出的判断主要是根据接码来进行的，如果右归的同时接码加一。接码的值来到了巴比特权一的情况，

那么就说明发生了上亿，而如果接码的值当前已经是零零零一。但是尾数仍然需要继续左归，那么此时就会直接把阶码置为全零，并且尾数保持不变。而一旦接码全零说明发生了下溢。那此时运算的结果有可能会下移到非规格化浮点数的这个区间。对非规格化浮点数的真值，这个尾数部分的解读，大家需要注意。非规格化浮点数的小数点，前面隐含的不是一而是零。好的，那么以上就是浮点数加减运算这个部分的全部内容。大家可以在结合课件回顾一下我设计的这些例子。
