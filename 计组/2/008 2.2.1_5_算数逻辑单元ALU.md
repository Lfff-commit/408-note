各位同学大家好，在这个视频中，我们会学习什么是算术逻辑单元aru。首先，我们会介绍aru在计算机内部的一个作用。以及它需要支持哪些功能，紧接着我们会介绍aru具体的实现原理，当然这个部分简要了解即可考试，不太可能考它的实现原理。最后，我们会教大家怎么看懂aru的图示。在考研真题当中，有可能会给大家一个电路图作为题目的信息。在电路图当中，可能会包含aru这个部件。

好的，首先看一下aru在计算机内部的一个作用。alu是一个英文缩写，它的全称是这样的，翻译过来就是算术逻辑单元。我们知道CPU主要由控制器和运算器这样的两大部件组成，其中控制器负责解析指令。根据当前执行的这条指令的功能，向主机内部的其他部件发出控制信号。那这个控制信号也会发给运算器。接下来，运算器会根据控制信号对相应的数据进行处理，比如说进行加减，乘除等运算。运算器的内部主要由这样的一些部件组成，

首先会有一些通用寄存器用于存储，正在参与运算的呃，各种各样的数据。而这些数据的加减，乘除，运算会交给算术逻辑单元aru去完成。aru它是一种组合逻辑电路，这个电路实现了各种各样的运算功能，因此aru它是运算器的核心，我把它标成了红色。那除此之外，运算器内部还会有一个psw程序状态字寄存器，这个寄存器会用于存储CPU的状态以及运算的状态等等各种各样的信息。啊，在这我们先不展开，

在这个地方我们要强调的是算术逻辑单元aru，它是运算器的核心。因为这个电路部件实现了各种各样的运算功能。那大家继续往后学习，就会了解到，无论是加法，减法，乘法，还是除法，这些运算的实现都需要基于加法来完成。因此alu的核心是一个加法器。好，通过这张图，我们了解到CPU由控制器和运算器组成，其中运算器的核心是aru，

而aru的核心又是加法器。这两点可能在选择题当中进行考察。好，现在我们了解到了aru在CPU内部的一个地位和作用。aru需要对数据进行处理。那么，需要进行哪些类型的处理呢？也就是说aru需要具备哪些功能呢？首先aru需要支持一些基本的算术运算，比如加减乘除。以及逻辑运算，比如与或非异，或一位等等，那观察左边这个aru的图示。它有两个输入的呃运算数a和b，

分别是n个比特以及这边会输出n比特的运算结果。在图示的右边，这有m个比特的控制信号。这个控制信号是由控制器根据当前执行的指令发出的。结合刚才这个图示来看，假如当前执行的是加法指令，那么控制器就会把加法对应的控制信号发给aru。那aru这边接收到m个比特的加法信号之后，就会在输出端这个地方输出a+b的结果。那这个地方需要强调一点aru的这个控制信号，总共有m个比特那m的取值应该为多少呢？显然，这个m的取值应该和aru的功能总数有关系。假如说aru支持k种功能的话，那么控制信号的位数m肯定要大于等于log以二为。

k的对数。比如某个aru总共支持八种功能，那控制信号m就至少应该等于三比特。因为三比特刚好可以表示八种状态，也就分别对应八种功能。再比如上面这个思维导图当中，我们罗列了aru的功能总数，总共有呃算术运算四种逻辑运算五种。呃，其他的功能总共有两种，所以总共有11种功能。那在这种情况下，控制信号m的位数应该至少等于四比特。只有四比特才能表示11种功能。好，

那这个是很常考的一个考点。那aru除了支持算术，运算和逻辑运算之外，还有可能支持其他的一些功能，比如说求补码和直送。来分别解释一下，首先求补码的功能就是把一个源码送入到aru的某一个输入端，比如从bj端输入。同时，给出求补码的控制信号，那么输出端这个地方就可以得到这个源码所对应的补码。当我们把源码转变成补码之后，就可以让计算机硬件更方便的实现加减运算。好，这是求补码的功能。

再来看直送。直送的功能就是不对，这个数据进行任何处理，让它怎么进就怎么出。比如这一端输入零零零一，同时给出直送的控制信号，那么输出端这边也会输出零零零一。没有对数据进行任何处理，直接让它通过aru送出去好，这是直送的功能，那算术运算和逻辑运算的其他这些功能大家会通过之后的学习有更深入的了解。那这是aru需要实现的一些功能。接下来，我们简单了解一下aru的电路原理，一个比较简单的实现方法是这样的。

aru有AB两个运算数，我们可以在aru的内部呃设置k个功能电路。比如这个电路实现加法，这个电路实现乘法，这个电路实现与这个电路实现移位。每一个电路对应一种具体的功能，那我们可以把a和b两个运算数分别送到每一个电路的输入端。比如a这个运算数，它会被送到乘法电路b这个运算数也会被送到乘法电路。那经过乘法电路的处理之后，就可以输出乘法运算的结果。同样的道理，我们把a和b两个数同时送到其他的运算电路这儿，比如经过与运算电路的处理，这儿就可以输出a和b逐位相与的一个结果。

接下来，我们可以把这k个功能电路的输出结果通通接入到呃多路选择器的输入这一端。同时，用m个比特的控制信号去决定到底要让多路选择器的哪一条线路。输出。比如我们可以规定，当我们只有四个功能的时候，可以用两个比特去控制这个多路选择器。当控制信号为零零的时候，让加法电路的这个信号从输出端这输出出去。当控制信号为零一的时候，让乘法的这个信号从多路选择器的这一边输出出去。当控制信号为一零的时候，让与运算的这个信号输出，当控制信号为一一的时候，

让一位运算的这个信号输出出去。接下来把这个电路进行封装，隐藏内部的实现细节就得到了我们之前介绍的aru。那这儿介绍的只是一种最简单的电路实现方式，大家有一个简单的了解即可。考试不会考这些电路细节。我们在考试当中更需要注意的是，怎么去看懂aru的这个图示。之前我们说aru肯定会有m个比特的控制信号。那这个m的取值和aru支持的功能数量是相关的。m个比特必须要能够表示k种不同的状态。所以m需要大于等于log以二为底k的对数向上取整。好，另外aru有两个运算数可以输入，以及一个运算结果被输出，

那运算数和运算结果的长度恩比特。和我们的计算机的机器字长是相同的。应该这么说，计算机的机器字长就是由aru的运算位数来决定的。所以像左边这个图当中，这台计算机的机器字长肯定就是n比特。那在有的题目当中，有可能你还会看到呃aru有这样的一些输出信号，分别叫zfofsf和CF。我们把绿色的这几个输出信号称为标志位，这些标志位表示了运算结果的特征，比如说zf表示运算结果是否为零。of表示有符号数的运算是否发生了溢出。sf表示的是有符号数的运算，它到底为正还是为负？

CF表示，无符号数的运算是否发生溢出？这些标志位的信息通常会被送入到psw程序状态，自寄存器当中。把这些标志位的信息写入psw是有好处的，比如说当aru完成一个乘法运算之后。计算机可以去检查psw当中保存的of这个标志位，如果of标志位等于一，那么就说明刚才进行的这个乘法运算发生了溢出。也就是说，这个乘法运算的n比特输出结果是错误的，那在这种情况下，计算机就需要对这种异常的状态进行处理。总之aru进行某一个运算之后，把这些标志位信息保存到psw寄存器当中是有用的。

那在有的计算机系统当中，也会把psw程序状态字寄存器称为标志寄存器，英文缩写叫fr。flag register.所以大家在做题的时候，如果看到标志寄存器，那你要知道它和psw是一个东西，两个名字而已。好，最后在这个图当中，大家还会看到呃cin和c out这样的两个信号。ce是进位输入信号c out是进位输出信号，大家可以类比之前学过的带标志位加法器。那这两个信号大致了解一下就可以，不用过度的深究好，

那上面介绍这些就是考试可能会考察的一些重点，我们来看一下二零二二年的一个统考真题。我们先不管这个题目说了什么来看aru的图示，这有一个aru部件。a和b两个运算数分别是16比特。以及运算结果f也是16比特。同时侧面这儿有一个虚线箭头啊，对应的是aruop，也就是aru的控制信号。那现在我们知道这个控制信号肯定来自于控制器。同时，控制信号的位数需要大于等于log，以二为底k的对数向上取整。其中k指的是aru支持的功能数量。好，

再看另外这一边，这儿aru还有一个输出，这个输出会送到fr，也就是标志寄存器这儿。所以蓝色的这条线，它表示的就是aru输出的一些标志信息。也就是sfofzfcf。好，那通过这节课的学习，相信aru的图示相关的这个部分已经难不倒大家了。好的，这个视频中我们介绍了算术逻辑单元aru，它的一个作用以及需要支持哪些功能？如果aru总共支持k种功能，那么aru的控制信号位数m就应该大于等于log以二为底k的对数向上取整这么多个比特。

同时，我们强调了aru的两个运算数以及运算结果，它们的长度都是和机器自长相同的。aru可以对数据进行加减，乘除与或非等等各种各样的处理。在处理完数据之后。除了得到当前这次运算的结果之外。还会输出ofzfsfcf等等，这些标志位的信息，而这些标志位信息会被自动的送入psw程序状态字寄存器当中。在有的计算机系统当中，psw又会被称为fr，也就是标志寄存器。好，最后再次强调CPU由运算器和控制器组成。

aru是运算器的核心，而加法器又是aru的核心。这有可能在选择题当中进行考察。那关于aru的实现原理，大家有一个简要的了解即可，不需要过度的深究。好的，那以上就这一小节的全部内容。
