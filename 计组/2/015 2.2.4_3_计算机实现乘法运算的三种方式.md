各位同学大家好，在这个视频中，我们会以无符号整数的乘法为例去介绍呃计算机，实现乘法运算的三种常见方式。首先，我们会回顾之前已经学过的啊，用aru移位器，寄存器控制逻辑啊，去实现的这种乘法电路。这种电路的原理我们已经通过之前的学习，有了深入的理解啊，那这节课我们还会补充阵列乘法器以及用移位运算。呃加减运算用这种软件的方式去等效实现乘法的呃方式。本节课介绍的内容主要在选择题简答题当中进行考察。那在之前的学习中，

我们深入探讨了这种呃乘法电路，它主要由aru。以及移位寄存器。还有控制逻辑。由这样的一些核心部件组成。那细心的同学会发现我在这一课件的标题当中提到了移位器和寄存器这样的两个东西。这是两个不同的啊，相互独立的，这个硬件部件那事实上之前我们学习这个电路的时候。呃，我们介绍的这个寄存器p寄存器y，它们既拥有呃普通寄存器的功能，与此同时。也拥有移位器，就是支持移位运算这样的功能，

所以这页课件的标题当中提到的移位器和寄存器。其实这两个硬件部件的呃功能就和我们之前学习的这个硬件电路当中呃寄存器p和寄存器y的功能是一样的。只不过因为考试当中有可能会出现移位器，寄存器，有可能会出现这样的术语，所以在这页课件当中给大家进行一个补充。反正这两个硬件部件呃结合起来，它的功能等价于支持移位功能的寄存器。好，那么对于之前我们介绍的这个乘法电路来说，我们知道两个n比特的无负号数相乘。那我们的计数器刚开始初始值会设为n，接下来会进行n轮处理，每一轮。只处理一个比特，

因此两个n比特无符号数相乘，我们至少需要n个时钟才可以完成。每一轮处理至少需要一个时钟。那对于两个四比特无负号数相乘啊，对于这样的一个电路来说，我们至少就需要四个时钟才可以得到乘法运算的最终结果。好，那接下来的问题是有没有可能让这个乘法电路的运算速度更快一些呢？那一种常见的优化思路就是。呃，我们可以把呃一比特的这种乘法改成两比特的乘法，两位乘法。也就是在每一轮处理当中。处理这个陈述的两个比特。比如在左边这个例子当中，

第一轮处理可以用乘数的这两个比特和被乘数相乘。第二轮处理用呃乘数的更高的两个比特和被乘数相乘。那具体怎么做？我们不再展开，在这我们只需要知道，如果我们把这个电路稍加改造，每一轮对陈述的两个比特进行这个呃相乘相加的处理。那么显然，这个乘法电路，它完成乘法运算所需要的时钟数就缩短为了原先的一半，对吧？以前每一轮只处理一个比特，那么就需要n轮处理，现在每一轮处理两个比特，那么就只需要二分之n轮的处理。

好，所以把一位乘法改成两位乘法，这是一种很常见的提升。乘法电路运算速度的呃思路。好，接下来介绍第二种乘法运算电路叫做阵列乘法器，这给出了两个四比特无符号数相乘的阵列乘法器。这里的每一个小方框，内部它的细节呃，用左边这张图进行了表述。那这个阵列乘法器的电路原理，我们不需要深究，我们只需要关注阵列乘法器它的特点即可。首先，这个阵列乘法器完成两个恩比特数的相乘运算，

只需要一个时钟即可。比如说在左边这个例子当中，我们把被乘数记作x呃，乘数记作y那x，它的一一零一四个比特。会直接一一零一。会直接输入到阵列乘法器的这几个引脚处。然后乘数y一零一一。一零一一这几个比特会呃输入到阵列乘法器的右侧的这几个呃输入引脚。好，只要把被乘数乘数给输入了，那么巴比特的乘积结果一零零零一一一。一一零零零一一一这个运算结果就可以呃，在一个时钟内夸一下，马上就出来。

那可以看到，阵列乘法器相比于我们之前学习的这种呃aru移位器，寄存器和控制逻辑组成的这种乘法电路来说，它的运算速度就会快得多。只需要一个时钟就可以。另外，需要补充这样的一个概念，叫做快速乘法器。在这些课件当中，我们介绍的阵列乘法器是快速乘法器当中的一种，那大多数的呃，可以被称为快速乘法器的这种电路。都可以在一个时钟之内完成恩比特乘法运算。那在大多数计算机组成原理的教材当中啊，都是以阵列乘法器作为快速乘法器的一个典型代表。

所以我们主要了解的是这个阵列乘法器，那如果在考试当中，你遇到快速乘法器阵列乘法器，你遇到这样的术语，你就要知道呃，反正他们就是。呃，可以在一个时钟之内完成乘法运算的这样的一种呃，很厉害的电路就行了。好，接下来我们介绍计算机实现乘法的第三种思路，思考这样的一个问题，假如一个计算机啊，它的CPU内部。并不支持乘法运算。

而只支持呃逻辑运算加减运算，这些呃基础的运算，那么这样的计算机它能否呃用其他的运算去等效的实现乘法呢？好，这是可以的呃，在这页课件当中给大家写了一段程序，这段程序实现了两个呃，32比特无符号整数的乘法。无符号整数xy相乘之后，最终返回相乘的结果，这运算结果也是一个32比特的无符号整数。好，那么在这段程序当中，你会发现我并没有使用x×y，没有使用乘法这个运算符，

对吧？我只使用到了呃未运算，比如说用y与上一那这个操作就是可以把乘数y的最低一个比特给它取出来。然后根据最低一个比特为零，还是为一去决定到底要不要把被乘数x加到啊，这个最终的累加值result上面。那本质上我做的这些动作就是在模拟我们之前学习的这个电路，它的工作的一个过程。好那么呃，除了这个与运算加法运算下面这还使用到了一位运算。有兴趣的同学可以呃研究一下这段代码的逻辑，反正呃它在背后本质上就是在模拟我们这套电路的一个工作过程。好，那么通过这段代码想要强调的就是在一个没有乘法运算电路的这种计算机当中。我们可以只使用呃逻辑运算和加减运算去等效的实现乘法。

只不过这样等效实现乘法的速度显然是很慢的。因为在一个非流水线的计算机当中，任何一条指令的执行至少都需要一个时钟。呃，那现在你还不知道什么叫非流水线计算机，这个不用深究。总之，每一条指令的执行，都至少需要一个时钟，对吧？那么，这儿是不是有一个加法运算，有一个负值的运算？然后下面这个地方又用到了两个一位运算，对吧？

那每一个运算都至少需要一个时钟。而我们观察呃，在这段代码当中，我这的for循环是循环了32次，对吧？也就是说，这个乘数它的位数就是我们循环的次数。那每一轮的循环当中，都需要执行加法，移位等等，就是需要执行好几条指令。假设每一轮循环，我们需要执行k条指令。k条指令。那32轮的循环总计需要的时钟数是不是至少就需要32k这么多个时钟，

对吧？所以用软件等效实现乘法的这种思路，它虽然效果上是一致的，但是它的运算速度又要比我们之前介绍的这种。乘法电路要慢得多。好，接下来我们看一道真题。二零二零年的43题，这个题目给了两个很简单的函数呃，上面这个函数实现的是两个无符号整数的乘法运算。返回这个乘积的结果，下面这个函数实现的是两个带负号整数的乘法运算，返回带负号整数乘法运算的结果。然后题目说某台计算机m。它里边的aru只能进行加减运算和逻辑运算。

只能进行加减运算和逻辑运算，那逻辑运算就包括了呃位运算，对吧？比如说逐位语啊，逐位取非等等这些。位运算，它属于逻辑运算的范畴，另外我们介绍的呃，各种移位运算。逻辑移位，算术移位，这些都属于逻辑运算的范畴。好，然后让我们回答下面的这些问题，第三个小红问我们针对以下的三种情况，

就是说这台计算机。假如第一种情况就是它没有乘法指令，第二种情况就是呃，由使用aru和位移器实现的乘法指令。第三种情况是由使用阵列乘法器实现的乘法指令。这个问我们第一个函数也就是两个无负号整数相乘的，这个函数在哪种情况下的执行时间最长？在哪种情况下的执行时间最短？请说明理由，那这个小问他考察的就是我们这节课介绍的这三种乘法的实现方式。好，先来看第一种情况，假如这台计算机它的内部直接就没有惩罚指令。那么，这段C语言程序是否可以在这个计算机上面去运行呢？

答案是可以，我们知道C语言需要经过编译器的处理，把它翻译为呃与之等价的机器级的指令。那这些机器级的指令可以直接丢给CPU去执行，对吧？所以对于这个例子来说，如果这台计算机它不支持乘法指令的话。那么，这一句简单的C语言代码就会被编译器翻译为呃功能上等价的若干条加减运算指令。和移位指令。也就类似于我们之前提到的这种逻辑。反正两个32比特的无符号整数相乘，总能被我们拆解为32轮的呃加减运算，一位运算啊，这样的基础处理。

好，所以如果没有乘法指令的话。那么，这个CPU它实现两个无负号整数相乘，它的执行时间是不是最长的？就对应于刚才提到的这第三种实现方式嘛。它的运行速度是最慢的。所需要的时间是最长的。我们说了，经过32轮的处理，每一轮的处理都需要把它等价为呃若干条加法移位，还有呃像位运算这样的呃一些基础的指令。那假设每一轮的处理都需要执行k条指令，那经过32轮的处理，我们至少就需要32k个呃时钟。

才可以等效的完成32比特的这种乘法运算，对吧？所以这种情况下，执行的速度是最慢的。好，接下来对于第二种和第三种情况来说，怎么去理解呢？就是说这个CPU它支持乘罚指令。那么x×y这一段简单的C语言代码呃，对于编译器来说，只需要把它翻译为呃等价的一条乘法指令，是不是就OK了？那这条乘法指令直接丢给CPU来运行，那对于这儿提到的第二种情况，他的意思是说，

这个CPU完成乘法指令。呃，所调用的或者说所激活的电路是这样的一个电路，那通过之前的探讨，我们知道这样的一个电路，假如每一轮只处理一个比特的话，那它至少需要32个时钟，对吧？才可以完成。呃32比特的无符号整数运算，但如果CPU内部它直接就拥有一个阵列乘法器。那只需要一个时钟就可以完成。好，所以这提到了二三两种情况，虽然它们都拥有乘法指令，

但是显然底层硬件当中包含阵列乘法器的这种情况。它的运行时间是最短的，对吧？只需要一个时钟就直接得到这种乘法运算的结果。好，那这道题目我们点到即止，通过这道题目主要是想让大家呃感受到这节课介绍的这几种呃乘法运算的常见方法，它在考试当中一般是怎么考的？我们对这三种乘法的实现方式进行总结，阵列乘法器，它属于快速乘法器当中的一种。那大部分的快速乘法器当然包含阵列乘法器在内哈，它们的运算速度都很快，基本上都可以在一个时钟之内完成这个乘法运算。那对于我们详细学习的aru移位器，

寄存器和控制逻辑呃，由这些核心部件组成的这种乘法电路来说，它的运算速度。还是比较快的呃，但是通常需要多个时钟。那运算速度最慢的是用一位运算加减运算这些呃，这些基础的运算去等效的实现乘法。那这种情况，它的运算速度是最慢的。好，最后需要补充一句，虽然这个视频当中我们是以无符号整数的乘法为例去介绍了一二，那这三种实现的思路。对于带符号整数的乘法同样适用。以及对于带符号整数的乘法，

我们同样可以得到一二三这样的三条结论。好，那以上就是这节课要补充的东西。
