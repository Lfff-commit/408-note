各位同学大家好，在这个视频中，我们要探讨定点数的移位运算，首先从大家熟悉的十进制数出发啊，带大家感受。一位运算是怎么回事？好比如说这是一个十进制数九八五点二一一，那所谓的一位运算就是。通过改变各个数码位和小数点的相对位置，从而改变呃，各个数码位的位权。好听起来云里雾里的是吧？但其实我们每个人对所谓的一位运算都非常的熟悉。好，那对于这个数小数点在五的后面二的前面，

接下来我们可以让小数点的位置固定。让所有的数码位左移移位左移移位，那左移带来的效果就是。各个数码位和小数点的相对位置发生了改变，因此每一个数码位的位权也都发生了改变，对吧？那这么做的效果就相当于在原有这个数的基础上，乘了十的一次方。这个是我们都很熟悉的，对吧？好，那如果我们再往左移一位，那这么做的效果就相当于在原先这个数的基础上乘了十的二次方。好，现在你可以感受到这句话的意思了，

我们刚列举的是左移这种运算，我们把小数点的位置固定。让数值位左移，一位相当于乘以十的一次方。左移两位相当于乘以十的二次方。那类似的道理，我们也可以把数值位右移，把小数点固定数值位右移一位。那这样的效果相当于除以十的一次方，那也等价于乘以十的负一次方，如果右移两位就相当于除以十的二次方。等价于乘以十的，负二次方。那这就是十进制的移位运算，我们在平时啊手算做数学题的时候，

我们经常会用这种呃移位的这种思想。去快速的实现某些特殊数值的乘法或者除法，对吧？比如说你做数学题的时候一个数，它要乘以十的r次方。那我们的做法一般就是让这个数进行左移r位，对不对？或者换个角度，相当于让小数点往后走r位。那除法同理。那在这个小节中，我们要探讨的是定点数的移位运算。在计算机内部啊，一个定点整数。是用二进制来表示的，

那计算机在对二进制进行一位运算的时候和我们处理十进制数的时候有一些相通的地方，也有一些不同的地方。那通过之前学习，我们知道计算机内部呃表示无符号整数和表示带符号整数的规则是不一样的。带符号整数，通常用补码表示对吧？那么对于无符号整数和带符号整数的一位运算呃，它的规则就会有一些区别。如果我们要处理一个无符号整数，那通常采用逻辑移位去实现，如果要处理带符号整数，那通常用算术移位啊去实现。那接下来我们先探讨逻辑移位的规则，我们说逻辑移位通常用于处理无符号整数。好，

假设这是一个计算机内部的八比特无符号整数，那每一个比特位都有自己这个位的位权，对吧？比如说呃，从右往左的第三个比特，它的位权就是四好，然后从右往左的第五个比特。它的位权是二的，四次方也就是16，所以啊，上面给出的这个数，它对应的十进制的呃，真值就是20。好，接下来我们会对这个无符号整数进行逻辑左移，

那逻辑左移的规则很简单，就是高位移出，丢弃低位补零。好对于无符号整数来说，我们把它的小数点啊视为在最后一个比特的，后面就隐含了一个小数点，对吧？好，接下来我们把这一串二进制串往左移一位。往左移一位。就变成了这个样子。这样的一个数，通常是放在寄存器里边的那一个寄存器的那个位数是有限的。比如说巴比特寄存器，它就只能保存巴比特对吧？

那么所有的这些数值位整体往左移了一位之后，最高位这一位，我们就必须把它给舍弃。这就是高位移出丢弃的意思。好，另外，由于左移导致了最低这一位，现在变空了，对吧？那么最低这一位我们补零。这样就可以好，所以左移一位，它的结果就是这样的。好，那我们可以看一下左移之后它的真值变成了多少？

这个比特现在它的位权是二的三次方。这个比特它的位权是二的，五次方那二的三次方等于八。二的五次方等于32，32+8=40对吧？也就相当于在原有这个数值的基础上。乘以二的一次方。这个很好理解吧，你看原本的这个比特一，它的位权是二的二次方对吧？那它左移了一位之后，它的位权不就从二的二次方？涨到了二的三次方吧，也就是翻了两倍，好类似的，

另外的这个比特一，它原本的位权是二的四次方，那左移了一位之后，它的位权是不是就变成了二的五次方？好，所以左移一位带来的效果就相当于乘以二的一次方那类似的道理。如果左移两位，就相当于乘以二的二次方左移三位，就相当于乘以二的三次方，对吧？好，截至目前，我们移出的所有的这些比特都等于零，那接下来我们在呃160这个无符号数的基础上，再继续左移一位。

那么，最高的这一位一就会被我们无情的丢弃，对吧？好，那么把这个一丢弃之后啊，低位补零那么最终得到的结果，如果把它翻译为十进制，应该是64。你看出现了问题，对吧？160的真值，如果继续左移一位，那理应是320才对。但现在左移之后，真值变成了64，

发生了错误，那此时我们说这个逻辑左移运算发生了溢出。也就是超出了呃n比特无符号数所能表示的范围。我们知道八比特的无负号整数，它只能表示零到255这个范围的数值。那现在160左移一位。我们期待的效果本来是乘以二对吧？但是由于乘以二这个真值已经溢出了。所以最终导致了数值的错误。好，所以当我们对无符号整数进行逻辑左移的时候，要注意的就是这个问题。这可能会导致运算错误。好，接下来我们探讨逻辑右移运算回到20这个数值。

这是巴比特物符号数。逻辑右移的规则啊，也很简单，低位移出直接丢弃高位补零即可。好，所以如果逻辑右移一位的话，那么呃，最低的这一位，你看移出丢弃对吧，然后每一位反正都是呃。相应的，往右移一格。那右移之后空出来的最高位，我们用零去补充就可以了，这就是逻辑右移的规则。

那逻辑右移，一位它的效果相当于除以二的一次方，或者说乘以二的负一次方。那道理是类似的，对吧？你看原本的一个比特一，它的呃位权是二的。二次方。当我们把它右移一位之后，它的位权就来到了二的一次方，那每一个比特一它的位权都会除以二。那作用在整个无符号数上，就相当于整体除以二，对吧？那类似的道理，

如果右移两位就相当于除以二的二次方好，截至目前，我们的右移运算呃丢弃的这个低位比特都是零。20，除以二=10，十÷2=5。好，接下来我们看五÷2，它的真值应该等于二点五，对吧？但是显然，巴比特的无符号整数不可能表示这个小数，那我们对这个无符号数继续在右移。一位。接下来，

我们丢弃了呃末尾的这个一并且高位补零，那么最终得到的这个无符号整数，它对应的十进制值就是二。所以可以看到，在逻辑右移的过程当中，如果低位丢弃的是一，那么就有可能会丢失精度。就像刚才这个例子，我们丢失了零点五这样的精度对吧好，所以虽然逻辑左移，逻辑右移。可以用于快速的实现呃无负号整数的乘以二的r次方，或者除以二的r次方可以快速的实现这样的乘除法运算。但是在逻辑左移的过程当中，我们需要注意溢出问题，

在逻辑右移的过程当中，我们需要注意的是丢失精度的问题。好，那截至目前，我们介绍的是逻辑移位，它用于处理无符号整数。接下来，我们探讨算术移位运算。通常用于处理带符号整数。那我们知道带符号整数在计算机的内部都是用补码来表示的，那这是一个巴比特的带符号整数。由于它是补码，所以最高的这个比特把它视为符号位，符号位为零，表示它是一个正数。

好当符号位为零的时候，后面的这些数值位和源码的解读方式呃保持一致，那这个带符号整数它对应的十进制值就是正的20。接下来介绍算术左移运算算术左移的这个规则和逻辑，左移一模一样。没有任何区别，你看高位移出丢弃低位补零。逻辑左移，高位移出，丢弃低位，补零处理和规则是一模一样的，对吧？好，所以算术左移一位呃，得到的效果就是这个样子，

再左移一位，得到的效果就是这个样子。每左移一位，就相当于乘以二好。接下来问题来了。到这一步呃，这八个比特，它对应的十进制应该是80，我们继续把这个数进行算数左移。左移之后，你会发现这个数变成了一个负值，对吧？符号位为一，表示它为负嘛。这八个比特补码对应十进制的负96。

那显然，这一步的算术左移，它的效果就并不是乘以二这么简单。你看上一步这个数值应该是八十八十×2，如果正确的话，它的数值应该等于160，对吧？但是，巴比特的补码所能表示的数值范围只有负的128到正的127，只能表示这个区间。所以160这个数值显然已经超出了巴比特补码所能表示的合法范围，此时我们说。这一步算术左移运算发生了溢出，那计算机是如何判断算术左移运算是否发生了溢出呢？很简单，

计算机硬件会检查左移之前和左移之后，这个符号位有没有发生改变？比如刚才这个例子中，符号位从零变成了一，那么此时计算机就知道啊，这次算术左移运算发生了溢出。好因此算术左移运算它的溢出判断方法和逻辑，左移运算溢出判断的方法出现了一些区别。对于逻辑左移来说，计算机判断的是高位丢弃的这个比特是否唯一，而算术左移，计算机判断的是呃左移之前和左移之后。这个符号位有没有发生改变？如果符号位变了，那么就说明发生了溢出好，

那这是算术作移。接下来探讨算术右移。算术右移的这个规则和逻辑右移开始出现了区别。最低位直接丢弃，这个是一样的，然后高位我们会用呃符号位去进行补充。什么意思呢？比如说在这个例子当中，我们给出了八比特的呃正整数正的20。此时符号位等于零，我们把这个数进行逻辑右移，最低位移出之后直接丢弃。原本的这个符号位也往后移了呃，一个比特好，现在空出来的这个符号位，

我们用原本的这个符号位去进行补充。比如说原本的符号位等于零，那么这个空位我们也得补零。作为对比，这儿给出了一个负数的例子，可以看到这八比特的补码，它对应的数值是负的108。对它进行算术右移，那原本的符号位往右移一格，那这个高位就空出来了，对吧？那可以看到我们这一次在高位这个地方并不是补零，而是补了一对吧？原本的符号位等于一，所以这次我们也补一。

之前这个例子当中，原本的符号位等于零，那这次我们也补零好，那这就是高位补符号位的意思。那对于一个带符号整数，我们每进行一次算术右移，效果上相当于呃除以二的一次方。再右移一位，就相当于除以二的二次方。那对于刚才这个负整数也是一样的，108÷2=54，大家可以验算一下哈，把这个二进制补码转换成原码。然后看一下它的值是不是负的54好，接下来负的54再往右移一位，

效果相当于再除以二也就是负的27。好，接下来不一样的地方来了，我们观察到现在这个二进制串，它的最低位等于一，如果再右移一位，那么就会丢弃这个一，对吧？那么，和逻辑右移类似，如果最低位我们丢弃的这个呃比特等于一的话，那么就会导致这个除法的精度丢失。你看负的27，它除以二应该是等于呃负的十三点五对吧？但是，

由于巴比特的带符号整数啊，补码不能表示这个小数，所以算数右移一位就导致了啊，最终得到的这个值丢失了。精度那前面这个正整数的例子也是一样的，如果继续往右移一位末尾丢弃了一个一。那么，这次算术右移就会啊，出现这个精度的丢失好，那么这是算术右移的过程当中需要注意的东西。如果右移丢弃的位等于一，那么会丢失精度。好，接下来看道真题。一八年的16题题目给了一个巴比特机器数，

让我们对这个呃机器数x。分别进行逻辑右移，移位和算术右移，移位的操作。问，最终得到的机器数各是什么？好，很简单吧，这题考的就是逻辑右移和算术右移的规则。逻辑右移，我们需要把低位丢弃，高位补零，算术右移，我们低位丢弃，高位要补符号位。

那么，对于这个巴比特数来说，它的最高位是一对吧？所以，如果进行逻辑右移。那么，我们把低位丢弃，高位补零就行，而进行算术右移的话，我们低位丢弃高位需要补和符号位一样的一。因此，这个题目的答案应该选择b选项。那借着这个题，我们再强化对于逻辑左移和算术左移的记忆规则，很简单，

高位直接移出，丢弃低位。都是补零，所以对于逻辑左移和算术左移来说，从机器的呃层面来看，其实对二进制串的处理是一模一样的。那这个地方呃，大家可能会有疑问，之前我们一直在强调呃逻辑移位，通常是用于处理无符号整数。算术移位通常适用于处理带负号整数，我们之前说的是常用于，而不是只能用于。明白我意思吧，就是在应用层面，

我们一般来说会这么使用，但是在机器在计算机硬件层面。只要你丢给他一串二进制串机器，不会管你给他的是无符号数还是带符号数？在机器看来，这就是一串二进制比特，无论是什么数值类型，只要你给机器呃，这个特定的指令，比如说给出一个逻辑右移的指令。或者给出一个算术右移的指令，那么机器总会按照你的指令要求去处理这个二进制比特串。所以你看在这个题目当中，同样一个二进制比特串，你既可以对它进行逻辑右移，

也可以对它进行算术右移。对于机器来说，它到底是无符号整数还是带符号整数无所谓？处理的逻辑都一样，对吧？好的，在这个视频中，我们介绍定点数的移位运算，首先我们要记住逻辑，左移右移，算术左移右移呃，它们的规则各是什么？逻辑左移和算术左移在底层硬件看来，它们执行的规则是完全一致的，但是逻辑右移和呃算术右移就有些区别。

算术右移呃高位补的是符号位。那移位运算和乘除法运算之间是有呃非常密切的关系的，当我们对无符号整数进行逻辑左移的时候。相当于乘以二的r次方。逻辑右移相当于除以二的r次方。那带符号整数需要进行算术左移和算术右移才可以等效于呃乘以二的r次方，或者除以二的r次方。好，所以一位运算和呃乘除法之间的这种关系。这是很常考的一点。这个技巧经常在一些大题中使用，到那需要注意的是，无论是进行逻辑左移还是算术左移。虽然这个效果等效于乘以二的r次方，但是有可能发生溢出问题。

而在进行右移运算的时候，虽然等效于除以二的二次方，但是需要注意丢失精度的问题。那所有的重点都总结在了这张导图里，大家可以再结合这张导图回顾一下好的，那么以上就是这个视频的全部内容。
