各位同学大家好，在这个视频中，我们要探讨C语言中的浮点数类型转换，那本节课涉及的内容是真题的高频考点，也是很容易出错的点。在本节课，我们会以int型float型和double型为例，分别探讨整型转浮点型。浮点型之间的互转以及浮点型转整型这几种情况下，有没有可能发生溢出，以及有没有可能发生精度丢失？好，那首先看整型转浮点型，我们先回顾一下浮点型的格式C语言中定义的float型和double型是完全符合I triple e七五四标准的。其中float型由一比特符号为八比特阶码和23比特的尾数组成。

对于一个规格化浮点数，在尾数的前面还会隐含一个呃隐藏位一。那double型也是类似的，总共64比特，其中阶码11比特，尾数52比特，同样的啊，规格化的double型，它在这个52比特尾数的前面还隐含了一个一。好，赶紧唤醒回忆，接下来我们看整型转浮点型。我写了一段这样的程序啊，这儿我定义了一个整型变量x。它的值等于二的24次方加一。

接下来我会用这个C语言的强制类型转换，把这个整型变量分别转换成float型和double型。然后下面这个位置会输出整型变量x的值float型的值和double型的值。这段程序运行的结果是这个样子。整型变量x等于啊一六什么什么二幺7 float型有点奇怪，它打印出来的值是呃二幺六。最后，打印double型的变量，它的值和int型的原值是一致的。在这个例子中，int型转float型就发生了呃精度的丢失。先给结论int型转float型是有可能发生精度丢失的，比如说这个例子就是很典型的代表，而int型转double型是不可能发生精度丢失的。至于为什么我们一会再来解释，

那这节课的开头我们说过，对于数值类型转换的这种问题，我们除了关注精度丢失以外，还需要关注是否可能会发生溢出。同样的，先给结论int转float或者int转double都是不可能发生溢出的。关于两种数值类型强制转换是否可能发生溢出，我们只需要关注一个方面。就是各种类型的数值表示范围。int型变量，在C语言当中通常是32比特，如果用十进制科学计数法，那大概是二点一四×10的。九次方。这个是int型可以表示的范围，

那float型和double型我们分别用十进制科学计数法写出，它们的这个呃合法的表示范围float型，它的表示范围大概是负三点四×10的38次方。到正的呃，这个值同样是乘以十的38次方是这样的一个数量级，而double型它的表示范围是负的一点七九×10的308次方。那正值的最大值也是一样的好，所以可以看到float型和double型，它们可以表示的这个数值范围明显啊，覆盖了int型可以啊，表示的这个范围。因此，如果我们把int型转float或者int型转double，那么溢出是不可能发生的。好，

接下来我们再回头看刚才说到的精度丢失问题，我们分析一下这个精度丢失的背后发生了什么？同样基于这个例子，我们先把x的数值写成二进制的形式，32比特的二进制补码长这个样子。在我的这个示意图当中，一个小格子对应一比特，刚好32比特最高位是符号位。后面的31比特是数值位。那float型同样32比特可以看到，和int型的这个图示是完全对齐的，最高一比特是符号位中间这八个比特是接码。偏置值是127，而最后这有23比特的尾数。对于规格化，

浮点型在23比特尾数之前还隐含了一个一。好，那么double型是类似的。一比特符号为11比特的这个解码。52比特的尾数。尾数的前面还会隐含一个一。好，接下来我们要把这个int型的呃变量x强制转换成float型和double型。这个框架结构我已经画出来了，大家可以尝试一下int转float float型的这个二进制表示应该是怎么样的？可以打在公屏上，那十秒钟之后我再公布答案。三二。1 float型的二进制表示应该是这个样子。根据float和double的结构，

我们可以知道float型它的尾数可以保存的这个精度位数。应该是一+23比特。而double型它可以保存的这个精度应该是一+52比特。那么，观察我们这个int型变量x。从右往左看，第一个有效的数值为一，一直到最后一个一，总计包含了25比特。然而float型它最多只能保存一+23比特的这个精度对吧？所以当我们把int型转成float型的时候。这25个比特的呃有效数据为。在float型当中，是不是只可能保留下前面的这24比特对吧？最末尾的这一个比特有效位一会被丢弃。

而前面的这24比特当中，最高的这个比特一在float型当中可以用这个隐含位去表示。那剩下的23比特就原模原样的，把它复制到这23比特的尾数当中。好，所以这个地方我们打印出来的float值和原值不一样，问题就是出在最后这一个被丢弃的比特。大家可以把这个float型的二进制转换成真值。对应的就是一六七七七二幺六。好，所以我们说int转float型是有可能发生精度丢失的。对了，这个地方我们呃考虑另一种情况，如果把int型的呃数值改成一六七七七二幺六。那这样的话int型的二进制最后一个比特就会变成零对吧？

那么在这种情况下，当我们把int强转成float的时候。最后被丢弃的这一个比特。也是零那零这样的数值位，即便被丢弃了，其实也不会影响它的精度，对吧？所以int转float它有可能发生精度丢失，但并不是总会发生精度丢失。具体得看，这个强制类型转换的过程当中，被丢掉的这些比特费。是否为全零？如果丢弃的都是零，那么精度就不会受到影响。

好，接下来再看int型转double型，那由于double型的这个尾数非常长，对吧？总共可以保留53比特的有效数值位，而int型它整个长度都只有32比特。所以double型的这个尾数可以保存的精度。远大于int型的这个比特数。所以你会看到int型转double型之后。这25比特的有效数值为当中最高的，这一个比特。用隐含位表示。然后最低的这一个比特。转成double之后，放在这个位置，

对吧？总共25个比特被无损的保存在了double型当中。后面我们还得添一些零。好因此int转double是不可能发生精度丢失的。好，以上是整型转浮点型这种情况。对了，如果做题的时候这个整型变量遇到负值怎么办？那分析的方法是雷同的，对吧？只不过由于int型，它是补码表示，那为了便于分析呃，我们可以先把int型的补码先转换成源码表示。然后再基于源码把它转换成float型。

观察转换的过程中，末尾被舍弃的那些呃数值位是否全零？如果全零就不会发生精度丢失。好，由于原理是雷同的，这儿就不再赘述。接下来，我们探讨第二种问题，浮点型之间的互转。这个问题反而很好分析。嗯，对于精度丢失问题float型转double型是不可能丢失的，而double转float是有可能精度丢失的。很好理解吧，因为float型，

它的尾数呃和double型，它的尾数可以保存的有效数值位比特数是呃完全不一样的，对吧？double型明显可以保留更多的有效数值位。你看float float型可以保存一+23比特，那么把这一+23比特如果强转成double型的话啊。它在double型的尾数当中，所处的位置就是我这黄色高亮的这些部分。所以float转double是无损转换的。精度不会丢失。那相反，如果double转成float，那么由于double型的这个尾数有效，数值位太多了。所以强制转换成float类型，

那么呃，只能保留double型的呃，前面这一+23比特的尾数。而后面的这些有效的数值位就全部得丢弃。所以就有可能导致精度丢失的问题。好，这是精度丢失，接下来溢出问题，同样的，我们只需要考虑啊，各种数值类型，它的这个实际表示范围。可以看到，double型它的这个表示范围远远大于啊flos型。所以float强转为double不可能发生溢出，

而double转float有可能发生溢出。好给大家写了一个程序。在这段程序中。double型变量的值，我把它设置成了一×10的40次方。然后在下面这一句把double型强制转换成float型，最后打印出double和float的值。大家不妨猜一下，最终打印出的float值等于多少？三二一好是这样的一个结果。首先打印出的double值啊，它是一×10的40次方。而float型打印出的值是inf，这是infinity无穷大的意思。还记得吧，

这个float型，它有一些特殊的这个呃数值状态，包括正无穷大和负无穷大。那从这个例子可以看出，如果double状float型发生了溢出，那么最终这个float的呃数值就会被记为正无穷或者负无穷，到底是正还是负？具体取决于这个double型原本的数值为正还是为负。好，那么以上是两种浮点类型的互相转换。最后，我们再来看浮点型转整型。先考虑溢出问题。显然，两个float型它们可以表示的呃，

真值范围都要远远大于int型。所以，无论是float转int还是double转int，都有可能发生溢出。只要它们原本的真值超出int型，可以表示的这个范围，那么就会溢出。好给大家写了一段程序，先定一个float型变量，它的数值已经超出了int型可以表示的最大范围。然后强制转换成int型，最终打印啊float和int的值。打印的结果是这个样子。最终，这个int型的变量被置为了呃int型变量，

可以表示的绝对值最大的这个负值。也就是一个比特的一后面的所有比特都为零。事实上，在C语言当中并没有啊明确的规定说，浮点型，转整型发生溢出的时候，这个整型变量的二进制应该怎么去确定？并没有一个明确的标准做出规定。所以浮点转整型溢出后int到底为多少？这个并没有标准答案，你可能换一个呃硬件平台或者换一个编译器。它最终得到的效果都不一样，那这我给出的这个示例是呃最常见的一种结果。如果int发生溢出，那么会把int值记作一个一，

后面全是零，也就是补码，可以表示的绝对值最大的那个复数。好，那么这是浮点型转整型，有可能发生溢出，虽然我们只说了float型，但是double型转int型是不是一样的道理？double型的这个数值范围还要更大，那更有可能发生溢出了。好，这是溢出问题，接下来看浮点型转整型的精度丢失问题。显然，无论是float还是double，

它们都可以表示小数点后面的一些。数值信息对吧？但是int型，不可能保留小数点，后面的信息，所以浮点型转整型是有可能发生精度丢失的。好写了一段这样的程序定义一个float型变量，它的真值等于十进制的五点七五强制转换成int。大家猜一下，最终打印出来的这个int型变量x，它的值是多少？三秒钟后公布答案。三二一。等于五。看起来这个处理简单粗暴，

对吧？小数点后面的所有的这些信息全部丢弃，只保留整数部分。所以精度发生了丢失。浮点型转整型，我们可以从十进制的这个视角很快的得出结论，反正就是把小数点后面的全部舍弃。这是十进制的视角，那接下来我们再来看一下二进制的视角。五点五七这个float型变量，如果把它展开成二进制表示，就应该是这个样子。其中，接码的真值等于二。尾数部分是这个样子。

把这个二进制科学计数法转换成二进制的定点数，表示就应该是幺零幺点幺幺。当float转int的时候，小数点后面的这些有效数值位就直接舍弃，只保留小数点前面的这些有效数值位。那这个int型对应的真值就是五。好，那这是精度丢失问题，那无论是float还是double，只要强制转换成整形，那么都会把小数部分。全部扔掉。注意，并不是四舍五入，而是直接砍掉，

就像这个例子所体现的这样。好的，那么在这个小节中，我们介绍了C语言当中的啊，浮点数的类型转换问题。我们探讨了整型转浮点，浮点数的互转以及浮点型转整型。无论是什么样的呃数值类型转换，我们都要关注溢出和精度丢失的问题。我在这一页课件当中总结了所有的这些情况，有没有可能发生溢出以及有没有可能发生精度丢失？不可能发生的啊，打了一个叉，可能发生的打了一个勾，大家可以自行回顾。

当我们判断是否可能发生溢出的时候，我们主要考虑的是呃，转换前和转换后的两种数值类型，它们的表示范围。int型的32位补码可以表示的就是这样的一个范围。float型和double型表示范围依次增大。所以表示范围小的转换成表示范围更大的这种数值，那肯定不会发生溢出。而如果倒过来表示范围大的这种数值类型转换成啊，表示范围更小的就有可能发生溢出。好，这是溢出的判断，那精度丢失问题，对于浮点数来说，我们重点关注它的尾数，

可以保存多少个精度位数？注意，无论是float还是double，在它的23比特尾数以及52比特尾数之前，还会有一个隐含位。所以float型实际上可以保存的这个精度位数是24位，而double型可以保存的精度位数是53位。显然float转double是无损转换精度，不可能丢失，而double转float就有可能丢失一些精度。接下来，如果考虑整型和浮点型之间的这个呃转换精度丢失问题的话，那么我们在分析的时候，首先把整型变量转换成源码表示。因为在C语言当中，

无论是int也好，short也好，还是long型也好。所有的带符号整型变量，它都是补码表示，而浮点数的尾数部分，它又是源码的，这种呃标准。所以我们要分析整型转浮点型的时候，我们需要先把呃整型变量转换成源码表示。那源码的符号位是不可能导致任何问题的。因为无论是float还是double，它们都有呃一个比特专门用于保存符号位。那我们需要重点关注的是源码的数值部分，数值部分到底需要多少比特的呃尾数才可以无损的保存？

那么，上面这些结论大家都可以不记，关键是掌握这种分析的方法。这节课的内容，大家一定要掌握。无论是小题还是大题，都很喜欢考。好的，那以上就是本节课的所有内容。
