好在这个视频中，我们一起来总结祭祖的一个重要题型，五段式指令流水线。先来看考研大纲对我们的要求，在指令流水线这个部分，要求我们掌握指令流水线的基本概念。指令流水线的基本实现。结构冒险数据冒险控制冒险的处理。超标量流水线，动态流水线的基本概念好。首先看蓝色框的这两个部分。只要大纲里边呃提到什么什么的基本概念，如果是这样描述的考点，那这类考点通常只会考察简单的概念题。所以这类题目最好的复习方法就是直接去看王道书对应小结的课后习题就可以了。

好，再来看绿框这个部分指令流水线的基本实线，那这所谓的基本实线。特指五段式的指定流水线。所有的考题当中，深入考察指令流水线的那些题目，一定都是五段式的指令流水线。好，所以接下来我们会重点研究五段式的指令流水线要如何去分析？各类冒险有没有发生以及各类冒险应该如何处理？在历年真题当中，最常出现的一种考察形式是给你一堆MIPS指令序列，让你去分析。有没有发生各种冒险，以及如果发生了各种冒险，

那应该如何去处理这些冒险？这类题目的考评是比较高的，而且分析的难度会比较大，很多同学不知道该如何处理这类题目。因此，接下来我们会依次聊一聊啊，考结构冒险的题，考数据冒险的题以及考控制冒险的题，你应该如何分析，如何去处理？那首先说结构冒险这个考的比较少，而且结构冒险的可考性呃，考察的灵活性不是特别高。通常来说，不会结合MIPS指令序列来考察，

一般只是考察简单的概念或者简答。好，我们来快速回顾一下结构冒险是怎么发生的，以及如何处理。嗯，我们知道主存里边存储了，我们要执行的指令以及数据。当然，主存里边的数据一般来说是会啊，在cache里边都可以命中，所以当CPU执行指令的过程当中。呃，可以直接去cache里边找到指令，以及找到指令要处理的数据。那如果说我们把指令和数据无差别的存储在同一个cache当中，

那这就意味着在五段式的指令流水线当中。第一个阶段取止阶段，我需要访问这个cache。然后第四个阶段缓存阶段，我也需要去访问这个cache。基于这样的特性，一条指令的执行分为一二三四五这样的五个阶段。其中if这个段和m这个段。需要使用到同一块cache。因此，多条指令连续执行的时候就会出现结构冒险。比如说在第四个时钟，这个地方。指令四。它的if这个段需要使用到cache。

指令一，它的m这个段也需要使用到cache。两条指令在同一个时钟内都需要使用同一个硬件资源。那这就导致了资源冲突，也就是结构冒险。那如何处理这种结构冒险呢？通常有两种处理方法，第一种处理方法就是在出现。结构冒险的时候，我们把后面这条指令让它阻塞若干个时钟周期。直到这条指令的呃执行不会导致结构冒险为止。这是第一种处理结构冒险的方法。在考研当中，更常见的是第二种处理方法，就是把指令cache和数据cache分离。

在五段式的指令流水线当中。第一段需要取指令。第四段需要访问主存里面的数据。那由于第一段和第四段要访问的内容不一样，因此我们可以把呃。cache拆分为指令cache和数据cache。顾名思义，指令cache就是专门用于存储啊，程序指令，而数据cache就是专门用于存储指令之外的那些数据。那这么做的好处显而易见。相当于我们拥有了两个相互独立的cache。每一条指令在第一个时钟和第四个时钟之内，它所需要访问的cache是不一样的。所以当第一条指令执行到第四个时钟的时候，

它此时要访问的是数据cache。同时，在第四个时钟之内指令四，它要执行if段，但是这条指令。他此时要访问的是指令c。因此，我们把指令cache和数据cache相分离，就可以解决五段式指令流水线里边的这种结构冒险的问题。那来看一个一四年的真题。采用指令cache和数据cache分离的主要目的是什么？显然是d答案可以减少指令流水线的资源冲突，那别忘了资源冲突和结构冒险是同义词。这三个术语的含义是一样的，只不过不同的教材啊，

习惯用不同的术语而已。好，这是一四年的一个简单的概念题。那除了一四年这个题目之外，还有一些年份的题目当中也会啊，出现数据cache和指令cache分离也会出现这样的啊。呃，描述比如说一二年的这个大题里边，他就说数据cache指令cache分离。那现在我们知道了这个题目条件，它其实就是想告诉我们啊，使用这种策略解决了结构冒险。大家以后在考研真题当中，只要遇到五段式的指令流水线，只要遇到这种流水线。

那你都可以默认嗯，在这个系统当中，它的数据cache和指令cache 1定是分离的，你可以默认有这个条件。也就是不需要去考虑结构冒险的问题。好，那这是关于结构冒险的一个复习，反正考的比较少。我们只需要重点理解指令cache和数据cache相分离，可以解决结构冒险理解，这一点就可以。好，接下来我们再看数据冒险和控制冒险相关的题目要如何去分析？数据冒险的题目是最常考的，控制冒险啊，

考察的频率会更低一些。但这两类冒险通常都会结合MIPS指令序列来考察，不过大家不要慌，如果题目当中给了MIPS指令序列，那它一定会给注释。我们依靠注释一定可以理解每一条MIPS指令的含义。所以不用着急，那回到我们的主线剧情。数据冒险和控制冒险应该如何分析呢？给大家一个图。在这页PPT当中，我给大家梳理了题目当中可能会出现的四类指令。分别是load指令store指令转移类指令和运算类指令题目当中只会出现这四类指令。在这页PPT当中，给大家梳理了每一类指令，

在if段ID段EX段m段和WB段。分别做了什么事情，重要的信息都为大家整理到这个PPT当中了，好，现在我们先不看这页PPT里边的细节。我们现在要聊的是如何分析数据冒险和控制冒险。在这个图当中，橙色的部分就是有可能会导致数据冒险的地方，而绿色的部分。只有这个地方。它可能会导致控制冒险。我们来快速回忆一下数据冒险和呃控制冒险。首先是数据冒险。假如说有两条指令需要执行，分别是I1，

I2，I1。这条指令会把九八五这个数。写到寄存器r1当中，而I2这条指令会把r1的值啊。加一，然后再写到r2这个寄存器当中。那执行完这两条指令之后，我们所期待的正确结果应该是r1里边的值是九八五。r2里边的值是九八六。对于一个没有引入指令流水线的系统，这两条指令依次执行，最终就会得到这样的一个结果。但是当我们引入指令流水线之后，情况就变得更复杂了，

每条指令的执行会被分为if。idexm和WB这样的呃五个分段。当第一条指令执行到ID这个段的时候，第二条指令就可以啊，同时执行if段。然后是ID。EX m.wb，好，现在问题发生了，任何一条指令对寄存器的写操作都一定是在WB这一段。所以第一条指令要把九八五这个数写到寄存器r1里边，需要到第五个时钟。才完成了这个写操作。另一方面，

一条指令对寄存器的读操作一定是在ID这一段。所以I2这条指令它在第三个时钟就把r1里边的值给读出了。但是在第三个时钟之内，其实r1的呃正确的值还没有被写进去。那这就导致了所谓的数据冒险。那如果要解决这个数据冒险，最简单的方法就是让I2这个指令的ID段。让它阻塞三个时钟。阻塞三个时钟，等待I1这条指令，把寄存器的值写回之后。再去读寄存器，这样就可以解决数据冒险。好，那回到刚才这个总结图，

我们说橙色的部分可能会导致数据冒险，所有的这些指令当中。有可能会写寄存器的指令只有两类，一类是漏的指令，一类是运算类的指令。无论是哪一类指令写寄存器的这个动作，一定是在第五个段WB这个段。去做的。如果前一条指令写了某个寄存器，而后一条指令在前一条指令还没写回之前。就去尝试读同一个寄存器的值，那么就会发生数据冒险。所以在分析题目的时候，我们需要注意观察漏的指令和运算类指令有没有写某一个寄存器？如果发现了这种情况，

那么接下来我们就需要观察，在这两类指令的后面。后面的几条指令有没有去读同一个寄存器，如果有的话，那OK就可能会发生数据冒险。那一会我们会用真题来实践。好，接下来在这个图里边，我们说绿色的部分，这个地方可能会导致控制冒险。所有的指令当中，只有转移类的指令有可能会更改PC值，控制冒险的发生是这样的。呃，我们的指令是顺序执行的I1，

I2，I3那下一条要执行的指令。呃，是由PC所指向的。假设当前执行的是I1，这条指令下一条应该顺序执行I2，这条指令但是恰巧I1，这条指令。它是一条转移类指令，那我们知道转移类指令可能会更改PC值。那如果PC值改变的话，下一条应该执行的指令就不是I2这条指令。而应该是新的PC值指向的那条新指令。好，所以转移类指令可能会引起控制冒险，

那需要特别强调的是，转移类指令更改PC值是在第四个段进行的。所以当我们分析控制冒险的时候，重点关注转移类指令的第四个段，那同样的接下来，我们会用真题来演练。好，下面让我们一起来看一下如何去处理，让我们分析数据冒险的这类题目。考研当中，如果考到数据冒险，那么通常来说会有这样的两种考察方式呃，如果比较简单的题目，它可能只是让我们分析。是否存在数据冒险以及哪些指令对之间存在数据冒险，

让我们分析是否存在的问题。如果一个题目考查的想要更难一些，更深入一些，那么除了分析数据冒险是否存在之外。他可能还会考察这个数据冒险如何解决的问题。那一般来说，题目会呃明确的给出数据冒险的解决方法。最常见的方法就是硬件阻塞。那基于硬件阻塞的这种处理方法，当我们发现数据冒险之后，我们要知道。在什么时候应该硬件阻塞，应该阻塞多少个时钟？这个是我们需要明确的问题。那这是数据冒险的最常见的一种处理方法，

那还有一种啊，处理方法就是旁路技术，也就是转发技术。如果流水线当中引入呃转发技术，那么可以解决大部分的数据冒险，但是唯独不能解决由漏的指令。啊引起的这种lord use。数据冒险。那这个具体是什么意思？我们现在先不展开。我们现在先把转发技术这种解决数据冒险的方式，先把它呃抛在一边。因为考察转发技术，解决数据冒险的题目相对来说还是比较少。我们现在先重点解决两个问题，

第一，如何分析指令序列当中是否存在数据冒险。第二，如何使用硬件阻塞去解决数据冒险。我们先重点解决这两个问题。好，直接上真题，这是一六年的一个选择题，这题目说在无转发机制，没有转发机制的五段式啊，基本流水线当中。有一二三四这样的四条指令。问我们哪些指令队之间存在数据冒险？好，如何去分析呢？

这给大家总结了方法。呃，存在数据冒险的指令队，他们的核心特点是这一队指令。前面的那条指令写后面那条指令读了同一个寄存器。如果有这样的情况，那么就有可能存在数据冒险。好，具体来说，我们可以这么做，从第一条指令开始，结合注释观察。这条指令写了哪个寄存器？如果一条指令写了某个寄存器，那么我们就需要观察呃，

它后续相邻的三条指令是否读了同一个寄存器？如果发现此类情况，那么就说明存在数据冒险。如何判断一条指令是否写了某一个寄存器呢？嗯，很简单，我们只需要结合注释。去看一下这个箭头指向的这一侧是不是寄存器？比如一六年这个题嗯，第一条指令它的注释箭头指向了r1，那r1是一个寄存器，所以说明第一条指令写了r1。第二条指令他写了r5，第三条指令写了r4，第四条指令写了r5。

同样的道理，对于一九年这个题目，我们要判断每一条指令是否写了某一个寄存器，那只需要结合它的注释。看一下它的箭头指向的这个东西是不是一个寄存器？结合题目给的这些条件，我们知道rs 2 rs 3 rs 2这些都是寄存器。所以第一条指令它就是写了rsr，第二条指令它写了rs 3，第三条指令写了rs 2。第四条指令它的箭头指向的这个东西是嗯大m。他是写了主存。只不过这个主存的地址是用rt 2+0呃，也就是用这种寄存器间接寻址的这种方式去得到的。反正第四条指令它的箭头指向的是大m呃，

表示的是一个主存地址，所以第四条指令它并没有写。寄存器，而是写了主存。好，现在我们已经知道如何去判断一条指令是否写了某一个寄存器了，那回到刚才的这个总结。当我们确定一条指令，写了某个寄存器之后，接下来我们要呃观察后续相邻的三条指令。是否读了同一个寄存器？如果发现类似情况，就存在数据冒险。比如说一六年这个题目，第一条指令他写了r1这个寄存器，

那么接下来我们需要观察与之相邻的三条指令。是否读了r1这个寄存器？好，我们先来解释一下，为什么要往后检查三条指令？我们知道，在五段式的指令流水线当中。任何一条指令它写寄存器的动作，一定是在第五个段WB这一段，而任何一条指令读寄存器的动作，一定是在ID。第二个段，所以假设I1这条指令他写了某一个寄存器，那么这条指令。后续的三条相邻的指令，

它们的ID段都是在WB段写回寄存器之前。就会从寄存器里边读出数据。所以如果一条指令写了某一个寄存器，那么我们需要检查它后面的三条指令是否读了同一个寄存器？如果有这种情况，那么就会出现数据冒险，而如果是后面的第四条指令，那么。它的ID段，它的ID段肯定会在第一条指令的WB段之后。也就是说，即便第四条指令和第一条指令访问的是同一个寄存器，那这种情况下也不会出现数据冒险。当第四条指令读寄存器的时候，第一条指令肯定已经写完了。

好，因此我们需要观察的是，后续相邻的三条指令是否读了同一个寄存器？第四条指令之后的，我们就不用再检查了。那怎么结合注释去判断一条指令是不是读了某一个寄存器呢？很简单。除了被写的寄存器之外，注释里边出现的寄存器名就是这条指令需要读的寄存器。刚才我们已经知道了如何去判断呃一条指令，它是不是写了某一个寄存器只需要看？箭头一侧是不是直接指向了一个寄存器名？那箭头直接指向的寄存器名就是被写的寄存器，除此之外，在注释里边出现的其他寄存器名。

都是需要被读的。比如，对于第一条指令，它要写的是r1这个寄存器，它要读的寄存器是r2r3。那对于第二条指令，它要写的是r5这个寄存器，它要读的是r2r4这两个寄存器。好，再看一九年这个题目也是一样的，对于第一条指令他要写的寄存器是rs 2。那除此之外，在注释里边出现的其他寄存器名就都是需要读的寄存器，所以第一条指令需要读rs 0 rs 1。第二条指令要写的是rs 3，

那除此之外，在注释里边出现的其他的寄存器名就一定是要读的寄存器。所以第二条指令的执行，需要读rt 2这个寄存器。好，再看第四条指令，第四条指令箭头指向了大m，也就是说，这条指令它是要写主存，不需要写寄存器。好，所以第四条指令它不需要写任何一个寄存器，那么在这条指令的注释当中出现的所有的寄存器名。都是它需要读的寄存器，比如rt 2是需要读的rs 2，

也是需要读的。好，现在我们已经知道了怎么结合注释去判断一条指令他写了哪个寄存器，读了哪些寄存器？接下来，我们回到对题目的处理。我们刚才说，当我们发现一条指令写了某个寄存器之后，我们要观察后续相邻的三条指令是否。读了同一个寄存器，如果有此类情况，那么就存在数据冒险好，回到一六年的这个题目。第一条指令他写了r1，那我们要观察后续相邻的三条指令有没有读同一个寄存器？

第二条指令要读r2r4，第三条读r5r3，第四条读r2r6。都没有读r1，所以第一条指令和其他指令之间不存在数据冒险。好，接下来观察第二条指令。第二条指令需要写r5。那我们需要观察后续的三条指令是否需要读r5。当然，对于这个题目来说，它后面呃只有两条指令了。那显然，第三条指令它需要读r5。所以I2和I3之间存在数据冒险。

因此，这个题目的答案选择b。好，我们可以继续往后分析哈，同样的方法，接下来看I3这条指令会不会引起啊？数据冒险。呃I3这条指令它写了r4，那我们需要观察后续相邻的三条指令当中。有没有哪条指令读了r4？那显然它后面的这条指令并没有读r4。所以I3这条指令写r4的动作并没有引发数据冒险，所以I3I4之间不存在数据冒险。好，这是一六年的这个题目，

现在再回来体会这句话，我们分析数据冒险的方法是。从第一条指令开始，结合注释，先观察这条指令是不是写了某一个寄存器。如果他写了某个寄存器，那么就观察后续相邻的三条指令是否读了同一个寄存器。如果有这样的情况，那么这两条指令之间就存在数据冒险。那从第一条指令开始，第一条指令分析完了，再分析第二条，接着第三条，第四条依次往后去推进。这样我们就可以用一种很清晰的思路去分析数据冒险。

好，再看一九年的这个题目，同样的五段式的指令流水线，然后s0s1s2s3和t2表示的是寄存器编号。嗯，它里边的寄存器就是用一个大r+1个中括号里边，再写上寄存器编号，用这样的方式去表示的。问我们下面这些指令对之间不存在数据冒险的事，好，我们试着用刚才总结的方法来分析一下哪些指令之间可能会存在数据冒险。首先，第一条指令它写了s2这个寄存器。观察它后续相邻的三条指令当中有没有哪条指令，读了s2这个寄存器。

好，从后面这几条指令的注释当中，快速的找出rs 2。这里有一个，这里有一个，这里有一个。好，首先看这个rsr，它是由。箭头直接指向了这个rsr，所以说明呃。这条指令它是要写rsr。好，因此这个东西我们不看好，那除了写的这个呃寄存器之外，

这条指令它也需要读rsr。所以第一条指令要写rsr。第三条指令又要读rsr，所以第一条指令和第三条指令之间是存在数据冒险的。同样的第四条指令也要读rsr，所以第一条和第四条指令之间也存在数据冒险。那题目问我们的是不存在数据冒险的指令队是什么啊？刚我们说了第一条和第三条之间是存在数据冒险的。我们把a答案排除。好，接下来再分析第二条指令引发的数据冒险，第二条指令它要写rs 3。那观察后面这些指令，只有第三条指令会读rs 3，所以第二条指令和第三条指令之间存在数据冒险。

b答案排除。c答案I2和I4那可以看到呃第四条指令，它并没有读rs 3。所以I2和I4之间不存在数据冒险，因此这个题目应该选择c选项。好，最后我们再来看I3这条指令有没有引发数据冒险？这条指令要写rsr，那它后面的这条指令。I4又需要读同一个寄存器，所以I3和I4之间也存在数据冒险。所以d答案也排除，因此这个题目最终选择c选项。好，现在我们已经知道了如何去分析一堆指令序列是否存在数据冒险。

那很多选择题，他就只考到这一层，但是在很多大题当中，他可能会考得更深一些，会让我们尝试着用啊，比如硬件阻塞的方式去。去解决这个数据冒险，那这就要求我们能够画出多条指令的这个指令流水线嗯，它的五个段分别是怎么安排的？在什么地方需要硬件阻塞？好，我们尝试基于刚才这两个题目啊，来练练手。接下来，我们会尝试画出这些流水线的执行情况。

好，首先理想情况下啊，如果指令流水线没有发生任何冒险，不需要硬件阻塞。那么，这几条指令的五个段都可以顺利的依次执行。但是，由于存在数据冒险呃，因此我们需要使用硬件阻塞的方式去解决数据冒险，那解决数据冒险的核心。就是需要把读寄存器的那条指令，它的ID端让它阻塞几个时钟。然后确保它的ID段在写寄存器的指令WB段之后，也就是要先保证。数据已经写回了，

然后我再去读。那经过之前的分析，我们知道啊，这几条指令当中只有I2和I3之间存在数据冒险。I2需要去写r5。而I3需要去读r5。好，因此我们就需要保证I3的ID段。需要在I2的WB段之后。好，所以I3的ID端，我们需要让它硬件阻塞啊，几个时钟，最终要确保。I3的ID段在I2的WB段之后。

所以我们可以让I3的ID段阻塞三个时钟。那这样我们就可以保证，只有I2写回寄存器之后I3才会去读寄存器。这就解决了数据冒险。那另一点需要注意的是，当I3没有进入到呃ID这一段的时候，I4是不可以进入，if这个段的。因为I3在阻塞的这呃几个时钟之内，其实它一直占用着if这个段的硬件资源。只有它真正的进入到下一段ID段，那么if这个段的硬件资源才会被释放给。下一条指令使用，所以下一条指令的if段千万不能把它安排在这个位置。只能安排在这个位置。

好，那这就是这四条指令的执行，那整理一下就是这个样子。第二条指令在WB这一段写r5写回之后，第三条指令。才可以进入ID端去读r5。在没有转发机制的这种流水线系统当中，我们就只能采用硬件阻塞这种方法去解决数据冒险。好，接下来再尝试着把一九年这个题目的这几条指令也安排一下，它的流水线应该如何执行？通过之前分析，我们知道I1和I3之间存在数据冒险。I1要写s2I3需要读s2。好，

因此我们要确保I3的ID段必须在I1的WB段之后。I3的ID段要在WB段之后好，那么我们把I3的ID段。整体的往后移。那这就需要添加两个时钟的硬件阻塞好，再继续往后看好，除了I1I3。那I2和I3之间也存在数据冒险。I2需要去写s3。I3需要读s3，所以我们还需要确保。I3的ID段需要在I2的WB段之后。要确保I2写回完成之后I3再开始读寄存器好，所以我们还需要把I3的这个ID段再往后。再呃移一个时钟。

也就是说，中间这需要阻塞三个时钟。好，那除此之外，I3和I4之间也存在数据冒险。I3需要去写s2I4，却要去读s2。所以I4的呃ID段需要在I3的WB段之后。要确保I3写回完成之后I4才去读好，因此我们要把I4的ID段整体的往后阻塞几个时钟。也就是变成这个样子好，那这样我们就安排好了这几条指令的执行，再次强调。当前，一条指令进入到下一个阶段之前，

它会一直占用上一个阶段的硬件资源。因此，对于I3和I4来说，I4的这个if段只能在I3进入ID段之后。才可以开始，而不能在这个位置开始，因为在这个位置，其实I3还在占用if段的硬件资源。好，所以I4的这个if段应该放在这个地方。那整理一下，就是这个样子，这个题目并不要求我们分析这个指令流水线的执行过程啊，我们只是作为训练来带大家练练手。因为类似这样的题目，

其实在大题里边会有考察，比如二零一二年的第三小问和第四小问。这两个小问就是啊，要求你去分析这些指令流水线，它的各个段应该如何去安排？以及发生硬件阻塞的这些地方，为什么会阻塞？是要让你分析这些问题好，那由于一二年的这个题目，它的分析方法和我们刚才啊分析的这个过程如出一辙，所以这儿就留给大家自己去做练习。如果做不明白，那可以参考王道的呃答案或者配套的这个习题讲解视频。那这就不再追述。好，

那这是五段式的指令流水线这个部分啊，最常考的一个命题角度，也就是考数据冒险。首先，我们要能够根据嗯指令序列去分析出是否存在数据冒险。其次，我们要掌握如何用硬件阻塞去解决数据冒险。具体来说，就是要学会去安排硬件阻塞的这个时钟。核心就是要让读寄存器的那条指令，它的ID段。要安排在写寄存器那条指令的WB段之后。好，那这是数据冒险这个部分最常考的东西，采用转发技术去解决数据冒险的这个问题，

我们现在先不讨论。好，接下来我们看如何去分析控制冒险以及如何处理控制冒险。那控制冒险的分析和数据冒险相比，反而会更简单，因为控制冒险是因为PC值改变。导致的，而唯一会引起PC值改变的指令就是转移类的指令。当然，call return这些啊，函数的调用返回这类的指令，它也属于转移类的指令，因为它也会改变程序的执行流。总之，这儿我们把会改变PC值的指令都统称为转移类的指令。

一般来说，在考研当中，大家更有可能见到的是条件转移类指令。对于call和return这种指令，在指令流水线里边考察的概率不大，因为它会更复杂一些。好，所以我们重点关注的是条件转移类的指令。由它引发的控制冒险。那给你一个指令序列，你要分析它有没有发生控制冒险很简单，就是去检查题目给的指令序列当中。是否存在转移类指令？如果有，那么OK会引起控制冒险。

那控制冒险的解决方法也很简单，只需要把转移指令的后一条指令。的if段硬件阻塞三个时钟就可以了，好来解释一下，对于转移类指令来说。它的执行同样分为ifid。exm和WB这几个分段。那我们知道程序是顺序执行的，因此执行转移类指令的时候。程序计数器PC会自动的指向后一条指令。那同样的后一条指令，也会被分为ifidexmwb。这五个分段好，现在问题来了，转移类指令的执行有可能会修改PC的值。

而PC值的修改是在m这个段。在这个端进行的。所以如果我们能让后一条指令的执行阻塞三个时钟，也就是让它的if。取止这个段。在转移类指令的第四段之后再开始，那这样我们就可以保证后一条指令取指的这个动作。它一定是取到了啊，一一条正确的指令，在修改完PC值之后再去取指。因此，转移类指令导致的控制冒险处理起来很简单，只需要让后一条指令。阻塞三个时钟，让它阻塞三个时钟就可以。

好来看一个题目，我们看二零一四年的这个大题的第四个小问。啊，第四个小问考的就是五段式指令流水线。它这给了一些指令序列，这个程序它要实现的是一个循环的动作。整个循环有六条指令组成，其中第六条指令嗯，可以看一下，它是一个。条件转移指令。当满足r2不等于r6的时候。转向loop也就是跳转到这个位置，继续往后执行，所以这是一条条件转移类指令。

当CPU执行第六条指令的时候，PC值会自动的加一指向第七条指令。但是第六条指令的执行，有可能会让PC值PC值重新指向第一条指令。所以，当我们在执行第六条指令的时候，我们不能着急着把下一条指令给取出。此时的PC可能指向的是一条错误的指令好，所以你会看到在第四小问当中。他说这个系统它采用五级的指令流水线。分支指令的执行均引起三个时钟周期的阻塞，那这个题目的条件其实跟我们刚才说的是同一个意思。只要执行转移类指令，那么就呃，让它引起三个时钟周期的阻塞，

这样的话就可以确保。下一条指令在取指的时候。PC的正确值已经被修改完了。好，所以题目给的条件是想表达这个意思。紧接着，他问哪些指令的执行会，由于数据相关而发生流水线阻塞。哪条指令的执行会呃发生控制冒险？好，我们先回答第二个问题，第二个问题最简单，会引发控制冒险的一定是。会修改PC值的指令。那在这个题目当中就是这一条条件转移指令叫做bne。

好，再看前面这个问题，哪些指令的执行会因为数据相关而发生流水线阻塞？好，首先看第一条，指令他写了哪个寄存器箭头指向r4，说明他写了r4。接下来，关注与之相邻的后面三条指令有没有读r4的。那可以看到呃第二条指令和第三条指令都读了r4，所以我们需要确保第二条指令和第三条指令的ID段。一定要在第一条指令的WB段之后，因此我们的流水线就会变成。这个样子，第二条指令要阻塞三个时钟。

以确保它的ID段是在第一条指令的WB段之后。当第二条指令阻塞三个时钟之后，第三条指令的ID段就天然的呃挪到了第一条指令的WB段之后。所以第三条指令和第一条指令之间就不会发生数据冒险了。好，这是由第一条指令引发的数据冒险。接下来看第二条指令有没有引发数据冒险？第二条指令他写了r4这个寄存器，观察后续的三条指令当中。只有第三条指令是需要读r4的，因此我们需要保证第三条指令的ID段在第二条指令的WB段之后。因此，第三条指令的ID段需要被阻塞三个时钟，以确保第三条指令的ID段在第二条指令的WB段之后。好，

这是由第二条指令引起的数据冒险。接下来看，第三条指令有没有引起数据冒险？第三条指令写了r5这个寄存器。观察后续的三条指令。啊，是需要读r5的，只有指令四，所以我们需要保证指令四的ID段，在指令三的WB段之后。因此，指令四的ID段需要被阻塞三个时钟。变成这个样子。这样就可以确保第四条指令的ID段在第三条指令的WB段之后。好，

接下来看第四条指令有没有引发数据冒险？第四条指令写了r1这个寄存器。那后面的这些指令当中没有任何一条指令，需要读r1，所以第四条指令不会引发数据冒险。好，接下来再看第五条指令，第五条指令他需要写r2这个寄存器，那么观察他后面的这个指令。需不需要读r2从这个呃注释里边可以看到r2这个寄存器的名字，那么说明后面这条指令。肯定需要使用到r2里边的数据，也就是说一定会读r2这个寄存器，所以第五条和第六条指令之间，它们是存在数据冒险的。

我们需要确保呃第六条指令的ID段，在第五条指令的WB段之后。因此，第六条指令的啊ID段，我们也需要让它阻塞三个时钟。这样就可以确保呃前一条指令把寄存器的值写回之后。后一条指令再去读那个寄存器。好，所以I1到I六会因为数据相关而发生流水线阻塞的是。I2，I3，I4和I六二三四六这几条指令。会因为数据相关而发生流水线阻塞。好，再来看最后一个小问。

问，为什么指令一的执行不会因为指令五的数据相关而发生阻塞呢？最后这个小问是有难度的嗯，需要理解它的这个循环实现的原理。我们来看一下，为什么说第一条指令和第五条指令之间存在数据相关呢？我们刚才只分析了这些指令顺序执行的情况，但事实上，由于第六条指令。它是一个转移类指令，所以第六条指令执行完之后。有可能会重新回到第一条指令，对吧？也就是说I6之后，紧接着可能需要执行I1这条指令。

那这样的话I1和I5之间不就出现了I5在前I1在后的这种关系，如果I5。写的寄存器刚好是I1需要读的寄存器，那么它俩之间是不是就会存在数据相关的问题？好，那来看一下它这个代码的注释I5这条指令，它需要写r2寄存器。I1这条指令它需要读r2这个寄存器。好，那这就意味着二者之间存在数据相关，我们必须保证I1这条指令的ID段。一定要在I5这条指令的WB段之后才能执行。那I5的WB段在这个位置，同时I6它是一个条件转移类指令条件转移类指令。题目告诉我们，

这种转移类的指令，也就是分支指令的执行，会引起三个时钟周期的阻塞。正常来说，只要I6这条指令进入到ID这一段，那么后一条指令就可以顺利的进入if段。ID段EX段。正常来说是这样的，对吧？但是由于I6第六条指令它是一个分支指令。转移类指令，所以它的执行会固定的引起啊，三个时钟的阻塞。也就是说，后一条指令的if段会推后三个时钟再开始执行。

也就是这个地方，我们需要阻塞三个时钟。因为对于转移类指令来说，它是在第四个段m这个段去修改PC值的。而只有PC值修改完成之后，我们才可以确定下一条指令真正的地址在什么地方。这样才可以去执行后一条指令的if段。也就是取止的这个动作好，那可以看到经过这三个时钟周期的阻塞。后面这条指令的ID段已经远远落后于I5那条指令的WB段。对吧，所以虽然二者之间有数据相关，但是因为固定三个时钟周期的阻塞。我们可以确保这两条指令之间不需要再因为数据相关而发生阻塞。好，

所以这是最后一个小问。好，那这是对控制冒险的分析和处理。很简单，只需要去找到底有没有转移类指令，如果找到了转移类指令，那么处理的方法可以简单粗暴一些。就是把后一条指令的if段硬件阻塞三个时钟即可。
