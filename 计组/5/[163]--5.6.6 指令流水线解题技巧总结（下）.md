好的，在之前的视频中，我们探讨了如何去处理数据冒险相关的题目。在考研当中。数据冒险的处理通常是采用硬件阻塞的方式。那有少部分的题目可能会涉及到采用转发技术或者叫旁路技术去处理数据冒险。不过，大多数的题目肯定不会采用转发技术，比如说一六年的这道题，它明确的告诉我们，在没有转发机制的五段基本流水线当中。没有采用转发技术。再比如一二年这道大题，它也明确告诉我们没有采用转发技术。再比如一四年这个题目也明确的告诉我们，

没有采用任何转发措施，没有采用转发技术。好，所以大多数情况下，只要考察数据冒险，那通常都是没有采用转发技术。但是偶尔会有一些题目，可能会考察采用转发技术去解决数据冒险，比如说二零二三年的这道真题。他说，在一个呃采用五段式流水线的这个处理器当中，给了这样的几个指令序列。呃说如果采用转发技术去处理数据，冒险采用硬件阻塞的方式去处理控制冒险。那么，

在指令I1到I4执行的过程当中，发生了流水线阻塞的指令有。好，首先我们来分析一下哪些指令对之间会存在这个数据冒险。首先，观察指令一指令一写了s2这个呃寄存器。那指令二需要读s2这个寄存器，所以指令一和指令二之间存在数据冒险。好，再看指令二，它需要去写s3这个寄存器，而后一条指令又需要去读s3这个寄存器。所以指令二和指令三之间也存在数据冒险。好，另外，

由于指令三它是一条转移，指令是一条转移指令，而我们说过转移类的指令，它有可能会改变PC值。所以第三条指令的执行可能会引起控制冒险。好，接下来梳理一下这几条指令的执行。在理想情况下，如果没有发生数据冒险和控制冒险，那么这几条指令应该是呃，可以呈现出一个完美的流水线。但是现在第一条指令和第二条指令之间存在数据冒险，也就是说我们需要等待第一条指令把数据写回之后。第二条指令才能去读数据，对吧？

那常规的对于数据冒险的处理，我们可以采取硬件阻塞的方式，让后一条指令硬件阻塞三个时钟。以确保前一条指令把数据写回寄存器之后，第二条指令才去读寄存器。那这是采用硬件阻塞的方式去处理数据冒险的方法。也是我们最熟悉的一种方法。那对于这个题目来说，他特别强调说啊，在这个系统当中采用了转发技术。也就是旁路技术去处理数据冒险好，旁路技术是怎么去解决数据冒险的呢？我们一起来回顾一下任何一条指令，都是在ID这一段去寄存器里边读数据。那通常来说EX这一段都是呃交给aru。

算术逻辑单元去运算取出来的那个数对吧？等EX段执行结束之后。就得到了计算的结果，比如说对于第一条指令来说，它是一个加法指令。这条指令做的事情是把s1和s0这两个寄存器呃的值读出来。读出来之后，进行一个加法运算，然后把加法运算的结果写回s2这个寄存器。所以这条加法指令在EX段执行结束之后就得到了加法的结果，对吧？就得到了加法的结果，只不过这个加法的运算结果需要等到WB第五个段的时候才会被写回。才会被写回s2这个寄存器。好，

接下来对于后一条指令来说，它需要基于上一条指令的运算结果去进行进一步的处理。那由于采用了转发技术，我们可以在上一条指令得到加法的运算结果之后。就直接用一条呃特殊的线路，把这个加法的运算结果转发给呃后一条指令的这个aru。也就是说，对于I2这条指令来说，虽然它在ID这一段取得的rs 2的值是错误的。但是，由于采用了这个呃转发技术，因此它在EX这个段。运算的时候使用的这个数据。其实是正确的。因此。

I2这条指令它不需要有任何的流水线阻塞。好，因此如果题目当中明确的告诉我们采用了旁路技术，或者说转发技术。那么，绝大多数的数据冒险都不需要呃，产生硬件阻塞。就像刚才分析的这种情况一样，不用增加硬件阻塞，也可以去解决这个数据冒险。这是转发技术带来的一个好处。好，这是第一个结论，转发技术可以解决大部分的数据冒险。接下来需要强调的是，

转发技术唯独不能解决的是由漏的指令。写寄存器引起的这种啊，我们把它称为load use数据冒险。好，这点值得解释一下呃，我们知道漏的指令，它的作用是把数据从内存。读到寄存器里边。那什么叫load use数据冒险呢？load指令把数据加载到寄存器里边，紧接着下一条指令就需要去使用。这个寄存器里边的值，这就是所谓的load use数据冒险。那为什么转发技术不能解决这种数据冒险呢？我们来结合二三年的这道题继续分析。

在这个题目当中I2这条指令，它是load指令。会从内存里边呃读出数据，然后把数据写回rs 3。那在漏的指令后面紧跟的呃，这条指令它又会使用到rs 3这个寄存器里的值。所以I2和I3之间就形成了load use数据冒险，那我们知道load指令从内存里读数据。这个动作是在m段完成的，然后在WB段会把从内存里读出来的这些数据写回寄存器。因此I2这条指令它要m这个段呃，结束之后才可以从内存里获得呃，最终的这个数据。然后在WB这个段再把数据写回rs 3好，另一方面相邻的后一条指令。

需要在EX段就去使用load指令读出来的这个数据。因此，这个EX段必须安排在上一条指令的m段之后，所以我们不得不让EX段阻塞一个时钟。那阻塞一个时钟之后就可以使用转发技术，从上一条指令的m段，把正确的数据转发给呃后一条指令的EX段。好，所以对于这种比较特别的load use数据冒险，即便引入了啊，转发技术也没办法去完美的解决这个流水线阻塞的问题。好，我们来总结一下，在引入了转发技术的这种流水线当中，大多数的数据冒险是不需要阻塞的。

用转发技术就可以直接完美的解决，但是对于load use这种比较特殊的数据冒险，也就是load指令。写某一个寄存器相邻的后一条指令，又要读同一个寄存器，发生这种类型的数据，冒险的时候。后一条指令的呃EX段必须阻塞一个时钟。好，那这个题目还没有完成呃，刚才我们分析了前三条指令的这个流水线情况，接下来我们看第四条指令。由于第三条指令是条件转移类指令。当这个条件不满足的时候，我们的指令会顺序的执行后一条，

也就是I4这条指令。而如果这个条件满足的话，那么I3之后有可能需要执行的就是I5这条指令。好，那么对于转移类指令都是在m这个段才去更新PC值。那I3的后一条指令是，在if这个段根据PC值的指向去取指令的。因此后一条指令的这个if段，就不得不阻塞四个时钟。把if段安排在上一条转移指令的m段之后一个周期。这样才可以确保我们取到的是正确的一条指令好，所以I3引发的这个控制冒险采用硬件阻塞的方式去处理的话。就是把呃后面这条指令的if段阻塞四个时钟。好，所以这个题目当中发生流水线阻塞的就应该是I3这条指令以及I4这条指令。

因此，这个题目选择c选项。那稍作整理就是这个样子，大家可以暂停来感受一下。通过这个题目要理解为什么load use这种啊？特殊的数据模型没办法用转发技术完美的解决。好的，那么这就是指令流水线这个部分呃，最常考的一些东西。
