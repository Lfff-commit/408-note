好，这节课我们探讨一个呃很常考的要点CPU当中的寄存器，我们要记住最好是理解哪些寄存器是用户可见的。哪些是用户不可见的？所谓用户，可见还是不可见，这个取决于汇编，语言，程序员能不能用汇编，语言，指令去直接的读，或者写某一个寄存器。如果可以直接去，这么操作，那么就是用户可见的，

而如果不能用汇编语言去直接操作的寄存器，就是用户不可见的。好首先来看用户可见的寄存器。通用寄存器组是用户可见的，以及有一些专用的地址寄存器，比如说基址寄存器，变址寄存器以及堆栈指针寄存器，都是用户可见的。也就是说，我们可以用汇编语言指令去直接的读写这些寄存器的内容。我们以叉八六的CPU为例。在叉八六CPU当中，这里罗列的八个寄存器都被归为通用寄存器。它们的名字是eaxebx呃，然后什么esiebp等等。

那所有的这些通用寄存器，我们都可以用move指令去完成读或者写。比如说把一个立即数五，把它move写到某一个通用寄存器当中，这八个寄存器都可以去这样的写。另外，也可以进行读，比如说同样用move指令把任意一个寄存器里的呃值写到主存里面。那这个动作就涉及到了毒寄存器，对吧？好，所以我们说通用寄存器组是程序员，是用户可见的，我们可以显式的用汇编语言指令去读写。好，

再看下面这提到的几个专用寄存器，基质变质和堆栈。在叉八六架构里边ebx这个寄存器，它会兼职做基质寄存器，而esi和edi会兼职做变质寄存器。另外，ebp和esp会兼职做堆栈寄存器好，那么刚才我们都说了，这些寄存器我们可以直接用move指令去读，或者去写其中的内容，对吧？那相应的，我们是不是也可以说基质寄存器变质寄存器和堆栈寄存器？我们都可以用汇编语言去读写。换句话说，

就是这三类寄存器也是用户可见的，对吧？好，接下来程序计数器PC也是用户可见的。我们可以用转移类的汇编语言指令或者函数调用类的汇编语言指令call return。可以用这些指令去直接的改写PC的值。所以PC对我们用户是可见的。好，最后还有一个用户可见的寄存器叫做标志寄存器，也可以称为psw程序状态字寄存器。我们不可以用像move指令去直接的修改标志寄存器里边的值，但是这个寄存器里边的嗯，比如说ofcfzf这些标志位。都会受到aru的运算结果的影响，所以一般来说，

我们把标志寄存器也归类为用户可见的寄存器。例如，我们可以用compile指令去做减法，那减法的运算结果会产生ofzfsf这些标志位。这些标志位的信息就会被写到标志寄存器fr当中。另外，当我们使用条件转移类指令的时候。相当于我们又在间接的读fr寄存器里边的这些标志位的信息。好，所以标志寄存器是用户可见的。接下来再看几个用户不可见的寄存器。首先是指令寄存器ir。我们程序员可以用汇编语言操作的是PC寄存器的值ir指令，寄存器里边的值是呃CPU的硬件，根据PC的指向。

去取出指令之后，把这个指令放到ir寄存器里的，虽然我们可以影响PC值，从而间接的影响ir的值，但是几乎所有的教材都会告诉我们说。指令寄存器是用户不可见的。好，记住这个结论，另外mar和mdr寄存器呃，我们也不能用像move指令这种去设置它们的值。这两个寄存器里的值只能是CPU在仿存的时候自动的去呃读写去使用的。所以它也是用户不可见，最后还有一个寄存器液表基质寄存器。操作系统中有多个程序在并发的执行，每个程序或者说每个进程都会有各自的页表，

那么页表基址寄存器就是指向了当前正在执行的这个进程的。页表的起始地址。那这个寄存器的值，我们作为普通用户也不能去读或者写。它是操作系统在切换进程的时候，用特权指令去设置的。所以，页表机制寄存器对于用户同样不可见。好，那以上就是本节课的所有内容。
