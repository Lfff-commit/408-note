各位同学大家好，在这一小节中，我们会补充一种大家在考试的时候经常会遇到的一种指令，流水线的类型就是经典的五段式的指令流水线。那上个小节当中，其实我们已经对五段式的指令流水线有过初步的分析，分为取止阶段。然后译码阶段，译码阶段，除了译码之外，还会负责把啊当前这条指令所需要用到的一些操作数，把它取到这个段的锁存器里面。然后第三个阶段就是执行阶段，进行相应的运算，第四个阶段就是仿存阶段，

第五个阶段就是写回阶段，就是把最终的运算结果写回通用寄存器组。那上个小节中，我们强调过这样的两个问题。通常来说，为了方便我们对流水线的安排，因此我们对每一个段的这个耗时。通常会向耗时更长的这个段靠齐，比如说这五个段的耗时各不相同，但是为了方便流水线安排，那么我们都会把它们安排为呃100纳秒这样的一个时间周期。那在理想的情况下，每个机器周期或者说这样的每一个功能段，只需要消耗一个时钟周期就可以好，那经过上一小节的学习，

不知道大家会不会有这样的疑问？我们之前学习过的那些指令，是否都需要经过这样的五个功能段呢？那这个小节当中，我们会给大家介绍考试当中常见的五个类型的指令。分别是运算类指令load，指令store，指令条件转移和无条件转移，这样的五类指令我们来详细的分析这五个类型的指令如何根据这样的五个功能段。来完成他们相应的工作好，首先看运算类的指令，那这我们给出三个例子，加法指令当中，我们可以指定。我们要相加的两个数分别来自rs和rd这样的两个寄存器，

那实现的功能呢？就是把这两个寄存器的内容相加，然后再存回rd，这个寄存器。对了rs和rd这样的寄存器，命名规则是很常见的。rs通常指的是源操作数的一个存放的寄存器，因为源是source。你的操作数的来源是哪？然后rd指的是目的操作数d，这个d指的就是destination啊，你的操作数运算结束之后。应该把它存放到什么位置？如果其中一个呃，那个寄存器的名字，

我们把它命名为rd，那么通常来说就是把两个寄存器的内容相加之后再给存回rd。因为d是destination，我们操作数存放的目的地。好，另外在加法指令当中，我们也可以指定。用某一个寄存器的值和某一个力计数进行相加，所以你看我们这儿把原操作数用力计数的形式来表示。之前我们说过，如果在汇编语言当中，前边加了一个井号，后边加了一个数字，那么就表示这是一个立即输。然后右边的rd同样指的是目的操作数，

我们要把最终运算结果存放回rd所指向的寄存器当中，所以它所实现的功能就是九九六+rd里边的内容，然后再存回rd。好，再来看一个算术左移指令啊，这样的一条指令指明了寄存器rd，那么这条指令所实现的功能就是把rd这个。寄存器里边存放的内容，把它算术左移两位，然后再存回rd，这个寄存器当中。也就是说，这个算术左移指令它只包含了一个操作数好，那我们来看一下，对于这种运算类的指令，

如何把一条指令的工作分在不同的阶段来执行呢？那首先第一个阶段取齿阶段，那这个阶段自然不必多说，就是要根据PC就是程序计数器所指明的位置。去指令cache当中，指令cache当中，去取出指令，取出当前的这条指令，然后把这条指令放到if段的锁存器里边，也就是放到这个位置。好，接下来第二个阶段ID就是译码的阶段，这一阶段要完成两个事，第一就是要对指令的操作码进行译码。第二，

更重要的是还需要把呃当前的这条指令所需要用到的操作数。操作数把它取到ID段的锁存器里边，那可以看到ID段有三个锁存器AB，还有这个imm。我们之前说过imm存放的就是立即数immediately，就是立即的意思嘛。所以对于我们刚才所列举的这三条运算类的指令来说，第一条加法指令，这条指令是把两个寄存器里的内容进行相加。所以对于第一条指令来说，就是要把两个操作数分别的放到a和b这两个锁存器里边。而第二条指令同样也是一个加法指令，但是其中一个操作数是一个立即数。因此，第二条指令的这个ID功能段会把其中一个目的操作数目的操作数先把它放到a这个。

锁存器里边，然后再把例计数指令里边指明的这个例计数九九六，把它放到imm这个锁存器里边。好，那对于第三条指令也是类似的，只需要把这一次要进行左移运算的那个操作数，把它从通用寄存器组。取到这个锁存器a就可以好，所以对于运算类的指令来说，在指令译码阶段都需要把呃下一个阶段所需要用到的操作数把它放到ID段的。这几个锁存器里边。那到底要放到哪个锁存器里？根据呃不同的指令，会有一些区别。好，

那现在准备好了，操作数就可以进入第三个阶段，就是啊，执行的阶段，在这个阶段aru算术逻辑单元。会根据上一个阶段得到的呃，这个操作数的信息进行运算，运算的结果把它放到EX段的这个锁存器里边。运算结果放到这个地方好，然后接下来是不是应该进入第四个阶段就是仿存的阶段，但是。我们啊，对于risk精简指令集系统来说，所有的这些运算类的指令。运算的两个操作数一定是直接来自于啊某一个寄存器，

或者也有可能是用力计数来进行运算。然后这个运算得到的结果一定是存回某一个寄存器，而不可能直接存回主存。因此，对于精简指令集的系统来说，在第五个阶段就是仿存的阶段，运算类的指令是不需要做任何事情的，我们不需要把结果。写回主存，所以在仿存的这个阶段，什么也不用做，但是这个时间是必须消耗的，因为之前我们说过，为了方便流水线的安排，我们会让所有的指令都统一的啊，

经历这样的五个阶段。即便其中的某个阶段是不需要做任何事情的，然而我们也需要消耗这一段时间好，那接下来就进入第五个阶段。写回阶段，我们可以看到刚才EX阶段运算的结果是放在了这个地方，对吧？那这儿有一条线。可以直接连到这个地方，也就是可以把啊第三个阶段运算的结果，直接把它放到第四个阶段，仿存阶段的这个锁存器这儿。然后接下来第五个阶段写回阶段是不是就可以把这个锁存器里边的内容把它给写回到某一个通用寄存器里面？好，那这就是。

运算类的指令，它的啊，执行过程。对于运算类的指令来说，缓存这个阶段是不需要做任何事情的。好，那什么时候需要缓存呢？接下来我们来介绍呃risk。精简指令及系统下很重要的两条指令，一个是load指令，取数指令，一个是store指令，存数指令。漏的指令会把内存中我们指明的这个数取出来，然后存放到rd这个寄存器当中。

那我们常见的load指令会有这样的两种描述方式，第一种描述方式。它所表示的意思是说，在rs这个寄存器的值的基础上，加上九九六。然后以这个相加的结果作为我们的有效地址去取出这个地址所对应的主存单元，里边所存放的数据把它取到rd寄存器当中。那为什么要有这样的奇怪的设定呢？因为之前我们是不是说过，我们每一个程序，每一个进程，它在内存里边存放的起始地址。起始地址都是不一样的，所以当我们要访问这个进程里边的某一个变量的时候，通常通常会采用基址寻址的方式。

也就是这儿的rs用rs这个寄存器来指明这个进程，它在内存当中的起始地址。那么，相对于它的起始地址而言，往后偏移九九六才是我们想要找的数据。好，所以这就是为什么要用rs里的内容，加上九九六进行一个偏移的原因，当然了，也有的地方会把load指令简写为下面这种形式。就是直接用mem表示，此次要访问的主存地址，把这个地址所对应的内容取到ID寄存器当中，但是大家要知道这个主存地址通常也需要进行这样的一次。地址变换才可以得到好，

所以这儿想强调的是我们在执行漏的指令的过程当中，也需要进行一个加法运算。好，那我们如何安排这条load指令的执行呢？首先第一个阶段都是一样的，根据PC的指示，从这个指令cache当中。取出当前要执行的这条指令，把它放到锁存器里。然后第二个阶段ID指令页码阶段，除了指令页码之外。我们还会把这条指令所指明的rs这个基质寄存器里的值基质寄存器里的值。把它放到锁存器a里面，这是基地址，另外还会把我们这个指令当中所指明的呃例计数就是九九六。

把力计数放到imm这个锁存器里边，好接下来进入第三个阶段，第三个阶段会把。我们的偏移量和基质进行一个相加，相加之后的结果把它放到EX段的锁存器里边。这样我们就得到了有效地址EA好，接下来第四个阶段仿存阶段，我们需要根据刚才得出的这个有效地址。从数据开始当中，从这个数据开始当中。取出我们想要的那个数把数，取出之后放到m这个功能段的锁存器里边也就放到这个位置。好，接下来第五个阶段写回阶段会把我们刚才取出的这个数。写回到目的寄存器rd里面。

好，那这就是load指令的一个执行过程，注意load指令也是必须经过运算这个阶段的，因为我们需要经过运算器的处理之后才可以得到最终的一个有效地址。好，另外上一小节我们强调过第四个阶段。它叫memory，就是访存。访问主存的一个阶段，但是这个地方我们把它写为data cache的原因是。我们在主存里的数据很大概率上可以在这个数据开始当中被命中。如果数据cache没有命中，那才会去进行缓存好，所以我们在这地方直接是画了一个data cache。因为cache的访问速度非常快，

我们之前说过呃，我们要保证每一个阶段的功能在一个时钟周期。一个时钟周期内，或者说一个节拍内就可以完成，那么显然如果在这个阶段对主存进行访问，那么一个时钟周期内肯定是完成不了的。所以，为了保证流水线的流畅工作，通常我们访问主存的时候大概率情况下都可以在data cache当中直接找到我们想要的数据。好，这是上一小节强调过的点好，另外刚才我们还说过一句话，在risk精简指令集系统当中。只有取数指令照我们介绍的这个取数指令，还有接下来要介绍的存数指令store。

这两个指令才可以访问主存，其他所有的指令在执行过程当中都不会直接访问主存。其他指令想要得到的数据一定是直接来自于某一个寄存器，或者就是指令当中直接包含了某一个立即数。那由于只有这两个指令可以访问主存，因此我们对指令流水线的设计就可以变得更简单，好，那接下来我们再来看下一条存数指令。存数指令store，它和取数指令load其实很像啊，来看一下我们这rs指明了一个源操作数。就是我们要把rs所指明的寄存器里的内容，把它存到主存当中。那存放的目的地址就是呃rd，再加上九九六，

这九九六是我们随便写的一个数字哈，九九六指的是一个偏移量rd，指的是一个基地址。那同样的也有的地方会把这个存数指令进行这样的简写，直接用m1m来指明，最终要存放的这个地址单元。但是同样的，大家要知道我们要得到最终存放的这个有效地址，也需要进行一次加法运算好，所以来看一下如何安排。store这条指令的执行第一个阶段，不用说了，取出这条指令，把它取到if段的锁存器里边。第二个阶段，

译码阶段，除了指令译码发现这是一条store指令之外，同时也需要把基地址。也就是这儿的rd把它存到锁存器a里边好，另外也需要把指令当中呃带有的这个偏移量把它放到imm这个锁存器里边。好，除此之外，store指令还指明了我们这次要存放的是哪一个数？这个数本来是存放在rs这个寄存器里的，所以我们还需要把rs。寄存器把我们要存的数先把它放到b，这个锁存器里边好，所以基地址放到了锁存器a。然后偏移量放到了这个imm锁存器里，然后我们这次要存的数放到了b这个锁存器里。

接下来就可以进入第三个阶段，运算的阶段，这个阶段需要根据基地址和偏移量。根据这两个值计算出我们啊，此次要存的一个有效地址EA。得到有效地址，另外还需要把我们要存的数通过上面这个绿色的这条线，把它从锁存器b。直接转存到锁存器store里边好，接下来第四个阶段缓存，我们就可以根据这个有效地址EA。写入我们现在要存的这个数据，那么同样的，虽然我们这儿是要访问主存，但是由于主存的数据在data cache里边大概率情况下都有副本。

所以我们可以把此次要存的数据把它存到data cache当中，随后再把data cache里的这个数据同步回主存。好，这就是第四个阶段要做的事情，那可以看到，对于store指令来说，我们只需要把啊，我们想要存的数据存回data cache里边是不是？就大功告成了，也就是说第五个阶段写回阶段不需要做任何事情。好，那这就是存数指令的一个执行过程，大家再捋一捋。那接下来我们进入下一种类型的指令条件转移，指令这个也是我们在考试当中有可能遇到的，

那这需要说明的是。转移类的指令，无论是条件转移还是无条件转移，通常来说，这一类的指令都是采用相对寻址。也就是相对于PC偏移多少这样的一个寻址方式好，所以来看一下我们这儿给出了两条啊，条件转移指令的例子。第一条指令beq第一个b指的是branch就是分支。然后EQ指的是一口相等的意思，也就是说，当我们指明的这两个寄存器里的数。相等的时候就满足了这个条件，就需要进行转移，那么应该转移到什么位置？

我们这用一个力计数来给出了偏移量。当这两个数相等的时候，我们要转移到的地址就应该是当前这条指令。加上指令字长。再加上偏移量乘以指令字长好，我们还是代入一个具体的数字，可能大家比较好理解，假如我们每一条指令。每一条指令都固定为四个字节，指令字长为四个字节，那么这个条件转移指令的呃，转移地址就应该是当前这条指令的地址。当前，这条指令的地址加上四。然后再加上偏移量，

比如说偏移量是九九六九九六，再乘以指令字长四把，这个值赋给PC。也就是说，我们这给出的偏移量，通常是指明了我们要往前或者往后偏移多少条指令。而从什么位置开始偏移呢？应该是从下一条指令的地址开始偏移。所以这个地方我们用PC先加了一个四好，大家可以暂停体会一下这个偏移量的啊，具体的含义。好，我们继续往后，刚才我们是说如果这个条件满足的话，那么我们会让程序的执行流偏移到这样的一个位置。

修改PC的值，而如果刚才这个条件不满足的话，那接下来要执行的程序就应该是顺序往后的下一条指令。所以这个地方只需要用当前指令的PC值，加上指令字长就可以。这样就可以找到下一条顺序的指令。那通常来说，我们PC的值自动加一，这个过程是在取指结束之后就会自动的完成的。好，这是相等的时候进行转移的一条指令，接下来这一条指令也很类似b，同样指的是branch分支。然后ne指的是not equal，当我们给出的这两个寄存器里的值not equal不相等的时候。

我们才会让程序的执行流进行改变，那么PC应该转移到的位置，同样的用上面这种方法来确定。好，这就不再赘述。好，那接下来我们要分析的是条件转移类的指令，在各个阶段需要完成什么样的工作，第一个阶段不用多说，所有类型的指令都是一样的。都是从这个指令cache当中取出当前的这一条指令，那接下来第二个阶段，第二个阶段，取数阶段。由于我们接下来要比较rs和rt所指向的两个呃操作数，

因此我们需要把这两个操作数。分别放到锁存器a和b当中，然后另外我们还需要把这个条件转移指令的偏移量，它里面带的这个偏移量是一个力计数，把这个力计数。把它放到imm这个锁存器里边，好接下来进入执行阶段，那这个阶段会通过aru的计算，通过aru的计算。得到a和b两个数的一个比较结果，把这个比较结果放到下一级的储存器当中，好接下来第四个阶段。仿存阶段，显然当我们想要进行条件转移的时候，肯定是不需要仿存的，

对吧？经过第三个阶段的处理之后，我们就能够知道这个条件到底满不满足，那么接下来我们是不是就可以根据当前的条件是否满足来决定。到底要让PC程序计数器的值改变为什么样的一个值？好，所以对于条件转移类的指令来说，在仿存这一阶段不会进行仿存，而是会修改这个PC的值。把PC的值修改为我们呃，想要转移到的那个地方，也就是说修改PC这个寄存器的值并不是放在写回这个阶段的。写回这个阶段，第五个阶段呃，通常来说都是修改通用寄存器里的这些值，

而PC这个寄存器不属于通用寄存器，它比较特殊。所以对于PC这个寄存器的值的修改，并不会放到写回阶段，而是会放到仿存这个阶段，所以对于条件转移指令来说，最后这个写回阶段。是什么也不用做的，那很多教材会把啊写回PC的这个功能段称为wrpc这样的一个段。很好理解吧wr就是right嘛，写PC。这个功能段在逻辑上并不属于仿存阶段，也不属于呃写回阶段，但是由于这个功能段所需要使用到的硬件部件。和这儿我们画出的五个功能段都是相互独立的，

同时写回PC的这个功能段，它所需要的耗时也比第四个阶段仿存段要更短。因此，我们可以把写回PC这个功能段的呃，实际的处理放在第四个阶段来进行。好，这个地方比较绕，如果跟不上的同学可以暂停来消化一下。总之，这我们想强调的是，对于条件转移指令来说，我们只需要经历前边的四个功能段。在第四个功能段的时间结束之后，我们就可以确定最终要执行的呃下一条指令的地址是什么地方？好，

这是条件转移类的指令，接下来我们再来看一个更简单的也是最后一类，考试当中可能会遇到的指令就是无条件转移指令。无条件转移指令，我们很熟悉了jump，后面直接跟一个偏移量，这个偏移量通常用补码表示。可以为正，也可以为负好那呃无条件转移指令，同样的，它也是采用相对寻址的方式。相对于PC的值偏移多少，那对于无条件转移指令的功能描述是这样的，这个地方给出的PC。这个PC的值指的是当前这条指令的一个存放地址。

然后当前指令的地址，再加上指令字长是不是相当于先给PC加了一个一？双引号的一哈先指向下一条指令，然后相对于下一条指令的存储地址而言。进行一个偏移。那应该偏移多少呢？jump指令里边给出的这个偏移量指的是应该向前或者向后偏移啊，多少条指令？因此，我们还需要用偏移量乘以这个指令字长这点和前边条件转移指令是很类似的。同样的，这个地方跟不上的同学需要暂停下来自己捋一遍这个公式的由来。再次强调，这地方的PC值指的是当前这条指令所存放的一个地址。PC值再加上指令字长才是下一条指令的存放地址。

好，那来看一下，无条件转移指令的执行，第一个阶段取指阶段，不用多说，就是把这条指令取到啊，第一阶段的锁存器里边。然后第二个阶段，我们需要把偏移量这个偏移量放到imm这个锁存器里边。好，接下来就可以进入第三个阶段，然而对于无条件转移指令来说，由于我们不需要判断任何条件，因此其实第三个阶段用不到aru算术逻辑单元。而是会直接根据这个偏移量，

把我们目标的PC值直接写回到PC寄存器里边。直接进行无条件的转移，所以也就是说对于无条件指令来说，刚才我们提到的写回PC的这个功能段，这个功能段又会被安排在。EX这个阶段来执行。所以接下来的缓存和写回阶段，就不需要做任何事情好，那由于写回PC的这个功能段所需要的耗时要比EX这个段更短，所以我们同样的可以把它安排在第三段时间内来完成。那为什么我们要尽早的修改PC的值呢？是因为上一小节我们说过，对于这种有可能修改PC值的指令来说。可能会导致控制冲突。也就是说，

在指令流水线里边，按理来说，我们应该是顺序的，一条一条的取出指令的。但是由于这种呃转移类的指令可能会修改PC的值。因此，如果一直都是顺序的取出后续指令，那么后续取出的这些指令也许并不是我们接下来应该执行的指令，那这种情况下就发生了控制冲突。所以如果我们能够尽早的确定PC的值，那么我们是不是就可以尽早的发现控制冲突？好，所以这就是为什么无条件转移指令会提前在第三个阶段就把PC的值修改的原因。不过呢，也有的地方可能会把修改PC值。

这个功能放到最后一个阶段来执行。但是显然，这种方式发现控制冲突的时间会更晚一些。那清华大学出版社袁春风版的祭祖教材里边，他介绍的就是我们刚才提到的第一种方法，就是会把无条件转移指令修改PC的。呃，这个功能段把它安排在第三个阶段来完成。好的，那么到此为止，我们捋清楚了这个五段式流水线当中，大家有可能会碰到的五类指令。分别是运算类指令。漏的指令。store指令。

条件转移类指令。以及无条件转移指令好，那来看一下，我们捋清楚这些指令的执行过程之后，对于做题会有什么帮助呢？这是我们某一年的真题。这我们只截取了这个真题当中的其中一小部分，大家会在课后习题里面遇到好，那这个题目比较长，我们就先不具体的分析。我们来简单的解释一下啊，他这给出了四条用汇编语言描述的指令，这四条指令要完成的事情就是把a和b。这两个变量的值相加，然后赋给变量x，

而这三个变量在主存当中的存储地址分别用x外面打个中括号。a和b外面打一个中括号，用这样的方式来表示好，所以这个地方第一条指令漏的指令是取数指令，对吧？要从主存当中取出某一个数。放到指定的寄存器当中好，所以第一条指定要做的事情就是从主存当中读出a这个变量。这指明了a这个变量的一个存储地址，然后把这个变量的值放到r1寄存器当中。那这儿我们虽然把它描述为访问主存，但事实上是不是很大概率是直接访问了data cache？因为局部性原理嘛，命中率还是很高的好，接下来第二条指令，

第二条指令同样是一个漏的指令。这条指令就是要把b这个变量从主存当中读入寄存器r2。好，接下来第三条指令分析它的作用，它是一个加法，指令要把r1和r2里的值相加，然后再存回r2。好，接下来第四条指令store存数指令要把r2里边的值存入x所指向的储存单元当中。也就是把a和b相加的结果赋给变量x好，所以第四条指令的功能就是这样的一个效果。他问，这四条指令执行的过程当中I3的ID段，I3的ID段。就是译码和取数的这些阶段，

以及I4的if段I4的if段这两个段被阻塞的原因各是什么呢？好来看一下，本来理想情况下I3的ID段是不是应该在这个地方执行，但这个地方我们被迫把ID段的执行推迟，推迟到了这个时刻，七这个时刻。好，那这个地方我们注意到I1和I2这两条指令是取数指令，而刚才我们是不是分析过？对于呃漏的指令，取数指令来说，当我们从主存或者说从cache当中取出一个数之后。得到第五个阶段。写回阶段结束之后，才会把我们取出的那个数给写回到目标寄存器当中。

好，因此我们这个题目当中这个题目当中I1和I2这两条指令的执行。我们要取出的数是需要到WB。这个阶段结束之后才可以被放回到r1和r2这两个寄存器当中。而I3这条加法指令它在ID这个段是不是除了指令译码之外，还需要从r1和r2当中取出它所需要的两个数？好，因此我们I3这条指令的ID取数阶段必须安排在前边，两条指令的WB。写回阶段结束之后才可以进行。所以这就是I3的ID，这个段被阻塞的原因，因为它和I1和I2之间存在数据相关，或者说数据冲突。好，

接下来看I4的if段为什么会被阻塞，先思考一下。其实是这样的。I3这条指令。我们看起来好像在这个时间段内，是不是就完成了if段的执行，然后按照我们的惯性思维，当I3的if这个段工作完成之后I4的取止工作是不是就可以进行？但是为什么他会被推迟到这个地方呢？好原因是这样的，虽然在这个地方看起来I3的取止阶段的工作好像已经完成了，但事实上直到I3。进入下一个阶段为止，if段的锁存器会一直存放I3这条指令。好，

我们还是结合刚才这个图可能更好理解，就是I3这条指令先执行if段。是不是把I3这条指令放到了存器里面，但是由于数据相关的存在，刚才我们说的这个数据相关的存在。因此I3这条指令暂时无法进入ID这一段，所以I3这条指令的指令数据。会一直被存放在这个if段的锁存器里边好，所以如果我们此时开始执行I4这条指令的呃取指阶段。那么，是不是意味着我们会把我们取出的I4这条指令把它覆盖掉？呃，这个if端的锁存器这样的话，刚才我们取出的I3这条指令是不是就不见了？好，

因此我们必须等到I3这条指令进入到ID段，释放掉if段的锁存器之后，我们才可以让I4这个指令的if段开始执行。好，因此这就是I4的if段被阻塞的原因，好，这个地方还是比较绕，大家也需要静下心来认真的体会。好，所以通过这一例题，希望大家能够感受到我们这一小节介绍的这些内容的一个重要性，如果你不知道漏的指令。是在WB这个阶段才把我们想要取的这个数写回到了寄存器，那么你怎么可能知道ID段为什么要放在WB之后呢？对吧？

好的，那么关于这个小节学习的内容，如何使用，大家也需要根据课后习题来进行训练，指令流水线这个部分经常出现大题的考察，并且考察的难度是非常非常高的。好，所以希望大家静下心来把我们考试当中有可能遇到的这五个类型的指令，他们是如何通过这样的五个阶段完成工作的？把这个问题给捋清楚。
