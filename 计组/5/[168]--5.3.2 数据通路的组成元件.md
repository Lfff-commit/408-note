各位同学大家好，这节课我们来认识数据通路的基本组成元件，我们说CPU由数据通路和控制部件来组成。那么，数据通路的基本组成元件只有两大类，一类叫做组合逻辑元件。也称为操作元件，另一类叫做时序逻辑元件，也称为状态元件。典型的组合逻辑元件像加法器aru，这是我们比较熟悉的，以及大家不太熟悉的呃像译码器mux就是呃多路选择器以及三态门。后面这三种元件，这节课我们会有具体的例子，那加法器和aru这两种组合逻辑元件，

他们是不可以存数据的，对吧？以及当前的输出值只和当前的输入值有关。比如说一个加法器，它的输出值是什么？取决于当前输入的两个恩比特数分别是什么？那aru同理。就类似于一个食品加工厂当中，我们需要在食品生产线上去对食品进行相应的处理，对吧？那么，食品生产线上可能会有切丝器。这样的一个硬件部件。这种硬件部件的输出只和当前的输入有关，比如说你在切丝器当中输入一个土豆，

那它输出的就是土豆丝，你输入的是一个鱿鱼，那么输出的就是鱿鱼丝。这种硬件部件它不可以存储，这个食物对吧？那类似的食品生产线上油炸锅也属于这种组合逻辑元件。你不会在油炸锅里边去存你的食物，对吧？油炸锅是用来加工食物的，如果你输入的是土豆丝，那它输出的就是炸土豆丝，你输入的是鱿鱼丝，它输出的就是炸鱿鱼丝。好，这是组合逻辑元件，

又叫操作元件。另一种叫做时序逻辑元件。一句话概括这类元件，就是可以用于存数据的，那这种元件的当前输出值。和当前的输入值有关，也和这种元件的历史状态有关，它有记忆的功能。比如说各类寄存器或者呃主存储器都属于时序逻辑元件。好，同样还是以食品生产线为例子。在食品生产线上，可能会有冷藏柜，对吧？那冷藏柜是可以用于呃存储食品的。

它属于时序逻辑元件。你从冷藏柜的一号格拿出什么东西？取决于它的历史状态，要看你之前存入了什么，对吧？同理，你从二号格拿什么也取决于之前你在二号格存入了什么，那么到底从一号格拿还是从二号格拿呢？这个是用控制信号去决定的。就类似于你需要指明你要从寄存器组当中的呃一号寄存器还是二号寄存器？去拿数据。那么，这个控制信号相当于是给寄存器组给这个时序逻辑元件的。当前的一个输入值。好，

所以体会这句话时序逻辑元件的输出，看当前的输入。比如说控制信号输入的是什么？以及这个时序逻辑元件之前的历史状态是什么？好，这是数据通路的两种基本组成元件。接下来，我们补充译码器，多路选择器和三态门这三种组合逻辑元件的功能。在考试当中可能会遇到，首先来看译码器。译码器的型号很多有二四，译码器三八，译码器46，译码器等等。

好，这两个数字什么意思呢？二四译码器二的二次方等于四，相当于用两个比特可以对应四种状态，对吧？所以它叫二四译码器。类似的三八一码器就是说用三个比特去表示八种状态好，先看下面这个二四一码器。如果我们输入的是零一，那么它的四根输出线当中编号为一的这根输出线会输出一个有效信号。而零二三都是无效信号。如果输入信号给的是一一。一一对应二进制的三，那么三号这个输出线，它的信号有效，

另外几根输出线信号无效。好，这是二次译码器。三八译码器同理，我们输入三个比特。输出的线路有八根，但是只会有其中的一根，输出线有效，比如说当我们输入一一零的时候，一一零是二进制对应，十进制的六。因此，在零一二三四五六七这八根输出线当中，只有编号为六的这根输出线，它的呃信号有效。

其他的输出线信号无效，那么左边这两个例子同理。好，所以当我们在电路图当中看到译码器的时候，首先我们要问它是什么译码器，二四三八还是46？前一个数字表示的是输入信号，有多少个比特？假如有k比特输入，那么就会对应二的k次方根输出线。k比特的输入信号会让其中的某一根输出线输出有效的这个信号，而其他输出线都是无效的。好，这是译码器这种组合逻辑元件，接下来再看多路选择器和三态门这两种组合逻辑元件。

这两种元件我们在第二章的补充视频中呃，给大家有过介绍。我们再来快速回顾一遍。多路选择器会有k个输入一个输出。需要m个比特的控制信号。比如说有八个输入，那么控制信号就需要至少三个比特二的三次方等于八，那么三个比特的控制信号。是用来决定这八条输入信号当中到底让哪一条通过从这边输出。相当于八个人只会选其中一个人，让他过去。这是多路选择器的作用好，再看三态门呃，很简单。这是一道门。

那如果控制信号是一，这n个比特的输入信号就可以顺利的通过，如果控制信号是零，那么这些信号就不能通过。好来看具体的例子。左边这是一个呃四路选择器，有四个输入，一个输出。注意每个输入可以是多个比特。这点和译码器是不一样的。我们画译码器的时候，每一根线，它对应的就是一个比特，对吧？但是在多路选择器的这个示意图当中，

这一根线背后可能对应的是多个比特。那假设每一个输入值是八个比特，输出值相应的也是八个比特，那么如果当前的控制信号是零一，那么就是零一对应的这个输入口。这八个比特可以通过从另一端输出。而零零一零一一另外的这三组巴比特都不能通过多路选择器。好，这是四个输入，一个输出，再看右边这个图，它是两个输入，一个输出。由于只有两个输入，因此我们的控制信号就只需要一个比特，

对吧？如果控制信号为零，那么让左边这一路通过控制信号为一，那么让右边这一路的八个比特通过。那像一五年的43题啊，这里边就会有一个多路选择器，并且是最简单的二路选择，对吧？只有两个输入口，一个输出口，那么这个多路选择器，它的控制信号muxop需要几个比特呢？显然，只需要一个比特就行，对吧？

如果这个比特是零，那么就让左边这一路通过，如果比特是一，就让右边这一路通过好，所以这些常见的组合逻辑元件，我们是需要熟悉的。接下来再看三态门的一个实例。三态门的符号就是一个简单的三角形，三角形的尖尖，这一头就是输出端，然后三角形的平的这一头就是输入端。控制信号为零的时候。输出端这边是高组态，就是什么也不输出。相当于没有数据通过，

如果控制信号为一，那么三态门就会顺利的导通，输入什么就输出什么。原模原样，原封不动的让它通过。那么，像二二年的这道真题里边，你就会看到很多三态门，你看这有个小三角对吧？三态门。这也有小三角三态门，这也有小三角也是三态门，那么每一个三态门的这个控制信号，比如说上面这个三态门，它的控制信号需要几个比特？

mdr out.显然，只需要一个比特，对吧？如果我们想要让mdr寄存器里面的这个呃数据存储的数据，想把它打到这个内部总线上。那么，我们只需要让mdr out这一比特的控制信号等于一就可以了。那类似的，如果我们想让寄存器组当中的呃某个数据从寄存器输出打到这个内部总线上，那我们只需要让这个控制信号等于一，对吧？不想让数据跑到总线上，那么让这个控制信号保持为零就可以，那么这些零一这种控制信号是由。

控制部件发出的。在二二年的这张图当中，并没有把控制部件画出来好，那么刚才我们补充了三种大家不太熟悉的组合逻辑元件，分别是三态门。多路选择器。以及译码器。那除了组合逻辑元件，另一种数据通路的基本元件就是时序逻辑元件。在这张图当中，我标了绿色箭头的这些都是很典型的时序逻辑元件。就是可以用来存数的，你看GPRS，这是寄存器组对吧？它属于时序逻辑元件。

可以存储二进制，然后标志寄存器，暂存寄存器以及主存储器，反正只要可以存数据的，它都属于时序逻辑元件。那aru呢？它不能存对吧？只能处理数据，所以aru不是时序逻辑元件。好，那么本节课的最后我们来解释一下课本上的这张图。在CPU内部数据通路，在呃完成这个指令的相应功能的时候。本质就是让呃数据在数据通路的各种部件之间相互流通的一个过程，比如说我们可以从这个通用寄存器组这。

取个数。把它打到总线上。然后从aru的呃b这一端输入。与此同时，我们给aru发出控制信号，比如说我们可以发出取反主比特取反这样的一个控制信号。那么，从寄存器这边过来的这个数据。就会主比特取法。然后存到z这个暂存寄存器当中。好在这个例子中，我们看到了很典型的数据通路的这种呃工作的一个模型哈。就是从一个寄存器，从一个持续逻辑元件这。输出信号，

然后作为组合逻辑元件的输入。组合逻辑元件把这个数据哎进行处理之后，再把它呃写到下一个时序逻辑元件这。好，这是很典型的数据通路的基本结构，我们可以把数据通路看作是状态元件，操作元件，状态元件，操作元件在逻辑上抽象成这样的一个简单的模型。好，那么课本上的这张图，他想表达的是什么呢？其实就是我们之前的课程当中给大家提到过的。就是CPU的最小时钟周期长度受限于这个电路的延迟。因为一个组合逻辑元件，

比如说加法器或者一个阵列乘法器，它的这个电路延迟是各不相同的，越复杂的这个电路，它的延迟越高，例如这个乘法电路延迟是零点九纳秒。那么，除了这个组合逻辑元件之外，寄存器这种时序逻辑元件，它在输出信号的时候以及输入信号的时候。也会带来一定的延迟，对吧？比如说带来零点一纳秒的延迟。你看这小段电路，它完成运算存好结果，至少需要一纳秒的时间，

那这就意味着CPU的这个时钟周期也不能小于一纳秒。因为CPU的一个时钟周期对应的就是一个最基本的，最小的，不可再拆分的动作。好，这是前面用的一个简化模型。那课本上的这个图就是把我们刚才简化的这个模型做了更细的这种拆分。从一个寄存器输出数据作为这个组合逻辑电路的输入，然后这边还需要信号稳定，对吧？以及把运算的结果写入下一个寄存器，那么整个过程当中，左边这个寄存器它输出信号。有一定的延迟。我们把它记住tclk to q很复杂的一个名字，

对应的就是这一小段这一小段。好把信号输出之后，这个组合逻辑电路会有t max这么长的延迟，对吧？这么长的延迟之后输出信号才会稳定下来。那么，对于右边这个寄存器b来说，它还需要t set up，就是需要这么长的时间。在这段时间内，要确保呃写入寄存器的这个信号要保持稳定。好，那么三个部分加起来就决定了这个数据通路的最小时钟周期。它的一个下限，一个时钟周期，

或者说这样的一个节拍。不能低于这个值，否则数据通路的这一小段就没办法在一个时钟周期内完成这种运算。好，那么这张图以及背后更细节的这种电路原理，有兴趣的同学自己去看一下。对于考试来说，我们只需要理解CPU的最小时钟周期长度受限于这个组合逻辑电路的呃。延迟就行了，特别是所有的组合逻辑，电路当中延迟最高的那一段。就像我们这个例子，当中乘法电路的延迟要比加法电路的延迟更高，那么CPU的最小时钟周期就受限于。延迟更高的这一段。

好的，通过本节课的学习，我们认识了数据通路的呃，两种基本的组成元件，分别叫组合逻辑元件。也叫操作元件以及时序逻辑元件，也叫状态元件。不能存数据的就是操作元件，可以存数据的就是状态元件。在这节课当中，我们也补充了译码器，多路选择器，三态门这三种呃，很常见的组合逻辑元件。有可能在真题中出现。

大家可以结合课件进行回顾好，那么以上就是本节课的所有内容。
