好的，那么经过上一小节的学习，我们知道了什么是指令流水线，那么上一小节，我们所建立的那个指令流水线的工作模式，工作模型都是很完美的情况。一个阶段和另一个阶段都可以完美无瑕的衔接，然而有的时候指令流水线有可能工作的不会那么完美。那么，有可能导致各个阶段呃，没有那么完美的衔接的因素是哪些呢？这就是我们这个小节要探讨的主要问题。好，那为了让问题探讨起来，没有那么抽象，

所以我们首先引入一个经典的五段式的这种指令流水线。也就是说，一条指令的执行被分为这样的五个阶段。第一个阶段呃，叫做if这个阶段，那它的全称应该是instruction。AH,fetch.那instruction就是指令嘛，这个大家都知道，然后fetch就是取出拿出的意思，所以if这个阶段指的是。取指令的阶段。好取出指令之后，接下来进了ID这个阶段。

I就是instruction，然后d就是decode，就是指令译码的阶段。好，然后下一个阶段叫execute，这个大家都很熟悉，就是执行嘛，指令的具体执行像我们WINDOWS电脑里边可执行文件不就是点EXE吗？它其实就是execute的。一个缩写好，所以EX表示的是指令的执行阶段，然后MM表示的是memory，就是需要进行缓存的一个阶段。然后最后一个阶段叫呃right back WB，就是要把最终运算的结果把它写回到呃通用寄存器组好，

那现在大家可能会觉得比较奇怪。呃，经历这样的五个阶段，所有的指令都可以完成吗？啊，这是一个大大的问号，那这个问题我们用下一小节再来呃，进行详细的解释，那事实上这种五段式的指令流水线。是我们考试当中考试的大题，还有选择题的分析题都最常考，最常见的一种模式，因为这种五段式的指令流水线。就是经典的MIPS架构所提出来的啊，一种指令流水线，

那MIPS是世界上第一个啊，精简指令集的这种。呃，指令系统像现在流行的arm架构，它也是经典指令集的系统。不过arm架构其实是MIPS的一个孙子吧。总之，精简指令集的这种理念就是MIPS架构所提出的，有兴趣的同学今天晚上可以去查一下MIPS好，总之MIPS架构下。一条指令的执行会被划分为这样的五个阶段。当然了，有的指令有可能会跳过其中的某些阶段。比如说有的指令可能会跳过仿存这个阶段，只包含四个阶段，

然而为了方便指令流水线的这个安排。即便有的指令有可能在实际运行的时候不需要进行中间某个阶段的处理，然而所有的指令也都会被安排为这样的五个跑不掉的。这个机器周期。所以在MIPS架构下，所有的指令一定都是五个机器周期，分为这样的五个阶段。有的机器周期内有可能什么都不做，但是这个机器周期的时间你是跑不掉的，必须经过好另一个方面，我们之前说过每一个机器周期呃，完成对应工作所需要的时间花销是不是有可能？可能会不一样，比如说取止阶段的这个时间开销需要100纳秒，然后第二个阶段80，

接下来70，50，50。好，那同样的，为了方便指令流水线的安排，我们会规定在每个阶段的耗时。会把它们规定为一样，就是会向耗时最长的那个阶段靠近，所以这五个阶段当中，耗时最长的是100纳秒，因此我们把所有五个阶段的这个机器周期都统一的设置。为100纳秒，这样可以方便我们安排指令流水线，很好理解吧，

因为如果呃指令执行的五个阶段，它的那个时间耗时都是一样的话。你看画图的时候都是每个阶段可以呃，相互对齐的对吧？而如果说如果说呃，有的阶段很长，有的阶段很短。那么呃，在你画图的时候，你看第一个阶段结束之后，第二条指令的第一个阶段有可能会经过这么长的时间，对吧？你的这个指令流水线呃，各个阶段不可以完美的对齐，那么对于你设计指令流水线。

会比较麻烦，另外如果说各个阶段的这个呃时长不一样，那么你的控制电路设计起来也会更麻烦。好，所以对于MIPS架构来说，或者说啊，大部分的risk就是精简指令集系统来说。通常我们都会规定，每一条指令所包含的机器周期数都相同，并且每一个机器周期的长度也都相同。这样可以方便我们安排指令流水线好，那现在问题产生了，我们想要让每个阶段。耗时为100纳秒，所有的阶段耗时都是一一百纳秒，

那么像这个图里的第三个部分就是执行阶段所需要用到的硬件部件。他应该是在第200纳秒的时候，第200纳秒的时候才把呃他所需要的数据给输入，对吧？但是由于前边的第二个阶段，实际啊，只需要80纳秒的时间就可以准备好这些数据，所以为了让第二个阶段流出的数据。能够刚好赶在200纳秒的时候，才让它流出，那么我们就需要在各个阶段中间。给他们添加一些这种暂存的寄存器，就是说第二个阶段准备好，第三个阶段所需要的数据本来只需要80纳秒，但是我们会先把准备好的数据先。

呃，存放在这个呃，暂存的这个寄存器里边，然后到了第三个阶段想要数据的时候，我们再把这数据送出去。那每一个功能段的这种硬件部件的后面一定都需要添加一个缓冲寄存器。那这种特殊用途的缓冲寄存器，还有另一个名字叫锁存器，它的作用就是保存当前这个指令流水段的执行结果。然后提供给下一个流水段使用，有了每一个阶段，中间的这个锁存器我们就可以更方便的来统一，每一个阶段的。实际的时间开销，因为我们的目的是要统一成，

每个阶段都耗时100纳秒，对吧好，所以大家需要注意每一个功能段，中间的这个锁存器的一个作用。好，现在我们来简单探讨一下这个图的一个含义。第一个阶段，取址阶段instruction fatch，这个阶段要做的事情就是根据PC所指向的位置。去instruction cache里边找出当前要执行的这条指令，然后把取出的这条指令放到这个功能段的锁存器里边。好，这个地方出现了一个呃，大家既熟悉又陌生的一个概念cache，大家都很熟悉。

之前我们一直在说取指令是去主存里边取指令，对吧？但是第三章其实我们说过，在大多数情况下主存里。当前，需要使用到的指令还有数据，这些信息会在cache当中保存一个副本。并且基于局部性原理，我们知道cache的命中概率一般都是很高的，所以这个地方取止阶段，他画了一个cache的意思，就是说呃，大部分情况下我们都可以从cache里边。直接找到我们现在想要的那一条指令的数据，而不需要去进行仿存。

好，这是取止阶段，访问了指令cache。然后现在我们看第四个阶段，先跳到第四个阶段，第四个阶段，我们本来是要访问主存的，但是这个地方。他写了呃data cache。原理是类似的，对吧？我们要访问主存，但是大部分情况下，实际上我们都可以从cache里边直接命中想要的数据。那由于第四个阶段我们访问的并不是指令，

而是指令要操作的某一些具体的数据，因此这个地方它写的是data cache。也就是说，我们CPU内部的cache会被分为两个独立的模块，一种cache是专门用来存储这个指令的，而另一种cache是专门用来存储变量啊之类的。呃，数据的那么把指令和数据分开存放，把它们存在两个独立的cache模块当中，是不是就意味着对这两个模块的访问是可以并行的进行的？这个可以理解吧，如果我们只有一个cache模块，那么当我们在取指令，在访问这个cache模块的时候，我们就不可能往这个cache模块里边写入数据，

对吧？好因此指令和数据用两个独立的cache模块来呃，独立的存储是很有意义的，可以使得第一个阶段和第四个阶段所需要使用的硬件部件。可以并行的工作，这样就可以让我们呃更方便的来安排指令流水线好，这是关于两种cache。对了，有的同学可能会有疑问，就是如果我们呃这一次要读或者要写的数据没有在这个数据cache当中命中的话怎么办？那这种情况下，我们的指令流水线也会出现断流的情况，因为cache不命中，我们就必须去访问主存。那这种复杂的情况，

我们先不考虑好，接下来我们回到第二个部分，第二个部分是ID，就是instruction decode。指令译码阶段，在这个指令译码阶段，除了完成指令译码的工作之外。还会完成取数的操作，取数就是指要把这条指令所需要用到的操作数。从通用寄存器里边把它取出来，然后放到这个呃锁存器a和b里边。那这一点又和我们之前学习的啊，大家已经很熟悉的指令不太一样，之前我们学习的呃指令，比如说加法指令，

可以直接指明我要访问的那个加数或者被加数。是来自于主存这个操作所需要用的操作数，会直接来自于主存，这是我们之前介绍过的啊，那些指令。但是在这种risk精简指令集的系统之下，我们想要进行运算的操作数一定是直接来自于通用寄存器的。不可能直接来自于主存，如果要来自于主存，那么一定需要先把那个数据先放到通用寄存器，然后再从通用寄存器当中取出那个数据。所以这个地方指令译码的阶段，除了指令译码之外，还会从通用寄存器里边取出当前这一条指令所需要用的操作数。会把它放到a和b这两个锁存器里边，

然后接下来第三个阶段执行阶段。执行阶段，我们需要用aru算术逻辑单元来处理。刚才前一个阶段取出的操作数，比如说操作数b和操作数a的一个啊，一个运算。那上面这个地方还有一个呃，一个锁存器叫imm，大家可以把头。啊，往左边旋转90度可以看出来，这是immimm就是immediately就是呃力计数。这个锁存器是用于存立即数的，因为我们有的指令当中呃，可能会使用立即寻址，

也就是指令的地址码部分就直接说明了一个立即数，那么在这种情况下，我们可以直接从指令当中取出。接下来执行阶段所需要使用的力计数把它放到这个锁存器里边，所以有的时候啊，第三个阶段就是执行阶段处理的数据，有可能一个是。这个力计数，另一个是来自于某一个通用寄存器。是这样的一个意思好，那第三个执行阶段是把刚才的两个操作数啊，进行了一个处理，处理了之后会把输出的结果会把这个计算运算的结果放到这个。锁存器当中。然后再把这个运算的结果，

有可能需要写回主存，当然这个运算的结果也有可能不需要写回主存。有的时候我们一条指令的运算结果是直接写回寄存器，对吧？直接写回某一个通用寄存器，所以你看下面这个地方，它还有一个线。这根线会直接输出到最后，这个倒数第二个阶段的这个锁存器里边，然后接下来再把这个数据。你看，通过第五个阶段写回的阶段。把我们运算的结果写回到某一个通用寄存器里边，好，因此这是第五个阶段，

写回阶段的啊，一个实际的工作有可能需要把之前运算的结果。写回到某一个通用寄存器当中，当然了，也有可能不需要写回这个得结合具体的指令分类再来分析，我们放在下一小节再来继续展开好。现在我们已经看懂了这个图，我们考试当中很多大题对于指令流水线的分析一定需要理解，我们刚才说的这些过程。然后下个小节我们还会再添加一个视频，对这个图进行进一步的探讨好，总之现在大家看这个图应该已经能够懂个八九成了。好，现在我们注意到第二个阶段，有可能对通用寄存器某一个通用寄存器进行读。

有可能进行读操作，然后写回的阶段，有可能会把数据写回到某一个通用寄存器，所以啊，这个阶段有可能对通用寄存器进行写的操作。那这两个阶段对寄存器的读和写就有可能造成一些问题。好，所以接下来我们要探讨的问题就是影响指令流水线的具体的因素，分为这样的三个大类，第一类叫做结构相关，又叫资源冲突。第二类因素叫做数据相关的因素，也就是数据冲突。那如果学过操作系统的同学。第一个大类其实就是操作系统当中的互斥问题，

某些资源是不可以同时共享的。然后第二个数据冲突，其实就是同步问题，因为不同的指令，它们之间有可能有一前一后的相互依赖关系。如果已经学过操作系统的同学一会儿啊，我们在分析这两个部分的时候和我们操作系统学习的互斥和同步的问题，进行一个类比的学习。好，然后第三种有可能影响的因素叫做控制相关的因素呃，又叫控制冲突好，那接下来我们来依次探讨。第一个分类数据相关又叫资源冲突，指多条指令在同一时刻征用同一个资源而形成的冲突。这种现象就叫结构相关好，

比如说结合刚才我们提出的五段式的这个呃指令流水线。那么，第一个阶段就是。取止阶段是要访问主存，对吧？当然了，刚才我们也说过呃，大多数情况下，其实可以cache民众，不过它背后的逻辑是在访问储存。好，接下来第二个阶段指令译码阶段，除了译码之外，是不是会对寄存器进行读操作？第三个阶段，

执行阶段主要使用到的功能部件是aru算术逻辑单元，然后第四个阶段叫做呃仿存的阶段。这一阶段显然也会使用到主存这种资源。然后第五个阶段就是写回write back，这个阶段会对寄存器进行写操作。好，那什么是结构相关，或者说什么叫资源冲突呢？比如说红色的这两个部分，当我们的这条指令啊，执行到第一个阶段就是取止阶段的时候，它需要使用到主存，对吧？然而，我们最开头的这条指令，

在呃这个时刻，它也会使用到呃存储器这个部件。所以它们俩同时访问存储器是不是就会造成资源冲突？好，另一个方面看蓝色的，这两个部分第一条指令的第五个阶段需要访问某一个通用寄存器写回。然后下面这条指令的蓝色，这个阶段它同样有可能会对某一个寄存器进行读的操作，对吧？那么如果它们俩访问的是同一个寄存器。是不是又会造成资源冲突？好，那这种资源冲突的问题怎么解决呢？第一种方法很简单，就是可以让排在后面的这条指令，

让它暂停一个周期，然后再来尝试着执行。这个很好理解吧，我现在这个时刻用不了，那我再等一个时刻，等到下一个周期再来使用不就行了，好第二种方法就是呃资源的重复配置。其实这种解决方案就是我们刚才强调过的那个问题，如果我们把指令和数据把它分别放到不同的cache当中，或者分别的放到不同的两个存储体当中。那么，第一个阶段，取址阶段和第四个阶段就是呃仿存的这个阶段所需要访问的数据一定是不相同的。第一个阶段是指令数据。而后面这个阶段是变量之类的，

其他数据。所以如果采用这种方式，那么当呃第一条指令执行到就是仿存的那个阶段的时候。下面这条指令，它是刚好开始取止阶段，那取止阶段所需要访问的cache体和缓存阶段所需要访问的cache体是两个独立的cache体，对吧？所以用这种方式就可以解决结构相关，或者说资源冲突的这个问题好，那这是资源重复配置的一个原理。那大家体会一下，本质上和操作系统的互斥问题是一个路子，对吧？当互斥问题发生的时候，我们可以让呃另一个人先等一下。

等前一个人先用完了，后一个人再去使用，这是第一种解决方法，还有一种方法就是你资源不够，那多增加一个资源，两个资源可以并行使用不就行了。好，所以这些解决问题的思想都是相通的，大家在学习的时候可以把它们联系起来好，第二个影响流水线的因素叫做数据相关。也可以叫数据冲突，刚才我们说过这有点类似于操作系统的同步问题。好，还是结合一个图示比较好理解。我们假设第一条指令，

第一条指令是执行了一个加法，把r2和r3这两个寄存器的值相加了之后放回r1。然后第二条指令是执行了一个减法。是把r1里的内容和r3里的内容相减之后，放到了r4。对了，这条指令看它名字应该就能猜到是减法吧，因为减法就是呃，减法是什么来着？subtract对subtract减法嘛好，然后第三条指令，第三条指令是一个与逻辑的与运算，同样需要使用到r1这个寄存器里的内容。r1和r7进行相遇，然后放到r6第，

然后第四条指令是一个逻辑或运算。r1和r9的内容相互之后放到r8。然后最后这条指令是一个异或运算r1和r11里的呃，内容异或之后放回r10。好，那来分析一下，正常来说，按照我们所期待的一个执行顺序，是不是应该等我们先用第一条指令？确定了r1的内容，也就是把r1这个寄存器的内容已经写回了之后，才应该执行第二条指令，因为第二条指令的这个操作数是从r1里面读出来的。他在读r1之前，是不是必须保证前边这条指令已经把数据写回r1了？

那这不就是同步问题吗？必须保证两个动作一前一后的完成。好，那同样的下面这两个指令也会用到r1里面的内容，同样在他们读出r1里的数据之前，需要保证第一条指令已经把数据写回r1了。好，所以如果我们按照我们所期待的完美的流水线来安排这些指令的运行的话，那来看一下。第一条指令把运算的结果写回寄存器r，一是在最后这个写回的阶段。写回的阶段才会进行的，但是第二条指令，第二条指令它从r1里边读出数据，是不是在指令译码阶段？

在第二个阶段就会把那个数据给读出了，所以这也就意味着在前一个操作还没有完成之前，第二个操作就先进行了好，那后面这两条指令也是一样的。当他们取出操作数r1的时候，其实他们做这些动作的时候r1的正确数据还没有被写回。好，所以这就是数据冲突的问题。我们再来看最后这条指令，它同样需要使用到r1，但是我们会发现，最后这条指令取出r1的时候。也就是在它的第二个阶段，指令页码阶段取出r1的时候，r1的数据已经被写回了，

对吧？所以最后这条指令和第一条指令之间不会存在数据冲突的问题，而第二条，第三条，第四条指令。这三条指令和第一条指令都会存在数据冲突的问题。好，那来看一下这种问题怎么解决？第一个比较容易想到的方法，我们是不是可以等啊？这个数据还暂时没有写回，那么我们等一段时间不就行了？好，那实现所谓等待的方法有两种，第一种是可以用硬件的方式来实现，

叫做硬件阻塞。就是说如果两条指令之间存在这个数据冲突，那么采用硬件阻塞的方式，意味着会由硬件系统添加这样的几个气泡。bubble那也就是一些空拍子，对吧？把第二条指令的开始时间往后拖呃三个周期。也就是到这个时间点，才让它进行这个取止，然后取操作数，那由于它取操作数的时候。前边这条指令就是与它有数据冲突的，这条指令已经把操作数写回寄存器了，所以在这个时候读出r1是没有问题的。好，

那具体硬件是怎么判断？怎么实现这种所谓的气泡的？这个大家不需要过度的深究，总之可以用纯硬件的方式来实现阻塞等待。接下来我们还可以用软件的方法来解决这个数据冲突的问题。nop。还记不记得这些缩写？这指的是空操作对吧？空指令什么也不干的指令。就是说如果我们生成这些机器指令的时候，编译器发现这条指令和后面的这条指令。它们之间有数据冲突的关系，那么我们的编译器会为我们在这两条指令中间。插入三条空指令，每一条空指令的执行也会完整的经过五个这种机器周期。

那么我们拖了这样的三个指令的时间之后再来启动刚才的这条指令，是不是也可以保证啊？数据不会冲突。好，所以这是第二种解决方法，用软件的方式来解决，也就是插入啊几条空指令，那具体要插几条，这个是编译器关心的事情，当然了，最底层的机器语言。程序员，或者说那个汇编语言程序员也可以用手动的方式来插入这些空指令。所以机器语言程序员和汇编语言程序员除了要理解这个语言本身的功能之外，还需要理解你的这个语言是运行在什么样的机器上的。

这个机器是否支持指令流水线？如果支持指令流水线，那么你是否需要增加一些空指令来保证指令流水线的正确运行？因此，汇编语言程序员和机器语言程序员都是一些非常厉害的大佬，软硬通吃好的，那这是第一类的解决方法，就是数据没准备好，那么我们就往后。拖延几个周期，等他准备好了，我们再开始。接下来，第二类解决方法叫做数据旁路，技术又叫做转发机制。

啊，什么意思呢？来看一下。我们呃第一条指令它是要把r2和r3的这个相加的，结果存回r1。然后第二条指令会取出r1的内容，然后以r1的内容作为其中的一个操作数和r3进行呃减法操作，对吧？那我们结合这个图来看一下，事实上r2和r3相加的结果，其实在第三个阶段执行阶段。经过这个运算器的处理之后，是不是就已经有结果了？而第二条减法指令所需要用的其中一个操作数r1。谁说一定要从那个寄存器里取呢？

我们可不可以增加一个呃电路直接从这个alu的输出端把它接到alu的输入端？也就是直接把r2和r3相加的结果作为下一条指令的其中一个输入端，那这样的话，第二条指令需要的这个数据是不是就不需要等了？好，所以这就是数据旁路技术在第一条指令它的加法运算结果出来之后。我们会连出一个数据旁路，红杏出墙，让这个数据直接送回alu的其中一个输入端。作为下一条指令的这个输入，那这样的话，我们就不需要等待上一条指令的写回操作。好，这是第二种解决方法。接下来，

第三种，第三种叫编译优化，通过编译器调整指令顺序来解决数据相关。还是结合这个例子来解释第一条指令它的计算结果会被后面这几条指令所使用到。那如果说我们再往后还有一系列的指令。后面这些指令的执行不需要依赖前边这几条指令的一个运算结果，那么我们是不是可以把这几条指令？先把它安排到第一条指令之后，让它来执行。那这样的话，当减法操作这条指令执行的时候，事实上已经呃过了这三条指令的一个执行的那个周期了，对吧？就有点类似于我们之前在这个地方插入了空操作的三条指令是一样的效果，但是比起插入空操作的指令来说，

我们把后续的那些呃指令。把它提前到这个位置来执行，是不是相当于这个时间我们没有浪费，我们也在做有意义的事情，而不是在抖腿，不是在做空操作。所以如果说有后续的指令，可以提前执行，不需要依赖于前序指令的一个执行结果的话，那么我们可以用编译器来。来调整这个指令的执行顺序，从而就可以解决数据相关的问题。好，那这就是数据相关第二类影响指令流水线的因素，那这地方给大家提个醒，

数据相关的考题是非常多的，最常考的不管是小题还是大题。所以这个部分一定要注意，当然了，我们还会通过下个小节进行进一步的学习，好接下来看第三个可能影响指令流水线的因素。叫做控制相关。当我们遇到转移指令或者改变PC值的指令而造成断流的时候，会引起控制相关或者说会引起控制冲突。还是结合图最好理解，比如我们此时此时我们要执行的是一个条件转移指令。然后这条指令是被存放在了这个呃地址为12的位置，然后下一条指令顺序存储的指令是存储在16的位置，也就是说每一条指令是占四个字节，对吧？

这是这个图表现出来的信息，每一条指令占四个字节。好，所以如果这个条件转移，指令它的判断条件不满足的情况下，正常来说，我们的这个指令呃，执行流应该是顺序的一条一条往下执行，对吧？先执行12这一条，再执行存在16的这一条，接下来是二十二十四，但是如果这条指令它想要判断的那个条件。那个条件满足的话，那么有可能根据这条指令的指示跳转到某一个呃不一样的位置，

比如说跳转到地址为1000的这个地方。让PC的值直接变为1000。那这样的话，如果按照我们之前安排的这个指令，流水线是不是前边？我们取进来的这三条指令是不应该执行的，那这就是所谓的控制相关的问题。由于转移指令导致PC的值跳到了另一个位置，而造成了指令的断流。那除了转移类的指令之外，还有其他一些指令也有可能会改变PC的值，比如说函数调用。还有就是函数的返回指令，这些都会改变程序的执行流，改变PC对吧？

那除此之外，当发生中断的时候。CPU是不是会转去执行中断处理程序？那这种情况下PC的值也会改变，所以这些情况都会导致控制相关的问题。好，所以控制相关的问题还是比较复杂的，那接下我们着重来探讨由转移指令导致的这种控制冲突应该如何解决？那么，解决的方法可以有这样的几种，第一种叫做呃转移指令的分支预测，就是说你可以掐指一算，算一下这个条件转移指令，它的条件会满足，还不会满足。

那可以有两种啊，预测的方式，第一种叫做简单预测就无脑猜，你永远都猜他的条件会满足，或者永远都猜他的条件不会满足，这是简单预测。还有一种叫动态预测，就根据历史情况动态的调整，你这个猜测的策略，那显然第二种方式的呃，预测正确率可能会更高一些。好，这是第一种解决方法，你得算上一卦，那第二种解决方法叫做预取转移成功和不成功的两条控制流方向上的目标指令。

这个也很好理解吧，由于你的条件转移，指令有可能导致程序的执行流往两个方向进行，那么我们把两个方向所需要用到的指令。就是成功和不成功的指令，都把它预取出来，是不是就可以了？所以这种方法就是做两手准备，那显然如果要预取两个控制流方向上的指令，那么我们可能会需要增加一些硬件部件。比如说增加两个指令寄存器啊之类的。采用这种方法也是需要付出一定代价的，好接下来看第三种，第三种叫加快和提前形成条件码。那这一点其实我们在第二章关于加法器的设计，

那个小节当中，大家应该会有体会，我们在设计加法器的时候。需要把呃一位全加器进行一个串联，对吧？然后每一个全加器都会依赖于它前边这个全加器产生的进位信息。但是我们可以用电路改造的方式，让这些进位信息提前的产生，提前的传递给啊，前边的这个全加器，所以回到我们这个地方。第三种方法提前形成条件码也是类似的原理好，所以这种方法的思想也是我们之前接触过的，接下来第四种，第四种方法叫提高。

转移方向的猜准率。其实它是对第一种解决方法的一个优化，反正就是引入一些方法来提高我们猜准的那个概率。好，那这就是解决控制相关的一些啊，常见的方法，那我们把刚才这些内容给汇总一下，影响指令流水线的因素，有这样的三种。结构相关，数据相关和控制相关，其中结构相关有点类似于我们操作系统里的互斥问题。而数据相关有点类似于我们的同步问题，那么具体的解决方法，这就不再重复，

大家可以再暂停来回顾一下，然后最后一种啊，控制相关是由于程序执行流的改变。而导致的因为正常来说，我们指令流水线当中肯定都是顺序的往下取一些指令的，但是如果这个程序的执行流发生了改变。那么，我们一条一条往下顺序取的这种方式，就可能会出现一些问题，那这就是控制相关好，那这我们就不再重复，大家可以再暂停来回顾一下。好，那这个小节的最后还需要再讲一个，还算是比较重要的考点，

就是叫做流水线的多发技术。第一种叫做超标量技术，看图比较好理解，如果采用这种技术，那么在一个时钟周期内。在这样的一个时钟周期内，我们可以并行的发射出多条指令。所以它就要多发技术。那我们需要保证同时执行的这几条指令，在这种同时工作的情况下，不会导致问题，那显然如果我们同时要运行三条指令。那是不是意味着至少我们需要配置三个指令寄存器，然后也有可能需要配置三个aru，对吧？

好，因此要实现超标量技术，那么我们必须配置多个功能部件。另外，我们程序里边所包含的这些呃指令的执行顺序是不可以调整的，就是说经过我们编译器处理之后。形成的机器代码呃，第一条指令，第二条指令，第三条指令，它们是这么排列的，那么当我们在执行这些指令的时候，也一定是一二三这种按序的执行，不可以改变它们的执行顺序。比如说这个地方有四这条指令，

不可以把四这条指令把它调到一这个地方，只要编译器得到了最终的机器指令序列，那么我们就不可以调整这些指令的执行顺序。那有的CPU可能会支持指令的乱序发射，那这一类的CPU就意味着，即便我们给出的指令序列是一二三四这么排序的。但是啊，对于这种CPU来说，这个指令的顺序是可以调整的，可以在执行的时候动态的调整。好，所以这个地方不能调整指令的执行顺序，需要和乱序发射的这种情况进行一个对比好，那回到超批量技术，由于指令的排列。

是由编译器来确定的，因此编译器在得到这个指令序列的时候，是不是就需要考虑到说哪些指令可以并行着来执行？可以把它们搭在一起。而因此，采用超标量技术，那么我们的编译优化技术的要求可能会比较高，需要考虑到哪些指令可以并行的运行，哪些不可以并行的运行。然后把可以并行的指令安排在一起，好，那这就是超标量技术。接下来第二种多发技术叫超流水技术，这个概念和超变量技术有点像，比较容易混淆，

那看图可以看出它们的区别。超流水技术是会把一个时钟周期把它再进行分段。像这个图里边就是分为了三段，然后在一个时钟周期内的不同时间点会发射出三条指令。刚才我们说的超标量技术是在同一个时刻同时发射出三条指令，当然了，也有可能是多条。这个具体得看题目给的条件，而超流水技术，它是在不同的时间点发出的，所以这是它和超变量技术的一个区别。其实看图就很直观，那么采用这种技术就意味着在一个时钟周期内，一个功能部件有可能会被使用到多次。那结合这个图可以看出来啊，

这个图里面是把一个时钟周期再进一步的细分为了三个段。所以其实这种超流水技术，它事实上是一种十分复用技术，对吧？就是把一个完整的时间段。把它细分为了三个更细小的时间段，所以它十分复用技术。而我们这个超标量流水线，事实上是空分复用技术，我们增加了多组功能部件。所以可以支持同一时刻多个事情并行的同时来干，那这就是空分复用好，所以超流水技术和。超标量流水线，他们的这个思想还是有一点区别的，

但是效果呢？都差不多，那同样的对于超流水技术来说，同样我们也不能调整指令的执行顺序。而指令的执行顺序，也是由编译器来决定的。因此，如果要让超流水线的工作效果更好的话，那同样的，我们得靠编译程序的优化来优化这个指令的执行序列。好，那对于这个例子来说，显然我们流水线的速度提升为了原来的3倍，这个很好理解。好，

那最后一种多发技术叫超长指令字。呃，看这个图，就是说我们一条指令，同样是经历了这个取指，然后ID译码。然后第三个阶段是执行。然后第四个阶段，第四个阶段是写回。这例子当中，只把指令的执行分为了四个阶段，然后我们会看到，在执行阶段，它会有多个就是同时出现的一个黑框。好，

那这种骚操作是如何实现的呢？就是当我们的编译程序发现某几条指令，它们潜在并行性。就是说如果有多条指令，他们在执行阶段所需要用到的功能部件不一样，那么这些指令的功能其实是可以让他们并行的完成的。所以，当编译程序发现了这种潜在并行性之后，会把多条可以并行操作的指令组合为一整条超长的指令。所以为什么叫超长指令字就是这个原因。这种超长的指令，其中可能会包含多个操作码字段，总长度有可能会达到几百个比特。那显然，多种操作想让它们同时进行，

那么我们就必须提供多个相互独立的处理部件。好的，那这就是三种多发技术，那么这个小节当中我们介绍了指令流水线的影响因素。关于这三种影响因素，是我们考试当中的呃高频考点，特别第二类的影响因素数据相关。这个类型的分析甚至可能在大题当中出现，我们需要通过指令的流程图来分析这个指令，执行各个阶段有可能出现的情况。关于这个问题，我们会在下个小节再来补充一些啊东西，然后除了影响因素之外。我们也需要熟悉指令流程图，还有时空图的一个画法，

另外性能指标的计算也是经常在选择题当中进行考察的，那至于流水线的多发技术，这些比较零碎的问题大家可以再结合这个导图进行一个。回忆好的，那么以上就是这小节的全部内容。
