各位同学大家好，从这节课开始，我们会进入第五章中央处理器，也就是CPU的学习。本节课我们先认识CPU的功能和基本结构，在这节课当中我们不会对CPU的啊，内部结构做过多的这种解释，但是我们需要通过本节课去熟悉。CPU内部会使用到的一些啊，寄存器以及有一些硬件部件的这种英文缩写以及各个部件，各个寄存器啊，大致有什么作用，我们需要有个印象。因为第五章是大题的啊，高频的命题点那么像二二年的这道题，

你看它给了你一个硬件电路图对吧？里边就包含了很多寄存器的名字，什么pcmdrarufr什么gprs？就是ir等等啊，各种各样的硬件部件，我们要先知道它们有什么作用，然后当我们在呃后面的学习当中去理解这些电路图的时候，我们才能够知道。哦，当一个数据从一个寄存器流到另一个寄存器的时候，它在背后想要完成的功能是什么？我们才可以把这些寄存器的功能把它整合起来。好，所以本节课我们会涉及到一些啊，比较陌生的名词，

现在我们理解的还不是很透彻，没有关系，但是需要对这些名词有一些印象。那除了二二年的题目之外，还有像一五年的这道题，你看也涉及到了很多寄存器的名字，对吧？甚至还要控制部件啊，主存总线等等好，那么搞清楚本节课的学习目标之后，我们就正式开始。去了解CPU内部会使用到的一些部件。我们先来分析CPU需要实现哪些功能，基于这些功能，我们再去扩充各种功能需要什么样的硬件部件去实现。

说来也简单CPU的功能无非就是执行程序以及检测并响应啊，中断和异常。CPU需要执行程序，这个我们已经很熟悉了，我们写的程序经过编译，链接等等一系列的处理之后，最终会生成与之对应的这种机器级的代码。那这些机器级的代码，或者说机器级的指令会被存储在内存当中。CPU执行这个程序的过程。就是从内存中取出一条一条的指令，取一条执行一条，取一条执行一条，然后逐次的往后执行就是这样的一个过程。好，

那么具体来说，当CPU执行呃一段程序的时候，需要完成这样的一些呃更细的这种细分任务，首先是取指令，并且完成指令译码。取指令不用说了，就是去访问主存对吧，然后把一条指令取到CPU内部，在CPU内部需要去分析这条指令。去解析这条指令的操作码是什么，那从而根据这个指令译码的结果去生成控制信号，那控制信号就是CPU去啊，控制内部的一些硬件部件，比如说控制aru。去工作以及呃控制内存的读写等等，

总之到底要发出什么样的控制信号，取决于当前执行的这条指令，它到底是什么样的功能？好，另一方面CPU在取完指令之后还需要去更新程序计数器PC，那么PC是CPU内部的一个寄存器的名字。这个寄存器总是指向下一条即将执行的指令，比如说当前CPU正在执行的是指令三。那么，当CPU把指令三呃从主存取到CPU内部之后。它的这个PC寄存器就会自动的加一自动的加一指向下一条即将执行的指令，那我们知道指令的执行通常来说是顺序的，对吧？一条一条往后执行。除非当前执行的指令是一个转移类的指令，

转移类的指令啊，会改变PC的值，但是如果不是转移类指令，那么PC的值就总是自动的往后加一。好，这是CPU需要完成的事情，要更新PC的值。好，那么我们说当一条指令，比如说指令三正在执行的时候啊，这条指令可能会啊，要求CPU去进行加减乘除与或非等等各种各样的算术或者逻辑运算，对吧？那么，这一条指令运算的对象，

这些数据来自于哪呢？通常来说，是来自于内存。看这个地方蓝色的这片区域表示的就是说这个程序啊，它除了指令之外，还会有一些和程序相关的数据，比如说你写程序的时候在里边定义的int型变量。或者一个呃数组等等，这些数据也会存放在内存当中，那么一条指令，如果要对啊，某一些数据进行加减乘除，这些运算的话。那执行指令的过程是不是就需要去内存里面去取相应的操作数，以及如果运算完成之后是不是应该把运算的结果写回主存，

对吧？好，所以取操作数和写结果，这也是cpo在执行程序的时候需要啊，实现的一些事情。好，最后在执行指令的时候，我们取到了这个操作数之后CPU就可以对操作数进行算数或者逻辑运算，对吧？运算的结果最后再写回主存就是这样的一个过程，那这几个动作就是CPU在执行程序的时候需要完成的一些事情。好，接下来我们说CPU还需要检测并响应呃，中断和异常。什么意思呢？

正常来说CPU会逐条的去执行啊，某一个程序对吧？但是我们知道计算机内部并不只是有一个程序正在运行。可能同时会有很多很多程序正在运行，以及当一道程序正在运行的时候。CPU可能会检测到这个程序的呃，某一些运行的异常状况，与此同时CPU可能会收到某一些IO中断。比如说当这个程序p1正在执行的时候，你用鼠标啊，点击了一下。那么，点击鼠标这个动作会使得这个IO设备向CPU发出一个中断信号，然后CPU需要响应并且处理这个中断信号。假如这个程序p1是你正在打的某一款游戏，

那么你对游戏的操作。需要点击鼠标，那么你点击鼠标的这个动作并没有被提前安排在这段程序当中，对吧？但是CPU需要对这种呃例外的情况进行处理。那除了IO设备的这个中断信号，可能会让CPU有一些呃意外的事情需要处理之外，一道程序的运行也有可能会导致某一些突发情况。比如说某一条指令，指令五它发生了溢出，或者这条指令想要访问呃内存的这个非法的区域。那这些动作都会导致一条指令的执行引发异常，那么CPU也需要去检测这种由指令的执行引发的异常，并且对这些异常的状况。进行处理。

好，所以处理中断和异常是CPU需要呃完成的事情，根据中断信号的类型以及异常的类型。CPU需要暂停当前正在执行的程序。转而去执行啊，系统内部提前安排好的啊，中断异常的处理程序。那不同的中断，不同的异常会有相对应的这个处理程序，具体的我们还会在后面进行学习。好，总之CPU的功能就分为两大块，一个是呃去执行常规的程序，另一个是呃去处理一些非常规的一些情况，比如说IO设备发来的中断。

或者由当前正在执行的指令引发的异常。好，这是CPU需要实现的五个更具体的功能。好，那么这些功能的啊，落地实现到最终都需要有时序控制功能的支持。那所谓时序控制的这个功能，就是呃，用一个时钟信号去打节拍，然后按照节奏去指挥CPU内部的各个功能部件以及各种寄存器。去相互配合着工作。好，那么这是CPU的功能，接下来我们来看CPU的基本结构。这地方嗯，

国内的各种教材，它的这个理论体系不太一样，像唐树飞老师的教材。它把CPU的基本结构分为了控制部件，执行部件以及各种寄存器，还有中断系统这样的四个部分。而袁春峰老师的教材里边，把CPU的基本结构先是划分为了控制部件和数据通路这样的两个部分。如果对应上唐老师的这个呃理论框架的话，呃袁老师的这个控制部件。它所描述的范畴和唐老师的这个控制部件所啊，涵盖的范畴是有一点点区别的。袁老师，这边会把某一些寄存器也归类为控制部件。那除了控制部件之外的，

其他部分就全都属于数据通路，简单来说，控制部件就是指挥的。而数据通路就是干活的。数据通路里边包含执行部件以及呃各类寄存器，还有中断系统，这样的三个部分。那由于最近这些年四零八的这个考试和袁春风老师的这个教材贴合的会更紧密一些。所以接下来我们基于袁老师的这个呃框架图去介绍嗯，各个部分好，首先来看的是控制部件。我们说控制部件它的职责。就是取指令，分析指令，以及根据指令类型去发出控制信号，

那么为了实现这几个具体的功能。控制部件内部就需要有一些关键的寄存器。以及关键的硬件电路好，首先看为了实现取指令这个动作啊，控制部件内部需要包含两个关键的寄存器，分别是程序计数器PC以及指令。寄存器ir那程序计数器PC它它总是指向下一条即将要执行的指令。控制部件根据内部的PC寄存器去访问主存，从主存中取得下一条指令。取完指令之后PC的值自动加一指向下一条指令。那如果说当前执行的指令是转移类指令，那PC的值可能会发生改变。就像我们写程序的时候，有if else这样的逻辑对吧？如果条件满足的话，

这个程序的执行流可能会发生跳转。好，所以PC寄存器它属于控制部件。总是指向下一条即将执行的指令。那么，控制部件取得一条指令之后啊，这条指令会被暂存在控制部件内部的，这个ir指令寄存器当中。那这样就完成了取指令这个功能。好，现在取得指令之后呃，控制部件还需要分析指令，对吧？那么，为了实现分析指令的功能控制，

部件内部需要有指令译码器这样的一个硬件电路。这个电路会分析指令寄存器ir当中的指令操作码，以确定这条指令的功能是什么。识别出指令类型之后，就可以根据这条指令的功能去输出这个译码信号。好，那么确定了当前这条指令是什么功能之后，接下来就可以根据指令的功能去发出控制信号了。那么，为了能够发出控制信号，控制部件的内部需要有这样的一些硬件部件，首先是时序信号产生部件。说白了就是负责打节奏的一二三四五六七八二二三四五六七八，提供一个时钟信号作为节奏的这个基础。好，

有了节奏信号，还需要由操作控制信号啊，形成部件去产生相应的这个啊，微操作控制信号。相当于一条指令的执行，它其实分很多步，对吧？那每一步需要哪些电路被激活，都是通过这种微操作控制信号去实现的。这个控制信号形成部件会综合考虑呃，当前执行的这个指令，它到底是什么？以及会遵守这个时序信号，产生部件，它给的这种节奏信息。

另外，还会结合某一些状态标志去进行调整。什么意思呢？比如说当前执行的是一条呃条件转移类指令jz，这条指令的意思是说如果zf标志位等于一。那么就把PC跳转到啊，这条指令指定的那个位置。而如果zf标志位等于零，那么就不跳转，那么就不跳转好，所以同样一条指令，它最终到底要怎么去执行？除了取决于指令本身之外，还取决于当前CPU内部的一些状态标志位。所以我们说呃，

产生这种微操作控制信号的时候，也需要考虑状态标志。好，这是控制信号，形成部件。好，最后呃控制部件这块还需要涵盖总线的控制逻辑。当CPU需要通过总线去和主存以及和各类IO设备之间进行这个数据交换的时候。就需要总线控制逻辑发挥作用，去控制总线传输。好，那么这是控制部件的内部啊，涵盖的一些关键的寄存器以及关键的硬件部件。好，接下来除了控制部件之外，

其他的这呃三个小部分都属于数据通路。数据通路就是干活的。比如说一条指令的加减乘除，这些活就得数据通路来实现。数据通路在控制部件的指挥下进行工作。好，那么具体看一下，首先数据通路的内部包含执行部件，那核心就是aru。执行部件的职责就是实现指令的运算工作。包括算术运算逻辑运算。那a而优，就是实现加减乘除与或非异或等等这些运算的核心部件。那通过第二章的学习，我们知道像加减乘除这些运算，

它最终除了产生这个运算的结果之外。还会产生一些状态标志，比如说ofzfcfsf这些标志信息，那这些标志信息。有可能会影响到下一步的运算动作。那运算产生的这些状态，标志位会被保存到状态寄存器fr当中，这儿的fr是flag register的缩写。中文通常翻译为标志寄存器。这是英特尔的叉八六CPU里边惯用的一种呃描述方式。而在早期的一些计算机当中，比如说IBM的一些CPU内部。对这个寄存器有另一种称呼，叫做程序状态字。寄存器英文缩写是pswr。

也可以简写为psw。好，所以psw和fr这两个术语你可以认为它们呃，指的是同一个东西。只不过是在不同的CPU内部，它的这个名称不一样而已。那标志寄存器里边保存了啊，上一次运算的这些ofzf这些标志之后。就可以用于实现条件判断以及一些转移类指令的这种控制。比如说一些条件转移，指令像jz。这条指令就会根据zf标志位呃等于零还是等于一去决定到底要不要完成跳转。那么，标志位的信息从哪来呢？就是从fr寄存器当中取到的好，

所以执行部件它就是为了实现指令的这个运算。里面包含了aru以及标志寄存器fr，在有的教材当中，甚至会把通用寄存器也归类为这个执行部件。因为当我们进行加减乘除，这些运算的时候，这些操作数往往是来自于通用寄存器组的。好执行部件这块重点关注aru关于其他的寄存器到底是属于执行部件还是属于其他寄存器这个分类，我们不需要去纠结。既然不同的教材，它的这个说法不一致，那考试大概率就不会那么考，我们关注到这些寄存器以及这些硬件部件，它们有什么作用就行了。好，

那么刚才提到通用寄存器组，以及我们很熟悉的两个寄存器，叫做mar和mdr寄存器。这些寄存器它也属于数据通路的范畴。首先，通用寄存器组，这个是我们程序员汇编语言程序员啊，使用的最多的寄存器。一个CPU内部可能会有很多很多个通用寄存器。而多个通用寄存器组合起来，就称为通用寄存器组，英文缩写叫GPRS。可以让汇编语言程序员用于暂存操作数以及运算的中间结果，还有就是像数组的首地址等等，这种地址的指针。

另外，刚才提到的mar和mdr啊，这两个寄存器我们都很熟悉了。CPU通过这两个寄存器去控制主存的读写动作。好，这是我们需要关注的一些重要的寄存器。最后，中断系统或者叫中断机构，它也属于数据通路的范畴。中断机构可以实现对异常或者呃外部中断的这种处理。异常和中断是怎么处理的？我们会在最后一章进行更详细的学习。这我们知道，中断机构属于数据通路就可以了。好，

那么以上就是cpo的基本结构。好，那么本节课介绍的这些呃，各种各样的寄存器的名字在考试当中有一些值得注意的呃考法。首先，第一个问题，一个CPU它可以支持的最大内存容量是多少？这个问题由mar寄存器的位数和CPU的编址单位共同决定。mar的位数就决定了CPU可以啊，寻址的这个地址单元数量有多少？而编制单位决定了一个地址到底对应多少个比特，考试中如果题目没有特别说明，那么CPU都是按字节编制。也就是每个地址对应一个字节，八个比特。

而如果题目有特别说明，按字编制的话，那么一个地址就对应一个存储字，而一个存储字具体是多长？题目一般会直接给出，如果题目没有显示的给出，那么我们可以默认一个存储字的位数和mdr的长度是保持一致的。好，下面有两个小示例啊，第一个例子CPU按字编制mar的位数是20位。存储字长或者说mdr的这个位数是32位，那么CPU所支持的最大内存容量是多少字节？好，首先mar二十位说明CPU可以寻址的范围是二的20次方个地址。那既然是按字编制，

而一个字的长度是32位，那么二的20次方乘以32比特，最终就得到了CPU可以支持的最大内存容量。等于4 MB。那其他条件不变，我们把CPU改成按字节编制，在这种情况下，虽然存储字长是32位。也就是mdr=32位，但是由于按字节编制，所以每个地址对应一个字节。那在这种条件下CPU可以支持的最大内存容量就是emb。好，这是第一个值得注意的地方，接下来第二个值得注意的是啊，

各类寄存器的位数到底怎么去确定？这也是小题以及大题的某一些。简答的小问当中，很喜欢去问到首先mar和mdr这两个寄存器是为了实现CPU和主存之间的，这种呃数据的读写操作对吧？mar的位数和呃地址总线的宽度通常来说保持一致，而mdr的位数和数据总线的宽度通常来说保持一致。好，这个大家应该都知道。接下来很值得注意的一个，如果我们说一个计算机，它的机器字长是32比特。那么就意味着，这台计算机，它CPU内部哈这个aru的位宽也是32比特，

这就意味着aru可以支持32比特。和32比特的两个数的这种运算，比如说两个32比特数的加减，乘除等等。好，所以机器自长，它体现的是aru的微宽好，另一方面，机器自长也决定了通用寄存器的位数。因为aru里边要处理的啊，那些需要加减乘除的那些数据，一般来说都是来自于通用寄存器，对吧？所以，通用寄存器的位数和aru的位数要对齐。

好，因此这三个东西它的位数都是一致的。这一点需要注意。好，接下来指令寄存器ir，它的位数取决于CPU的指令字长。这个不难理解，因为指令寄存器ir它的作用就是保存当前正在执行的那一条指令，对吧？所以只要我们设计好了，这个CPU的指令系统。确定了最长的指令占几个比特，那么相应的我们就可以确定这个指令寄存器的长度。好，这是ir的位数，

最后程序计数器PC的位数。通常来说PC的位数和mar的位数是保持一致的。因为PC寄存器它总是存放下一条指令的存储地址，对吧？那内存的地址范围和mar的位数是直接相关的，如果保证PC的位数和mar的位数完全一致。那么就意味着，只要是mar寄存器可以指向的呃，任何一个内存地址，我PC寄存器也能够指向，对吧？它总是在PC寄存器的射程范围之内。好，这是最常规的一种情况。再介绍一种不常规的情况。

如果某一个CPU，它采用的是定场指令字结构，并且指令按边界对齐，那么PC寄存器的位数。可以比mar更少几位。比如说某计算机，它的内存是按字节编制的mar的位数，总共是22位。那么，整个内存大小就应该是二的24次方字节，也就是4 MB。那假设在这个系统内采用的是定场指令字结构，并且呃，每一条指令固定为四字节的长度。各条指令按照边界对齐的方式去存储。

什么叫按边界对齐去存储呢？就说一个四字节的指令，它第一个可以存储的位置就是地址零到地址三这四个字节。第二个可存储的位置是地址四到地址七这四个字节。指令按边界对其存储，意味着一条指令不可以存储在二三四五这四个字节处。不可以这样。好基于这样的条件，我们知道整个内存当中。指令可以存储的位置就只有二的20次方个，对吧？因为一条指令的存储总是要消耗四个字节嘛。那相应的，我们的PC寄存器，它的位数就只需要20个比特。

好，这是程序计数器PC的位数，特别注意后一种情况。好的，本节课我们对cpo的功能以及基本结构进行了一个呃粗浅的认知。CPU的功能无非就是要例行的执行程序以及要处理一些意外的情况，也就是需要检测和响应异常和中断。执行程序这个功能又可以进一步的拆分为更细的几个呃小功能。那为了实现执行程序和处理终端异常，这些事情CPU内部还需要有时序控制的功能。就像一个DJ在打节奏一样，去控场发出节奏信号。好，另外我们介绍了CPU的基本结构呃，包含控制部件和数据通路两个部分，

前者是负责指挥的，后者是用来干活的。控制部件这个地方，我们重点关注PC程序计数器ir指令寄存器和ID指令译码器这三个东西是最重要的。PC指向了下一条即将执行的指令。控制部件根据PC的指向，取出一条指令，并且把指令放到ir寄存器当中。那取出这条指令之后，就可以交给ID指令译码器去呃，确定这条指令的功能，确定了指令的功能之后，就可以再让其他的这些硬件部件去发出相应的这些控制信号。那数据通路相关的知识，我们还会通过后面的小结进行更详细的探讨。

本节课对这些英文缩写有些印象即可。那最后我们还补充了一些呃，比较常考的要点，要知道一个CPU支持的最大内存容量是由什么因素去决定的？以及各类寄存器的位宽啊，彼此之间有什么关系？这些东西在历年的小题当中反复的出现。好，那么学完本节课之后，你再回来看一下二二年的这个硬件电路图，看一看有没有你熟悉的一些英文缩写？好，那么以上就是这节课的全部内容。
