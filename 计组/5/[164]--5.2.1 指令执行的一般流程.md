各位同学大家好，本节课我们来学习指令执行的一般过程在四零八考研当中，经常会出现这样的题目。呃，可能会给你一个硬件电路图，然后让你结合这个电路图去分析指令的功能在。这个硬件当中是如何实现的？那当我们去呃钻到硬件细节里边之前，我们需要先捋清楚指令。在呃整个执行的这个生命周期内。需要发生哪些事情？好，首先CPU需要取指令，根据PC的值去主存当中取出，接下来要执行的指令。

那与此同时，会让PC+1相当于自动的指向下一条呃要执行的指令。好取出指令之后，这个指令呃，通常来说会被放到ir指令寄存器当中。接下来就可以对指令进行译码CPU内部的控制单元，会根据指令的操作码。去确定当前这条指令是什么指令，比如说它是一个加法乘法还是别的类型。那么，确定了指令的类型，指令的功能之后，接下来就可以正式的执行指令。首先，我们需要关注当前执行的指令，

它是不是分支指令？分支指令也可以称为转移指令。假如是分支指令的话，那么呃，当前这条指令的执行可能会改变PC值。正常来说，我们PC都是不断的加一各条指令，是顺序执行的。但是我们知道，在程序当中难免会遇到类似于if else。这种分支的逻辑，那对于一条分支指令来说，呃，它的执行过程就是会去检查ofcfzfsf等这些标志位。根据这些标志位去判断，

我们if的这个条件是否满足。那根据条件，满足与不满足去决定我们是否要跳转这个程序的执行流。因此，分支指令可能会修改PC的值。好，这是分支指令的执行，我们需要特别关注，那如果不是分支指令的话，其他类型的指令可能会经过。取操作数执行以及存操作数这样的呃几个步骤，比如说典型的一个加法指令。加法的这个运算对象可能来自于主存，所以取操作数就是从主存当中把数取到这个寄存器里面。当然了，

加法的运算对象也有可能直接来自寄存器，因此从主存当中取操作数这个动作呃并不是必须的。得根据当前指令的寻址方式去决定，如果是寄存器寻址，那么就意味着操作数本来就在寄存器当中，此时就不用去主存当中取数，对吧？好，所以这一步并非每条指令都呃，必须执行好那么一条指令，无非就是对某一些数据进行处理，对吧？那取到了这个数之后，接下来就可以执行指令，比如说加法，

减法，乘法，除法等等，根据具体的指令类型。来确定到底应该执行什么样的运算。好运算完了之后，运算结果可能需要存回主存。当然，运算结果也有可能存回寄存器，所以写回主存这个动作也并不是每条指令都必须的。当我们在做题结合硬件电路分析这个指令的执行原理的时候，我们需要具体问题具体分析。首先关注一条指令是否是分支指令。如果是分支指令，那么关注这个分支条件满足的时候PC值应该如何变化，

而如果不是分支指令，那么就得看具体这条指令是什么类型，它完成了什么样的运算等等。好，那这是指令的执行阶段，那无论是分支指令的执行，还是其他指令的执行？当这条指令执行完了之后，都会例行的进入一个中断阶段。除非CPU当前处于关中断的状态，否则都要例行检查是否有中断需要处理。例如，鼠标点击或者键盘敲击，这些动作都会让CPU接收到一个中断信号。那如果检测到这些呃中断信号之后，

我们就需要对这些中断事件进行相应的处理。CPU会根据呃中断信号的类型，比如说这是键盘发出的中断还是鼠标引发的中断？根据中断的类型去。确定这种中断的处理程序入口在什么地方？那相应的就需要把程序计数器PC指向中断处理程序的第一条指令。接下来就可以像执行普通程序一样，一条一条的去啊，执行这个中断处理程序。当然，在跳转到中断处理程序之前，还需要保存断点，就像我们函数调用，你在调用其他函数的时候还知道。返回到原函数需要从什么位置继续往后执行？

那么，中断处理程序也是同样的道理。我们得知道中断处理程序执行结束之后，返回到原程序，那么它应该从哪一条指令继续往后执行？那所谓的保存断点，其实就是保存旧的PC值。原本这个旧的PC值就是指向了原本的这个程序当中，下一条即将要执行的指令，对吧？好，那么这是发生中断的情况，那如果没有发生中断的话，就可以继续执行原程序。好，

那么这就是指令执行的一般流程，这个图的目的不是让你背，而是让你建立一种呃思维框架。我们做题的时候可以基于这样的框架逐步的去分析好，那么以上就是本节课所有内容。
