好来看硬件多线程的基本概念，有这样的三种硬件多线程的实现方式，分别是细粒度，多线程，粗粒度，多线程和同时多线程。英文缩写叫smt大纲，里边只要求我们掌握硬件多线程的基本概念，所以这个部分它只可能考选择题。那我把这个部分最有可能在选择题当中进行考察的一些点总结，到了这个表里边好，接下来我们解释一下这个表里边的各个内容。首先，我们先来看一下我们更熟悉的这种不支持硬件多线程的普通的处理器。这是一个普通的处理器，

有一个指令寄存器，同时可能有一个或者多个功能部件，嗯，也就是aru那些东西。同时有一个寄存器组，这个寄存器组包含了呃一系列的通用寄存器，还有一个PC程序计数器。那这就意味着，同一时间段内，在这个处理器当中，只可能去运行一段程序，对吧？也就只能处理一个线程，相关的代码。那现在如果有两个线程的两段代码，

同时需要运行，那在这种普通处理器之下，我们会怎么做？呃，对于线程a对于线程a，它有自己的一系列指令，对吧？那这个PC是指向了线程a。接下来要运行的那条指令的地址，那如果现在我们要切换成线程b。右边这个线程，那右边这个线程，它的程序代码，它的指令和线程a是不一样的。同时，

线程b也有可能会用到这些通用寄存器，有可能会把线程a之前运算的一些中间结果给覆盖掉，对吧？所以，当我们从线程a切换成线程b的时候，是不是需要把这些通用寄存器的值以及PC的值？全部给放到主存里，这样的话，当我们从线程b切换回线程a的时候，是不是才可以把线程a的这些信息以及PC的值给它进行恢复？好，所以在传统的这种不支持硬件多线程的处理器上，我们要进行线程之间的切换，就需要去保存和恢复。线程的运行环境主要就是保存和恢复这些寄存器的值，

那这个保存和恢复的过程就给线程切换带来了呃不小的代价。好，那么接下来再来看右边这个可以支持硬件多线程的处理器，在硬件层面去支持多线程的运行。看这个处理器里边我们设置了两个寄存器组，那在这种情况下，我们是不是可以把线程a的运行环境，包括PC的值放在左边这个组？然后把线程b它的运行环境PC的值放在右边，这个呃寄存器组这样的话，如果a和b两个线程想要同时运行。那我们就没必要把这个寄存器组的内容保存到主存里，对吧？线程a运行的时候，我们使用左边这个寄存器组当中的信息。

线程b运行的时候使用右边这个寄存器组里边的信息，是不是就OK了？好在理解了，支持硬件多线程的处理器和传统的处理器，它们之间的区别之后。我们来看一下这三种硬件多线程的实现方式。首先，细粒度多，线程会轮流发射，各个线程的指令每个时钟周期内是发射一个线程的指令，那这个图给了一个示意。在第一个时钟周期内会发射线程a的j和j加一两条指令，我们可以不太严谨的这么理解，就是在第一个时钟周期内会把线程呃一的两条指令。给取进来，

然后两条指令并行的运行，为什么两条指令可以并行运行呢？因为他们有可能会使用到不同的功能部件。左边这条指令需要左边这个功能部件，右边这条指令需要右边这个功能部件，那两条指令是不是就可以同时并行的去完成？好，所以第一个时钟周期完成了呃线程a的两条指令，第二个时钟周期又会呃完成线程b的两条指令，然后第三个时钟周期会完成线程a的两条指令。好，所以每一个时钟周期都会切换一次线程，对吧？切换的频率很快，所以我们把这种呃实现硬件多线程的方式。

称为细粒度多线程好，所以线程切换频率每个时钟周期切一次，而线程切换的代价是比较低的。那显然，系列读作线程当中，同一时刻可能会有两条指令是并行的正在。执行对吧？但是同一时刻只会有一个线程正在执行，所以线程间是不并行的，但是指令之间是并行的。好，接下来看粗粒度多线程，它和细粒度多线程的区别在于我们会用连续的几个时钟周期去。去处理同一个线程的呃多条指令，除非这个线程的这些指令序列遇到流水线阻塞。

比如说呃cache缺失。这一类的流水线阻塞的事件之后，才会去切换成另一个线程好，所以这个粗粒度多线程，它并不是每个时钟周期切换一次。而是只有流水线阻塞的时候才会切换一次线程，因此它的线程切换的频率呃会更低。那值得一提的是，在流水线阻塞的情况下，去切换线程有可能会需要付出比较高的线程切换代价。因为需要重载流水线，把流水线的呃各个功能部件给填满好，这个什么意思？我们不用深究，大家只需要知道它的那个线程切换代价会更高就可以了。

那粗粒度多，线程和细粒度多线程一样，指令之间是可以并行执行的，但是线程之间是不并行的。好，最后再来看。同时，多线程这种实现方式会在一个时钟周期内同时发射，多个线程的指令。呃，来看这个表，第一个时钟周期内我们发射了线程a的指令，也发射了线程b的若干指令好。如果结合这个图来理解的话。呃，

对于线程a和线程b而言，如果采用同时多线程的这种方式，那么在一个时钟周期内，我们会把a的一条指令。呃，取到左边，然后把b的一条指令取到右边，然后接下来呃a的这条指令和b的这条指令就可以让他们并行的执行，对吧？好，所以这种同时多线程的方式呃，既实现了指令集并行，同时也实现了线程集并行，因为在同一时刻我们其实是在呃处理两个线程的。呃，

各自的指令，所以线程之间也是并行的，指令之间也是并行的，那同时多线程的这个线程切换频率和线程切换代价。这个教材里边没有详细的说明，我们也不必深究这部分，既然只会考察选择题，那么它就不会考的太深。最有可能出现的出题方式还是张冠李戴，移花接木的那种呃，错误选项的设置，比如说它可以告诉你说呃，在细粒度多线程当中。实现了指令集并行和线程集并行，如果他这么说，

那肯定是错的，对吧？他也可以这么说，在粗粒度多线程当中呃，一个时钟周期内。同时发射了多个线程的指令，那这个说法显然也是错误的好，所以为什么要给大家总结这个表呢？就是想把这三种方式的呃，各自的属性相似的，这些属性放在一起。供大家对比，这样的话，你遇到考试里边的那些呃误导选项才不会掉到坑里好，那这就是三种硬件多线程，

大家可以再结合王道书再来巩固一下。
