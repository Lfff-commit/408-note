各位同学大家好，本节课我们要探讨DRAM芯片的几种关键技术，首先我们会介绍地址，引脚复用技术。那这个技术可以用于减少DRAM芯片的引脚数量，从而可以控制DRAM芯片的体积。其次，我们会介绍DRAM芯片的刷新机制。由于DRAM芯片的这个存储元件是用电容来实现的，而电容的物理特性。天然的决定了，它会随着时间的推移发生漏电，那这就会导致电容里边存储的啊比特零比特一啊，随着时间的推移，可能会发生变化。

从而导致数据的错误，那为了解决这个问题，我们需要引入刷新机制。那最后我们还会介绍行缓存机制，这种机制可以加速DRAM芯片的呃读写。大家应该听说过程序的局部性原理，一段程序啊，在运行的时候在某一段时间内。有可能经常会访问地址连续的多个存储单元，比如说当一段程序在遍历数组的时候啊，就是这样的一个情况以及一段程序顺序执行的时候。这个程序的指令也是存放在地址连续的多个存储单元里边，所以在计算机内部程序运行的时候。CPU访问连续的多个地址，这种事情是大概率事件。

那么，在DRAM芯片中引入了行缓存机制之后，就可以加速对这种连续地址的呃读写速度。好，那么接下来我们先介绍地址，引脚复用技术。这个在前面的课程中也有简要的提及好，那么这张图是一个SRAM以及DRAM芯片的内部结构图。可以看到中间的这个存储矩阵，它是64行64列，每一个行列对应八比特的信息。64行64列，对应四零九六个，这个存储单元那么总共就需要12比特来表示啊，完整的这个地址。

在这张图示当中，更高的六个比特，我们从上面这个端口输入更低的呃六比特地址，从下面这个地方输入。那上面这输入的就是行地址，下面这输入的六比特就是列地址。那假如这是一块DRAM芯片，那么这个DRAM芯片通常会采用地址引脚复用技术，就是把六比特的行地址。和六比特的列地址通过六根地址引脚。先后输入到这个芯片内部。比如说来看一个具体的例子，这是一块DRAM芯片，它总共包含4m个地址。每个地址，

或者说每个存储单元对应四比特的信息。我们总共需要22比特来表示，这块芯片的内部地址。但是由于DRAM芯片使用了地址引脚复用技术，因此我们在这儿只看到了11个地址引脚。总共22比特的地址被拆分成了11比特行地址和11比特列地址。分两次先后从这11个引脚处输入。把这块DRAM芯片的内部结构啊，具体展开大概就长这个样子。可以看到，4m个存储单元嗯，被排列成了二零四八乘以二零四八这样的一个呃正方形的结构。每一个存储单元对应的是四比特的信息。左边这儿a0到a10对应的就是我这儿标绿的啊，这几个芯片引脚。

那么行，地址和列地址各11比特，我们可以分两次从这11个引脚处输入。可以看到，这儿有一个行地址缓冲器，列地址缓冲器。相当于可以把11比特的行列地址呃暂存下来。好，然后上面这儿是控制电路。这有两个控制信号，分别叫ras和cas，这两个控制信号分别用于呃指明。当前输入的这11个比特，它到底是行地址还是列地址？如果当前输入的是11比特行地址，

那么ras这个控制信号就应该有效。那由于这个控制信号的啊，上方画了一条横线，因此这个信号的有效状态是比特零，也就是对应低电平。那与此同时cas应该保持为比特一。那么，这两个控制信号就会呃，让相应的控制电路去打开这个含缓冲器的呃，一个写入口。于是11比特的行地址信息就可以放入行缓冲器。好传输完行地址之后，接下来要传输列地址，同样的11比特的列地址信息，通过这11个引脚输入。

与此同时ras给出高电平信号。cas给出低电平信号，此时说明我们正在送列地址。此刻列地址缓冲器的入口被打开，于是11比特的列地址信息就可以通过地址引脚传入这个地方。可以看到，行地址经过呃一个多路选择器mux来到了行页码器。关于这个多路选择器有什么作用，我们一会再回来解释，反正这个行地址最终是要来到行译码器这个地方。那列地址会接到列译码器，显然这两个译码器的型号应该是十一二零四八译码器，对吧？输入的是11个比特，那二的11次方等于二零四八，

所以输出的这个线路应该总共有二零四八条线。根据输入的11比特来决定，这2048条线当中到底哪条线输出有效信号？那相应的，这个列译码器，它的二零四八条输出线应该是往上走的，连接到这个啊IO门这个地方。最终，二零四八条呃列选择线会接到这个存储矩阵的每一列处。那么，根据行号和列号信息，最终就可以确定。此次要读或者要写的是存储矩阵的哪一个位置？那具体要读还是要写是通过we这个控制信号来控制的，如果此次是要读某一个呃地址。

那么，这四比特的信息会经过这个读出放大器，然后再写到数据输出缓冲器这。当这个信号稳定了之后，就可以让oe输出使能信号有效。于是，四比特的信息就可以顺着啊，第一到第四这四个地址引脚处并行的往外传。那这是读操作。那相应的，如果是写操作，那么we这个控制信号啊，做出相应的调整，然后要写入的四比特信息。通过啊芯片的这四个数据引脚处并行的传入。

这四比特的信息会首先被放入数据输入缓冲器中。然后经过读出放大器IO门这个地方，最终被写入到存储矩阵当中，对应的行列那个位置。好，那么现在你应该能够看懂这个电路图的含义了。以及通过这张电路图，我们看到了地址引脚复用技术背后的一个实现原理。那地址引脚复用啊，最典型的考题就是一四年的15题，很多同学可能做过这道题目。这道题考查的就是DRAM芯片的呃引脚数量。那如果我们把DRAM芯片这个条件改成SRAM芯片。那么，这道题目的答案就会变得不一样，

因为SRAM它不会采用地址引脚复用技术。好，那么没做过这两道题的同学可以暂停来做一下，三秒钟后公布答案，这两题应该选择a和c两个选项。具体的分析，我们就不再赘述，大家可以自行看这个解析。可以看到，在这道真题中，出题人并不会好心的提醒你说DRAM芯片采用了地质引脚复用技术。所以，在解决真题的时候，但凡提到DRAM芯片，我们都要默认它一定采用了地址引脚复用技术。

那这会影响到我们对题目条件的解读。好，那么没跟上的同学可以暂停来理解一下这两道题。接下来，我们介绍DRAM芯片的第二项关键技术。刷新机制，这是一款DRAM芯片的存储矩阵。总共64行64列。每一个地址对应八个比特，所以在实现层面，这个存储矩阵，它应该由八个64×64的位片。啊，叠加而成。由于是DRAM芯片，

所以每一个小格子对应一个DRAM的存储元件。由一个电容以及一个MOS管组成。这个MOS管的导通与否是通过啊，红色的这条自选择线，它是高电平还是低电平？来决定的，如果红色的这条自选择线加高电平信号，那么MOS管就会导通。此时电容里如果存储了电荷，那么这些电荷就会顺着这条线往外跑。那相应的，如果此时在数据线上加上一个高电平信号，那么就可以往电容这儿充电。那如果电容里边存储了电荷，我们就认为这个存储源，

它对应的是比特一，如果没有存储电荷，那么对应的是比特零。那电容虽然可以存储电荷，但是这个电荷会随着时间的推移逐渐的泄漏。那这就会导致原本存储了电荷的电容，因为漏电逐渐的变成没有电荷。那就会发生比特一变成比特零这种错误，对吧？好，那么为了避免这种错误，我们需要引入定时刷新的一个机制。在DRAM芯片当中啊，刷新是这么做的，会逐行定时刷新。

每一行多久刷新一次呢？这个取决于刷新周期的长短，而刷新周期的长短又取决于这个电容啊，它的。信息不失真的一个啊，时间的上限。比如说假设我们设计的这个电容，它在64毫秒之内可以确保啊，里边保存的信息不失真。也就说64毫秒之内，虽然电荷会逐渐的泄露，但是泄露的还不是特别厉害，不会导致我们把比特一。误判为比特林。那在这种条件下，

我们就可以规定说这个电容的刷新周期，把它设置为64毫秒。每隔64毫秒，我们要给有电荷的电容补电。以及原本没有电荷的这个电容进行彻底的放电。那这就是刷新周期的由来好，那么既然每个存储元件每隔64毫秒，就得给它刷新一次。而刚才我们说DRAM的刷新是整行，整行进行的。那么像我们给的这个例子，它总共有64行对吧？存储矩阵总共有64行，那我们是不是可以这样去设计啊？比如说每隔一毫秒就刷新一行。

第一个一毫秒，我们先刷新第一行的所有的这些存储源。这些存储源该充电的充电，该放电的放电。好下一个一毫秒，再刷新下一行的存储源。那这样的话，刚好每隔64毫秒，所有的这些行都可以被刷新一次。这样就保证了啊，这块芯片不会因为电荷泄漏的问题而发生比特错误。好，那假如我们这个存储矩阵，它总共有128行，行数更多的话，

那同样是64毫秒的，这个刷新周期。128行的存储矩阵，我们是不是就得加快这个刷新的频率啊？每零点五毫秒就得刷新一行，对吧？只有这样才可以保证每64毫秒，所有的行都会被再次刷新。那值得一提的是，每刷新一行这个存储芯片是需要付出一些代价的。每一次刷新需要消耗一个存取周期。那在这个刷新周期不变的呃条件下，行数越多的这种存储芯片，它的这个逐行刷新所需要付出的代价也会越高，对吧？

好，我们回到前面这张DRAM芯片的结构示意图。在这儿有两个东西，刚才没有提，一个是刷新计数器，一个是mux多路选择器。这个刷新计数器其实就是指明了行号，当前要刷新的是哪一行？可以看到上面这个方形的部件，这它有定时的功能，这个定时功能就是根据刷新周期以及整个存储阵列的行数。去决定每隔多长时间应该刷新下一行，那么在这个定时功能以及呃相应的控制线路的这个作用之下。我们的这个刷新计数器就会隔一段时间往上加一。比如说刷新第零行，

然后再隔一会儿刷新第一行，第二行，第三行这些计数器会不断的增加。那对于这个存储帧率来说，它最终要刷新的是呃，第二零四七行等刷新计数器，到二零四七之后下一次。再加一又会回到零，也就是重新再刷新第零行，就这个意思。好，所以刷新计数器这个地方给出的其实是行号的信息。好，现在再来观察mux这个多路选择器，这个多路选择器，

它有两个输入以及一个输出。两个输入的信息都是行号的信息。你看下面这接的是含地质缓冲器嘛？那么，控制电路会给这个多路选择器发出控制信号零或者一。如果mux的控制信号为零，那么这个多路选择器就会让上面这一条输入线路。通过，然后去输出。给到含义码器。那什么时候会让上面这个行号通过呢？显然就是如果目前呃，刚好要对其中的某一行进行刷新，对吧？那么就让刷新计数器里边指明的这个行号。

通过，然后去选中这个存储阵列的某一行，并且对那一整行进行刷新的动作。那相应的，如果mux的控制信号，比如说变为一，那么mux的下面这条输入线路就可以让它通过。就可以让他通过。此时这边给到行译码器的这个行号信息，就来自于行地址缓冲器。比如说，当前正在进行读操作，对吧？要读某一行某一列，那么在这种情况下就应该让下面这个行号通过。

然后去选中存储阵列的对应位置。好，那么这是这个刷新计数器以及这个二选一通的多路选择器，它们的一个作用。好，我们回到这一页，刚才我们用呃定时逐行刷新的这种方式解决了这些电容呃漏电的问题。接下来还有一个问题有待解决。由于电容是破坏性读出的。因此，读出数据之后，我们需要立刻对电容里边的这个比特零或者一进行再生，也可以称为重写。如果这个电容本身存储了比特一，那么它里边应该是有电荷的，

假如要读这里边的比特信息，那么自选择线这儿就会加一个高电平信号。相应的，这个MOS管这就会导通，那MOS管只要一导通里边存储的这些电荷，是不是就会顺着这条线路跑出去，对吧？我们可以根据这根位线，这儿到底有没有检测到这个电流的脉冲来判断？我们读出的这个比特到底是零还是一？好，那么电荷跑了，是不是意味着原本存储了一的这个电容，它的状态就会切换为？没有电荷，

也就是变成零，对吧？所以我们才说它是破坏性的读出。好，那这种破坏性的毒出得用再生机制去解决。它的原理和刷新是类似的，每次读取呃比特信息之后，就需要立即再申。这个再生的动作是由DRAM芯片的内部呃控制电路去呃自动完成的。如果本次读出的是零，那么就应该让电容充分的放电。要稳定在零这种状态不变，而如果本次读出的是一，就应该为电容重新充电。那值得一提的是，

对于DRAM芯片的这个存储阵列，当我们读取某一个地址的时候。这个地址信息会被拆成行号和列号，对吧？那么根据行号对应的一整行的存储元件都会被激活。也就是都会放电。比如，当我们要读啊，第零行第63列，这个呃，具体的地址的时候。第零行的所有的这些存储元件。都会被激活。也就是第零行的每一个存储元件的，这个自选择限，

这我们都是统一的，给它用一个高电平信号去控制的。因此，所有的存储元件的这个MOS管都会导通，对吧？那这是不是会导致第零行的每一个存储元件电容里面的电荷都会往外跑？所以虽然本次我们要读的只是这八个比特，但是这一整行的所有的这些存储员都会受到影响。因此，我们刚才说的再生这个动作，也需要以行为单位进行再生，并不是说读了这个位置，只需要再生这八个比特。而是要把这一整行的呃64×8个比特全部都一起再生。好，

怎么理解这句话呢？给大家画了一个更详细的示意图。这张图表示的是啊，前面这个存储矩阵的其中一个位平面。其中一个位平面。它总共有八个未平面对吧，但是如果八个未平面，我们全部叠在一起画的话，这个图就会变得非常凌乱，所以我只画了其中的一个未平面。大家自行想象一下，这是x轴方向行的方向。这是y轴。列的方向。那在纵向空间z轴的方向。

其实是有八个这样的呃位平面堆叠而成的。好来理解刚才说的那句话，为什么说我们读取某一个地址的时候，对应的那一整行存储源都会被激活？来看它的内部连线。观察左边这儿，这是一个行地址译码器。结合我们的例子，应该选用六六十四译码器这种型号。二的六次方对应64个状态。所以这个译码器的输入端，我们输入六比特的行地址，而输出端这边应该输出64根行选择线。根据行地址的状态来决定，这64根线当中，

哪一根线的呃输出信号有效？好，我们假设当前CPU想要读啊，临行63列这个地址。零行63列呃，如果对应12比特的这个完整地址的话，应该是六个零作为行号。然后六个一作为列号。那行，地址六个零，它作为译码器的输入。这就会导致译码器的第零根线就是最上面这根线。它会输出一个有效的高电平信号。而除了这根线之外，其他的所有的呃线路都是低电平无效的信号。

好，那么上面这条线是高电平信号，是不是意味着第零行的所有的这些存储源？它的MOS管都会被导通。都会被导通，对吧？虽然此次毒操作我们只关心第零行第63列的那个呃，那八个存储源。到底是什么状态？但是根据drem芯片的这种啊，电路设计。除了我们关心的那个存储员之外，其他的这些存储员也都会受到影响。所以其他这些存储源里边的电荷就会跑出去，对吧？

因此我们需要对一整行的所有的存储源都立即再生。好，那么结合这个电路图，大家就可以理解，刚才我说的那句话了。我们需要对一整行的信息立即再审。好，所以DRAM芯片的再生机制以及DRAM芯片的刷新机制都是以行为单位去进行的。那显然，我们对一整行的这些啊比特进行再生，肯定需要付出一些时间代价，对吧？那正是这个时间代价导致了DRAM芯片的存取周期。要大于SRAM芯片。还记得存取周期的定义吗？

这块芯片从这一次读到下一次读，中间所需要间隔的这段时间就叫做存取周期。现在我们知道了中间的这段时间，本质上是在对这一整行的信息进行再生，对吧？那相比之下SRAM芯片由于它并不是破坏性的读出，所以它就不需要有再生的这个动作。那相应的srm芯片的这个存取周期是不是就会远远小于DRAM芯片？那这就是背后的原理。好，那么由于刷新一整行的信息以及再生一整行的信息，这个背后是需要付出时间代价的。那正因如此，我们在设计存储阵列的这个啊规格的时候，也就是决定它到底有几行几列的时候。

我们应该综合的去考虑地址引脚的数量以及行刷新的开销，这样的两个因素。我们说存储阵列的行数越少，那么在每一个刷新周期内。逐行刷新所付出的这个时间代价也会越小，对吧？那另一方面，地址引脚的数量也和这个存储阵列的行地址位数和列地址位数息息相关。取二者当中更大的那个值。好来看个例子，假设某一个芯片啊，它的容量是4m×8比特。那4m这个大小，我们可以把它设计为二零四八行乘，以二零四八列可以是这样的规格，

也可以是一零二四行乘，以四零九六列。那这两种规格所对应的地址引脚数量啊，就会有区别，在上面这种方案当中。无论是行号还是列号，我们都只需要11比特去表示。二的11次方刚好等于二零四八。那相应的采用地址引脚复用技术，我们只需要11个地址引脚，是不是就可以传输啊？这些行地址和列地址的信息了。而如果我们采用下面这种方案，那么行号我们就只需要用十个比特来表示。而列号我们需要用12比特来表示。

那由于列号的这个宽度更宽，所以我们地址引脚的数量肯定要向更宽的这个列号去靠齐，对吧？所以采用下面这种方案，我们会让这个地址引脚的数量变多。不过相应的，它也带来了一些好处，因为存储阵列的行数减少了，所以在一个刷新周期内，我们只需要进行一零二四次的刷新。相比之下，上面这种方案一个刷新周期内逐行刷新的这个次数是不是更多？那我们说了。每刷新一行，都是需要消耗一个存取周期的。

所以刷新次数多，那么刷新付出的代价也会更高。好来看一个真题。一八年的17题，大家可以先暂停做一做。十秒钟后公布答案。三二一这道题选择c选项。这道题给出了芯片的容量。它要求这块芯片的地址引脚数量要最少，在这个条件下还需要保证尽可能的让刷新开销呃。更低问我们这块芯片的这个存储阵列应该是多少行多少列？好2 k=2的11次方，所以这块芯片的地址总位数应该是11比特，那这11比特我们需要把它拆成行号以及列号，这样的两个部分。

我们不妨把abcd几个选项都分析一遍。对于选项a，如果存储阵列，它是二零四八行一列。这种形态的话，那么整个11比特的地址。会完全作为行号而列号零比特，因为本身只有一列嘛，所以你根本没有必要去指明你到底要读的是哪一列，对吧？你只会读一列。所以a选项的这种呃存储阵列设计，它本质上是用一维译码法。11比特的地址并没有拆分成行列两个维度。它只有一个维度。

好在这种条件下，地址引脚的数量应该是11个。那由于总共有二零四八这么多行，所以在每个刷新周期内需要刷新二零四八次。那类似的另一个极端是d选项。d选项说呃，这个存储阵列是一行二零四八列。那基于这样的条件，行地址可以没有零个比特，11比特的地址信息全部作为列地址。此时也是一围一码。那么，地址引脚数量也是11位。由于整个存储阵列，它只有一行，

所以每个刷新周期就只需要刷新一次。这个刷新的开销是极低的，对吧？但是地址引脚数量并不符合题目的要求。好，我们想让地址引脚数量最少，那么我们应该尽可能的让行号和列号就是尽量的接近。好那么b和c两个选项就是让行列的数量尽可能接近的两种方案。b选项，64行32列。二的六次方等于64，二的五次方等于32，所以相应的b选项的这个行地址就是。六比特列地址就是五比特，这是典型的二维一码法，

对吧？那地址引脚的数量应该是和行地址的位数和列地址位数当中更大的那个为准。好所以引脚数量等于六，那总共64行就意味着每个刷新周期需要刷64次。那相比之下c选项，它既保证了地址引脚数量最少，又确保了刷新的开销尽可能的低。因此，这个题目选择的是c选项。好，这是一八年的17题，可以看到存储阵列的行数和列数，它既会影响地址引脚数量，也会影响行刷新所带来的开销。我们要根据题目的要求去综合的考虑两个因素。

OK，那最后我们再来介绍DRAM芯片的缓存机制。正是有了这种行缓存的机制。才让这个DRAM芯片可以支持突发传输，好先观察这张图。中间有四行四列的存储矩阵，每一个小格子对应的是八比特。所以整个存储阵列，它的规格就是呃，16×8位。对了，这个地方有一个小细节，在有几个印刷批次的王道书当中，这个地方它标注的是存储源这个术语。如果你的书是这样的，

那么把它改成存储单元更合适。一个存储元，也就是存储元件，它对应的是一比特的信息，而一个存储单元。在这个例子中，它对应的是巴比特的信息，所以在这个图示里边给定一个行号和列号。那么，这个行列号，它指向的这个小格子，我们应该把它视为保存了巴比特的信息，对吧？所以它对应的应该是存储单元，而不是存储元。

好，这个小细节，如果大家的书印错了，那么把它改一下。好，这块存储芯片总共有16个存储单元，二的四次方等于16，所以总地址的位数应该是四个比特，对吧？那我们把它拆成了四行四列，那相应的行地址和列地址就是各占两个比特就行了。那由于采用了地址引脚复用技术，所以你会看到这个地方输入的地址线，它的宽度只有两位。行地址，

列地址，复用这两个音角。好，另外下面这个地方数据线的宽度是八位，因为每一个存储单元对应的是八比特。这八比特的信息是并行的呃，传入或者传出的。好观察这张图，下面这四个小格子，它表示的是行缓冲器，总共四个小格子，每个小格子可以存储八位的信息。那它的作用是什么呢？我们结合具体的例子来感受，假设现在CPU要对地址一一零一啊进行读操作。

把它拆成行号和列号，那么对应的就是第二行第一列，这个存储单元那通过之前的学习，我们知道。如果给定了行号和列号，那么行号对应的这一整行所有的这些存储元件都是会被激活的，对吧？如果电容往外漏电了，那表示比特一如果电容没有往外漏电，那么表示比特零对吧好？那么既然这一整行所有的比特都要往外漏。那我们为什么不呃，干脆把这一整行的这些所有的比特信息全部缓存下来呢？好，所以虽然在这个例子中，

我们要读的是第二行第一列这个存储单元，那由于这一整行的所有的存储元件都会被激活，对吧？那么，我们干脆把这一整行这四×8比特的信息唉，全部把它放到一个行缓冲器里。那这个行缓冲器通常是用SRAM去实现的。上面这是DRAM，下面这是SRAM。那我们知道SRAM的读写速度要远高于DRAM，对吧？因为DRAM每读一次，我都需要对这一整行的所有的比特都进行再生。或者叫重写，而SRAM没有再生重写这个动作，

因此刚才我们把这一整行的信息都加载到这个行缓冲器之后。是不是意味着虽然目前CPU，它只是想读红色的这啊八个比特数据，但是如果接下来CPU还要读相邻的这些。同一行的其他数据的话，那么这些数据就可以直接从SRAM芯片这儿通过数据线传出去，对吧？那这样的话，数据的传输速度是不是就可以提升啊？所以引入了这种行缓冲技术之后。只要这一整行被加载到行缓冲器里，那么接下来每一个内存总线的时钟周期都可以连续的快速的读出啊，各个存储单元里边的数据。现在我们还没有介绍内存总线的时钟周期是啊，什么含义？

当前阶段，我们只需要了解这个DRAM的存取周期和内存，总线时钟周期之间的一个关系就可以。一般来说DRAM芯片的一个存取周期等于多个内存总线的时钟周期。好，那这意味着什么？如果我们每一次的读操作都是直接去这个DRAM芯片的呃存储阵列里边去读。那么一次读和下一次读之间时间间隔至少是一个存取周期，对吧？那也就相当于要等很多个内存总线的时钟周期才可以进行下一次读操作。但是现在引入了行缓冲技术之后，我们每一个内存总线的时钟周期内都可以至少进行一次读操作。那这样的话，我们读的速度就明显提升，对吧？

那这也是我们的主存，可以支持突发传输的一个底层原理。所谓突发传输，就是CPU提供一个首地址，然后连续的读取多个啊，相邻的这种存储单元里边的数据。好关于突发传输的知识，我们还会在总线那个部分有更进一步的探讨。现在我们重点体会行缓冲机制背后的一个原理。有了行缓存机制，才可以支持突发传输。好的，本节课的内容比较多，并且不太好理解。我们介绍了DRAM芯片的三种关键技术，

分别是地址引脚复用技术刷新机制以及行缓冲机制。地址，引脚复用技术的目的是要减少DRAM芯片的引脚数量，从而去控制芯片的体积。这个相对好理解，那刷新机制要解决的是DRAM的存储元件，也就是电容会漏电的一个问题。漏电不太严重的时候就给电容重新补电。以确保电容里边保存的比特零比特一的这个信息不会发生错误。那这就引出了刷新周期这个概念。我们刷新是以行为单位进行的。某一个特定的行从这次刷新到下一次刷新，中间的时间间隔就是刷新周期。那刷新是逐行进行的。与之类似的，

当我们读某一行某一列的这个呃存储单元的时候，一整行的信息都会被破坏。所以我们需要对一整行进行再生。也叫重写好，那么由于读操作会破坏一整行的信息。那反正都破坏了，我们不如把这一整行的信息给它缓存下来。把整行的信息缓存到一个更快速的SRAM芯片当中，那这就引出了行缓存机制。有了行缓存机制，我们就可以支持突发传输。这样可以加速CPU对于多个地址连续的这个存储单元的这种读写速度。在之后的学习中呃，大家会了解到cache这种机制。touch和内存之间每一次会交换一整块，

也就是地址连续的多个存储单元的数据。那cache和内存之间的这种数据交换，本质上也是利用了突发传输以及利用了这种DRAM的行缓存机制。那随着后面的学习，大家会越来越认识到啊，这个关键技术它到底有多大的作用？好的，那么以上就是本节课的所有内容。
