各位同学大家好，在这节课我们要探讨存储器芯片的组成结构，先给出存储器芯片的结构图。那无论是SRAM还是DRAM，它们的那个呃芯片内部结构都类似于呃这张图所示的这种结构。那正式讲解这张图之前，建议大家把a0到a5a6到a11。这些地址位的信息呃标号给它改写一下，我们把它改为上面，这是a6到a11。下面这是a0到a5，总共有12比特的地址信息，我们用更高的六比特。作为行地址信息而更低的六个比特，作为列地址的信息。

这样的分配方式更符合DRAM芯片的惯例，而四零八的考研大纲对DRAM芯片的这个要求是要比SRAM芯片更高的。所以，为了贴合考研建议，大家先把这张图的这个地址信息啊，按照我这儿这样给它改造一下。好，接下来我们先聚焦于这张图的中间存储矩阵，这个部分可以看到一个存储矩阵是64×64，这样的规格。总共有一二三四五六七八八个存储矩阵芯片好，怎么理解这个存储矩阵呢？我们把它展开来看一下，我们把这个存储矩阵内部展开就长这个样子，我们不妨用大家熟悉的呃xy这样的坐标给它标注起来。

可以看到，在这我总共画了64行以及64列的这种小格子。好，那么每个小格子都可以用xy，也就是行号和列号这样的两个坐标去表示。我们不妨规定行号和列号从零开始，那么像最右上角的这个小格子，它的行号和列号就分别是零和63。那左边像紫色这个小格子，它的行号是一列号是零蓝色，这个小格子行号是一。列号，13。好，别的都类似。

那每一个这种小格子对应一个比特的存储源，根据这个存储器，它是SRAM还是DRAM？我们可以在这些小格子处安排呃，两种存储源SRAM的存储源是由六个MOS管组成的。而DRAM的存储源是由一个电容以及一个MOS管去组成的。好，所以如果你要制造的是SRAM芯片，那么就在每一个这种小格子里边塞进一个SRAM的存储元件。而如果你要制造的是DRAM芯片，那么就在每一个小格子里边塞入啊，一个这样的DRAM存储元件。那这样的话，每一个小格子就拥有了存储一比特信息的能力。那么，

像这样的一个64×64的存储矩阵，它里边总共包含了四零九六个存储元件。那这些存储元件组成了一个未平面。并且在这个位平面中，我们只要给定一个行号，一个列号就可以指明对应的一个比特。那由于总共有64行64列，所以我们需要分别用六比特来表示列号以及六比特来表示行号。那值得一提的是，我们这儿看到的位平面，它是一个规则的正方形，对吧？但事实上，如果你是这个存储芯片的设计者。那么呃，

你也可以让这个v平面呈现出矩形。比如说同样是四零九六个存储源组成的位平面，你也可以把它变成32行128列这样的一个规格。那在这种情况下，行号和列号的比特数就需要做出相应的调整，二的五次方等于32，所以行号应该用五比特来表示。二的七次方等于128，所以列号就应该用七比特来表示。好，所以行号列号的这个比特数和这个位平面的物理结构是有关系的。好，接下来我们要把八块同等规格的位平面。在z轴方向进行一个堆叠。变成这个样子。

观察前面给的这个电路图。一二三四五六七八八个64×64的存储矩阵，它在背后就应该长这个样子。当我们把八块同等规格的位平面堆叠起来之后，我们就组成了一个自长等于八比特的存储矩阵。此时如果我们给出一个行号列号，比如说行号等于零，列号等于63，那么这个行列号对应的就是八个比特的信息。相当于把八个位平面同一个位置的这个存储元件呃，把它们组合起来。形成一个八比特的字长。如果只看单个位平面的呃，这样的一个存储源，那由于它只能存储一比特的数据，

所以我们把它称为位元。而如果八个呃对应位置的这个位元，把它们组合起来，那就变成了一个超元。好，那么这是这个图示背后的一个呃细节。对了，在有的题目当中，或者在有些教材当中呃，这种八个呃位平面叠加起来的这种电路图，也有可能会被简化的记作。这个样子。就是说可以简单的画一个矩形，然后在这个矩形内部标注呃，有多少行多少列。

然后每一个行列号对应的比特数有多少？那这样的图示和上面这种图式是等价的。好，接下来我们继续理解这个电路图的其他部分。通过前面的分析，我们知道这块存储芯片，它总共包含4k个，也就是四零九六个存储单元。那么相应的就应该用12个比特去表示这个芯片的内部地址对吧？二的12次方刚好就是4k。好，现在我们把这12比特的地址拆分成高六位和低六位，分别作为行地址以及列地址。那行，地址对应的就是呃a6到a11，

而列地址对应的就是a5到a0。好代入一个具体的数值作为例子，假设现在我们要读或者要写这样的一个地址。那把它的高六位低六位拆出来，可以看到行地址是这样的，六个比特列地址是呃六个全零。那六比特的行地址信息会首先放到这个行地址寄存器里边，这个是行地址寄存器。同样的，六比特的列地址会被呃放到列地址寄存器里边。好，接下来你会看到这有一个呃x译码器，也就是行地址的译码器。而右下角这有y译码器，也就是列地址译码器。

这个译码器的左边这会有六比特的输入信号。而右边这个地方可以看到它有零到63，总共有64个。输出信号。那译码器的作用是根据输入的这六比特信息去决定输出的这64比特。当中到底哪一个比特是有效的？比如说现在行地址是零零零一。那么，这个值对应的就是十进制的一。所以经过译码器的处理，它的64根输出线路当中，只有这个一号输出线路。它的信号是有效的。这儿会输出一个高电平的信号。而其他的呃零号线以及一直到63号线都是低电平信号。

好，那么只有一号线，它是高电平信号，那么这根线输出的这个有效信号在经过这个驱动器的放大之后。最终编号为一的这一整行。存储源。就会被激活。那与此同时，再看列地址这个方向。在我们这个例子中，列地址是六比特全零，那么这个列地址译码器和行地址译码器原理是雷同的。在输入这一端，需要输入六比特的列地址信息，而输出这一端。

它总共有64条，总共有64条输出线。因为刚好二的六次方等于64嘛。所以，根据六比特的输入信号，译码器会决定到底让其中的哪一条线。的输出信号有效。那在这个例子中，就是零号输出线，它的这个信号是有效的。只有零这根线。会有一个高电平信号。好，那么经过IO电路的信号放大。编号为零的这一整列的。

这些存储源会被激活。那最终行号列号交会的这个存储员，也就是第一行第零列的这个存储员。以及相对应的呃，总共这八个存储源。里边的巴比特信息就可以被读出或者被写入。好，那么我们再来看电路的剩余部分。先看右边，这有输入和输出呃两组信号。如果要从这个存储芯片这读出八个比特的话。那么，这八比特的信息就是从这条线路这出来的。而如果是要写入八个比特，那么要写入的八个比特信息就是从这个地方传入芯片。

那到底是要对这个芯片进行读，还是写操作呢？这个是通过呃读写控制信号来完成的。下面这根线，这儿对应的是一比特。如果这个比特为零，那么表示我们要进行的是写操作，于是从这个地方输入的八个比特信息。就会呃，经过这个控制，电路来到IO电路，然后就写入到。一行零列，这八个比特的位置。这是写操作。

那相应的，如果这个控制信号给的是一，那么表示这是进行读操作。此时，刚才被选中的这个一行零列的这八个比特信息会。经过这个IO电路。然后来到输出驱动，这个输出驱动的作用是放大这个电信号，然后再输出这一端，把八个比特传出去。这个是读操作。好，所以到底要读还是要写？是通过这一比特的控制信号来完成的。对了，

这个读写控制信号we，它是write enable的缩写。很多中文教材也会把它翻译为写史男性号。write写enable使能够。writing enable写使能。好，另外右边这我们还有一个信号叫做片选信号。英文缩写是CS。c是chip s是select。chip是芯片的意思，芯片选择。骗选信号。就是说只有这个信号有效的时候，这块芯片才是被选中可以被读写的状态。那这个片选信号也只有一个比特。

可以用于实现多个芯片的组合扩展。好，什么叫多个芯片的组合扩展呢？来举个例子。假设现在你手里只有4k×8比特这种规格的存储芯片，你有四块，那么你想用这些存储芯片？把它们进行合理的连接，组成一个16k×8比特的就是更大容量的存储器。那你可以这么做。这是四块4k×8比特的存储芯片。每一块存储芯片都有一比特片选信号的，这个呃输入端口。好，那么最终你组成的这个存储器，

它总共有16k这么多个地址对吧？那么16k对应的就是二的14次方，所以地址的总位数应该是14比特。每一个存储芯片内部总共是四零九六个这个地址。所以每个存储芯片内部呃只需要12比特去表示片内地址。好，那么地址的总位数14比特当中，我们把更高的两位。更高的两位。把它作为二四译码器的一个输入二四译码器的意思就是说呃，输入两个比特的信息。然后由于二的二次方等于四。两个比特可以对应四种状态，对吧？所以这个译码器的输出端，

它会对应四根线。也就我这画的这四个箭头，如果输入的高位两个比特是零零，那么就让右边这条线。信号有效相当于就是把最右边这块芯片给选中。那类似的道理，如果输入的这两个比特是零一，那么就让呃。这一条线输出有效信号，这样的话，呃倒数第二块芯片被选中。那同理，如果最高两位为一零或者一一，那么就分别选中这块和这块芯片。那只有片选信号有效的那块芯片才会被读或者写，

对吧？那我们只需要把更低的12比特作为这几块芯片的片内地址。再结合上片选信号，是不是就可以完美的实现这个16k×8比特的存储器了？那这四块芯片的地址范围分别就是零零，后面是12个比特零一，后面是12个比特一零，后面12个比特一一。后面12个比特。好，那么这就是骗选信号的作用。可以实现多块芯片的连接。那多块芯片的连接方法，我们还会在后面有更详细的探讨。这我们先对片选信号有一个呃大致的认知。

好，本节课的最后我们再来补充呃两种译码方式，分别是一维译码和二维译码。一维译码又称为单译码法，二维译码又称为双译码法。像这节课前面我们介绍的那个存储芯片，这种策略就是典型的二维译码。对于一个大小为4k×8比特的这个呃存储矩阵来说。我们需要用12比特作为地址信息。其中，更高的六个比特，我们作为行地址。更低的六个比特，作为列地址。由于我们把完整的地址信息拆分成了xyj 2维的信息，

所以就把它称为二维译码法。那相应的，这个存储矩阵里边就包含呃64行64列，所以这个行地址，译码器以及列地址译码器的输出端。也会对应呃，总共64条，64条，这个输出的线路。再来看这个一维一码法，顾名思义，12比特的这个地址信息，我们只把它看作一个维度。在这种情况下，相当于只有行地址，

没有列地址，那么我们这个行地址译码器需要把12比特的输入信号。转换成对应的四零九六根输出线路。根据这12比特的输入信息去决定到底让哪一条输出线信号有效。那由于这12比特的地址信息，它最终是指向了八个比特。所以在这种一维一码的条件下，我们这个存储矩阵就会变成细长细长的。总共四零九六行一列。每一个地址对应八比特的信息。那可以看到左边这种呃一维译码法，它的缺点就是译码器的输出线路会非常非常多。你看右边这种二维译码呃，每一个译码器只需要输出64条线路，对吧？

这种一维译码的话，它需要输出四零九六条线路。我们这个例子还只是4k×8比特这样的存储芯片，如果这个存储芯片的容量更大。那么，这儿的输出线路还会变得更多，更可怕。那也正因如此，像左边这种单译码法，它可以用于小容量的SRAM芯片。而右边这种双译码法常用于大容量的DRAM芯片，当然了，有的SRAM芯片。如果容量比较大的话，也常常会采用这种双翼麻法。

由于四零八考研对于DRAM芯片的这个呃要求会更深入一些，所以我们重点关注双译码法。好的，那么本节课我们介绍了存储芯片的组成。其中，最核心的部分当然就是存储矩阵。如果某个存储芯片，它的容量是2n×k个比特。那么，这儿的k指的就是存储字长二的N次方，表示的是地址的数量，那么我们可以把二的N次方个存储源排列成r行c列。这样就可以构成一个位平面，然后我们再把k个位平面堆叠起来。这样的话，

一个确定的行号列号就会对应k个比特的信息。那这就是存储自长的本质。理解了存储矩阵的底层原理，我们就能够理解为什么在DRAM芯片当中，我们总是要把完整的恩比特地址。呃，拆分成行地址和列地址两个部分。好，那么这节课的最后我们也补充了两种地址译码法，分别是双译码法和单译码法。大容量DRAM通常使用双译码法，这样可以避免单译码法的这个译码器输出线路过多的问题。那关于双译码法的这个行地址的位数，列地址的位数和这个呃位平面的。行号列号之间有什么关系？

大家可以再结合思维导图进行回顾和梳理。那在本节课的电路图中，我们也介绍了读写控制信号we和片选信号CS。这两个信号在以后的电路图当中，甚至考试的电路图当中都有可能看到。那细心的同学会发现we这个呃信号的上面画了一条横线以及CS这个信号的，上面也画了一条横线。信号名称上面画了一条横线的意思是说这个信号是低电平有效，也就是比特零的时候有效。如果结合它这个英文缩写，我们可以理解为当这个信号值为零的时候。就right enable也就是可以进行写操作，对吧？所以为什么we上面画了一条横线，这个信号我们。

血对应的是零，而相应的毒对应的是一，就是这个原因。那如果we这个信号上面没有画横线的话，就说明这个信号是高电平有效。也就是当这个信号为比特一的时候right enable就这个意思好，所以一个信号名称的缩写上面有横线还是没有横线？其实隐含了这个信号，它到底高电平有效还是低电平有效，就隐含了这样的信息。好，那么注意到这些细节，有助于大家看懂后面的啊，更多的电路图好的，那么以上就是本节课的所有内容。
