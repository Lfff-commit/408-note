各位同学大家好，在这节课我们要探讨主存储器与CPU的连接，我们要掌握位扩展法，自扩展法。和自卫同时扩展法这三种连接方式。其中，未扩展法可以增加存储器的存储字长。所谓存储字长，就是一个地址对应多少比特。好第二种自扩展法可以增加呃存储器的这个地址空间。那么，自位同时扩展法，它既可以增加存储器的字长，同时也会增加地址空间。好，

首先我们从宏观的这个视角来了解CPU和主存之间的连接关系，可以看到CPU的内部有两个寄存器，一个叫mar，一个叫mdr。mar寄存器里边指明了本次要对主存的哪个地址进行读或者写操作。而mdr里边呃保存的是此次要写入主存的啊数据。或者从主存读出的数据。那地址信息是用地址总线传输给主存的。那从主存读出，或者写入主存的这些信息，又是通过数据总线啊去传输的。除了地址和数据之外，还需要用控制总线去传输一些控制信息，比如说读或者写的这些控制信号。好所以主存和CPU之间通过数据总线，

地址总线和控制总线来连接。好，那么在我们的个人电脑上一个主存完整的构成分为ROM区和RAM区。ROM区大家可能不太熟悉，通常来说会在你的电脑主板上集成一块啊，小的ROM芯片。那ROM芯片它有一个呃很重要的特性，就是非易失性，对吧？就是断电之后ROM芯片里边存储的那些信息不丢失。而我们知道，当你的电脑开机的时候，这个CPU肯定需要固定的去执行一些指令，一些开机相关的指令，对吧？

那这些开机固定要运行的啊，这些指令程序就会被放在ROM区当中。好，这是ROM区那相比之下RAM区，它就是一时性的，你的电脑断电关机之后RAM里边存储的东西就消失了。我们在改装电脑的时候，购买的内存条。它对应的就是RAM区。那通过之前的学习，我们已经知道内存条，它是由DRAM芯片来实现的。在网上搜了这样的两个内存条。左边这个内存条8 GB，右边这个16 GB。

如果把这个内存条白色的这个贴纸撕掉，你会看到下面有黑色的一二。三四四块DRAM芯片。每一块drem芯片是两GB的大小。那四块组合在一起就变成了8 GB的内存条。再看右边这种黑黑的DRAM芯片，总共有一二三四五六。七八总共有八块，同样的每个DRAM芯片两GB。那八块组合连接在一起就变成了16 GB的更大的一个内存条。好，那么这些DRAM芯片，它们是如何连接在一起的呢？以及它们和CPU之间的这种连线的关系？又是怎么样的？

这就是本节课，我们要探讨的重点好。首先我们来看位扩展法，这种方法可以增加存储器的存储字长。看这张图，左边是CPU，右边这个是主存。这个主存储器由一二三四五六七八八块DRAM芯片。构成每一块DRAM芯片的这个呃型号是8k×1比特。也就是说，单看一块呃，这个存储芯片的话，每一个地址它只对应一比特的信息，对吧？但是现在我们希望每一个地址对应巴比特的信息。

那相对应的CPU和主存之间的这个数据，总线的宽度也是八个比特，每次可以并行的传输八倍的信息。这八根数据总线，我们分别把它编号为d0到d7。好观察这八根数据总线和这八块芯片的连接关系。可以看到，最上面第零这根总线，它连接的是啊，最右边这块芯片。好，然后第一这根数据总线。它连接的是。七这块芯片。好，

后面以此类推。最后第七，这根数据总线。它连接的是第一块芯片，也就是说每一块d软芯片提供一个比特，那么组合起来就可以让存储器的这个存储字长。增加到八比特。接下来再观察上面这个位置地址，总线这儿总共是a0到a12，这13比特的地址信息。我们都会输入到这八块芯片的这个呃地址引脚处，每一块芯片的大小是8k嘛，二的13次方等于8k。所以每一块芯片的地址信息都要用13比特来指明。好，

所以当CPU要读或者要写某一个地址的时候，13比特的地址信息，通过地址总线啊传输过去。并且并行的输入到八块芯片当中。那每一块芯片会被选中一比特，那么总共八个比特就可以通过八根数据总线。传输给CPU就是这样的一个原理。好，那类似的道理，假如CPU和主存之间的这个数据总线总共是64位宽，每一次可以并行的传输64比特。以及在这个例子中，我们假设每一块芯片是8k×8位，那每一块DRAM芯片的八个比特。都分别的接上数据总线的呃八根线路，

那这样的话，八块芯片每一块。接八根数据总线，那总共就可以组成64位的这个啊，存储字长。好，那么这是胃扩展法。好，接下来再看自扩展法这种连接方法，可以扩大存储器的地址空间。观察这个电路图，上面四个方框表示的是四块啊RAM芯片。每一块芯片的规格是8k×8位。那8k的容量应该对应13比特的地址。八位的这个数据宽度，

意味着这块芯片每一次可以并行的读或者写八比特的信息。好，再看cpucpu的这个数据，总线的宽度是八位，那么我们可以把数据总线的这八位。从CPU分别连接到。四块芯片。和每一块芯片都会有相连。那具体访问的是哪一块芯片呢？接下来我们再看这个地方。我们最终要构建的是一个32k×8位的存储器。32 k=2的15次方，所以地址的总位数应该是15个比特对吧？那么更低的13位地址。我们把它接到各块芯片的呃a0到a12这几个位置。

另一方面，更高的两位地址。作为二次译码器的输入。二的二次方等于四，所以二四译码器的意思就是说有两个比特的输入。根据这两个比特是零还是一决定，四条输出线路到底哪一条的信号有效？例如，当更高的两位地址为零零的时候。这个译码器会让最下面的这条线路信号有效。此时最左边这块芯片被选中，可以看到CS这个是片选信号，对吧？所以只有这个信号有效的时候，这块芯片才会被激活，

才可以被读或者写。那么，根据更低的13位地址信息，就可以决定具体要读写这块芯片的哪个位置，对吧？那每一个地址对应的是八个比特的信息，这八个比特你看它是直接和CPU的数据总线相连就可以传到CPU这边。好类似的，如果更高的两位地址为一一的时候，那么译码器的最上面这根线。会输出有效信号。此时最右边这块芯片会被选中激活，而其他的三块芯片就不可以读写。此时，根据更低的13比特去决定要读写的是哪一个存储单元。

紧接着，八比特的信息就可以通过这个数据总线和CPU之间进行传输。好，可以看到我们用这种自扩展法把四片8k×8位的RAM芯片组成了一个32k×8位的存储芯片。我们扩大了存储器的地址空间。存储的字长是八位保持不变。那四块芯片的地址范围分别是这样的。每一块芯片的更高，两个比特的这个地址信息都不一样。好，这个地方大家会看到CPU，还有一个呃地址位a15这个比特我们没有用到。这块CPU最多支持16比特的地址。那它的寻址能力就是64k，对吧？

只不过在本例当中，我们和CPU连接的是一个32k的存储器。还没有到CPU的这个上限，所以最高的这一位啊，地址a15，我们暂时没有启用。那在后面这个例子中，我们就是用呃四个16k×8位的芯片组成了64k×8位的存储器。此时你会看到CPU的16比特地址啊，都会被启用。每一块芯片的容量变大了，所以每一块芯片的片内地址来到了14比特。然后更高的两个比特。会通过二四译码器的这个解读，把它转换成。

四块芯片的片选信号。根据a14a15这两个比特为零零零一一零还是一一去决定到底要选中一二三四哪一块芯片。四块芯片的地址范围如图所示。低14位都是从全零到全一更高的两位。各不相同，分别是零零零，一一零和一一。好，这是自扩展法。最后，我们介绍自位同时扩展法，其实就是前面两种方法的一个结合。在下面这个例子中。我们用八片16k×4位的RAM芯片去构成了呃64k×8位的存储器。可以看到，

存储字长扩展了，以及这个地址空间也扩展了。好，假设这个CPU它的地址总线宽度是八个比特，而我们的这个RAM芯片。每一块的存储字长只有四位。那这样按照未扩展的思路，我们可以以两块RAM芯片为一组。把存储字长扩展到八位。相当于这两块芯片各自分别提供四位。那么，这八个比特通过这个呃数据总线。传给CPU就这个意思。好，所以两块芯片为一组，

先完成了位扩展，把存储字长扩展到八位。接下来，我们还需要进行自扩展，要把存储器的这个容量从16k提升到64k。那么很简单，我们只需要把四组芯片用自扩展的这种方式，再把它们连接起来。就可以构造出64k×8倍的这个存储器。对于CPU来说，更低的14位地址作为片内地址。观察连线这14位地址信息会被呃输入到每一组芯片的呃第一块，以及你看后面这还有一条线对吧？也会输入到第二块芯片。反正每一块芯片都会完整的接收这低14位的地址信息。

好与此同时，更高的这两个比特接入二四译码器，如果这两个比特为零零。那么就可以让译码器的最下面这条线。选通于是一二这两块芯片，它的片选信号就会有效。就可以从这组芯片这呃读或者写四+4比特的数据。同理，如果最高两位的呃地址为一一，那么最上面。这条线会输出有效信号。你会看到这条线接入了第四组芯片的呃，这个片选信号这。于是，第四组芯片这两块会被激活。

我们就可以对这两块芯片的四+4比特进行读，或者写的操作。好，那么这四组芯片的地址范围啊，分别就是这个样子。好，这节课的最后我们看两道真题。一零年的15题。说有若干个2k×4位的芯片组成8k×8位的存储器。问，我们这样的一个地址，它所在的芯片的最小地址是多少？好，大家暂停动手，十秒钟后公布答案。

三二。一这道题选择d选项。好来看我们手里的芯片是这样的型号，最终要组成8k×8位的存储器。那首先观察。胃需不需要扩展？需要需要从四位扩展到八位，因此我们需要以两块芯片为一组。这样就可以实现胃扩张，对吧？好再来看字，需不需要扩展？要从2k扩展到8k，要翻四倍，所以我们需要有四组这样的芯片。

来实现自扩展。那么，最终这个8k的存储器，它的总地址位数是13个比特。我们总共有四组芯片，那么相应的，我们就需要用两个比特来选中其中的某一个分组，对吧？那每组芯片的内部包含2k个地址。也就是11比特的片内地址以及更高两位作为片选信号，所以第一组芯片的地址范围就应该是零零，然后后面的11个比特的话就是从全零到全一。第二组芯片是零一，第三组一零，第四组一一好题目，

最终给了我们一个具体的地址。问，我们包含这个地址的芯片组，它的最小地址是多少？那我们把它更高的这呃两位给拆出来零一。显然，这个地址，它对应的是第二组芯片，对吧？那第二组芯片的地址范围是高两位零一确定了。更低的11比特是从全零到全一。那其中包含的最小的地址当然就是更低，11比特全部取零对吧？好，最终转换成16进制就是零八零零h，

因此题目选择d选项。好，这是一零年的15题，最后再来看一六年的16题。存储器的总容量64 KB，按字节编制，按字节编制是什么意思啊？就说一个地址对应的是八个比特，对吧？相当于这个存储器呃，它的这个规格按照前面那种方式来表述的话，应该是64k。乘以八位就是这样的一个规格。好，接下来说这个地址范围是ROM区，

其余的是RAM区。并且这个RAM区它是用8k×4维的SRAM芯片去设计的。问，我们需要多少块这种srm芯片？好，暂停练习，十秒钟后公布答案。三二一。这个题目选择c选项。好总容量64k，所以我们需要用16比特来表示这个地址的信息。题目给了ROM区的地址范围，那我们把这个地址范围展开。可以看到ROM区总计包含了8k个地址。那除了这8k之外，

剩余的64 k- 8k，也就是56k的这个地址区域都是要用啊，题目给的这种SRAM芯片去实现的。要用8k×4位的SRAM实现56k×8位的这个RAM区。那首先看需不需要胃扩展。显然需要对吧？那四位扩展到八位，我们可以以两片为一组。这样就达成了未扩展的目的好，接下来总共需要几组呢？我们总共要组成56k个，这个存储单元对吧？那么每一组的SRAM芯片它只包含。8k个存储单元56÷8，相当于我们要七组芯片，

那七组芯片就可以实现自扩展。那最终总计需要14个啊，这种规格的s软芯片就可以，所以题目选择c选项。这道题目有能力的同学可以自己尝试画一下这个呃芯片的连接电路图。不过考试不会考这么难哈啊，有兴趣的同学自己尝试给大家点一下简单的思路。我们可以用一个三八译码器，更高的三个比特作为译码器的呃输入，然后这一端输出八个片选信号。我们总共有七组srom芯片以及一组ROM芯片，这个ROM芯片的大小也是8k，对吧？那根据这个ROM芯片的地址范围，我们可以看到ROM芯片，

它应该对应的是零一零这个状态。也就是说，如果更高的三个比特是零一零的话。那么，这个三八译码器当中对应的那根线就应该输出有效信号。让它选通ROM芯片。零一零对应的是二。如果我们把三八译码器的八根输出线编号零一二三四五。六七给出这样的编号的话，那么零一零对应的应该就是二这个编号的这条线被选通对吧？那这根线我们应该接到ROM芯片那。那剩下的零一三四五六七就应该用自位同时扩展法去分别接到七组SRAM芯片那儿。好有能力的同学挑战一下。这个部分的考察主要还是以这种计算型的选择题为主。截至目前，

还没有出现过呃，让大家画电路这种变态的考法。好，那么大家可以再结合课件课本，再来回顾一下，那以上就是本节课的所有内容。
