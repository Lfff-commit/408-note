各位同学大家好，在这个小节中我们要学习的是cache的替换算法，在之前的小节中我们留下了这样的三个问题，上一个小节我们解决了第一个问题。而第二个问题就是说啊，我们的cache是很小的，主存是很大的，但是我没有说过每一次被访问的主存块一定会被立即调入cache当中。那这就意味着cache很容易被装满，那么当cache装满之后，我们应该怎么办呢？这就是替换算法要解决的问题，我们结合上一小节学习的这些地址映射方式来进行考虑。首先看第一种全相连映射，这种映射方式意味着每一个主存块有可能会被放到cache的任何一个位置。

所以如果采用这种映射方式，那么就意味着只有整个cache全部被装满之后，我们才需要在呃整个cache当中选择一个cache块把它进行替换。再来看直接映射方式。对于任何一个主存块，我们只能把它放到一个指定的，特定的位置，那么这就意味着如果这个位置原来已经有数据了，那么我们直接把原来的这份数据给。替换掉，淘汰掉就可以了，所以如果采用直接映射方式，那其实我们不需要考虑替换算法，到底要替换哪一块这样的一个问题？我们毫无选择，

只能放到固定的这个位置好，再来看第三种组相连映射，那么呃，每一个主存块会被放到指定分组当中的任何一个位置。所以如果采用这种方式，就意味着只有这个内存块，它所属的cache分组整个组都被装满之后，我们才需要选择到底要替换哪一块。好，所以替换算法只会被用到全相连映射，还有组相连映射这两种方式，直接映射的时候不需要考虑替换算法。那这一小节的讲解中，我们会以全相连映射为例，那我们会学习四种替换算法，

第一种是随机算法，第二种先进先出，第三种近期最少使用，第四种叫最近不经常使用。那同学们也需要注意各种算法的英文缩写。好，那首先来看第一种随机算法啊，顾名思义就是说如果cache满了的话，那我们随机选择一块进行替换，非常的free。好来看这样一个例子，假设呃有一个系统当中，它的cache块总共有四个，初始的时候所有的cache块都是空的，然后采用全相邻映射方式。

接下来，我们访问的主存块啊编号分别是这样的一个序列，也就是说先访问一号主存块，再访问二号，三号，四号，然后再访问一号，二号好，以此类推。好来看一下，总共四个cache块，并且刚开始所有的块都是空的，所以我们刚开始访问一二三四这几个主存块的时候，我们都需要把这些主存块把它依次调入到cache当中。一号主存块，

我们把它放到零号cache这个位置，二号主存块，我们把它放到一号cache这个位置好，接下来三号和四号主存块也是类似，把它放到cache的相应位置。那刚开始访问的这几个主存块，我们都是没有命中的，每访问一个主存块就需要把这一块的数据从主存调入到cache。那由于前边的这四次访问啊cache一直都没被装满，所以我们不需要进行cache替换算法。好，继续往后。接下来我们要访问的是一号主存块，那么由于此时一号主存块已经存放在cache当中，所以这次的访问是可以命中的。

接下来二号也是一样，也可以命中在接下来五号，那由于此时四个cache块当中存放的主存块分别是一二三四号，那么五号主存块此时没有被调入cache。而我们之前说过，每访问到一个主存块，就一定需要把这个主存块立即调入cache好，所以此时我们就需要根据这个呃cache块的替换算法来决定要替换哪一块。那根据随机算法的规则，我们可以随便选择一个块进行替换，比如说我们把三号主存块给换出去，然后换入五号主存块。没有任何规律，我们就是随便选的一个位置好，接下来一号和二号的访问都可以命中，

再接下来访问到三号块，那此时三号主存块没有放到cache当中，所以同样的我们随机选择一个块进行。替换，那我们把四号主存块，把它换出去，然后把三号主存块换到它原先存放的这个位置，也就是三号开始行这个地方。好，接下来又需要访问四号主存块，那么我们随机挑一个把一号主存块给换出去，把四号换进来，接下来访问五号主存块，那五号此时是在cache当中的，所以可以直接命中。

不需要发生替换好，所以这就是随机算法，毫无规律啊，我们并没有考虑到什么程序的局部性之类的啊，这些因素。所以这种算法，它实际的运行效果会很不稳定。cache的命中率可能会很低。接下来看第二个先进先出算法，那顾名思义就是我们会优先把最先被调入cache的块，给它替换出去。还是用刚才那个例子，那刚开始访问一二三四这几个主存块的时候，由于这个cache初始是空的，所以我们只需要把这些主存块分别调入到cache的相应位置就可以。

好，接下来访问一和二，那这两个显然是可以命中的，再接下来访问五号主存块，那么五号主存块此时并没有在cache当中。那根据先进先出的规则，此时我们放在cache里的这些主存块最先被调入的应该是一号主存块，对吧？所以我们会优先淘汰一号主存块，然后把五号主存块放到cache 0这个位置。好，接下来我们又要访问一号，那一号刚才被我们淘汰了，所以现在又需要把一号调入cache。那此时我们放在cache里的这几个块当中，

最先被调入的应该是二号cache块。所以我们会淘汰二号放入一号，那淘汰二号之后，紧接着又会访问二号那类似的原理，在剩下的这几个主存块当中，最先被调入的应该是三号块。所以淘汰三号放入二号，接下来访问三号，那么应该淘汰四号调入三号，再接下来访问四号储存块，那么剩下的这些块当中最先被调入的应该是五号，对吧？好，所以淘汰五放入四，接下来又访问五，

那么此时剩余的这一块最先被调入的应该是一号，所以换出一调入五。好，那这就是先进先出算法，很好理解，并且用硬件实现也很简单，可以这么做，刚开始。开始的所有行都是空的，那么我们可以按照行号递增的次序零一二三呃，这样的次序来依次放入最初始的几个内存块。然后接下来我们就只需要按照零一二三零一二三这样的次序来替换各个块就可以了，因为按照刚才这种策略，我们最先放入的就是呃零这个位置。所以我们最先应该淘汰的也是零这个位置，

那零这个位置被替换之后，接下来最先调入的应该就是处于一这个位置的这一块，对吧？所以接下来还需要替换的话，我们替换一。那一替换之后再往后想要替换就替换二就可以了，好，所以先进先出算法，用硬件实现起来也会很简单，只需要轮流的替换，各个开始行就可以。但是显然，先进先出也没有考虑到局部性原理，因此cache的命中率也会很低，最先被调入cache的块也有可能在之后会被频繁的访问到。

这个应该不难理解，比如大家写的C语言程序，很有可能在刚开始的时候就调用到了print f这个函数，那这个函数你是最开始就使用到的，那这并不意味着再往后这个函数你就用不到了，对吧？好，所以我们把最先被调入cache的啊，这个块给淘汰这种处理策略是不科学的，因此先进先出这种替换算法实际的运行效果也不太理想。另外，刚才我们分析到后面呃，这个部分的时候，大家会发现我们出现了这样的一个有意思的现象。就是在这个地方，

我们换出了一换入了五一，刚被换出，紧接着又访问了一，所以我们不得不换出二，然后再换入一。而二刚被换出，接下来又访问到了二，所以就出现了这种频繁的换入，换出的现象，那这种现象被称为抖动现象。就是说刚被换出的快，很快又被访问，所以紧接着又会被调入。那这些概念我们在操作系统里边其实也讲过好，接下来看第三个啊，

算法叫近期最少使用。英文缩写叫lru嗯，这个算法的思想是这样的，我们会为每一个cache块cache行设置一个计数器。用于记录这个cache块里边存放的主存块已经多久没有被访问过了，然后当我们需要替换的时候，我们会选择计数器最大的一个cache块，把它替换出去。计数器最大是不是就意味着这个块是最久没有被访问过的？好，还是用刚才那个例子来体会一下啊，这首先跟大家介绍一个，就是手算做题的方法，我们先不考虑机器是如何执行的。总共有四个cache块，

刚开始都是呃空的，那第一个被访问的主存块块号是一，我们可以把它放到零这个位置，第二个是二，那我们可以把二号主存块放到。一这个位置，然后三四也是类似的好，那访问了前四个主存块之后呃cache里的情况是这个样子，一二三四存。好，接下来一和二他们已经在cache当中，所以可以命中再接下来要访问五，那么此时就必须把其中的某一个主存块给替换出去。那根据这个算法的规则，我们要替换的是最久没有被访问过的一个主存块，

对吧？那我们可以从五这个位置。从这个位置开始往前看，二最近被访问过，一最近被访问过，四最近被访问过，所以此时cache里边这几个主存块。最久没有被访问过的，应该是三号块，所以在这个时候我们可以淘汰三掉入五，然后其他的几个保持不变。好，再接下来一和二也可以命中，然后这个地方又要访问到三号主存块，三号没有在cache当中，

那和之前的处理方法类似，我们从这个位置。往前看。二一五是最近被访问过的，所以最久没有被访问过的，应该是四号快，所以我们淘汰四号换入三号，然后其他的几个保持不变。好，所以这就是我们做题的时候可以采取的一个方法。当需要替换一个cache块的时候，从当前访问的这个主存块号，这个位置开始往前看看一下哪些主存块是最近被访问过的，那从后往前看，最后一个出现的。

呃，这个主存款号就应该是需要替换的主存款。好，那后续的两个就不再展开，这是我们做题的时候可以采取的一个比较快的方法呃，接下来我们再来看一下，用硬件站在机器的角度，它是如何实现近期最少使用算法的？那上一小节中，我们说过每一个cash行呃，需要有一个标记，就是用来记录这个cash行里边所存储的主存块号。另外，还需要增加一个有效位，那除了这些信息之外，

如果要使用lru这种算法，那么我们还必须给每一个cache行。添加这样的一个属性就是计数器。刚开始所有的cache行都是空的，计数器也会被全部置为零好，那计数器的变化规则是这样的。首先看二这个地方，如果说我们访问某一个主存块号在cache里边没有命中。那此时如果还有空闲的cache行，我们就会把这个主存块装入到一个呃空闲的行里边。同时，把这个cache行所对应的计数器置为零，然后其余的非空闲的cache行。计数器会全部加一。好来看一下什么意思？

刚开始我们访问的是一号主存块，此时没有命中，并且还依然有空闲的cache行，所以我们会把一号主存块放到cache 0这个位置。然后把这个cache行所对应的计数器设置为零。其余的非空闲行才需要把计数器全部加一，那在这个时刻，其他这些cash行都是空闲的，所以他们的计数器我们不需要管，不需要加一。好，接下来访问二号储存块，那么我们可以把它调入到cache 1这个位置，并且把它的计数器设为零，同时把其他的非空闲cache行所对应的计数器加一。

所以此时cache 0的计数器变为了一。好，注意理解这个计数器的逻辑，他想要反映的就是这个cache块已经多久没有被访问了。好，继续。接下来是三号主存块，那么我们把它放到呃cache 2这个位置，计数器设为零，同时其他的这些非空闲cache行计数器加一。在接下来访问四号也是类似的。其他的这些计数器都会加一。好，继续接下来要访问的是一号主存块，那显然这个主存块此时已经在开始，

当中是可以命中的，那当我们命中之后。我们会把所命中的这个cache行，它的计数器清零。同时，计数器的值比它更低的，其他的那些计数器我们会把它加一。其余不变，总之就是我们此时访问了啊，零号cache块，那么它的计数器应该从三变为零，然后计数器的值比三更小的这些啊，这些cache行。它的计数器的值都要加一二变三一变二零变一。好，

所以这种方式可以保证我们最近访问过的这个呃主存块，它的计数器的值肯定是最小的，可以恢复成零。然后越久没有被访问过的cache行，它的计数器的值会不断的加一。就会变得越来越大。好，我们继续接下来要访问的是二号储存块，那显然也是可以命中的，那么和之前的处理方式一样，我们会把当前访问的这个cache行，把他计数器的值清零。另外，其他计数器的值都加一。好，

继续接下来要访问的是五号储存块，那此时cache没有命中，我们必须选择替换某一个cache块。那按照这个算法的规则，我们会替换掉计数器的值，最大的一个啊cache块也就是二号，它的计数器是三。那这个cache块当中存放的是三号储存块。最后，我们把三号主存块替换掉，五号主存块放到这个位置。同时，把计数器的值清零，然后其余的计数器的值全部加一。好，

继续接下来要访问的是一，那么此时一是可以命中的。因此，我们会把一号储存块所存放的这个cache行，它的计数器给清零，另外比这个计数器的值更小的那些计数器的值，我们需要加一。也就说，我们只需要把呃比二更小的一和零这两个计数器的值加一，而比二更大的三这个计数器的值，我们不需要加一。思考一下，我们把二变为了零，然后一和零分别加一，我们当然也可以把这个三同样的也进行一个加一变成四。

但是把三变为四，其实毫无意义，让它的值保留原本的三也可以达到同样的效果，因为我们设置计数器的目的其实是想通过计数器的值的大小。来判断我们应该替换哪一个主存块，我们想要选择的是计数器的值最大的一个c始块，会把它给替换掉。那三这个值本来就已经是最大的，所以我们再让三继续加一，其实是没有意义的。那这种处理策略的好处就是，当我们只有四个cash行的时候，我们的四个计数器的值肯定是零一二三，只有可能出现这几种情况。不可能出现四这种情况好，

所以注意体会这个细节好，我们继续接下来访问二号主存块那么呃，二号也可以命中，我们会把二号块所对应的这个计数器。把它变为零，同时把计数器更小的啊，这些值让它加一，然后计数器更大的，这个三让它保持不变。好，继续接下来要访问的是三，那么此时cache没有命中，并且已经没有空闲的cache行了，所以我们会选择计数器的值最大的。这个呃cash快进行淘汰。

把四号主存块淘汰出去，然后把三号主存块换进来，另外还需要修改计数器的值为零，然后其余的这些计数器都需要加一。好再往后访问四没有命中，那么我们同样需要把计数器最大的这个给它替换掉，就变成这个样子。好，接下来访问五也是类似的，没有命中，那么我们把计数器最大的这个给它替换掉。其余的这些计数器都需要加一好，那这就是lru算法，我们模拟机器的执行比我们手算要麻烦一些。但是，

机器对计数器进行这样的处理是有好处的。刚才我们说过，采用这样的策略可以保证所有的这些计数器的值，只有可能出现零一二三。那么，这也就意味着，当我们的c的总数只有二的N次方，这么多块的时候，我们就只需要n个比特位来作为计数器就可以。比如这个例子当中，我们只有四个开始块，那么我们就只需要两比特的信息来表示计数器就可以，两比特刚好可以表示零一二三这几种数字。好，所以这样的策略就保证了，

当我们用硬件实现这个替换算法的时候，硬件电路的设计可以变得更简单，我们只需要增加两个比特的冗余信息。就可以。好，那很显然，这个算法它其实是考虑到了局部性原理的，因为基于时间，局部性可以知道我们近期访问过的主存块在不久的将来也很有可能会被再次访问。所以这种算法的思想就是每一次会淘汰最久没有访问过的主存块，这种策略是合理的。遵循的局部性原理，所以这个算法在实际当中运行效果会很优秀c的命中率很高。好，那这是lru算法，

那虽然这个算法很优秀，但是如果说我们频繁访问到的主存块的数量要比cache行的数量。要更大，那么我们也有可能会发生抖动现象，比如说我们按一二三四五一二三四五来，这样的顺序来访问各个主存块。那此时被我们频繁访问到的主存块总共有五块，而cache行的数量只有四，那大家可以自己模拟一下，如果是这样的访问序列。那即便是采用lru算法，依然会发生抖动现象，那这儿就不再展开。接下来，我们看这个小节的最后一种算法，

最近不经常使用算法。呃，叫lfu这个f指的是频率frequently和lru算法类似，我们也需要给每个cache块cache行设置一个计数器，只不过它这儿的计数器是用于记录。每一个cache块呃到目前为止被访问过了多少次，然后当我们需要替换某一个块的时候，我们会选择计数器，最小的也就是被访问次数最少的那一个进行替换。好，还是用刚才的例子来看一下啊，刚开始我们需要把计数器都设为零。然后计数器的变化规则是这样的，当我们调入一个新的块的时候，这个块所对应的计数器会置为零，

之后这个块每被访问一次计数器的值就会加一。好，所以刚开始我们访问一二三四，只需要把这几个主存款分别的调入到cache的相应位置。那由于第一次被调入的时候，计数器的值都是置为零，所以这个计数器此时是四个零，接下来要访问一那么一号储存块命中，我们就可以让他计数器的值加一再接下来访问二。二号命中，那么与它所对应的这个cass行计数器也会加一。好，接下来访问五此时就需要替换一个块。那么，按照这个算法的规则，

我们会替换掉计数器最小的一行。不过，现在大家会发现，我们二和三这两行的计数器的值都是最小的，都是零。那到底替换哪一块呢？通常来说可以有这样的两种处理策略，第一种策略就是按照行号cache行号递增的次序来判断他们的优先级。比如说我可以选择优先淘汰这个cash行号，更小的这个这个块，这是第一种策略，第二种策略也可以按照先进先出的规则来给他们进行排序。对于这个例子来说，二和三这两个cache行，它里边存入的这个主存块应该是二号cache行的，

这一块最先被调入的。所以，按照先进先出的规则，我们会优先淘汰二号cache块里边存放的这个内容。好，所以我们把三号主存块给淘汰掉，相应的位置换入五号好，那再接下来一和二又再次会被访问，这两次访问都可以命中，所以我们需要把一和二所对应的这两个计数器都加一。好再往后访。问到三，此时又需要运行一次替换算法。那同样的第二行和第三行，他们的计数器都是最小的，

那我们可以选择按照行号递增的次序来给他们进行一个优先级的排序，我们会选择淘汰。行号更小的这个cache块，所以我们把五换出，然后三换入好，接下来四号可以命中，那么与四号相对应的，这个cache行它的计数器加一。最后，要访问五号，那这个时候计数器最小的cas行只有一个，所以毫无疑问我们需要淘汰的是三这个主存块换入五号。好，那这就是lfu算法，显然采用这种算法的话，

那这个计数器的值有可能是零到一个很大的数。所以如果要用硬件实现，那么每个cas行所对应的计数器就需要用比较长的几个二进制比特位来表示。好，另一个方面lfu这种算法看起来好像很科学，这个计数器相当于我们对每一个cache块呃被访问的次数进行了一个总体的统计，对吧？看起来好像很科学，就是总体来看啊，被访问次数越多的，我们就越有可能把它留住。但事实上，考虑到时间局部性原理，我们不难想到就是曾经经常会被访问的主存款。在未来，

其实不一定会被用到。比如说大家使用微信视频聊天的时候，那和视频聊天相关的那些啊，快的数据显然在一段时间内经常会被访问。那么，这就意味着与这些块相对应的计数器在你使用视频聊天的这段时间内会一直增加，加加到一个很大很大的值。好，那之后你不再需要视频聊天了，但是由于这些块的计数器已经变得很大了，所以接下来这些块在cache当中的数据副本很有可能在长时间内不会被淘汰。因此，这种算法实际运行效果其实也不好，它并没有遵循局部性原理。所谓局部性，

就是我们其实只需要考虑到最近的一小个局部，而不需要像这个算法这样，有可能会记录下。整个全局的一个呃访问的频率。好，所以这个算法看起来科学，但实际上它的cache命中率可能也不高。好的，那么这一小节当中我们学习了四种cache替换算法，因为cache的存储容量很小，而主存的存储容量很大。所以我们不得不考虑到，当cache存满了之后，我们需要替换哪些数据这样的一个问题。那随机算法和先进先出很好理解。

稍微复杂一点的是，近期最少使用这个算法，但是这个算法的实际运行效果就是cache的命中率。在这几个算法当中，又是最优秀的。所以我们考试最经常考察的也还是这个算法，不过我们手工做题的时候呃，大多数情况下我们也不需要写出计数器的值怎么变化？大家可以用我们刚才提到的更快的那种方法来快速的做题。只不过大家也需要理解这种算法计数器，它的一个工作原理。好的，那以上就是这个小节的全部内容。
