各位同学大家好，本节课我们要探讨主存储器的基本组成和基本操作。首先，我们会结合一个硬件框图，介绍主存储器的基本结构。紧接着，我们会介绍两种访问主存的基本操作，就是读主存或者写主存。好先来看主存的基本结构，这张图乍一看比较复杂啊，我们逐步来拆解。好，首先观察左边虚线框，这个部分虚线框，这个部分表示的是CPU。

而右边。有一个实线框比较大的实线框，这个部分表示的是主存。可以看到，主存和CPU之间通过三组线路进行连接，分别叫数据线。地址线以及控制线。之后，我们会结合读主存，写主存的例子来感受这三组线路的作用。现在我们先来观察主存储器的内部结构。在这张图示当中，这儿有一个虚线框，圆形的虚线框，这个圆形的虚线框和左边啊，

这个矩形的这个阵列。指的是同一个东西，只不过在右边这个虚线图示当中，更侧重于从地址的这种角度去描述主存的这个呃内部情况。大家在买手机，买电脑的时候肯定会关注它的主存大小，这个参数对吧？有的手机可能是8 GB，有的手机可能是16 GB。当然，好一些的手机可能到64 GB都有可能。那主存的总大小啊，比如说8 GB，它在这个图当中对应的就是中间这个存储矩阵。也叫存储阵列，

或者叫存储体。看这个图就知道这个存储阵列，它就是用于存储零一零一二进制的，对吧？那整个存储阵列可以进一步拆分成很多很多个存储单元。而每个存储单元又会对应一个呃主存的地址，当CPU要往主存写数据，或者要从主存读数据的时候。CPU需要指明它要读或者要写的到底是哪一个数据，对吧？那为了有这种指向性，所以我们就需要给这些存储单元。进行一个地址的编排。第一个存储单元的地址是零，第二个存储单元的地址是一，

第三个存储单元的地址是二，好以此类推。那像这个图示当中，每一个存储单元的大小刚好是一个字节，也就是八比特。由于每个字节对应一个地址，所以我们把这种编制方式称为按字节编制，这也是现代计算机普遍采用的一种方式。一个字节对应一个地址。好，接下来看左边黄色高亮的这个部分，可以看到这样的一行存储单元内部，它又可以进一步的拆分为记忆单元。或者也可以叫存储源或者位元。每一个存储元，

对应的就是一个二进制零或者一。在工程实现层面，可以用很多种物理器件去呃实现这个存储源的这个功能。但是无论是哪种物理器件。它都需要有两种稳定的状态，物理器件的两种稳定状态就可以分别对应二进制的零或者一。好，现在我们已经知道了主存的内部啊，存储二进制零和一的这些啊，东西分别是什么样的一个称呼？在我们这个例子当中，一个地址对应的是一个字节的数据。在考研当中，如果题目不特别说明的话，我们都可以默认一个地址对应的就是一个字节。

因为现代计算机通常也都是按字节编制的。不过，部分特殊用途的计算机，比如说有一些用于呃科学运算的计算机，它经常进行这个64位的浮点数运算。那像这类计算机，它的这个编制方式可能就是按字编制，比如说可以规定每64位为一个字。那在这种条件下，一个地址对应的就是64比特的数据。好，这是需要补充的一点，在考试当中我们默认还是按字节编制？好，接下来我们把目光转向CPU这边。

CPU的功能是执行指令。有的指令可能会对主存的某个地址单元进行写操作，有的指令可能是读操作。那为了完成读写这两个动作CPU的内部有两个这样的寄存器，分别叫mdr主存数据寄存器以及mar主存地址寄存器。这个中文译名不重要，在考试和题目当中，通常就是直接用mdr或者mar这两个缩写。可以看到，mdr寄存器通过数据线和主存储器的这个存储阵列进行相连。当CPU要往主存当中写数据的时候，它就会把即将要写入主存的数据啊，零一零一这些数据啊，先写到这个mdr寄存器当中。然后通过这n位的数据线，

把这些二进制给写入主存储器的对应的这个存储单元。相应的，如果CPU要从主存中读一个数据，那么主存中存储的这些零一零一二进制数据也是通过数据线。传到mdr当中。因此，大家在这个图示当中可以看到，数据线的这个箭头，它是双向的，既有可能是从mdr指向主存。也有可能从主存指向md 2。双向箭头表示，两个方向的数据流动都有可能发生。好，这是mdr寄存器，

那通常来说mdr寄存器的位数和数据线的位数是相同的。比如说在这个图示当中，数据线我们假设它是64位，那么在这个图示当中的mdr，它的位数也会是64位。相当于mdr呃寄存器里边的这64比特会分别用64根细小的线。64根细小的线去啊，并行的传输到这个主存当中，那每一根细小的线就是可以传一个比特，总共有64根这种细小的线。所以这儿的数据线我们写了64位。那显然mdr以及这个数据线的位数，它决定了呃对主存的一次读或者一次写最大的数据量是多少？好，一会儿我们会结合读写的这个具体操作，

带大家来感受好接下来我们再来看mar这个寄存器。当CPU需要读主存或者写主存的时候。它会把接下来要读或者要写的这个主存地址给放到呃mar寄存器当中。mar寄存器通过地址线和主存的这个地址寄存器进行相连。在这个图示当中，地址线占36位，那么通常来说mar的位数和地址线的位数也是相同的。所以这个图示当中的mar也是36个比特。mar的位数决定了CPU可以支持的最大寻址范围。大家在买电脑或者买手机的时候，如果你愿意多花钱，那么你的这个电脑是不是可以选择更大的主存容量？比如说你的电脑可以选配32 GB的主存，甚至64 GB的主存，对吧？

那你的电脑可以选配的这个主存容量是没有上限的吗？当然不是。这儿的限制就取决于你的电脑CPU的mar寄存器，它到底是多少位？比如说这个地方36比特的mar，意味着这台电脑的CPU，它可以支持的最大的这个主存容量。就是二的36次方个字节。也就是对应64 GB，如果说这个CPU想要选配128 GB的主存，那不好意思，无法支持。因为128 GB已经超出了36比特的mar，可以支持的最大寻址范围了。好，

那么这是mar寄存器，接下来下面这个地方CPU会发出读写控制信号，通过控制线。传给这个主存储器的读写控制电路。比如说发出读命令，或者发出写命令，那么这个控制电路就可以根据CPU发来的信号去完成相应的这个操作逻辑。好，接下来我们基于这个主存的结构框图啊，来描述CPU读一次主存，或者说从主存当中取一次数。它需要做的一些动作，首先第一步CPU会把接下来想要读的那个主存啊地址。写到mar寄存器当中，那假设我们要读的是零零零一这个地址。

好，接下来这个地址的信息会通过地址线送给主存储器的地址寄存器。然后再交给地址译码器，那么这个译码器会根据呃，这个地址的比特信息去选中对应的存储单元。那零零零一对应的就是这一行的存储单元。接下来第二步CPU会发出这个控制信号，比如说通过控制线发出读的控制信号。高速主存，接下来我要进行的是读操作，并且通过这个控制线还可以指明这一次要读的是多少个字节。具体读多少得看CPU当前执行的指令，它的功能是什么好？总之接下来我要进行读操作，以及接下来我要读多少个字节，

这些都是通过控制线。给主存呃发出的。那么，主存的这个读写控制电路会根据控制信号去完成相应的逻辑。在这个例子中，我们要直接读八个字节。所以接下来从零零零一开始的这一个字节，再往后总共八个字节的信息。都会通过数据线，你看送回给CPU。这64位直接写入CPU的mdr寄存器当中。那这样我们就完成了一次呃读的操作。并且这次我们直接读了八个字节。接下来再来看对主存的写操作，也可以称为存操作。

首先第一步还是一样的CPU，它正在执行某一条指令，那这条指令会指明接下来要往主存的什么位置写一个数据。要往哪个位置写呢？这个地址信息需要放到mar寄存器当中，比如我们要往零零零一这个地址去写。同样的，这个地址信息通过地址线传给主存，并且通过地址译码器的处理。选中对应的这个存储单元。好，这是第一步，送地址，第二步CPU需要发出呃控制信号，这次我们是要进行写操作，

所以会发出写的控制信号。并且会通过这个控制线指明本次要写的数据量是多少，最少是一个字节，最多不超过八个字节。刚才我们说了一次读写的数据量上限是由数据线的位数来决定的。好，那么在这个例子当中，假设我们只写一个字节的数据。CPU会把要写入主存的啊，这一个字节数据放到mdr寄存器当中，比如说要写的是四个幺四个零。好，准备好数据以及发出了这个控制信号之后，接下来这一个字节的数据就可以通过数据线。传给主存。

那由于控制线这个地方啊，指明了只写一个字节，所以虽然这个数据线它有64位，但是只有其中的八位是有效的，那这八位对应的就是我们要写入的四个一四个零。好，另一方面，刚才我们说这个地址译码器，它根据CPU发来的地址信号已经选中了，我们要写的这个存储单元。于是四个一四个零就会顺利的写入。原本是这样的数据，现在被改写成了四个一四个零好，那这就是一次写操作。好的，

在本节课我们介绍了主存的基本结构以及读写这两种基本操作，在主存的基本结构这个部分啊，我们重点关注存储单元这个概念。存储单元的大小对应的是编址单位的大小，在现代的计算机当中，通常都是按一个字节去编制的，所以现代计算机一个存储单元通常就是一个字节。当然，有部分特殊的计算机有可能按字编制那一个字的长度到底是64位还是32位，这个是由计算机自身去规定的。那记忆单元存储阵列这些概念稍微有个印象就可以。那通过本节课的学习，我们对啊主存的一次基本的访问操作，需要经过的这个步骤进行了描述。无论是读操作还是写操作，

前两步都是一样的，得把地址信息放到mar寄存器当中。那如果是读主存，那么被选中的那个数据就会通过数据线呃流到CPU的mdr当中，而如果是写主存的操作，那么CPU需要提前把。呃，要写的数据放到mdr寄存器。接下来又通过数据线流向主存。好仿存的一二三三个步骤，大家需要理解。另外，数据线和mdr的宽度以及地址线和mdr的宽度。它们之间的关系也需要了解。数据线的宽度决定了啊，

一次主存的读或者写最多支持多少字节，多少个比特？而地址线的宽度决定了CPU可以支持的最大寻址范围。好的，那么以上就是本节课的所有内容。
