好的，那么从这一小节开始，我们会来探讨第二种IO控制方式，叫做程序终端方式，那这个部分的内容除了大题。就是对于时间的计算，这个方向考察的比较多之外，也经常会在小题里边进行考察。同时，中断相关的考点也会在操作系统里面啊，遇到所以我们也会在操作系统那门课里对中断进行一个简要的学习。那在g组这门课当中，我们更多的是关注中断系统。他工作的一个硬件基础，硬件原理，

那在这个视频中，我们先来探讨中断的基本概念。它在系统当中有什么作用，然后我们需要掌握啊CPU处理中断的这个工作流程，每一步需要做一些什么操作？以及是用什么样的硬件来完成的，然后在下一个视频当中，我们会学习多重中断啊，就是多层嵌套的一个中断，然后再下一个小节，我们才会来正式的介绍。程序中断方式，那在第三个视频当中，我们会更着重于探讨啊，程序中断方式在大体里边的考察形式好，

那接下来我们先来认识一下什么是中断。其实中断这个概念，我们在操作系统的啊，第一章里边前边几个小节就有过介绍好，我们来看一下。这个图左边这一溜，大家可以理解为是啊，主存的地址，那么在这个主存里边是不是会保存我们要执行的一系列指令啊？很多很多指令就是这样顺序的存放的，那经过之前的学习，我们知道我们的CPU执行指令序列的方式。就是会用一个PC程序计数器指向当前要执行的那条指令的一个存放地址。然后取出这条指令之后，会让PC的值自动加一啊，

指向下一条应该执行的指令。因此，正常情况下CPU对指令的执行一定是顺序执行的，除非遇到跳转指令或者什么函数调用之类的情况。否则，这个指令的执行流一定是啊，一条一条往下执行，那即便是遇到跳转指令。啊jump还有就是函数调用啊，call这一类的指令，即便是遇到这一类的指令，那接下来CPU执行的指令序列。依然属于同一个进程。好，那这就意味着，

如果一个进程的这些指令序列开始上CPU运行了，那么这个进程就会一直霸占着CPU。对吧，除非这个进程，它自动的自主的结束，但事实上我们结合平时的使用经验可以知道。我们的电脑里边同时会运行很多很多个进程，很多很多个程序，并且除了当前正在运行的这个程序之外。CPU是不是还需要处理一些外部设备发来的信号，比如说我们用键盘进行一个敲击，敲击呃就输入一个字符。或者用鼠标进行一个单击等等，这一系列的外部的呃操作都需要被CPU所响应，因此如果CPU只能按照顺序的方式来。

来执行某一个特定程序的指令序列，那么CPU就永远无法响应我们用户给计算机发出的一系列请求。所以，为了解决这个问题，我们会引入中断系统。正常情况下CPU依然是顺序的，执行这些指令序列。但是如果你的计算机实现了中断系统的话。那么就意味着CPU每执行完一条指令之后，在这个指令周期的末尾都会例行的检查，此时是否有？某一种中断信号需要处理或者更多数上会把它称为中断请求信号，比如说我们键盘啊，进行了一个敲击的动作，要往计算机里输入一个字符。

那么CPU执行完这条指令之后，它发现有一个来自于键盘这个设备的中断请求信号。那么，本来接下来CPU应该执行的是k+1这条指令，但是由于发现了这种啊，需要处理的中断请求，因此CPU会暂时改变自己的一个呃指令的执行流。会转而执行另一段程序，用这一段所谓的中断服务程序，通过这段程序的执行来响应键盘敲击的啊这个动作。那结合我们之前学习的啊，这个例子CPU要对键盘的敲击动作进行响应。其实就意味着键盘先把某一个字符的信息，比如说a这个字符先放到了IO接口里的某一个数据缓冲寄存器里，然后CPU。

在执行这个中断服务程序的时候，它需要从IO接口当中取出这个a字符。然后再把这个数据进行相应的处理，比如说显示在屏幕上之类的，那么当CPU处理完这个中断请求之后才会回去执行刚才。前边执行的这个啊，程序继续的从k+1这条指令往后执行好，所以这就是中断的一个作用。那显然，中断请求的类型一定是多种多样的，比如说键盘敲击产生的这个中断请求，它的处理过程和打印机输出完成这个中断请求。这两种中断请求的处理一定是不一样的，所以对于不同的中断请求，我们需要用不同的中断服务程序进行处理。

因此CPU如何响应一个中断信号呢？那首先像键盘鼠标，打印机等等这一类的IO设备。或者也有可能是某些时钟部件会向CPU发送一个中断请求信号，那我们把这种可以产生中断请求信号的部件。称为中断源。好，那CPU在每个指令周期的末尾都会例行的检查是否有中断请求信号需要处理，如果检测到中断请求信号，那么就需要对这个信号进行一个响应。那首先CPU会判断当前自己的状态是否是可以响应中断的，有的时候CPU会不想理这些呃中断请求信号，比如说执行了关中断。执行了关中断的指令之后CPU就会暂时不理睬这些中断信号，所以首先需要判断CPU当前的状态是否处于关中断的状态。

如果处于关中断的状态，那么就暂时不会响应任何中断信号好，那如果此时需要响应中断信号，那么就需要进行中断判优。中断判优做的事情就是如果说同一时刻有好几个中断信号都需要响应，比如你的键盘敲击，还有鼠标的单击发出了两个不同的中断信号。那CPU同时接收两个中断信号的时候，就需要确定一个处理的先后顺序，因此中断判优做的就是这样的一个事情。需要判断各个中断源的优先级啊，是怎么样的，然后CPU会优先响应优先级更高的中断源发来的一个中断信号。好，那经过中断判优之后就可以确定好接下来要响应的是哪一个中断请求信号，

那么刚才我们说过不同的中断请求信号。所需要处理的这个过程是不一样的，也就是说不同的终端信号所对应的中断服务程序。肯定是不一样的。因此，接下来要对这个中断进行处理的时候，首先需要通过中断引指令的帮助，把CPU的指令执行流转移到正确的一个中断服务程序这儿。那本质上就是修改PC的值，让PC指向这个程序的第一条指令，然后接下来CPU就可以执行这一个中断服务程序了，那本质上也就是执行一系列的指令。中断服务程序的指令序列和我们普通程序里边使用到的指令序列也是很类似的，也是需要PC不断的加一啊，这样一条一条的往下执行好，

那这就是处理中断的一个基本流程。那接下来我们对这个过程进行一个细化，那刚才我们提到一个叫做关中断的一个指令，如果CPU执行了关中断的指令。就意味着接下来CPU不会响应任何一个中断请求信号，那么CPU是如何判断自己当前是否处于关中断的状态呢？那很简单，这个状态信息会被记录在psw这个寄存器里边，也就程序状态自寄存器那这我们给出的是八零八六这个很经典的芯片。它的psw的每一位的一个呃名字。有一些标志位，我们之前是提过的，比如说of还有什么cfsfzf这几个标志位可以用于辅助那个呃条件转移指令的一个执行。我们在之前介绍条件转移指令的时候啊，介绍过这几个标志位好，

那现在我们来看光中断的一个标志位if。I指的是interrupt，也就是中断的意思，然后f就是flag，当if这一位等于一的时候，表示此时是开中断的一个状态，也就是说。此时，如果有中断请求的信号到来，那么CPU是会响应这个中断请求信号的。而如果if这一位的值等于零。那么就意味着CPU此时处于关中断的状态，即便当前有中断信号到来，那么CPU也不会响应这个中断信号，而是会继续执行之前的那个程序。

直到CPU再一次开中断为止，对了，上面这个看起来很复杂的图啊，是从操作系统小哥那边倒过来的。其实就是对中断信号进行一个分类啊，在祭祖这门课里边，我们先暂时不用深究，但是如果已经学过操作系统的同学，也可以结合这个图进行一个回忆，那现在大家先不要被。上边这些复杂的信息给吓到我们继续讨论刚才说的这个关中断，如果CPU。它的psw的if这一位。等于零，那么就意味着它此时处于光中断的状态，

那光中断这种状态的作用就是可以辅助实现某一些原子操作。这可能需要给跨考的同学解释一下，什么叫做原子操作呢啊？原子操作的意思就是说这一系列的操作，要么就一点都不做，要么就一气呵成的全部完成。所以如果我们期待这样的三条指令，他们做的事情能够一气呵成，一口气完成的话，那么我们在执行第一条指令之前。是不是可以先插入一个光中断指令执行了光中断指令之后，接下来即便有外部的中断信号到来，那CPU依然会继续往下执行。因为光中断就意味着CPU暂时不会理睬这些呃中断，请求信号好，

那执行完这三个指令之后。接下来啊，我们再执行一个开中断指令，在这插入一个开中断指令，那开中断之后就意味着CPU的if那个位变为了一。那接下来再有中断请求信号的到来CPU就可以正常的响应好，因此可以看到，如果我们用关中断指令和开中断指令。在它们中间加注一段程序，那么这一段程序代码的执行就不可能被中断，一定是一气呵成的。那这就是所谓的原子操作，要么就一步都不做，要么就每一个步骤全部都一口气做完。那学过数据库的同学肯定也知道原子操作的一个重要性，

很多时候我们需要保证系统当中啊，一系列的数据，它们是能够相互统一的。你不能说只改了其中某几个数据，而其他相互关联的数据没有更改，这样就有可能导致系统的数据错误，所以其实很多时候我们都会用到这种原子操作。包括操作系统里边的pv，元宇的执行也需要把它们封装成原子操作好，那以上这些是为了让大家能够体会光中断的一个作用。好，那刚才我们说CPU执行光中断指令之后，任何的啊中断请求信号暂时都不会被响应，但是也有一些很特别的啊。优先级很高的这种中断信号必须被响应，

这种中断信号就是非屏蔽的中断。比如说如果，你的CPU此时处于关中断的状态。if这一位等于零，但是你强行按住你自己的关机按钮，那么强行关机，这种很特别很紧急的事件。是必须被响应，被处理的，在你的电脑完全断电之前CPU需要做一些啊，这个强制关机相关的紧急的处理。比如说紧急的记录，系统的状态啊之类的，所以像这种系统掉电的情况就是必须被响应的一种特殊的中断信号，我们把它称为非屏蔽中断。

即便CPU此时处于关中断的状态，也必须响应这种非屏蔽的中断好。那除了关机掉电之外，也会有别的一些非屏蔽中断，有兴趣的同学可以自己再下去查一下。那与非屏蔽中断相对应的另一个概念，就是可屏蔽中断。大多数的中断请求信号都是可屏蔽中断，那当CPU处于关中断状态的时候，就不会响应可屏蔽中断。那在g组这门课当中，我们更多的是在探讨外部设备发来的一些中断请求信号，而外部设备IO设备发过来的中断请求。基本上都是可屏蔽终端。因为我们这一章的标题是IO系统嘛，

所以我们更多的是在探讨IO设备，所以接下来如果没有特别说明，那么当我们提到所谓中断的时候，指的就是可屏蔽的中断。那关于中断的具体分类，我们这就不展开了，大家可以去看一下操作系统的，应该是第四还是第五个视频？好，那接下来问题是这样的，如果此时CPU处于开中断的状态，当它检测到一个中断请求信号的时候，应该如何判断这个中断请求信号到底是哪？一个IO设备发过来的呢好，那为了解决这个问题，

我们可以设置一个中断请求的标记寄存器，那可以看到这个寄存器是由一个一个的触发器啊来组成的。那什么是触发器？我们之前介绍过每一个触发器，可以啊，记录一个二进制的零或者一，那么当某一个IO设备，它所对应的这个触发器。这一个比特啊，唯一的时候就意味着此时有来自于这一个l设备的中断请求，需要被处理。好，所以当CPU检测到中断请求信号的时候，是不是只需要来看一下这个啊？寄存器里边哪些比特是一它就可以知道接下来要处理的中断是哪些个？

那之前我们是不是说过，对于这种来自CPU外部的中断，也就来自于各种IO设备的这些中断CPU会统一的在每一条指令执行阶段呃结束之前。去检查此时有没有中断请求信号以及判断这个中断请求信号是哪些部件发过来的？那只有CPU检测到中断请求信号，并且此时处于开中断的啊状态的时候才会去响应这个中断请求信号。那第五章我们是不是介绍过一条指令的执行，可以分为这样几个阶段取止阶段，接下来是间止阶段，然后是执行阶段。啊，最后第四个是中断阶段，那我们之前说的第四个中断阶段就是用于检查这些中断请求信号的。好了，现在问题又出现了，

有的时候CPU有可能会同时收到多个部件发过来的，这个中断请求信号，那么到底应该先响应哪一个部件的中断请求呢？那这就是中断判优要解决的问题。可以用硬件来判优，也可以用软件的方式来判优，首先我们来看一下用硬件如何实现这个中断的判优，那我们刚才说过这个寄存器里边。这个寄存器里面记录了啊，到底哪些设备哪些中断请求信号需要被处理？那我们可以用这样的电路实现硬件排队器，不知道大家有没有忘记这儿，我们写了一个一，然后画了一个小圆圈，指的是这是一个非门。

就是输入零会输出一输入一的话会输出零，这是一个非门，然后对于这种画了一个语符号。这边又画了一个圆圈的这个部件，就是一个与非门，就是先给两个输入的信号进行与与完的结果，最后再取一个非是与非门。好，那我们可以把不同的这些中断源，他们的这些啊，中断请求的标记位，把他们用这样的电路接起来。那最左边的这个中断源所对应的信号是优先级最高的一个信号，然后越往后优先级越低。好，

所以这就意味着当我们啊，比如说这样的四个中断请求信号都同时到来的时候CPU会优先响应最高优先级的这个中断源发来的。请求信号好，来看一下什么原理，现在所有的这些中断源都同时发来了中断请求信号，那么对于最左边这个电路，我们输入了一个一经过非门的处理，是不是要变成零？然后这个零作为上边这个非门的输入啊，经过取反之后是不是又变成了一所以第一个中断源所对应的这个输出信号？最后的输出就是一好另一个方面，零这个信号会顺着这条线作为右边这个与门的其中一个输入。那现在优先级更低的第二个设备也发来了一个中断请求信号，它发来的信号是一，那由于我们左边这输入的信号是零。

那零和右边的一相与之后得到的结果应该是零对吧？那相与之后得零再取一个非这边的输出是不是就是一？那这个一在作为最后这个呃非门的输入，最后的输出是不是就是零，因此如果优先级更高的这个中断源，它也发来了一个中断请求信号的话。那么，只有第一个中断源所对应的这个输出信号，会是一那对于优先级更低的三和四这几个中断源，大家也可以自己静下心来推一下。反正在这种情况下，右边这几个优先级更低的，最后的输出都是零，当然了，这个电路一般来说不会考，

只是为了让大家呃更好的理解这个用硬件实现这种排队器的一个原理。那除了这四个设备同时发来中断请求信号的啊，这种情况之外，大家也可以自己推一下，自己验证一下，如果第一个是零，第二个是一。然后第三个比如说是一，第四个是一，那么最后的输出是什么呀？一定是当前优先级最高的这个啊，中断源的输出位。为一，然后其他的都为零好，那这个大家可以暂停来试一下，

那这就是用硬件排队器进行中断判优的一个原理。当我们同时收到多个中断请求信号的时候，用类似于这样的电路可以给每一个中断信号进行一个优先级的排序。优先级越高的中断信号，它会它会更优先的被处理，所以像刚才我们说的这种情况，四个中断源都有这个中断请求信号。但是只有第一个中断源的输出为一，那接下来是不是根据这个输出信号哪一个端的输出为一？就可以判断到底是哪个中断源发来的一个中断请求信号，那显然用硬件排队器进行啊，中断的判优是很快速的。好，那除了硬件排队器之外，既然我们这儿是一个寄存器，

那我们是不是也可以使用软件的方法来？一位一位的检查这个寄存器里边的啊，这些二进制比特位。需要按照优先级递减的次序来。一位一位的检查，那直到发现第一个二进制的一就可以决定接下来优先处理这个呃中断信号。所以这是程序查询的方式，那显然用这种程序用软件的方式来进行判断，也需要写一系列的指令，对吧？需要执行很多条指令才可以选择出某一个优先级更高的中断源。因此，用软件实现的中断判优显然要比硬件实现的要更慢很多，所以现在的计算机当中通常都是用硬件排队器来实现中断的判优。那我们可以按照这样的一些啊原则来设计多个中断源的优先级排序的次序，

首先首先第一条。硬件故障相关的中断信号属于最高级的，就比如说我们刚才说的掉电一定是最紧急，需要被处理的，其次是软件中断。比如说用户程序发起，系统调用的时候会触发软件中断，那显然我们应该先处理硬件故障相关的这些中断。好，第二点非屏蔽中断，要优于可屏蔽中断，这点我们之前是不是也说过这点显而易见，对吧？非屏蔽中断一定是某一些非常紧急的，连关中断都不能把它屏蔽掉的中断信号。

那么，这一类的中断信号一定是需要优先处理的。好，接下来第三点dma请求要优于IO设备传送的中断请求，那第三个原则和第四个原则其实本质上是很类似的，第四个原则说高速设备。的中断信号要优于低速设备。那之前我们说过dma控制器就是用于控制某一些高速设备的，对吧？而这个地方所谓的IO设备指的是一些更慢速的IO设备。所以第三和第四这两条啊，他们想表达的意思是一样的，我们需要优先的响应高速设备发来的中断请求。然后再来响应低速设备发来的中断请求，那为什么要这么设计呢？

原因是高速设备它既然工作速度很快，那是不是就意味着呃这个高速设备的中断请求你响应的越慢？越耽误人家工作，而低速设备本来它生产力就不行，本来速度就很慢，你让它拖一拖，是不是也没有太大关系，就有点类似于你当老板的话，你有一个那个生产力很高的员工。还有一个生产力很低的华水员工，那如果这两个员工都给你发来了一个请求，如果他们的请求你不给他们处理，不给他们响应他们俩的工作，就无法继续往下推进的话。那你是不是肯定会优先响应这个呃，

效率比较高的员工，他发过来的请求，先把他的问题解决了，让他先回去工作，那这样的话，你的员工是不是可以帮你干更多的事情？而对于效率很低的员工啊，划水的员工，你拖一拖也没有关系，损失不大啊，大概就是这个意思好，所以这就是高速设备的中断请求。要比低速设备的中断请求优先级更高的一个原因好，接下来第五个原则输入设备的优先级要高于输出设备。这个也不难理解吧，

比如说键盘，它是把字符输入给计算机的，先要输入到IO接口的某一个缓冲器里边。然后打印机是要把主机内的啊字符给输出打印，同样的主机里的字符也是会先被放到某一个缓冲寄存器里边好，那为什么输入设备的优先级要更高呢？原因是键盘敲击之后啊，某一个字符信息被放到缓冲寄存器里边，那如果说CPU不及时的把这个字符信息取走的话。那接下来使用键盘的那个人手速很快，又紧接着输入了第二个字符，那是不是就会把之前的那个字符信息给冲掉？也就会导致输入信息的一个丢失，因此CPU需要优先的响应这个键盘，而对于打印机来说，

是不是无所谓啊？CPU给他一个字符，打印机慢慢的取走一个字符，然后当这个字符打印完成之后给CPU发一个中断请求，那CPU是不是可以慢慢的给它第二个字符，让它等一下是无所谓的。所以我们会优先响应这个输入设备发过来的一些中断请求，好接下来第六点实时设备的优先级要高于普通设备。那看这个名字是不是就很好理解，实时设备就是你给他反馈要很及时很实时嘛，那普通设备就是反应的迟钝，一点也没有关系，所以实时设备的优先级更高，这个比较好理解。这就不做过多的解释，

好，那这就是我们设计多个中断源的一个优先级，可以参考的一些指标，我们按照这样的原则进行优先级的排序会比较科学。好，那回到前面这个PPT，我们现在是不是已经解决了多个终端信号，到来的时候先处理哪一个终端信号已经解决了这个问题？那接下来根据我们选中的这个中断请求信号，我们是不是得找到与这个中断请求信号相对应的中断服务程序啊？也就是要找到中断服务程序的入口地址，那这又是怎么实现的呢？首先刚才我们说过这个CPU执行指令的时候。会让PC的值不断的加一，所以当CPU执行了k这条指令之后，

PC继续加一。会指向k+1这个位置。好，现在故事是这样的k这条指令执行啊，快结束的时候CPU发现了一个中断信号，一个中断请求信号，那么接下来CPU就需要执行这个中断服务程序。相对应的那些指令，那这是不是就意味着我们需要把PC的值，把PC的值指向？这个中断服务程序的第一条指令。原本下一条我们应该执行的指令是k+1，但是现在这个中断的事件导致程序执行流发生了改变。好，所以当我们处理完这个中断之后CPU是不是应该回到以前的位置啊？

以前应该接着往下执行k+1这条指令。那么CPU处理完中断之后，是不是又需要让PC的值恢复为k+1这个值好，所以当程序执行流需要改变的时候，我们是不是应该先把PC之前的这个值？先给保留下来，只有把PC的值保存下来，那在中断服务结束之后才可以恢复PC以前的值。那这个工作可以交给所谓的中断引指令来进行。中断引指令并不是一条呃，具体的指令，而是CPU检测到中断信号之后会自动来执行的呃，一系列动作。好来看一下中断引指令需要做什么？首先第一步需要保存源程序的PC值，

就是要把k+1这个值保存下来。以便于我们处理完中断之后可以恢复以前的这个程序，继续往后执行好第二点中断引指令，还需要让PC。指向这个中断服务程序的第一条指令，要修改PC的值，那这些动作都是CPU检测到中断信号之后自动的啊，会做的一系列动作。好，那现在我们要留下两个问题，第一个问题PC的值应该如何保存？保存在什么地方？第二个问题，当我们保存了以前这个PC值之后。应该如何找到特定的中断服务程序？

它的入口地址好，这两个问题我们先留下啊，我们先来把中断引指令所需要做的事情再给细化一下。就是CPU检测到某一个中断信号之后，会做以下一系列的事情第一步，首先会关中断，那之前我们是不是说过关中断是为了实现原子操作？因为接下来保存这个程序，断点这个事情是非常重要的，我们必须保证这个事情能够一气呵成，不被打断。所以执行了关中断指令之后就可以啊，来保存这个断点，也就是要把PC当前的这个值。保存下来，

那通常来说PC的值可以放到堆栈里边，就是操作系统会有一个内核堆栈区。把PC的值放到内核堆栈里，这是比较常见的一个处理方式，那堆栈我们都很熟悉啊，就是把PC放到栈顶嘛，把PC的值也就是。啊k+1。k+1的这个值放到内核堆栈的栈顶，那所谓的内核堆栈其实也是主存内的某一片区域，那除了用主存里的堆栈保存之外。也可以用其他的一些特定的存储单元来存储，反正我们只需要保证这个PC的值，能存的进去还能取得回来就可以了。好，

那现在这个程序的运行断点保存完了之后，是不是就可以把PC的值，把PC的值？指向中断服务程序的第一条指令。也就是需要引出中断服务程序，本质上就是在修改PC的值，那么回到我们之前留下的问题，如何确定某一个中断信号所对应的中断服务程序？它的起始地址呢？那我们可以用两种方式来解决这个问题，同样的软件，硬件两种解决思路，那我们重点来探讨硬件的实现思路，叫硬件向量法。做法是这样的，

首先我们是不是可以给每一个中断请求信号，给它进行一个编号，比如说一二号呃，中断请求一三号，中断请求。一四号中断请求，那么可以看到在主存里边与这些啊中断请求号所对应的啊主存单元里边。保存了一个jump，无条件转移指令，这个无条件转移指令指明了当前这个中断请求。一二号中断，请求所对应的服务程序中断，服务程序，它的入口地址。指明了这样的一个信息，

那我们把指向这个中断服务程序起始地址的啊，这个这个地址信息把它称为中断向量。好，那来看一下整体的工作过程，之前我们是不是说过我们可以用硬件排队器实现中断判优？那中断判优会导致啊，只有一个中断信号，所对应的输出线会输出一，然后其他的输出线一定都是零。那我们可以把之前这个呃排队器的输出作为中断向量地址，形成部件作为这个电路的输入。然后经过这个电路的处理之后，会把所对应的某一个信号把它映射为某一个向量地址。比如说我们的排队器输出的结果是这样的，只有最左边这个比特是一，

那么通过这个比特的信息是不是就可以判断到底是哪一个中断源？发来的中断信号好，接下来根据一零零零根据这个输入信号，然后再经过呃中断向量地址形成部件，经过这个部件的处理。会输出一个这样的向量地址，那这个向量地址我们把它转换成12进制啊，右边这个应该是二左边这个应该是一。所以用12进制表示，这儿输出的向量地址就是一二h。也就是最上面这个地方对吧好，所以这个电路的作用就是根据某一个中断请求信号得到与这个中断请求信号相对应的。中断向量的保存地址。刚才我们说过，中断向量是指向了啊，

这个中断请求信号的一个中断服务程序入口地址。而我们左边这个部件输出的向量地址，指的是中断向量的一个存储地址，就相当于这个中断向量，它是函数的指针。而向量地址是指针的指针。就是这样的一个关系好，我们再来换一种输入，假设现在只有第二个比特输入的是一，那么根据这个比特的信息是不是肯定可以判断出？此时是哪一个中断源发来了中断请求信号好？那同样的，通过这个部件的处理之后，会输出一个与这个中断源所对应的向量地址。这个向量地址，

把它翻译成16进制的话，应该是一三h，所以这个向量地址就是指向了啊，当前这个中断请求信号。所对应的中断向量的存储地址好，接下来再根据这中断向量，也就是根据这个函数的指针就可以找到与这个中断请求信号。相对应的中断处理程序。好，那这就是用硬件来确定某一个中断请求信号所对应的中断服务程序的一个方法。那大家需要区分向量地址和中断向量这两个概念，很容易混淆中断向量，指的是函数的指针。然后向量地址可以理解为是这个函数指针的指针。那这边输出的线量地址，

还有一个名字叫做中断类型号。我感觉第二个术语看起来更好，理解一些，反正我们就是根据输入的某一个比特来判断当前呃接收到的这个中断请求信号。是什么类型的？是哪一个中断源发过来的？接下来根据这个中断类型号去找到与这个中断类型。所对应的中断处理程序是不是就OK了？好，那这可能会有同学有疑问，既然我们一三号这个中断类型号，它所对应的呃服务程序入口地址是300这个地址。那么，我们为什么不把这个硬件电路直接让它输出的这个地址信号直接让它输出的这个地址信息？让它直接指向300，

这样不香吗？好，事实上，这种方案也是可以实现的，但是大家需要知道，我们的中断服务程序。有可能需要被修改，对吧？比如现在打印机所对应的中断服务程序，它只占了200到二九九，这些个地址。那如果它变长了，它直接占了200到三九九，那怎么办？那这样的话，

显示器所对应的服务程序是不是只能从400这个地址开始往后存放了？所以一旦我们修改了这个中断服务程序，那么是不是就意味着我们还得再来修改这个硬件电路啊？那这显然是不科学的好，那再回头看我们之前提出的这种两级指针的方案，虽然我们的这个中断服务程序有可能会变长变短。可能会修改，但是系统当中可以出现的中断类型，我们是可以刚开始就规定好，以后一直固定不变的。只有可能出现一二号中断，一三号中断，一四号中断，给所有的中断类型进行一个固定的编号就可以，那如果显示器所对应的中断服务程序要从400这些地址开始存放。

是不是我们只需要修改主存里边的这些中断限量，把以前的300把它修改为400？是不是就OK了，而不需要去修改这个硬件电路？好，因此这就是为什么要用指针的指针这种思想来解决问题的原因。对了，这地方要让这个中断向量生效，是不是只需要执行这条无条件转移指令就可以？jump三百会无条件的使得PC的值变为300，而300这个值刚好就是呃显示器所对应的中断服务程序。所以这就是中断引指令，引出中断服务程序的一个硬件，实现的方法。那再次强调，

中断引指令并不是一条具体的CPU指令。其实这个中断引指令的背后，需要做很多很多的事情，甚至有可能需要执行多条指令，比如说要执行jump指令，要执行关中乱指令。所以，所谓的中断引指令，指的是一系列的任务，而不是某一条特殊的指令。但是，这一系列的任务都是由CPU啊自动的完成的。只要检测到中断请求CPU 1定会干这一系列的事情好，那现在我们已经找到了这个中断处理程序的入口地址，并且让PC的值指向了它的第一条指令。

接下来是不是就应该执行这个中断服务程序了？比如说键盘相关的服务程序或者什么打印机相关的服务程序，那么这些中断服务程序又需要做什么事情呢？有一些事情是公共的必须做的，第一步是保护现场。就是要保护之前运行的这个程序，它的一些CPU环境，之前的这个程序不是执行了一系列的指令吗？执行到了k这条指令，然后就被中断了。好，现在在这个中断服务程序的呃，刚开始需要先保护现场什么意思？大家可以回忆一下我们之前讲过的一些比较具体的例子。CPU内部是不是会有很多很多寄存器，

比如说ACC累加寄存器，还有很多通用寄存器，那么当这个程序之前的这个程序运行的过程当中，是不是会把自己运算的某一些中间结果？存在这些寄存器里边。但是现在这个程序执行了一半，就被中断了，那万一在这个中断服务程序里边也需要修改这些寄存器里边的值，那是不是就会导致以前前边的这个程序？它中间的运算结果就会丢失，对吧？好，所以为什么要保护现场呢？就是为了让之前运行的这个程序在继续往后执行的时候。能够接着之前啊，

得到的这些中间结果继续往后运算，那通常来说呃，要保存的现场环境就是各种各样的寄存器的值。可以把这些寄存器的值压到系统堆栈里好，那这是中断服务程序要做的第一个事情，那接下来第二步这个中断服务的一个啊主体。那这个部分要做的事情就千差万别了，比如说呃键盘的输入中断信号，还有打印机的输出完成中断信号啊，要做的相应的处理肯定都是各不相同的。好，最后处理完那个中断请求之后，第三步就需要进行恢复，现场就是把之前运行的这个呃程序的acc啊，什么通用寄存器啊之类的。

那些寄存器的值给它恢复，也就是要从我们之前压入的这个堆栈站顶弹出各种寄存器的值，把它们写回去。对了，还记不记得之前我们说过这个堆栈的啊？另一个作用就是原来运行的这个程序啊，它的PC值。PC值PC=k+1。这个值也会被压到堆栈里边，对吧？然后刚才我们又说在这个堆栈的栈顶还会被压入什么ACC啊？还有各种通用寄存器的值。压到站顶，然后这个中断服务程序处理完成之后，又需要弹出站顶的什么ACC这些寄存器的值给它恢复寄存器的现场。

然后再接下来是不是就可以弹出我们之前保存的PC，这个值把PC的值赋为k+1就可以返回到原来的这些程序，继续往后执行了。它的运行环境，各种寄存器的值和之前一模一样，同时PC的值也指向了一个该有的位置。好，那这就是中断服务程序要做的最后一步，弹出这个站点保存的呃程序断点的信息，把这个断点的信息k+1赋给PC。从而使得这个指令的执行流回到原来的程序断点处，继续往下好，那这就是中断服务程序要做的一些事情。那这个小节的最后，我们再用一个流程图来把中断处理的整个过程捋一遍，

首先正常情况下CPU 1定是取一条指令执行一条指令。局一条指令执行，一条指令来顺序的执行，各条指令对吧？那在每个指令周期的末尾CPU都会例行的检查，此时是否有中断信号需要处理？如果没有终端信号，需要处理的话，就可以继续运行，继续取出下调指令啊，往下执行好，那假设我们取出k这条指令，并且执行之后。产生检测到了一个中断请求信号，那在这个时候PC的值应该是指向了k+1下一条指令的位置。

那接下来CPU检测到中断信号之后，会自动的完成一系列的动作，也就是所谓中断引指令要完成的。这一系列动作。首先要做的就是把之前这个程序执行的断点信息PC值给它压入栈中，把它保存下来。除此之外，也需要执行一条关中断指令。对了，之前我们是说先执行关中断指令，再来保存程序断点，对吧？但这个地方我们把关注段写在了后面，但其实由于是用硬件完成这些动作，因此这两个动作是可以同时进行的，

所以大家在看书的时候啊，无论看到哪一个被写在了前面。呃，其实都无所谓，这些动作可以同时完成，一会儿我们再来补充解释这个光中断真正的一个作用好那啊，除了刚才说的这两个动作之外，还需要通过硬件电路的处理得到。这个中断请求信号所对应的向量地址，然后再通过这个向量地址得到中断向量，然后再把这个中断向量赋给PC，这样的话，我们程序的执行流。就指向了啊，中断服务程序的第一条指令，

那接下来就可以执行这个中断服务程序，而这个中断服务程序刚开始的一些指令一定需要做这样的一个事情，就是要保护。前边这个程序的呃运行现场。最重要的就是要保存各种寄存器的值好，那保存了现场信息之后就可以对这个中断请求信号啊，进行一个具体的处理。那这一步要做的事情千差万别，具体要看到底是什么样的中断请求信号好，那完成了中断服务之后就可以恢复以前这个程序的运行现场。把寄存器啊什么的，把这些值给恢复，然后最后需要执行一条开中断指令，再让PC的值指回以前的这个程序，它应该运行的地方。

从而就可以使得这程序的执行流再次回到以前那个位置，那么这就是CPU对啊，中断请求信号处理的一整个过程。首先是中断引指令，然后要执行中断服务程序。好，现在我们再回头填一个坑，在中断引指令这儿，我们说要执行个关中断，然后一直到中断服务程序执行结束之后。才进行开中断。那之前我们是不是说过用关中断和开中断这两条指令加注的，中间这一整堆的呃事情的运行？这一整堆的动作执行都不可能被中断，而是一定会一气呵成好，

所以在中断引指令这个部分进行关中断，最后在中断服务结束之后。才进行开中断，这样的处理就可以保证我们对中断请求的服务是一气呵成，是不会被打断的，这也就意味着即便在运行中断服务程序的。啊，这个中间某一个时刻有一个中断，有一个新的中断请求信号到来，那CPU会先忽视这个中断请求信号，直到最后执行了开中断才会去响应前边的这个中断请求信号。好，所以这就是中断引指令和中断服务程序，它们在这两个位置安排关中断和开中断指令的一个作用。那对于这种中断处理的方式，

我们把它称为单重中断，就是当我们在执行中断服务程序的时候，不响应新的中断请求。然而，在有的系统当中，这种单重中断的呃实现方式也许无法满足需求。那这就涉及到了多重中断的一个处理方式，这个问题我们留在下一小节继续探讨好的，那么以上就这一小节的全部内容，大家需要搞清楚这个中断处理的啊，各个过程。需要做一些什么事情？
