各位同学大家好，从这个小节开始，我们会详细的学习之前提到过的几种IO控制方式。那通过之前的学习，我们已经认识了什么是IO系统以及主机和外部设备之间如何通过IO接口来进行这个数据的交互。那在第一节当中，我们提到过IO控制方式相关的内容机组，这门课里我们要详细探讨的是呃程序查询方式，程序中断方式和dma方式。那这个小节我们先来看程序查询方式，那我们之前给过这样的一个示意图，对于程序查询方式来说。CPU启动一个IO操作之后，比如说启动一个读操作，那么当IO设备正在准备数据的时候。

CPU会不断的轮询检查，检查IO接口当中这个状态是否已完成？当它发现这个IO设备的数据输入已经完成之后。CPU才会去IO接口当中取走数据。那么，在IO设备准备数据的这个过程当中，CPU是不可以干其他的事情的，它需要一直不断的检查这个IO接口的状态。那我们把刚才说的这两个过程给细化一下，首先CPU查询IO设备的状态，当IO设备的状态。变为已完成的状态之后，CPU就可以进行数据的传送，也就从IO接口的数据缓冲寄存器里。取走数据，

取到CPU内部的某一个寄存器当中，用这样的方式传输一个字的内容，然后CPU才可以继续往后执行。好，那为了让这个模糊的描述变得更具体，接下来我们结合叉八六架构当中的两条IO指令。来解释一下CPU是如何通过这样的IO指令完成呃字符的打印的。假设CPU此时要打印三个字符，那么为了控制IO设备，我们会用到这样的两条叉八六指令集里的。IO指令一条叫in就是输入，就是要把IO端口当中的某一个指定寄存器的数据把它输入到CPU的寄存器rd当中。这是输入，然后输出out这个IO指令要做的事情，是把CPU的寄存器rs的内容把它输出到指定的IO端口rd当中。

那上一小节，我们说过IO端口指的就是IO接口当中这些一个一个的寄存器。每一个寄存器，或者说每一个IO端口都会有一个自己的地址，那我们假设上面这个数据缓冲寄存器。给它的地址是n，然后下面这个状态和控制寄存器给它的地址是rn+1。然后CPU内部有两个通用寄存器，分别是r0和r1好，现在来看一下我们如何打印输出三个字符。好，首先我们肯定得先确定啊，我们要打印输出的这三个字符存在什么地方？比如说存在主存里，那么我们是不是可以把这三个字符？

先把它弄到CPU的寄存器里面，比如说寄存器零放a这个字符，寄存器一放b这个字符，然后第三个寄存器。放c这些字符之类的，总之我们要打印输出的数据，有可能本身就存在寄存器当中，也有可能是存放在主存里，那如果是存放在主存里，我们只。我们是不是只需要进行一次读主存的操作，就可以先从主存里把这次要打印输出的字符把它读到CPU的某一个寄存器当中？好，那假设我们此时先要打印的字符是a，那首先CPU要向这个打印机。

发出打印的一个命令字。啊，比如说和打印所对应的命令是什么？零零零一零零之类的好，那如何发出打印命令呢？很简单。只需要用out输出这一个呃指令，这个IO指令，比如说经过打印机的驱动程序的处理。呃，已经把这个打印启动打印的这个命令字已经放到呃寄存器r1里边了。那么，我们是不是应该把寄存器r1里边的这个命令字把它输出到rn- 1这个IO端口这儿？好，所以只需要用一个输出的指令就可以完成这个操作，

因为上小节我们说过所有的这些IO端口。它是不是都有一个地址啊？那CPU通过地址线，通过地址线指明，此时我要输出的IO端口啊，地址是n+1。然后再通过控制线指明我此次是要对这个寄存器要对这个IO端口进行写操作。然后要写的这个命令字数据，只需要通过数据线给它传过来，是不是就可以了好，所以通过out这样的一个IO指令。就可以对打印机发出打印的命令，但是现在这个打印的命令只是被放到了呃，这个呃IO端口里边。接下来，

这个打印机对应的IO接口，这个相关的IO控制逻辑，电路是不是需要根据CPU发过来的这个命令信号？打印的命令信号。给打印机。通过控制线。发出相应的控制信号。收到这个命令之后，是不是就可以开始启动？那当它启动的工作准备工作完成之后会通过。状态线会通过状态线给这个IO接口进行一个反馈，那IO接口发现这个打印机。启动操作已经完成，已经就绪了，那么同样是这个IO控制逻辑，

电路会把打印机的ready。呃，就绪的这个状态信息把它存到n+1，这个状态寄存器当中。所以之前这个寄存器本来是用来存放命令字的，但是由于这个命令已经发出去了。所以这个寄存器里边的命令字，我们就不需要再保存了。打印机已经知道它是要进行一个打印的任务好，所以我们可以把打印机的ready。呃，就绪的这个呃状态，把它存到寄存器当中，当然了，这儿虽然我们写的是一个就绪的状态，

但其实这个状态寄存器的背后。是不是有很多零一零一零一那每一个比特位，我们可以给它赋予一个特殊的含义，比如说第三个比特就是表示是否就绪之类的。好，那现在这个打印机的就绪状态已经被放入到状态寄存器里边了，那对于CPU来说。如果它采用的是程序查询方式，那就意味着当这个CPU通过刚才的out指令。呃，发出这个打印的请求之后CPU，它会一直轮询的，通过这个数据线。轮询的来检查这个状态寄存器，它是否已经变成了ready已经就绪的状态？

那这个轮询检查的操作是如何实现的呢？就是用这个in输入的IO指令CPU可以不断的执行输入。这个IO指令，比如说把n+1 n+1这个寄存器的值，把它输入到CPU的，比如说r0吧啊，反正随便挑一个通用寄存器。那通过这样的一条指令执行，就可以取得状态寄存器里边的值，然后CPU再来测试取得的这个状态信息。是否已经ready？是不是就OK了？那如果说如果说这个打印机启动的速度比较慢，那么CPU可能会取好多次状态，每一次都发现它还没有就绪。

那没办法，程序查询方式就意味着CPU你必须这么做，你只能等好，那回到我们刚才的故事，现在这个打印机已经ready已经就绪了。并且就绪的状态已经被存到这个状态寄存器里了，那么同样的CPU的这次轮询检查通过一个in指令，是不是就可以发现？这个打印机，它此时是处于就绪的状态，它已经准备好可以输出一个字符了，好因此CPU接下来就可以给打印机输出第一个字符的信息。刚才我们是假设要打印ABC这样的三个字符，那么首先CPU通过地址线指明。我此次要操作的这个l端口是rn这个端口，

然后控制线指明这次我是要往这个l端口里边写入数据。同时，通过数据线。把a。把a这个字符的信息打到数据线上，那么这三个信号就会导致a这个字符的数据被写入到数据缓冲寄存器当中。那是不是和之前一样a这个字符的写入，只需要用一个out指令就可以实现，对吧？好，现在第一个字符已经到位了，那么呃，接下来会有IO控制逻辑。把这个字符的信息。通过我们这画的这条线。

再通过数据线输出给这个打印机。输出一个a这样的字符。当然，同时也会发出相应的控制信号。那么，当打印机在打印这字符的时候，它是不是处于忙碌的状态？直到这个字符的打印工作完成之后，它又会通过状态线给IO接口反馈，这个打印已完成的一个信号。那么IO接口再把这个状态寄存器再次改为就绪的状态，那CPU这边是不是也一样？它刚才一直在检查嘛？之前一直发现这个打印机还一直在忙，没有完成工作，

那直到这个打印机再次就绪之后CPU是不是就可以往这个数据缓冲寄存器里？充入第二个呃字符的信息，那第二个字符比如说是b，然后接下来IO控制逻辑，再把b。通过这个线路给输出到打印机这儿，然后打印机是不是接着刚才的a继续往后打印b就可以了？好，同样的打印b的时候，打印机也会处于忙碌的状态，此时CPU会不断的来检查这个状态寄存器。如果打印机还没有完成b这个字符的打印，那么CPU只能继续等，直到b打印完成，那接下来是不是又到了c？

第三个字符，那第三个字符的处理也是一样的好，那最后ABC这三个字符都已经输出完成之后，打印机最后一次。给这个啊IO接口进行一个反馈，然后同样的在这个电路的帮助下会把状态啊再次改为。嗯，比如说是就绪或者也有的地方把它称为finish就已完成的状态。好，那此时CPU是不是也是一直在轮询检查？那对于CPU来说，当它第三次检测到这个打印机的打印输出动作完成之后。是不是就意味着他要打印输出的三个字符就已经完成了？打印输出是否完成？这个判断很简单吧，

比如说我们可以设置一个变量嘛。变量I存到某一个寄存器里，让I从三变为二啊，变为一，那么等I的值减为零之后，是不是就意味着我们想要打印的所有的字符啊，都已经输出完成了？好，那打印工作全部结束之后CPU是不是就可以给这个打印机发出一个停机的一个指令？停机的一个命令字，那同样的，这个停机的命令字也需要驱动程序，这打印机的驱动程序的一个帮助，只有驱动程序知道。停机应该发出什么样的命令字？

好，那接下来在这个IO接口的控制下，这个停机的呃信号同样的会被传给打印机，那么打印机的工作就此停止。好，那这就是用程序查询方式打印三个字符的一个例子，我们结合了叉八六里边真实的啊，两个IO指令。给大家举了一个比较详细的例子，把这个过程捋清楚了，那你基本就能够知道CPU是如何控制呃外设的了。好再补充一句，我们王道书里给的这个IO接口的呃内部构造，其实并不是通用的，它只是在逻辑上描述了，

我们至少应该包含这样的一些部分。但实际当中的IO接口有可能还会包含更多的，更复杂的一些寄存器之类的，所以如果大家在其他教材上看到和这个图不一样的IO接口的表示方式，也不要觉得奇怪。IO接口的种类千奇百怪，都是正确的好，那我们用流程图的方式把程序查询方式的这个过程给总结一遍。首先，第一步，预置传送参数就是我们刚才说的什么ABC啊？要传送三个字符，那所谓传送参数就是指比如说设置一个计数器。用来记录你现在还剩余多少个字符没传。好，

接下来这些参数搞定了之后，是不是就可以启动外设？就是我们刚才说的启动打印机啊，发出相对应的一个命令字。好，接下就需要进入到程序查询，不断的轮询的这个过程需要不断的检查外设的状态，是否已经就绪？如果外设已经就绪，那么CPU就可以传送一次数据。并且修改传送参数，那传送参数就是我们刚才说的这个什么计数器I之类的，这些参数好，那如果传送还没有完成，还需要传下一个字的数据。

是不是就还需要回头来检查这个外设状态，直到外设再次就绪的时候再传送下次数据？好，那重点看中间这个框里边的部分。可以看到，如果说外设此时一直是没有就绪的状态，那么CPU就会不断的被卡在这个循环这。不断的轮询检查，不可以去做其他的事情，所以这就是程序查询方式的一个缺陷，那我们这儿再给出一些文字的补充。这样可以方便大家结合文字再来回顾每个步骤做了什么事情，那程序查询方式显然它有踏步的现象。CPU一直在原地踏步。原地踏步一直在死循环。

并且它和IO设备是创新工作的IO设备，在干工作的时候CPU一直在检查IO设备。工作完成之后，再次就绪之后CPU才可以传下次数据，所以它和IO设备是创新工作的。资源利用率非常低，那程序查询方式是比较早期的，那些计算机里边采用的一种啊IO控制方式，它的IO接口设计起来比较简单。就是用我们之前提出的那种基本的结构就可以，不需要再添加其他的一些呃硬件机构。那缺点呢，刚刚已经说过，就是CPU需要花大量的时间来查询和等待好，那接下来我们通过一个例题感受一下啊，

这个部分。在大体里边有可能出现了考察形式。首先，题目告诉我们啊，这个系统采用的是程序查询方式，然后不考虑处理时间，每一个查询操作需要100个时钟周期。它这不考虑处理时间的意思，就是说CPU和这个IO接口之间数据的输入或者输出这些，这些时间可以不计。我们只计算这个CPU，每一次查询所需要的时间，那每一个查询都需要花100个时钟周期，并且它已经告诉我们时钟频率。是50M赫兹，

那我们是不是可以先根据时钟频率算出每一个时钟的一个呃长度，每一个时钟是20纳秒，那100个周期？才可以完成一次查询操作，就意味着每一次查询需要花费2000纳秒的时间，好继续往后说，有鼠标和键盘这样的两个设备。然后CPU必须每秒钟对键盘，对鼠标进行30次的查询，然后对于硬盘来说，每一次传送32位字长的。呃，数据。也就是说，对于硬盘每读或者写32位，

就需要被CPU查询一次。然后硬盘的传输率啊，传输速度需要达到这样的一个速度。问，我们CPU对鼠标和键盘这两个设备查询，就是程序查询的这个时间花费。它的时间比例是多少？好，我们首先看鼠标，每秒钟CPU需要对鼠标进行30次的查询。那么呃，每一次的查询，刚才我们算出来了，需要消耗2000个纳秒。因此，

每一秒钟里边需要花费呃六万个纳秒来进行鼠标的这个查询。那进行一个简单的相除，就可以得到啊，对鼠标的查询操作。需要花费的时间占总时间的比例是这样的一个比例。显然，每秒钟对鼠标进行30次的查询是呃，不太占用CPU的这个时间的基本不影响CPU的性能。那这儿可能有同学已经有疑问了。为什么要对鼠标进行查询呢？呃，这儿有个IO接口，然后这儿连了一个鼠标。一个鼠标好，那我们知道鼠标的常规操作是鼠标的移动或者点击之类的信息，

对吧？那么，鼠标的移动和点击的啊，这些信息是不是也会被输入到IO接口里边？比如说用某一个寄存器来表示，此时鼠标向左向右向前向后。滑动了多少，或者是否发生了点击之类的，那么由于鼠标随时都有可能移动，随时都有可能点击，因此CPU是不是就需要定期的？来检查鼠标所对应的这个IO接口，如果他发现鼠标移动了，或者鼠标有点击了，那就需要做出相应的一些处理嘛，

所以CPU才需要每秒钟对鼠标进行30次的查询。那如果说一秒钟只检查鼠标一次，是不是就有可能导致鼠标的滑动或者点击的信息丢失？比如说如果鼠标发生了点击操作，那么这次点击位置之类的信息会被放到上面这个l端口这儿。那如果CPU每一秒钟才检查一次IO接口，里边的各个寄存器，那是不是就意味着如果我们一秒钟之内？点击了鼠标两次，那么第一次的点击信息还没有被CPU取走，是不是第二次的点击信息又会把第一次的点击信息给覆盖掉？因此，如果CPU对鼠标这个外设的呃检查就是程序查询的这个频率太低的话，就有可能导致这个外设的某一些输入数据。会丢失，

因此我们第一个小问计算的百分之零点零零六，这个时间开销指的就是CPU用于检查鼠标的点击。对于这个检查，查询的过程所需要付出的一个时间代价好，这是鼠标接下来看硬盘。啊，对硬盘的查询需要花多少时间呢？首先这个题目告诉我们，硬盘每一次是传送32个比特，对吧？然后硬盘的传输速度需要达到这样的一个速度，所以为了达到这个速度，每秒钟是不是就需要进行二的19次方的呃这么多次的传送？还是结合比较具体的例子，大家可能更好理解磁盘。

连接到这个IO接口，这然后假设现在CPU是要从磁盘里边读入啊，一系列的数据。那么，从磁盘里读入的数据首先会被放到一个数据缓冲寄存器当中。每一次会往里边放32个比特，也就是我们这儿说到的一个字，每一次放32个比特，那除了这个数据缓冲寄存器之外。是不是也得有状态寄存器用于反映啊？这次的32比特是否已经输入完成？如果输入完成，那CPU是不是就可以从这个地方直接取走32比特的信息好，所以每秒钟要进行这么多次的传送。就意味着CPU每秒钟至少需要检查这个状态，

寄存器二的19次方这么多次。而每一次的检查都需要花费2000纳秒，因此每秒钟查这么多次，所需要的时间总开销大概就是这么多纳秒。那可以算一下这个时间和一秒的一个比例，每秒钟我们需要花这么多的时间来查询，那这个算出来的值105%。这就意味着，一秒钟之内CPU是不可能完成这么多次的呃程序查询的。好，所以这就意味着，即便CPU把所有的时间都用于这个程序查询，并且我们在这还不考虑CPU取走这个数据的时间。但是即便如此，采用这种程序查询的方式。

我们依然不可能得到呃，这么高的一个磁盘传输率。好，所以从这个例子是不是可以感受到程序查询方式对于一些慢速的IO设备其实是OK的？慢速的IO设备如果采用定时查询的这种方式，那么对整体的CPU性能并不会有太大的影响。但是对于一些快速的IO设备来说。如果我们同样采用程序查询方式，那么就有可能会大量的占用CPU的查询时间。甚至像这个例子当中，即便CPU把所有的时间都花在查询和传输数据上，即便是这样也依然达不到我们。我们想要的这个磁盘传输率。好，那这就是我们可以自然而然感受到的一个结论，

那刚才我们是从时间的角度来计算啊，这个查询时间的一个占比。但其实我们也可以从频率的角度啊来计算那关于这个计算方法的理解很简单，大家可以暂停来自己看一下从频率的角度时钟周期的角度，也可以得到正确的结果。好，那简单的计算问题，这就不再赘述。那通过这题目，我们可以感受到，之前虽然我们说。如果采用程序查询方式，那么CPU会一直不断的轮询检查。然而，事实上CPU也可以采用这种定时查询的方式。

定时查询的方式就意味着CPU每隔一段时间会检查某一个特定的IO接口。而之前我们说的CPU不停的检查一个IO接口的那种方式，叫做独占查询的方式。独占查询就意味着CPU会花100%的时间，全部的时间一直在等待这个IO的完成。而定时查询的方式意味着。CPU每隔一段时间查一次，然后这次查询如果没有结果的话，CPU可以去做其他的事情，然后再隔一段时间再查一次，然后再做其他事情，再查一次，再做其他事情。就是查询和程序的执行，可以穿插着进行。

好，那这就是程序查询方式的一个原理好，所以之前我们给出的这个图，这个图表示的应该是独占查询的方式。当CPU启动IO之后，它就一直不停的去问这个IO设备，哎，好了没？好了没？直到它好了之后再进行数据的传送。然后继续往后运行，运行之前的这个程序。但是我们也要知道，程序查询的方式也有定时查询的这种方案，但是定时查询的方案需要保证在数据不丢失的情况下。

每隔一段时间就去查询一次IO状态，那什么叫数据不能丢失？这也通过刚才鼠标的例子，大家应该能有体会。好的，那么这就是第一种IO控制方式，程序查询方式。
