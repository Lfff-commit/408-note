好，我们继续在这个小节当中，我们主要会介绍多重中断，所谓多重中断指的就是当我们在执行某一个中断服务程序的时候，这个中断服务程序的执行。还有可能再被中断。所以又开始了更深层的套娃，好那么为了实现这种多重中断，我们来看一下之前我们提出的这种方案需要进行什么样的改进？之前我们说中断引指令，这儿需要先关中断，然后在最后整个中断服务程序结束之后才开中断。那这种方式就意味着中间整个过程是不可以再被中断的，也就不可能跳转到更深层的中断服务程序，对吧好？

好，因此如果想要实现多重中断，那么当我们的这个中断引指令处理完了，这一系列动作之后。中断服务程序在保存了之前那个呃程序的运行现场，以及保存了屏蔽字之后。需要执行一个开中断指令好，那至于什么是屏蔽字这个我们啊，之后会有学习，这我们先不纠结。好，那现在我们在这儿插入了开中断这条指令之后，是不是就意味着接下来执行中断服务程序的过程当中，有可能再次被中断？因为开中断就意味着CPU会响应新来的这个中断请求信号嘛好，

因此新增了这条开中断指令之后，我们就可以实现多重中断。使得某一个中断服务程序执行的中间过程可以再次被中断好，那现在有这样的两个问题值得注意。第一个问题，我们为什么要把开中断指令？放在保护现场的后面呢。我们中断服务程序，在保存以前的这个主程序的运行现场的时候，这个事情的背后其实也是在执行一系列的指令。也就是要把主程序所对应的各种寄存器的值压入站里，这样的一些指令，那么如果我们把开中断这条指令安排在保护现场。的前边那是不是就意味着主程序的现场，我们可能只保留了一半，

然后又被新的中断程序给中断掉了，那这就有可能导致主程序以前的运行现场丢失。所以我们只有保护完以前那个呃主程序的现场之后才可以打开中断保护现场，这个动作必须是一气呵成的。那恢复现场的动作也是一样的，我们必须保证一气呵成，不能说你恢复到一半，然后又被中断了，对吧？所以在恢复现场之前，我们在这儿插入了一个关中断指令。然后恢复完现场之后才会进行开中断好，所以支持多重中断的系统，相比于单重中断的系统来说啊，区别就是增加了这样的两条指令。

那除了这两条指令之外，我们还可以看到一个东西叫做屏蔽字。那接下来我们就来看一下屏蔽字到底是什么？这个屏蔽字的全称叫做中断屏蔽字，那顾名思义，它是用于屏蔽某些中断的好，它的作用是什么呢？之前我们说过一个系统里边有可能出现各种各样的啊，中断请求信号，比如说此时这个中断服务程序三。它是在处理dma控制器发来的中断请求，那么按照这个多重中断的尿性，是不是意味着当我们在处理dma的中断请求的时候？中间如果再来一个，比如说来自于键盘的。

来自于键盘的中断请求信号，那是不是又得进入更深一级的这个中断处理啊？但是这显然是不科学的，之前我们说过。这些中断请求信号应该是有一个优先级的，我们应该优先搞定这些高速的设备呃，发来的中断信号，然后再来响应低速的设备，发来的一个呃中断信号。所以当CPU在处理某一个中断信号的时候，就需要一个所谓的屏蔽字来指明接下来哪些中断请求应该被屏蔽。应该把护士。那这就是中断屏蔽字的一个作用，本质上其实也是在解决这个啊，中断请求信号的一个优先级的问题。

好了，上一小节当中，我们介绍了这个呃排队器，实现了中断信号的判优，就是会优先响应这个优先级更高的一些中断请求信号。那我们可以在这个排队器的基础上进行一个改造，就可以得到一个呃具备中断屏蔽功能的一个，这样的一个硬件电路。那它们的作用是有区别的，左边这个硬件排队器是收到多个中断请求的时候只响应其中的一个。是固定优先级的。各个中断请求信号之间的优先级顺序是固定不变的，而右边这种增加了屏蔽功能之后。我们就可以更灵活，更动态的来调节呃，

各种中断之间的一个优先级。那每一个中断元都会有个与之相对应的中断屏蔽字，这个中断屏蔽字由很多个比特来组成。中断屏蔽字指明了当CPU在处理这个中断源发来的呃中断请求的时候，应该屏蔽掉其他的哪些中断源的呃中断请求。好，举个实际的例子，大家感受一下，假设我们呃之前最右边这个排队器的最右边是一个键盘的啊，中断请求信号，然后最左边优先级最高的是一个dma控制器的。中断请求信号dma控制器的中断请求信号，它被处理的优先级要大于键盘的中断请求信号。所以基于左边这个排队器当二者同时发出中断请求的时候，CPU 1定是优先响应dma。

控制器。另外，当CPU在处理键盘发来的中断请求的时候，如果紧接着就接收到了dma发来的中断请求，那么键盘所对应的中断处理程序。又会被dma再次的中断。好，那如果想要避免这个问题很简单，我们只需要给键盘这个啊，中断元设置一个与之对应的啊，中断屏蔽字。所有的这些比特位都设为一，当这个比特位设为一的时候，就意味着与这个比特位相对应的中段元。不可以把当前这个中断处理程序给打断。

好，所以一二三这几个中断元，虽然他们的优先级都比键盘的这个中断元优先级更高，但是如果给键盘设置了一个屏蔽字，四个全一的话。那么可以看一下这四个全1 mask啊，头上画了一横，意思就是说先取个反那在这节与非门这儿都会输入一个零。那任何一个输入信号和零相与之后，一定是输出零对吧？那这边输出零再进行一个取反，是不是就一定会输出一个一？好，这个一再进行一个取反，这边是不是就只能输出零了？

所以如果给键盘设置四个权一的中断屏蔽字，那么当CPU在处理键盘的中断请求的时候。键盘的这四个比特中断屏蔽字就可以保证与键盘相对应的中断服务程序，不可以再被中断。那这就是中断屏蔽字的作用。一般来说，如果我们给某一个比特位设置为一，那么就意味着需要屏蔽啊，与这个比特位相对应的，那个中断源发来的中断请求信号。另外，刚才我们说过，每一个中断源都应该对应一个中断屏蔽字，用这样的方式可以很灵活的实现多重中断。那显然，

如果一个中断源，它的优先级要搞得很高很高，那么我们是不是就可以给这个中断源设置更多的一？就像刚才这个例子一样，如果所有的比特位都为一，那是不是就意味着这个中断源的啊？中断处理一定不可能再为中断。那这就可以保证这中断源的呃，处理优先级可以变得很高好，再一点需要注意的是每一个屏蔽字当中至少需要有一个一，也就是至少要屏蔽自身的中断。什么意思呢？如果CPU在处理键盘发过来的中断请求信号的时候，在处理的过程当中，键盘又发来了一个中断请求。

那么，键盘自己是不可以再中断自己的。所以为了保证这个事情，我们至少就需要把与键盘它自己对应的那个屏蔽字设为一。好，那这个硬件电路一般来说不会考，我们来看一下，在考题里面会如何考察中断屏蔽技术，这儿给出了一个条件。说有四个中断元abcd。然后这四个中断源的硬件排队优先次序是a大于b，大于c，大于d，也就是说，如果abcd同时发来中断请求。

那么CPU会优先响应a的，如果说bcd同时发来中断请求，那么CPU会优先响应b的。好，现在题目要求我们把中断处理次序，要把它改造为d大于a，大于c，大于b。那这句话的意思就是说，如果此时CPU正在处理a这个中断源发来的中断信号，那么处理的过程当中。d这个中断源也发来了中断请求信号，那么a所对应的中断处理程序就会被d所中断。好，那这种设计我们就可以用中断屏蔽字来实现，

首先啊，对于中断源a来说，它所对应的屏蔽字是哪些呢？通过题目给出的条件可以知道a它的中断处理程序，只能被d给中断，所以当CPU在处理a的中断信号的时候。只有d的中断信号不可以被屏蔽，所以与d相对应的这个啊比特位把它设为零。然后其余的这三个中断源都被屏蔽掉。接下来b这个中断源，它可以被其他三个中断源再次给中断，所以除了屏蔽b它自身之外。其他三个中断源发来的中断信号都需要被响应，因此只有b它自己对应的这个比特位为一。然后接下来CC是不是类似c会屏蔽b也会屏蔽它自身，

所以与b和c相对应的两个比特是一，然后a和d相对应的比特位设为零就可以。好，最后是dd的优先级最高，所以当CPU为d这个中断源服务的时候，其他几个中断源发过来的中断信号都应该被屏蔽。那除此之外，自己不可以中断自己，所以与d它自己相对应的这个比特位屏蔽字也需要设为一。好，那这就是每个中断源对应的一个屏蔽字。接下来看第二个小问，这个时间轴上给出了每一个中断源，发出中断请求信号的一个时间。然后让我们画出CPU执行程序的轨迹啊，

并且告诉我们说这个每一个中断源的中断服务程序。运行的时间都需要20微秒，好来看一下怎么搞，在五这个时刻b这个中断源。发来了一个中断请求信号，那CPU就会运行与b相关的中断服务程序。那当它运行了五微秒之后，在十这个时刻d也发来了一个中断请求信号。那么，由于b的这个中断屏蔽字并没有屏蔽d。所以d这个中断信号会把b的中断服务程序再给中断，因此接下来会先执行d所对应的中断服务程序。那么d所对应的中断服务程序总共需要运行20毫秒，所以到30这个位置d的中断服务程序就可以完成好，然后就可以再回到刚才的b，

对吧？然后b再往后运行了五微秒之后，在35这个时刻a又发来了一个中断请求信号。那由于a的信号也没有被b给屏蔽，所以接下来又会被a给中断，那同样的接下来需要花20微秒运行，与a相对应的中断服务程序。运行20微秒，那到55这个时刻是不是又可以回到之前的这个b？好，接下来b再运行五微秒到60，这个时刻又有一个中断源发来了中断请求，那由于c它的优先级也比b更高，所以b又会被中断。然后接下来执行与c相对应的中断服务程序。

运行20微秒到80，这个时刻之后又回到b所对应的中断服务程序，那之前b的这个中断服务程序已经运行了五微秒五微秒。五微秒，所以等c完成之后，只需要再运行五微秒就可以凑足20微秒，那b的中断处理就可以结束。那这就是CPU执行这些啊，中断处理程序的一个流程，那大家可以在这里琢磨体会一下好，那上个小节和这个小节我们介绍了中断系统。首先，我们要理解中断在计算机里边有什么作用。其次，更重要的是，

我们要搞清楚CPU响应中断请求信号的一个工作流程。那对于中断请求的分类，大家可以参考操作系统的啊，第四还是第五个小节，在机组这门课里边，我们更多的是要理解这些硬件部件在逻辑上需要做一些什么样的事情。来进行中断的处理，特别是中断引指令，需要做一些什么事以及中断服务程序，需要做一些什么事。这些大家再结合课件来巩固一下，那需要注意中断向量和向量地址的区别，中断向量指向了。中断服务程序的入口地址，而向量地址又指向了这个中断向量的地址，

相当于函数的指针和指针的指针。好，另外这个小节当中，我们介绍了什么是单重中断，什么是多重中断，为了让支持多重中断的系统，对于中断信号的这个优先级管理更加的灵活。我们又介绍了中断屏蔽技术，可以给每个中断源配套一个呃相对应的屏蔽字。用屏蔽字来控制当前这个中断源的中断处理，是否可以再被其他的中断源给中断？好，那这个地方第一次学的同学肯定会觉得比较绕，还需要大家慢下来，结合课本课件来细细的琢磨。

另外，我们也需要掌握刚才那个例题，如果给你呃各个中断源的中断屏蔽字，那么大家要能够画出啊程序的执行轨迹。好，那么以上就这一小节的全部内容，那最后这个部分啊，想听的同学简单听一下，不想听的可以直接跳下一节就可以了，这我们给大家一个实际的例子。让大家看一下，就是CPU是如何检测这个中断信号的，那这是八零八六这块CPU的一个引脚图。首先上一小节，我们说过八零八六这个CPU内部，

它的程序状态自寄存器里边会有一个标志位。if这个标志位为零还是为一，表示当前CPU是否开中断还是关中断？当if的值等于一的时候，表示此时CPU可以接收CPU可以响应啊，中断请求信号，那中断请求信号会通过int。通过这个引脚发过来，这个引脚会接上控制总线当中的某一根信息线好，所以当CPU处于开中段的时候。如果他检测到这根引脚有一个呃高电平的电信号，那么他就知道此时我需要处理一个中断请求信号。但是如果此时if这一位等于0 CPU处于关中段的话，那即便这个引脚输入了一个高电平信号CPU，也暂时不会响应。

对了，当CPU响应一个中断请求信号的时候，会通过这根线给中断控制器返回一个响应的信号。好，最后nmi这个引脚是用于输入非屏蔽终端信号的，就像我们之前说的什么断电啊，还有其他一些更紧急的，必须处理的终端信号。那对于这个引脚输入的电信号，无论if的值等于零还是等于1 CPU，一定都会响应。好，那这是八零八六的三个与中断相关的引脚，那我们刚才提到过一个东西叫做中断控制器。大家也可以自己去上网搜一下呃，

和八零八六CPU配套的一个终端控制器，这块芯片的编号叫八二五九a。可以看到，在这块芯片这最右边这块，它有一个中断请求寄存器英文缩写irr。那这个中断请求寄存器就是我们刚才给出的这个东西，对吧？每一个比特位会对应一个具体的IO设备或者IO接口。好，那这就意味着我们可以给每一根线连上一个特定的IO设备或者IO接口。然后不同的IO接口都会通过一根线。给这个中断控制器发来中断请求的一个信号，当某一个IO设备或者IO接口发来中断信号的时候。中断请求寄存器里边相应的这个二进制比特位是不是就会变为一，而如果有多个l接口同时发来中断请求信号，

那么就会出现多个二进制比特一。好，那无论收到多少个IO请求信号，这个中断控制器都会通过内部总线。给CPU，给CPU这个八零八六CPU发送中断请求的信号。也就是这个输出端，这个输出端会接上八零八六CPU的in NTR，接上这个地方。用来通知CPU，你现在需要处理一个中断信号了，好，那现在如果CPU没有关中断的话，它就会通过这根引脚。这根引脚。

给中断控制器发回一个反馈信号。就是给他一个反馈，我知道了，要处理中断，但是现在你得告诉我，我应该处理的是哪一个中断？好，那么回到中断控制器经过这个啊，中断判优的电路的处理之后，会有一个优先级最高的。优先级最高的呃中断请求信号被选中，然后根据这个中断请求信号，再经过这个所谓呃中断服务寄存器的处理。就可以给CPU指明当前被选中的这个呃中断信号，它的类型号是多少？

所以这个地方最左边的这块电路。指的就是我们之前提到的这个东西，中断向量地址的形成部件。反正这个部件可以形成中断类型号，那么这个中断类型号会经过内部总线的啊数据总线。经过数据总线。给CPU的这些数据的输入端啊，这些输入端就是会告诉他这个中断的类型号。然后接下来CPU就可以根据这个中断类型号去找到啊，中断服务程序的一个入口地址。对了，刚才大家没有听错，中断类型号是通过数据线传送给CPU的。好，最后这地方有一个中断屏蔽寄存器，

里边存储的就是各个中断源所对应的中断屏蔽字。那它的作用就是用于决定到底要屏蔽哪些终端信号。好，那这就是一个很实际的例子，大家可以结合这个例子与我们啊，这两节学习的东西进行一个类比，虽然这些东西都不考。但是这些实例可以帮助大家更好的联系实际，更好的理解，那在理解了之后也可以更方便记忆。好的，那以上就是这个小节的全部内容。
