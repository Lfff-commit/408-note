好的，那这个视频中我们会学习指令系统这一章的最后一个小节的内容cys k和ry sk，其中会涉及到一些我们在第五章才会学习的知识。所以在这个视频中，我们只会对cisc和risc进行一个简要的了解，那通过之前的学习，我们已经知道了在一个指令系统当中指令的格式，我们应该怎么设计？另外，我们介绍了指令寻址和数据寻址这样的两个内容，指令寻址探讨的是如何找到下一条应该执行的指令地址。而数据寻址探讨的是如何来解释这个指令，里边所包含的形式，地址。那通过前边的学习，

我们可以感受到指令系统的设计其实是很灵活的千变万化。那这一小节中，我们要学习的cisc和risc是指令系统的两种设计方向。那所谓cisc指的是复杂指令集的计算机系统。而risk指的是精简指令集的计算机系统。顾名思义sy sk复杂指令系统，它的一个设计思想就是我们设计的这个指令系统当中会包含很多很多条指令，然后每一条指令可以完成一个比较复杂的基本功能。那典型的代表就是叉八六架构，那这种复杂指令集的设计方式通常会用于笔记本台式机，或者是一些商业服务器这些地方。因为这些地方可能对功能的复杂性要求会更高一些，好再来看risk精简指令集，它的设计思想就是只会提供一些最基本的呃指令。那如果我们想要完成一个复杂的功能，

我们可以用多条简单指令的组合来完成。那risk的代表就是arm架构，现在大家使用的几乎所有的智能手机，还有平板电脑的CPU处理器都是使用arm架构，也就是使用了精简指令集的一个设计思想。那大家可以这么理解risk和cisc相比，risc这种设计思想就相当于给你提供了一个C语言。但是你只能使用这个c语言里边的最基本的那些语法，什么if else定义函数定义变量啊之类之类的就是最基础的那些语法。而sy sc有点类似于它，不仅给你提供了呃，最基本的C语言，同时还给你提供了一堆。一堆你用得上用不上的库函数。那显然要给你提供一堆复杂的指令，

一堆复杂的功能，那么这种指令系统，它所对应的电路是不是设计起来也会复杂的多？刚开始，英特尔公司以为指令集设计的越复杂，功能越强大，会越好。CPU的效率也会越高，但是后来随着这个指令集越来越复杂。有人就发现了80，20规律，就是说在大多数程序当中，可能80%的语句。仅会使用到最基本的20%的指令，这点是不是也很好理解，

就像大家用C语言编程，其实你会用到的语句也就是什么分支if else。循环for while还有什么加减乘除就是最基本的，这些语句是你使用的频率最高的。而C语言里边包含的各种各样花里胡哨的库函数，可能绝大多数你都用不到，对吧？并且即便C语言不给你提供库函数，那么你用C语言的基本语法自己来定义这些函数，自己来实现这些函数是不是也是OK？可以吧，好，所以大家可以结合我们写程序的经验来类，比着理解对于指令的使用都是类似的原理。在复杂指令集系统当中，

有很多花里胡哨的这种指令，我们可能使用的频率非常非常低。并且这些功能复杂的指令其实可以用多条简单的指令组合而成，但是硬件和软件不一样的是软件的函数，我们写一些代码就OK了，但是硬件的这些复杂的指令。我们需要设计更复杂，更复杂的这种硬件电路来实现，这可能会导致成本迅速上升好。现在我们站在硬件角度来举个例子。比如说现在我们要设计一套呃，一套指令集，这套指令集需要实现整数的加法，减法，乘法操作。

还有矩阵的加减乘操作，那如果采用cisc的设计，思想就相当于我们会设计呃，实现整数加减乘的指令。同时，还会提供直接实现矩阵的加法，减法，乘法的指令。这是cisc的设计思路，那每一条指令都可以用一个专门的电路来完成，也就是说对于刚才这个例子，我们需要设置呃三个电路来实现整数的加减乘。另外，还需要设置三个更复杂的电路来实现矩阵的加法，减法和矩阵的乘法指令，

总共设计六个电路。但是有的电路也许设计起来会非常非常复杂，比如说矩阵的乘法，用电路设计也许就会比较困难。那由于这种很复杂的指令，很难用硬件电路直接去完成，直接去实现，因此随着cisc里面的指令越来越复杂，人们逐渐采用了存储程序的设计思想。那存储程序这个概念大家都知道，就是我们可以给定一串的基本的指令，把它提前存储在某个地方。比如说对于刚才这个例子来讲，我们可以设置五个比较通用的电路，就是整数的加减乘，

还有矩阵的加法，矩阵的减法。这样的五个通用的电路，这五个电路是比较好实现的，那由于矩阵的乘法更难实现一些，所以我们可以把矩阵的乘法的实现。把它拆解为由，这五个基本的指令来完成的一个操作，那这就是所谓微程序的一个概念。CPU对外提供的某些复杂的指令功能，在内部是用某些更基本的，简单的功能组合来实现了这个复杂的指令。好，这是cisc的设计思路，可以提供很多呃功能丰富的指令，

但是这些指令的设计实现也许会很复杂。对于一些特别复杂的指令，没办法用硬件直接来实现，那么我们就会采用v程序来实现它好，那现在对于刚才这个场景，如果我们采用risk的设计思想。那我们就只会提供整数的加法，减法，乘法这几个指令，因为两个矩阵的加法，减法，乘法这些操作。都可以使用矩阵元素的加减乘的一个组合来实现，对吧？所以这就是risk的设计方式，

每一条指令会对应一个电路，但是由于risk里边。只提供最基本的，最简单的那些指令，因此这些指令的电路设计也会相对简单，同时功耗也会更低。那也正是因为这种精简指令集的CPU功耗更低，所以它才会被广泛的应用在我们的移动设备上。好，另一个方面risk里边由于这些指令都很简单，都是一些基本的指令，因此所有的这些指令执行时间都差不多。那这个特性可以很方便的，让我们实现并行技术和流水线技术，那这两个术语的具体含义，

我们会在下一章当中进行学习，这样大家先有个印象就可以。好，那我们再对两种设计思路造成的一些特性差异进行一个对比，采用cisc这种设计思想啊，设计出来的指令系统通常是复杂且庞大的。指令的条目会很多，一般会大于200条的指令，指令功能很丰富，而如果采用risk设计思想得到的这个指令系统通常是简单精简的。其中所包含的指令条数一般会小于100条，只会保留我们最常用的那些基本的指令。一些复杂的功能可以用简单指令的组合来完成好，那由于sy sc当中指令的功能很丰富，因此各指令的指令字长是不固定的。

有可能会有很短的指令，有可能会有很长的指令，而risk当中由于各个指令的功能都比较单一，因此可以比较方便的设计出定长指令字结构。那之前我们说过一个指令系统当中，如果每一条指令的长度都是相等的，那么对指令的解析这些速度也会也可以得到提升，对吧？好，再一个方面cisc当中有简单的指令，有复杂的指令，那不同的指令，它们所需要的执行时间相差是比较大的。另外，各种指令的使用频度也相差会很大，

就是之前我们提到的80，20那个规律。而risk当中，由于所有的指令都比较简单，所以大多数指令都可以在一个周期内就可以完成，并且所有的指令都是比较常用的。好，另一个方面，对于seek指令系统当中，我们通常不会限制哪些指令可以缓存，哪些指令不可以缓存。但是在risk当中，我们规定只可以有这样的两条指令，可以仿存load，就是读某个主存单元，

读到某一个寄存器当中，然后store就是。从某一个寄存器当中往主存里边写一个数据。只能用这样的两条指令来仿存，那讲到这一点，我们可以回顾一下之前提到过很多很多次的这个例子。这儿的第二句代码是一个乘法指令，是指明了呃b这个数的一个存放地址，是存在这个地方执行这条乘法指令的时候会直接去。去进行一次仿存操作，直接从主存当中读出这一串数据。然后再把b这个变量和a相乘得到a×b的结果好，所以由于这个地方的乘罚指令，这条指令可以直接访问主存，因此我们之前举例的这个呃指令系统。

它一定是cisc复杂指令系统，因为刚才我们说过，如果是risc精简指令系统，那么我们只能用load或者store这两个指令去访问主存。而不可能说一条乘法指令也会访问主存，所以之前我们举例的这个指令系统，它一定是一个cisc的指令系统。好，那也正是由于这个特性cisc当中只需要有比较少的寄存器，而risc当中需要有比较多的寄存器，这点可从刚才的乘法指令，也可以体会到。cisc里边的乘法指令会直接把那个需要相乘的数取到某一个寄存器，然后紧接着就完成乘法操作，不会过多的占用寄存器。

而对于risk来说，无论我们要实现的是加法，乘法，除法，任何一种运算一定都需要我们用一条指令，先把数据从主存读入到某一个寄存器。然后再用一条乘法指令，对两个寄存器的值进行相乘，这种操作因此可以看到，如果采用risk，那么我们作为程序员，也许会长期的占用很多很多个寄存器。因此risk这种设计方式一定要准备足够多的寄存器。好，再看下一个特点，

对于cisc指令系统来说，通常我们难以用优化编译的方式生成高效的目标代码程序，而risc我们可以采用优化编译程序。生成比较高效的代码啊，这个怎么理解呢？还是可以结合C语言来进行理解，之前我们说过cisc有点类似于给你提供了基础的C语言。基础语法你都可以使用，同时也给你提供了很多你用得上用不上的库函数，你当然可以通过一句代码调用一个库函数就来完成某一个复杂的功能。但是如果这个库函数实现的不是特别好，它的执行效率不是很高，那你可以修改库函数吗？是不行的，对吧？

而对于risk来说，有点类似于我们只使用这个C语言的最基础的那些语法，当我们要实现某一个复杂功能的时候，我们不可以直接调用某个库函数。但是由于我们使用的是C语言，最基本的语法，因此我们完全可以自己来优化我们的程序代码。用合理的方式优化我们的代码，就可以让我们的代码执行起来更高效，所以这个地方提到的这两个特性的区别，其实原理都是和C语言类似的。好，这是关于cisc和risc目标代码的一个区别，那还有一个区别就是控制方式。cisc绝大多数情况下都会采用v程序控制。

这一点我们会在第五章进行进一步的探讨，这有个印象就可以，而risk我们可以采用组合逻辑控制。现在大家只需要知道这种控制方式效率更高，而左边这种控制方式效率会更低，只需要知道这个就可以。好，最后一点，由于cisc当中各种指令的执行时间相差比较大，所以指令流水线的实现会比较困难，但是也有办法实现。就实现起来麻烦一些而risk当中，由于所有的指令几乎都可以在一个周期内完成，因此可以很方便的实现指令流水线。而实现指令流水线之后，

可以让CPU整体的效率得到一个质的飞跃，因此risk当中指令流水线都是必须实现的。好，那这我们留下了几个空白的知识点，什么是指令流水线，什么叫控制方式，什么叫微程序控制，什么叫组合逻辑控制？这些术语背后有什么含义？那这就是我们下一章需要探讨的问题。好的，那么以上就是第四章的全部内容。
