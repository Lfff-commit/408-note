好，继续看函数调用的机器级，表示通过之前的学习，我们知道函数调用需要用到两条很关键的指令，分别是call和return。在函数调用发生的时候啊，每一层函数在这个函数调用栈里边都会有一个与之对应的栈帧，而栈帧里边保存了和这层函数相关的各种各样的信息。并且我们已经知道，在函数调用发生的时候，以及函数调用返回的时候，如何去切换战争，以及我们如何去访问各个战争里边的数据。那在这个视频中，我们要回答剩下的两个关键问题。

首先，每一个函数战争里边，它到底包含了哪些内容呢？第二，我们如何通过这个函数的栈帧去传递调用参数以及函数调用返回的时候，返回值是怎么带回给上一层函数的？好，首先探讨第一个问题，在一个函数的战争内部，可能会包含哪些内容呢？我们以color这个函数为例。通过之前的学习，我们知道每一个函数战争底部的这四个字节一定是保存了上一层战争的基地址。对于color函数来说，它的栈帧底部需要保存p这个函数的栈帧基地址好，

除此之外，我们还可以确定。任何一个函数在调用下一层函数的时候，一定会使用到call这个指令，而call指令必然会把IP寄存器的值压到站顶。去保存，所以任何一个战争的顶部一定是保存了函数的返回地址，也就是IP寄存器的旧址。好，接下来看一下color函数的这个代码。它里边定义了三个局部变量，分别叫temp 1，temp 2和sum。这三个局部变量分别在这几个位置，对于任何一个函数来说，

在它的内部定义的这些局部变量。都会被集中的存储在呃，这个函数的栈帧靠近底部的这一片区域。另外，注意观察在color这个函数里边，三个局部变量呃，在代码当中出现的顺序先是temp 1，然后是temp 2，最后是sum。而在这个战争底部，如果我们从上往下看的话，最先出现的是sum，然后是temp 2，接下来才是temp 1。C语言里边越靠前定义的这个局部变量会越靠近站顶好，

那这是用于存储局部变量的区域。会靠近战争的底部。那我们不妨思考一下，站在汇编语言的视角，如何去访问函数内部定义的这几个局部变量呢？很简单，对吧？我们只需要基于这个函数的ebp指针减四，就可以找到呃，第一个局部变量减八，就可以找到第二个局部变量。减12可以找到第三个局部变量，因此以后大家阅读汇编语言代码的时候，如果你看到呃汇编语言当中去访问了ebp- 4。或者去访问了ebp- 8，

那么你要知道这里边存储的肯定是函数内部定义的某一个局部变量。如果结合C语言来分析的话ebp- 4这个位置肯定是存储了C语言当中最后一个出现的变量。而ebp- 8这个位置呃，它对应的应该是C语言当中倒数第二个被定义的局部变量。好，现在我们知道了战争的底部，这片区域是用于存储局部变量的。接下来，在战争的顶部，这片区域会用于存储函数调用的参数啊，可以看到color函数在调用add函数的时候。需要给它传两个参数，分别是x和y，那这两个参数在栈帧内部是存储在这个地方。靠近战争的顶部，

同时在参数列表当中越先出现的参数越靠近顶部的位置。好基于这样的特点，我们不妨思考一下，如果现在正在执行的是add函数，那么ebp指针是指向了add函数的，这个占底的位置对吧？那么，在add函数当中，如何去访问呃上一层函数给它传过来的两个参数呢？是不是很简单，我们只需要用ebp+8 ebp+12就可以分别找到x？和y这两个参数。所以当你以后阅读汇编语言代码的时候，如果看到ebp+8这个地址，那你就要知道。

这个地址所对应的一定是啊，当前执行的这个函数，它的参数列表当中的第一个参数。而ebp+12对应的是当前执行的这个函数，它参数列表当中的第二个参数。好的，那这是函数调用参数呃所存储的位置，那最后我们发现color函数的栈帧，它中间还有四个字节是我们没有使用的空闲区域。这是正常现象，因为当我们使用GCC编译器把C语言编译为汇编语言的时候。为了追求数据对齐GCC编译器会规定每一个战争的大小应该是16字节的整数倍。像color函数，它的栈帧大小就是32个字节，也就是16字节的两倍。

那p函数和main函数肯定也是一样的呃，任何一个函数的战争大小都是16字节的整数倍。只有当前正在运行的这个函数，不受刚才说的条件限制，也就是说，如果现在正在运行的是add函数，那么它的栈帧可以是四字节，可以是八字节。都可以，但是只要这个函数需要再调用下一层函数，那么这个函数的栈帧一定需要给它凑齐到16字节的整数倍。好，那现在我们知道为什么会有这片空闲区域了，相当于一个用不完的零头。对了，

刚才说到这个GCC编译器，有兴趣的同学可以自己下去查一下。GCC编译器是linux世界里边最常用的一个编译器，在程序员的世界里边，GCC编译器非常流行。那由于这个编译器是开源的，而且任何人都可以自由使用，所以绝大多数的教材在讲叉八六汇编语言的时候，通常都是用GCC编译器把C语言。翻译成汇编语言好，那有兴趣的同学可以自己下去了解一下。好的，我们对刚才的内容进行总结，一个战争的内部可能会包含这样的一些呃区域，首先底部的四字节是上一层战争的激励者。

接下来，靠近底部的这片区域会用于存储局部变量。而靠近战争顶部的这片区域，会用于存储函数调用的参数，那显然对于任何一个函数战争来说。底部的这四个字节一定是存在的，一定要存储上一层函数的战争机制，不然你就没法恢复上一层函数的战争了，对吧？接下来，存储局部变量的这片区域并不是每一个函数栈帧都有，因为有的函数它内部可能不定义局部变量。有这样的可能性，对吧？好，

那再来看战争顶部的这个东西，可以肯定的是，只要我们发生了函数调用，那么顶部的这四个字节一定是用于存储IP寄存器的旧值的。这是返回地址，必须存储，而上面的这片区域就不一定存在，因为有的函数调用可以不传参数。好，最后中间可能会有一片区域是没有被使用到的，不过这片区域也不一定存在，因为如果其他这些数据刚好可以凑足16字节的整数倍。那么，中间这个零头就不会存在。那这就是一个战争的内部，

可能包含的内容，那在这个视频的开始，我们提过一个问题，函数调用的时候如何传递调用参数呢？现在答案已经很明显了，在靠指令发起函数调用之前，我们需要把调用的参数。写入到当前这个函数的战争顶部，这片区域一定是这样的，对吧？好，那接下来我们分析一下caller和add两个函数的汇编语言代码。来验证一下，刚才我们学习的东西好，首先在color函数之前有一个函数叫做pp函数，

调用了color函数。现在假设p函数的栈帧是这样的一个范围，那么当p函数调用color函数的时候，呃color函数的前两句汇编语言指令。会切换战争对吧？首先要把p函数的战争基地址给push压栈保存。然后再让ebp寄存器指向color函数的栈帧底部，那执行的效果就是这个样子。好，接下来这条指令会让esp- 24，也就是变成这样。ebp和esp指明了当前执行的color函数，它的栈帧范围。相当于画了一块自己的地盘，对吧？

好，接下来看一下这个C语言代码color函数，它定义了三个局部变量，其中temp 1和temp 2是有初始值的。好，那来看一下接下来这一句指令，会把125写到ebp- 12这个位置，那ebp- 12应该是在这个地方，对吧？那这句指令的执行就是完成了temp 1变量的初始化，它的初值是125。再往后一句把80写到ebp- 8，那ebp- 8对应这个位置，因此下面这条指令就是完成了temp 2=80这个操作。好，

另外我们还有一个局部变量叫sum，但是这个局部变量它没有初始值，接下来需要调用add函数，把add函数的返回值赋给sum变量，对吧？那通过之前学习，我们知道sum变量，它对应的存储位置应该是ebp- 4，这个地方只不过它的值是没有被初始化的。好，可以看到刚才的这两步操作完成了局部变量的初始化工作。站在战争的视角，就是要往战争底部的这片区域写入相应的数据。好，继续往后，

接下来需要调用add函数，同时需要给add函数传递两个参数temp 1 temp 2。在靠指令发起调用之前，显然我们需要往战争顶部的这片区域写入两个参数。那来看接下来这句指令，把ebp- 8的值写到eax寄存器当中，那ebp- 8对应的是temp 2对吧？所以就是要把80这个数据先给写到eax寄存器当中。接下来再把eax里的值写到esp+4的位置，那esp+4应该是这个地方。所以这条指令的执行效果就是把80写到这个位置，也就是传递了y这个参数。这地方大家可能会奇怪，为什么要先把80写到EX当中，再从EX寄存器写回内存呢？

难道我们不能用一条move指令就把数据直接从ebp- 8复制到esp+4这吗？好，这是不可以的，大家可以回去复习一下，我们讲move指令的时候强调过move指令，不支持两个操作数都同时来自于主存。所以我们只能先从主存复制到寄存器，再从寄存器复制回主存好，接下来这条指令把ebp- 12的值复制到eax当中。125复制过来，然后把EX的值复制到esp所指的那个主存地址。那执行的效果就是这样子。接下来，就可以通过call指令发起函数调用，那我们知道call指令会把呃IP的旧值压到站顶。

同时，会让程序的执行流转移到add函数的第一条指令，那add函数前两条指令，它也是要切换战争，对吧？push指令会把color函数的战争基地址。给保存下来，然后move指令会让ebp指针指向当前函数的这个基地址。好来看一下add函数做了什么？我们会使用x和y这两个参数，对它们执行加法，并且把相加的结果返回给上一层函数。那么，在add函数内部如何去访问x和y这两个参数呢？这个问题我们探讨过，

通过当前这层函数的基地址ebp+8可以访问到第一个参数。加12可以访问到第二个参数。所以来看后一句指令，从ebp+12把值复制到eax当中，也就是把80复制到eax寄存器当中。接下来ebp+8复制到edx寄存器当中，所以执行的效果就是这样的。好再往后，这条指令是一个加法，指令eax和edx的值相加，并且把结果写回eax。那80+125=205，所以执行的效果就是这样的。好，最后执行live指令切换回上一层函数的战争，

然后再执行return指令，让程序的执行流回到上一层函数。可以看到，刚才add函数把返回值写在了eax寄存器当中，那对于color函数来说，它要把add函数的返回值复制到。sum变量这儿对吧？所以来看这条指令，把eax的值复制到ebp- 4，那刚好ebp- 4对应的就是sum这个变量。那执行的效果就是这样的。好，接下来color函数需要把sum的值返回给再上一层函数，对吧？来看一下他是怎么做的，

下面这条指令把ebp- 4也就是sum的值复制到了eax寄存器当中。所以color函数给上一层函数返回值的时候，同样也是通过eax寄存器来返回这个结果的。我们知道，在C语言里边，一个函数可能会有多个参数，但是它的返回值永远只会有一个。我们没有见过同时返回两个值这种写法，对吧？好，所以函数调用的时候多个参数可以通过函数调用栈来传递。而函数调用返回的时候，由于只需要有一个返回值，因此通常我们都是把返回值写到eax寄存器当中。这样当我们返回上一层函数的时候，

它只需要去eax寄存器当中去取，这个值是不是就可以获得函数的返回结果了？好的，那我们再对之前的总结进行一个拓展函数调用的机器级表示，首先是有两个关键的指令，分别是call和return。这两条指令会改变程序的执行流，那通过这个小节的学习，我们知道在执行call指令之前，我们需要把调用的参数。写入到当前函数的战争顶部那片区域，那把参数压入栈顶的动作可以用push指令或者move指令来实现。接下来call指令会把返回地址压栈保存，并且跳转到被调用函数的第一条指令。首先需要进行战争的切换，

接下来如果这个函数里边定义了一些局部变量，那么我们通常会把呃ebp- 4 ebp- 8 ebp- 12这些位置的值给进行初始化。这些位置存储的就是当前函数的局部变量好，接下来经过一系列的逻辑处理之后，当前这个函数需要给上一层函数返回一个结果，对吧？那我们可以通过EX寄存器来传递这个返回值，那在return返回之后，这个调用者就可以通过EX寄存器去使用。返回值好了，最后我们还要补充一个小细节，思考这样的问题，假设在发生函数调用的时候。这个调用者，它有一些运算结果被存在了，

某一些寄存器当中，比如edx e cx或者eax。那么，当函数调用发生的时候，被调用者这个函数是不是也有可能会使用到这几个寄存器啊？那就有可能把调用者的这些中间结果给覆盖掉，就有可能会导致数据的丢失，对吧？好，那怎么解决这个问题呢？是不是很简单？如果有必要的话，在发起函数调用之前，我把我所需要的这些寄存器值压栈保存，是不是就OK了？

那在函数调用返回之后，我再把这些寄存器的值从栈恢复到寄存器。这样就可以保证寄存器里边的这些数据不会丢失好，因此我们对刚才的这个图进行一个拓展的补充。有的函数栈帧里边可能会有这样的一片区域，用于保存部分的寄存器值。当然，这片区域不一定存在。因为如果这些寄存器的值对我这层函数呃，接下来的运行没有意义，那我就不用保存，对吧？好，所以具体情况具体分析，总之在函数调用之前，

如果某一些呃寄存器的值有必要保存的话，那么就可以把这些寄存器的值压占。然后在函数调用返回之后，再通过栈恢复这些寄存器的值。好，那这就是函数调用的机器级表示。这页PPT可以作为大家阅读汇编语言代码的一个分析框架，你要知道在call指令之前需要做什么，在return指令之前需要做什么。好，那最后我们对函数调用的机器级代码表示进行一个总结，首先是栈帧在函数调用栈里边。一个战争会对应一层函数，那一个战争内部包含哪些内容呢？呃，

最底部肯定是ebp的旧值，接下来可能会存储若干个局部变量。接下来，由于每个战争的大小是16字节的整数倍，因此可能会有一些没有被使用到的碎片。好再往战争顶部走，就可能会有一片区域用于存储部分寄存器的值，因为在发起函数调用之后，呃，有一些寄存器的值可能会被覆盖掉。有数据丢失的风险，所以有的时候函数调用之前需要把部分的寄存器值给保存到战争里边。好，接下来就可以往战争里边压入这个调用参数，最后再使用call指令发起函数调用，

而call指令会把函数调用的返回地址给压到战争的顶部。那需要注意站底的方向是高地址的方向，而站顶的方向是低地址的方向，并且在叉八六计算机当中，通常以四个字节为单位去操作战争。在叉八六CPU内部，有ebp和esp两个寄存器，分别标记了当前执行的这个函数战争的底部四字节和顶部四字节。我们可以使用push和pop指令，结合esp也就是站点的指针去操作，站点的数据push指令就是把数据压入站点。pop指令是把站点的数据给出站。当然，这两条指令会让esp指针啊，自动的减四或者加四，

那除了push和pop指令之外，我们也可以用我们最熟悉的move指令，结合esp。或者结合ebp指针去访问战争里面的某一个数据，那由于一个CPU只有一个ebp和一个esp，因此当函数调用发生的时候。我们需要改变ebp和esp的值，从而完成战争的切换，那在每个函数的开头一定需要例行的执行enter指令。以及在每个函数return返回之前，一定需要例行执行live指令，这两条指令可以完成战争的切换，当然我们说过可以用push和move指令去等价的实现，这两条指令的功能。我们发起函数调用，

需要使用call指令，而函数调用的返回需要使用return指令，那在函数调用之前，我们需要先传递参数。传递参数的方法就是在call指令之前，把若干个参数写入到当前战争的顶部区域。参数入栈之后，才可以执行call指令。执行call指令首先会把IP寄存器的旧值压栈保存，也就是要保存函数的返回地址。接下来，靠指令还会把IP寄存器指向被调用函数的第一条指令，也就是设置一个新值。那第二个效果相当于执行了一个无条件转移指令，那这是函数调用的时候需要做的事情，

首先把参数压栈，接下来执行call指令。接下来，如果一个函数执行结束，就需要用return指令返回上一层函数，那return指令的效果就是从战争的顶部找到IP旧值，然后恢复IP寄存器的值。最后还有一个问题值得我们注意，如何去访问战争内部的数据呢？特别是局部变量和上一层函数传过来的参数如何去访问呢？由于局部变量会集中保存在呃靠近战争底部的那片区域。因此，可以通过ebp- 4 ebp- 8 ebp- 12，通过这几个地址分别找到一个函数内部定义的局部变量。而调用参数会保存在上一层函数的战争顶部的位置，

所以我们可以从当前这一层函数的。基地址往上偏移八个单位或者往上偏移12个单位就可以找到第一个参数，第二个参数。好的，那以上就是这个部分的全部内容。
