好的，在这个视频中，我们会学习一些常用的叉八六汇编指令，在之前的学习中，我们知道呃，一条指令由操作码和地址码组成，其中地址码指明了这条指令，它要处理的。数据存放在什么地方？要么就在寄存器里，要么就在主存里，要么有可能直接被包含在指令里。以常量的方式给出。那这一小节我们要探讨的是前面这个问题，就是呃，

常见的一些指令有哪些？每一条指令是？是对数据进行什么样的处理？要探讨的是这个问题，那我们会分为这样的三大类指令来进行介绍。首先是算术运算类的指令。也就实现加减，乘除等等，这些大家很熟悉的算术运算，然后逻辑运算实现与或非啊，这些逻辑运算相关的指令。以及其他一些指令好，首先来看算术运算相关的一些指令加法对应的汇编预言指令是这个样子。减法对应的指令是，这个样子好，

那你会发现在这一页PPT当中，有的指令它的操作数有两个，我们分别用字母d和字母s来代替。那这两个操作数d和s呃，有可能来自于什么地方呢？是不是还是我们上节课说的这三个位置啊？操作数d和操作数s有可能分别来自于寄存器或者来自于主存或者是直接包含在指令里边的一个常量。好，所以这个地方给出的d和s，你用上个小节当中介绍的那种方式去替换就行了。那大家已经知道怎么去表示这个操作数来自寄存器还是主存，还是一个常量好，那现在有必要给大家解释一下为什么我这一页PPT里用d和s两个字母来分别表示两个操作数呢？d指的是目的操作数。destination取它的第一个字母。

s表示，原操作数source。取第一个字母好，现在我们重点关注d字母，它所代表的这个目的操作数。那一条指令对d和s两个操作数进行相应的处理之后。最终运算的结果会被放回d所指的那个位置，所以为什么要把它称为目的操作数？destination呢，因为我们最终处理完的结果是要放回到d所指的那个位置好，那这意味着什么呢？d字母所代表的这个目的操作数，它只有可能呃是来自于寄存器或者来自于主存，对吧？它不可能是一个常量，

只有寄存器或者某一个主存地址才有可能去存放我们的结果，对吧？好，所以在我的这个PPT当中，大家看到这个d字母所表示的目的操作数，你就要知道它不可以是常量，只可能是寄存器，或者是一个主存的地址。而s所代表的原操作数，你就可以随便取了，可以来自于寄存器，可以来自于主存，也可以是一个常量。好，那接下来教大家怎么记忆这些汇编语言指令？

那汇编语言指令其实是来自于英文。你像这个减法指令sub，其实它是取了减法substract它的前面三个字母，对吧？然后这儿呃乘法指令mul它是取了multiply的前三个字母，对吧？好，然后除法div，它是取了divide的前三个字母，那相应的取复数啊，什么自增自减都是取了英文的前三个字母。所以只要你能记住英文单词，那你肯定就能呃，记住每一条汇编指令它实现的是什么样一个功能？好，

接下来我们把每一条指令的功能啊，进行一个解释，加法指令是对d和s。进行相加，然后结果存回d所指的地方。减法指令是对d和s进行相减，结果存回d好。接下来，乘法指令你会发现有两个，对吧？其中一个，它前边带了一个I。那这儿的I你把它理解为integer，也就是带符号的整数，我们定义的int型变量就是带符号的整数，

对吧？好，所以呃，两条这个乘法指令上面没带I的，这个指的是两个无符号的整数进行相乘d和s进行相乘乘积会存入到d所指的位置。而前边带I的这个，这条指令是用于实现两个有符号整数的相乘d和s相乘乘积存入d。好，那同样的道理呃，这儿也有两条触发指令，一个前边没有I，另一个前边有I，那前边有I的这条指令。就意味着它是进行有符号整数的除法。好，

那值得注意的是，你会发现两条除法指令，它只有一个操作数s，那除法做的是被除数除以除数，对吧？那在这个除法指令里边，所指明的s它是除数而被除数，会被提前放到。edx和eax两个寄存器当中，所以这个除法指令的被除数其实是采用了隐含寻址，对吧？就是这条指令会默认被除数，现在已经放到了edx和eax两个寄存器当中。好，现在解释一下edx冒号eax啊，

这个写法是什么意思？在进行除法运算之前，需要把被除数进行位扩展，比如32位的呃被除数除以32位的。除数那么需要把这个被除数扩展为64位，用64位的被除数除以32位的除数。那要存放64位的被除数，是不是就需要两个寄存器？我们说每一个以e开头的寄存器都是32位，对吧？所以要存放64位的这个呃被除数，我们是不是就得用两个寄存器把它连起来，用更高的32位存放在edx？更低的32位存放在eax。好，

那么这个除法的运算结果除得的商会存放在eax寄存器当中，除法的余数会存放在edx寄存器当中。好，那这是除法运算相关的指令，虽然指令当中只给了一个操作数s，但是你要知道这个s它指的是除数被除数会被提前安排到edx和eax两个寄存器当中。拓展为64位再进行除法运算。好，接下来取负数啊，这个指令很简单，就是把d所指向的这个数取负，比如说正时变成负时。正的九九六变成负的九九六，就这个意思。那取负的结果同样存回d所指的这个位置。

好最后自增和自减，也就是我们熟悉的加加和减减，分别是increase和decrease。这两条指令做的事情就是把d这个数进行加加或者进行减减，然后运算的结果存回d所指的这个位置。好的，那这就是常见的一些算术运算指令，重点还是要背单词啊，记住英文单词，你就能记住每一条指令，它大致是完成了什么样的一个功能？那这儿需要跟大家解释一下，这个王道书里边的呃，这种写法哈呃，你会发现王道书里介绍一条指令，

比如说加法指令和减法指令的时候。他的这种写法我不知道大家能不能看明白，大家可以自己先琢磨一下。好，那解释一下，它这个尖括号里边的这几个字母，它其实是英文缩写哈寄存器的英文是register。所以reg打个尖括号表示的是呃，这个操作数它可以是一个寄存器里边的操作数。那m就是memory内存的呃，一个缩写，然后CON是constant常数的一个缩写。好，所以现在你再回来看王道书里的这种写法呃AD指令有两个操作数嗯，两个操作数regreg也就是说两个操作数可以都是呃，

来自于这个寄存器对吧？两个操作数进行相加，然后结果放回第一个寄存器当中就是这个意思。那第二行他想表达的是这个加法指令的呃，两个操作数可以分别来自于寄存器和主存。然后相加之后的结果放回前边这个寄存器所指的位置当中，好那类似的最后这一行，它想表达的就是这个加法指令它的两个操作数。呃，第一个可以来自于内存，第二个操作数可以是一个常量，两个数相加之后的结果放回到逗号前面所指的这个内存地址当中就是这样一个意思。好所以王道书里边的这种表示方法，就是要告诉你啊，这两个操作数左边和右边的两个操作数可以分别来自于什么地方，

他是想说明这样的一个问题。那这是不是跟我们之前的这种总结add一个是d，一个是s，其实一个意思对吧？刚才给大家强调过这个左边这个目的操作数，它不可以是一个常量，那你看一下左边你有看到。zo n.这样的缩写吗？没有吧，就说明左边这个数，它不可以是一个常量，但右边这个数，它可以是一个常量，对吧？

那对于减法指令也是一样的，左边这个操作数，它肯定是要么寄存器，要么内存，不可能是一个常量，就这意思。好，另外一点。值得一提的是，在叉八六汇编语言当中，不允许两个操作数都同时来自于主存，所以你会看到，不管是AD指令还是SUP指令。两个操作数，要么同时来自于寄存器，

要么一个在寄存器，一个在主存，就不允许出现两个操作数，都同时在主存里，这种情况是不被允许的。那这么规定的目的主要还是为了保证每一条指令呃，它不要访问太多次主存，因为访问主存的次数越多，这条指令的执行速度肯定也会越慢，对吧？所以叉八六汇编语言当中不允许两个操作数都同时来自于主存好，那给大家解释了这些东西之后，你再回去看王道书，应该就没有问题了。好，

那刚才我们说了，用于实现算术运算的指令，接下来介绍用于实现逻辑运算的一些指令逻辑运算啊，重点是与或非异或左移右移这些运算。相应的，汇编语言指令长这个样子，同样的d表示的是目的操作数s表示的是原操作数。每一个运算的结果，最终都会被存放回d所指的这个位置，所以d所表示的这个目的操作数不可以是常数。那要记住这些汇编指令的功能，同样的，大家只需要记住英文单词就可以了，来看一下异或运算嗯，这条指令叫做xor。

那这个异或的英文是exclusive or啊，然后拼上后面的or。这就是异或。然后左移是shift left shl对吧？然后右移是shift right shr。好，所以只要能和英文单词进行联系，这些汇编语言指令的功能并不难记。好，接下来快速过一下每一条指令的含义啊，与运算这条指令是把d和s所指的两个操作数逐位的相与，然后结果放回到d所指的位置。啊，或运算是主谓相互，结果放回到d所指的位置，

非运算是把d所指的这个操作数。逐位的取反就是零变一，一变零，结果放回到呃d所指的位置。那异或运算呢？就是主谓的异或结果放回到d这些都是一样的是吧？好左移运算是把d所指的呃，这个操作数进行逻辑左移。左移s这么多位，然后结果放回到d所指的这个位置，那这儿的s通常来说是一个常量，比如说我们可以把呃eax这个寄存器里的值。进行逻辑左移，左移几位呢？左移两位，

那么指令就是这么写的，那类似的逻辑右移，这条指令也是一个道理啊，右移之后的。这个结果同样是放回到d好，现在我们认识了这些指令之后，大家就更能理解之前我们讲的那些知识和内容了。就是CPU的内部控制单元cu会依次执行啊，这些指令对吧？去解析这些指令好，那当它确定这条指令。它所对应的功能之后cu控制单元，它就会给aru算术逻辑端单元发送与这条指令相对应的一个控制信号啊，比如说是进行。与运算对吧？

与运算那么d和s两个操作数分别被送到aru的两个输入端，现在控制单元cu又给aru发来了，和这个运算相关的。啊，这个控制信号那么aru就会把d和s相与的结果从输出端这边输出。那这就是执行一条指令的一个原理好，那除了刚才介绍的算术运算和逻辑运算相关的指令之外，我们还有一些其他的指令需要掌握。啊，用于实现分支结构和循环结构的指令有cmp test jump，还有j叉叉叉这样的一些指令，还有用于实现函数运算的一些指令。push pop call return。以及我们之前介绍过的用于实现数据转移的指令move。I like to move it，

move it。I'm moved这里。好，那现在上面这些指令我们先放弃，不学之后讲到分支循环，还有函数调用的时候，我们再来依次介绍这些指令的作用。OK，那以上就是这小节的全部内容。
