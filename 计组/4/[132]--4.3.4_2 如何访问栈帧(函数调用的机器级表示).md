好，继续来看函数调用的机器级，表示在这个视频中，我们主要探讨如何访问战争里边的数据。那上小节的末尾，我们留下了这样的一些问题。这个小节中，我们首先会探讨为什么我们在画这个栈的图示的时候，把栈底画在了上面，栈顶画在了下面。那由于函数调用的底层原理和战争里边的这些数据息息相关，所以这个小节当中我们也要解决如何访问战争里边的数据，这样的一个问题。好，首先我们来回答第一个问题，

为什么占底画在上面，占顶画在下面？对于一个32位的操作系统来说。系统会为每一个进程分配4 GB的虚拟地址空间，也就是虚拟内存地址的范围从八个零一直到八个f，总共4 GB。高地址的这1 GB属于操作系统的内核区，而低地址的这3 GB可以由用户进程使用，那我们之前提到的这个函数调用站。是在这个位置。函数调用站的站点在c七个零这个地址，而站点是在地址变小的这个方向。因此，大多数教材在画函数调用栈的时候，会把栈底画在上面，

会把栈顶画在下面，因为上面是高地址方向，下面是低地址方向。这和我们在数据结构课程里面画栈的那种习惯呃会有所不同，好，那接下来的讲解我们会用这样的图示来描述函数调用栈。那在这一片3 GB的用户区当中，除了函数调用栈之外。还会被划分成其他的区域用于存储不同的数据，其他这些区域有什么作用，我们暂时不深究，现在先把注意力放在函数调用站这个部分。接下来，我们要探讨的问题是，怎么用汇编语言指令去操作？

栈里边的这些数据。好在之前的课程中，我们学过在叉八六的呃CPU，里边有这样的一些常见的寄存器。其中ebp和esp这两个寄存器和我们的函数调用站息息相关。ebp寄存器用于存储啊，战争的基地址。也就是指向当前正在运行的这个函数的战争底部。好，另一个寄存器esp会指向当前战争的顶部，注意在一个CPU内部只有一个ebp和一个esp。我们不妨把这张图展开来看一下内部的细节。假设黄色部分对应的是当前正在执行的add函数，它的栈帧，而绿色部分对应的是。

polar函数的战争。在这个图当中，这样的一小格表示四个字节的大小，那这是因为在叉八六系统当中，对于函数调用栈的操作。不管是读还是写默认，都是以四字节为单位。所以esp指针指向了当前战争顶部的四个字节，而ebp指向的是当前战争底部的四个字节。再次强调，在这个图里边，上边是底，下边是顶。上边是高地址的方向，下边是低地址的方向好，

这两个寄存器标记了当前正在执行的这个函数，它的栈帧的范围。那这个函数运行完之后，是不是应该退回到caller这个函数？那相应的退回到color函数之后，ebp和esp就得做相应的修改，让它们分别指向color函数的。底部四字节和顶部的四字节。那这两个寄存器里边存储的本质上是两个内存地址，对吧？那接下来我们要探讨的问题就是怎么用汇编语言指令结合这两个内存地址？对栈里边的这些数据进行读或者写。好来看这样的一个例子，假设当前正在运行的是add这个函数ebp和esp，分别指向了它的底部和顶部四个字节。

现在eax寄存器里边的值是二幺幺。接下来我们要访问这个战争里边的数据，可以用push和pop两条指令，首先声明push和pop这两条指令在叉八六里边默认，都是以四字节为操作单位。push指令可以把一个例计数或者把某一个寄存器里边的值，又或者把某一个主存地址里边所存储的内容。压入到站顶的这个位置，而pop指令可以把esp此时指向的这个站顶的呃数据。给它出站，并且把这个出站的数据放到某一个寄存器里，或者放到某一个主存地址当中。那这我用一个狗头来表示push指令的操作数可以是呃，哪些类型用用一个马脸来指明pop指令可以把这个站点元素？弹到什么位置？

好来看一个例子，你就知道push和pop怎么使用了，好第一条指令是要把eax寄存器里的值，也就是二幺幺这个值给它压到站顶。push指令的执行过程是这样的，首先会让esp- 4，也就是让esp啊指到下面这四个字节。接下来再把eax寄存器里的值压到esp所指的位置，所以执行第一条指令的效果就是这样子。接下来，第二条push指令会把一个利基数九八五压占。同样的道理，会先让esp- 4，也就是指向。下面这四个字节，

然后再把九八五压入到这四个字节当中，也就是这样子。好，下面第三条指令要把ebp+8这个主存里边的数据给它push压到站顶。那来看一下ebp，此时指向这个位置，那ebp+8应该是指向这个位置，也就是指向六。所以这条指令的执行效果就是先让esp+4，然后把ebp+8里边的这个数据复制一份。给它压到站顶。那执行的效果就是这样的。好，接下来执行pop指令，pop指令的效果是这样的，

先让当前esp所指的这个站点元素出站。并且把这个元素的值复制到pop指令所指的这个位置。那站顶元素出站之后，还要让esp+4，也就是要指向上面这个位置好，所以第一条pop指令执行的效果就是。这个元素出栈，并且把数据复制到eax寄存器当中，也就是复制到这个位置，然后esp+4。那执行的效果就是这个样子。接下来，第二条pop指令同样的要把站点元素出站。弹出站点元素之后，要把它写到哪呢？

要把它写到ebp+8这个主存地址，也就是要写到这个位置。所以，执行第二条pop指令的效果就是这样的。好，那刚才我们学习了push和pop两条指令，这两条指令以四字节为单位，结合esp这个站顶指针去做入站或者出站的操作。push和pop指令只能对站顶的位置进行读或者写，那这样的限制会使得当我们想要访问站的其他位置的时候会很不方便。那有没有更灵活的方法，可以让我们访问到站的其他位置呢有的？接下来介绍第二种方法很简单，就是我们很熟悉的move指令。好，

同样看这样的一个例子，现在ebp和esp分别指向了add这个函数的战争底部和战争的顶部。你可以使用减法指令或者加法指令对esp和ebp这两个寄存器里边的值进行加或者减。这是被允许的好第一条指令sub减法，要把esp的值减12。然后把减的结果再写回esp。那esp- 4应该是这儿减八是这儿减12应该是最下面的这四个字节好，所以第一条指令的执行会让esp指针指向这个位置。好，下面我们可以用move指令把eax寄存器里边的值把它复制到esp+8这个位置。此时eax的值是二幺幺esp+8，应该是这个位置。所以执行的效果就是把这个数复制到这。变成这样。好，

下面一条move指令把九八五这个力计数move复制到esp+4 esp+4，在这所以执行的效果就应该是酱紫。接下来还是move指令，要把ebp+8这个内存里边的数据给它复制到EA当中。那ebp指向这儿加八，应该是指向六六六，所以执行的效果就是把六六六复制到eax寄存器当中。好，再往后要把eax寄存器里的值复制到esp所指的这个内存地址这。所以执行的效果就应该是把六六六复制到这个地方，变成这样。好，最后一条加法指令，把esp的值加八。执行这条指令之后esp应该指向这个位置。

因此，我们可以用减法或者加法指令对esp栈顶指针，让它往上移或者往下移，用这样的方式来调整当前函数的栈帧范围。同时，我们可以用move指令结合esp和ebp这两个指针对战争里边的这些数据进行访问。好的，那总结一下这个小节当中，我们介绍了如何访问战争里的数据，首先我们介绍两个非常重要的寄存器，分别是ebp和esp。二者分别指向当前执行的函数战争的底部和顶部。我们对栈里边这些数据的读写都是以esp或者ebp作为基准来进行的。那如何访问战争里的数据呢？第一种方法就是用push和pop指令。

push指令可以把一个指定的元素压入站点。同时，也会让esp指向新的站点，而pop指令会让esp所指的站点元素出站。并且这个元素会被复制到pop指令所指的这个位置。这是第一种访问战争的方法，而第二种访问战争的方法更简单粗暴，就是用move指令在esp或者ebp的基础上进行偏移，去找到想要访问的数据。比如esp+4可以找到这四个字节ebp+4+8+12，分别可以找到上面的这四个字节。当然，我们也可以让ebp- 4或者减八，这样我们就可以找到ebp下面的这些位置。那再次强调，

这个图里面占底是在上面。站顶是在下面这么画的，原因是站底这边是高地址的方向，而站顶这边是低地址的方向。好的，那以上是这一小节的全部内容。
