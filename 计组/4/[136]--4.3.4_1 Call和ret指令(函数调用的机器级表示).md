各位同学大家好，在这个小节中我们要学习函数调用的机器级，表示那这个视频当中我们先来认识和函数调用相关的两条非常重要的汇编语言指令。分别是call和return。首先，我们从大家熟悉的高级语言视角出发，看一下函数调用的背后发生了什么。我们知道，当你的程序运行的时候，系统会为它分配一片内存区域，我们把这片区域称为函数调用栈。顾名思义，这片内存区域会用于实现函数调用。好来看一下这个例子，每一个程序肯定都是从main函数出发，

先执行main函数。执行main函数的时候，函数调用栈里边有这样的两块数据，我们把用于存储main函数的这一片区域。称为main函数的栈帧。除了main函数的信息之外，在占底的位置还会保存一些和硬件以及系统相关的其他的一些信息。好，这个部分我们不用深究好，接下来我们在main函数当中调用了p这个函数，因此程序会开始执行p的这些代码。当这个函数执行的时候，栈顶就会有和p这个函数相关的栈帧。好，接下来p函数调用q函数，

那q相关的信息被压入栈顶，当q函数返回之后。就会从站顶删除q的战争。好再往后会调用caller这个函数，同样的会把caller这个函数的栈帧压入栈顶。每个函数的栈帧里边主要包含局部变量和函数调用相关的信息，比如说对于caller，这个函数在它的大括号里边，我们定义了temp 1，temp 2和sum这样的三个局部变量。那这三个局部变量就是存储在color这个函数的栈帧里边。caller函数在调用add函数的时候，需要给它传递两个参数，那这两个函数调用的参数同样也是被放在caller。这个函数的栈帧里边。

那具体细节我们之后还会展开探讨好，接下来执行add函数。那可以看到，当前正在执行的这个函数，它对应的战争一定是在站顶的位置，如果这个函数执行完毕并且返回之后。那这个函数的栈帧也会出栈。此时，程序的执行回到caller这个函数，那对应的站顶的位置就是caller这个函数的栈帧。同样的道理cola返回之后cola的战争也会出战。接下来，程序回到函数p继续执行。当函数p return返回之后，它的栈帧也会出栈，

最后就回到了main函数。好通过刚才这个例子，我们站在高级语言的视角，看到了当函数调用发生的时候，以及函数调用返回的时候。在内存调用站里边发生了哪些事？接下来，我们会探讨站在汇编语言的视角是如何实现函数调用的，那我们把目光聚焦于color和add这两个函数。这两个函数，它所对应的叉八六汇编语言啊，是这样的。左边是caller函数的汇编语言代码，右边是add函数的汇编语言代码，那在汇编语言当中使用了call和return这两条指令。

实现了函数的调用与返回，两条指令的语法很简单，我们通常会在汇编语言当中用一个标号去标记一个函数的开始。add+1个冒号，表示接下来这些指令是add函数caller+1个冒号，表示接下来这些指令是caller函数。所以当这个地方需要调用add函数的时候，只需要使用call指令加add标号，call指令发起了函数调用。接下来就会执行add函数的这些指令，那add函数的这些呃逻辑处理完之后，最后一条指令是RET return指令用于函数返回。执行return之后，程序会回到刚才这个call指令后面的一条指令继续往后执行。好，

所以在叉八六汇编语言当中实现函数调用和函数调用返回的指令，分别就是call和return。那我们知道在CPU内部有一个寄存器叫做PC程序计数器PC的作用，就是指向接下来要执行的那条指令。当程序的执行流发生跳转的时候，本质上就是要让PC寄存器指向另一个位置。那显然，call指令和return指令都会使得程序的执行流发生改变。因此，这两条指令肯定会影响PC寄存器的值。那由于x八六处理器当中程序计数器PC通常会被称为IP寄存器instruction pointer，就是指令指针的意思。所以接下来我们会用IP寄存器这个术语，你只需要知道IP寄存器就是你熟悉的程序计数器PC。好回到函数调用，

当函数调用发生的时候，我们需要解决两个核心问题，第一，靠指令执行了之后要过得去，要能够让IP寄存器。指向正确的位置。那第二个要解决的问题是要回得来执行return指令，之后我们要让IP寄存器回到call指令的后，一个指令继续往后执行。那这两条指令如何保证过得去，还回得来呢？首先看call指令的一个作用。当CPU正在执行call指令的时候，IP寄存器应该是指向下一条move指令，那执行call指令的第一个作用就是会把。

当前IP寄存器的值给它压栈保存。把它压到站顶的位置。接下来会让IP寄存器指向新的位置，也就是被调用函数的第一条指令。好，接下来就是执行I的函数。那执行完前边这些语句之后add函数的栈帧就会出栈。接下来CPU执行return指令。那return指令的作用就是从函数栈帧的顶部找到IP就址。并且把它写到IP寄存器里边，那这样的话IP寄存器是不是就可以回到call指令的后一条指令了？好，所以如何保证函数调用的时候能够过得去，还能回得来呢？关键就在于call指令执行的时候，

它首先会把IP寄存器的这个旧值。给压栈保存，然后才会根据这个标号所指明的位置进行无条件转移，让IP寄存器转移到被调用函数的第一条指令。当被调用函数执行完之后，return指令会从站顶的位置取出刚才保存的这个IP旧值，把这个值写回IP寄存器。这样就可以使得函数的执行流可以回到正确的位置。好的，那这就是函数调用的两条关键指令call和return两条指令的作用，可能在选择题当中进行考察。现在我们只解决了函数调用的时候怎么过去怎么回来的问题。接下来，我们还需要解决这样的几个问题。首先，

函数调用的时候，我们会传参数，对吧？同时，一个函数返回的时候，可能会有一个返回值。显然，刚才我们学习的call和return两条指令不可以直接带参数，也不可以直接带返回值。那么，参数和返回值是如何被传递的呢？另外，如何访问战争里边的数据？每一个战争里面到底包含哪些内容呢？这个也是我们需要探讨的问题。

最后还有一个小问题，大家有没有发现在这个图里边，我把站底画在了上面，站顶画在了下面。为什么这个函数调用栈要倒过来画呢？好，这些问题接下来我们会一一探讨。
