各位同学大家好，上个小节中我们介绍了数据寻址的六种方式，那在这个小节当中我们会介绍剩下的三种数据寻址的方式。这三种寻址方式都可以归为偏移寻址，这样的一个大类好，那在上个小节刚开始的时候，我们举过这样的三个例子。最左边这个例子，我们可以采用直接寻址的方式，也就直接去访问当前这条指令所指向的位置。用这样的方式可以得到我们期待的结果，而第二个例子当中，由于我们这段程序，它起始的存放地址是从100这个地址开始存放的，所以我们对于jump这条指令的解析。

对于七这个地址码的解释方式，就应该把它理解为从起始地址开始，往后偏移七个单位。应该用这种方式来理解，而第三个例子，我们应该把它理解为是从当前PC程序计数器所指向的地址往后偏移三个单位。用这样的方式来解释三这个形式地址好，所以右边这两个例子都有一个统一的特征，就是我们会以某一个地址，某一个特定的地址作为起点。然后加上我们形式地址所表示的这个偏移量，用这样的方式得到最终的有效地址。那我们这个小节会学习三种寻址方式，一种叫相对寻址奇数，寻址还有变址寻址这三种寻址方式都可以被归为偏移寻址的一个大类。

因为它们的共同特点就是以某一个某一个特定的地址作为起点，然后偏移a这么多的单位。而a就是我们指令当中给出的形式地址。那刚才我们提到的两个例子，其实就是基址寻址和相对寻址，所谓基址寻址就是我们会以程序的起始存放位置。作为起点，然后再加上a这个形式地址作为偏移量而相对寻址，就是会以程序计数器PC所指向的地址作为起点。然后再加上a所表示的这个偏移量，那还有一种偏移寻址是变址，寻址会以变址寄存器ix。这个寄存器里存放的地址作为起点偏移a这么多的偏移量，所以这三种偏移寻址的区别就在于它们选取的这个起点。偏移的起点会不一样。

那由于偏移的起点不一样，因此这几种寻址方式在实际当中的应用面也会不太一样好。那接下来我们按从上至下的顺序来依次详细的探讨这些寻址方式。首先来看机址。寻址在有的CPU内部，可能会有一个专门的机址寄存器，英文缩写叫br。那br指的是base address register。很多时候都会用两个英文字母的缩写来表示，这是一个什么寄存器，那有的寄存器有特定的作用。对于这些寄存器的英文缩写，大家也应该去关注一下。它的英文全称是什么？这有助于大家记忆每一个寄存器，

它到底有什么作用？好，那如果一条指令采用机址寻址的话，指令当中会包含一个形式地址a，那这个机址寄存器会指向当前这个程序起始的一个存放地址。那最终的有效地址只需要用基址寄存器里存放的地址，再加上形式地址a，也就是加上这个偏移量，就可以得到最终的有效地址。那这我们画了一个aru算术逻辑单元，也就是说把br和a这两个数据送给算术逻辑单元进行一个加法运算，就可以得到最终的有效地址EA。那EA又指向了我们指令想要访问的操作数到底存放在什么地方？注意，这儿的br是一个专门的基质寄存器，

那学过操作系统的同学应该知道，我们在第三章的第一个小节当中讲过程序的装入方式。在那个小节当中，我们讲过一个叫做重定位寄存器，那操作系统里边提到的重定位寄存器其实就是机制寄存器。只不过是中文的翻译不一样而已，所以这个内容大家也可以和操作系统第三章第一节那个视频进行着对比着学习。好，那刚才我们说到的这种情况是CPU里面会专门的设置一个基质寄存器br，那在有的计算机内部也许不会专门的给你设置一个基质寄存器。而是使用某一个通用寄存器，用一个通用寄存器来代替基质寄存器的一个功能。比如说总共有n个通用寄存器编号分别为零到n- 1，那么这条指令当中指明了我们的寻址特征。是要采用基址寻址，

另外还需要花几个比特位来指明我们的基地址存放在哪一个寄存器当中。像这个例子当中，我们假设基地址是存放在r0这个通用寄存器内部，所以接下来这个地址变换的过程就是。取出r0里边存放的这个地址作为基地址，把它送到aru加法器里边，然后再把形式地址a作为偏移量也送到aru里边。然后aru通过加法运算，得到最终的操作数的一个存放地址好，所以有的计算机内部是不会专门的设置一个基址寄存器的。在这种情况下，我们需要在指令内部指明用哪一个通用寄存器来代替机制寄存器。好，现在思考这样一个问题，既然这个属性是要指明某一个通用寄存器的编号，

那么这个属性应该占几个比特位呢？很简单吧，我们得根据通用寄存器的个数来确定这儿需要几个比特位，假如通用寄存器有八个。那么这个地方我们指明通用寄存器的编号，是不是只需要三个比特，因为二的三次方等于八嘛，只需要三个比特就可以指明零到七这几个数。好了，这个小地方，大家需要注意，考题里边有可能会遇到好。目前为止，我们已经大致了解了机制寻址的一个硬件，实现原理。

接下来，我们再来探讨为什么机制寻址有它存在的必要。还是用我们第一章的第四个视频所提到的这个例子来进行讲解，那大家会发现最近的讲解当中，这个例子出现的频率很高，如果有同学还没有理解这个例子。那么，建议大家可以回去看一下第一章的第四个视频，这个例子是很重要的，好之前我们说过这样的一段简单的高级语言写的代码。经过编译，链接等等，一系列骚操作之后，形成了与之对等的一系列机器指令，还有数据。

指令和数据被无差别的，以同等的地位存到主存当中。那这个程序的第一条指令被我们放到了主存地址为零的这个地方，从零开始存放，也就是处于整个主存的呃低地址部分。整个程序在主存当中的地址范围是零到八。好，那现在我们知道刚开始应该是要执行第一条指令，也就是存放在零号地址的这条指令，这条指令要做的是一个取数的操作。想要取出内存地址为五的那个存储单元里的数据，也就是a这个数据把a这个变量取到ACC累加寄存器当中。这条指令的地址码，也就是形式地址，把它翻译为十进制，

刚好就是五而五，刚好就是变量a的实际存放地址。所以这条指令的数据寻址方式，我们采用直接寻址，直接寻址，也就是直接去访问五号地址，用这种方式处理是可以得到期待的结果的。好，但是现在问题来了，假设我们的这段程序，它是从内存地址为100的，这个地方开始存放的。也就是说，这儿的第一条指令存放在100这些地址，第二条指令存放在幺零幺，

第三条指令幺零二好一直往下，然后a这个变量是存放在幺零五那个物理地址里边的。所以对于刚才我们提到的第一条指令，我们想要把a这个变量的数据取到ACC累加寄存器当中。我们对地址码的解析就不能采用直接寻址的方式，而必须采用基址寻址的方式。基尺寄存器br会指向这个程序，它在内存当中的起始存放地址也就是100这个地方，大家可以自己试一下，把这个二进制转换成十进制，刚好就是100对应十进制的100。那么，按照机址寻址的规则，这地方的形式，地址五应该再加上。

基地址也就是100，用这样的方式来得到最终要访问的有效地址EA。那算出来的结果是105刚好就指向了a所存放的这个主存地址幺零五。那这样的话就得到了我们期待的结果，所以基址寻址的意义就在于可以便于程序的浮动，就是说这个程序可以从内存当中任何一个地址作为起始地址开始往后存放。当这个程序在主存当中的位置发生改变之后，操作系统只需要修改br这个寄存器里的内容，就可以永远是让br指向当前这个程序的起始地址。那这样的话，我们写的这段程序是不是就不用更改，保持原来这个地址码就可以好，所以有了基址寻址之后就可以很方便的实现多道程序并发运行？因为主存里边有可能会有多个程序的数据同时存在。每一个程序的起始存放位置都是不一样的，

那么每一个程序运行之前CPU的基址寄存器的值。都会修改为当前运行的这个程序的一个起始存放地址，而这个信息通常是存放在。进程控制块PCB当中的好，那现在还没有学操作系统的同学看不懂，没有关系，这个只是一个拓展考试补考。讲这个只是为了让大家进行学科之间的一个知识的融合好，这就是基质寻址，那需要注意的是基质寄存器。br，这个寄存器里的内容是面向操作系统的，只可以由操作系统或者操作系统的某些管理程序来修改，这个寄存器的内容。我们普通的程序员是不可以操作br寄存器里边的值的，

因为我们写的应用程序到底被放到内存的什么位置？这一点应该是由操作系统来负责管理的，我们决定不了，而基址寄存器又是指向了我们应用程序的一个起始存放地址。因此，基质寄存器的内容显然是应该由操作系统来负责管理。基质寄存器的内容作为基地址。然后指令里面给出的呃形式地址作为偏移量，把它们一相加就可以得到最终的一个有效地址。那这个地方还需要补充一点，我们普通程序员可以用汇编语言来直接操纵某一个通用寄存器里的内容读或者写都行。但是如果某一个通用寄存器被我们指定作为基质寄存器来使用的话，那么接下来这个通用寄存器里边的值我们也不可以随意的修改。其中的内容会由操作系统来负责管理好，总之一个程序在内存里的基地址，

无论是保存在专用的寄存器里，还是保存在通用寄存器里，这个信息都应该是由操作系统来负责管理的。我们普通程序员不能修改。也就是说，我们作为普通的程序员，并不需要考虑自己的程序被存放在主存里的哪一块区域内。不管存在哪，操作系统都会帮我们解决这个基地址的问题，那采用基址寻址还可以带来一个好处，就是可以扩大我们指令的寻址范围。原本我们这条指令的形式，地址a可能比较短，它的寻址能力有限，但是如果再加上基地址，

那么由于基地址的位数比较长。因此，最终这条指令可以访问的有效地址的一个地址空间范围就可以扩大。好，这是基质寻址，接下来来看第二种变质寻址，那看这个图，大家会发现，其实它和基质寻址几乎一模一样。除了这个寄存器的名称不一样之外，其他的看起来都一样。那实现变址寻址所引入的这个寄存器啊，我们把它称为变址寄存器，英文缩写是ix，也就是index register，

这是它的英文全称。我们的一条指令给出了形式地址，那形式地址，再加上这个变值寄存器里存放的这些地址信息，就可以得到最终的有效地址。那和上一种寻址方式类似，有的系统当中会专门的设置一个变置寄存器ix，用来实现变址寻址。也有的系统当中会使用某一个通用寄存器来作为变址寄存器，这原理和基质寻址是类似的，这就不再赘述。好，那变址寻址和基址寻址这么类似，它们之间的区别又是什么呢？那区别就在于变址寄存器。

ix里边的内容是面向用户的，就是说我们作为普通的程序员，应用程序员可以修改ix里边的这个数值。另外，在这种寻址方式当中，ix 1般会作为偏移量，被我们视为偏移量，而形式地址a。会被我们视为基地址。这一点和上一种寻址方式刚好相反。好，还是通过一个例子来了解变址寻址的作用，假设现在我们要实现这样的一个功能，就是把数组a0到a9的元素。分别进行加和存到一个变量sum里面。

这是我们高级语言的视角，那如果说这段代码要把它用机器语言或者说汇编语言来实现，那我们可以采用这样的方式来实现。来看一下第一条指令。是取数指令，把数取到ACC，累加寄存器当中，那这地方我们的地址码前边打了一个井号。还记不记得这个是什么？上一小节当中，我们说过，如果地址码前面打一个井号，那么说明当前这条指令采用的是立即寻址。这地方会直接给出我们要使用的立即数，那么执行这条指令的结果就是会把零这个数把它放到ACC累加寄存器当中。

对了，在这个例子当中，我们给一条指令，只划分了操作码和地址码，这样的两个部分，但是实际上这个指令当中还应该包含呃几个比特的信息，用来指明这条指令的。寻址方式。那这地方我们就在前边用文字的方式把它标注一下。好，继续往后下一条指令，要执行的是一个加法，操作就是用ACC里边存储的这个值。加上a0a0这个数字元素，把相加的结果放回ACC累加寄存器当中，

而这地方可以看到a0这个元素是存放在主存地址为12这个地方的。好，所以这个地方地址码我们只需要让它指向12就可以。这我用了十进制表示，方便大家理解好，接下来再往后的一条指令，同样是ACC加法，就是把ACC的内容和a1这个数组元素进行相加，再存回ACC当中。那a1是存放在13这个地址，因此这个地方地址码应该是对应十进制的13好，后面都是类似我们要实现这个循环里边的十次加法操作。可以用十条加法指令来实现。然后最后再把ACC里边累加得到的结果放回sum这个变量，里边sum变量是存放在22这个地址。

好，所以这地方我们能够感受到，对于这种循环类的操作，如果按照我们之前知道的这些土办法。那么，每一次循环都需要对应一条指令，这就会导致我们编程很不方便，并且很不灵活。万一未来我们需要实现a0到a19的一个相加。那是不是按照这种方法，我们还需要继续在这条指令的后面再增加一些加法指令？所以这就导致了编程极大的不方便，因此我们就要需要引入变址寻址来实现我们的循环操作。好，那引入了变址寻址之后，

我们刚才这段程序可以把它改造成这个样子，用六条指令就可以实现，一会我们再来依次解释这些指令，分别会起到什么样的作用。那这儿我们也给每一条指令标明一下，这条指令所对应的寻址方式，首先第一条指令是取数指令。把力计数零放到ACC累加寄存器当中，放进去第二条指令，同样也是取数指令，只不过是把它取到变址寄存器ix当中。同样是把零放进去。好，接下来这儿有一条加法指令，只不过这条加法指令采用了变址寻址，

那变址寻址就意味着有效，地址应该是等于。变值寄存器里的地址，加上形式地址a，那这条指令的形式地址a指向了七这个位置。也就是数组的第一个元素的存放地址，然后现在变值寄存器ix的值是零。因此，现在我们执行这条加法指令导致的结果就是ACC里的内容。零。加上七+2x，也就是七这个单元里边的数据，也就是加上了a0的值。然后把这相加的结果放回ACC寄存器当中。如果对照高级代码来看的话，

就是完成了第一轮的循环。好，接下来执行后面这一句这一句同样是要进行一个加法操作，只不过加法的对象是ix。变值寄存器要让变值寄存器的值加一，然后再把这个值放回变值寄存器。就有点类似于我们执行了这个I加加的操作好，接下来当I小于十的时候，我们还应该让这个循环继续，对吧？那站在汇编语言的角度，我们可以使用一条比较指令来比较ix里边的值和十谁更大。接下来，根据这两个数的比较结果来确定我们需不需要进行跳转。当ix的值小于十的时候。

也就是这儿的I小于十的时候，我们需要让程序的执行流跳转回二这个位置，那二这个地址对应的就是。ACC的加法指令。也就是要进行第二轮的循环，第二次的加法。好，第二次执行这条加法指令。这条指令采用变址，寻址的方式，那么就意味着我们的操作数应该是存放在。七加上这个变值，一也就是存放在八这个位置，那八这个位置刚好存放的是a1第二个数字元素。所以执行了这条加法指令之后，

就会导致ACC里的值变成了a 0+a一的值。进行了第二轮的循环好，接下来是类似的，我们会让ix变值寄存器的值加一由一变为二。然后同样的需要判断呃，这个变址寄存器的值是否小于十，如果它小于十的话，满足这个条件，那么同样的我们还会再跳转回二。这条指令也就在执行下一轮的循环好，那后续都是类似的多轮循环，之后ix的值会呃逐渐的增加为九。那么刚好a9这个数据是存放在七+9是存放在这地方，那完成了最后这个数组元素的加法之后ix同样的会进行一次加法。变成十接下来ix和十这个力计数进行比较，

但是此时十减ix的值已经不大于零了。没有满足这个条件，因此这个条件跳转就不会发生，就不再跳转回二这条指令，而是会让这个程序继续往后执行。那最后这一句是把ACC里的数存放到地址为17这个地方，那17这个地方刚好是sum变量。所以这样就完成了多个数组元素相加，然后赋值给sum变量这样的一个事情好，那现在大家再回头来体会我们之前说过的那句话。在指令当中给出的形式地址a这个地址，我们应该把它视为一个基地址。一个起点。然后ix变值寄存器，我们应该把它视为偏移量。

从基址往后偏移多少个单位，所以引入了变址寻址之后，我们在处理数组元素的时候，可以把a就是这个形式地址。设置为数组的首地址，然后不断地改变变质寄存器ix的值。让它不断的往上加，这样的话就可以很容易的呃，对数组的所有元素进行访问。因此，有了变址寻址之后，可以很方便的让我们实现循环程序。好，那这就是变址寻址的作用，变址寄存器里的值是我们普通程序员可以采用指令的方式来进行修改的。

这点和机制寄存器不一样。对了，这个地方大家可能会疑惑，就是ix的值和力计数十如何进行比较，在硬件层面实现的一个逻辑，我们暂时还没有细讲。这个问题我们先把它留下，在这个小节的最后我们再来补充。这地方大家只需要知道进行十和ix的比较，本质上就是硬件会进行十减ix的一个操作，然后接下来这个条件跳转指令会来判断刚才的这个计算结果。是否大于零？如果大于零，满足这个条件，那么就会跳转回二这个地址，

也就是把PC的值改为二。是这样的一个意思，好具体的细节，我们之后再来补充，那我们再对变址寻址进行一个总结，通过刚才的例子，大家应该能够体会什么叫面向用户。什么叫可以由用户改变？并且现在我们知道了为什么变址寄存器里边的那个值会把它视为偏移量，而形式地址a会被我们视为基地址。好，那有问题的同学还可以再暂停来消化一下，好，接下来我们来看一种符合的寻址方式，就是基址寻址和变址寻址的一个结合。

在刚才这个例子当中，我们是默认了这个程序，它是从主存地址为零，这个地方开始往后存放的。因此，当ix=2的时候，我们刚好可以访问到a2这个单元。因为基指七加上变指二刚好等于九可以访问到a2好，现在假设这段程序是存放在另一个地方，从内存地址为100的这地方开始存放。也就是说，第一条指令存放在一零零，第二条是一零一一零二那呃a2，这个数组元素就应该是存放在一零九这个地址。那这个时候我们执行这条加法指令，

就不能只用变址寻址，而是需要结合基址寻址和变址寻址来得到最终的有效地址。那要采用基址寻址，就需要有基址寄存器，它指向了当前这个程序的起始地址100好，那之前我们已经知道了基址寻址和变址寻址的一个有效地址的计算方式。现在我们把这两种寻址方式结合先进行基址寻址，那么就意味着我们需要先把当前这条指令当中给出的这个形式地址a。a是等于七先把这个形式地址a加上基地址br的内容，那br的值是100，所以先进行了基质寻址就应该是得到107。107。那可以看到现在107已经指向了呃a0这个元素，那接下来在基质的基础上再进行一次变质寻质。也就是再加上变址寄存器ix里边的内容，

也就是加上二，那么就得到了最终的有效地址一零九。那这样就可以符合我们的预期，因此在实际应用当中往往是需要多种寻址方式复合着来使用的。大家可以把每一种寻址方式理解为是一种函数，因为每一种寻址方式都是要把形式地址a映射为一个有效的真实地址EA，对吧？只不过，不同寻址方式，它的映射规则不一样。那多种寻址方式的复合，其实就相当于把不同的映射和规则给结合起来。那这不就是数学里的复合函数吗？当然了，这只是我自己的思维习惯，

大家可以按照自己习惯的方式来理解。在课后习题里边，大家还会遇到各种各样的这种寻址方式的一个组合好，那具体呢，大家再通过课后习题来进行知识的补充。那接下来我们要认识这个小节的最后一种寻址方式，叫做相对寻址。在相对寻址当中，我们会以PC程序计数器所指向的位置作为基地址。然后把形式地址a看作是一个偏移量，那这形式地址a可以是正的，也可以是负的，通常用补码的形式来表示。好来看一下这个图是什么意思？我们从主存地址为1000的这个地方取出了这条指令，

取出这条指令。然后这条指令它采用了相对寻址的方式，其中包含了一个形式，地址a，那么之前我们说过。每当CPU取出一条指令之后，都会让PC的值加一，这儿的加一，我们打了双引号，意思是说PC在取出一条指令之后，它应该指向下一条指令的存放地址。当前执行的指令存放在1000这个地址，那如果当前这条指令它的指令字长占两个字节，那我们就需要让PC的值加二。而如果当前这条指令指令字长是四个字节，

那我们就需要让PC的值加四，所以PC在1000的基础上有可能是加二，也有可能是加四。当然，还有可能是加其他的值，所以此时PC的值有可能是一零零二或者是一零零四。那我们为了讨论方便，我们就把它规定为此时呃，假设PC是指向了一零零二是这样的一个值好，那现在CPU执行的这条指令。它采用的相对寻址方式是要在当前PC的值的基础上偏移a这么多的数量。也就是一零零二的基础上，再加上a的值。那a有可能是正是负由于地址a是用补码表示，因此当它为负的时候是不是就相当于在PC的基础上减掉了某一个正数？

也就是说，这次寻址可以从PC所指的这个地址往前偏移，或者往后偏移都是可以的。因为a可正可负好这地方，想和大家重点强调的是，当我们取走一条指令之后PC的值，一定会自动的加一。会指向下一条指令。好所以王道书上的那句描述是有点问题的。王道书上说，我们指令里给出的这个形式地址a，它是相对于当前指令地址的一个位移量。其实不是这样，应该是相对于下一条指令地址的位移量，因为CPU取出当前指令之后，

一定会让PC的值自动的加一自动的指向下一条指令。所以a应该是基于下一条指令的存放地址的一个偏移量，这一点大家需要注意。好，接下来我们还是通过一个例子来理解相对寻址有什么作用，这是我们刚才给出的那一段指令，就是实现了a0到a9的一个加和。for循环的主体应该是对应这样的几句代码呃二，这句代码是实现了加法，然后后面是ix的加法ix的比较，然后最后这一条是一个条件跳转指令。当条件满足的时候，又会跳回二这条指令执行。也就说我们黄色这一条呃跳转指令，我们采用的是直接寻址的方式。

直接指明了我们想要跳转回的一个地址。好，现在问题来了，我们写代码的时候有可能代码越写越多，对吧？然后你写的某一段代码是不是也有可能会在你的程序里边调整它的位置？比如你的这段代码，可能本来是放在程序的比较开头的这段位置，但我们写程序的时候，有的时候写着写着你会想把它挪到下面这个地方，对吧？这种需求在我们编程的时候是显而易见的，所以接下来我们要考虑的问题就是如果你的代码现在越写越多，然后你想挪动这个for循环的一个位置。那站在汇编语言程序员的视角来看，

那是不是就相当于要把这几句指令的位置给它挪一下？以前是从二这个地址开始存放的好，现在我们把它挪一下。假设我们把它挪到这样的一个位置，就是从m这个地址开始往后存放，然后此时各条指令的这个操作码和地址码，我们都让它保持不变。那现在把注意力放到这条跳转指令，当for循环的条件满足的时候，我们会跳转到二这个位置。这是我们之前按照直接寻址的方式来解析这个形式地址导致的一个结果，那现在如果我们还按照直接寻址的方式来解析，是不是意味着？执行这句代码之后PC会指向二这条指令，而这条指令并不属于我们for循环，

因此这样的执行流肯定会导致错误。好，现在为了解决这个问题，我们引入相对寻址，那相对寻址是在PC所指的地址的基础上进行一个偏移。目前CPU正在执行的是这个跳转指令。那么CPU取出这条指令之后PC的值会自动的指向下一条指令也就会指向m+4这个地址。好，现在为了让这个循环能够正常的工作，那我们所期待的结果是不是可以让PC的值从当前？PC所指的这个地址往回偏移四个地址。因为m+4再减四不就可以回到m吗？好，所以我们把这条跳转指令的地址码把它改一下，改为负四。

这地址码是用补码表示的，那现在这条指令我们让它采用相对寻址。因此，这条跳转指令真正要跳转到的地方，要跳转到的地址就应该是PC，加上a，也就是跳转到m。那由于跳转指令本质上是在修改PC的值，所以就意味着呃，会把我们经过相对寻址得到的这个有效地址，这个值把它赋给PC。让PC重新指回m这条语句。那这样是不是就可以保证for循环的正常运行，并且从今往后无论这段for循环的呃，这几句指令我们把它放到程序的哪一段位置？

只要我们采用了这种相对寻址的方式，那么这条跳转指令一定都可以让程序的执行流重新的跳转回加法这些地方。也就说，从今往后，无论这一段汇编代码被我们放到了什么位置，我们都不需要再修改这个跳转指令的地址码。让它永远保持负四，并且采用相对寻址就可以得到正确的结果。因此，我们说引入了相对寻址之后，可以方便我们的呃某一段代码，在程序的内部浮动。把这段代码放到程序里的任何一个位置都OK，我们都不需要再修改这个地址码。好，

所以这是相对寻址的一个作用，这地方细心的同学可能会发现我们的这段程序整体变长了之后。数组元素a0不再是存放在七那个地址。它有可能存放到后面的某一个位置，那这是不是意味着我们得修改呃，上面这条加法指令的这些地址参数呢？显然，如果我们每一次挪动代码都需要修改这个地址参数的话，那这对于程序员来说也是很麻烦的一件事。那现实当中是如何处理这个问题的呢？通常来说，我们可以进行分段好，这个地方又可以和大家以前学过的知识串联起来，我们可以把一个进程的数据。把它分为程序段，

程序段就是只保存我们这些指令代码，然后还有一个是数据段，用数据段专门来存放我们的数据，比如说这是一个独立的数据段。我们可以让呃数组a数组a永远是从七这个地址开始存放的，那进行分段之后，我们想要访问的那些变量数组元素。它在这个段里的相对位置，是不是就可以保持固定不变？也就说我们之前写好的这条指令，给出的形式地址，我们不需要再改变。让它继续保持为七就可以好，所以通过这个例子，大家应该也能够体会到我们把进程的数据进行分段的一个好处。

当然了，刚才这个地方如果有同学听不懂也没有关系。这个只是我个人给大家的一个扩展，是为了让大家把跨学科的知识融合起来好，那我们再对相对寻址进行一个总结。那采用相对寻址可以有这样的一些优点，如果当前我们执行的这条指令，想要访问的呃操作数或者接下来想要跳转的一个地址。它是随着PC的值变化而变化的，和PC所指的地址总是会相差一个固定值，在这种场景当中就很适合用相对寻址来解决问题。可以方便我们呃进行程序的浮动，也就是一段代码在程序内部的一个浮动。就像刚才我们给的例子一样，相对寻址经常会被用于转移类的指令，

对了，这再补充一点，之前我们说采用基址寻址也可以，方便我们程序的浮动，对吧？那在基址寻址当中，我们所说的程序的浮动，指的是诊断程序在内存里的浮动，而相对寻址这边，我们说的程序浮动，指的是。指的是一段代码，在程序内部的浮动，它们是有区别的，大家不要混淆好，

那这一小节当中，我们介绍了相对寻址基址，寻址和变址寻址。这三种寻址方式都可以归为偏移寻址，这样的一个分类原因就在于这三种寻址方式都是基于一个基地址。加上一个偏移量来得到我们要访问的有效地址。那相对寻址通常会被用于转移指令，而基址寻址通常会用于多道程序并发运行会，由操作系统来管理基址寄存器里的值。而变址寻址可以方便我们实现循环程序或者对数组的访问。那这几种寻址方式，在指令执行期间，都只需要缓存一次。那再次强调，对于相对寻址来说，

我们每次取出一条指令之后PC都会自动的指向下一条指令，所以相对寻址其实是相对于下一条指令的一个偏移，而不是相对于当前。执行的这条指令的一个偏移，这点很容易出错，大家不要误解好，那这就是这一小节的全部内容，那最后我们再来补充一个啊，之前留下的小问题。就是硬件是如何对两个数进行比较的，比如说谁更大谁更小，或者是否相等。首先，我们平时最熟悉的是高级语言，那高级语言里边我们经常会写什么ifa大于b。

else啥啥啥，对吧？那站在硬件的角度，这段高级语言是这么被处理的。首先CPU会执行一条compile指令。compile就是比较比较的对象是a和b这样的两个数。实质上，比较的过程就是用a减掉b，那a-b是不是有可能得到各种各样的值？有可能是零，有可能是正数，有可能是负数，甚至有可能出现溢出等等。各种各样的情况，那么这个减法操作的一个运算结果相应的信息，

相关的信息。会被记录在程序状态，字寄存器也就psw这个寄存器当中，这个寄存器里边会有几个比特位？用来记录上一次呃运算的结果，也就是a-b这次运算的结果，比如说这个寄存器里的CF，这个比特位。是用来表示之前的这次运算当中有没有产生进位或者借位，如果说运算过程中最高位产生了进位或者借位。那么CF的值会等于一，否则等于零，另外还有一个比特位叫做零标志位zf。如果说上一次运算的结果是零，那么zf会等于一，

否则zf=0好，还有一个符号标志sf。这个标志位呃表示的是上一次运算，如果结果为负，如果结果为负，那么sf会等于一，否则为零，最后一个是溢出标志。of如果运算结果有溢出，那么of会等于一，否则为零好，也就是说机器执行了compile这条指令之后。会把a-b的一个结果是否有进位借位相减之后，是否等于零相减的结果为正还是为负，是否发生了溢出，

把这些信息都记录到psw这个寄存器里的某几个比例。比特位当中，接下来CPU就可以根据这几个呃标志位，这几个比特位进行条件判断。用来决定是否进行程序执行流的转移，那站在高级语言的视角就相当于a大于b这个条件满足的时候。程序执行流会被转移到else所包含的这一系列的语句当中。那站在硬件层面，我们执行这条if语句，相当于硬件帮我们做了一个a-b的，一个呃，一个操作，如果a大于b的话，那么这个减法操作。是不是会导致零标志为zf？

应该是等于应该是等于零对吧？因为a大于b，那么它们相减的结果肯定是非零的，所以zf会等于零，另外符号位。符号为sf应该也是等于零，因为a大于b嘛，所以它们相减肯定是一个正值。好，所以硬件电路会以这两个比特位作为输入信号，然后通过电路的处理，把PC程序计数器的值把它指向else。所对应的第一条语句，那这样的话就完成了一个所谓的条件转移指令。那这儿再给大家补充一点，

在汇编语言当中条件跳转指令，有可能会有很多种，比如说je。je 2这条指令表示的是之前我们compile的这两个数，如果相等的话，那么接下来程序执行流PC就让它跳转到二这个位置。j表示的是jump，然后e表示的是when equal。当我们compared这两个数一口相等的话，我们就跳转到二这个位置好了，除了这个指令之外，还会有其他的一些条件转移指令，比如说jg。呃，第一个j同样是jump，

然后第二个g表示的是greater就是更大，当更大的时候。jump进行跳转，所以它的汇编语言就写成了jg，那这也就意味着如果我们呃compare比较的这两个数。前一个数比后一个更大更greater的时候，就会jump跳转到二这个位置，也就是让PC的值等于二。是这样的一个意思好，那除了这两种条件跳转指令之外，还会有其他的很多很多种条件跳转指令，大家可以在参考王道书给的一个总结。好，那与条件跳转指令呃相对应的就是我们之前提到的这个无条件转移指令，或者叫无条件跳转指令。

jump如果直接使用jump 2，那么就意味着我们不会管任何条件，直接就让PC的值直接等于二。是这样的一个意思，好，那了解了这些知识之后，大家就能够知道为什么我们刚才这两条指令会用这种方式来写？啊，第一条指令四，这条指令是呃要和ix寄存器里的内容比较用，谁和他比较呢？用力计数十。和它进行比较，那么比较的本质就是进行一个减法，减法的结果如何会被存到psw当中，

然后接下来这个条件跳转指令是来判断我们刚才的这个减法。结果是否大于零？如果大于零，那么就让PC的值跳转到二这个位置是这样的一个意思。好，那现在大家应该就可以理解这两个指令是什么意思了？对了，再补充一点，有的机器当中也会把psw就是这个程序状态字寄存器把它称为标志寄存器。这个术语在我们课后习题里边也会遇到好，那这就是用硬件实现数的比较的一个背后的原理，大家可以再琢磨消化一下好的，那这个视频的内容还是比较多的，并且还不是很好理解。不知不觉就录了这么长的时间，

希望同学们静下心来好好的吸收好的，那这真的就是这个小节全部的内容了。
