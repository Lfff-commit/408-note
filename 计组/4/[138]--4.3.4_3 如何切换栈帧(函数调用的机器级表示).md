好的，通过之前的学习，我们已经知道在函数调用的时候，我们怎么去访问战争里边的数据？在这个小节中，我们要着重探讨如何切换战争。我们说CPU内部有ebp和esp两个寄存器。这两个寄存器标记了当前正在执行的函数，它的战争的范围。当发生函数调用的时候，需要修改ebp和esp，让它们指向新的这一层函数，它的底部和顶部。而现在，这层函数执行完毕之后，

我们又需要把ebp和esp重新让它指回啊，上一层函数的栈帧。好，那我们通过caller和add的汇编语言代码来研究这个问题。假设现在绿色这一部分是caller函数的栈帧，现在我们要执行的是call这条指令，也就是要调用add这个函数。那当CPU执行这条指令的时候，IP寄存器应该是指向下一条相邻的指令。之前我们说过call指令的执行，有这样的两个作用，首先会把当前的IP值给它压入栈顶。那这个效果就相当于push IP，首先会让esp- 4指向这个位置。然后把IP寄存器的值压入栈顶好，

这是call指令的第一个作用，把程序的返回地址压栈保存。那靠指令的第二个作用是会设置IP寄存器的新值，让它无条件的转移到呃被调用函数的第一条指令，也就是指向这个位置。所以靠指令的第二个作用效果相当于jump AD。我们之前学过无条件转移指令jump，如果在无条件转移指令后面加上一个标号。那么，程序的执行流就会转移到这个标号所指明的这个位置。好，所以执行完call指令之后，效果就是这样的，返回地址压栈保存，同时IP指向了add函数的第一条指令。

好，那执行完这条指令之后，接下来是不是应该执行这条指令了？也就是要执行push ebp，同时IP自动地指向下一条指令。来看一下这条指令的作用。push ebp就是会把ebp寄存器里的值压到站顶。那ebp寄存器此时指向的地址是a零零f零零三零，所以执行这条指令的效果就是会把这个地址信息。压站保存到站顶的位置。也就是变成这个样子。好，接下来要执行move指令，把esp寄存器里的值给它复制到move到ebp寄存器当中。也就是要让ebp寄存器指向esp当前所指的这个地址。

a零零f零零一零指向这个地方。好，可以看到刚才这两条指令更新了ebp和esp的指向。第一条指令把ebp的值压栈保存。也就是要把上一层函数的战争激励值ebp的旧值给保存在这个地方。把这个地址保存下来，那么当我们要返回到上一层函数的时候，是不是也可以根据呃我们记录下来的这个地址，让ebp重新指回到？上一层函数的基地值。好，所以这是第一条指令的作用。接下来第二条指令我们让esp的值复制一份到ebp当中，也就是让ebp指向了新的这一层函数的基地址。所以当函数调用的时候，

通过这两条指令就可以完成战争的切换。那可以看到caller这个函数，它的前两句呃汇编语言指令也是push ebp先记录上一层函数的呃战争基地址。然后再move把ebp指向当前这层函数的基地址。所以刚才这两条指令在每一个函数的开头都一定会这么例行处理。并且我们发现每一个函数的栈帧，它的底部一定是存储了它上一层函数的基地址。add函数，它的战争底部存储的是上一层color函数的基地址，也就是零零三零这个地址。而color函数，它的战争底部同样的也是存储了调用color函数的上一层函数，它的战争机制。这么做的好处显而易见。当一层函数执行结束要返回之前，

我们总能在当前函数的呃战争底部。找到上一层函数的这个基地值，那这样我们就可以恢复ebp寄存器的值。好，那需要补充的是呃，刚才的这两条指令可以精简为一条指令叫做enter。也就是说，你也可以把这两条汇编语言指令用enter指令代替。enter指令是零地址指令，后面不需要跟任何的操作数。很好记对吧？enter就是进入的意思，当一个函数刚开始被进入的时候，呃例行执行enter指令，那enter指令的执行效果就等价于刚才我们说的。

呃，push和move这两条指令。好，回到这个地方，我们已经切换成add函数的栈帧，接下来我们是不是可以往这个栈帧的顶部压入一些数据？好，那现在的问题是当add函数return返回之前，我们要如何恢复esp和ebp的值呢？方法也很简单，只需要用这样的两条指令，就可以实现。首先，第一条move指令把ebp的值复制一份给esp。也就是让esp和ebp指向同一个地方，

所以第一条指令的执行效果就是这样的。好，接下来第二条指令是pop。pop指令会把esp当前所指的呃，这个站点元素出站。同时esp+4指向这个位置。那这个站点元素的值会被复制到ebp寄存器当中，也就是会让ebp的值等于a零零f零零三零。也就是让ebp这个指针重新指回了color函数的栈帧的底部这个位置。好，所以当一个函数return返回之前，只需要执行这两条指令，就可以让esp和epp重新指回上一层函数的栈帧。那刚才介绍的这两条指令呃，执行的效果等价于live指令。

live的意思是离开对吧？当我们要离开呃当前的这层函数的时候，我们例行的执行一下这个live指令或者说例行的执行，刚才介绍的这两条指令。这样就可以恢复上一层函数的栈帧。好，所以回到刚才的这个代码，你会发现add函数在return返回之前，它执行了一个live指令。同样的color函数在return返回之前，它也执行了live指令。显然，在每个函数return返回之前，这都是一个必须要做的例行处理好。那现在我们已经明白了，

在函数返回的时候，我们是如何切换栈帧的？那我们不妨再看一下这个return指令，它的执行效果，那之前我们说过，在color函数当中执行call指令的时候。已经把IP也就是函数的返回地址压到呃color函数的站顶了。所以现在esp所指的这个数据刚好就是函数的返回地址，所以接下来return指令它的执行就顺理成章了。return指令它会从函数的栈帧顶部这个位置找到IP寄存器的旧值。并且取出这个值，然后写到IP寄存器当中，也就是让程序的执行流回到call指令的后面，这条指令继续往后执行。好，

我们对之前的内容进行一个总结，首先在发生函数调用的时候需要执行call指令。而被调用的这个函数，在所有的呃任务完成之后，需要执行return指令。那为了实现战争的切换，当函数调用发生的时候，被调用的这个函数在它的刚开始，这个位置需要例行的去执行，这样的两条指令。当然也可以呃，用一个更简单的enter指令来替代这两条指令，效果是一样的。好，接下来被调用的这个函数，

经过一系列的逻辑处理呃，可以完成这个函数的内部功能，那在return返回之前。需要先恢复上一层函数的战争，恢复的方法是使用这样的两条指令或者更简便的，可以使用一条live指令。那战争切换完成之后return指令会让程序的执行流回到调用者继续往后执行。显然，除了main函数之外，其他所有的函数肯定都是被调用的函数。因此，在题目当中，除了main函数之外，其他所有函数的这个汇编代码结构肯定都是刚才我们总结的这个结构。所以刚才说的呃，

这套结构可以作为你的一个阅读框架，有助于你理解这个汇编语言。比如看二零一九年的这个真题，右边这个部分是f1这个函数的汇编语言代码。那刚才我们说除了main函数之外，任何一个函数刚开始肯定要例行的，做一个动作就是要保存上一层函数的战争，并且设置当前函数的战争要完成战争的切换，对吧？所以你看这个函数的第一句是push ebp，第二句指令没有给出来，但是我们能猜到肯定是move ebpesp。这就是一个必须要做的例行动作。好，接下来中间这些代码就是完成这个函数的呃，

相应的功能到最后，这有return指令。一个函数执行到最后肯定要返回上一层函数，对吧？那在这个return指令前边，他打了一些点点点，省略了一些部分，但是我们可以确定。return指令的前边这个位置，它肯定有一个live指令，或者就是有move和pop这两条指令。因为任何一个函数在return返回之前，肯定需要恢复上一层函数的栈帧。好，这是一九年的真题，

再来看一七年的真题。同样的一七年，这也写了一个函数f1。然后右边的这些东西是这个函数的汇编语言指令。那现在我们知道了，除了main函数之外，任何一个函数肯定都是被其他函数调用的。那在它刚开始的位置，一定需要保存上一层函数的战争，并且设置当前函数的战争，所以你看这地方，它有一个push epp。下面点点点省略了，但是我们知道后面一句肯定是一个木指令。并且我们知道push ebp的作用是把上一层函数的这个栈帧基地址给压栈保存。

好，继续往下，下面这些东西就是实现函数的功能，就一系列的逻辑处理，最后这也有一个return指令。在return之前，有一些点点点省略了一些指令，但是我们知道任何一个函数在返回之前一定需要恢复上一层函数的栈帧，对吧？所以return指令前边一定有一个leave指令，或者有一个move+pop指令，我们都可以把这个残缺的代码给它补齐。好，所以刚才总结的这个代码框架可以作为大家分析呃汇编语言题目的一个思路。好的，

那以上就是这一小节的全部内容。
