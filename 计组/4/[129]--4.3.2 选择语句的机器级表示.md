好的，通过之前的学习，我们已经知道了一条汇编语言指令它的基本构成，我们要怎么去看懂一条汇编语言指令？那在这个小节中，我们要学习怎么用一系列的汇编语言指令来实现我们熟悉的选择语句，也就是if else这种分支结构的。呃，程序好，那么我们知道我们的指令是存储在主存里边的，那为了讨论方便呃，在这给大家画了一个示意图，这是主存。然后每一条指令，假设就是占四个字节，

左边这一溜是地址，那CPU会一条一条的呃，取出这些指令，并且执行。而CPU内部有一个比较特别的寄存器，叫做程序计数器PC，这个寄存器它的作用就是会指向下一条即将执行的指令。那假设现在已经执行到三这条指令，那下一条需要执行的指令就是四对吧？所以PC会指向四这条指令，它所存储的呃这个地址。那需要补充说明的是，在叉八六处理器当中，程序计数器PC通常被称为IP IP寄存器。英文缩写是instruction pointer，

就是指令指针的意思好，所以如果大家在做题的时候，你看到题目里边说IP寄存器，那你要知道IP寄存器其实它就是程序计数器。反正就是用于指向下一条要执行的指令的好，这是大家做题的时候有可能看到的一个别名IP寄存器，其实就是程序计数器PC。那我们知道，当我们执行一条指令的时候，程序计数器PC会自动的加一，这儿的加一意思是指向下一条应该执行的指令，一条一条往后指。也就是说，这些指令的执行通常是顺序进行的，但是在有的时候，

这个程序的执行不一定按照顺序一条一条往后。比如我们很熟悉的if else这种语句，它就不是一条一条往后执行的，有可能在执行了。if这个呃判断语句之后，它就会跳过中间的某些语句。转而执行后面的这些语句程序的执行流，有可能会出现这种分支的结构，那这是C语言的视角，那么在汇编语言视角，我们要如何实现这种程序执行流的跳转呢？那这就需要用到转移类的指令好，还是刚才这个例子，假设当前正在执行的指令是这一条指令，三而PC会自动地指向下一条即将执行的指令。

程序的执行流默认是顺序进行的，那现在假设当前执行的这条指令刚好是一个无条件转移指令。jump指令，那情况就会变得不太一样。当前CPU正在执行的是这条jump指令，那这条指令的意思是说要把程序计数器PC的值，让它直接跳转到幺二八这个地址。所以执行这条指令的效果，就是会让PC从原先指向幺幺二这个位置，转而指向幺二八这个位置。那由于PC现在指向幺二八这条指令，所以接下来CPU会执行的就是指令八。好，那这就是无条件转移指令的一个作用。无条件转移指令的格式是这样的。

jmp后面跟一个地址，那执行这条指令的作用就是会让PC。无条件的转移到啊，我们这儿给出的这个地址，那这个地址参数可以像这个例子一样直接用常数用常量的方式给出。当然，这个地址也可以是来自于某一个寄存器，比如eax寄存器里边，此时它的值刚好是一二八，那么jump e as。x意思就是要让PC。跳转到eax里边呃，所指向的这个地址，那效果就等同于上面这条指令会跳转到幺二八。好，

那还有一种写法，我们的这个地址参数也可以是来自于主存，比如说jump九九九，那如果说此时主存地址九九九里边当中的内容。呃，是幺二八，那么这条指令它的执行效果就等同于jump幺二八。好，所以jump指令和我们之前学过的那些指令呃是一样的，这个地址参数就是jump指令的操作数，而操作数无非就是来自于寄存器或者来自于主存。又或者是直接给出一个常数，如果来自于寄存器，那么直接标明寄存器的名字就可以，如果来自于主存，

那么肯定会有一个地址，外面加上一个中括号。好，所以学习了之前的内容，这个地方应该不难理解好，那现在问题来了，如果一个汇编语言程序员，他在编程的时候只能用我们这儿提到的这三种方式。来书写jump指令，那是不是意味着这个程序员他必须要知道？它接下来跳转到的这个指令。放到内存里边之后，是被存储在什么地址对吧？但是程序员怎么能知道自己的这条指令最终会被放到什么位置呢？所以如果只能使用呃，

刚才介绍的这三种方法来写jump指令的话，那对于程序员来说会非常难受。因此，为了解决这个问题，我们可以用这样的方式来书写呃jump指令左边这个例子，是用刚才说的那种方法。jump幺幺六意思是要让PC直接跳转到幺幺六这个位置，那接下来就会执行这条指令，对吧？那如果我们改一下这段程序的存储地址。改成从二零零开始存储，那刚才这条指令是不是就会执行错误啊？我们本来是想让PC跳转到呃这条指令。而这条指令现在是存在二幺六这个位置，所以如果直接在jump里边写死幺幺六的话，

这么编写程序是很不灵活的。好，那可以怎么办呢？可以用右边这种方法来书写程序，我们想让PC跳转到这个位置。执行这条指令，那可以用汇编语言里边的标号去锚定一个位置，就像这。在汇编语言里面，如果你看到这样的一个冒号，那就说明这是一个标号，它是标记了一个位置好，那现在你再来看这条jump指令。jump next意思就是让PC。jump跳转到next这个标号所指明的这个位置，

那右边这种写法是不是要比左边这种要灵活的多？好，所以总结一下无条件转移指令，有这样的四种写法，那更灵活的还是第四种方法可以直接用标号去锚定一个位置。当然，这个标号的名字你可以自己取，这儿我们把它取名为next，你也可以取其他的名字，比如说什么l1l2等等，只要你愿意。就是用一个合适的名字去锚定这个位置好，那通过之前的学习，你会发现jump指令它的作用是不是有点，类似于C语言里边的go to语句啊。

只要执行这条指令PC，一定是会跳转的，就不用判断任何条件。那显然，这种无条件转移指令无法用于实现我们熟悉的if else这种语句对吧？因为if else这种语句你得先判断。呃，是否满足某个条件，然后根据这个条件是否满足再来决定是否要跳转程序的执行流。好，那么为了实现这种更复杂的功能，汇编语言提供了一些条件转移指令，这我把它写成j叉叉叉。这同样是jump的意思，然后后面的这些叉叉叉呃有可能出现这样的一些情况。

这些条件转移指令的写法和无条件转移指令是一样的，都是一个操作码，后面跟上一个地址。但是是否要跳转到这个地址呢？需要进行一定的条件判断，比如je这个指令就是要判断。两个数a和b是否相等？如果它们相等才跳转到这个地址，而jne它是判断a和b两个数是否不相等？如果不相等的话，就跳转到这个地址，那我们如何对比a和b两个数呢？需要使用到另一条指令叫做cmp。就是比较。cmp后面跟两个操作数a和b，

意思就是比较a和b两个数。在我们使用任何一条条件转移指令之前，通常都需要先使用cmp这条指令。比如我们可以这么写cmp eaxebx，意思就是对比eax和ebx两个寄存器里边的值。左边这个数就是我们所谓的a右边这个数就是所谓的b，好在这个指令之后，我们跟上一个呃条件转移指令jg。它的作用是，如果a大于b，那么PC进行跳转，因此结合上面这条指令可以知道。下面这个指令它的执行效果就是，如果eax大于ebx，那么就会跳转到。

next，这个标号所指明的位置应该很好理解，对吧？那刚才的这个例子当中，我们比较的两个数a和b。分别是来自于两个寄存器那a和b两个数，其实还可以来自于主存或者也可以直接指明一个常量。那具体的写法，我们之前已经学过，如果来自寄存器，你就直接给出寄存器的名字，如果来自主存，那么你需要打一个中括号，中括号里边给出主存的地址。如果是常量的话，

那你直接在指令中给出一个常数就可以。好，那接下来我们来看一下这些条件转移指令呃，你应该怎么去记忆？首先最明显的一个特征是以j开头的，对吧？jump而后边的这些字母，它的含义，如果你结合英文，其实并不难记，首先j是jump对吧？jump when equal。所以je的意思就是当e口a和b相等的时候进行jump。jne就是jump when not equal。当a和b不相等的时候进行跳转。

那jg就是jump when greater than当a大于b的时候进行跳转。jge的意思是jump when greater than or equal to，所以jge的意思是当a大于或者等于b的时候进行跳转。好，那类似的GL的意思是jump when less than当a小于b的时候进行跳转。而le是when less than or equal to当a小于或者等于b的时候进行跳转。好，所以虽然这些条件转移指令比较多，但是你结合英文的话并不难记忆好，那接下来我们利用刚才学习的条件转移指令来把这一段C语言程序尝试着把它翻译为汇编语言。的形式长这个样子好，那为了讨论方便我们假设a这个变量呃，会被我们放到eax寄存器里边。b这个变量会被我们放到ebx寄存器，然后c这个变量会被我们放到ecx寄存器当中。

那首先前边两条指令，我们会先给a和b呃两个变量进行赋值，它们的值分别是七和六。好，接下来我们需要对比a和b的大小，所以就需要使用到compile指令compile a和b。在compile的后面，通常会紧接着跟一个条件转移指令jgg是什么greater than对吧？当a大于b的时候，jump跳转到next所标注的这个位置。好，所以执行这条指令的效果就是，如果a大于b的话，那么就会move。eax到ecx当中，

也就是把a的值复制到c里边好，这是当a greater than b大于b的时候。会跳转到这个位置，那如果不满足这个条件呢？如果a不大于b，那么程序计数器PC它是自动的指向下一条相邻的。这个指令对吧？也就是不进行跳转。那这条指令做的事情就是把ebx的值复制到ecx也就实现啊，这个逻辑。好，那后面这个地方又紧接着跟了一条无条件转移指令jump。无条件转移到end这个地方。也就是要跳过这一句代码。好，

那大家可以暂停来体会一下，那可以看到刚才我们把else部分的逻辑是写在了呃前边这个位置。然后把if这个部分的逻辑是写在了后面这个位置，这刚好和我们C语言的这种if else的书写习惯是反过来的，对吧？先写else的部分。那如果觉得这么写很奇怪，我们也可以换一种写法呃，变化在这个地方，我们改了一下刚才的条件转移指令。刚才我们是用jg greater than就是当a大于b的时候跳转到后面这地方。现在我们换一条条件转移指令。jr e.gle的意思是什么？jump when less than or equal to也就是当a小于或者等于b的时候。

进行跳转。所以这种写法相当于是取了a大于b的一个呃否命题，对吧？好，那这么执行的话，就相当于前面这compile对比a和b呃两个变量，接下来执行这条指令。如果a小于等于b。那么就会转移到next，也就是转移到这个地方。执行这句。这句的作用是把b的值赋给c，也就实现了else所对应的逻辑。当满足条件的时候才进行转移。但是如果这个条件不满足，

那么执行完这个指令之后PC应该是指向下一条指令，对吧？也就是不会发生跳转，那上面这条指令是把a的值赋给了c，也就实现了if。所对应的逻辑。好，所以换了一个条件转移指令，我们就可以把if啊对应的逻辑写在前边else对应的逻辑写在后面。这样就和C语言对应起来了，只不过我们这儿的这个条件转移指令是取了if条件的否命题。a大于b的否命题就是a不大于b，对吧？也就a小于等于b。好，

那学到这儿，我们再回来看一下一九年的这个真题啊，你会看到这儿有两个熟悉的家伙，首先compel。那左边这个数也就我们所谓的数a，它是来自于主存，因为打了一个中括号，然后右边这个数b，它是一个常数。那上面这条指令是说要对比a和b，接下来jlejle对应的是什么less than or equal对吧？也就是如果a小于或者等于b，那么就jump jump到这样的一个位置。那在这个题目当中，他也给出了这些汇编语言对应的C语言这两条指令，

对应的就是ifn大于一。那你看一下这两条汇编语言，指令它的写法是不是类似于刚才我们介绍的第二种方法，这个条件转移指令是取了n大于一的否命题。n小于或者等于一的时候进行转移，对吧？jump when less than or equal？那显然，这个主存地址里边存储的就是n这个变量，把n和一进行对比，如果n小于等于一的话。就jump到这样的一个地址，好现在问题又来了呃，它的这个地址的写法看起来像是直接给了一个16进制数对吧？因为后面加了一个h嘛。

f1看起来像是一个16进制数，三五看起来也是一个16进制，不过对于这个题目来说，这儿的f1你得把它理解为是一个标号。你会看到呃，这个函数的名字就叫f1，所以这个函数的呃起始地址通常会为我们用呃函数名进行标记。也就是f1打一个冒号，因此这f1加三五h的意思是要从这个函数的第一条指令。所对应的这个地址偏移三五h。那偏移三五h之后得到的地址就是这样的一个地址，这题目怕你看不明白，已经用小括号给你说明了。那可以看到它所对应的就是这个地址，对吧？

好，所以执行这一条呃条件转移指令的效果就是。如果n小于等于一的话，那么PC就会跳转到这个地方。所以接下来就会执行这一条汇编语言指令，那前边这一串乱七八糟的东西是机器语言的表示。好，所以这个地方想跟大家强调的是汇编语言程序员，在用汇编语言写代码的时候，通常会以函数名。作为标号去标注一个函数，它的起始地址那在进行条件转移的时候就可以从这个函数的起始地址进行偏移。用这样的方式去计算出呃，要跳转到什么位置？那同样的道理在下面这个地方，

你看到一条无条件转移指令jump它跳转到哪呢？跳转到f1+3 AH。现在你知道了这个f1指的是这个函数，它的起始地址也就是它的第一条指令所存储的这个地址。好的，那这就是用汇编语言实现呃分支结构程序的一个原理。我们学习了无条件转移指令和一系列的条件转移指令，条件转移指令通常要和compile指令呃连起来用。compile指令需要说明，要对比的两个数a和b分别来自于什么地方？紧接着，条件转移，指令根据a和b两个数的呃一个大小关系去判断到底要不要转移。好的，那以上就是这小节的全部内容，

那最后再给大家扩展一个呃底层的原理。我们来解释一下compile指令，它的硬件实现。直接给出结论，实际上compile a和b对比a和b的大小在背后是做了a-b这样的一个减法运算。并且这个减法运算会产生ofzfzfsf这些标志位。当然，还会产生其他的标志位。那这些标志位是什么意思？以及如何产生我们在第二章已经学过，对吧？所以CPU执行这条指令，在背后完成的事情就是。让alu计算a和b两个数相减，这个运算。

这个减法运算会产生呃，我们熟悉的这几个标志位的信息，那每个标志位是如何确定以及如何产生的？忘了的同学可以回去复习第二章。好，那这次运算产生的这几个标志位会被自动的存放到CPU内部的一个呃寄存器当中。一个很特别的寄存器叫做psw程序状态字寄存器。psw是英文缩写，当然在英特尔的CPU当中通常会把啊，这个寄存器称为标志寄存器。比如在英特尔的八零八六，这块CPU当中就有一个16比特的标志寄存器。其中，第零个比特用于保存呃CF这个标志位，然后第六个比特用于保存zf这个标志位。

sf和of也有相对应的位置。事实上，当alu每进行一次运算之后，都会产生相应的这些标志位，不管是加法运算，减法运算还是乘法运算，除法运算等等。都会产生相应的这些标志位，而每一次运算之后的这些标志信息都会被自动的呃存放到标志寄存器当中。每进行一次运算，就会把上一次产生的这些标志信息给覆盖掉。好，所以每执行一条运算指令之后，是不是就可以根据啊？这个寄存器里边相应的这些标志位。

去判断刚才的这个运算，它的结果到底是什么？运算的结果是否为零？运算的结果为正还是为负？运算的结果有没有发生溢出？等等。这些信息都可以从标志寄存器当中获得。好，那回到compile指令，compile指令背后做的是一个减法运算，产生相应的标志位。被保存到标志寄存器当中，紧接着compile指令的后面通常会跟一条条件转移指令。那CPU如何判断这个条件是否满足呢？就可以根据刚才我们说到的这些标志位。

比如jne这条指令，它是当a不等于b的时候进行跳转，那我们知道compile指令背后是做了减法，对吧？如果a不等于b的话。刚才这个减法的结果肯定是非零的。a-b的结果肯定非零，对吧？而如果结果非零，那么zf标志为。应该是零。好，因此CPU在执行这条指令的时候，本质上它就是去看一下zf这个标志位是否等于零。如果等于零，

那么就满足条件就jump进行跳转。而如果不满足zf=0这个条件，那么就不进行跳转。好，那在这个PPT当中，我也给大家整理了呃，每一条条件转移指令，它在背后是要判断哪些标志位的信息？有兴趣，有能力的同学可以自己琢磨一下，好的，那么这就是compile指令的底层原理，本质上是进行了一个减法，然后条件转移，指令再根据减法所产生的这几个标志位。

去判断是否满足这条指令所对应的条件，以此来判断是否发生转移。好的，那么关于这几个标志位是什么含义以及怎么得到忘了的同学可以再回去复习第二章的内容？好的，那以上就是这个视频的全部内容。
