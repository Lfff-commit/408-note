各位同学大家好，从这个小节开始，我们会进入第六章总线相关的学习，那么关于总线的概念，我们其实在之前的讲解当中已经有过很多的接触。虽然我们没有详细的学习，但是相信大家对总线的作用啊，已经有了一个初步的感性认识，就是说CPU可以通过地址总线。可以通过地址总线给主存，或者给打印机给硬盘这些发送地址信息，所以你看这个地方CPU和地址总线。中间有一根连线，然后硬盘，硬盘和地址总线之间也有一根连线，

主存和这个地址总线之间也有一根连线，那除了地址，总线之外。CPU可以通过数据总线和主存啊，打印机啊，硬盘啊之类的，其他的一些硬件部件进行数据的传输数据总线。有可能并行的传递32比特甚至更多个比特的数据。那这个特性是如何实现的？我们一会再探讨好最后啊，还有一个叫控制总线就是CPU，可以通过控制总线。给其他的这些硬件部件发送一些控制的信号，控制的电信号好，那接下来我们来解释一下为什么？

一个数据总线或者一个地址总线，它可以并行的传递很多个比特的信息呃，原因是每个总线它里边可能会包含很多根信号线。比如说我们把这个数据总线给它拆解一下，可能这个数据总线的内部是长这个样子，一个数据总线它里边有可能会包含呃，像这个图里边就是画了四。四根信号线当然了，如果想要并行的传递32比特的数据，那么这个数据总线内部就可以包含32根信号线。所以比如cpu呃，想给主存发送四个比特的数据，什么零一零一那是不是就意味着cpu只需要给数据总线的？啊，这四根线上分别加上高低电平信号就可以那么零一零一这四个数据。

是不是可以被主存接收，也可以被呃硬盘接收，还可以被打印机接收，因为这些高低电平信号，只要啊加到上面这四根线上。那么，由于所有的这些硬件部件都是连在这个数据总线上的，所以所有的这些部件是不是都可以通过这个数据总线来接收CPU，要发给他们的信号。好，所以像上面这个图，里面我们就画了四根信号线，由四根信号线组成了一整根的总线，所以我们平时通常说的一根总线。一根数据总线或者一根地址总线，

虽然说的是一根，但是这个总线内部有可能是包含了多根信号线的，那到底要包含几根？这个主要看我们的具体需求。如果想要并行的传递32个比特，那么我们就可以让里边包含32根信号线，然后通过这个图也可以很直观的体会到我们所有的硬件。只要接到这个数据，总线上那么都可以通过这根总线来传递数据，并且同一时刻可以传递四个比特的数据。然而，我们想一下，刚才我们是说CPU往这个总线上面呃加了四个高低电平信号，那么这四根信号线是不是就会带有零一零一这样的呃一些数据的信息？好，

现在的问题是这样的，假设此时SSD这个固态硬盘，它也想往这个数据总线上传送数据，比如说它传送的是什么？一一一。四个一，然后全部加高电平信号，那么这个时候是不是就会导致每一条信号线上面的信号会有冲突，对吧？CPU想发的是零，但是这个SSD往上面发送的是一，那在这种情况下，其他部件检测到这根线的信号应该到底是零还是一呢？就没办法判断了。所以对于一根这样的数据总线来说，

同一时刻只能有一个部件往上面发送数据，但是可以有多个部件同时接收数据，这也很好理解吧。CPU往上面发送，比如说零一零一这样的呃，四个高低电平的信号，那么由于所有的这些部件都是直接呃和这个总线相连的，所以所有的这些部件是不是都可以从呃它们所连接的这个？这检测出高低电平信号，因此虽然只有一个部件可以往这个总线上发送数据，然而可以有多个部件同时接收数据。好，那这就是总线的物理实线。好，那大家现在可以暂停听课，

先把自己的那个手机或者电脑给拆了啊，看一下你的手机或者电脑的主板上是不是可以看到这样一根一根细细的线？那这些线其实就是总线，那我们的电脑主板上会留下这样一个一个的插槽，比如这个插槽就是插CPU的，然后上边这个插槽就是用来插内存条的。那由于这个插槽的呃里边的线路已经和总线进行了连接，那么只要我们把主存和CPU给他插上去，那他们俩就可以通过这个主板上面的这些总线。来进行数据的交互了，只要留出对应的接口，那么我们可以很方便的在这个主板上扩展一些硬件部件。好，那现在我们对总线的物理原理啊，有了简单初步的认识，

接下来我们正式进入啊，这一章的第一小节的内容。先来看总线的定义。总线是一组为多个部件分时共享的公共的信息传送线路。下面这个图是我们啊王道书里第五章给出的一个图啊，上面的部分是CPU。CPU的运算部件，还有CPU的这个控制单元，那CPU和主存以及呃各种各样的输入输出设备之间会通过一组系统总线。啊来进行数据的交互以及控制信号的收发，早期的计算机由于外部设备的种类非常少，比如说CPU只需要和一种特定的外部设备。和纸袋机进行数据的交互，那么由于外部设备少，因此当我们想要把外部设备接上计算机的时候，

我们就可以用分散连接的方式。就是建立一条专门的这种呃数据传送的线路。但是随着计算机技术的发展，外部设备的数量变得越来越多，因此如果我们每增加一个外部设备，都增加一个专门的线路来连接的话，那么显然这种方式是不利于。于增减外部设备的好，所以为了解决外部设备和主机之间连接的灵活性的问题，人们就引入了这种总线连接的方式。每当我们要增加一个外部设备的时候，只需要把这个外部设备连到总线上就可以，就像我们刚才说过的，这个插上一个芯片，或者拔掉一个芯片这样。

就是这么简单好，所以这就是为什么要采用总线结构的一个原因，那么啊，可以看到总线，它有两个很明显的特点。一个叫分时，另一个叫共享，很好理解吧，所谓共享就是说所有的这些硬件部件都是共享的，来使用这个总线的，这是共享的特性。那分时的特性就是说啊，同一时刻只允许有一个部件往这个总线上发送信息。因此，如果多个部件都想要往总线上发送信息，

那么显然它们需要分别在不同的时间内来使用这个总线，那这就是所谓的分时的特性。接下来，我们要重点关注的是总线的分类。那我们可以从不同的维度对总线进行分类，第一个维度我们可以按数据的传输格式。把总线分为串行总线和并行总线，好来解释一下什么叫串行总线，什么叫并行总线。假设a要给b发送四个比特的数据，一一零一，那么如果采用的是串行总线，就意味着a只能一个比特，一个比特的给b来发送，这呃四位的数据。

所以b接收数据也是一个比特，一个比特的接收的，而如果采用的是并行总线，那么就意味着a可以同时并行的。给b发送多个比特的数据。串行意味着每次只能传一位，而并行意味着每次可以传多位。那显然，由于串行总线每次只需要传第一个比特的数据。因此，我们只需要用一根传输线，是不是就可以实现这样的一个串行总线？所以，串行总线实现起来成本是比较低的。当然了，

除了它成本低之外，还有一个原因是这种只有一根传输线的串行总线，它的抗干扰能力是很强的。那抗干扰能力强，就意味着呃，在长距离传输的时候，这个数据不容易发生跳变。好，另外，由于只需要一根传输线，因此当这种串行总线呃使用在计算机内部的时候，是不是也可以节省我们布线的空间？好，那这是创新总线的优点，那它的缺点也显而易见，

就是在我们这个数据发送和接收的时候，肯定需要对数据进行。拆卸和装配，通常来说，我们发送一组有意义的二进制信息，肯定至少是以比如说一个字节为单位。或者多个字节为单位，但是由于我们每次只能啊传递一个比特，因此就存在说把一个完整的信息包把它拆卸和装配这样的一个过程。那这点学过计算机网络并不难理解。好，另外我们还必须考虑到串行和并行的转换问题，比如说我们用USB给你的电脑主机发送数据都是一个比特，一个比特发送的。但是CPU不可能一个比特一个比特的来接收这些数据，

所以我们给电脑发送的这些数据虽然是串行的发送的，然而我们串行发出的这些数据还必须经过转换之后。用并行的方式就是多个比特同时送给CPU。好，那接下来再看并行总线的优点，使用并行总线传输数据啊，我们的信号线的数量肯定要比串行总线要更多。也会占用更多的布线空间，好另一个方面，当我们把多根信号线并行的布在一起的时候，这些信号线在传递信号的过程当中，由于它们之间的距离比较近。因此，有可能会造成信息的干扰，很有可能会导致二进制信息位的跳变。

那这种电气特性会导致并行总线。它的工作频率不会特别高，那这就可能导致并行总线在实际信息传输的速度上，甚至不如这个创新总线。所以并不是说并行总线，同时可以并行的发出多个比特数据，就注定它比创新总线快，这是不一定的。因为各个信号线之间的干扰问题，所以导致了并行总线在发送数据的时候，这个发送的频率是有上限的，不能特别高。而串行总线由于不存在这种信号线之间的干扰问题，因此串行总线的发送频率就可以做得很高好。所以如果一个题目说并行，

总线一定比串行总线更快。那么这种说法是。啊，站不住脚的好，那这就是串行总线与并行总线，那我们再回到刚才对总线的分类，刚才探讨的是按照数据传输的方式。把总线分为了串行和并行这样的两类，接下来我们按照总线连接的呃功能部件不同，再把总线分为片类总线，系统总线和通信总线这样的三类。先来看片内总线。片内总线就是CPU内部CPU芯片内部啊，连接各种寄存器，还有寄存器与aru，

反正就是CPU芯片内部的某一些部件之间的公共的信息通路，这就是片内。而系统总线我们之前也介绍过，就是指连接计算机系统内部各个功能部件之间的总线，比如说连接CPU和主存连接CPU和各种各样的IO接口。那通过之前的学习，我们知道系统总线又可以呃，根据这个总线传输的信息内容不同。可以进一步的划分为数据总线，地址总线和控制总线这样的三类，那这个地方我们需要重点探讨呃，这三种总线的一个数据传输方向的问题。以及每种总线应该包含多少根信息线，这样的一个问题好，那首先看数据总线。

数据总线英文缩写叫db。d是data嘛，数据，然后b是bus总线的英文就是bus，它的作用就是让各个功能部件之间传递呃，实际的这个数据信息。比如说从主存当中可以取出指令，或者说从主存当中取出操作数。这些都是通过数据总线来传输的。那数据总线应该包含多少根信号线呢？这个可以呃，参考我们的机器字长或者存储字长。如果数据总线的宽度和机器字长一致，那么这就意味着CPU可以通过一次的数据读入。就可以直接取得CPU可以处理的啊，

那么多的数据。这是数据总线和机器自长保持一致的一个好处，那另一个方面，如果数据总线让它和存储自长相关的话。那么，这就意味着每一次的主存读或者写操作，只需要用数据总线传一次数据就可以。可以想一下，如果数据总线的宽度只有存储字长的一半，那么是不是意味着我们要取出一个存储字？就需要用数据总线传两组数据才可以好，所以通常来说，我们数据总线的这个宽度位数。可以选择与机器字长一致，也可以选择与存储字长一致，

同时通过之前的学习，不难理解它的信息传递方向，肯定是双向的。好，接下来看地址，总线英文缩写是AB，因为地址是address对吧？那么CPU可以通过地址，总线发出地址信号来指明他想要访问的主存单元的地址是多少？另外，也可以通过地址总线来指明他想要访问的呃，输入输出设备是哪一个？地址总线的宽度，它的位数需要和我们的主存地址空间能保持一致。当然，

如果主存和IO设备采用统一编址的方式，那么除了主存空间之外，也需要考虑设备的数量。比如说这个主存，这个主存，它对应的地址空间是零到n。然后这个输出设备，它的编号是n+1，然后这个输入设备，它的编号是n+2，那么在这种情况下，地址总线的这个宽度至少就需要能够表示零到n+2这样一个范围的。啊，地址信息。所以当我们的这些IO设备和主存采用统一编址的这种策略的时候，

IO设备的数量也会影响到地址总线应该设置的宽度。好，那显然地址总线传递信息肯定都是单向的，一定是由CPU发送这个地址信号来指明，他想要读或者写的这个设备或者主存的地址是哪一个？因此，它的信息传输方向是单向的好，最后看控制总线英文缩写是cb，因为控制是control嘛。那控制总线内部也会包含多根控制线，每一根控制线可以负责传送一个控制信号。那它里边包含的一根一根的控制线，对于单根控制线来说，这种控制信号的传输方向肯定都是单向的。有的控制线是由CPU发出发给其他的设备，

或者发给呃主存的，而有的控制线是CPU用来接收其他的那些硬件设备给它返回的控制信号的。所以对于单根的控制线来说。这个信号的传输方向是单向的，然而对于整个控制总线来说，它的传输方向既有可能是CPU向其他设备传输，也有可能是其他设备向CPU反馈。好的，那么这是关于控制总线，那我们把刚才这三种不同功能的系统总线，再把它们罗列一遍。对了，之前我们第五章里面介绍过一个概念，叫数据通路，那大家需要注意数据通路和数据总线是有区别的。

数据通路是描述这个数据流经的路径，它是一种逻辑上的描述，而数据总线是承载这个数据流动的一个物理的媒介。事实上，除了数据总线之外，我们的地址总线是不是也可以用来传送数据？因此，地址总线也可以作为数据通路。的一个物理媒介好，所以数据通路和数据总线是有区别的，当然这一点通过之前的学习并不难理解。好，那刚才我们介绍了片内总线，它是连接CPU内部的各个部件的，而系统总线是连接计算机系统内的各个功能部件的，

就是CPU主存，还有各种各样的IO设备。然后接下来第三种通信总线，通信总线是用来连接呃各个计算机系统的。事实上，我们可以把网线理解为某一种通信总线，反正它就是用于连接计算机与计算机之间的，所以刚才我们介绍的这三种总线片类，总线系统，总线和。通信总线这三种总线，它们所连接的这个功能部件的层级是不一样的，片内总线连接的是CPU内部的各个功能部件。系统总线连接的是一台计算机，里边的呃各个功能部件，

而通信总线是连接不同的计算机之间的。一个总线。那我们在第六章里边重点要探讨的是系统总线好的，那么这个小节当中我们介绍了总线的概念与分类。所谓总线，就是一组能够给多个部件分时共享的公共的信息传送线路。根据数据的传输格式不同，可以把总线分为串行和并行这样的两种总线。大家需要注意它们各自的优缺点，另外按照总线的功能可以分为片类总线，系统总线以及通信总线，其中系统总线是我们这一章需要重点探讨的。大家需要关注呃数据总线，地址，总线和控制总线这三类总线传输了什么内容？

好，最后按照时序控制方式，我们其实还可以把总线分为同步总线和异步总线，当然这个是我们之后的小结，会详细学习的内容，所以这个小节当中我们暂时没有展开。好的，那么这一章的内容通常只会考察选择题啊，信息会比较多，但是啊，逻辑链不会特别深，不像我们前面学那几章那么复杂，有很多也是需要大家记忆的东西。当然了，大家也需要通过课后习题来进行查缺补漏，

课后习题里面会包含一些我们呃课本的正文，里边没有包括的零碎的概念，还有一些零碎的考点。所以这章的选择题大家一定需要下去做。好的，那么以上就是这小节的全部内容。
