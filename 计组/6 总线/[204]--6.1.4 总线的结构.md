各位同学大家好，在这节课我们要探讨总线的结构，那早期的总线都是共享式的总线啊，我们要了解单总线，双总线以及三总线。这几种结构。随着总线技术的发展，后来出现了分层式的总线。在英特尔的奔腾处理器呃，那个时代南北桥的这种结构是非常流行的。那再往后到了英特尔的酷睿I7啊，这个处理器的时代又出现了集成化的点对点结构。那早期的这种共享式的总线呃，它的特征是快速和慢速的设备都共用呃，一条总线，

并且呃，不管是快速还是慢速设备，它们都是需要互斥的去访问这条总线的。要分时竞争，那这样就会导致有限的总线资源竞争特别激烈，那后来分层式的这种南北桥结构。就会让快速和慢速的部件呃去分别去使用高速和低速总线就进行了一个速度的分类。那再往后，我们出现的这种点对点的结构啊，其实就是为了减少总线资源的竞争，让部件和部件之间有专用的点对点的通路。好首先看一种古老的总线结构单。总线结构。啊，这种结构主要流行在啊，

60年代末到80年代，早期可以看到。CPU主存以及各种各样的IO设备，通过一条单总线系统，总线连接在一起。在这种结构下，CPU储存和各个IO设备需要分时竞争，这条总线的使用权需要互斥访问。比如说CPU经常需要从主存当中读数据对吧？但是假如某一段时间内。一个IO设备正在给主存写数据，那么就意味着主存IO设备。这二者会占据总线的使用权，那这样的话CPU想从主存读数据，这个动作就需要等待。

那可见这种结构的缺点很明显就是冲突，会很多传输的效率也很低，并且随着CPU和主存的这个技术发展。CPU和主存之间交换信息的速度越来越快，但是CPU主存以及这些慢速的IO设备。全部都混在一条慢速的，这种系统总线上，那这样的话，这个系统总线就成了CPU和主存之间交换数据，交换信息的这个瓶颈。为了解决这个问题，在70年代到90年代出现了这种双总线的结构。那这种总线结构的思想就是CPU和慢速的IO设备之间，用一条慢速的IO总线相连。比如说键盘是一种典型的慢速IO设备，

当键盘敲下字符的时候，一个字节的这个字符信息。可以通过IO总线传给CPU那所有的这些IO设备，同样的需要互斥的去共享这条IO总线。当键盘正在呃IO总线传数据的时候，鼠标这个IO设备就暂时传不了数据，就这个意思。好，另一方面CPU和主存之间用一条更快的啊，这种存储总线也叫内存总线去进行连接。那这条更快的总线就可以更好的去适应CPU和主存之间的这种呃快速的数据传输。不过，这种结构也有很明显的缺点，就是如果IO设备要从主存读或者写数据的时候，必须经过CPU的中转。

例如，当我们用键盘敲下一个字符数据的时候，这个数据想要存入储存，那么需要先经过IO总线传给CPU。放到CPU的某一个寄存器里，然后再由CPU啊写入主存。好，所以这是啊，这种以CPU为中心的双总线结构，它的一个缺点，那为了解决这个问题。后来又出现了以存储器为中心的这种双总线结构，可以看到两条总线分别是系统总线以及存储总线。系统总线连接了CPU主存以及各个IO设备，如果IO设备想要往主存读或者写数据的时候可以呃，

不经过CPU的中转。而是直接通过系统总线和主存进行这个数据的交换，另一方面，如果CPU和主存之间需要有这种更快速的数据交换，那么就可以通过呃二者的这个存储总线。来进行。好，所以这种以存储器为中心的双总线结构，它既提高了CPU和主存之间的传输效率，同时也允许IO设备和主存之间直接的进行这个数据交换。不过，随着计算机技术的发展啊，后来出现了越来越多的快速IO设备，比如说像磁盘。磁盘这种IO设备，

它的读写速度要比呃鼠标键盘这些IO设备要快得多。所以如果磁盘和主存之间通过这样的一条慢速的系统总线来进行数据交换的话，那显然这个效率又会大打折扣。那为了解决这个问题，后来又出现了三种线的结构，这种结构流行于呃八零年代后期到九零年代。可以看到，这种总线结构是在以CPU为中心的双总线结构基础之上，在高速IO设备。和主存之间再增加一条dma总线。dma是直接内存访问的一个缩写。有了这条dma总线，高速磁盘和主存之间就有了一条专用的快速通道，进行数据的交换。磁盘和主存之间数据传输的量是比较大的，

那二者之间通过dma总线呃来快速的传输。这个动作不需要CPU过度的干预，那另一方面，其他的这些慢速的IO设备。比如说键盘这种l设备，由于它和主存之间即便交换数据也不会说呃，交换的数据量太大，对吧？就像你敲键盘呃，一秒钟能敲个十次顶天了，所以慢速设备和主存之间的数据交换呃，让CPU帮忙中转。这个动作所付出的代价是可以忍受的好，所以这种三种线的结构既保证了CPU和主存之间的快速的这种数据交换。也保证了呃，

高速l设备和主存之间的这种数据交换的速度好，这是90年代以前的三总线结构。好，截至目前，我们所介绍的一总线，二总线和三总线结构，它们都具有共享式，广播式的，这种总线的特点。会有多个节点共享同一条总线，比如说在这个三总线结构当中CPU和多个IO设备之间。就需要争抢这个IO总线的使用权，所以多个设备需要抢夺IO总线，这个是很明显的缺点。另外，

这些挂载在IO总线上面的设备，他们需要长期的无效监听以及比较什么叫监听和比较呢？可以这么去理解，假如CPU现在要通过这个IO总线给设备二。去发送一个数据，那么CPU会在这个l总线的地址线部分去指明设备二的这个呃设备地址。相当于每个设备它都会有各自的编号，对吧？那由于IO总线上挂载了很多个IO设备。那每一个设备是不是都得去监听这条IO总线，并且去比较IO总线的地址线上面给出的设备编号是不是自己？如果是那么OK，就把这个数据接下来，如果不是，那么就不接收这个数据。

好，所以所有的挂载在共享总线上面的这些设备都必须随时随地监听，这个l总线。以确保CPU发给自己的数据不被错过。那为了解决传统的共享式总线的这些问题，随着技术的发展，差不多到呃90年代后期。随着英特尔的奔腾，处理器的这个呃流行就开始出现了这种分层的多总线结构。这种总线结构的思想，就是会按照设备的速度进行分层连接，并且引入了两块呃，专门用于管理总线传输的。芯片分别叫做北桥芯片和南桥芯片，连接北桥芯片的这些总线。

它们都是高速总线，例如像显卡以及内存，这些吞吐量很大的呃节点就可以连接在北桥芯片上。好，另外南桥芯片这边，它专门用于呃连接低速的各类设备，比如说鼠标键盘。以及各类USB设备。好这么一画，你应该能够看到分层这个特性了，对吧？北桥芯片连接的各个节点，把它视为高速的这一层。而南桥芯片连接的各个节点，把它视为低速的这一层。

北桥管高速，南桥管低速好，接下来我们简单的拆解一下这个图，首先看上面北桥芯片。它的英文缩写是mch，是内存控制器，集线器这样的一个缩写。它负责连接CPU。主存显卡以及南桥芯片，北桥芯片内部集成了内存控制器。内存控制器和主存之间通过内存总线进行连接。当CPU要从主存读或者写数据的时候，就是通过这条FSB总线。和北桥芯片先进行一个呃传输，然后北桥芯片的内存控制器最终再通过内存总线。

完成数据的读写。好，另外看左边这个地方北桥芯片还连接了图形卡，图形卡就是显卡，显卡的内部有显存。显存和内存本质没有什么区别，都是用来存放数据的，只不过显存的内部存放的是你的屏幕，上面要显示的那些图像的数据。显卡和CPU之间的这个数据传输，也要北桥芯片来进行中转。北桥芯片的本质是一个dma控制器，什么是dma控制器？大家会在呃计算机组成原理。输入输出系统那个部分进行学习。

好，这是北桥芯片，好，再来看南桥芯片，南桥和北桥之间用一条桥尖总线进行连接。南桥芯片的英文缩写是ich，它是输入输出控制器，集线器的一个缩写。南桥芯片负责连接各种各样的低速IO设备。并且提供了各种各样的IO接口，比如说USB的接口，以及还会有一些IO的扩展槽。通过IO扩展槽，就可以在主机上连接更多样的IO设备好，这是北桥和南桥芯片稍作了解即可，

不用死记硬背。二者可以统称为桥接器。那整个系统当中最快的一条总线是FSB总线，中文翻译叫前端总线。由于这条总线是连接CPU和北桥芯片的，所以在有的地方也会把它归类为处理器总线。好，这是最快的一条总线，那第二块的总线呃，一般是这个内存总线也可以称为存储器总线。内存的读写速度本身也很快，对吧？所以我们必须保证这条总线的带宽。那另外，这条PCI总线它的速度也比较快，

再往下这条桥尖总线连接北桥和南桥这条总线的速度仅次于FSB内存总线和PCI总线。因为这条总线，它需要承接所有的呃，来自于南桥芯片的这些数据的传输，对吧？虽然每一个IO设备，它的呃数据的传输速度都不会特别快，但是如果所有的IO设备汇聚在一起，那么呃它的这个数据吞吐量也是不容小觑的。所以这条桥间总线的速度也要比较快好，那么再往下和南桥芯片相连接的这些呃，不管是USB。SATA isa以及甚至上面这种PCI总线，它们都属于IO总线的范畴。PCI总线在呃90年代属于IO总线当中呃更快的那一类总线，

那其他的USB is a以及SATA这些总线的速度就要慢得多。好，所以整体分析下来，我们得出这样的结论，越靠近CPU的总线速度就越快。最快的以及最值得我们注意的就是FSB总线。简单了解一下，它是一种同步总线并行传输，并且是半双工的，这种特性半双工的意思是说FSB这种总线，它既支持从上往下的传输。也支持从下往上的呃，这个方向的数据传输，只不过在同一时刻只支持其中的一个方向。不能同时双向的收发，

这是半双工的含义。那为了确保这个总线的带宽，这种总线它呃采用了四倍并发技术，在每一个总线的时钟周期内可以传输四次数据。在这种结构之下，总线的这个时钟信号是由北桥芯片发出的，每一个时钟传四次数据。来看二五年的20题，这道题目它通篇没有提FSB总线这个词。但是这道题目，它就是根据FSB前端总线呃，它的特性来进行题目设计的。说某处理器总线采用了同步并行的传输方式，同步传输意味着有一个统一的时钟信号。那像FSB总线，

它的时钟信号就是由北桥芯片发出的并行传输，意味着每次传输可以并行的传多个比特。题目的后面说总线宽度是64位，那么就是说每次可以并行的传64位好，后面又说。每个总线时钟周期可以传输四次数据，这就是刚才我们提到的4倍并发技术。好说，总线的工作频率是一三三三兆赫兹，最后问我们总线的带宽是多少？好，大家可以暂停做一下，十秒钟后公布答案。三二一这题选择a选项。我相信有同学会错选b选项，

对吧？如果你选了b，那么请把不愧是我打在公屏上。那这个题目的坑来自于工作频率这个概念。一定一定要注意区分总线的时钟频率和工作频率，这两个概念。时钟频率指的是发出总线时钟信号的那个芯片，一秒钟发出几个时钟信号。而工作频率指的是每秒钟可以传多少次数据。题目告诉我们，每个时钟周期可以传四次数据，那就说明工作频率是时钟频率的4倍，对吧？而题目直接给了工作频率，一秒钟可以传一三三三兆这么多次的数据。

所以最终的总线带宽，我们只需要用工作频率乘以总线宽度就可以，那答案就是十点六六GB每秒。好，可以看到二五年这道真题，它是按照FSB这种总线的特性来进行出题的。但其实它并不要求我们呃去记住FSB的特性，对吧？题目会把该给的条件全部给你交代清楚。好，所以本节课我们介绍的各类总线标准，大家稍作了解即可，不必死记硬背。好，那么在英特尔的奔腾处理器时代，

这个北桥芯片发出的时钟频率通常是三三三兆赫兹。总线宽度是64位，那么总线带宽算出来的结果是十点六六GB每秒。你看和二五年这道题的正确答案一模一样，对吧？所以出题老师在出这些题目的时候，其实并不是空穴来风，他们通常会参照现实生活中。真实存在的某一款总线，去把它的一些核心特征进行交代，然后给大家进行计算，用这种方式啊，变成一个可考的题目。好，那么在这种南北桥结构当中FSB这条总线，

它最终成为了系统的瓶颈。虽然这个总线的带宽很大。但是，由于主存显卡还有南桥芯片上连接的各类IO设备，所有的这些数据最终都会汇聚到FSB总线这。所以，随着内存显卡还有各类设备的速度进一步的提升，那fsp总线已经无力承担这么多节点的这个数据交换了。为了解决这个问题。随着英特尔酷睿爱七处理器的问世，一种新的总线结构，集成化的点对点互连结构开始流行起来。零八年第一代的英特尔酷睿I7问世，这个是酷睿I7处理器，它是一个四核处理器。

可以看到CPU的内部集成了三个内存控制器。原本内存控制器是被集成在这个北桥芯片里边的，但是在酷睿I7处理器之后。呃，内存控制器被集成到了CPU的内部，并且可以看到有三个内存控制器。每个存控可以通过一条点对点的总线和内存条进行连接。总共可以连三组内存条，这三组内存条可以呃，同时支持并行的呃数据传输。我们把它称为三通道的内存。以前在南北桥芯片那种结构当中CPU要缓存的话，需要先通过FSB前端总线。把仿存的一些呃信息命令先传到北桥芯片，再通过北桥芯片的存控。

最终去完成主存的读写操作。而现在这种点对点互连的结构当中，缓存的路径变短了CPU的内部，直接集成了存控。CPU只需要把呃主存的这个读写地址以及读写命令这些信息放入存控当中，那么存控就可以去操作主存完成读写。那么，我们知道CPU和主存之间的数据交换是非常频繁的，所以每一次的缓存它的这个路径变短，这就意味着缓存的呃效率可以大幅提升。好那么和CPU直接连接的这块芯片叫做ioh中文翻译，叫输入输出集线器。它相当于一个阉割版的北桥芯片，就是这块芯片，它不再管理啊，

这个内存的读写。不过，像显卡这一类呃，高速设备的这种连接还是通过ioh这块芯片去进行的。那ioh的下方这儿依然是我们熟悉的南桥芯片，它负责连接呃低速的IO设备。好，接下来我们介绍几个很有这种啊，出题潜质的总线。分别是qpi总线。PCIE总线以及dmi总线。这是三种总线标准。这些总线都不是共享式的总线，而是点对点的总线。比如说这条qpi总线，

它只负责CPU和ioh这块芯片之间的数据传输。所以这种点对点的连接就不存在说总线的竞争问题，你想给我传的时候随时传，我想给你传的时候也随时传。这条线路只为你我服务，就这个意思qpi总线是英特尔提出的一种总线标准。这是它的英文全称，这是它的中文翻译。它是一种点对点的全双工串行总线。全双工意味着。从上往下或者从下往上，双向的这个数据传输都可以同时进行。那串行总线意味着这种总线的工作频率可以做的特别高。它是基于包传输的思想，每个qpi数据包占80位，

其中只有64位是有效信息。剩余的16位是校验信息。那什么叫包传输？如果学过计算机网络，大家应该并不陌生。在计算机网络当中，物理层以比特为单位进行数据的传输，那么在数据链路层。是不是需要把物理层的这些比特把它组装成针对吧？若干个比特，组成一个数据帧，就像这个地方，每64比特的有效信息会用一个数据包去传送。就是类似的道理。对于这种全双工的总线，

我们计算它的总线带宽的时候，需要把两个方向的这个啊，数据传输的速度。综合的考虑进去。像qpi总线，它的上行和下行速度都可以达到十二点八GB这样的一个速度，那么乘以二就应该是二十五点六GB每秒。那这是qpi总线的带宽。好再看dmi总线呃，它负责连接这个ioh和南桥芯片，它也是一种点对点全双工的创新总线。上行和下行的带宽是1 GB，所以两个方向的速度加起来总带宽就是2 GB每秒。dmi总线的可考性不是特别高，如果未来要考的话，

不如考PCIE总线。我们一会再回来介绍PCIE的特性，我们先看二零年的这道真题，这个题目在本节课我们就不做细讲。大家可以自己去做课后习题，以及参照我们的习题讲解视频去理解这道题目，在这儿我只是想带大家看一下这个题目它是怎么去考？qpi总线的。题目给出了qpi总线的各种特点，它是一种点对点全双工的，这个同步串行总线。可以同时进行接收和发送信息，也就是全双工，对吧？每个方向可以同时传输20位。

20位信息当中，有16位是有效数据，有四位是校验位。每个qpi数据包是80位。分两个时钟周期传送，每个时钟传两次，相当于80位的信息，它又会被拆成20位加20位，再加20位，再加20位，分四次进行传输，对吧？每20位的信息包含16位有效数据和四个交易位。那这四次的传输需要通过两个时钟周期才可以完成，每个时钟周期传两次。

然后题目贴心的给出了qpi总线的总线带宽，它的计算方法。每秒的传送次数乘以两个字节，再乘以二这儿的乘以二，就是来自于全双工通信。我们说，对于这种全双工的这种总线来说。它的总线带宽需要综合考虑两个方向的，这个数据传输速率，然后题目告诉我们它的时钟频率。带宽是多少？好，大家也可以顺手做一下，一会我们公布答案。三二。

一这个题目选择c选项。好，可以看到这道题，它虽然考察了qpi总线，但是它并不要求我们记住qpi总线的这些特性，对吧？题目会把该交代的信息全部给你交代清楚，最后他考察的本质上还是总线的性能指标的计算。好，那么做错的同学可以参照课后习题的答案以及讲解视频。现在我们回到PCIE这种总线。个人认为，这种总线的特性在未来也很有可能像qpi那么去考察。所以我们不妨进行一个预防性的学习。好PCIE，

它是一种点对点全双工的串行总线。在PCIE总线当中，有两个重要的概念，分别叫链路以及通路，一条链路可能包含多个通路。比如说ioh这块芯片，通过一条链路。和我们的显卡可以进行连接，所以链路指的是两个节点之间的，这个互相连接，那么这条链路的内部可能由多个通路组成。比如说PCIE×16这样的一条链路，意思就是说它的内部有16条通路。你可以理解为这两个节点之间有16条线相连，这16条线都会用于传输这个数据。

当然，这16条通路之间传输的这个比特并不是像并行总线那样，就是每16比特为一组，不是这样的。16条通路的这个数据是相互独立的，相当于每一条线都是一个独立的车道。那显然，如果这个车道的数量翻倍，那么整个链路它的带宽是不是也可以翻倍？所以如果一条链路包含16个通路，那么它的理论带宽就是呃，包含八个通路的，这种链路的两倍。那如果说我们要连接呃高速的这种设备，例如显卡，

那么我们就可以让一条链路内部包含的通路数呃尽可能的多。那在这个图当中可以看到ioh这块芯片，它最多可以连接36条PCIE的通路，那么这36条通路。可以进行很自由，很灵活的组合，比如说可以每16个通路为一组，那么就可以有两组链路。或者每八条通路为一组，这样就可以有四条这样的链路。当然还可以有其他更自由的组合。我们可以充分的利用这36条PCIE通路，它的一个带宽。那这些PCIE的通路，在传输数据的时候，

会把每一个有效的字节数据转换成十比特的信息来进行传输。好，那么来看一下PCIE未来有可能会怎么考？说PCIE是一个点对点的全双工串行总线。设备之间以链路相连，每条链路可以包含多个通路PCIE×n表示具有n个通路的PCIE链路。每条通路可以同时发送和接收数据，这是全双工的特性，对吧？由于PCIE采用了自同步的机制。所以在总线上并没有专门的时钟信号线，而是把每个有效字节啊，在传输前都转换成十比特的信息。以此去实现自同步的目的。那么，

在PCIE一点零的规范当中，一条通路的这个每个方向的发送接收速率是二点五g。小b每秒问，如果某计算机采用PCIE一点零×8这种接口去连接高速外设，那么这个PCIE总线的带宽是多少？好，大家暂停训练，把自己的答案打在公屏上。十秒钟后公布答案。三二。一这个题目选择c选项。我猜这个题目选a选b选c选d的人都会有，如果你选的是a选项，那么你肯定是把全双工的这个呃总线带宽的这种计算。搞错了，

全双工的总线，我们要综合考虑两个方向的数据传输，得把它累加起来。那如果选择的是d选项，那说明你已经考虑到全双工双向的这个数据传输速度，但是你可能没有理解这句话。就是每八比特的有效信息最终会被转换成十比特的，这个信息相当于有两个比特是冗余信息，对吧？这两个比特是为了实现自同步而额外添加的好，所以肯定也会有同学选d选项，而如果你选了b选项。那么你既没有考虑到八比特会编码成十比特这件事，也没有考虑到全双工这件事。好，

所以不得不说我在这道题目当中还是给大家埋了很多坑的。好这道题的思路说来也简单，我们先计算单条通路的带宽题目，已经告诉我们单条通路每个方向的这个呃数据传输速率是二点五g小b每秒。那题目最终让我们计算的是大b每秒，这个单位好，我们用一条通路的单向带宽乘以二，为什么要乘以二？因为它是全双工，对吧？两个方向的呃，速度我们都要考虑进去好单向带宽乘以二，那么就是五。g小b每秒。单条通路每秒钟可以传输5g个比特好，

最终我们要计算的是大b每秒。字节每秒，这个单位对吧？那刚好每个字节一个大比，最终会被转换成十个比特。所以每秒钟可以传5g个比特，那么其中包含了多少个字节的有效信息呢？是不是刚好除以十就可以得到这个答案？每十个比特对应一个有效字节嘛好，所以啊，最终计算的结果就是零点五g大b每秒，这是单条通路的带宽。那么，题目最终给的这个PCIE总线，它是乘以八这样的规格，

也就是说，这条PCIE链路包含了八条通路。所以我们把通路数乘以刚才计算出来的单条通路的带宽，最终就得到了答案，4g大b每秒。因此，本题选择c选项。好，那么这是PCIE总线，对了，刚才我们提到的qpi总线，它会连接CPU和ioh。这两块芯片与此同时在酷睿爱七处理器的内部四个核心之间也会两两的有qpi这种连接。可以看到，这种点对点的连接逐渐取代了共享式的总线。

在现代计算机中，变得越来越重要。好，后来随着技术的发展，ioh这块芯片和ich这个南桥芯片又进行了进一步的整合。然后就出现了这样的结构CPU和一个叫做pch的呃芯片进行连接，这块芯片相当于把南桥芯片和iOS。h芯片的那个功能集成在了一起，与此同时还增加了其他的一些功能，好这个稍作了解即可。啊，我们刚才说到的那种三芯片的结构就是CPU+ioh+ich，这种结构它是在第一代的酷睿I7当中去采用的。那从第二代的酷睿I7开始就开始流行这种双芯片的结构CPU，加pch这个芯片功能的集成度正在变得越来越高。

这种结构在我们的个人电脑上流行了很多年，直到后来各类智能手机的处理器。以及苹果的m系列芯片出现之后，这种总线的结构又发生了变化。很多手机内部使用的是高通的芯片，对吧？那像苹果手机，它使用的是a系列的芯片，几乎所有的智能手机它都不会采用这种双芯片结构，而是进一步的把pch。这个芯片的功能又集成到了CPU里边。所以这个总线的发展，从曾经的这种系统性的互联，也就是整个计算机系统内各块芯片之间的这种互联。逐渐的演变成了单芯片化，

所有的部件的连接全部都在处理器的芯片内部来完成。那二零二零年苹果的m1芯片把手机芯片的这种啊，单芯片系统的这种，这种设计思想。引入了个人电脑，于是整个计算机行业又发生了变化。所以在最近这些年，我们的共享总线正在变得越来越少，更多的是在处理器内部用点对点的这种传输线路。去实现部件与部件之间的信息交换，以前的那种共享式的总线是把总线传输线路印刷在了这个呃主板上。现在时代已经变得不一样了，那也正因如此，我们的四零八大纲也做出了这种呃符合时代的一些调整。在二零零九年到二零年之前，

四零八的大纲总线这个部分，它要求我们掌握总线仲裁的这个知识。什么叫仲裁呢？就是如果多个设备，多个节点需要使用，需要争抢一条共享总线。那么你也想用，我也想用，他也想用，对吧？这个总线的使用权到底分配给谁？这就是总线仲裁要做的事情。但是通过这节课，我们知道了我们这些年的总线发展正在从共享总线演变成点对点的总线。那既然是点对点的连接，

我就不存在说多个节点竞争同一条总线，就不存在这种问题，对吧？所以总线仲裁这个东西，随着技术的发展，它在现在已经不重要了。因此，在二零二一年的大纲当中，把总线仲裁给删掉了，那更进一步的在二二年的大纲当中。又把总线的这个总线标准，这个部分给删除了。大家在课后习题当中会看到，在二一年以前，其实很多总线标准的名字，

比如说呃pci这种缩写，还有什么usb这种缩写，什么isa？这种缩写。这些都是总线标准的名字，在二一年以前，它需要你死记硬背啊，记住这些总线标准分别有什么特性对吧？但这种纯记忆性的这种考察。其实并没有意义，所以在二二年大纲把总线标准这个考点也给删了，那这也意味着。这节课我们提到的qpi也好，PCIE也好，还有FSB也好，

这些总线标准的呃特性你都不用记，以后如果考的话，它一定会在题目当中给你完整的信息。好，另外在二二年的大纲当中，你会看到二一年之前哈总线，它是作为一个一级考点。它作为一个一级考点和什么主存和IO，这些呃属于同级的，但是随着技术的发展以及点对点总线的这个流行。那传统的总线概念其实正在变得越来越不重要，因此二二年的大纲当中，它把总线这个考点。和输入输出系统啊，合并到了一起总线，

它从一个一级考点变成了二级考点。好，所以不得不说四零八的考研大纲，它是与时俱进的。只要计算机技术在发展，那四零八的大纲也会跟着时代继续往前进。好的，那么在这节课中，我们介绍了总线结构早期的共享式的总线单，总线，双总线，三总线啊，这些结构大家稍作了解即可。对于单总线结构来说，需要特别注意啊，

如果所有的这些节点都共用一条总线，那么分时竞争会非常严重。各个节点需要互斥的去使用总线资源。那么，在南北桥这种结构当中，大家需要对北桥芯片，南桥芯片这两个经典的芯片啊，有一个印象，万一在选择题当中出现了这个术语，你要知道它说的是什么。北桥芯片管的是高速，而南桥芯片管的是低速。设备的连接，北桥芯片的内部。集成了主存控制器，

主存控制器常简称为存控，那么在南北桥结构当中FSB总线。也就是前端总线啊，是比较重要的，它的速度很快用于连接CPU和北桥，但是由于所有的数据全部集中在了FSB上。所以这条总线成为了系统吞吐的瓶颈，因此在英特尔酷睿I7的时代又出现了集成化的点对点结构。从此以后，传统的，共享式的总线正在越来越被弱化，计算机内部点对点的这种连接正在变得越来越多。在英特尔酷睿I7处理器当中，北桥芯片的这个存控内移到了CPU的内部。CPU可以直连主存，

并且支持多通道的。并行存取，那么在这种总线结构当中qpi总线曾经已经考过，还有PCIE呃，它属于第三代的IO总线。这种总线截至目前还没有考过，大家可以预防性的去了解一下呃，这种总线的特性以及把本节课我设计的那道例题给捋清楚。需要特别注意，无论是qpi总线还是PCIE总线，只要题目说它是一条全双工的呃总线。那么，在计算总线带宽的时候，你需要综合考虑两个方向的数据传输速度，把两个方向的速度加起来，

才能体现这个总线的带宽。好的，那么以上就是这节课的所有内容。
