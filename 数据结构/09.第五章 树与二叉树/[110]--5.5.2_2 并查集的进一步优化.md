好在上个视频中，我们已经学习了并查集的基本原理以及对于并查集union操作的一个优化。那我们的优化思路是让小树并入大树，每次都这么干，那这种优化思路可以保证并查集构造的树的高度不超过o的log 2n这样的一个数量级。这已经非常优秀了。好，但是由于我们以前没怎么考过，并查集，我们也不知道这个出题老师，他们想要把并查集考察到什么样的一个深度，所以为了防止出题老师不讲武德给大家。使坏，所以在这个视频中，我们再来补充一下，

对于并查集的更进一步的优化，也就是对find操作的优化。好，我们来看一下，之前我们实现的这个find操作干的事情，就是从某一个指定的节点出发，然后依据我们这个数组的信息一路往上。找到它所属的根结点对吧？比如说我们要找到11号结点，也就是l这个结点，它所属的呃集合是哪一个？那我们会从l出发。先找到e找到b再找到a。a是根结点，它是一个负值嘛，

那这样就find找到了l所属的一个集合好，那刚才我们从l1路往上找的这条路，我们把它称为查找路径。查找路径，那现在我们所谓压缩路径的这种优化方式，其实就是要使得这个查找路径让它变短。好，怎么做？刚才我们已经确定了a。已经确定了a它是根结点，并且从l出发，可以依次找到，就是这条查找路径上的各个节点。那接下来我们要做的事情，就是把查找路径上的这几个蓝色结点，

全部都把它们挂到a这个根结点的下面。也就是说l。我把它挂到a的下面。e把它挂到a的下面，然后b本来就已经在a的下面了，所以b就不动，对吧？好，那这样的话，我们在find操作，确定了根结点之后。再经过第二轮的处理，依次把这些节点都挂到了根结点的下面，那这样的话，你下一次还想要再确定l所属的集合的时候是不是只需要往上走一次就可以找到根结点了？

也就是说，从l出发，找到根结点的这个查找路径，路径的长度是不是被我们压缩了？好，这就是所谓压缩路径的一个啊做法，你看路径压缩完了之后l这个节点，它直接挂到了a的下面。然后e这个节点，它也直接挂到了a的下面，然后b这个节点，它本来就是挂在a的下面的，所以b的这个数组的值嗯，不需要修改。好，

所以这就是压缩路径的处理方式好，那代码实现也很简单，首先按照我们之前提出的那种思路，就是一个while循环简单的while循环就可以一路往上。找到x这个节点，它所属的根。找到了它所属的根像，刚才就是我们从这个11号节点l出发，一路往上找到了它的根a。也就是零号节点好，那接下来啊，我们再一次的从l这个节点出发，一路往上。找到查找路径上的各个节点，然后每一个查找路径上的节点，

我们都把它的这个数组s，也就是它的副节点指针，直接让它指向刚才我们找到的这个根节点。root那这样的话，我们通过一个简单的while循环，就把所有的这些非根结点都通通挂到了根结点的下面。然后最后同样的return返回呃，我此次查找的这个节点，它所属的根结点的编号好，那这就是对find操作的一个优化压缩路径。分为两步，第一步和之前的find操作一样，就是一个while循环先找到根，第二步再来一次while循环再分别的处理，这个查找路径上的各个顶点。

把每一个点都直接挂在这个根结点的下面，那这样的话，我们通过一次放映的操作，不仅找到了根，同时还把这个查找路径给压缩了。等下一次我还要查l所属的集合的时候呃，下一次的这个查找路径就可以被我们压缩，就可以缩短好，那这就是对于find操作的优化。那引入了这种压缩路径的优化策略之后，我们可以使得树的高度不超过o的阿尔法n，这个阿尔法是什么你不用管，反正它就是一个增长很慢很慢的一个函数。阿尔法n的增长速度要比log 2n的增长速度还要慢，同时对于常见的n值，

比如说n在一万以内或者n在1000以内，对于这些常见的n值。通常，阿尔法n的值小于等于四，那这也就意味着对于常见的n值来说。find操作的这个时间复杂度，甚至可以啊，小于等于o4，也就是几乎可以使用常数级的啊，这样的一个时间就可以完成find操作。那我们再来总结一下我们之前提出了并查集的一个最初的一个啊，实现的策略find操作就是要从指定的元素出发，一路往上找到根结点。而union操作就是要把两个呃指定的集合合并为一个，对于union操作的优化思路就是让小数合并到大数，

这可以使得啊union构造的这个数的高度不超过o的。log 2n，这已经非常好了，但是我们这个小节中又提出了啊，对find操作的优化，采用压缩路径的方式。首先找到根结点，确定了根结点之后再通过第二次的while循环把这个查找路径上的各个结点全部都摘下来，直接挂到根结点的下面。从而达到所谓压缩路径的一个效果。引入这种策略之后，我们可以使得树的高度不超过o的阿尔法n这样的一个数量级，那这个函数阿尔法n它的增长速度要。远远低于log 2n，所以啊，

find操作压缩路径的这种策略非常的优秀。总之，无论是对union的优化，还是对find的优化，我们的核心思想都是尽可能的让树变矮，因为树越矮，那find操作它的一个最坏的时间复杂度呃也会越优秀。好，那再来总结一下对find操作的这个时间复杂度和树的高度息息相关，那如果没有做任何优化的话n个呃元素所组成的集合呃，也就是n个元素所组成的。它可能长得非常非常高，高高的那n个元素，它的高度就有可能达到on这个数量级，所以find操作它的最坏时间复杂度也会达到on这个数量级。

好，那如果我们要把n个独立的元素刚开始相互独立的元素，通过多次的union操作合并为一个集合的话，那总的时间复杂度就会达到o的n方，这样的一个数量级。呃，因为要合并n个独立的元素，那肯定需要n- 1次的union，对吧？那每一次我要union两个集合的时候，是不是首先得从指定的两个元素出发，先找到两个集？集合的根结点对吧？那每一次进行union之前都得先find，先找到两个集合所属的根结点，

而刚才我们说find操作的时间复杂度是on。所以如果重复n- 1次union，那我们总的时间复杂度最坏，是不是就有可能达到o的n方这样的一个数量级好那？我们把union操作优化了之后，find操作它的最坏时间复杂度就可以达到o的log n，那将n个独立的元素通过n- 1次的union。总的时间开销就只会达到n×log 2n这样的一个数量级，那原理还是一样的，每一次union的时候先进行find，先确定两个集合的这个根结点，对吧？而每一次find的这个时间复杂度都是log 2n，所以重复n- 1次，那么总的时间复杂度不就是o的n×log 2n吗？

那最后我们再对find进行优化，采用压缩路径的这种策略。可以使得这个树高不超过o的阿尔法n这样的一个数量级，那我们把n个独立的元素经过n- 1次的u，把它们合并为同一个集合。所需要的时间开销就是n乘以阿尔法n这样的一个数量级好，那这就是对并查集的优化以及优化之后的一个时间复杂度。好，那如果还有同学觉得并查机这块学的还不够形象的话啊，可以从我们的课件里边点这个链接啊，可以使用手机pad电脑都可以哈。然后去这个网站啊，去看一下并查集的find和union是怎么一步一步来操作的？那如果你把后面这个勾就是什么union by rank这个勾给勾上，然后后面这选第一个选项。

选项的话就是会采用小数并入大数，这样的一个策略。如果不打这个勾的话，就不引入这个策略好，那前边这个如果你把它勾上的话啊，就会再引入这种压缩路径的一个策略。另外，这个网站在模拟算法的动画的时候啊，这个什么speed这个框你往左拖它会。更慢一点，然后往右拖的话，它这个动画就会更快一点，还有就是这个地方有个暂停的按钮，你先按了暂停，然后再来union或者再来find，

那你使用这个step forward就可以像调试代码。一样让这个动画一步一步的往后执行。好，所以这个大学的网站我在很多场合都给大家推荐过，大家可以去玩一玩，有助于你更形象的理解各个算法，那除了并查集以外。其实这个网站几乎涵盖了所有大纲要求掌握的算法，我在这个图里面也给大家呃标注了。同样的，通过课件点击这个链接就可以啊，把你带到一个快乐的天堂哈。
