各位同学大家好，在这个小节中我们会学习一个比较难也比较重要的一个考点，叫线索二叉树，我们会先用一个例子让大家体会线索二叉树和普通的二叉树相比，它有什么作用？然后我们会介绍线索二叉树，它的存储结构应该怎么实现，并且会介绍三种线索二叉树好，那首先来看一下线索二叉树的作用。我们从这样的一个问题出发，我们之前学过普通的二叉树，我们可以对它进行中序遍历，先序遍历和后序遍历，那对这棵二叉树进行中序遍历，应该可以得到这样的一个遍历序列。

d是第一个被访问的节点。g是第二个被访问的节点。那经过中序遍历之后，本来这些数据元素之间，它们呈现的是一个非线性的关系。但是从得到的遍历序列这个角度来看，它们之间其实是一种线性的关系，每一个元素有一个与之对应的前驱，还有一个与之对应的后继。当然，和线性表一样，第一个元素是没有前驱的，最后一个元素是没有后继的。注意，我们刚开始在讲树的时候，

我们说过一个节点，它有一个唯一的前驱，并且可能会有多个后继，这是从树本身的逻辑结构出发所定义的前驱和后继。但是刚才我们所说的一个数据元素，它的前驱和后继是基于它的遍历序列来定义的，前驱和后继。所以不要和之前的前驱后继的概念相混淆，这是基于遍历序列所定义的。好，那现在思考这样的一个问题，每当我们要对一个二叉树进行中序遍历的时候，是不是都得从根节点出发？只有我们知道根结点是哪一个才有可能对整颗二叉树进行中序遍历好，那现在的问题是有没有可能在某些场景当中，

我们只能知道某一个节点的指针。比如说g节点的指针，也就是说我们并不知道根结点是哪一个，那现在的问题是如果只给你g节点的指针，你还能不能从g节点出发？对这棵树进行中序遍历呢，也就是遍历gbeafc这些节点，那显然这种操作是完成不了的，因为g节点只有指向它孩子的指针。并不可能从g节点往回找，找到它的双清或者直接找到它的后继节点b。好，所以这是普通的二叉树存在的第一个问题，就是要对它进行遍历的时候，我们每一次都必须从根节点出发，

否则是完成不了中序遍历的。那可以回忆一下线性表，假设这些元素他们是一个线性表的话，那给你其中任意一个元素的指针，你肯定都能找到这个元素后面的所有的元素。也就是说，你对线性表的遍历并不是每一次都必须从头开始遍历的，你可以从任何一个元素开始往后遍历。但是对于二叉树来说，我们现在是做不到这样的事情的，我们每一次都只能从头开始遍历好，那这是第一个问题。接下来思考第二个问题，假设现在给你一个指定的节点，给你这个节点的指针，

那你有没有可能找到这个节点它的前驱呢？像f这个节点，它是第六个被访问到的，那它的前驱就应该是a这个节点，我们这指的是中序遍历序列当中的前驱。好，那如果只给你f这个节点的指针，那你肯定没办法找到它的前驱a，因为每一个节点只拥有向下一层的指针，不可能向上找，对吧？这是找前驱的问题。那如果要找到一个节点的前驱，我们可以这么做，我们要找的不是中序遍历序列当中的前驱吗？

那我们可以对这整颗二叉树再进行一次中序遍历。也就是从根结点出发，然后再进行中序遍历的过程当中，我们用一个指针q来记录当前被访问的结点，并且用一个指针p。用来记录当前这个节点的前驱中序，遍历的代码当中，当我们在访问一个节点的时候，是不是会对这个节点执行visit操作？那当前正在visit的这个节点，其实就是我们这的q那中序遍历过程中d节点是第一个被visit的节点，所以刚开始会让q指向d，而prey是指向nun的。因为d这个节点，它是没有前驱的，

所以它的前驱指针现在指向n好，现在我们可以对比一下q和p并没有指向同一个节点，所以我们还需要继续往后visit。那在下一个节点被visit之前，我们需要把pre指针指向当前q所指向的这个节点，然后q指针指向下一个被visit的节点。好，现在pre指针所指向的这个结点就是q指针所指向结点的中序的前驱。所以用这样的思路，我们可以不断的让q指向后一个被访问的节点，然后pre指针也跟着依次的往后移。一直到。这样的一个时刻，也就是q和p指向了同一个节点，那么就说明此时pray所指向的节点就是p所指向节点的前驱，

对吧？好，所以在一棵普通的二叉树当中，我们要找到一个指定节点的前驱，我们只能从头进行一次完整的中序遍历，才可以找到它的前驱。那如果要找到节点p的后继的话，是不是原理也是类似的？我们只需要让q再往后移一次，也就是说我们判断是否找到的条件，我们可以判断p和p。它到底是不是指向了同一个节点？当它们指向了同一个节点的时候，q所指向的节点就是p节点的后继。好，

所以在一棵普通的二叉树当中，我们要从某一个节点开始，往后遍历，这是很不方便的。同时，如果指定一个节点，要找它的前驱和找它的后继，我们必须从头开始，重新进行一次完整的中序遍历，才可以找到它的前驱和后继。因此，这些操作很不方便，所以如果在某些应用场景当中找前驱，找后继，还有便利，

这种操作会十分频繁的话，那这种普通二叉树的呃设计就会有一些缺点。所以就有人提出了所谓的线索，二叉树那来看一下这棵二叉树的中序遍历序列，我们已经知道了，我们这儿给各个节点标上了啊，他被访问的一个序号一二三四五六。那现在我们想要把这棵二叉树稍微改造一下，让他能够更方便的查找前驱，查找后继。或者是进行便利操作。那可以怎么做呢？我们之前说过，对于一颗有n个节点的二叉树来说，它总共有n+1个空链域，

也就是这些这些空指针，我们可以把它利用起来。用这些空链域指向各个节点的前驱和后继，什么意思呢？像g这个节点，它是第二个被访问到的，那它前一个被访问到的，也就是它的前区应该是d这个节点。所以我们可以让它的这个左孩子指针，让它指向d。这样的话，我们通过它左孩子指针是不是就可以找到它的前驱了？那后继也是类似的b，这个节点是g的后继，所以我们可以让它的右孩子指针指向b这个节点，

这样的话我们就可以很方便的从一个节点出发，找到它的前驱和后继。好，那像d这个节点，它是第一个被访问的节点，所以它没有前驱，那我们可以让它的这个左孩子指针就是指向n表示它没有前驱。那e这个节点，它的前驱是b，所以让它的左孩子指针指向b，右孩子指针指向它的后继a。那f这个节点，它的前驱是a，所以左孩子指向a，后继是c，

所以右孩子指针指向c。c这个节点是最后一个被访问到的，所以它的右孩子指针，我们让它指向n表示它没有后继，因此按照中序遍历的序列。对这棵二叉树进行所谓的线索化之后，所得到的就是这样的一棵二叉树，那这棵二叉树就是所谓的线索二叉树。或者更准确的来讲，应该把它称为中序线索二叉树，因为它的这些前驱和后继指针，我们制定的规则是按照中序遍历中的前驱和后继来弄的。那如果一个节点的左孩子指针和右孩子指针指向的是前驱和后继，而不是他的左右孩子的话，那我们就把这种类型的指针称为线索。

指向前驱的就是前驱线索，指向后继的就是后继线索。好，这是线索的概念，好，那当我们把这棵二叉树线索画之后，现在再来看刚才的问题，如果给你一个节点g的指针，怎么找到节点g的后继节点呢？是不是很简单，只需要通过它的后继线索，它的后继线索指向的是b，所以它的后继就是b。因此，线索二叉树想要找它的节点后继是很方便的，

那既然从一个节点出发能够找到它的后继，找后继很方便的话，那是不是也就意味着从一个节点出发，往后便利？这件事情也是可行的，因为所谓的便利，无非就是不断的找后继的过程，对吧？好，那有同学可能会说你的这个节点g它的右孩子指针本来就是指向它的后继的，所以找后继当然方便了，但是如果对于一个右孩子指针指向的是它的右孩子，而不是它的后继。这样的一个节点，又怎么找它的后继呢？

这个问题我们留在后面的小结，再来探讨。总之，这儿大家只需要知道，当我们把二叉树线索化，把它变成一棵线索，二叉树之后。找节点的前驱后继变得更方便并且便利也更方便，这儿大家只需要体会到线索二叉树的这样一个作用就行了，也就是要知道y。建立这些线索的意义是什么？为什么要建立这些线索？好，那接下来要看一下这种线索，二叉树怎么存储它呢？

和普通的二叉树相比有什么区别？在普通二叉树当中，我们只定义了一个数据data，然后还有左孩子指针和右孩子指针。但是刚才我们说了线索二叉树当中，我们的左孩子指针和右孩子指针，它有可能指向的不是左右孩子，而是指向他的前驱和后继。所以为了区别这样的两种状态，我们就需要增加这样的两个标志位啊，分别叫air tag和r tag。当l tag=0的时候，表示的是它的左孩子指针指向的真的是他的左孩子。而只有tag值等于一的时候，表示的是这个指针是一个线索，

那像左孩子指针充当的线索，指向的就是它的前驱，右孩子指针充当的线索，指向的就是它的后继。在这个图当中，我们用虚线表示线索，用实线表示左右孩子指针黄色的虚线，指的是前驱线索紫色的虚线，指的是后继线索。那之前我们说过二叉树，我们也可以把它称为二叉链表。现在我们把它改造成线索二叉树的这种节点之后，我们可以把这种线索二叉树称为线索链表。好，所以这样的一棵中序二叉树，

如果从存储的视角来看的话，就是呈现出这个样子。只有当tag位为一的时候，才表示与之对应的这个指针是一条线索。tag位为零的时候，表示的是与之对应的这指针，指向的是它的孩子。好，这是中序线索，而插数也就是说我们是根据中序遍历的序列来确定每一个节点，它的前驱和后继到底是谁？那既然有中序线索，二叉树是不是就应该有先序和后序线索？二叉树那原理是类似的，对于先序线索，

二叉树来说，我们同样的可以先在稿纸上给这些节点分别标上他们被访问的次序。abdgecf，这是先序遍历序列。它的一个次序。好，那现在我们可以按先序序列来确定各个节点，它们之间的前驱和后继的关系。并且把这n+1个空链域，把它们变成线索，那经过线索化之后所得到的先序线索，二叉树就是这个样子。其实，原理和中序线索二叉树是一样的，只不过各个节点之间前驱后继的这个顺序会有所改变。

像d这个节点，它的前驱是b，而它的左孩子指针本来是空的，所以我们可以把它左孩子指针变成线索。指向它的前驱，也就指向b节点，那g它的前驱是d，所以我们让左孩子指针指向d后继是e，所以让右孩子指针指向e。其他的就不再细聊，总之最后我们可以得到这样的一颗先序线索二叉树。那这是从逻辑视角看到一个样子，如果从存储视角来看的话，那它就应该是这个样子。还是一样的，

只有tag值为一的时候表示这个指针，它是一个线索，tag值为零的时候表示与之对应的这个指针，它是指向了啊孩子。好，这是先序线索。二叉树最后再看后序线索。二叉树原理和之前一样，只不过我们是按照后续遍历啊所得到的这个遍历序列来确定各个节点。他们的前区和后区。g的关系那g这个节点是第一个被访问到的，所以它没有前驱，因此我们需要把它左孩子指针变成它的前驱线索，而由于它没有前驱，所以这个前驱线索应该让它指向n。

而它的后继应该是d这个节点，所以让它的右孩子指针指向d那d的左指针是空的，所以可以让左指针变成线索，让它指向它的前驱，也就是。就指向g好，以此类推，其他的就不再列举，那最后经过后续线索化之后，所得到的后续线索二叉树就是这个样子。好，如果从存储视角来看的话，这棵后续线索二叉树就会呈现出这个样子好了，那这个小节我们介绍了什么是线索二叉树？其实就是利用了二叉树本身所存在的n+1个空链域，

把这些空链域把它们变成线索。也就是指向前驱或者指向后继，那我们介绍三种线索，二叉树分别是中序，先序和后序线索，二叉树。它们的区别在于，我们在确定各个节点的前驱后继关系的时候，到底是按中序遍历还是先序遍历，或者是后序遍历来确定的。那这还要补充几个，大家可能在选择题中会看到的概念比较容易混淆中序，前驱中序后继，当我们在聊到一个节点，它的中序前驱的时候。

其实指的是如果按照中序遍历遍历这棵二叉树，那么在中序遍历序列当中，它的前驱到底是谁？对吧？所以像e这个节点，它的中序前驱。它的中序前驱就应该是b。相应的，它的中序后继就应该是a。好那先序前驱，先序后继，也是类似的，就是指在先序遍历的过程当中，我们指定的这个节点，它在先序遍历序列当中的前驱到底是谁？

那在先序线索二叉树当中，不难发现它的先序前驱就是g，而它的先序后继就是c。好，那后续前驱和后续后继的概念也是类似的，这就不再赘述。当我们把一棵二叉树线索画之后，我们就可以很方便的找到一个指定节点的前驱，或者找到它的后继。那能够找到前驱后继的话，就意味着我们也可以很方便的从一个指定节点出发，进行便利操作。与普通的二叉树节点相比，我们需要在线索二叉树的这个节点的数据结构当中增加air tag和rtag这样的两个标志变量。当tag值为一的时候，

表示与它对应的这个孩子指针是一个线索，而不是指向孩子。只有tag值为零的时候，才表示孩子指针指向的真的是孩子。那一个比较重要的考点是，大家需要能够手算来画出一颗线索二叉树。当然，线索二叉树分为中序，先序和后序线索二叉树，所以在审题的时候一定需要注意。那用手算的方式线索化，其实也很简单，我们先根据它的遍历规则，确定各个节点被访问的顺序，然后像我们刚才那样，

就是给各个节点编上一个它的访问编号。接下来再把那n+1个空链域，把它们分别连上前驱和后继，也就是完成线索化，这是选择题当中有可能经常会考到的一个点。当然，这个点并不难。好的，那以上就是这一小节的全部内容。
