各位同学大家好，在这个小节中我们要学习树的存储结构，我们会学习这样的三种存储结构，分别是用顺序存储的思想实现的。双清表示法。以及顺序存储和链式存储相结合而实现的孩子表示，法最后是单纯的链式存储实现的，孩子兄弟表示法。那首先我们来回顾一下树的逻辑结构，那我们说树是一种递归定义的数据结构。一棵树，它要么是空树，要么它至少有一个根结点，而根结点的下方可能会有零棵或者多棵互不相交的子树。对于根结点a来说，

它的下方有三颗互不相交的子树，分别是BC。d那么，对于最左边这棵子树来说，b节点是子树的根，而b节点的下方又有两棵互不相交的子树。分别是以e为根，以f为根，因此可以看到树是一种递归定义的数据结构。那之前的学习中，我们主要学的是二叉树，二叉树也是一种特殊的树，在二叉树当中，我们要求一个分支节点，最多只能有两棵子树。

而这个小节，我们学习的数对于分支节点的子树的数量是没有限制的，每个分支节点可能有零棵子树，也可能有m棵子树。好，那现在我们要探讨如何用顺序存储的方式来存储树，这种数据结构那首先我们回顾一下二叉树的顺序存储是如何实现的？看看能不能找到一些灵感，那对于一棵二叉树来说，由于每个分支节点最多只能有两棵子树，因此我们可以把一棵二叉树。按照完全二叉树当中的这种节点的顺序，对各个节点进行编号，并且把这些节点。存储在数组当中和节点编号相对应的这个位置。

那在这种情况下，数组的下标就可以反映各个节点之间的逻辑关系，比如说如果根结点从数组下标为一的位置开始存放的话。那么，下标为I的节点，它的左孩子在数组当中下标应该是2I，而它的右孩子在数组当中下标应该是2 I+1。好，所以当我们对二叉树进行顺序存储的时候，我们会把这些节点的编号和完全二叉树相对应。那现在的问题是这样的思路，可不可以推广到普通的树当中呢？显然，对于一棵普通的树来说，由于每个分支节点的子树数量是不确定的。

所以我们不可能把一棵普通的树把它和所谓的完全树对应上，对吧？没有完全树这样的说法，因为每个节点它下面有几个分支是不确定的。因此，如果把这些节点按顺序存储到数组当中，那么单纯依靠数组的下标，我们是无法判断这些元素之间的逻辑关系的。好，那这个问题要怎么解决呢？很简单，注意观察，在树当中除了根结点之外，其他任何一个结点都有，且仅有一个双氢。

或者说，有且仅有一个副节点，那基于这样的特性，我们可以这么做，首先把各个节点。在数组当中，顺序的存储，那每个节点会对应一个数组下标，比如说a节点在数组下标为零的位置。b节点在数组下标为一的位置，那由于每个节点它都只会有一个双清，因此我们可以在这个数组当中记录。每一个节点的双亲节点或者说父节点的编号，比如b节点的双亲是a，而a节点存储在数组下标为零的位置。

因此，在这个地方，我们可以记录b节点的双亲的下标等于零，因此采用这样的思路，每一个非根结点的。双清指针都会指向它的双清结点，在数组当中的下标，那由于根结点是没有双清的，所以根结点的双清指针，我们把它设为负一。好，那刚才说的这种思路就是树的第一种存储方式，叫做双清表示法，那捋清思路之后，数据结构的定义就变得很简单了。

首先，数组元素当中需要包含两个字段，第一个字段表示，这个节点的数据，第二个字段可以用一个int型的变量。去指明它的双清在数组当中的下标是多少，那接下来我们用刚才定义的这个结构体去声明一个数组。那这个数组就是双氢表示法的主体，那最后我们还需要记录节点的总数。好，那这就是树的顺序存储，我们把它称为双清表示法。那显然，这种双氢表示法除了存储数之外，也可以用于存储森林。

我们说森林是m颗互不相交的树的集合。那对于这个森林而言，我们也可以用双氢表示法去存储它。由于这个森林当中有四棵树。因此，我们把这四棵树的根结点abcd，把它们的双亲指针都设为负一，表示这四个是根结点。好，所以双氢表示法既可以用于存储树，也可以用于存储森林，那接下来我们来看一下双氢表示法有什么优缺点？当我们在使用树这种数据结构的时候，我们经常会需要找到某一个指定节点的父亲，或者找到某一个指定节点的孩子。

那么，基于双清表示，法存储的树如何实现这两个操作呢？假如现在我们要找到四号结点的父亲。那么是不是很简单，在数组下标为四的这个地方检查一下这个元素，它的双清指针等于多少？那显然，四号节点的父亲是一号节点，所以找副节点这个操作是很方便实现的。再来看怎么找到它的孩子节点，我们只能把每一个数组元素都遍历一遍，看一下哪些元素的这个双清指针。是指向四的，那么从头到尾扫一遍之后发现只有最后一个元素，

十号元素，它的parent=4。所以要找到一个节点的孩子节点，那我们只能遍历一遍整个数组，因此双清表示法的优点就是找副节点方便。但是找孩子节点很不方便。基于这样的特性，双氢表示法比较适用于找父亲多找孩子少的应用场景，比如说之后我们会学习一种树的应用，叫做并查集。并查集这种应用场景，就只需要找父亲，而不需要找孩子，好具体的就不再展开好，那这是树的第一种存储方式。

双清表示。法。接下来看第二种存储结构，孩子表示法。首先，我们还是用一个数组顺序的存储，各个节点的值，每一个节点对应一个数组下标。那由于每个节点可能会有多个孩子，因此在每个数组元素当中，我们可以保留一个链表的头指针。如果一个节点，他有孩子，那么我们就可以在链表当中保存他所有的孩子节点的编号，比如说。

a节点的孩子是bcd，那bcd的编号分别是一，二，三，所以a节点对应的这个链表也保存了一。二三这样的三个值。那由于既使用到了数组，也使用到了链表，因此孩子表示法它既使用了顺序存储，也使用了链式存储。那代码的定义很简单，首先一个数组元素当中包含data和一个链表的指针。而链表结点当中，只需要保存孩子的编号以及指向下一个链表结点的指针就可以。那我们可以用刚才定义的这个结构体去声明一个数组，

在这个数组当中存储各个节点的信息。同时，还需要记录啊，这棵树当中总共有多少个节点以及根节点的下标编号是多少？记录根结点的位置是很有必要的，因为对一棵树的访问通常是从根结点开始。好，那这是树的第二种存储方式。孩子表示，法那显然这种存储方式也可以用于存储森林。比如，对于这个森林来说，它所对应的孩子表示法就应该是这个样子。a节点没有孩子，所以a节点对应的这个指针把它设为n。

而b节点的孩子是e和f，分别存储在数组，下标为四和五的位置，所以b节点的这个链表当中应该存储四和五，这两个孩子节点的下标信息。好，其他的就不再追述，那需要补充的是，如果用孩子表示法存储森林，那么我们需要在呃结构体当中记录多个根的位置。那这个地方就不再展开好，那接下来我们分析一下孩子表示法的优缺点，同样的，从找父亲和找孩子这两个动作入手。假设现在我们要找到四号结点的孩子，

那是不是很简单？只需要找到数组下标为四的这个位置。对应的呃链表，这样就可以找到孩子节点好，所以找孩子节点很方便，但是找父节点就比较麻烦。我们只能去依次遍历这些链表，在其中找到四这个值。那最终我们可以在一号结点的这个链表当中找到四这个值，说明一号结点是四号结点的父亲。那可以看到，基于孩子表示法存储的数，我们找孩子是很方便的，但是找双亲节点，或者说找父节点就很不方便。

所以这种存储方式适用于找孩子多找父亲少的应用场景，比如说当我们用树来存储服务流程的时候。就可以使用孩子表示法，什么叫服务流程树呢？举个例子，大家应该打过一些客服电话。当电话拨通的时候，它会让你选择普通话服务，请按一英文服务，请按二什么人工服务，请按三。然后当你按了某一个数之后，它又会告诉你办理a业务，请按一办理b业务，请按二对吧？

那在这种场景之下。这个客服系统，它可以用一棵树来存储，服务流程从你拨通电话一直到选中某一个具体的服务。这整个过程都是在找孩子。所以这种应用场景就可以使用。孩子表示，法来存储这棵服务流程树。所以这个地方我们能体会到大多数的数据结构，它都不可能是完美的，既有优点也一定会有缺点，关键在于我们要把这种数据结构放在什么样的应用场景当中。合适的才是最好的，才可以最大的发挥。这种数据结构的特性。

就像我们每个人一样，我们一定有自己的缺点，但我们也一定有自己的优点，不要因为自己的缺点而去否定自己。每个人都是独一无二的，我们要做的就是通过不断的学习，不断的实践，去找到适合自己的位置。要相信每个人都一定会有能够让它发光发热的地方。好，那回到课程本身这个地方，我们学习了树的第二种存储结构，孩子表示法，接下来看第三种存储结构，孩子兄弟表示法。

这是一种链式存储的实现方式，每一个节点用一个struct结构体来保存，其中包含这个节点的数据。另外，还包含两个指针，第一个指针会指向当前结点的第一个孩子，第二个指针会指向当前结点右边的一个兄弟。那你会发现这个数据结构定义和二叉树的节点的定义是很类似的，二叉树的节点当中同样是保存节点的数据，同时也会有两个指针。分别指向左子树和右子树，因此使用孩子兄弟表示法存储的树，它和二叉树的存储是类似的，都是采用了这种二叉链表的方式来实现。每个节点当中会包含两个指针，

这个和二叉树的节点是一样的，只不过我们对这两个指针的含义的解释。各不相同。好用一个例子帮大家理解，对于左边这棵树，现在我们要采用孩子兄弟表示法去存储它。那么，首先根结点是a。这个节点当中包含了左右两个指针。其中，左指针要指向第一个孩子，右指针要指向右边一个兄弟，那么对于a来说，它有bcd三个孩子。b节点是它的第一个孩子，

所以a的左指针应该指向b。而由于a节点没有右兄弟，所以a节点的右指针应该指向n。好，现在我们已经画了a节点和b节点，再来看c节点c节点是b节点的右兄弟，所以c应该连到b节点的右指针下面。那d节点又是c节点的，右边一个兄弟，所以d节点应该连在c节点的右指针下面。接下来e节点，它是b节点的第一个孩子，所以e节点应该连在b节点的左指针下面。好往后f结点应该是e结点的右兄弟，所以f结点应该连在e的右指针下面。

接下来g节点，它是c节点的第一个孩子，所以g节点应该连在c的左边。好，接下来h节点应该是d节点的第一个孩子，所以h应该连在d的左指针下面。好再往后I是h的右兄弟j，又是I的右兄弟，所以我们再用右指针把它们分别连起来。最后k节点是e节点的第一个孩子，所以k应该连在e节点的左边这个位置。好，所以如果用孩子兄弟表示法存储这棵树。那么，从存储的视角来看，

是不是和二叉树的形态是类似的？好，那除了存储数之外，孩子兄弟表示法也可以用于存储森林。来看一下这个森林当中有三棵树，现在采用孩子兄弟表示法存储这个森林。那它应该呈现这样的形态，首先第一棵树的根是b节点。c和d是另外两棵树在森林当中，我们可以把呃这些树的树根把它们视为平急的兄弟关系。所以可以把c节点看作是b节点的右兄弟，把d节点看作是c节点的右兄弟。因此c应该连在b的右指针下面，而d应该连在c的右指针下面。好，

接下来e节点是b的第一个孩子，所以e应该连在b的左边。f节点是e的右兄弟，所以f连在e的右边g节点是c的第一个孩子，所以g连在c的左边。h是d的第一个孩子，所以h连在d的左边，接下来I是h的右兄弟j又是I的右兄弟，那我们用右指针把它们分别连起来。再往后k是e的左孩子，所以k连在e的左指针下面。l是k的右兄弟，所以l连在k的右指针下面，最后m是h的第一个孩子，所以m连在h的左指针下面。

那通过刚才的学习，可以感受到孩子兄弟表示法既可以存储树，也可以存储森林。好，那在这个小节当中，我们学习了树和森林的三种存储方式，分别是双亲表示法，孩子表示法和孩子，兄弟表示法。其中，双清表示，法采用顺序存储的方式，用一个数组存储这些节点的数据。那同时，我们需要记录每个节点的副节点，

或者说双亲节点的一个下标。采用这种方式可以很方便的找到指定节点的副节点，但是要找到指定节点的孩子，节点就很不方便。那第二种存储结构叫做孩子表示法，我们用一个数组顺序的存储节点的数据，同时在数组当中还需要记录每一个节点所对应的孩子链表的头指针。我们会用一个链表去记录节点的孩子编号分别是多少？那孩子表示法是顺序存储和链式存储相结合的一种方式。采用这种方式，那么找到一个指定节点的孩子很方便，但是要找到它的副节点就不太方便。那本小节的最后，我们又学习了孩子兄弟表示法。这是一种纯链式存储的实现方式，

我们会用二叉链表去存储，每个节点和二叉树的节点是类似的形态。一个节点当中，包含左右两个指针，左指针指向当前节点的第一个孩子，右指针指向当前节点的右边一个兄弟。从存储视角来看，孩子兄弟表示，法存储的树或者森林在形态上和二叉树是类似的。所以树和森林可以把它转化为对应的一个二叉树。那这也是一个重要的考点，树森林和二叉树之间的转换。那最后需要注意采用孩子兄弟表示法存储森林的时候，我们需要把森林当中的每棵树的根结点。视为平急的兄弟关系。

好的，那以上就是这一小节的全部内容。
