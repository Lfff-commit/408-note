好的，这一小节中我们要学习knp算法的进一步优化，经过之前的学习，我们知道了knp算法的基本原理。首先有了模式串的信息，我们就可以求出next数组，然后基于这个next数组来实现knp算法。那上一小节，我们也介绍了手算求next数组的方法，这儿建议大家先停下来，把王道书的课后习题给做一做。你会发现有的题目，它是让你求next数组，而有的题目，它是让你求next view数组，

那大家可以先把求next数组的那些题目给做一下。就是利用上一小节我们介绍的手算求next数组的方法来解决这些题目。而next view数组怎么求？我们会通过这个视频来介绍。好，现在我默认大家已经做过了，我们的课后习题，并且对next数组怎么求？next数组怎么使用？已经有了一个比较清楚的认知。那接下来我们来看一下next数组可以进行怎样的优化，从而进一步的提升knp算法的效率。好，我们以之前那个小节的呃模式串为例abaabc，我们已经求出了这个模式串的next数组。

好，现在考虑这样的一种情况，假设主串和模式串匹配到第三个字符的时候，发现上下对不上。那在这种情况下，我们利用next数组应该怎么处理呢？是不是应该让j指针指回next 3，也就是让j。等于一让j，等于一从模式串的第一个字符开始，往后匹配，也就是变成。这个样子好，那现在问题来了，刚才我们发生失配的时候，

主串的I指针所指的这个字符。和模式串j指针所指的这个字符是不相等的，对吧？因为我们在代码里边是不是会判断呃I所指的这个字符是否等于j所指的这个字符？那如果发生失配的话，说明这两个字符肯定是不相等的，而既然它们不相等模式串的第三个字符。它的值是a，那是不是就说明主串的？I指针所指向的字符，它也一定不是a对不对，因此当第三个字符。匹配失败的时候，如果我们让模式串的指针j指向一，那么接下来这一次的匹配也一定是失败的。

因为模式串的第一个字符是a，我们是已知的，而上面这个字符虽然我们不知道它具体到底是什么，但我们至少可以确定它肯定不是a。对吧，因此这一次的匹配一定是失败的，而这次匹配失败之后，我们是不是应该让j指针等于next j？也就是等于零对吧？等于零好，所以我们回到前边这一步。j指针指向三这个位置的时候发生匹配失败。那next数组让我们把j指针改为一，也就是接下来匹配第一个字符。但是我们又知道，

由于第一个字符和现在匹配失败的第三个字符，它们是相等的。因此，第一个字符的匹配也一定是失败的，对吧？因此，当第三个字符匹配失败的时候。我们完全可以让j直接等于零，直接等于零，因为如果让j直接等于一的话。意味着接下来要匹配第一个字符，对吧？而第一个字符注定是要匹配失败的，那当第一个字符也匹配失败的情况下。我们是不是应该让j=next一啊？

因此最好的处理方式是直接让next 3=0。好，那我们对next 3这个值进行了优化之后，故事就会变成这样。当j=3的时候，如果匹配失败，那么我们就直接让j=next j，也就是next 3。让它直接等于零，那么下一个匹配的位置就会直接跳过啊，这个字符还记得knp算法的这个代码逻辑吗？当j此时指向零的时候，接下来会让I和j同时加加对吧？同时加加所以就意味着啊。三这个字符和一这个字符的匹配，

我们直接给它跳过了，直接步入了下一个步骤，让四和一进行一个匹配。好，接下来看另一个例子，假设模式串匹配到五这个位置的时候，发现匹配失败，那在这种情况下knp算法会让。j=next j也就是等于next 5，对吧？所以j应该让它等于二也就是从这个位置开始，往后匹配。但是同样的问题，我们匹配到五这个字符的时候，发现匹配失败。

那么，虽然我们暂时不知道主串的I指针所指的这个位置是什么字符？但至少我们可以确定的是，它和模式串的五这个字符一定是不匹配的，对吧？也就是上面这个字符，它肯定不是b，肯定不是b，所以我们刚才让j指针指向二这个位置。接下来的这一次匹配也一定是失败的，因为模式串的二这个字符和模式串刚才失配的五这个字符。它们的值是一样的，所以刚才我们让j=2，这次匹配一定注定是失败的，那这次再匹配失败之后。

j的值是不是应该改成next 2对吧？应该改成next 2，也就是让j=1从这个位置开始匹配。所以回到前边这一步，当j=5的时候，匹配失败，匹配失败，那么我们不应该。让j回到二的位置，因为二这个位置的字符，二这个位置的字符和当前五这个位置的字符是一样的。一样的，如果让j=2，那么。这个字符和刚才匹配失败的字符也注定还会匹配失败，

那在这种情况下，我们还需要让j。等于next 2对吧？也就是等于一好，所以与其中间去多比较，这样的一个步骤，那我们不如直接让next 5就等于一。好，所以我们可以对next 5的值进行一个优化，让它等于一，也就是直接等于next 2的值。直接等于next 2的值，好在这种情况下，我们再来看一下刚才那个故事，当j=5的时候，

上下不匹配。此时让j=next j，也就是等于一直接等于一。因为I所指的字符，它肯定不是b，所以我们没有必要像之前那样让next 5=2，也就是没有必要再呃对比二这个字符。肯定是匹配不上的，我们现在选择让j直接等于一，这样我们就可以少做一次对比。好，刚才我们对next 3和next 5都进行了优化，那是不是所有的next数组的值都可以被优化呢？不是的，来看这样的一个例子，

假设我们现在呃匹配到六这个字符的时候才发现匹配失败。那么，在这种情况下，我们能确定的就是六这个字符，它一定不是c，对吧？那来看一下原本next数组。它的next 6是等于三，也就意味着当六匹配失败的时候，我们会让j指向三。从三这个字符开始，往后匹配开始，往后匹配，那刚才我们只能确定匹配失败的这个字符，它一定不是c。

但是它有没有可能是a呢？是有可能的，因此当第六个字符匹配失败的时候，我们让j。等于三从第三个字符开始往后匹配，这个是有必要的，这一步没办法跳过。好，那这就是next数组的一个优化思路，重点在于我们要判断next数组它所指的这个呃字符。和原本失配的这个字符，它是否相等？如果这两个字符，它不相等的话，那么next数组的值就保持原有的值不变。

而如果next数组所指向的这个新位置，新位置和当前匹配失败的这个位置。两个字符是相同的。那这种情况下就可以对next数组进行一个优化，像五这个字符匹配失败的时候，我们没必要让j=2。因为二这个字符注定也会匹配失败，我们可以让j直接等于next 2所指向的这个位置，因此刚才我们把next 5的值变成了next 2的值。也就是一。好，所以这就是knp算法的一个优化思路。事实上，我们只是优化了next数组而已，对knp算法的整个逻辑是没有变的。

因此，优化之前的knp算法是这样的，首先，我们根据模式串求出next数组。然后利用next数组来实现knp算法，那对knp算法的优化本质上只是优化了next数组。我们把next数组按照刚才那种思路优化成next view数组，接下来在knp算法匹配的时候，我们用next view数组去替代。去替代next数组就可以了，当主串和模式串匹配失败的时候，同样是让j=next j。只不过我们用next view数组来替代这个next数组而已好，所以优化之后的knp算法，它的整个逻辑和优化之前没有任何改变。

唯一改变的是next数组，它变成了next view数组好，所以接下来我们要解决的问题就是怎么通过next数组来求出next view数组。好方法很简单，接下来从考试的角度给大家介绍手算求next view数组的一个方法。那首先我们需要通过上一小节介绍的方法，先求出next数组，那上一小节我们给大家留过这个练习，对吧？模式串ababaa，它的next数组求出来是这个样子好，接下来我们要把next数组优化为next view数组。那方法是这样的，首先next view 1的值无脑写零，无脑写零。

好，接下来我们呃从前往后依次求后面的这些next view的值。方法是这样的，如果当前的next j。所指的字符。和目前j所指的字符，它们俩不相等，它们俩不相等，那么我们就让next view的值。等于next的值。所以next view 2应该是等于一。好，接下来处理三。三这个字符是a。而next 3所指向的字符，

也就是一这个字符，它也是a也是a。在这种情况下，我们就让next view j=next view的next j。也就是等于零。所以next view 3=0。结合我们之前讲的例子，不难理解吧，因为三和一这两个字符是一样的。所以当三这个字符匹配失败的时候，那么一这个字符也注定是匹配失败的。所以我们没有必要再对比一这个字符，而是直接考虑一对比失败之后，我们应该跳到哪个位置，直接跳到后一步就可以了。

所以我们直接把next 3的这个值把它优化为next 1的值，也就是零。好，那同样的道理，四这个元素来看一下。四，这个元素是b那看一下next 4是指向了二，因此这个next值所指向的元素和当前的这个元素它们是一样的，对吧？都是b。都是b，所以在这种情况下，我们让next view的值，让它等于next 2。等于next 2而next 2应该是等于一对吧，

所以直接让它等于一就可以好接下来看五。五所指向的是a这个字符，然后next 5=3看一下三所指向的元素诶，刚好也是a那这两个元素都是相同的。所以当五这个元素匹配失败的时候，我们没有必要再去对比三这个元素，它注定也是匹配失败的，那我们就可以看一下三这个元素，它匹配失败的时候，接下来一步。应该跳到什么位置？应该跳到j=0对吧？所以next V5的值直接写成零就可以。直接写成零好，最后一个当j=6的时候。

六是a这个字符，然后next 6=4，那我们看一下四这个字符是多少哎？四这个字符是b。b和a不一样，对吧？所以在这种情况下，我们直接让next view和对应的next值保持一致。也就保持四就可以了。因为什么呢？因为当六这个字符匹配失败的时候，我们只能确定呃主串上的那个字符，它肯定不是a。但它有没有可能是b呢？是有可能的，

所以呃，接下来匹配四这个字符这一步，我们不能跳过。好，所以用这种方式，我们就快速的求出了next view数组啊，整理一下它的值就是这样的。先求出next数组，然后按照这样的逻辑来求出next view数组好，再来一个练习之前的小结中，也给大家留了这个例子。aaab这个模式串它的next数组呃，求出来应该是零一二三四好，接下来我们尝试着求它的next view数组。首先next view 1无脑的写零。

接下来，我们从第二个元素开始处理，第二个元素。它是a。那这个元素，它对应的next值等于一好来看一下一这个字符和它是一样的，对吧？所以在这种情况下，我们让next view 2直接等于next view 1，也就是直接等于零。直接等于零。好，再看下一个当j=3的时候，三这个字符是a，然后呃，

它所对应的next值是二。那二这个元素，它也是a。因此，应该让next view 3=next view 2。next，view 2也就是等于零好。接下来，第四个字符，第四个字符，它是a。它所对应的next值等于三。那三这个元素，它也是a，所以我们应该让next view 4的值等于next view 3。

而next view 3是等于零，所以next view 4它等于零好。接下来第五个元素等于b。那它所对应的next值是四好来看一下第四个字符，是a和当前的这个字符a和b是不相等的，对吧？那在这种情况下，我们直接让next view和对应的next值相等就可以了，也就是保持四不变。所以aaab这个呃模式串它的next view数组就应该等于零零零四。好整理一下，是这个样子，好通过这两个例子，相信不难理解，怎么根据next数组呃用手算的方式快速的求出next view数组？

接下来，为了让大家更直观的体会到呃，我们优化了ne xavier数组之后带来的一些性能提升。那我们不妨用这个模式串的next数组和next view数组分别跑一次knp算法，大家来感受一下。好，首先我们使用优化之前的knp算法，也就是使用next数组，那aaabb它的next数组是零一二三四。我们来跑一遍，在这个例子当中呃，很显然当匹配到第四个字符的时候，会发现匹配失败，对吧？所以一二三四总共对比了四个字符，

此时匹配失败，那next 4等于多少？next 4=3，所以接下来我们会匹配。这两个字符好，这两个字符依然匹配失败，那接下来我们需要让j=next三，也就是等于二。也就匹配这两个字符，那这两字符一样，匹配失败，接下来应该让j=next二，也就是等于一。接下来匹配这两个字符，那这两个字符还是匹配失败，

所以接下来让j=next一。也就是等于零。变成这样子，好当j指向零的时候，让I和j同时加加，然后开始匹配后面的这个串。那后面这个串就可以匹配成功，对吧？好，这是使用原有的next数组进行knp算法的一个呃，执行的过程。接下来，我们使用next view数组，刚才我们求得的next view数组再来跑一遍啊，这个例子。

首先，前几个肯定都能匹配成功，直到第四个匹配失败，第四个匹配失败。好，那当j=4的时候匹配失败，接下来我们应该让j=next view 4，也就是等于零。等于零，那就相当于直接跳过了，刚才中间这几个字符的对比，对吧？直接跳过了这些aaa的对比。而当j=0的时候，我们的代码逻辑是会让I和j统一的加加，

然后匹配后一个子串。好，所以可以看到我们把next数组优化为next view数组之后啊，中间减少了很多没有必要的一个对比。好，那关于next数组和next view数组的呃求法，大家主要掌握手算的方法就可以了。kmp这个部分第一次学习呃，听起来比较吃力，这是难免的，不过通过课后习题的训练，相信大家可以更好的掌握呃kmp算法的一个考法。那如果你想再换一个思路来呃，理解这个knp算法，那大家也可以去B站去搜索王道数据结构呃，

在那我们上传了旧版的knp算法的一个。讲解的视频，有兴趣的同学也可以去看一下。好的，那么第四章串这个部分，特别是kmp算法的这个部分呃，看起来很难理解。但事实上，它在四零八当中并不算一个重点，让大家看一下以前的这个四零八考研大纲，你会看到大纲把我们要考察的内容分为了什么线性表栈队列。图二叉树和树，然后查找和排序。唯独没有串这一章对吧？没有串这一章，

那这一章的内容在哪呢？在这个位置。字符串的模式匹配。所以在大纲里边呃串这个部分的内容，它只是其中的一个小的考点。只不过，在很多国内的教材当中，都把串单独的列为了一章，而且由于knp算法，它学起来确实比较难，需要花比较大的篇幅才可以讲明白。因此，我们王道书也把串这个部分单独的变成了第四章，那从考试的角度，大家没必要在这一章花太多的时间。

只需要掌握呃王道书串这个部分，那些课后习题的小题就差不多了好。好的，那么这就是knp算法的全部内容。
