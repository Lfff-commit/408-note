各位同学大家好，这个小节中我们要介绍图的第二种遍历算法，也就是深度优先遍历，那讲解的思路和上一小节基本上是一致的，那由于树本身就是一种特殊的图。所以我们会从树的深度优先，遍历算法开始来看一下，对于树的深度优先遍历分为先根遍历和后根遍历，而图的深度优先遍历其实比较类似于树的。先根遍历，所以我们这个地方会简单的回顾树的先根遍历，那这是用递归算法来实现的，首先要visit访问的是一号节点。然后二是一的一个子数，所以接下来要访问的是二号节点，

那五又是二的一个子数，所以接下来访问五号节点。那五号结点的下面已经没有子数了，所以会跳出这一层的低规回到二号结点，对于二号结点，它还有一个没有访问的子数，也就是六号结点，所以接下来访问六号。那六号结点，由于它已经没有更下一层的子树，所以会跳出当前的递归返回二号结点这一层。而二号也没有子数了，所以会返回一号结点这一层，那一号结点还有一个子数是三号，所以接下来访问的是三号结点。

那由于三号节点已经没有下一层的子数了，所以会返回一号节点这一层的递归那一号节点，还有下一个没有访问过的子数，也就是四号节点。所以接下来访问的是四号，而对于四号节点，它有一个没有访问的子树七，所以访问七七已经没有下一层的子树，所以会跳出当前的递归返回四号，这一层的递归。那四号还有下一个子数，也就是八，所以会访问八。那由于八号结点已经没有更下一层的子树，所以会跳出当前这一层的递归返回四号，

而四号也没有呃，别的子树需要访问了，所以会跳出四号结点的这一层递归。回到一号节点，那对于一号节点所有的子数也都访问过了，所以一号节点的这层递归也会返回。那这样我们就递归的执行完了，树的先根遍历。得到了这样的一个先根面积序列，那这个地方和上一小节类似，由于树的特性，我们在探索这些新的节点的时候，新找到的那些节点肯定是没有被访问过的节点。但是图就不一样了，我们通过某一个顶点找到的与之相邻的其他顶点，

有可能是已经被访问过的，所以和广度优先一样，我们也需要设置这样的一个。数组用来标记每一个顶点是否曾经被访问过，那其实图的深度优先，遍历算法和树的先根遍历是很类似的，都是先访问一个节点。然后再用一个循环来依次检查和这个节点相邻的其他节点，然后再进行更深一层的访问，只不过对图的深度优先遍历，我们增加了这样的一个数组而已，大家可以再对比一下。是很类似的。好，那我们来模拟一遍这个算法的执行过程，

假设我们是要从二号节点出发，对这个图进行深度优先遍历，所以刚开始调用的dfs函数啊，传入的这个v的值应该是二。那我们首先会访问二号节点，接下来会把与之对应的这个visit值设为true，然后这个for循环做的事情是要检查和二号节点相邻的呃，其他的节点。那和二相邻的是一和六，这两个顶点，但是和它相邻的第一个顶点应该是一号顶点，而一号顶点此时没有被访问过，因此下一层的这个dfs函数我们传入的这个参数应该是。一好那在一号顶点的这层dfs函数当中，我们会访问一号顶点，

并且把它的visit值设为true，接下来要检查和一号顶点相邻的啊，其他的顶点。那和一号相邻的第一个顶点是二号，但是由于二号顶点的visit值等于true，也就是这个if条件不满足，因此二号顶点会被跳过。那接下来找到的下一个和一号相连接的顶点，就应该是五号顶点。所以下一层的dfs函数传入的参数应该是五。好，那访问了五号顶点之后，由于和五号顶点相邻的呃，所有的顶点都已经被访问过，所以五号顶点的这个for循环什么也不会做。

那这个顶点的dfs函数执行完了之后就会返回上一层的递归调用，也就是一号顶点这一层，那由于之前处理的五号顶点已经是一号顶点的。最后一个临界点，所以一号顶点的这个for循环也可以执行结束，因此一号顶点的dfs函数执行结束。接下来返回到二号顶点的这一层，那之前已经处理了二号顶点的第一个临界点，也就是一号接下来通过这个基本操作，又可以找到除了一号顶点之外。的后一个和二号顶点相连接的顶点，也就是六号顶点，而六号顶点此时也没有被访问过，因此接下来会对六号顶点执行dfs这个函数。好，

那访问了六号顶点之后，又需要通过这个for循环找到和六号相邻的呃，其他顶点二号已经被访问过，所以接下来应该访问的是三号。因此，接下来对三号执行dfs那和之前类似，和三号顶点相邻的这些顶点当中，接下来第一个应该被dfs的应该是四号顶点。所以下一层被dfs的是四号。那和四号相邻的这几个顶点当中，三号已经被呃访问过，所以接下来应该被dfs的呃，应该是七号顶点。好，最后和七相邻的所有的这些顶点只有八号是没有被访问过的，

因此再下一层的dfs应该是处理八号顶点。好，那在八号顶点的啊，这个dfs这一层调用当中，由于与之相邻的所有的顶点都已经被处理过。它们的visit值都是true，因此八号顶点的dfs可以执行结束，然后返回上一层，对于七号也一样，与之相邻的所有顶点。值都为true，所以会继续返回，再上一层，那其他的也都是类似，那这就是从二号顶点出发，

进行深度遍历的一个过程。得到了这样的一个深度遍历序列，好，那其实这段代码也存在，我们在广度优先算法当中提到的那个问题，就是如果我们给出的是一个非连通的图。那么像这个图，我们从二号结点出发，调用dfs函数可以遍历一到八这几个节点，但是九十十一是遍历不到的。那处理的方法和上一小节类似，我们在进行了一次dfs之后，其实还可以再扫描一次这个数组，如果在这个数组当中发现了有某一个元素，它的。

visit值依然是FALSE，那么就说明与之相对应的顶点是没有被访问过的，那我们再从这个顶点出发，再调用一次dfs函数就可以了。所以图的深度优先，遍历算法的完整代码应该是这样的，当我们要遍历一个图g的时候，这个for循环做的事情是会。帮我们找到第一个就是visit值等于FALSE的顶点，从这个顶点出发调用dfs函数。对于我们的例子来说，就是从一号顶点出发调用dfs那访问完一到八之后，会导致一到八号顶点的这些visit值都为true。因此，第一次执行了dfs函数之后，

这个for循环又会继续往后找找找，直到找到一个。visit值为FALSE的顶点，也就是九号顶点，那接下来又会对九号顶点进行一次dfs这个函数。显然，通过九号顶点也可以访问到十和11，那这个处理其实和广度优先遍历是很类似的，好，那接下来我们要分析这个算法的空间复杂度和时间复杂度。这个算法的空间复杂度主要是来自于这个函数的递归调用，最坏的情况是呃，像上面这个例子。如果我们从一号顶点出发，进行深度优先遍历，

那么我们的dfs函数的递归调用深度应该和这个节点数是相同的。所以最坏的情况，我们的空间复杂度应该是ov这样的一个量级，那最好的情况是啊，类似于下面这种，如果我们从一号顶点出发，用深度优先遍历这个图。那显然，在这种情况下，我们的函数递归调用栈最多只会有两层，也就是我们只需要常数级的空间复杂度。当然，如果题目让你答呃，这个算法的时间复杂度的话，那如果题目没有特殊说明，

大家得答这种最坏的情况。好，这是空间复杂度，而对于时间复杂度，其实呃，分析的方法和广度优先是类似的。无论是广度优先还是深度优先，这两种算法我们在计算他们的复杂度的时候，其实都可以把时间开销简化为就是访问各个节点，还有探索各条边。这两部分的时间，那无论是广度优先还是深度优先，如果这个图是采用零阶矩阵存储，那么我们访问v个顶点总共需要ov这么多的时间。而要探索和某一个顶点相连的边需要遍历和这个顶点对应的这一整行的数据，

所以要查找每个顶点的呃临界点，或者说相邻的边。都需要ov这么多时间，而总共有v这么多个顶点，所以整体来看时间复杂度就是ov的平方。那对于临街表的分析也是类似的啊，深度优先和广度优先这两种算法，它们的时间复杂度其实都是一样的，主要的区别来自于我们到底用什么样的存储结构来存储这个图。好，那这是算法的空间和时间复杂度，那接下来我们再进行几个训练来看一下怎么快速的求出深度优先遍历序列？我们假设这个图是用临街矩阵或者临街表存储，当然这个临街表当中各个边节点出现的顺序都是递增的。都是递增的，

也就是说对于某一个指定的顶点，要找到与这个顶点相邻的，其他的顶点，那么在这个临界表里。找到的这个顺序和左边这个临界矩阵找到的顺序是可以保持一致的，那在这两种情况下，如果从二号顶点出发来深度优先遍历。整个图的话，其实得到的就是我们之前分析的那一个序列，那再来看，如果从三号顶点出发。进行深度优先遍历那首先访问三号顶点和三号相邻的第一个是四号顶点，所以接下来访问的是四号。那根据dfs的规则，接下来应该从四号啊出发，

再找到其他的没有被访问过的顶点，那与四号相邻的第一个三号已经被访问过了，七号没有被访问过。所以接下来访问的是七号。那往后就应该从七号出发，三四已经访问过六没有访问过，所以接下来访问的是六。那再从六出发，二没有访问过，所以接下来访问的是二那再从二出发，一号是没有访问过的，所以接下来访问的是一。接下来，从一号出发，二访问过，

五没有访问过，所以接下来访问的是五，那再从五出发。和五相邻的一号顶点已经访问过，所以会退回上一层，也就是要检查还有没有和一相邻，但是没有被访问过的顶点，那和一相邻的这些顶点都检查过了，所以会再退回上一层。也就是二这一层，那和二相邻的还有一个没有检查过，是六号顶点，但是六也已经访问过了，所以又会退回上一层。那和六相邻的顶点呃，

还剩下三和七没有检查，但是三和七都已经被访问过了，所以又会继续退回上一层，也就是七号这一层。那和七相邻的还有一个八号顶点没有被防过，所以最后要访问的是八号。好，那到此为止，我们得到了一个深度优先遍历序列。当然，从函数执行的角度来说，这个函数还会继续往后检查，继续执行。但是如果我们求的是深度优先，遍历序列的话，

那到这一步就OK了，所以从三号顶点出发，得到的遍历序列应该是这样的。好，那接下来同学们还可以啊，结合这个存储结构，然后从一号顶点出发，看一下可以得到什么样的深度优先遍历序列。可以暂停自己来练练手，我们这儿直接给出答案是这样的一个遍历序列好，那接下来的问题是对于一个图来说，它的临街矩阵存储表现方式表示方式是唯一的。但是临街表的表示方式不唯一，所以如果临街表不一样的话，得到的深度优先遍历系列也会不一样，

这个和广度优先是一样的，所以我们把这个图的临街表。把这些边出现的顺序稍微改一改。再来看一下，在这种情况下，从二号顶点出发，那么首先访问的是二，那和二相邻的是六号顶点，所以接下来访问的是六。和六相邻的二号已经被访问过，七号没有被访问过，所以接下来访问的是七那和七相邻的六被访问过，八没有被访问过，所以接下来访问的是八。和八相邻的四没有被访问过，

所以接下来访问的是四。好，那和四相邻的呃三没有被访问过，因此接下来访问的是三和三相邻的六访问过七访问过四也访问过，所以三号顶点的。这一层递归调用执行结束，返回上一层。也就是四号这一层，那和四相邻的，剩下的八和七这两个顶点也都访问过，所以这层递归也可以返回。也就是回到八这一层和八相邻的所有顶点都访问过，所以八这一层也可以退出，再返回上一层。也就是七这一层，

那和七相邻的三和四也都访问过，再一次返回再上一层的节点，也就是六号节点。那六也一样，访问过三，所以继续返回，再上一层和二相邻的，还有一个节点，一是没有访问过的，所以接下来访问的是一号节点。那和一相邻的还有一个五号节点没有访问过，所以最后访问的是五好，那这样我们就得到了。基于这个临接表的一个深度优先遍历序列，那同样的大家也需要暂停来练练手。

如果从三出发。或者从一出发，那么得到的深度优先遍历序列应该是什么样的？因为这些是选择题当中很高频的考点，所以需要多动手练习。好，所以这个地方想强调的是，如果采用邻接表这种方式来存储一个图的话，那么由于邻接表的表示方式不唯一，因此基于邻接表的存储结构。所得到的深度优先遍历序列，也是不唯一的。这个问题和广度优先遍历是一样的好，那接下来我们再来看。深度优先生成树的一个概念，

其实和广度优先生成树是一样的。其实，整个深度优先遍历，就是在探索各条边所连接的顶点。这样的一个过程。那么，如果说我们通过某一条边找到了一个此时还没有被访问的节点，那我们之前就是把这条边给标红。那如果只保留这些红色的边，把这些黑色的边去掉，其实这个图就变成了一个没有环的树。当然，采用临街表存储的图啊，即便是从同一个节点出发来进行深度优先遍历，也有可能得到不一样的深度优先遍历序列，

因此也会对应不一样的这种。深度优先生成树好，那我们把这些黑色的边去掉，把它变成我们熟悉的树的形状就是这样的两种状态啊，左边这种状态是基于我们之前提出的第一个临接表，或者说基于临接矩。来进行的深度优先遍历，而右边这个深度优先生成树，是基于我们之前提出的第二个临界表来得到的深度优先生成树，它们是不一样的。好，那既然有生成树，当然就有生成森林，如果一个图它是非连通的，也就是说我们需要调用多次dfs函数，

那么每调用一次dfs函数就会生成一颗这种深度优先生成树。那像这个无向图，总共有两个连通分量，所以需要调用两次dfs函数，因此也会对应的生成两棵深度优先生成树，那这两棵树就组成了深度优先生成森林。那这个和广度优先也是很类似的好，那在这个小节最后我们再来总结一下图的便利和图的连通性，它们之间的一个联系，那对于一个无向图来说，无论是对它进行广度优先还是进行深度。优先遍历调用bfs或者调用dif s函数的次数应该是等同于这个无向图的连通分量的数量。那像这个图，总共有三个连通分量，所以无论是进行广度优先还是深度优先遍历，

我们都需要调用三次啊，对应的这个函数。那如果一个无向图，它是连通图的话，那我们肯定只需要调用一次bfs或者dfs函数。这是对于无向图，而对于有向图情况就稍微复杂一些，如果我们选定的那个起始顶点到其他顶点都存在路径的话，比如像七号顶点。从七号顶点作为开始进行广度优先或者深度优先遍历，那我们只需要调用一次啊，这个函数。而如果我们从二号顶点开始，那从这个顶点开始调用这两个函数，我们只能找到一和五这两个顶点。

所以肯定没办法用一次函数调用就完成整个遍历，而如果这个有向图，它是一个强连通图的话，也就是说从任何一个顶点开始。都能够找到到达其他所有顶点的路径，那对于这种强连通图，我们不论是从哪个点出发，都只需要调用一次bfs或者dfs函数。好的，那这个小节当中我们学习了图的深度优先遍历算法，其实和树的先根遍历是很类似的，都是用递归的方式来实现的，这个深度优先遍历。那在图里边，为了防止重复的访问，

同一个节点，所以我们需要用一个visit数组来标记某一个顶点是否被访问过。另外和另外和广度优先一样，我们也需要注意处理这个非连通图的问题，需要在dfs函数的外面再套一层for循环。那至于这个算法的空间复杂度和时间复杂度，其实和广度优先遍历是一样的，只不过深度优先的空间复杂度主要是来自于递归工作站。而对于广度优先遍历，它的空间复杂度主要是来自于辅助队列好，那这个小节的末尾我们也介绍了通过深度优先遍历所确定的深度优先生成树。那由于邻接表存储的图表示方式是不唯一的，所以用邻接表存储的图，它的深度优先遍历序列和对应的深度优先生成树也是不唯一的。那最后我们也探讨了图的遍历和图的连通性，

它们之间的一个关系，这个也是在选择题当中比较容易考察的点好的，那以上就是这个小节的全部内容。
