好的，那之前的小节中，我们已经学了图的逻辑结构和存储结构，那这个小节中我们要认识数据元素三要素当中的第三个要素，也就是基本操作。我们会简单的过一遍和图相关的这些基本操作，大致上可以用什么样的思路来实现那基本操作的具体实现，肯定是和存储结构密不可分的。那之前我们说过，考研当中最常考的还是临街矩阵和临街表这两种存储结构，所以在这个小节中，我们只探讨临街矩阵和临街表这两种存储结构。应该怎么实现这些基本操作好？首先看第一个啊，这个基本操作是要判断图里边是否存在xy这样的一条边，

或者xy这样的一条弧。不知道大家还有没有印象，用这种尖括号表示的是有向边，也就是弧，而用圆括号表示的是无向边。好，现在看，如果存储的是无向图，那么用零阶矩阵来存储，想要判断两个顶点之间是否有边是很方便的，比如说要判断b和d之间是否有边，那我们只需要找到呃b和d所对应的。这个元素判断它是否为一就可以，因此采用临界矩阵实现这个基本操作，只需要o1的时间复杂度。

那如果采用临界表，要判断b和d之间是否有边，那么我们可以检查b的这个边界点到底有没有d，也就是有没有三。最好的情况是我们要找的那个目标节点所对应的编号，刚好就是第一个元素，也就是说最好的情况下，时间复杂度应该是o1，而最坏的情况是。你遍历完整个b的所有边节点，结果都没有发现我们要找的这个节点，而和b连接的边最多有可能有n- 1。一条啊，也就是v- 1这么多条，所以最坏的情况就是当前我们找的这个节点，

他连了尽可能多的边，但是遍历完所有的边都找不到我们。想要找的啊，这个节点因此采用临街表实现这个基本操作时间复杂度应该是o1到ov这样的一个量级，这是最好的，这是最坏的情况。好，再看有向图。其实对于有向图也是一样的，邻接矩阵的表现会更优秀一些，那分析的方法和之前一样好，下一个基本操作是要列出图g当中和节点x相邻接的所有的边。那如果采用零阶矩阵存储的话，那你想要找到和某一个顶点相连的所有边，那是不是只需要遍历和它所对应的矩阵当中的这一行，

或者这一列？然后检查哪些地方元素是一把这些元素所对应的边给列举出来，给罗列出来就可以。所以使用零阶矩阵完成这个基本操作时间复杂度应该是ov这样一个数量级，因为我们要遍历总共v个元素。而如果采用临接表的话，如果当前存储的是无向图，那么我们想要找到和某个顶点相连的边，只需要遍历和它相连的这个这个边节点的链表就可以。那最好的情况是，当前这个节点只连了一个边，或者甚至没有边，只需要o1的时间就可以完成最坏的情况就是当前这个节点上边连了尽可能多的边，那最多有可能连v减。这么多的边，

所以遍历完整个链表需要ov这样的一个时间复杂度，那显然临街表的表现更优秀，那如果我们存储的是有向图的话。对于零阶矩阵，其实也是一样的，你要找到和某个顶点相连的，所有的边，那么你想要找出边的话，你只需要遍历和它对应的这一整行遍历v这么多个数据。要找它的入边，那你只需要再遍历和它所对应的这一列，那同样也只需要遍历v这么多的数据。所以时间复杂度也是ov，而如果采用临接表，那要找出边是不是和刚才是一样的，

只需要遍历和它相连的这一整个边的链表就可以，而如果要找指向当前这个节点的入边的话。那是不是我们就得遍历整个临界表，所有的这些边界点，因为只有遍历完所有的这些边界点才可以确定。到底有几条边是指向c这个节点的，而整个临界表当中有这么多条边，因此时间复杂度应该是o1这个数量级。所以如果存储有相图的话，那实现这个基本操作显然是零阶矩阵会更优秀一些，当然这也不是绝对的，比如说如果你要存储的是一个稀疏图呢？你如果存系数图的话，这个e的数量级是不是就很小？那在零阶矩阵上实现啊，

这个功能也可以得到不错的表现。所以到底哪种存储结构更优秀？这个其实也得具体问题具体分析。根据实际的好，再下一个基本操作，要在图g当中插入一个新的顶点x。那么，刚开始插入这个顶点的时候，这个顶点和其他的任何顶点都是不相连的，所以其实插入是很方便的，如果采用零阶矩阵，那么我们只需要在保存这些顶点的这个数组的后面。空白的位置写入新节点的一个数据就可以。那在零阶矩阵当中，与新元素相对应的这一行和这一列，

就可以表现当前这个新元素和其他顶点的一个零阶关系。那现在看起来，我们似乎要在矩阵当中插入这么多个零元素。但是其实矩阵的这些元素化零这件事情应该是在你的零阶矩阵被初始化的时候就已经做好的，所以其实插入新顶点的唯一开销就是啊，写入这个顶点相关的信息。因此，可以在o1这样的时间复杂度内就完成那基于临界表的实现也很简单，我们同样只需要在存储节点的这个数组的末尾插入新节点的信息就可以，那由于新节点刚开始没有连任何的边。所以可以把它的这个指针设为n，因此同样的o1复杂度就可以完成好，那对于有向图也是类似的，都可以在o1复杂度内完成。好，

再看下一个操作，要删除某一个指定的顶点x，比如说我们要删除的是c这个顶点，那如果采用零阶矩阵存储的话，删除c之后。是不是相当于我们要把和它对应的这一行，这一列所有的这些数据都清空，那清空这些数据之后一个比较容易想到的方法是我们可以让后续的这些数据都依次前移。那对于接矩阵来说，就是要把啊我们删除的这个十字周围的这些元素都让他们再拼接起来，但是如果这么操作的话，那就意味着会有大量的数据元素需要移动。显然，移动这么多的元素开销是很大的，所以这种方案不太科学，

那怎么办呢？当我们删除一个顶点之后，其实我们只需要把和这个顶点相对应的这一行，还有这一列。全部变成零就可以了，然后我们可以在顶点的结构体当中，比如说增加一个布尔型的变量，用于表示这个顶点是否是一个空顶点。其实做一个这样的简单标记就可以用这种方式实现删除，显然要比移动大量的元素要呃更好很多。由于只需要修改一行和一列的数据，所以只需要在ov这样的时间内就可以完成好再来看临街表，我们要删除c这个顶点，那我们除了删除c还有。呃，

删除和它相连的这些边节点之外，我们还需要在其他和它相连的这些顶点的边表当中找到指向c这个节点的这些边的信息。也把这些信息给删除，也就是变成这个样子，那临街表的删除操作最好的情况，当然是当前删除的节点，它原本就没有连任何的边。那这种情况只需要o1的时间复杂度就可以完成，而最坏的情况是当前删除的这个顶点，它后边连了尽可能多的边。也就是它和其他所有的顶点都有相连的边，那么在删除当前结点的边表之后，是不是还需要去依次遍历和它相连的其他这些顶点的边表？那最坏的情况是，所有的这些顶点和当前删除的顶点，

二号顶点相连的这条边的信息都是接在最后一个位置。那么，是不是就意味着我们需要遍历所有这些边的信息，才可以依次的把这些该删的东西给删完，所以最坏的情况，我们需要oe这样的时间复杂度？需要遍历完所有的边好，这是无向图，那如果存储的是有向图，对于零阶矩阵删除的方法也是一样的。而对于临接表，我们要删除从当前节点往外发射的这些边很方便，只需要把它的后边连的这整个链表给删除就可以，而如果要删除。指向当前这个顶点的边，

也就是入边的话，那我们就只能遍历整个临界表，把所有的这些边界点都给遍历一遍，所以删除入边的时间代价是比较高的。当然，如果存储的是一个稀疏图的。那其实这个时间复杂度也就那样好，这是删除操作，下一个操作是增加一条边对于指定节点xy，如果说它们之间没有边或者没有弧存在，那么就要添加这条边。那显然，如果用零阶矩阵的话，那么只需要o1的时间，如果采用零阶表的话，

比如我们要添加c和f之间的一条边。那我们是不是需要在c和f边节点的链表后面添加这样的两个信息？那这是采用链表的尾插法。其实更省时间的做法是，采用投插法就是把新的这个边的信息插入到链表的头部。那使用投插法的话，也只需要o1这样的时间复杂度好，对于有向图也是类似的好，下一个基本操作是要找到指定顶点的第一个临界点。如果采用临界矩阵，那是不是只需要扫描和这个顶点对应的这一行，从左到右扫描扫描，找到第一个一。那这样的话就找到了和当前节点连接的第一个点，所以最好的情况就像c节点这样扫描到第一个元素就直接找到了，

而最坏的情况是你扫描完一整行，然后。都没有发现和它相邻接的顶点，那这种情况需要ov的时间好，如果采用邻接表的话，那是不是很简单，只需要找到它的边节点的链表当中的第一个节点。这个节点表示的就是和它相邻的第一个顶点的信息，所以只需要o1的时间复杂度好，那这是对于无向图，如果是有向图的话，那么在零阶矩阵当中找到某一个顶点的初边。就需要扫描它的行找入边，需要扫描它的列，而如果采用临街表，

那么找出边是很方便的，但是找入边。就比较麻烦，最好的情况当然是你遍历的第一个元素，就是指向当前顶点的一条边。也就只需要o1的时间，这是最好的情况，而最坏的情况有可能你需要遍历完整个临街表的所有的这些边的信息都找不到指向当前顶点的边。那这种情况就是需要用oe这样的时间复杂度，当然其实更常使用到的是找出边的操作，所以我们把下面这种情况给划掉。好，那这个基本操作可以找到第一个临界点，那如果我们想要找接下来的后一个临界点，我们就可以用下面的这个基本操作。

就是给定一个顶点y，这个顶点y是和x相连接的一个顶点，那这个基本操作需要找到y之后的下一个和x相连接的顶点。那对于临界表来说，你要找到一个临界点之后的下一个临界点，是不是只需要继续往后扫描就可以？那最好的情况肯定是o1，也就是往后的一个元素刚好就是一而最坏的情况是扫描了一整行。都没有找到下一个临界点，这种时候返回负一那这种最坏的情况，需要ov的时间，而对于临界表来说，给定一个呃边的信息，那是不是再往后找一位就可以？所以连接表找下一条边是很方便的。

那找到下一条边是不是就意味着你已经找到下一个临界点了好，所以只需要o1的时间复杂度好，再看最后的两个基本操作就是要给指定的某一条边或者某一条弧设置一个。对应的权值或者获取这条边的权值，那这两个基本操作其实主要的时间开销是不是也在于找边或者找弧啊？所以实现这两个操作，其实和呃判断某一条边是否存在这个基本操作时间开销是一样的。因此，这就不再赘述好了。那这个小节当中，我们介绍了和图相关的一些啊，比较常用的基本操作。我们是基于临街矩阵和临街表这两种存储结构来探讨的，当然有兴趣的同学也可以自己分析一下，如果采用十字链表或者临街多重表，

那这些基本操作大致上可以用什么样的思路来实现，时间复杂度。是多少？那这小节中介绍的所有的这些基本操作当中，其实在考研当中比较能用得到的就是啊，找到和一个顶点相连的第一个临界点，还有就是找到下一个临界点。这两个基本操作会经常在图的遍历算法当中，用到有兴趣的同学可以分别基于临街矩阵和临街表去实现和这两个基本操作相关的代码。当然，在考研当中其实。你直接调用这两个基本操作的这个函数接口也是没问题的，好那关于图的遍历相关的算法，我们会在之后的小结再进行进一步的探讨。好的，

那以上就是这个小节的全部内容。
