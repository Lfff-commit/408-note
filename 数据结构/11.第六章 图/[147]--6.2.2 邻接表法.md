好的，那上一小节中我们学习了图的临街矩阵，这种存储方式，这种存储方式，一个最主要的缺点就是空间复杂度很高。可以达到o的n方这样一个量级n是顶点的个数，所以如果我们用零阶矩阵来存储稀疏图，那么会造成大量的空间浪费。那零阶矩阵这种方式是基于顺序存储的实现，因为我们是用了一个一维数组和一个二维数组来实现它的，而这一小节中我们要学习的零阶表是用顺序存储，加链式存储的方式。来实现的好，那怎么实现呢？一图胜千言呃，

就是这个样子，我们可以用一个一维数组来存储各个顶点的信息。那其中包括顶点的啊，一个数据域，还有就是指向这个顶点的第一条边或者第一条弧的指针。那当我们声明一个图的时候，其实就声明了啊，顶点节点的一个数组，另外我们还需要记录当前这个图里边。具体有多少个节点？有多少条边？而对于各条边和各个弧，也会有一个与之对应的啊节点。这个字段指明了当前这条边是指向了哪一个节点，比如说a和b是相连的，

那b的编号是一，所以有一条从a指向一。也就是指向b的一条边，另外a和CA和d也就是啊，第二个第三个节点也是相邻的。所以这会有二和三，那如果我们要存储的是一个带全图，那可以在和边相关的这个顶点当中再加入权值的信息。细心的同学会发现，这种临街表法其实和我们之前讲述的孩子表示法其实是相同的一种实现方式。各个节点顺序存储，然后再用一个链表来指明和这个节点相连的各个边，那刚才我们给的这个例子是一个无向图。我们也可以用临街表来存储，有向图。

原理是类似的，有一条弧是从a指向b，所以a的后面跟了这样的一个结点，它的数值是一，也就是说有一条从零号结点指向一号结点，也就是指向b的弧。也就是说，如果用临街表来存储，有向图的话，那么每一个节点后面跟的这些信息其实是从这个节点往外射的一一条弧。而对于无向图来说，每一条边在临界表当中都会对应两个节点，比如说a是零号节点b是一号节点，那么这个节点表示的是从零号指向一号的一条边。而这个节点表示的是，

从一号指向零号的一条边，其实这两个节点在背后对应的就是同一条边，因此在无向图当中，其实边的数据是有冗余的。边节点的数量应该是边的实际数量的两倍，因为每一条边都会有这样的两份冗余的数据，所以存储无向图，总共需要的空间复杂度就应该是。这样的一个数量级而有向图的话，每一条弧只会对应这样的一个边界点，所以整体来看，它的空间复杂度应该是这样的一个数量级。好，那接下来要探讨的问题是我们怎么求出一个顶点的度或者入度和出度呢？那首先来看无向图，

对于无向图，我们只探讨度。如果要确定一个顶点的度是多少，我们只需要遍历和这个顶点相关的这个边链表就可以。有多少个边节点，它的度就是多少，同时遍历这个边链表，就意味着我们可以找到和当前的这个顶点相连的所有的边。好，这是无向图求它的度，或者找到相邻的边还是很方便的，再来看有向图，有向图的度等于入度，加上出度。如果要找到一个节点的出度很简单，

我们只需要遍历和这个节点相关的，这个边节点的链表就可以，那这个链表反映的是？从当前结点出去的这些弧好，所以找出度还有和当前结点相连的往外设的弧，这是很简单的。不过，如果要找入度，还有指向当前节点的弧，这就比较麻烦了。比如，我们要统计a节点的入度，或者说要找到指向a的弧。那唯一的办法就是把所有的这些节点的呃边链表都依次的遍历一遍。只能用遍历的方式来找到指向零号结点的边。

所以如果要找到指向某一个节点的弧，那是很麻烦的，时间复杂度也会很高。那这也是用临街表这种方式存储，有向图的一个一个比较大的缺点好，那另一点需要注意的是，对于一个临街表，其实它的这个表示方式是不唯一的。比如说a这个节点和bcd都相连，也就是零号节点和一二三都相连，那a节点的边链表其实可以用一二三这样的顺序，当然也可以用三二一这样的顺序。也就是说，各个边在这个链表当中出现的先后顺序是任意的，因此我们才说对于一个给定的图，

它的临接表的表示方式并不唯一。而上一小节介绍的临界矩阵，我们只要确定了各个顶点的编号，那对于一个给定的图。它所对应的临街矩阵表示方式肯定都是唯一的，这是选择题里面比较喜欢考察的一个点好的，那这个小节中我们介绍了临街表，大家在学习临街表的时候也需要和临街矩阵啊相关的内容。我们进行一个对比，零阶矩阵其实主要的问题是空间复杂度高，只适合存储稠密图。那临街表就可以比较好的解决这个问题，可以让空间复杂度变成这样的一个量级，所以如果用临街表存储稀疏图的话，就是比较适合的，

当然你要用临街表存储稠密图，当然也OK。另外一点需要注意的就是，刚才强调的那个问题，对于一个给定的图零阶表的表示方式并不唯一，但是零阶矩阵的表示方式是唯一的。在计算结点的度，入度和出度，或者找到和一个结点相连的边的时候，对于临界矩阵，我们需要做的就是遍历相应的行或者列就可以。而对于临街表比较麻烦的操作是，如果我们要找到入边，也就指向某个顶点的边，是不太方便的，

所以统计它的入度也会不方便。所以临街表解决了这个空间复杂度高的问题，但是同时用它来存放有向图的时候也会存在一些操作不方便的情况，因此临街表也是可以进一步优化的。好的，那以上就是这个小节的全部内容。
