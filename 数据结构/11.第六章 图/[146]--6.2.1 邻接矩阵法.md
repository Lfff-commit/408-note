各位同学大家好，这个小节开始我们要认识几种图的存储结构，那我们会认识临街矩阵法，还有临街表法，十字链表和临街多重表，总共四种方法。这个小节当中，我们先来学习第一种零阶矩阵，其实零阶矩阵的原理很简单，给个图大家就明白了啊，像左边这个无向图。就是a和b之间有一条边a和c之间有一条边a和d之间也有一条边。那由于无向图的这些边是没有方向的，所以a和b之间有一条边，同时也意味着b和a之间是不是也有一条边，

所以无向图里的每一条边在临界矩阵里其实会。对应着这样的两个一那a和e之间不存在边，所以a和e所对应的这个元素应该是零。所以在零阶矩阵法里边，零表示的是两个顶点之间相互不零阶。而一表示的是两个顶点之间，相互是邻接的呃，存在着一条边。那有向图的原理，也是类似的，有一条a指向b的边，所以a行b列这个元素应该是一，但是b行a列这个元素是零，因为有向图的这些边是有方向的。所以在有向图当中，

每一条这种有向边或者说每一条弧在这个临界矩阵里边。对应一个一而在无向图里边，每一条边，它会对应两个一，这是它们的一个区别。那这种零阶矩阵的实现是不是也很简单？只需要用一个二维数组就可以，就像这个地方，我们定义了一个int型的二维数组，这个二维数组的大小是100×100。也就是说，我们的这个顺序存储的图里边最多支持100个顶点，那除了用这样一个二维数组来表示各个边的信息之外，我们还需要定义一个一维数组。这个一维数组用于存放各个顶点的信息，

那像我们这儿举的这个例子当中，每个顶点的信息其实就只包含abcd这样的一个字符，所以我们把它定义为char类型。但其实你可以在这个顶点当中存放更多更复杂的数据，你可以定义一个更复杂的结构体。那这儿为了列举方便，所以我们把各个顶点就设为是一个差类型的数据，那由于我们在零阶矩阵当中只需要存放零和一这样的两种状态。所以其实我们在这个地方也可以把边的信息从int换成更小的，比如布尔类型或者枚举类型，因为一个int型的变量可能会这样，四个字节或者八个字节。但是如果换成布尔类型或者枚举类型，那矩阵里边一个零和一就只需要占一个字节，也就可以让这个矩阵变得更小。

当然了，你写int类型肯定也没错，另外一点需要注意的是我们的每个节点，它虽然有各自的数据abcd，但是另一个方面。这些节点的数据在这个数组当中存放的呃，具体位置是有一个编号的，比如a，它可能是存放在这个数组下标为零的位置。b是存放在下标为一的位置。c是存放在下标为二的位置，那用这些节点在数组当中的存放下标和这个临街矩阵里的行和列进行一个对应，是不是就可以把它们都给对应上了？比如你要判断a和b之间是否有啊，相互连接的边，

那么你在这个二维数组里边找，是不是就应该找它的第零行？第一列也就是这个位置，所以各个顶点在这个数组当中的存放位置，它的下标和这个二维数组是相互对应的。总之，零阶矩阵法还是很好理解的呃，如果说这个矩阵当中某一个元素为一，那么就意味着与之对应的那条边或者那条弧是存在的。而如果是零的话，那么就意味着与之对应的那条边或者那条弧是不存在的。好，下面思考这样的一个问题，如果给你指定一个节点，要你求这个节点的度或者出度入度，

这些你应该怎么算呢？啊，首先看无向图要求指定节点b的度，那其实我们可以这么做，我们可以检查一下和b相对应的这一行，这一整行总共有几个非零元素？那总共应该是有三个，所以b的度就应该是三，另外我们也可以检查和b相对应的这一列，这一列总共有几个非零元素，那同样的也是有一个两个三个。所以对于无线图来说，求顶点的度，我们可以遍历和它所对应的那一行或者那一列。检查非零元素的个数就可以求出它的度。

显然，这个操作的时间复杂度应该是on这个数量级，其中n指的是顶点的个数，当然你也可以写成这样的方式。就是我们之前使用的那个方式，这个v外面套一个类似于绝对值符号，表示的是顶点的个数。好，再来看有向图，如果给你一个指定的节点，比如说呃，指定a这个节点，那么你要求a这个节点的初度啊，应该求的是。和a相对应的这一行一整行当中，

有几个非零元素，这是它的初度。那如果要求这个顶点的入度，也就是有几条边指向它。那我们应该做的是需要遍历和它所对应的这一整列，看一下这一整列当中有几个非零元素那？而有向图中顶点的度等于入度加粗度，所以求这个顶点的度，我们只需要把啊，与之对应的这一行和这一列所有非零元素的个数进行一个加和就可以。那显然，求有向图的呃入度，出度和度的一个时间复杂度也是这样的，一个数量级。好，

那到目前为止，我们探讨的是各个边不带全值的情况，那对于这种不带全的图，我们只需要表示出各个顶点之间有没有这种邻接的关系就可以？所以我们在矩阵当中，只需要用零和一这样的两种状态来表示，而如果我们要存储的是一个带全图，或者说我们要存储的是一个网的话。那其实也很简单，我们只需要在对应的位置给它写上这两个顶点之间的，这条边的一个全值就可以。而如果两个顶点之间不存在边的话，那么我们呃可以用这种无穷来表示它们之间不存在相连的边。其实我们可以定义一个常量，就是比如说我们可以取int值的，

最大值int值的一个上限，用这个值来表示无穷。那在这个例子当中，我们认为每一条边的权值都是int类型，但是其实你是不是还可以把它的权值改成其他类型，比如说浮点类型之类的？这个可以根据你的业务场景来决定，那其实带全图也很好理解，唯一需要注意的是你应该怎么用代码来实现，来表示所谓无穷这样的一个数。可以用这种宏定义常量的方式。好，那在有的时候呃，有的人也会把就是自己指向自己的这条边的权值把它设为零。所以有时候在带全图当中，

如果一个元素，它的值是零或者呃是无穷的话，那么这两种状态都是表示说啊，与之对应的两个顶点之间不存在边。只是做题的时候有可能出现的情况啊，稍微有个印象就可以。好，接下来我们对临街矩阵这种存储结构进行一个性能的分析啊，既然它是要用于存储，那我们肯定需要关心这个数据结构，它所需要占用的空间，空间复杂度是多少？那显然，如果说这个图有n个顶点的话，那存储各个顶点的信息需要定义一个一维数组，

也就是需要on这样的一个呃存储空间。另外，我们还需要定义一个n×n的二维数组来存储和这些顶点相关的边的信息，所以存储边又需要o的n方这样的一个数量级。那我们只需要保留呃，阶数更高的这个部分，所以零阶矩阵法的空间复杂度应该是这样的一个数量级。那如果一个图里边，它的顶点数很多，但是边数很少的话，那其实这个连接矩阵当中会有很多空间是被浪费的，所以连接矩阵。这种存储方式比较适合用于存储稠密图，也就是边很多的那种图。另外，

由于我们的无向图，它的这个邻接矩阵其实是一个对称矩阵，所以我们可以利用对称矩阵的压缩存储的方法，也就只存储上三角区或者下三角区这样的方式。来进行进一步的压缩。那大家也可以趁着这个机会来回顾一下我们怎么压缩存储一个对称矩阵。好的，那最后我们还需要探讨临界矩阵的一个性质，我们这儿讨论的临界矩阵指的是不带全的图。也就是矩阵的元素只是零和一这样的一种图，我们记这个图g的临界矩阵为大a，那这个大a指的是整个临界矩阵。而不是说这个节点的信息，这可能会有一点歧义。那零阶矩阵的N次方，

它其中的某一个元素ij表示的含义是从顶点I到顶点j。长度为n的路径的数目，我们用这个具体的例子来分析一下，这是上面这个图的一个临界矩阵，用这个矩阵乘以它本身得到的结果，就应该是这个矩阵的二次方，对吧？好来看一下，如果两个矩阵相乘，那么他们相乘之后，比如说第一行第四列的元素应该怎么算呢？应该用这个矩阵的第一行。乘以它的第四列分别相乘相加，对吧？也就是a一一×a一四+a一二×a二四，

然后把这四个值都相乘相加。我们来看一下这些城市，它有什么现实意义？比如说a一二这个元素a一二对应的是AB，也就是这个元素，这个元素为一的意思是说。从a这个节点到b这个节点，是有一条边的，所以这个值是一，然后与它相乘的a二四指的是从b这个节点到d这个节点是有一条边的。从b到d，有一条边，所以这个值也是一那这两个值相乘等于一，其实这背后隐含的逻辑是。我们可以找到一条路径，

这条路径是先从a到b，再从b到d。这条路径是存在的。有一条这样的路径，再看下一个城市a一三×a三四a一三，表示的是从a到c有没有路径。这个值是零表示没有路径，然后a三四指的是从c再到d，这也是没有路径的，所以零×0=0，说明从a到c。再到d这样的路径是不存在的，所以我们把这些数值加起来就应该是零+1+0再加零，那意思就是说从一这个节点，也就是从a这个节点到四这个节点，

也就是d这个节点。如果路径长度为二的话，那我们只能找到一条符合这个条件的路径，也就是从a到b再到d，所以大家可以在暂停琢磨一下上面这句话。好，再来看一个例子，我们来看呃第二行第二列的这个元素，也就是要计算从b节点到b节点长度为二的路径总共有多少条？那第一个元素a二一×a一二，这个意味着从b到a，再从a回到b。显然，这条路径是存在的。那这两个值相乘得到的结果是一第二个式子a二二×a二二，

意思是说从b到b，然后再从b到b。但是由于我们这是一个简单图，并不存在。节点指向自身的路径，所以这条路径是不存在的，那得到的结果是零第三个a二三×a三二，这个在背后的含义是b到c，然后再从c到b。那这两个值都是一，所以说意味着这条路径是存在的，从b先到c再从c回到b，路径长度也是二。最后一条a二四×a四二啊，这个算得的结果是说从b到d，

再从d回到b。这个路径是否存在？那这个值为一，所以整体来看，从b到b的一个长度为二的路径，我们总共可以找到一条两条三条。总共有三条这样的路径。好，那后面的例子就不再展开，大家可以在自己琢磨一下，总之这个矩阵的含义就是啊，在矩阵当中对应的两个节点之间。长度为二的路径总共有多少条？从b到b，长度为二的路径有三条。

从c到c，长度为二的路径有两条。好，如果在刚才这个基础上再乘以啊，本身的这个矩阵也就是计算这个矩阵的三次方，那分析的方法也是类似的。比如这个元素的值为一，意味着从a到d长度为三的路径只有一条。那这个数是用啊，左边矩阵的第一行乘以右边矩阵的第四列得到的，看一下这个式子的含义，先看第一个部分一×0，也就是这个数。乘以这个数。它背后的含义是说，

从a到a长度为二的路径应该是有一条，但是从a到d。长度为一的路径只有零条，所以我们没办法把这两段路径拼起来，凑成一个长度为三的路径。所以一×0=0，意思是说这条路行不通。再来看这个部分。一×1，也就是AC×CD。左边矩阵和AC相对应的这个一意味着a到c长度为二的路径有一条。而另一个一意味着从c到d长度为一的路径有一条。所以我们最终要找的从a到d长度为三的路径，是不是可以把这两段路径一拼接就可以得到符合条件的一个路径，所以这个地方呃相乘等于一。

意味着说，确实可以从a到c再从c到d，然后长度总共为三，可以找到一条这样的路径，所以把所有的这些部分一加和就可以得到这个地方的一个数值一。从a到d长度为三的路径只有一条，那其他这些数值背后的一个含义，大家也可以用类似的方法再自己进行分析，这就不再展开。当然，如果想知道怎么用更严谨的数学方法。来证明那大家可以去看一下离散数学关于图论相关的那个部分。好的，那这个小节当中，我们介绍临街矩阵来存储图，

考试当中比较常见的就是这样的六个部分，其实都很好理解，大家只需要再根据这些问题再进行一个回顾就可以了。好的，那以上就是这一小节的全部内容。
