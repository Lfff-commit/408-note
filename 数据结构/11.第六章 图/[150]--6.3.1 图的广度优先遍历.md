各位同学大家好，从这个小节开始，我们要学习图的遍历算法，分别是广度优先，遍历和深度优先遍历。那这个小节中，我们要先学习广度优先，遍历算法，英文缩写是bfs，那由于树本身就是一种特殊的图，而我们曾经学过树的广度优先遍历，也就是层序遍历。所以我们会从树的角度入手来看一下广度优先遍历的一个大概的实现思想，最后还会介绍一个概念叫广度优先生成树。好的，

那我们先来看一下树的广度，优先遍历，也就是程序遍历，其实思想很简单，就是从根节点出发。找到和根结点相邻的所有节点，也就是二三四这几个节点，接下来再从二三四这几个节点出发，找到与它们相邻的其他节点，也就是五六七八这几个节点。那这样就可以依次逐层的找到所有的树里边的结点，并且我们在查找这些结点的时候都是尽可能的往横向的找，这也就是为什么叫广度优先的原因。那其实图的广度优先，遍历和树也是很类似的，

比如我们从这个二号节点出发开始进行广度优先遍历。那首先应该访问的是二号结点，那通过二号结点又可以找到下一层的一和六，这两个结点，所以接下来要访问的是一和六。那再往后，应该从一和六出发，再找到与它们相邻近的其他节点，也就是五三七这几个节点。因此，接下来访问的就应该是五三七。好，最后再从五三七这几个节点出发，再找到更下一层的节点，也就是四八这两个节点，

因此最后访问的是四八。那这个过程其实和树的广度优先是很类似的，那我们接下来看一下树和图的广度优先有什么联系和区别？第一，不论是树还是图，在进行广度优先遍历的时候，我们都需要实现这样的一个操作，就是通过某一个节点找到与之相邻的其他节点。因为只有实现了这个操作，我们才可以一层一层的往下找到所有的节点，那对于树来说，要找到与一个节点相连的其他节点。很简单，其实就是找他的孩子对吧？而对于图来说，

我们可以利用之前啊，我们提到过的两个基本操作来完成这个事情。另外一点，对于树这种数据结构，由于各个节点之间的路径是不可能存在回路，不可能存在环路的，因此我们通过树里的某一个节点搜索其他的节点的时候。那搜到的这些节点一定是之前没有访问过的节点，但是对于图来说就不一样了，由于图里有可能出现这种环路环形的路径，另外像这种无向图每一条边。它可以正向的走，也可以逆向的走，所以比如说通过六这个节点可以找到的与之相邻的节点，应该是二三七，

但是二号节点其实是之前就已经被访问过的节点。所以我们需要对这种情况进行处理，那处理的思路其实也很简单，我们只需要给各个节点进行一个标记，是不是就行了？就是标记每个节点到底有没有被访问过？如果一个节点已经被访问过了。那么下一次你在走到这个节点的时候，就把它跳过就行了，对吧？那这样我们就可以保证在遍历过程中，每一个节点都只会被访问一次。好，那不知道大家还有没有印象，我们在实现树的广度优先遍历，

也就是程序遍历的时候需要一个辅助队列的帮助。因为虽然我们用肉眼来看和这一层节点相连的，其他节点我们都可以一眼就找到，但是对于计算机来说，它只能一个一个的处理这些节点。那么暂时还没有被处理的节点，我们就需要用一个队列把它保存起来好，所以对于图的广度优先遍历，我们也可以类似的设置这样的一个辅助队列。好，那刚才我们提到三个问题，第一个问题，我们需要找到与某一个指定顶点相邻的，所有的顶点，那可以利用我们之前提到的两个。

基本操作通过第一个基本操作，可以找到指定顶点的第一个临界点，当然如果找不到的话，那需要返回负一，而第二个基本操作又可以找到后一个与之相邻接的顶点。所以通过这两个基本操作，是不是就可以找到与一个顶点相邻接的所有的顶点？当然，对于这两个基本操作使用，邻接矩阵存储的。图和临街表存储的图实现起来会稍微有一些区别，但是都很简单。而对于第二个问题，我们只需要定义这样的一个布尔型的数组来标记每一个顶点到底有没有被访问过就可以了。好的，

来看一下具体的代码实现啊，那这个数组的初始值刚开始都全部设为FALSE那数组的下标，一般来说都是从零开始的，但是由于我们这个地方给的例子。啊，各个节点的编号是从一开始编号的，所以我们在这个地方就不纠结那些细节啊，这个数组里边我们也是从一开始大家理解背后的原理就可以了。好，那来看一下，如果我们从二号顶点出发，想要广度优先遍历整个图的话。那首先我们要visit，要访问二号结点，并且把二号结点对应的visit数组设为true，

表示它已经被访问过了。接下来让二号结点入队，那我们呢？这儿的这个箭头指的是对头指针好，往后就是一个while循环，如果队列不空的话，那么我们就让对头的元素出队。也就是二号顶点出对，那接下来我们要通过二号顶点找到与之相邻的所有的顶点，那这个for循环就利用了我们刚才提到的那两个基本操作。来遍历与之相邻的所有的顶点，也就是一和六这两个顶点，那由于一和六的呃visit数组所对应的这个值都是FALSE，也就是意味着他们俩没有被访问过。所以这两个顶点都会被正常的visit，

并且会把它们俩对应的啊，这个数组的值设为true。另外，对于我们访问过的顶点，还需要把它放到队列的队尾中，也就是这个样子。好，这个for循环处理完了，和二相邻的所有的顶点，那接下来就应该再进行下一次while循环，由于此时队列是非空的。那么，需要让对头元素出对，也就是让一号顶点出对，那接下来又到了这个for循环来处理一号结点相邻的所有的结点。

那和一号相邻的有二号和五号这两个结点，但是由于二号结点的visit值为true，也就意味着它已经被访问过了，所以不满足这个if条件。因此，对二号结点不会再进行其他的处理，而对于五号结点，由于它是FALSE，所以这个if条件满足，因此会visit会访问五号结点。并且把它相对应的visit值设为true，同时把五号结点放到队列的队尾，那这样就完成了一号顶点的for循环。那接下来应该处理的就是六号顶点，六号顶点出对。

和六号相邻的是二三七，这几个顶点，那二已经被处理过，所以只会visit 3和七这两个顶点，并且这两个顶点会把它们放到对位。那这样就处理完了，六号顶点，而接下来要处理的是五号顶点，五号顶点出，对那五号顶点相邻的只有一号顶点，它已经被处理过。所以在五号顶点呢for循环里边什么也不会做，那接下来就应该是三号顶点出对那和三号相邻的有四六七这几个顶点。只有四号顶点是没有被访问过的，所以接下来访问四号顶点，

把它的visit值设为true，同时把四号顶点放到对位。那这样就处理完了，三号顶点的for循环，接下来是七号七号顶点的for循环会检查，与之相邻的所有顶点，也就是三四六八这几个顶点。那只有八号顶点是没有被访问过的，所以只会访问八号，并且把八号啊放到对位。好，那接下来只剩下四号顶点。和八号顶点，由于和它们相邻的所有的这些顶点都已经处理完了，所有的visit值都为true，

所以。四和八这两个顶点的负循环，什么也不会做好，那这样我们就完成了对这个图的广度优先遍历。得到了这样的一个遍历序列，那我们再来做个练习，如果我们从一号顶点出发，进行广度优先遍历的话，可以得到什么样的遍历序列呢？那首先肯定是一。那和一最近的应该是二和五，所以接下来应该访问二和五，然后再从二和五出发，下一步可以访问到的只有六号顶点。所以接下来访问的是六，

那接下来从六出发，可以访问到的是三和七，所以接下来是三七，那再从三出发，可以访问到的是四，所以接下来是四。接下来，从七出发可以访问到八，因此最后是八，所以这样我们就可以得到从顶点一出发得到的一个广度优先遍历序列。再看如果从三号顶点出发呃，得到的遍历序列，那第一个被访问的是三号和三号相邻的只有四六七这几个顶点，所以接下来是四。六七啊，

注意啊，这个地方大家会发现我们是按照这个各个顶点的序号递增的次序来排列的好，接下来通过四这个节点可以找到与之相邻的八号节点。那再通过六号结点可以找到与之相邻的二号结点，然后和七八相邻的结点都已经被访问过，那再从二号结点出发，可以找到一。再从一号结点出发，可以找到五，所以这就是从顶点三出发得到的广度优先遍历序列。那刚才当我们在访问三号结点的时候。找到与之相邻的其他节点，我们是按照序号递增的，这种次序来排列的四六七，但是其实。

对于广度优先，遍历算法，如果说你的这个存储结构不一样的话，那其实你从一个节点出发找到的其他节点的顺序有可能不一样。当然，对于零阶矩阵来说，一个图的零阶矩阵，它的表示方式是唯一的，所以如果你的图是用零阶矩阵存储，那你要找到三号顶点相邻的，其他顶点的话。那你找到的顺序就应该是四六七，也就是我们刚才的那种递增的次序一定是这样的。而如果你的图是用零阶表存储的，那情况就不一样了，

比如说对于二号节点，我们可以这么存储和二相邻的两个顶点，我们找到的顺序是先找到一再找到六。而如果我们的临界表是这么存储，那我们找到的和二相邻的顶点就应该是六和一。所以如果从二号顶点出发进行广度优先便利的话，那访问二之后，接下来访问的应该是六再之后才是一，而不是二一六。所以广度优先遍历的遍历序列，如果说采用临街表存储的话，那么这个遍历序列是可变的，是不唯一的。而如果采用临街矩阵存储，那由于临街矩阵的表示方式唯一，

因此广度优先，遍历的序列肯定也是唯一的。好，接下来我们回到刚才的代码看一下，刚才的代码存在什么问题？我们从顶点二出发，可以遍历其他所有的这些顶点。但是如果我们再在这个图里再增加三个顶点，并且这三个顶点和其他这些顶点是不连通的，那么对于这样的一个非连通图来说，我们从二号顶点出发。通过刚才的这一段代码是没办法访问九十十一这几个新节点的，那这个问题怎么处理呢？我们不是定义了这样的一个visit数组吗？这个数组里边记录了所有的顶点，

是否已经被访问这样的一个信息？那我们在第一次调用了bfs这个函数之后，其实可以检查一下，在这个visit数组当中能不能够找到啊？值为FALSE的顶点。如果能找到，那我们从这个顶点出发，再调用一次bfs，这个函数不就可以了好，所以啊，我们会增加这样的一段代码。增加上面这个函数，那这个函数做的事情是首先要把这个visit数组全部设为FALSE，这是一个初始化，然后会初始化一个我们需要的辅助队列。

接下来会用一个for循环来扫描这个数组，找到这个数组当中第一个呃值为force的顶点。那对于我们这个例子呃，值为FALSE的第一个顶点就是一号顶点，对了，这个地方王道书里给出的这个代码呃I的值也就顶点，编号是从零开始的。但是我们这儿的顶点编号是从一开始的，所以严格来说，这段的代码配我们的这个例子是有那么一点点不严谨的，但是没有关系，大家只需要知道我们要扫描的是这个数组。需要找到这个数组当中第一个值为FALSE的元素就可以了，好，那如果找到了第一个值为FALSE的元素，

也就是找到了一个。此时还没有被访问过的顶点，那我们就会从这个顶点出发，调用bfs。也就是刚才我们实现的这个函数，那对于我们的例子来说，就是从一号顶点出发啊，调用bfs。那这个函数的具体执行刚才已经比较细致了，这就不再分析，从一号顶点出发，我们可以访问完啊，一到八这几个节点，因为这些节点它们之间是连通的。那在访问完这几个节点之后啊，

我们的bfs函数第一次执行结束，此时一到八这几个节点的啊visit值都变成了。true好，那第一次调用完bfs函数之后啊，这个for循环还会继续的往后找，对吧？继续的往后找，直到找到一个visit值为FALSE的下一个顶点。也就是九号顶点，所以接下来又会从九号顶点出发，调用bfs函数，那显然通过九号节点可以访问到十和11。所以我们加上了这个for循环之后，我们就可以遍历完这种非连通图里的所有节点。那不难发现，

对于一个无向图来说。调用bfs这个函数的次数应该是等于无向图里的连通分量的数量。还记得连通分量的概念吗？就是极大连通子图，那对于这个无线图来说，有一个两个这样的，两个极大连通子图，也就是两个连通分量，因此就需要调用两次bfs。这个函数好，那这就是图的广度优先遍历算法好，接下来按照惯例，我们是不是应该分析一下这个算法的效率分为空间复杂度和时间复杂度？那空间复杂度主要来自于我们定义的这个辅助队列，我们来考虑最坏的情况，

这个辅助队列最大有可能需要多大呢？啊，最坏的情况是，比如说像这个图里，我们从一号节点出发来遍历这个图，那显然其他所有的顶点都和一相邻，所以在访问一号节点的时候，同时也需要把和他相邻的其他所有的。这些顶点都全部放到我们的辅助队列当中，那由于顶点的数量有呃v这么多，所以辅助队列的大小，我们最坏的情况下最多有可能需要ov这么一个量级的辅助队列。那这是空间复杂度，再看时间复杂度，对于这个算法来说，

我们需要visit每一个顶点，同时在这个for循环里，我们还需要探索所有的这些边。所以我们可以简化的认为，这个算法的时间开销主要就是来自于访问各个顶点，还有就是探索各条边，这是这个算法的主要开销。好，那来看一下，如果说我们的图是用零阶矩阵来存储的话，那么我们要访问这v个顶点。需要ov这么多的时间，另外当我们在访问各个顶点的时候，还需要探索和这个顶点相邻的啊，所有的那些边，

但是对于零阶矩阵来说，我们要找到和一个顶点相连的所有边，我们只能。就是把这个顶点所对应的一整行都依次遍历一遍，那这一整行总共有ov这么多个元素，所以我们在找每个顶点的临界点，也就是每个顶点相连的边的时候。都需要ov这么长的时间，而总共又有v这么多个顶点，因此访问临界矩阵中的各条边。其实总共需要的时间复杂度应该是ov的平方，这么样一个时间复杂度，那再加上我们访问v个顶点的时间，也就是ov这么多的时间。那只保留高阶的，

也就是平方这一项，所以整体来看对于零阶矩阵存储的图，我们进行一次广度优先遍历。需要的时间应该是ov的平方，这样的一个量级。好，再来看。如果说采用的是临界表的话，那么我们访问v个顶点总共需要ov这么多的时间，那除了访问这些顶点之外，还需要探索和各个顶点相连接的所有的边。那由于采用临接表，所以在找到和一个顶点相连的其他边的时候，其实并不需要做多余的操作，总共有多少条边其实就需要消耗多少时间。

那对于无向图来说，无向图的临界表，这些边界点的个数应该是二倍的e这么多，所以要访问一个无向图的临界表当中。所有的边结点的话，那总共需要的时间消耗应该是o的2e这么多。但是大o表示法，我们可以舍弃这个常数的系数，所以就只保留一个oe，而因此对于临街表的呃广度优先便利。总共需要ov的时间进行顶点的访问，又需要oe的时间对各个边进行访问，因此总体来看时间复杂度就是o的。v+e这么多。那这个地方大家会发现，

我们在分析这个算法的时间复杂度的时候，并没有像以前那样，就是分析最深层这个循环的循环次数。为什么没有这么做呢？大家可以去尝试这样的一个例子，就是你可以试一试，如果说一个图，它所有的顶点之间都没有边相连，并且这个图是用邻接表来存储的话。那显然，这个for循环的循环次数就应该是零次，但事实上，其实对于这些节点呃，每一个节点的访问都需要调用一次bfs，也就是都会进行一次visit。

因此，访问所有的这些节点也需要ov这么长的时间。所以如果像以前那样呃，我们只考虑最深层循环的这个循环次数的话，是会出现一些问题的，那这个和算法的实现方式有关。反正大家如果遇到让你分析广度优先算法或者深度优先算法的一个时间复杂度的话，那建议大家不要钻到这个代码里。就把这个问题简化，只需要记住广度优先和深度优先这两种算法，算法的时间开销主要来自于访问顶点和找各条边。所以大家只需要拆开来分析啊，访问各个顶点需要多少时间，访问各条边又需要多少时间，结合具体的存储结构来进行分析就可以。

好，那这是广度优先遍历的一个时间复杂度，那接下来我们还要介绍一个概念，叫广度优先生成树。不知道大家有没有注意，刚才我们在进行广度优先算法的那个过程演示的时候，有一些顶点之间的边，我们把它标红了，那标红的这些边其实是意味着说。呃当某一个顶点第一次被访问的时候，是从哪条边过去的？比如说像四号顶点，它第一次被访问到的时候，呃其实是从三号顶点的这条边过去的，而不是从七或者八过去。

那用这样的方式呃，我们对于这n个顶点的图来说，我们总共标红了n- 1条呃边。如果把其余的那些边给去掉，那这个图其实就变成了树，因为它里边已经没有回路存在了。好，那其实这就是这个图的广度优先生成树。这个生成树是根据广度优先遍历的过程得来的，我们再来重新快速的过一遍，我们先访问的是二号顶点，那和二号相邻的是一和六这两个顶点。啊，一号顶点是先入队的，六号是后入队的。

好，接下来通过一可以找到五通过六又可以找到三和七，那根据这个存储结构的信息，我们可以知道通过六往后找找到没有访问的节点，应该是先找到三号节点再找到。七号节点，所以三号节点应该是先入对的，七号节点是后入对的，因此接下来通过三号节点可以找到四号节点，而通过七号节点又找到了八号节点。那这就得到了一颗广度优先，生成树好，那现在我们对这个临街表进行一个小小的改变，我们把三和七这两个顶点进行一个调换。也就是七在前三在后，

那同样的从二号出发访问一和六一先入队，六之后入队，那通过一又找到了五通过六又找到了。七和三因为在这个临界表当中，如果找和六相邻的呃没有被访问过的顶点，那应该是先找到七再找到三，所以七应该是先入队，三是后入队。所以接下来应该检查的是和七号顶点相邻的啊，其他顶点那显然四和八都和七相邻，所以通过七号就找到了四和八这两个顶点。那这样我们得到的广度优先，生成树就和刚才的这一颗出现了一些区别。原因在于用零阶表存储一个图的时候，这个图的表示方式是不唯一的，

因此得到的遍历序列，遍历的顺序也不唯一。从而我们得到的这个广度优先生成数，也有可能不唯一，但是如果一个图是用临界矩阵来存储的话，那么它的广度优先生成数肯定也是唯一的。好，那这是广度优先生成树的概念，接下来再来看一个很相近的概念，叫广度优先生成森林。其实结合之前的例子就很好理解了。我们的这个图，它是一个非连通图，总共有两个连通分量，那上面这个连通分量可以得到一个广度优先生成树。

下边这个连通分量对它进行广度优先，遍历也可以得到一个广度优先生成树，那这两棵树。放在一起就可以组成一个广度优先的生成森林。这个相信不难理解，好的，那最后给大家留一个练习，我们之前讲的那个例子其实一直是在用无向图进行讲解，那这儿我们把无向图改成有向图。大家可以试一下从不同的顶点出发，进行广度优先遍历，能不能一次遍历完所有的这些顶点？如果从一号顶点出发，那显然从一出发只能找到五，因此就肯定需要调用多次bfs这个函数。

大家可以自己暂停来捋一遍这个函数的执行过程，那大家可以再试一下，如果从七号或者八号这两个顶点出发。来调用bfs这个函数的话，那由于从这两个顶点出发，可以找得到到其他任何一个顶点的路径，因此对这两个顶点进行bfs。肯定可以一次性就遍历完其他所有的顶点好，那这个大家可以自己试一下，那这个小节当中我们学习了图的广度优先遍历。其实它和树的层序遍历树的广度优先，遍历是很类似的，因为树本身就是一种特殊的图。所以我们设计图的广度优先，遍历算法的时候，

其实参考了树的层序，遍历的一些思想，首先我们需要建立一个辅助队列，另外我们也需要像树的层序遍历那样。需要从一个节点找到与之相邻的其他的节点，但是由于图里边会存在环路，因此我们需要设置一个visit数组来防止各个顶点被重复的访问。那除此之外，大家还需要注意如何处理这种非连通图，其实就是在bfs函数的外面再套一个for循环。这个是比较容易错的地方好，那对于图的时间复杂度分析，我们只需要呃把它简化考虑，就是访问各个节点的时间需要多少访问所有的边，找到所有的边需要多少时间。

然后把这两个时间的时间复杂度给加起来，就可以得到总的时间复杂度，那最后我们还介绍了广度优先生成树和广度优先生成森林，那在课本的这个部分其实还讲了通过。广度优先，遍历算法，来求单元最短路径的这个问题，但是这个地方我们先不讲，我们把它放到最短路径那个小节，再进行统一的讲解。好的，那以上就是这个小节的全部内容。
