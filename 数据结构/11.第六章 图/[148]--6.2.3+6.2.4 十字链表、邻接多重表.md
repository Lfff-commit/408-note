各位同学大家好，这一小节中我们要学习图的最后两种存储方式，十字链表法和零阶多重表十字链表。会用于存储，有向图而零阶多重表是用于存储无向图的，那上小节的末尾我们分析过零阶矩阵最主要的问题是它的空间复杂度太高。而临街表最主要的问题是，当它存储有向图的时候，要找某一个顶点的入边，也就是指向一个顶点的边是很不方便的，必须遍历整个临街表才可以。那如果采用十字链表来存储一个有向图的话，就可以解决这两个问题，直接看图比较好理解，这个有向图当中总共有四个节点abcd，

那这几个节点的信息会分别存在。数组的零一二三这几个位置，也就是说每个节点是有一个编号的，那对于a节点来说，有一条弧是a指向b的，另一条弧是a指向c的。所以如果从a节点的绿色绿色这个指针往右边找，会发现啊，它指向的第一条弧的信息是从零号节点指向一号节点，也就是a。指向b也就是对应这条弧，然后我们顺着这个节点绿色，这个指针也就是这条指针。继续往后找，找到下一个节点，

这个节点表示的是从零号指向了二号，也就是a指向c。所以这个节点表示的是这条弧。好，那这两条弧是从a指向其他节点，然后还有两条弧是从其他节点指向a，分别是d指向a和c指向a。那这两条弧的信息，我们要顺着a这个节点的橙色，这个指针往后面找，那这个橙色指针是指向了这一个弧的节点。这个弧结点指明了这条弧是从二号指向零号，那二号结点就是CC指向a。那如果我们顺着这个弧节点的橙色，这个指针继续往后找的话，

那就可以找到下一条弧。这条弧是从三号指向零号，也就是从d指向a。那除了c指向a和d指向a的这两条弧之外，已经没有其他指向a的弧了，所以下面这个弧结点的橙色这个指针是一个n。指向空也就意味着已经没有其他的指向a的弧了。好，所以用十字链表法来存储一个图的话，那么我们需要定义这样的两个结构体，其中一种结构体是表示顶点，另一种结构体是表示弧。那如果我们通过绿色这一条指针线一直啊，往后找各个节点的话，那么我们就可以找到。

从当前这个顶点往外发射的所有的这些弧，这些边，而如果我们顺着橙色的这条指针呃一直往后找的话，那我们就可以找到。所有指向当前这个顶点的弧。因此，如果采用十字链表法，那我们想要找到和一个顶点相连的所有边，包括出边和入边都是很方便的。那这些字段的含义在这儿已经用这种文字的方式给出，这个部分的内容最好是大家呃暂停，对着这些文字还有这个图来进行理解。用语言的方式越解释越容易乱。显然，十字链表法的空间复杂度应该是这样的一个数量级，

左边这个数表示的是顶点的个数，右边表示的是边的个数。那这个空间复杂度是不是和零阶表示同样优秀的，并不需要像零阶矩阵那样需要消耗v的平方这样的一个数量级？同时，使用十字链表法也可以解决临接表当中找入边不方便的那个问题。我们只需要顺着橙色的啊，这一系列的指针线路往后找就可以找到所有的指向，当前结点的入边。那再次强调，十字链表法啊，只能用于存储有向图。好，那这是十字链表法啊，有兴趣的同学可以自己动手实现一下十字链表法的一个代码的定义好，

所以我们用十字链表法能够得到一个比较好的方案来存储，有向图。那接下来我们再来看对无向图的存储，可不可以有一个更优的方案呢？那如果用临街矩阵存储，无向图问题是一样的，就是空间复杂度太高，而如果用临街表这种方式来存储，无向图的话啊，主要的问题是。每一条边会对应两份冗余信息，比如说a和b。之间的这条边就会对应这样的两份数据，所以如果我们要在这个无向图里边删除这一条边的话，那我们需要删除这样的两份数据。

是比较麻烦的，另外，如果我们要删除其中的某一个节点，比如说删除a节点的数据，那么我们除了删除a节点，还有a节点后边的这些啊，边节点之外。我们还需要找到这几条边的另外几个冗余数据，把这几个边节点的数据也全部删除，所以这就是用临街表来存储无相图的时候存在的一个啊，比较明显的问题。当我们删除一个顶点，或者删除一条边的时候很不方便，主要的原因在于每一条边会对应两份冗余的数据。好，

那怎么解决这个问题呢？我们可以用零阶多重表的方式来存储无相图还是一样的，直接给图比较容易理解。其实零阶多重表和刚才介绍的十字链表是很相似的，像这个无向图里边有五个顶点，那对应的就是这五份数据。同样的，这些顶点的信息。我们可以定义一个struct，然后用数组的形式来顺序的存储这些顶点的信息，所以每一个顶点会对应一个数组下标的编号。在顶点的信息当中，除了数据域之外，还需要有一个指针是指向和当前这个顶点相连的第一条边。比如，

对于a这个顶点，它和BD之间有两条边，那我们顺着a节点的这个指针往后找。找到的这一条边就是零号和一号相连的一条边，也就是a和b相连的一条边。那在这个边节点里边，我们如果继续顺着I link这个指针往后找的话，就可以找到和爱好节点相连的下一条边。好，所以如果我们顺着橙色的这一条指针继续往后找，那找到的下一条边的信息就是零号和三号相连的边，也就是a和d相连的这条边的信息。好，那除了这两条边之外，已经没有其他的边，

再和a也就是和零号结点相连了，所以这个结点的I link指针是指向n。表示已经没有其他的边和a相连了，好再来看b节点，对于b来说有ACE这几个节点和它直接相连。那么，我们通过b节点的这个指针，往后找找到第一条边的信息，这条边是a和b相连的一条边，那由于这是一个无向图，所以b节点的编号一它到底是在左还是在右？其实逻辑上来看是等价的好，现在b节点的编号一是存在右边这个位置，所以如果我们要继续找到和b节点相连的下一条边的信息，那我们需要。

通过绿色这个指针，也就是j link来往后寻找，也就是找到这个结点，那这个边结点是指明了有二号和一号相连的一条边，也就是c和b相连的一条边。那同样的b是存在右边这个位置的，所以我们需要通过绿色这个指针继续往后找。那后面这个结点指明了，这是四号和一号结点相连的边，也就是也就是e和b相连的边，那到此为止，我们就可以找到所有的和b结点相连的三条边的信息。当然了，如果你的这个无向图是一个带全图的话，那其实也可以在各个边节点里边增加一个字段，

表示每一条边的权值。好，总之用零阶多重表的这种方式来存储无相图，想要找到和某一个顶点相连的边是很方便的。同时，每一条边只会对应一个边节点，只有一份数据。因此，就不需要像临街表那样同时维护两份冗余的数据，那这种特性可以保证我们在删除一个节点或者删除一条边的时候，操作起来会方便很多。比如现在我们要删除a和b相连的这条边，那这条边对应的啊，边节点是这个节点，那这个节点被删除之后，

我们需要修改啊，这样的两个指针。上边这个指针是指向和a相连的第一条边，那么我们是不是其实只需要在删除这个节点之前顺着橙色这个指针来找到它的下一条边对应的是哪个节点就可以？所以在删除这个节点之后，我们只需要让a节点的啊，这个指针指向他后面的一个边就行了。那对于b节点是不是也一样的？其实我们只需要通过绿色的这个指针找到和b节点相连的下一条边的数据，然后让b指向它就可以了。所以删除一条边是很方便的，接下来看，如果我们要删除一个顶点，比如说要删除e的话，那除了删除e本身的数据之外，

我们还需要删除和e相连的。所有这些边的信息，那么我们通过这个指针可以知道和e相连的边应该是这个边还有。这个边，所以我们把这两条边对应的数据删除。那删除这两个节点，会导致这两个指针的指向会出现问题，那来看一下这两个指针的值应该修改成什么，比如说像左边这条指针。这条指针指向的是和一这个结点相连的下一条边。那从当前删除的这个节点来看，和一相连的，再往后的一条边已经没有了，所以我们在删除下面这个节点之后。其实只需要把上边这个节点绿色的指针把它复为空就可以。

那右边这个节点也是一样的，只需要把下边这个节点的啊空指针复到这个位置就可以，也就是变成这个样子。好，所以可以看到采用零阶多重表来存储无相图的话，那它的空间复杂度很优秀。甚至要比邻接表的空间复杂度还要更好，因为邻接多重表里边啊，不需要存储冗余的边的信息。另外，和临街表相比，在临街多重表当中进行编的删除或者节点的删除，这些操作会变得很方便。所以采用零阶多重表来存储无相图是非常好的一个解决方案。好再次强调，

零阶多重表只能用于存储无相图。好的，那这个小节中我们学习了十字链表和零阶多重表这样的两种图的存储结构。那由于这两种实现方式在实际写代码的时候，这个代码的逻辑会比较复杂，所以也不太可能在考研当中用手写的方式让大家实现十字链表和零阶多重表。因此，这两种存储方式只需要理解它们各自的一些特性就可以。那十字链表法可以用于存储有向图，那这种方案既解决了临接矩阵。空间复杂度太高的问题同时也解决了，用临街表存储有向图的时候，找到有向图的入边不太方便，必须便利的这个问题。而零阶多重表可以存储无相图，

同样的，它既解决了啊，零阶矩阵这个空间复杂度太高的问题，同时也解决了零阶表当中。进行边的删除或者顶点的删除不方便，每一条边还会对应两份冗余数据，这样的问题，另外对于一个给定的图，采用十字链表法或者连接多重表来存储的话。那表示方式也是不唯一的。好的，那以上就是关于图的存储结构相关的全部内容。
