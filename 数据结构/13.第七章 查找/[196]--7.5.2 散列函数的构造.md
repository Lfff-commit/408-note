各位同学大家好，在这个小节中我们要学习如何构造一个散列函数？散列函数的作用是可以把一个关键字映射到与之对应的存储地址上。当我们在设计一个散列函数的时候，应该尽可能的减少不同的关键字之间。发生冲突的情况，这是设计散列函数的一个核心目标。那除了这个核心目标之外，当我们在设计散列函数的时候，还应该注意哪些问题呢？这是我们首先要明确的问题。在这个视频中，我们会介绍四种常见的散列函数构造方式，分别是除留余数法，直接定值法。

数字分析法和平方曲中法四种方法适用的场景各不相同，在考试当中最常考的是除留余数法。在现实应用当中，最常用的也是处流于数法。好，首先我们来探讨第一个问题，当我们在设计散列函数的时候，应该注意哪些问题？首先第一个问题。我们要确保散列函数的定义域必须能够涵盖所有有可能出现的关键字。举个例子，比如我们这有个散列表，它的长度是13，而关键字可能取得的值是任何一个整数。可能是正的，

也可能是负的。那在这种场景之下，假设我们设计了一个这样的散列函数，先对关键字，开根号，然后再对13取余。那么，这个散列函数的定义域显然不能支持关键字为负值的情况。比如关键字的值是负四，那么对负四是没办法开根号的。好，所以这是第一个要注意的问题，我们设计的散列函数，它的定义域必须能够涵盖所有有可能出现的关键字。好，

第二点需要注意的是散列函数的值域不能超出散列表的地址范围，像刚才这个例子。散列表合法的地址范围应该是零到12，那假如我们设计的散列函数是用。关键字的值对15取余，那么这个散列函数的值域就有可能是零到14。显然，13和14是两个非法的地址。好，这是第二个需要注意的地方。接下来，为了尽可能的减少冲突，散列函数计算出来的地址应该尽可能的均匀分布在整个地址空间当中。举个反例，假设我们设计了这样的一个散列函数，

先用关键字的值乘以13，然后再对13取余，那显然任何一个整数乘以13，再对13取余余数肯定都是零。所以这个散列函数会把所有的关键字都映射到地址为零的这个地方。这些关键字不会均匀的分布在整个地址空间里，而是会集中的映射到零这个位置。显然，这么做肯定会经常发生冲突。好，那这是第三个需要注意的地方。接下来第四点，我们的散列函数应该尽可能的简单，要让计算机能够快速的计算出任何一个关键字，所对应的散列地址。

来看个反例，假如我们的散列函数会先对关键字取阶乘，然后再对13取余。那么，如果关键字的值比较大，这个阶乘的计算就需要耗费很长的时间。比如，关键字的值是100万。那么，计算100万的阶乘，对于计算机而言，就需要进行100万次的乘法运算，这运算的耗时是比较久的。那这样就会导致散列函数的这个计算效率会很低，从而也会导致散列表的查找效率变得很低。

好，那么这是设计散列函数的时候应该注意的一些问题，接下来我们学习四种常见的散列函数构造方法。第一种方法，除留余数法很简单，我们之前的例子当中一直在使用这种方法，就是用关键字对一个质数p取余。有的同学可能很久没有接触质数的概念了，质数指的是只能被一和它本身整除的数。比如说五是一个质数。而四不是一个质数，因为四可以被一整除，可以被四整除，也可以被二整除。而五只能被一和它本身整除。

那与质数相反的概念叫合数。刚才说的四就是一个合数。好回到除留余数法，用这种方法构造散列函数的时候。直接用关键字对一个质数p取余。那这个质数p应该取一个不大于散列表表长的最大的质数。来看两个例子，假设散列表的表长是13，那么不大于表长的最大的一个质数就应该是13，它本身。所以在这种场景之下，我们可以令散列函数等于呃关键字对13取。好，再看一个例子，假设散列表的表长等于15，

但是15它本身不是质数那么不大于15，但是又最接近15的质数。同样应该是13，所以在这种场景之下，我们同样应该让除留余数法的这个散列函数等于关键字对13取域。而不是对15取。好，所以使用除留余数法构造一个散列函数的时候。这个p值应该取不大于表长的最大的一个质数。好来看一下，为什么要有这样的规定？先说结论，我们对质数取于可以让散列地址的分布更均匀，从而也可以减少冲突。来看这样的两个例子，

假设散列表的表长是八在左边，这个例子当中，我们采用除留余数法构造散列函数。在这个地方，我们对八取余而八是一个合数。而右边这个例子同样散列表的表长是八，那不大于八的，最大的质数应该是七。所以右边这个例子当中，我们对七取。那假设可能出现的关键字是这几个。那么，基于左边这个散列函数，这些关键字会被映射到这几个地方，二和十被映射到同一个位置。

四和12被映射到同一个位置。六和14也被映射到同一个位置，那这就会导致冲突。而对于右边这个散列函数而言。大家可以计算一下，用这些关键字分别对期取域，那么它们就应该分别被映射到这些个位置。可以看到，对质数取余的时候，这些关键字的散列地址可以分布的更均匀，也更不容易发生冲突。导致这个现象的原因是，当我们在进行取余运算的时候，这个运算的结果会被公因子所影响。左边我们对合数八取于那八，

这个数可以被二整除，而这个场景中出现的所有关键字都是偶数。都可以被二整除，因此所有的关键字和八都有一个公因子二。那这就会导致这些关键字对八取于运算的结果肯定是零二四六。而不可能映射到一三五七这几个位置。好，对具体的原理感兴趣的同学可以去研究一下数论。通过这两个例子，主要是想让大家更直观的感受到，为什么除流余数法当中这个p值应该选择一个不大于表长的质数。只要能体会到这一点就可以。用除流域数法去构造散列函数，这种方法是比较通用的。在现实应用场景当中，

也经常使用除留余数法。接下来学习第二种散列函数的构造方法，直接定值法。这种方法比较适用于关键字的分布，基本连续的场景。用一个线性方程把一些连续分布的关键字映射到连续的散列地址上。好用一个例子来理解，假设有一个班有30个学生的信息，他们的学号都是连续的。要把这30个信息存入散列表，我们可以用直接定值法去设计散列函数。学号是每一个学生的关键字。可以用学号减掉第一个学生的学号。用这样的散列函数，就可以把这30个连续的学号分别映射到零到29这几个位置。

这种方法不会产生冲突。但是如果关键字的分布。不够连续，就有可能导致散列表当中出现比较多的空位，造成存储空间的浪费。比如这个班尾号为幺七九，尾号为幺八幺，尾号为二零二的。这几个学生都被开除了。那么，散列表当中这三个位置就是空闲的，没有被利用的。这就会导致存储空间的浪费，所以通过这个例子可以体会到直接定值法比较适用于关键字的分布，基本连续的场景。

用一个线性函数求得散列地址。好，这是直接定值法。接下来第三种方法，数字分析法。同样的，结合一个例子比较好理解，假设我们要把一些手机用户的信息存到一个长度为一万的。散列表当中，以手机号码作为关键字去设计散列函数，那长度为一万的散列表。它的存储地址应该是零到九九九。那手机号码的规则大家都很清楚，前面的三位通常是和运营商有关。中国移动，

联通，电信前三位各不一样，中间的四位通常是地区的区号。而末尾的四位每个人的电话号码就各不相同，所以以手机号码作为关键字的话。那前边的这七个数码位。这些值的分布是不均匀的。只有末尾的四个数码位，它的分布是相对均匀的，那我们可以直接选取末尾的四位。作为散列地址。比如尾号为二八七五的这个手机用户的信息就直接放到散列表当中，下标为二八七五的位置。好，所以如果在一个应用场景当中有可能出现的这些关键字的集合是已知的。

并且我们可以明确的观察到，在所有的关键字当中，某几个数码位，它们的分布是比较均匀的。那我们就可以直接用这几个分布均匀的数码位作为散列地址，那这就是数字分析法。好，最后一种方法叫平方曲中法。这种方法适用于关键字的每个数码位取值都不均匀的情况。同样的，看一个例子。假设某一个汽车品牌会用底盘，发动机和外观这样的三个维度去作为一款汽车的型号。比如这个品牌有这样的一些底盘型号。代号分别是二八三四三九六零。

这些数值的分布并不均匀，同样的，发动机的型号还有外观号，这些代号的分布也都不均匀。那么，一款车的型号可能是这样的，三元组组成的第一个部分是底盘的代号，第二个部分是。发动机的代号第三个部分是外观的代号。那在这种场景下，这些关键字每一位的取值都不够均匀，也没有任何规律。那假设这个品牌的汽车型号总共有100种。我们要把这100种车型的信息存入长度为100的散列表，要如何去设计这个散列函数呢？

在这种场景之下，就很适合用平方取中法，顾名思义就是先对关键字取平方。比如六零五零三零的平方是这样的，一个值下面这几个关键字都一样，先取平方。第二步，我们取平方的中间两个数值位作为散列地址，这就是所谓的平方取中法。虽然这些关键字的每个数码位分布都不均匀，但是平方取中法得到的这个值分布是相对均匀的。因为中间的这几个数值位会受到每一个数码位的影响。怎么理解这句话呢？举个例子，假设我们现在要计算985的平方，

那么乘法运算的式子是这样的。个位五和九八五相乘得到四九二五。十位八和九八五相乘得到七八八零。百位九和九八五相乘得到八八六五。最后，我们会把这些数错位相加，那可以看到我们取结果的中间这两位，这两位的值显然会受到。每一个数码位的影响。那这种特性就可以保证，虽然这些关键字的呃，各个数码位单独拿出来看分布都是不均匀的。但是先平方再取中间的几位数，那这几位数肯定会受到每一个数码位的影响，这就可以保证最终我们取得的这个散列地址。

分布是均匀的。好，那这就是平方曲中法，它适用于关键字的每一位取值都不够均匀的情况。好的，这个小节中我们学习了四种常见的散列函数构造方法，分别是除流余数，直接定值，数字分析，和平方曲中。每种方法适用的场景各不相同。主要得看在你的应用场景当中，有可能出现的这些关键字的值有什么特性？那这些方法都不难理解，大家可以再结合思维导图进行回忆。

考试当中最常考的还是除流余数法，这点大家会通过课后习题有更深的体会。好的，那以上就是这小节的全部内容。
