好的，那这个小节我们要学习一个很重要的算法，折半查找这个算法，不管是小题还是大题，其实考察的频率都还是挺高的。那我们首先会介绍这个算法的实现思想，并且给出实现的具体代码，另外折半查找的查找判定数也是很高频的考点，所以我们会花比较大的篇幅来介绍它的查找判定数应该怎么画？好，首先来看这个算法的实验思想，那折半查找又称之为二分查找，它只适用于有序的顺序表。首先，第一个特征有序的意思是说，

我们的这个查找表当中，里边各个元素的关键字都必须是有序的，要么是递增排放，要么是递减排放，那我们像这个例子当中。给出的这些元素都是递增的，另外顺序表的意思是说我们现在这些元素其实就是用一个数组把它们存放起来的，然后下面零一二这些指的是数组的下标。好，那来看一下这个算法的执行过程，假设我们现在要在这个有序的顺序表当中找到33这个元素，那首先我们会用两个指针low和high。来分别指向我们目前要搜索的这个呃区间范围，那刚开始low指向0 hi指向最后一个元素，接下来我们会不断的把这个搜索的范围减小。

那第一轮我们要检查的元素是low和high，它们中间的一个元素，我们用一个指针m来指向。那MID的计算方式很简单，就是low+high÷2，所以high=10，low=0，那十+0÷2就是五，所以我们第一个要检查的元素就是29。刚好是最中间的一个元素好，现在对比MID所指向的元素29还有我们的目标关键字33，会发现我们要查找的目标要大于当前MID所指向的这个元素。所以如果33这个元素存在的话，那么一定是在幂的所指位置的右边，这个部分也就是六到十这个部分。

才有可能找到33，因为MID往左的话，这些元素肯定都要比29还要更小，因此我们可以把左边这整个区域都给排除。好，所以这样大家就知道为什么要叫二分查找或叫折半查找了，因为我们当前检查的这个元素MID。把目前有可能出现目标元素的这一整个区间，把它一分为二分，为了左边的区间和右边的区间。那根据我们想要找的这个关键字和当前me的所指元素的这个大小关系，我们就可以知道我们到底应该是在左边还是右边来查找？好，那现在我们已经确定33肯定只会出现在右边这个区间，因此我们要做的就是把low这个指针指向。

六这个位置也就是刚才me的指针，右边的一个位置好，所以接下来我们要检查的就是这个区域内有没有33？同样是检查low和high，这个区域内的中间那个元素，也就是六+10÷2，那就是要检查八这个元素。那这个元素的值是37，37是要大于33的，所以如果33存在的话，那么肯定是在MID所指位置的左边这个区域。因此，接下来我们会让hi指针指向七这个位置，而接下来要检查的元素还是一样，我们用low+hi÷2。

那六+7÷2向下取整，应该刚好是等于六，所以接下来这一轮要检查的元素就应该是六，这个元素这个元素的值是32。而32是要小于33的，如果33存在，那么只有可能是在MID所指位置的右边区域，因此接下来我们会让low这个指针指向MID。右边那个位置，也就是让low=7好，再往后这一轮low和high都等于七那么low+high÷2也是等于七，所以接下来要检查的是。这个元素那刚好，这个元素的值是等于33的，那到这一步我们就找到了33这个元素查找成功。

刚才我们给的这些例子是查找成功的例子，再来看一个查找失败的例子，假如还是在刚才那个表当中，我们要找12这个元素啊，但是用肉眼看一下我们这里边是没有12的。好，那刚开始也是一样的，我们让low和hi指向这整个数组的头和尾这两个位置。那第一轮要检查的是五这个元素楼加hi÷2，那么12要小于29，所以如果12要存在的话，那只有可能是在MID所指的位置往左的区间内。因此，接下来我们会让hi=mid- 1，也就是等于四这个位置。

好，接下来在这个区间内搜索那么low+hi÷2，所以接下来MID应该是等于二这个元素的值是13，那么12小于13，所以还应该在MID的左边，所以我们会让hi等于。mid- 1好接下来low+hi÷2，因此接下来检查的是零这个位置，那当前MID所指的元素七。是要小于12的，所以如果12存在的话，那么只有可能是幂的所指位置，右边的这个区间内，所以接下来我们让low等于幂的加一。好，

接下来这一轮low+high再除以二，也就是一+1÷2，还是等于一，因此接下来检查的就是十这个元素。那由于我们要找的目标12依然是大于十的，所以如果12存在的话，那只有可能是在幂的所指的右边那个区间内，所以和刚才的处理逻辑一样，我们会让low这个值等于幂的。加一也就是变成这个样子，照理来说，我们的low指向的应该是整个查找区间的最左边，对吧？而hi指向的是查找区间的最右边。而现在，

本应该在左边的指针跑到右边来了，那就说明查找失败了，好，那这就是查找失败的例子，所以这就是折半查找的一个实现思想，那结合刚才例子，再来写出代码，就会相对轻松一些。刚开始让low和high分别指向我们整个查找表的最头和最尾的位置。接下来，每一轮的while循环都会检查low和high的中间位置这个元素，它和我们的目标关键字是否相等。如果相等，那么就直接返回MID所指的位置，就表示查找成功。

而如果当前MID所指的元素大于我们要找的目标元素的话，那就说明接下来我们应该在MID所指位置的左边区间内。呃，左半部分继续查找，因此我们会让hi=mid- 1那另一种情况呢，就是在右半部分查找，当然了，这个地方的代码我们是。基于我们的这个查找表，各个元素是升序排列，是基于这样的一个前提条件，如果各个元素是降序排列，是不是这些判断条件就得做相应的更改？当然，原理都是类似的好，

那这是查找成功的情况。好，那经过几轮的对比之后，如果最后导致low大于high，也就是while条件不满足的话，那跳出循环之后返回负一表示查找失败，这就是刚才我们举的第二个例子。好讲到这，大家应该就更能够理解刚开始为什么说这个折半，查找它只适用于有序的顺序表，因为用顺序表或者说数组的这种形式，我们可以根据数组的下标立即进行。就找到中间元素，到底是在哪？如果这个查找表是一个链表的话，

那我们要找到链表的中间元素是不是只能从头从头开始依次往后找？一直找到最中间的这个位置，也就是说链表不具备这种随机存取的特性，所以折半查找这个算法是不可能基于链表来实现的。好的，那这就是折半查找的实现对了，再次强调我们这儿给出的这些判断逻辑是基于里边的数据元素是按深序排列，基于这样的一个前提条件来写的。如果各个元素是降序排列，那这些条件应该怎么写呢？大家也需要思考一下好，那接下来我们来看一下刚才给出的这个折半查找它的查找效率如何？那对于刚才这个例子来说，刚开始low和high分别是指向头和尾这两个位置，那第一个被检查的应该是五这个位置的元素，

所以如果我们要查找的。关键字刚好是29的话，那是不是只需要经过一轮的循环就可以，只需要进行一次关键字对比好，那如果我们要查找的关键字小于29的话。接下来我们是不是应该在左边这个区间也就零到四之间来查找？如果是大于29，那我们就应该在六到十这个区间内来查找好，先来看左边，如果我们要找的关键字小于29，那么接下来我们应该检查的就应该是。low+high÷2，也就是零+4÷2，那么要检查的就是下标为二的这个元素，而如果我们的关键字大于29，

那我们接下来应该对比的是八这个元素。好，所以通过第二次的关键字对比，或者说第二轮的循环，我们可以找到的元素应该是13或者是37。那如果依然不是这两个元素，那接下来是不是一样的？我们需要根据我们要查找的关键字和当前MID所指向元素的大小关系来判断我们再往后应该在哪个区间内查找？比如，如果关键字大于13的话，那接下来我们就应该让low=3，high=4好。总之，在第三轮的循环当中，我们呃，

有可能会检查这样的几个。区间，所以现在第三轮的循环，我们有可能会检查的元素应该是76，32，41这几个元素。如果查了这几个元素，还没有找到目标的话，那我们是不是还得进行第四轮的循环？那第四轮的循环就肯定只剩下啊，十十九三十和43这样的几个元素。好，所以对于刚开始给出的这个查找表，在查找成功的情况下，我们最多有可能进行一轮查找，

两轮查找。三轮查找，四轮查找，最多就是四轮，这是查找成功的情况，而对于查找失败的情况，是不是我们再补上失败节点就可以？也就是说，我们要查找的关键字，如果落在这儿紫色方框内的某一个区间内，那么最终我们肯定是查找失败的。好，那来算一下，对于这个例子，我们采用折半查找的一个平均查找长度asl。

首先看查找成功的情况很简单，只有最上层的这个元素是只需要一次关键字的对比就可以找到的。而第二层的这两个元素，这两个元素需要进行两次的关键字对比。那第三层的这四个元素总共需要三次的关键字对比，而第四层的这四个元素需要进行四次关键字对比。而整个查找表当中，总共有十一个元素，所以我们默认查找各个元素的概率都是相等的，因此这要乘以一个十一分之一，那用这样的方式就可以得到。查找成功的情况下，一个平均查找长度。再看查找失败的情况，那由于这里边总共有十二个啊，

失败节点，所以我们认为每一种失败情况发生都是等概率的，因此我们在这个地方乘了一个十二分之一。好，对于上面的这四种情况来说，如果发生这四种失败的情况，那我们总共只需要进行三次关键字对比。而如果发生的是最下面这一层的啊，这八种失败情况，那么我们总共就需要进行四次的关键字对比。好所以相乘相加，然后再乘以一个平均的概率十二分之一就可以得到查找失败的时候，一个平均查找长度。好，那这是刚才的例子，

它的一个查找效率的分析，我们是基于它的查找判定数来进行这一系列的分析，那考研中还是很喜欢考察折半查找的这个判定数的。所以接下来我们再来进一步的研究查找判定树应该怎么构造，或者说这个折半查找的判定树它有什么样的一些特性。好，回到刚才这个例子的第一步，刚才给出的顺序表当中，总共有11个数据元素，也就是有奇数个数据元素。那么，在刨除了MID所指向的这个元素之后，刨除这个元素之后，还剩十个，也就是还剩余偶数个数据元素，

因此这偶数个数据元素刚好可以被均匀的平分为。左右两个部分，左边五个，右边五个，那再下一级的分割是不是也一样的？左边这个部分有奇数个，右边这个部分也有奇数个，那在分别刨除中间的这个元素之后，是不是刚好？又可以把它切分为均匀的两个部分，左边两个，右边两个，那再来看另一种情况，我们把原始的这个顺序表干掉，一个元素，

也就是现在总共有十。十个数据元素偶数个数据元素，那么此时算得的幂的值是四，原本本来是有偶数个数据元素，但是现在刨除了中间这个幂的元素之后。就只剩下奇数个元素，那在这种情况下，通过幂的切分之后，左半部分会比右半部分要少一个元素。好，所以根据刚才得出的这两个结论，这两个规律我们可以知道左边这四个元素，再往下切分一层的话，刨除MID之后，左边的元素会比右边的元素少一个。

而右半部分总共有五个元素，刨除MID之后，左右刚好可以一边分两个，所以再往下一层就应该是这个样子。好继续往后分割也是一样的，如果当前这个区间内只有偶数个元素的话，那么在进行分割之后，下一层的左边会比右边要少一个元素。因此，我们得到这样的一个结论，在折半查找的判定数当中，如果我们计算MID是用low+hi÷2，然后向下取整的话。那么，对于任何一个节点来说，

它的右子数当中的节点数肯定要比它的左子数，节点数要多一个，或者就是左右两个子数的呃，节点数是相同的。而不可能是左子树中的节点数，还要比右子数的更多，因此基于这个特性，我们来推演一下，如果说一个顺序表当中。有一个元素，那么它的查找判定数显然就只有这样的一个节点，而如果这个顺序表当中有两个元素，那么这个顺序表的折半查找判定数应该是什么样呢？根据刚才的这个结论，我们知道只有可能是右子树的节点比左子树的节点更多，

对吧？所以如果有两个元素的话，那么它的判定数形状肯定是这个样子。好继续，如果这个顺序表当中有三个元素，那么这个顺序表的折半查找判定数有没有可能是这样？不可能对吧，因为右子数最多比左子数多一个节点，所以如果这个判定数长这个样子，那么对于一这个节点来说，它的右子数当中的节点数就要比左子数的节点数。还多了两个就不符合，刚才我们得到的这个结论，所以三这个节点只有可能是连在这个位置。好，

接下来如果有四个元素的话，那么它的查找判定数只有可能是这个形态，对吧？那含有五个元素的情况是这样。这是六个元素。七个元素。好，总之，无论这个顺序表当中包含多少个元素。它所对应的折半查找判定数一定都是要满足我们这儿给出的这个特性的，那需要注意的是这儿我们给出的一二三四这些，它只是一个编号，并不是关键字的值。如果一个题目问你某一个顺序表，它有14个数据元素，

那么这个顺序表的折半查找判定数是什么样子？你是不是只需要把16和15这两个节点删掉，然后保留剩下的部分，这就是含有14个元素的顺序表所对应的折半查找判定数？所以这给出的编号并不是指关键字的值，这个不要混淆好，那通过刚才我们构造这个折半查找判定数的过程呃，不难发现。这个折半查找判定数，它一定是一个平衡二叉树，任何一个节点的左子树和右子树的深度之差都不会超过一。另外，从刚才我们推理的那个过程可以看到，这个折半查找判定数，它肯定只有最下面一层是不满的。

因为这个判定数和完全二叉树一样，都是只有最下面这一层是布满的，所以对于有n个元素的折半查找判定数，它的树高h的计算方法其实和完全二叉树的树高。计算的方法是一样的。那大家也可以去复习一下，完全二叉树的高度，我们是怎么算的？那这就不再赘述，总之我们这想强调的是这个判定树的树高h。可以达到log 2n这样的一个数量级，而这个数量级就直接反映了折半查找的一个时间复杂度。好，另外我们不难发现，折半查找判定树它里边的这些节点关键字都是左子树的关键字，

小于中间节点。而中间节点又要小于右子树的关键字。所以它是符合二叉排序树的定义的，并且它是一个平衡的二叉排序树。另外，对于有n个成功节点的判定数，它的失败节点个数应该是n+1个。其实我们在添加这些失败节点的时候，都是把这些失败节点连到了各个成功节点的空链域上，对吧？而在二叉树的性质那个小节中，我们说过，对于有n个节点的二叉树来说，总共会有n+1个空链域。因此，

失败节点的数量等于空间域的数量，也就等于n+1个。那这个部分不记得的同学也可以再回去看一下二叉树的性质，那个小节的内容。那大家也可以自己数一下，这总共有11个成功节点。然后失败节点是有11+1，也就是有12个。对于之前我们推出的16个元素的判定数，我们如果加上这些失败节点，大家也可以数一下，总共应该是有16+1，也就是有17个。好，那搞清楚折半查找的这些判定数之后，

我们就可以很方便的分析出折半查找的查找效率如何？之前我们得到了这样的一个结论，就是如果我们不考虑不考虑失败节点的话，那么对于含有n个元素的顺序表。它所对应的折半查找判定数高度应该是这样的一个数值，也就是说在查找成功的情况下，我们查找到任何一个节点，总共需要的对比次数肯定不会超过这个数高。h这么多。这是查找成功的情况，那查找失败的情况也是一样的，所有的这些失败节点的啊，关键字对比次数肯定也都不会超过树高h。而树高h，它的数量级也就是log 2n这样的一个量级，

因此折半查找的时间复杂度需要对比的次数也就是o的log 2n这样的一个数量级。所以它比顺序查找这样的一个时间复杂度要优秀的多好的，那这个小节我们介绍了折半查找的一个呃实现的方法。其实关于折半查找的算法思想，还有算法的实现都是比较简单的，只是折半查找的判定数的一个性质，我们花了比较多的时间来研究。最关键的特性就是这一点，如果说我们的幂的值是等于low+hi除，以二向下取整的话，那么对于任何一个节点来说，只有可能是右子树的节点数要比左子树的节点数要更大。多一个，或者就是相等，只有可能是这样的，

那由于这样的特性，我们得出了判定树的树高h=log 2 n+1向上取整。得到了这样的一个结论，当然这儿的树高h指的是不包含失败节点，如果包含失败节点的话，就应该是h+1。好，那既然折半查找它的时间复杂度啊，表现上要比顺序查找的on这样的一个数量级要更优秀的多，那么经常会有题目问折半查找的速度是不是一定要比？顺序查找的更快呢，那显然不是对吧？比如说像这个例子当中，我们要找七这个元素，如果采用顺序查找的话，

那只需要对比一个关键字，也就是第一个元素，我们就找到目标了，而如果采用折半查找的话，显然要查找的次数要。更多，所以我们可以说大部分情况下呃折半查找的表现要比顺序查找要更优秀，但是我们不能说任何情况下，它都一定要更优秀，这是错误的。好，这一点需要注意，那另一点我们刚才在构造那个判定树的时候，我们是让MID=low+hi除，以二向下取整。

而如果说MID=low+hi除，以二向上取整的话，这个数应该是什么样子呢？其实分析方法和之前类似，比如当前low和hi。所包括的这个范围内，它总共有11个元素，也就有奇数个元素的话，那么low+hi÷2向上取整。可以让MID刚好指向最中间这个元素，那这样也可以拆分为左右两个相等的部分，而如果我们减少一个元素，比如说有偶数个元素。那么MID等于漏加h÷2向上取整，所以MID的值应该是等于五，

因此通过MID把整个序列拆分成两个部分。此时会发现，左边的这个部分要比右边的部分要多一个，所以如果MID的算法我们改成这样的话，那么当我们在构造判定树的时候。就应该是左子树当中的元素个数，有可能会比右子树中的元素个数要多一个。和之前得到的结论刚好是相反的，所以在这种情况下，如果有一个元素，那么它的判定数是这样的，有两个元素，那么判定数应该是这个状态。因为只有可能是左子数比右子数更多，如果有三个元素，

那么对应的判定数应该是这个形态，那之后的就大家自己看。好再次强调，我们这儿给出的编号，其实指的是我们的判定数当中总共有多少个元素，并不是指关键字的值。好，总之幂的计算方式到底是向上取整还是向下取整？这个计算方式的不同，也会导致我们画出来的判定数的形态也不一样。那相关的问题，大家会在课后习题中遇到好的，那以上就是这个小节的全部内容。
