好的，在这个视频中，我们会以线性探测法为例，去探讨散列查找的性能分析。在历年真题当中，但凡考到散列，查找性能分析的有80%左右的题目都是基于线性探测法。所以我们会以这种方法为例好，那么首先我们会探讨如何计算散列表的asr，也就是平均查找长度。那这个是考研的重点，在大题和小题当中都有考察过asl的计算，其次我们会引入啊两个概念，分别叫做装填因子以及。聚集现象又叫堆积现象，

那这两个因素都有可能会影响散列表的查找性能。好，那首先我们探讨第一个问题，如何计算散列表的asl直接上立体假设这儿有一个散列表？啊，长度是16，那么对应的数组下标就是零到15。这个散列表初始为空，并且设计了一个散列函数啊，我们用关键字对13取余数。用这样的方式可以确定一个关键字的这个呃，初始散列地址当发生冲突的时候，我们会采用线性探测法去处理这种冲突。好，后面给出了这个关键字的插入顺序。

第一个小问让我们画出所构造的这个散列表好，首先插入关键字19，19对13取余，余数等于六。所以19这个关键字啊，它的初始散列地址应该在六这个位置，那插入到这个地方。接下来14，它的初始散列地址计算等于一插入后一个关键字，23初始散列地址等于十。插入到十这个位置好，再往后现在出现了不一样的情况，一这个关键字，它对13取于啊等于一。然而，

现在一这个位置已经被其他的元素所占领了，对吧？也就是说，发生了冲突。那么，当发生冲突的时候，题目说让我们采用线性探测法，那线性探测法的规则很简单，如果当前这个位置发生冲突，那么就往后面。往右边再探测后一个位置好，那后一个位置二这个位置，此时为空对吧？所以一这个关键字插入到二这个地方。好再往后68这个关键字，

它应该插入到三这个位置，再往后20插入到七。84这个关键字对13取等于六。然而，此时六这个位置已经有别的元素了，也就是产生了冲突，那么往后探索一个位置，再往后探索一个位置。八这个位置此时为空，所以84这个元素应该插入到八这个位置。好，继续后一个关键字27，它的初始散列地址等于一一，这个位置已经被占用。那么，

按照线性探测法，往后探测二这个位置也已经被占用，三这个位置也被占用，直到探测到四这个位置的时候。这个位置为空，因此27插入到四这个位置。接下来55这个元素初始啊，散列地址等于三这个位置已经被占用，探测四再往后探测到五。因此，55这个元素应该插入到五这个位置。好，接下来11这个元素插入到11这个初始散列地址。十这个关键字初始散列地址等于十发生冲突，往后探测一个单元，

再往后探测一个单元，12这个位置为空，所以十这个元素插入到12这个位置。接下来最后一个元素79，它的初始散列地址等于一一，这个位置已经被占用，发生了冲突，那么就会依次的探测。后面这些个地址，直到探测到九这个地址的时候，这个位置为空，所以79插入到九这个地方。好，那这样我们就把这些啊，关键字都依次插入到了这个散列表当中，

并且构造出了整个散列表的样子，接下来第二个小问。它让我们计算出在等概率的情况下，查找成功和查找失败的平均，查找长度asr。好，首先来看，在等概率情况下查找成功的这个平均asr应该如何计算好？那么既然要查找成功，那我们要查找的关键字肯定是已经存在于这个散列表当中的某。某一个关键字对吧？那么在等概率的情况下，我们认为我们查找其中的任何一个关键字的可能性都是相等的。那总共有十二个关键字，总共有十二个关键字，

因此任何一个关键字被查找的概率，被查找的概率都是十二分之一。好，那么假如我们要查找的是14这个关键字，我们是怎么做的？首先我们会以此次要查找的这个关键字。去计算它的呃散列地址对吧？计算它的初始散列地址，也就是对13取余嘛，那么14的初始散列地址等于一，因此我们会在一这个位置去啊，去对比这个元素。它的值是不是等于我们的目标关键字14，如果等于那么OK，我的查找就已经成功了。

所以对于14这个元素来说，我们查找它就只需要对比一个关键字，对吧？好，再看第二个元素。比如说假如我们要找的是一这个元素，要找的是一这个元素，那么我们首先会用这个元素去计算，去计算它的呃初始散列地址等于多少？那么，一对于13取于等于一，所以首先我们会去一这个位置去查找我们的目标关键字。那由于这个初始散列地址和我们要查找的这个目标，关键字不一样。所以我们会探测后一个地址对吧？

因此查找一这个元素，总共需要付出的查找长度，或者说关键字的对比次数就是对比了一次两次。直到对比第二个关键字的时候，我才可以确认查找成功好，那后面这些呃元素，它的查找长度的分析都是类似的。事实上，我们之前插入这些元素的时候，计算得到的这些探测序列刚好就是我们查找这个元素所需要经过的。这个对比次数对吧？比如说79这个元素，如果我们要查这个元素的话，那么首先它的初始散列地址等于一对吧？一这个位置。

没有对上那么往后探测后一个元素，没有对上继续往后直到探测到九这个位置的时候。哎，它和79这个关键字能够匹配上，那此时才可以确定它查找成功，所以查找79这个元素总计需要付出的，查找长度代价应该是多少呢？应该是一二三四五六七八九应该是九的查找长度对吧好，所以综上所述我们可以得到这样的一个计算公式。等概率情况下的平均查找长度，整个散列表当中总共包含十二个元素，因此每个元素被查找的概率是十二分之一。那19这个元素，它的查找长度是14，这个元素，

它的查找长度是一。23这个元素，它的查找长度是一。一这个元素，它的查找长度是二。好，后面的以此类推，那么把所有这些元素的查找长度都相加，然后再乘以十二分之一。就得到了查找成功的asl=2点五。好，那么这是查找成功的asl，接下来我们再看查找失败的呃asl怎么计算？好，那么计算方法是这样的，

先观察这个散列函数，这个散列函数有可能得到的初始散列地址。总共有13种，对吧？就是说你查找任何一个关键字，它的初始散列地址肯定只会落在零到12这个区间内。好，那么我们假设在查找失败的这种前提下，呃，这个关键字的初始散列地址。落在这13个位置，处的概率都是完全相等的。比如说假如某个关键字，某个关键字，它的初始散列地址落在了零这个位置。

那么，由于零这个位置本来就是空的，对吧？所以要确认这个关键字，查找失败所需要对比的关键字数量是不是只有一个？就对比零这个位置就OK了，对吧？好，所以初始散列地址为零的这种关键字，从开始查找到确认查找失败。所需要付出的，查找长度就是一。好，再看，如果我们的这个关键字，

这个关键字，它的初始散列地址落在了一这个位置。那么，假如这个关键字最终要确定，它查找失败，我们是不是得先对比一这个位置？呃，一这个位置的这个关键字和我们查找的这个目标不匹配，那么往后对比二这个位置。二这个位置也不匹配，还需要往后对比三三不匹配，再到四五六七。一直对比到12还是不匹配，直到对比到13这个位置的时候，发现这个位置已经空了。

此时，我们才可以确定啊，初始地址为一的这个关键字查找失败，那么整个过程当中，呃，它的查找长度就应该是13，对吧？就应该是13。好，那后面的情况是不是完全类似的？这种分析思路啊，假如初始散列地址是二这个位置，那么。这个位置不匹配，得往后依次的去呃探测每一个位置都不匹配，

那么只有探测到13这个位置的时候。也就是说，只有探测到第一个空位置的时候，才可以确认查找失败。所以，对于一个初始地址为二的这种关键字来说。确认查找失败所需要付出的，查找长度就应该是12嘛，从这个位置到这个位置。好，所以后面这我们又加了12好，那再往后的是同样的道理，这就不再赘述。好，那刚才说了，

由于初始散列地址总共有十三种情况，那每一种情况出现的概率都是十三分之一。所以相加之后，还要除以13，最后得到结果就是七。那这儿有必要提醒大家的是，当我们在计算查找失败的asl的时候，很多同学容易犯这样的经典错误。很多同学会认为，整个散列表的长度是16对吧？所以他会认为一个关键字，它的呃初始散列地址。落在零到十五呃，任何一个位置的概率都是相等的，也就是它会乘以一个十六分之一。

但事实上，这是错误的，对吧？我们散列函数计算的这个初始散列地址，有时候和散列表的长度是并不相同的。计算查找失败的asl的时候，要以这个散列函数可以覆盖的这个范围为准，而不是以散列表的总长度为准。那这是考试当中很容易出错的点，那上面这个式子就是正确的计算方法，下面这个式子就是错误的示范。好，那么这是查找成功和查找失败的平均查找长度。好，那么这是第二个小问，

接下来第三个小问，我们让问题变得更复杂一点，在之前得到的这个散列表当中。我们删除元素20，然后再计算等概率情况下查找成功和查找失败的平均查找长度asl。好，那么这种情况为什么要单独拿出来强调呢？因为很多同学会犯这样的错误，就说20这个元素，他既然已经被删了。那么就意味着数组下标为七的这个地方，它就空了，对不对？比如说假如我们误认为七这个位置已经为空。那么，

当我们查找一个关键字，这个关键字的初始散类地址，如果说直接就是指向了七。那假如七这个位置本身就为空的话，是不是只需要对比一次对比一次关键字就可以确认查找失败了？对吗？好，这是一个标准的错误哈，这是一个标准的错误，记住我们在之前的学习中强调过。采用开放定值法解决冲突的散列表当中，如果我们删除一个元素，那么我们进行的是逻辑删除，而不是物理删除。那逻辑删除和这个位置为空完全是两个概念，

对吧？当我们查找到一个逻辑删除的这种呃位置的时候。我们不能认为查找失败，因为这个元素被删除了，那么后面还有可能会存有同义词，对不对？还需要往后探测。好，所以这是删除元素之后很容易踩的一个坑。一定要注意，我们删除元素的时候是逻辑删除，而不是物理删除。好，那么在这种情况下，查找成功的呃，

平均查找长度，我们计算逻辑是一样的。目前，在整个散列表当中依然存在的元素总共只有11个了，对吧？我们把查找其中的呃，每一个元素所需要付出的这种查找长度全部都呃加起来，然后除以11就可以得到查找成功的asl。那分析的方法和之前是雷同的呃，我们以79这个关键字为例，那这个关键字它的初始地址是一。所以79这个元素首先会在一这个位置去查找好匹配不上，于是会探测二这个位置。还是匹配不上好，继续往后探测。

探测到七这个位置的时候，这个位置虽然没有存任何元素，但是它是逻辑删除，对吧？并不是这种空单元格。那就意味着，探测到这个位置的时候，我还不能停，还得往后继续探测好，所以接下来还会探测八这个位置。没匹配上再探测九这个位置OK，匹配上了好，所以79这个元素，它查找成功所需要付出的查找长度就应该是九这么多。那刚才这个例子，

我们也感受到了逻辑删除的这种位置，对查找过程的一个影响，它和这种空位置是完全两个概念。好，那刚才探讨的是查找成功的asr那查找失败的asr分析的方法，也和之前是雷同的。同样的，根据散列函数可以知道，有13种初始散列地址在等概率情况下，我们认为任何一个初始散列地址出现的可能性。都是十三分之一概率都是相等的，那我们依次算出呃，每一个初始散列地址。从开始查找，一直到确认查找失败，

它的查找长度应该是多少？把这些值相加，然后除以13就可以得到查找失败情况下的平均查找长度。好，那分析的方法和之前是类似的，这儿就不再赘述，重点要注意删除一个元素的时候，我们进行的是逻辑删除，而不是直接置为空。好，那么大家可以把这个例题当中的一二三三个小问都自己动手做一遍，把这三个小问啊搞定，那么考研真题当中。分析asr的题目啊，大部分应该都难不住大家了，

那接下来我们会引入两个概念，分别叫庄田因子以及啊聚集现象。接下来要讲的这两个东西会影响到散列表的这个查找效率，那先来看什么叫装填因子很简单。就是用散列表中的记录总数n除以散列表的总长度m，它反映了散列表满的程度。比如说刚才得到的这个散列表，整个散列表的长度是16，而整个散列表当中总共有12个记录。十二个元素，所以这个散列表的装填因子就是十六分之十二，也就是等于零点七五。那这就是这个散列表的装填因子，当装填因子阿尔法等于一的时候，是不是说明整个散列表都已经填满了？

对不对？那显然，当一个散列表它被填的越满的时候，或者说当一个散列表的装填因子越大的时候。我们就越容易发生冲突，对不对？从而就会导致插入一个新元素，或者查找一个元素的时候，这个呃效率会降低，也会使得asl。增大这个结论不难理解吧？就以之前得到的这个散列表为例。此时，这个散列表它的装填因子已经比较大了，那假设我们要插入一个新元素，

这个新元素的值是x。那么，这个新元素，它的散列地址是不是只有可能在零到12？这个区间内总共有13种可能，那我们观察现在这个散列表，如果这个新的元素。它的初始散列地址。刚好是插入到零这个位置，那OK我没有发生冲突，直接就插入成功。而只要这个初始散列地址是落在一到12当中的任何一个位置，那么这个插入操作的过程一定会发生冲突，对吧？比如说初始散列地址为一的话，

那么它会呃发生好几次冲突，直到探测到13这个位置，才可以把这个新元素插入到散列表当中。所以可以看到装填因子很大的时候，我们进行插入操作，这个效率是很低的，因为大部分的位置都已经被使用了。所以在插入的过程中，就极有可能发生冲突。那查找操作的效率也会因为装填因子过高而降低。这点我们根据前面这个例题的第二小问，已经有体会了。你看整个散列表的元素，很多的时候。查找其中的某些元素，

有可能会发生很多很多次冲突，才可以找到。所以，查找效率降低，从而导致啊，这个asr整体上升，对吧？好，那这是庄田因子这个概念，庄田因子会影响散列表的呃查找效率。接下来再引入一个概念，叫做聚集现象，也叫堆积现象。用个例子辅助大家理解。假设我们有这样的一个散列表，

长度是27，散列函数是用关键字对23进行取余。并且我们采用线性探测法来处理冲突，那假设我们要插入六二九五二七五呃九八幺二幺以及幺四四，假如我们接下来要依次插入这些个关键字。那么，这些关键字啊，对23取于都等于六，也就是说，这些关键字，它们的初始散列地址都映射到了同一个地方。好，那么在这种情况下，六是第一个被插入的元素。那接下来29这个元素，

由于这个初始散类地址已经被占用了，对吧？所以只能往后探测一个位置，因此29要放到七这个地方。那52这个元素，它的初始散列地址也等于六六，这个位置占用了，往后探测再往后探测，它得放到八这个位置。好，后面的类似75就得探测到九这个位置，98得到十这个位置，幺二幺得探测到11这个位置，幺四四得探测到12这个位置。好，

那么这是用线性探测法处理冲突的时候，这几个元素啊，最终的一个存储地址。好，此时我们看到有一堆元素扎堆聚集在一起。或者说堆积在一起，那这种聚集堆积的现象会导致什么情况呢？我们思考13这个地址，13这个地址，这个位置，此时为空可以插入一个新元素。但是如果这个新元素，它的初始散列地址是在六七八九十十一十二十三在其中的任何一个位置处。这个元素，它最终都会放到13这个位置，

那这就是所谓的聚集现象，我们看到在处理冲突的过程当中。如果几个初始散列地址不同的元素。争夺同一个后继散列地址，那么这种现象就称为聚集或者堆积。就像刚刚我们说的。如果我们插入一个新元素的话，那么。不管这个新元素，它的初始散列地址是六七八九十十一十二还是13，就是这么多个初始散列地址的新元素。都需要共同的去争抢13这个空位，那此时就发生了聚集或者叫堆积，那显然这种聚集现象它不是什么好的现象，对吧？

它也会影响到这个散列表的呃工作效率。会导致散列表的查找效率下降。as 2提升。这个不难理解吧，你看一堆元素，它扎堆聚集在一起。那假如我要查找一个呃，关键字，这个关键字，最后它会查找失败，那刚开始它的初始散列地址等于六。那从六这个位置开始查找，直到确认查找失败，我们总共是不是需要对比一次两次三次四次五次六次七次八次？得对比八次关键字才可以确认查找失败。

这就是一堆元素聚集在一起的呃，一个缺点嘛，假如其中的某一个位置，我们把它挖掉，把这些元素挖掉，就是让它保持为空。那么，我们从六这个位置开始查找，直到确认查找失败，是不是就不需要八次关键字对比了？只需要从初始散列地址开始往后找到第一个空的，这种单元格我就可以确认它查找失败。好，所以如果一堆元素都聚集在一起，中间都没有空位，

那这种情况对于查找效率是有呃比较大的影响的。那不幸的是，线性探测法，它的特点就会导致很容易出现这种聚集堆积的现象。因为在线性探测法当中，如果一个位置，它呃发生了冲突的话，那么我们总是往后探测相邻的后一个单元嘛。就很容易导致一堆元素聚集在一起，对吧？好，那么既然这种聚集堆积的现象会对啊，散列表的查找效率产生负面的影响。那我们怎么去缓解？怎么去减轻这种聚集现象呢？

还记不记得啊？我们在介绍开放定值法的时候，我们介绍了几种常用的探测序列。那线性探测法只是其中的一种，对吧？我们还介绍平方探测法，双散列法，伪随机序列法等等。接下来，我们尝试着把线性探测法改成平方探测法，看一下能不能缓解聚集的现象。好，还是同样的例子。散列表的长度以及呃散列函数保持不变，我们把线性探测法改成呃平方探测法。

用这种方法去处理冲突，同样是那几个元素，那60第一个元素，它可以顺利的插入到呃初始的散列地址。接下来29这个元素和六这个位置发生了冲突。那初始散列地址这个地方啊，发生冲突的话，接下来会探测到从初始散列地址往右偏移一个单位，这个位置也就是七这个地方。好，那这个地方为空，所以29插入到七。接下来52。它的初始散列地址等于六发生冲突。接下来，

探测从初始散类地址往右一格这个位置。还是发生冲突，接下来探测从初始散列地址往左一格，也就是减一这个位置。那这个位置为空，所以52插入到五这个地方。接下来75这个元素。出示散列地址，这个地方发生了冲突。接下来往右探测一格这个位置也发生了冲突，接下来往左探测一格还是发生冲突。那再往后就是探测初始散列地址，往右偏移四格那个位置。好，这是初始散列地址，

往右偏移，一二三四也就是十这个地方。此时为空，所以75。插入到十这个地方。好，接下来98这个元素，那显然它刚开始探测的这几个位置都已经被占满了，所以直到探测到初始散列地址往左偏移四个单位。也就是一二三四，也就是二这个地方。才可以找到插入的位置。那再往后幺二幺这个元素，它探测前面这几个地址都会冲突，对吧？

直到探测到呃初始散类地址往右偏移九个单位。也就会插入到实物这个地方好，最后幺四四这个元素，前面这些个地址都已经被占用了。那直到探测到负三的二次方，也就是从初始散列地址往左偏移。九个单位的那个位置好，我们来数一下，这是初始散列地址对吧？往左偏移一二三。四五六。往这边。八九往左偏移九个单位24这个位置，此时为空把它插入好，那到此为止，

我们用平方探测法，把相同的几个关键字插入到了散列表当中。接下来，我们观察它的聚集现象，那肉眼可见这些元素并没有像线性探测法那样。全部聚集在一起，对吧？所以采用平方探测法去处理冲突，肉眼可见的减少了聚集现象。还记得聚集现象，它的呃定义是什么吗？就是说多个初始散列地址不同的元素。争抢同一个呃位置，这种现象就是聚集现象，对吧？

那么在这个例子中，比如说我们看八这个位置。看起来八这个位置周边已经被呃很多元素包围了，但事实上这个位置此时只会被初始散列地址为七八的两类元素。去争夺，如果插入一个新元素，它的初始散列地址等于七这个位置发生冲突的话，那接下来它会往右探测一格，对吧？也就会探测到八这个位置。所以初始散列地址为七的元素会争抢八这个位置，那除此之外，大家可以去分析一下其他任何一个初始散列地址的元素。都不会来争抢八这个位置。所以从肉眼上看，

元素的聚集现象大幅度的减轻了，以及从逻辑理性上去分析，这种聚集的现象也确实减轻了。所以我们得到的结论就是，如果采用线性探测法去处理冲突，那么更容易导致聚集或者叫堆积现象。而采用其他的方法处理冲突，比如说用平方探测法，或者采用设计合理的双散列法，以及设计合理的尾随机序列法。采用别的这些方法，在一定程度上可以缓解聚集现象的发生，而聚集现象减轻就意味着。散列表的查找效率可以提升。好，

那么在这个视频中，我们首先用一个例题带大家学习了如何计算散列表的asl查找成功和查找失败的计算方法呃，略有区别。对于查找成功的情况，我们首先要确定的是啊，散列表当中到底存在多少个元素？那在这个总结式子当中，我把它记为n。散列表中总共有n个元素，其中I这个元素啊，查找成功所需的这种关键字比较次数是ci。那么，把所有的这些已存在的元素，它的查找长度相加，然后再啊乘以n分之一这个相等的概率就可以得到查找成功的asl。

好，另外在计算查找失败的asl的时候，我们需要注意的是这儿的r这儿的r指的是散列表的取值个数。也就是初始散列地址有可能出现多少个值？散列函数的这个取值个数和散列表的长度有可能是不相同的，这一点是做题的时候容易错的地方，需要特别注意。那这儿的ci指的是散列函数，取值为I的时候查找失败所需要的比较次数。那计算散列表的asr这是考研的重点，有一半以上的这种散列表相关的考题都是要求计算asr的，有可能考小题，也有可能考大题。那大家可以结合王道的课后习题，趁热打铁啊进行训练，

那散列表的asr反映了它的啊查找的效率。我们在这个视频的后半段引入了装填因子和聚集现象，这样的两个概念装填因子过高也会影响这个啊，查找效率。会使得asr增大。另外，对于线性探测法处理冲突的这种散列表来说，比较容易出现聚集现象。而一旦出现聚集现象，就也会影响这个呃查找效率，导致asl提升。那我们采用其他的方法，比如说呃平方探测法或者尾随机序列法，用其他的方法去处理冲突，就可以把元素。

稍微的打散一些，就别让它们聚集的太厉害，那减少了这种元素聚集的现象之后。就可以使得散列表的查找效率得到提升。好的，那么以上就是这个视频的所有内容，大家可以趁热打铁，把王道书的课后习题，特别是历年真题。啊，好好的感受一下。
