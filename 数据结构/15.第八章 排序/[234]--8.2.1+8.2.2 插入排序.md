好的，那这个小节中我们会学习第一个排序算法，插入排序这个算法的思想很简单，就是我们会不断的把一个一个的带排序的记录。按照关键字的大小插入到前边已经排好序的子序列当中，直到全部记录插入完成为止。好，我们结合动画来理解，刚开始我们会从呃第二个元素，从这个元素开始入手，我们会认为。当前处理的这个元素，之前之前的这些部分是已经排好序的好，现在我们需要把当前的这个元素38和之前已经排好序的元素。依次进行对比，

比当前处理的元素38更大的啊，那些元素我们都需要将它依次后移，那目前只有49这个元素49大于38，所以把49后移。然后把38插入到49前面好，接下来要处理的是65这个元素，那65要比49更大。所以如果我们是按递增，就是增序排列的话，那么我们并不需要把65放到49或者38之前，因此我们把65。放回来，那接下来处理的是97，它比前边的这个呃65还要更大，所以97应该是放到它原有的这个位置。

好，接下来处理的是76这个元素，在76之前的这些元素是已经排好序的，那我们依次往前检查，97要大于76，所以76应该插入到97的前面。因此，我们需要把97往右移，一位好再往后要检查的是65，那由于76大于65，所以它应该插入到65的后面。因此，我们会把76插入到这个位置好，接下来要处理的是13这个元素，那依次检查它前边的这些元素，

97比13更大。右移76比13更大，右移65比13大，右移49更大也要右移。最后是38，也比13大还是u1好像再往前已经没有别的元素了，所以13应该是放入到零这个位置，也就是这样。好，接下来处理的是27这个元素，那27之前的这些序列此时已经排好序，那处理的方式也是一样的，我们需要把27前边。这些已经排好序的，并且关键字的值大于27的这些元素全部右移给27，

挪出一个位置来。也就是这个样子，所以27应该插入到一这个位置。好，最后要处理的是49这个元素，注意我们这的49画了一个下划线，是为了和三这个位置的49做一个区分，那么处理的方式和之前一样，我们要把之前的。已经排好序的这些元素当中，关键字的值大于49的全部右移。一位。然后把49插入到四这个位置，也就是这样啊，注意刚才我们只是把比它更大的这些元素全部右移，

那和它相等的这个元素。是没有优异的，那这么做可以保证算法的稳定性好，所以插入排序的思想是很简单的，那代码实现也不难。我们假设这儿我们进行排序的就是一个简单的int型的数组，其中总共有n个数据元素。我们会定义一个变量。I指向当前需要插入到前边那些序列的一个元素好，接下来是这个if语句。只有当前处理的这个元素，比它前边的这个元素更小的时候，我们才需要移动它前边的这个元素。所以我们会把当前I所指的元素，这个值用一个中间变量temp，

把它保存下来。这是为了防止移动元素的时候，把原有的这个数据给覆盖掉好，接下来就到了第二层的for循环，我们会从I的左边。这个位置开始依次往左检查，它前边的这些元素是否要比38当前元素的值要更大？所有比它更大的元素，我们都需要把它给右移好，所以这个for循环导致的结果就是所有的大于38的元素都会右移。一位。那每右移一位元素，都会j减减，当j小于零的时候啊，这个循环就会停止，

所以最后我们还需要把。之前保存下来的这个temp值给它复制到j所指的右边这个位置，也就是把38复制到零。好，那这是第一轮的for循环，接下来是不是可以让I的值加加嗯，开始处理65这个元素？那后续的执行就不再分析这个原理都很简单好，那现在我们来看一下我们课本当中给出的另外一种。这个直接插入排序的实现方式，他用了带哨兵的这种方法来实现，还记得哨兵吗？我们在顺序查找那个算法当中提到过什么是哨兵。那这个地方的哨兵原理也是类似的，就是说我们实际的这些数据元素，

我们是从一这个位置开始存储的，而零这个位置我们会空出来。把它作为哨兵，那这种实现方式其实本质上的原理和之前的那种实现方式没有任何的区别，和之前一样，只有当前指向的元素。比它前边的元素要更小的时候，我们才需要移动它前边的这些元素，那和之前那种实现方式不一样的是，我们会把当前的呃处理的这个元素把它复制到。a0这个位置，而之前我们的实现方式是专门的，定义了一个temp变量，对吧？好，

接下来到第二层的for循环和之前一样，我们会检查。当前所指元素左边的所有的这些元素，如果说比当前所指元素更大的那些，我们都会把它们往右移一位。所以刚开始处理的是49，49要比38更大，所以49往右移好，接下来会j减减让j=0。但是由于现在j所指的元素刚好是指到了a0，那么它和a0是相等的，所以这一层的for循环就可以顺利的跳出。那跳出for循环之后，我们还需要把a0所指的这个元素把它复制到j+1这个位置，也就是变成这个样子。

好，所以其实我们加了哨兵的好处在于我们不需要再像之前那样每轮循环都还要判断一遍。j是否大于等于零？看一下之前的这种实现方式，这个for循环里边，我们每一次j减减之后，我们还需要判断一次j是否大于等于零。满足这个条件的时候，我们再来判断j所指的元素和我们当前处理的元素的大小关系，就需要进行两次判断。而加了哨兵之后，我们就不需要再进行这样的判断，所以可以让我们这个循环的执行效率更高一点点，但是这个提升其实很小很小。那我个人是不太喜欢这种带哨兵的实现方式，

因为我觉得之前的那种实现方式呃，逻辑上好像更清晰一些。所以大家在实际考试当中，如果让你写插入排序的算法，那你可以根据自己的喜好，自己的习惯来，不一定按照我们课本上给出的这种方式来写。好，那接下来我们来看一下这个插入排序的算法效率如何？空间复杂度不用说了，是o1这样的一个数量级，因为其实我们也就是需要定义ij这样两个啊，用于循环的变量。另外，带哨兵的这种实现方式，

我们会用a0这个位置把它空出来作为一个算法执行的辅助空间。而之前的那种实现方式，其实我们也就是定义了一个temp变量，所有的这些辅助变量所需要的空间都是常数级的和问题规模n没有关系。所以这个算法的空间复杂度就是o1这个数量级，接下我们来看时间复杂度，在进行插入排序的时候，我们都是从第二个元素开始，依次往后处理。那每一个元素需要进行这样的一轮for循环。所以总体来看，我们总共需要处理n- 1个元素，也就是需要进行n- 1趟的处理。每一趟的处理都需要进行关键字的对比啊，也就是这个if判断也有可能需要进行元素的移动，

那我们的时间复杂度主要的开销其实就是来自于这两个部分。那先来看一下最好的情况，如果说我们原始的这个表当中各个元素的排列本身就是已经有序的，那这种情况下我们每一趟的处理都。只需要对比一次关键字，而不需要移动元素好，所以总共n- 1趟的处理，那么我们就只需要进行n- 1次的关键字对比。因此，总体来看，我们最好的时间复杂度应该是on这个数量级好，这是最好的情况。原本这些元素就是有序排放的。接下来看最坏的情况，如果说呃，

原本的这些元素，它们是逆序排放，在这种情况下，我们每一趟的处理都需要把当前元素和之前的所有的元素都进行一次关键字的对比。并且把之前已经排好序的元素全部都往后移动一次，比如说当我们进行到最后一趟排序的时候。我们要处理的是十这个关键字。那我们需要把当前所处理的关键字移动到a0这个位置，接下来这个for循环当中，我们还需要把当前的这个元素和前边以及。已经排好序的所有的元素都进行一次关键字的对比，并且把这些元素都依次的后移一位。好，总之整体来看，对比关键字的次数，

还有移动的次数，我们把这些值进行一个加和肯定，都能够达到on的平方，这样的一个数量级。好，这是最坏的情况，原本这些元素都是逆序存放的，那这种直接插入排序算法的平均时间复杂度也会达到on的平方，这样的一个数量级。我们的课本当中是把最好时间复杂度和最坏时间复杂度加和，然后除以二用用这样的方式来确定了一个平均时间复杂度。那上一个小节，我们说过，当我们在关注一个排序算法的时间和空间复杂度的时候，我们也需要在关注这个算法的稳定性如何，

那直接插入排序显然是一个稳定的。啊，排序算法好，现在我们对刚才的这种实现方案进行一个优化，之前当我们在处理某一个元素的时候，我们都是用顺序查找的方式依次往前找。找到它应该插入的位置，但是由于当前处理的元素，它之前这些元素其实已经是有序的，并且在我们这个例子当中是用顺序存储的方式来存储的。所以不难想到，我们可以用折半查找的方式来更快的找到当前处理元素应该插入的位置。好来看一下怎么做，首先我们在a0这个位置把当前处理的元素给保存下来。防止它被覆盖好，

接下来我们会在当前这个元素前边的这个区域内啊，尝试着找到它应该插入的位置好，接下来我们会判断low+hi÷2。这个位置的元素和我们当前处理的元素大小关系如何？那在这个例子当中，50要小于55，所以55这个元素应该插入到50这个元素右边的区间内。因此，我们接下来会在它右边的区间内进行查找，也就是让low=MID+1，而接下来检查的元素MID应该是low+high÷2，也就是70这个元素，那70比55更大。所以55如果要插入的话，应该是插入到70的左边，

因此接下来我们会在70左边的区间内进行查找。也就是让hi=me的减一好，接下来me的等于漏加hi÷2要检查的是60这个元素，60要比55更大。所以55要插入的位置应该是在60的左边，因此接下来我们会让hi等于幂的减一好到了这一步，我们的low已经大于hi了。那这个时候我们就应该停止折半查找，此时low所指的位置，右边的这些元素都是要比当前处理的元素55更大的。所以接下来我们应该把这个区间内的元素全部都右移。一位给55腾出一个位置。也就是这样子好，最后我们再把之前保存下来的这个值给它复制到low所指的位置，那这样的话我们就完成了55这个元素的插入。

好，接下来处理下一个元素，要处理的是60，我们要在low和hi之间寻找60，需要插入的位置，那第一个被检查的元素MID应该是50。50要比60更小，所以60插入的位置应该是在50的右边区间内，所以我们会让low=me的加一。好，接下来MID=low+hi÷2，所以检查的是60这个元素，好，现在问题发生了，我们当前处理的元素60和这儿已经有的元素60，

它的值是相等的。那按照我们之前设计的折半查找规则来看，当我们找到一个和目标关键字相同的关键字的时候，我们就会停止这个折半查找，但是呢，在这个地方，我们为了保证。插入排序的稳定性，当我们发现一个和当前处理的元素值相同的啊，这种元素的时候。我们还应该往这个元素的右半区间内继续查找，来确定当前元素的插入位置。所以到这一步，我们并不会让这个折半查找停止，我们还会继续在MID所指的这个位置右边的区间内来尝试着查找60这个元素应该插入的位置。

所以接下来我们会继续让low=me的加一。好，继续MID=low+high÷2，那当前MID所指的元素70要比60要更大，所以我们会让high=mid- 1。好到这一步，low大于high，那我们就可以停止折半查找和之前的处理一样，我们需要把low到i- 1。这个区间内的元素全部往右移一格，给当前的这个元素腾出位置，而所以执行的结果就是这样的。那这样我们就完成了60这个元素的插入好，接下来要处理的是90这个元素，同样的我们会在前边这个区间内尝试着找到当前元素应该插入的位置。

那具体这个折半查找的过程，我们就不再一步一步的分析，按照我们之前提出的这种处理逻辑，最终会导致的结果是low这个指针。会指向hi的右边这个折板查找，同样是会停在low大于hi的位置，不过由于此时low大于i- 1，所以在这种情况下，我们并不需要移动任何一个元素。好，这是对90这个元素的处理，那最后一个要处理的元素是十和之前的处理逻辑一样。我们会尝试着在它前边这个区间内找到它应该插入的位置，那经过一系列的处理之后，最终这个折板查找会停在呃hi指向0 low指向一。

这样的一个状态，同样的也是low大于high，在这个时候停止遮光，查找那处理逻辑和之前一样，我们需要把low和i- 1之内的这些元素全部都右移。也就是这个样子，那最后再把a0这保存下来的副本给它复制到low所指的位置。好，总之，当我们在确定当前元素应该插入的位置的时候，由于前边的序列都已经是有序的状态，所以我们可以用折半查找的方式来更快的确定应该插入的位置。最终折半查找都会因为low大于high而停止，我们要做的就是把low和二减一之内的元素全部优于一位。那课本里面的代码的写法是呃hi加一到二减一，

这个区间内的元素全部u一。因为最终hi肯定是在low左边相邻的这个位置，所以hi+1和low其实是一样的意思，另外我们为了保证呃这个算法的稳定性。当我们找到和当前处理的元素，关键字的值相同的元素的时候，我们还应该继续在MID所指的位置右边来寻找插入的位置。就像刚才60这个例子一样，那捋清楚之前的过程，这个代码的原理就很简单了，这是我们课本当中给出的代码。这个while循环就是实现了，我们刚才说的折半查找的过程，而需要注意这个else条件。这个else条件里边已经包含了我们刚才说到的呃，

就是保证算法稳定性的一个逻辑。如果幂的所指的元素和当前我们要插入的那个元素值相同的话，那我们应该继续在幂的所指位置右边来寻找。插入的位置，所以我们会让low=me的加一，那这个地方是和我们之前学习的折半查找稍微有一点点区别，大家需要注意的地方。好，另外这个for循环实现的就是把所有的这些元素都统一的，依次的右移一位，那这种折半插入排序比我们之前提出的直接插入排序相比。其实就是呃，我们比较关键字的次数会减少一些，但是最终我们移动元素的次数呃，没有发生什么改变。

因此，整体来看，折半插入排序，它的时间复杂度依然是on的平方，这样的一个数量级并没有什么质的飞跃。好，最后我们再来看一下，插入排序算法，这种思想其实也可以用于对于链表的排序，我们之前的讨论一直都是基于顺序表，对吧？也正是因为基于顺序表，我们才可能实现这种折半查找，那如果这些数据元素采用链表存储的话，那显然折半插入排序就是实现不了的，

那对于链表的插入排序啊，具体的代码。怎么实现这个留给大家自己去练习，那和顺序表的插入排序不太一样的是呃，如果采用链表存储的话，那当我们要移动一个元素的时候，其实只需要修改几个指针就可以了。并不需要像顺序表那样把很多元素依次都右移，所以这种情况下，移动元素的次数会变少。不过呢，由于我们只能顺序的来依次比较各个关键字，用这样的方式来确定插入的位置。所以关键字的比较次数其实依然会保持on的平方，这样的一个数量级，

因此整体来看时间复杂度还是on的平方。总之，只要我们采用的是插入排序这样的一种实现方式，那最终我们的时间复杂度肯定都会达到on的平方，这样的一个数量级不过需要注意的是。如果说我们给出的原始的那个元素序列，它本来就是有序的，或者说基本上接近有序，那在这种情况下，其实插入排序也可以得到一个很不错的执行效率，执行效率。那插入排序的思想和实现，其实都是比较简单的啊，唯一大家可能需要注意的就是，如果我们采用折半查找的方式来确定插入位置的话，

那这个折半查找的过程需要特别的注意一下。注意这样的两个问题，如果当前幂的所指的元素和我们当前处理的那个元素值相等的话，那我们应该怎么处理？才能保证稳定性，这是第一点，要注意的，第二点应该注意的就是我们最终折半查找算法，停止之后我们应该在什么位置插入当前处理的这个元素。把这两个小细节注意到了，那折半插入排序的实现也会变得很简单，好的，那以上就是这个小节的全部内容。
