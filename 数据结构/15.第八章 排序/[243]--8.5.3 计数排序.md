各位同学大家好，在这个视频中，我们要探讨技术排序这个算法，英文叫counting sort。那技术排序在四零八考研大纲当中，其实并没有要求我们掌握，但是在过去几年的真题当中啊，有那么一些真题，我们需要使用到技术排序的这种思想。和技巧，所以技术排序这个考点虽然在啊大纲当中，大家找不到，但是我们还是会进行补充和学习。以免接下来出题老师来骗来偷袭，那首先我们会探讨技术排序的算法思想。

紧接着我们会啊，基于这种算法思想去实现这个算法，并且给出C语言代码。接下来，我们会分析算法的性能，包括时间复杂度，空间复杂度。当然，还有算法的稳定性。那最后我会结合两道历年真题啊，展示技术排序在考试当中的应用好，那首先我们用一个例子带大家感受技术排序的算法思想。好故事是这样的，假设在某小学某班级啊，英语老师给这个班的学生们啊，

做了一次英语测试。好，那么现在的问题是这样的，在这个班里有一个同学叫咸鱼，他这次英语考试考了16分。请问如果让你判断咸鱼，它在这个班排名第几，那么你会用什么样的方法呢？那直觉告诉我们，想要知道16分排在第几？那我们只需要关注在所有的这些学生当中，小于等于16分的啊，这个人总共有多少个对吧好，那么从上到下快速的扫一眼。小于等于16分的成绩，

只有卧龙凤雏，那算上咸鱼，它自身所以小于等于16分的元素总共有三个，对吧？因此，咸鱼的这个班级排名应该排在第三，当然这个是倒数第三啊。好，讲到这里，你已经体会到技术排序算法的核心思想了。刚才我们想要确定16这个元素应该排在第几个位置。我们的做法是快速的扫一遍。统计小于等于16的元素总共有几个？那总共有一二三三个元素的值小于等于16。所以16这个元素肯定就排在第三个位置好，

那类似的道理，如果我们想要知道呃，镇南这个同学。他83分排在第几位？那我们可以快速的扫一遍。统计小于等于83的元素总共有几个？那阿呆的分数是小于等于郑楠，他自己也是小于等于83。呃，再往后是卧龙咸鱼啊，凤雏好总共有一二三。四五总共有五个元素，小于等于83，因此正南它应该排在第五位，对吧？

好，那么我们观察呃，右边这个排序的总体的结果可以看到正南排在第五位。好，那这就是技术排序的核心思想，那接下来我们用一个更严肃的例子来探讨技术排序的实现。先给出一个待排序的数组，我们观察一下这个数组当中的呃各个元素的值，其中出现的最小值是零。出现的最大值是四好，那由于整个待排序数组当中所有的元素值只有可能落在零到四这个区间。那接下来，为了实现技术排序，我们设置一个辅助数组c，这个辅助数组它的总长只有五。

所以它的下标就落在了零到四这个区间。那由于待排序数组当中只有可能出现零到四这个范围内的元素，所以接下来我们可以遍历这个待排序数组。统计各个元素值出现的次数。那具体来说就是这么做，先遍历啊a数组的第一个元素，它的值为二好，那么出现了值为二的元素。那我们就可以对辅助数组的二号下标这个位置进行加一的操作，表示值为二的元素呃，目前总共出现了一个。好，继续往后处理下一个元素，下一个元素的值等于四好，所以我们对呃辅助数组c的。

四号下标对应的这个位置进行加一的操作说明，四这个值出现了一次。继续往后下一个元素是三。那么，对c3这个位置加一说明三这个元素出现了一次好继续往后零这个位置。下一个元素的值为零，所以对c0这个位置加一。说明值为零的元素出现了一次好，再往后下个元素的值为二，接下来要对啊c2这个位置。再加一说明二，这个元素又出现了一次，对吧？好，那么再次加一之后c2，

这个值就变成了二。说明值为二的元素总共出现了两次。好类似的道理，再往后三这个元素又出现了一次。那么c3就变成了二。再往后零，这个元素又出现了一次，那么c0这个位置再加一。最后，这个元素值为三，那么c3这个位置再加一。最终c3的值来到了三。好，我们再把c1这个值呃默认为零把它填上去，那遍历完这个待排序数组之后。

我们利用辅助数组统计出了零这个元素，出现了两次一这个元素，出现了零次二三四，分别出现了二三一次。好，所以经过第一步处理c这个数组就变成了这个样子。好，接下来我们要基于这个中间结果，再次对辅助数组c进行呃，下一步的处理，最终我们要追求的效果是什么呢？我们希望用ci去表示，小于等于I的元素总共有多少个？比如说c2这个位置，我们希望用这个位置的值去表示，

在原始的大排序序列当中。值小于等于二的元素总共有多少个？我们只要统计出这个信息，就可以知道。值为二的元素应该排在第几位对吧？这就跟之前啊，给咸鱼排名的那个例子是同样的道理嘛。好，怎么做这一步的处理呢？很简单，基于之前得到的结果，我们知道啊，值为零的元素总共有两个，对吧？好，

那么最终我们希望c0这个位置表示的是小于等于零的元素，总共有多少个？那显然c0这个位置肯定也等于二对吧？小于等于零的元素只有两个嘛？好，接下来c1这个位置，我们希望用它表示小于等于一的元素，总共有多少个好？现在我们已经知道小于等于零的元素，总共有两个。那再结合原先c1这个位置，我们知道值为一的元素有零个对吧？所以小于等于一的元素总计应该有二。加零。也就是二个。

好，继续往后。接下来c2这个位置，我们希望用它呃统计出小于等于二的元素总计有多少个？那现在我们已经知道了，小于等于一的元素，总计有两个，那之前我们知道值为二的元素，总共有两个，对吧？所以值小于等于二的元素总计应该有二+2=4个。好，后面三同样的道理要统计小于等于三的元素总共有多少个？那我们现在已经知道了小于等于二的元素总共有四个，我们再观察。

值为三的元素诶，有三个对吧好，所以小于等于三的元素的总数应该等于四。加三=7个。好，最后。我们要统计小于等于四的元素总共有多少个？那小于等于三的元素总共有七个值为四的元素，只有一个，所以小于等于四的元素总数应该是七+1=8个。好，可以看到我们利用之前得到的这个中间结果，再次进行处理，得到了这个统计数组。那经过第二步的处理之后，

辅助数组的值就变成了二二四七八。注意啊，经过第一步处理和第二步处理，虽然我们都是用辅助数组c去表示，但是它们的含义已经发生了变化。左边这个中间结果ci这个元素表示的是啊，在原始的这个待排序数组当中。元素值等于爱的总共有多少个？而经过第二步处理之后ci这个值表示的是。在原始数组当中，小于等于二的元素累计出现了多少个？没跟上的同学可以暂停消化一下，好接下来完成第一步和第二步的这个预处理之后，第三步我们就可以利用这个辅助数组。去完成排序。

好，接下来我们对排序的过程进行演示。a是待排序数组c是我们经过两步处理得到的辅助数组。那除此之外，我们还需要一个数组b，它的长度和a相同。b数组用于暂存a排序的结果好，另一方面，为了让大家观察到技术排序的稳定性。我会用不同的颜色去区分呃，相同的几个值。好，接下来我们对数组a中的元素进行排序，我们会从后往前依次处理各个元素。从后往前的这种处理，

可以保证技术排序是稳定的，那一会大家会感受到它的工作原理。好，那既然是从后往前处理，那么第一个被处理的就应该是a7这个位置的元素，当前元素的值等于三。观察c3这个位置c3=7说明在原始的这个数组当中值小于等于三的元素总计有七个。所以当前处理的这个元素三，它应该放在啊，最终这个数组的第七个位置。那由于数组下标是从零开始的，那它的第七个位置就应该对应数组下标六对吧好，所以我们对c3进行减一的操作。减一之后就确定了当前元素，它应该放在哪一个数组下标处，

应该放在b6这个位置。好，所以蓝色的三放在b6。好，现在我们处理完了，第一个元素，接下来第二个元素处理的逻辑是一样的，第二个元素的值等于零。那观察c0。首先，我们对c0的值减一。这样就确定了当前元素，它存储的这个呃数组下标的位置应该存在b1。好，所以我们把黄色这个零放在b1的位置。

再往后，接下来这个元素我们观察c3这个位置，它的值等于六，那说明当前扫描到的这个三。应该放在目标数组的第六个位置，所以我们同样的先对c3-1，然后。再把这个呃当前处理的元素放在b5，放到b5这个地方。好后一个元素，它的值等于二观察C2C2=4，说明当前这个元素应该存储在b这个数组的第四个位置。那第四个位置对应的应该是b3。好，所以我们先对c2进行减一，

减一之后等于三接下来减一之后的这个值就指向了当前处理的这个元素。它应该存储在哪一个呃数组下标处好，所以灰色的这个二把它放在b3这个位置。好，接下来的处理是一样的，我们加速后一个元素的值等于零观察c0，先对c0进行减一的动作。减一之后的这个值，指向了当前元素的最终存放位置。所以，当前元素应该存储到b0。好，再往后下个元素的值等于三观察c3，先对c3进行减一的动作。减一之后的这个值，

就指向了当前元素应该存储的最终位置好，所以当前的这个元素应该存储在b4。那截至目前，我们已经把呃所有的值等于三的这几个元素都放到最终位置了，那可以看到，按照我们刚才设计的这个逻辑来进行处理。可以确保这个算法是稳定的，对吧？白色的，红色的，蓝色的，这是原先的相对顺序，那经过排序之后，白色的，红色的，

蓝色的，它们的相对顺序没有发生改变。说明技术排序是稳定的。好，继续往后下个元素的值等于四观察c4，先对c4进行简一的操作。减一之后就确定了当前元素最终存储的位置，因此当前元素应该存储在b7这个地方。最后一个元素值为二，那观察c2先对c2进行减一，减一之后就确定了当前元素，它存储的最终位置。应该把它放在b2这个地方。好，那么我们从后往前啊处理了每一个待排序元素，

每处理一个元素，就会把这个元素放到它最终的位置。好，那这样我们就完成了技术排序。好，所以经过第三步的处理，我们的啊，这几个数组就从最开始的这个样子变成了这个样子。b数组用于存储最终的排序结果。c数组在排序的过程当中啊，各个位置的值会减一减一，所以c数组在排序之后，它的啊值也会发生改变。好，我们回到刚开始这个地方，

首先我们需要确定待排序数组当中它的这些元素的取值范围应该呃落在。零到k这个范围之内。那像这个例子当中呃，它的元素取值范围就应该是零到五，那接下来第一步处理，我们需要呃定义一个长度为k的辅助数组c。辅助数组c的下标范围应该是零到k- 1，刚好就可以对应上啊，这个原始数组当中各个元素的值。接下来，我们会遍历原始数组，统计出各个值出现的次数，然后再进行第二步的处理。经过第二步处理之后。ci的值表示的就是小于等于I的元素，

总共有多少个？然后第三步，我们利用之前得到的这个辅助数组进行排序好，那么代码实现就是这个样子。我们传入数组a辅助数组b以及数组a，数组b的这个长度。然后给出数组a的这个取值范围应该是零到k。取值范围k决定了这个辅助数组c，它的长度应该定义为多少好，接下来后面这个部分。这个for循环呃，我们对呃，这个辅助数组c进行了初始化的赋值，把整个数组都赋为零好，再往后一步就到了刚才提到的step 1。

步骤一，我们会遍历待排序数组，也就是a这个数组，然后统计a这个数组当中。每个关键字出现的次数。那由于a这个数组当中每一个关键字的取值范围只有可能是零到k- 1，对吧？所以任何一个关键字的值。都可以呃，让它对应到c这个辅助数组。好，所以这一个for循环实现的就是step 1第一步操作。那接下来我们还需要对这个辅助数组进行下一步的处理。还记得我们怎么做的吗？经过上一步的处理，

我们知道c0=2说明，小于等于零的这个元素，它总共肯定就是两个，对吧？所以c0这个位置呃，我们是不需要去改变呃，上一步得到的这个值的。那对于c1的值，我们需要做的就是把当前这个c0的值和c1的值进行相加，对吧？那二+0。等于二好再往后一个是处理c2，要统计小于等于二的元素总共有多少个，我们只需要把目前已经知道的。小于等于一的元素的个数加上值为二的元素个数，

对吧？也就是说，用目前的c1加上目前的c2。那c 1+c二就是二+2=4嘛？好，那回到代码可以看到我们是从c1那个值开始处理c1的，值等于原先的c1。加上原先的c0。好c1处理完了之后I加加就会开始处理c2，那c2的值又等于原先的c2值，加上c1的值。也就刚才我们梳理的那个逻辑代码基础不好的同学，可以自己用手算的方式模拟一遍好，总之经过这一步的处理之后。ci保存的就是小于或等于I的元素，

总共有多少个保存的就是这样的一个信息？接下来就可以进入第三步，我们利用辅助数组c进行技术排序，那在之前的演示当中，我们强调呃，对各个待排序元素的处理是从后往前进行的。所以下面这一段for循环可以看到I的值是从n- 1开始。好，还记得我们是怎么处理AI这个元素的吗？我们会根据当前处理的这个元素的值。去查找。c这个辅助数组对应的位置。比如说当前处理的元素AI，它的值等于三，那么我们就应该去啊查找c。

AI这个位置对吧？我们需要对c数组的啊，这个值进行减一的操作。那我们可以这么写。减一减一之后，又把它复回给cai。可以这么写，对吧？当然，如果简单一点，你也可以写呃cai减减，这样也是OK的。逻辑上是等价的。好，那c这个数组的值减一之后。

减一之后的这个值，指向了当前处理的这个元素AI，它应该存储在。b数组的呃，哪个下标处对吧？所以进行这一步处理之后，下一步就应该是把AI这个元素放到数组b当中，那放到什么位置呢？应该放到数组b的c。ai，应该放到这个位置，对吧？理解背后的过程，其实这个代码就没那么复杂。好，

所以经过这样的呃两步处理，其实就处理完了呃AI这样的一个元素，那再往后是不是就可以让爱减减？爱剪剪，然后处理下一个元素了。好，那回到这段代码，现在再来观察for循环里边的这两句代码，你应该知道是什么意思了。跨考的同学可以自己在手绘梳理一遍。好，接下来我们对这段代码的复杂度进行分析。先探讨空间复杂度a这个数组是原始数据，那为了实现技术排序，我们定义了一个输出数组b。

它的长度等于n，所以b这个数组带来的空间复杂度就是on好，另外我们还需要定义数组c。c的长度取决于k这儿的k反映了啊，原始数组a它里边的元素的一个取值范围。好，所以数组c带来的啊，空间复杂度是OK好，所以综合来看数组b。数组c，它们都属于为了实现这个技术排序而定义的辅助数组，因此整个算法的空间复杂度就应该是on+k。好，再来看时间复杂度。第一个for循环对c这个数组的各个元素进行了初始化的赋值。

那由于c数组总共有k个元素，所以这段代码它的复杂度是OK，再往后需要遍历待排序数组。统计这个数组当中，各个值出现的次数，那由于a这个数组它的长度是n，所以第二个for循环它的复杂度就是on。接下来，第三个for循环需要对啊c这个辅助数组进行再次的处理。那结合c数组的长度，可以知道第三个负循环时间复杂度OK？好，最后一个for循环，我们会从后往前去处理啊，待排序数组a当中的各个元素。

对每一个元素的处理，只需要经过常数级的呃时间。那总共有n个待排序元素，所以最后这个for循环它的时间复杂度也只有on好，那么把所有的这些时间复杂度呃加起来之后。可以知道整个技术排序，它的时间复杂度就应该是o的两倍n。加k。那这个常数我们可以把它去掉，所以综合来看技术排序的时间复杂度也等于o的n+k。好，那我们不难得出这样的推论，假如k的数量级和n的数量级呃相同的话，那么技术排序的时间复杂度就应该等于on。k和n同等数量级，

那么前面这个o的n+k就等价于o的2n嘛？而当我们表示这种数量级的时候，我们可以把常数项去掉，那把常数项去掉之后，这个时间复杂度不就回到on了吗？那显然，在这种条件下，技术排序它的时间，效率，时间，效率要优于快速排序和堆排序这种呃n倍的log 2n。要优于这种算法。而假如呃k的数量级大于等于n倍log 2n的话，那么技术排序的是？时间复杂度就会大于等于o的n+n倍。

log 2n对吧？那一个低阶加上一个高阶，我们可以把低阶这个部分给去掉，这是我们在第一章学过的东西。加法原则吗？不记得同学可以回去复习一下好，所以如果k的数量级大于n倍log 2n，那么技术排序的这个时间复杂度也会大于。n倍log 2n那在这种情况下，技术排序的时间效率就不如啊，快排和堆排序。好，所以上面这个推论反映出了什么情况下比较适合用技术排序去解决问题。关键取决于k它的数量级等于多少，假如这些待排序元素，

它的取值范围。刚好就是呃零到n对吧？小于等于n如果落在这个区间的话，那么就说明k。它的数量级是不是和n是相同的？此时我们就可以考虑哎，能不能采用技术排序？如果用技术排序，我们得到的时间效率会比快，排堆排都要更高。当然，我们也需要付出一些空间代价，因为我们需要定义一个辅助数组b。和辅助数组c对吧？付出了空间的代价。

换取了更高的时间效率。所以我们说技术排序，它是一种典型的空间换时间的，这种呃算法思想。牺牲空间复杂度去换取更高效的这个时间复杂度好，所以说当我们在考试的时候，如果啊，考题让我们去排序的这个数组。它的取值范围k比较小。所谓的比较小，就是k和n啊同等数量级，那么在这种情况下，我们就可以考虑采用技术排序的思想。比如我们用两道真题来感受啊，技术排序在考试当中的应用，

一三年的41题，他说有一个整数序列a。a0一直到an- 1，其实它这的整数序列就是啊，一个长度为n的数组嘛。紧接着，题目告诉你说整个数组当中的每一个元素，它的取值范围呃大于等于零。小于n。好，然后说如果存在呃，这样的一些元素，这些元素的值都相等，并且这些元素的。总个数m大于二分之n。

满足这个条件的时候，我们称x是a的主元素。这句话有点绕，但他想表达的意思就是说，如果整个数组当中有超过一半以上的元素，它们的值都相等的话。那么，这个值x就是数组a的主元素题目，怕你看不懂，还给了你一个例子。比如说这儿有个数组，在这个数组当中五这个值就是它的主元素，因为整个数组当中出现了一个五两个五三个五四个五。呃，五个五总共有五个五对吧？

整个数组的呃长度是八，其中的五个元素，它们的值都等于五。所以五这个元素就是a这数组的主元素，后面这个例子同样的数组长度为八。但是五这个元素总共有一个两个三个四个五这个元素的出现次数没有过半。所以在a这个数组当中就没有主元素，这是后一个例子。好，然后题目让我们巴拉巴拉设计一个算法，要找到数组a当中的主元素，如果存在主元素，那么就输出这个元素，否则输出负一。你看我们需要统计a这个数组当中各个元素值出现的次数，

对吧？并且题目告诉我们a这个数组当中各个元素的值肯定小于n。好，那么这个题目我们是不是就可以利用技术排序的那种思想去解决？我们可以定义一个辅助数组c。辅助数组的长度是n，那恰巧所有的元素，它的取值范围不会超过零到n- 1。那我们是不是只需要呃把a数组给遍历一遍，一边遍历一边在c这个数组当中统计各个元素值出现的次数？然后最终看一下有没有哪个元素值，它的总数是过半的，那这样的话，这个题目是不是就解决了？好，

所以这个题目的解决，我们只需要把技术排序呃step 1，也就是统计各个元素值出现的次数，只需要把这一步做完。基本上这个题目就可以解决了，那具体的训练留给大家，当然这道题目啊，并不只有技术排序。一种解法，它有很多种解法，但是采用技术排序的这种思想啊，我们可以得到一个挺不错的分数。因为时间复杂度很优秀嘛，可以达到on这个数量级好，再看一个题，

一八年的41题。说给定一个含有n个整数的数组设计，一个在时间上尽可能高效的算法，它只要求在时间上尽可能高效。也就是说，在空间复杂度上，它这个题目不会有任何的要求，对吧？一定程度上，它是在暗示你这个题目可以采用空间换时间的这种思路。好，继续往后读呃，它让我们找出这个数组当中没有出现的最小的正整数。比如说他给了一个例子啊，这儿有个数组，

在这个数组当中没有出现的最小的正整数是一。我们看一下零不是正整数对吧？所以未出现的最小正整数就是一好，后面又给了一个例子。在这个数组当中，出现了一二三三个正整数，那在后面这个例子当中没有出现的呃，最小的正整数就是四。好，后面就是让我们设计一个算法，去找到一个数组当中没有出现的最小正整数。那大家思考一下，这个题目可不可以采用技术排序的思想去解决？好像不行，对吧？

因为题目并没有给出啊，每一个元素的取值范围，比如说。它不像一三年的这道题，一三年的这道题，它明确的告诉我们就是每个元素的取值范围不大于n。也就限制了这个k的数量级，但是在一八年这个题目当中，它可能出现负数，可能出现正数。而且题目没有给任何的条件去限制这些元素，它的取值范围那是不是就用不了技术排序了？好非也非也，我们换一个角度来思考题目，让我们找出的是数组当中未出现的最小的正整数。

整个数组当中，只有n个元素好。那我问你在长度为n的这个整形数组当中有没有可能啊？同时出现零一二三四一直到n+1。有没有可能出现这么多个值？不可能对吧？因为它的长度只有n嘛，所以在一到n+1这个范围内。这其中一定至少会有一个数是这个题目给的，数组当中没有出现过的，对吧？好，接下来有思路了吗？我们假设题目给的这个数组，它叫an，

那我们可以定义一个辅助数组啊，叫做c。这个辅助数组的长度，我们把它设的长一点，长度为n+2，那长度为n+2的话，这个辅助数组c它的下标范围就应该是零到n+1这个范围吧。对吧，好，接下来我们是不是可以遍历这个呃题目给的这个数组an每遍历到一个数组元素的时候？比如说我们遍历到四这个值，那么我们就把c4的值加一，我们在数组a当中遍历到二这个值的时候，我们就把c2这个值加一。好，

那么把所有的数组元素都遍历完之后c这个辅助数组是不是就回到了技术排序的那种逻辑啊？ci反映的是I这个值出现了几次，对吧？好那么进行完这种统计之后，我们最终要找到的是原始数组当中没有出现的最小正整数。那可以怎么做？是不是可以从c1这个元素开始？依次的往后找找，找到第一个ci=0的地方。比如说当c1=0的时候，是不是说明值为一的元素在元数组当中出现了零次？对吧，那么一这个元素就是元数组当中没有出现的最小正整数。明白我意思吧。如果c1这个元素非零，

那么就继续往后找呗，反正就从左往右找，找到辅助数组c当中第一个值为零的，这个这个呃位置。那么，这个位置所对应的这个下标，这个下标就是元数组当中没有出现的最小正整数。好，所以一八年的这个题目也可以利用技术排序的思想呃去解决，只不过这个题目对呃技术数组的这种应用还是比较灵活的。在考场上，我们可能不太容易想到那训练的问题，留给大家呃，技术排序也不是这个题目的唯一解，只不过采用技术排序的这种方法可以得到很不错的分数。

因为这个题目只是在时间上呃，提出了这种要求，在空间复杂度上，我们可以呃肆意妄为，对吧？而技术排序的这种思想，这种处理的方式，它的时间复杂度只需要on，只需要on这个数量级是非常优秀的。好，那通过一三年和一八年这两道题，希望可以呃带大家感受到技术排序的思想，在考试当中的应用。抓住这个核心要点，如果我们发现题目给的这个待排序数组，

或者我们要处理的这些数据。它的取值范围k比较小的话，那么采用技术排序的这种呃思想就可以获得很优秀的一个时间复杂度。也可以帮助我们在考试当中拿到更高的分数好，那么在这个视频中我们介绍了技术排序这种算法。它虽然在大纲的要求之外，但是在考试当中偶尔还是会用到对于技术排序。我们首先要关注啊，它的适用条件。首先，待排序元素。它的关键字必须是整数型，并且待排序元素的呃取值范围应该是零到k。并且k的值要比较小，最好k和n是同等数量级。

因为如果呃k的数量级大于n倍log n，那么我们就不推荐使用呃技术排序。当取值范围k和on是同等数量级的时候。技术排序的这个时间复杂度和空间复杂度啊，都只有on，并且技术排序是稳定的。那关于技术排序的算法实现，大家可以再结合这个思维导图以及课件再进行回顾，这就不再赘述。那最后还需要跟大家说明的是，我们在这个视频中给出的这段参考代码是改写字算法，导论这本经典教材。这段代码写的比较简洁，目的是便于大家理解，那这段代码的风格比较适合。

初步的学习以及考试，但是在实践当中，你可以对这个代码的功能进行一些拓展，让技术排序更具备通用性。我会给大家附上技术排序代码的，这个呃PDF附件那在实战当中，如果要使用技术排序的话。我们其实不需要严格的约定，这个待排序数组当中的元素值是零到k- 1，不需要做这样严格的约定。我们可以尝试着去找到数组当中的最大值和最小值，然后用辅助数组c的，比如说c0这个位置去统计最小值出现的次数。然后ck- 1这个位置去统计最大值出现的次数，那c数组中间那些元素就可以统计最小值和最大值之间的其他元素出现过多少次，

对吧？好，所以只需要进行一些变通，就可以让这个技术排序，它更具备通用性，那具体的代码实现呃，我会。把这个PDF的附件发给大家，这段代码你可以直接呃copy到自己的本地去运行。这儿的测试样例也给大家写好了，你看在这个测试样例当中，我给的这个待排序数组，它的值就没有限制在零到k。这么死的一个范围，对吧？

它可以出现负值，可以出现正值。反正我们的这个技术排序的代码可以灵活的去应对这种情况，那有兴趣有能力的同学，大家可以再看一下呃，我给大家的这段代码。你要知道，有时候考试和实战还是有一些区别的，在学习的时候，考试的时候怎么简单怎么来。但是在实战的时候，我们需要考虑到各种通用性，健壮性等等。好的，那以上就是技术排序这个补充考点。
