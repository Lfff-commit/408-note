好的，那这个小节中我们会学习冒泡排序这个算法啊，冒泡排序在大类上来说它属于交换排序的一种。所谓交换排序，就是说我们会比较两个元素的关键字，然后根据比较的结果，它们的大小关系来确定是否需要兑换交换这两个记录的位置。那基于交换的呃排序算法，我们会学习冒泡排序和快速排序这些算法，这个小节中我们会先学习更简单的冒泡排序。有没有觉得这个算法的名字很有意思啊？什么是冒泡呢？来看这样的一个图呃，一个小鱼，他吐了一些泡泡，

然后这些泡泡会不断的往上面冒。这是自然界里边的冒泡，那一会大家看一下我们的冒泡排序和这个小鱼吐泡泡，它有没有什么比较类似的地方？最好的方式还是直接结合动画来让大家理解，接下来我们会从后往前来依次对比相邻的两个元素。他们之间如果顺序反了，那么我们就会交换他们的位置，那把所有的这些元素都比较完一遍呃，这样的一个过程就称为一趟排序。好来看一下算法的运行啊，刚开始第一趟排序，我们会先对比最后的这两个元素，它们之间的大小关系啊，对了，

这个地方我们想要追求最终排序的结果是一个递增的序列。那由于此时左边的这个元素27要比49更小，所以他们俩的位置不需要交换好，接下来我们检查再往前的两个元素。13也要比27更小，所以不用交换位置，再接下来是76和13，那76要比13更大，而最终我们想要追求的效果是一个递增的序列，也就是小的元素。放在前面，所以在这种情况下，我们需要把13和76这两个元素的位置进行一个对调。把它们交换好，继续往后处理，

后面的两个元素13要比97要更小，所以我们需要把它们的位置互换。继续往后13比56更小，那么13和56也需要进行一次兑换好，接下来的两个元素是13和38，那13更小，所以应该把它们交换。好最后对比的是13和49，那13更小，所以我们也会把他们交换好，那这样的话，我们就把所有的这些元素给处理了一遍。好，那这就是第一趟冒泡排序，这一趟排序结束之后的一个效果就是我们让关键字的值最小的那个元素。

让它冒到了最前面就是越小的泡泡，我们越让它往前冒好，接下来第二趟的处理也是一样的，我们会从后往前呃，也是两两对比相邻的元素。那这两个元素不需要交换位置呃，接下来的这两个27要比76更小，所以我们需要把更小的元素让它冒到前面去。好再往后，27比97更大，所以往前冒27大于65，往前冒27大于38，继续往前冒27大于49。再往前冒好的你的复读机已经上线好，那到这一步我们还需不需要像之前那样继续再对比啊？

再往前的这两个相邻的元素呢？其实已经不需要了，对吧？因为经过之前那一趟的处理之后，其实13这个元素，它的最终位置已经确定了，不可能再改变，它是最小的一个元素，所以其实第二趟的排序。我们呃对比完了一和二这两个位置的元素之后，就不需要再继续往前对比好，那第二趟的排序结束之后导致的结果就是我们把最小的两个关键字。把它们冒到了最前面，就像两个小泡泡一样好，接下来第三趟的排序啊，

原理也是类似的，我们会让值更小的元素不断的往前冒。那同样的，之前已经确定了最终位置的这些元素，我们不需要再往前对比，所以第三趟的处理当中，我们对比到二和三这两个元素之后。我们就可以停了，那经过第三趟的呃排序之后，值最小的三个元素已经冒到了最前面，好接下来第四趟的冒泡排序还是一样的，我们会把值更小的这些元素。让它不断的往前冒。如果说两个元素的值相同的话，那我们不应该交换它们俩的位置，

因为这么做可以保证算法的稳定性，原本在右边的元素不会跑到左边去。好，第四趟结束之后，我们前边四个元素的最终位置就已经确定了，接下来是第五趟的排序76小于97，不需要交换。65小于76，不需要交换，最后49小于65，也不需要交换，那可以看到在第五趟的排序当中，整个过程当中并没有发生任何一次交换。那这种情况其实说明整体来看已经达到了一个有序的状态，所以经过第五趟的处理之后，

我们就可以确定其实整个表已经有序了。我们就不需要再进行第六趟的处理好，那这就是冒泡排序的一个原理，那捋清楚刚才的这个执行逻辑之后，其实代码的理解就很简单了。对了，这个地方我们定义了一个呃函数，专门用于交换两个元素的位置，对于两个元素a和b。我们会先定义一个中间的中转变量temp，然后把a的值先付给temp，然后把b的值付给a，最后再把temp当中保存的值再付给b。那这样的话就完成了a和b的交换好，回到冒泡排序的主体，

我们这个地方啊，定义了一个变量I。那在这段代码当中I所指的这个位置，之前的这些元素是已经排好序的，就是已经冒到顶的那些泡泡。那第二层的这个for循环，我们会让j的值等于n- 1，在这个例子当中，我们的n是等于八，因为总共有八个元素。那由于我们这些元素是从零这个位置开始存放的，所以呃，让j指向最后一个元素就应该是让它等于n- 1。然后这个if语句做的事情就是会判断j所指的元素和它前边的这个元素，它们的大小关系，

如果是逆序的话，那就会调用我们之前。定义的这个交换函数交换这两个元素的值，那一旦发生了交换，我们就会把flag这个标记位设为true。因为之前我们说过，如果一趟冒泡排序当中没有发生任何一次交换，那么其实我们可以直接提前让这个算法结束。所以这是flag这个变量的一个作用好，那j这个变量会一直减减，也就会把当前还没有排好序的这些序列全部都两两检查一遍，是否需要交换？那这就是冒泡排序的一个代码，实现那值得注意的是，我们在这个地方给出的条件只有左边的元素比右边的元素更大的时候，

我们才会交换他们俩的位置。左右两个元素的值相等的时候是不会交换的，那这么做可以保证这个算法是稳定的。好，接下来我们看一下这个算法的性能如何啊？空间复杂度很简单，肯定是o1这样的一个数量级，因为我们只需要定义几个辅助变量，只需要用常数级的空间就可以让这个算法顺利的执行。好，再来看时间复杂度呃，最好的情况应该是如果一个序列，它原本就是有序的。比如说这儿的一二三四五六七八就是关键字的值，在这种情况下，

我们进行第一趟冒泡排序的时候，会从最后这个元素开始依次往前检查。两两相邻的元素是否需要啊？交换位置，但是经过检查之后发现，所有的这些元素都不需要交换位置，那根据我们之前提出的那种处理方式。如果说在这一趟的处理当中没有发生任何一次交换的话，那么这个算法就可以直接结束，所以像这种原本这些元素就有序的情况下，我们总共就只需要进行一趟排序。并且这一趟的排序只需要对比关键字n- 1次，因为是把呃相邻的这些元素都两两对比嘛，那总共有n- 1组，所以对比的次数就是n- 1次。

而所有的这些元素都不需要交换，所以交换次数是零，那整体来看，在这种情况下，时间复杂度就是on这样的一个数量级。好，这是最好的情况，原本就是有序的，那最坏的情况是原本这些元素都是逆序的。那像这个例子当中，进行第一趟处理的时候，我们是从尾到头依次对比两两相邻的各元素，那一要比它前边的所有的这些元素都要更小。所以一这个元素会不断不断的往前冒。因此，

把一这个元素冒到最前边，总共需要和前边的这n- 1个关键字分别进行对比。并且每一次对比都会导致一次交换，所以交换次数和比较次数是一样的，那当我们把一。冒到最前边这个位置之后，对第二个元素的处理也是类似的，我们需要把第二个元素和之前剩余的啊，这n- 2个元素全部都依次对比一遍。并且每一次对比都会发生一次交换好，所以我们用冒泡排序处理这种逆序的情况，最终我们的比较次数需要这么多次。那每一次比较都会导致一次交换，所以交换次数和比较次数是相同的，那从数量级上来看就应该是on的平方，

所以这是最坏的时间复杂度。那对于冒泡排序来说，平均来看它的时间复杂度也会达到on的平方，这样的一个数量级。对了，刚才我们这说的是元素的交换次数，也就是调用了多少次我们定义的swap这个函数，如果我们探讨的是移动元素的次数。那么，其实我们每执行一次swap，都会移动元素三次，对吧？第一次是把a移动到temp，第二次是把b移动到a，第三次是把temp移动到b，

那这点需要和大家强调一下，因为我们在考题当中有可能会问大家就是移动元素的次数之类的。要和交换次数进行一个区分好，那这是冒泡排序的空间和时间复杂度，那另外一个问题是稳定性呃，这个问题我们之前已经说过冒泡排序是稳定的。好，最后我们再来探讨这样的一个问题，冒泡排序的这种思想是否可以用于链表呢？显然是可以的，比如说这儿有这样的一个链表，我们想让这个链表啊递增有序。那这儿的l指向了链头的元素，那我们可以这么做，我们可以从链头这个元素开始用这个元素和它之后的元素进行一个对比。

如果当前检查的元素更大，那么我们就让它和它后面的元素进行一个交换，而如果当前处理的元素。比它后面的元素要更小，那么就不需要进行交换，只需要让这个扫描指针依次的后移就可以好，所以用这样的方式我们。可以让更大的元素依次的往后冒，然后一直冒到最后。那第二趟的处理是不是一样的？我们再来搞定，剩下的这些元素就可以了，而总之冒泡排序的这种思想其实也适用于链表。那具体的代码实现留给大家，自己去练习。

对了，之前我们给的这个代码实现是从后往前冒泡，其实也可以从前往后冒，就是先把大的元素给它冒到最后面，就像刚才链表那个例子一样。那代码的实现是很类似的，基础不好的同学可以自己去动手练习一下。好的，那这个小节中我们学习了猫跑排序这个算法。啊，算法的原理还是很简单的，我们每一趟的处理都会让当前剩余的元素当中最小的或者最大的一个元素。把它冒到最终的位置，对于冒泡排序这个算法，可能大家比较容易遗忘的是这个点，

就是如果我们在某一趟排序过程当中没有发生交换的话。那冒泡排序是可以提前结束的，所以这一点需要特别注意一下，那冒泡排序是一个稳定的排序算法，如果说原始给的这个表就是有序的，那么就可以得到最好的时间复杂度。而在原始的这些元素是逆序的情况下，会出现移动和比较次数最多的情况。平均来看，冒号排序的时间复杂度也会达到on的平方，这样的一个数量级。好的，那以上就是这个小节的全部内容。
