好的，那这个小节中我们会学习第二个算法，就是希尔排序，因为这个算法是一个叫希尔的人发明的。那这个算法其实是基于上小节中学习的插入排序进行的一个优化，之前我们说当我们采用直接插入排序算法的时候。如果说你的这些数据元素原本就是有序的，那么在这种情况下，我们的直接插入排序可以得到很不错的一个啊，执行的效率。或者我们再把条件放宽一点，如果你的这些元素能够保证基本上有序的话，那这种情况下。直接插入排序的效率也会很不错，比如说像下面这个例子当中，

当我们在处理30这个元素的时候呃，由于30要比20更大，所以我们不需要移动任何的元素。而当我们处理十这个元素的时候，我们只需要把20和30都分别往后移，然后把十放到一这个位置就可以。也就变成十二十三十这样的一个状态，那后续在处理五十六十这两个元素的时候也不需要移动任何一个元素。只有再往后处理到40的时候，我们才需要把50和60往后移，然后把40移到啊50原有的这个位置。好，总之如果我们能够像第二个例子这样能够保证各个元素之间，它们基本上有序的话，那在这种情况下，

我们的直接插入排序的啊，那种策略也可以得到很好的执行效率。所以希尔排序的思想就是基于这样的一个考虑，我们会先追求表中的元素，部分有序。然后再逐步的逼近全局的有序好，这个看起来很抽象，我们直接看一个例子来理解，我们会把表里的元素把它拆分成一个一个特殊的子表。怎么拆分呢？就是说在每一趟的处理当中，我们都会设置一个增量d，然后把相距距离为d的各个元素，把它们看作是一个特殊的子表。然后我们会把这些子表当中的各个元素进行直接插入排序好，

我们结合动画来看一下是什么意思呃，假设我们第一趟的排序，我们设定这个增量d的值是四。也就是用元素的呃，总数除以二，那总共有八个元素，所以八÷2=4，这是第一趟的循环，我们设置的增量d的值。那所有的相距距离为d的这些元素，我们都会把它看作是属于同一个子表的元素，也就是说，对于49这个元素。和它相距距离为四的元素应该是一+4，也就是五，

这个元素也就是76。而对于38这个元素和它相距为四的元素，应该是六这个位置，也就是13那后面的也是一样的，65应该和。27把它们看作是同一个子表，而97和49看作是同一个子表，也就是这个样子。好，总之在第一趟的处理当中，我们会把相距距离为四的这些元素看作是同一个子表当中的元素，那接下来我们要做的事情是要对各个子表。分别进行直接插入排序，那对于第一个子表当中的这两个元素，它们本来就是有序的，

所以直接插入排序执行的结果不会导致这两个元素的顺序有任何的改变。而子表二当中的两个元素38要比13更大，所以在对子表二的这两个元素进行直接插入排序之后。我们最终得到的结果应该是13放到前38放在后面，那子表三也是一样的，对这些元素进行直接插入排序之后，我们会让小的那个元素放在前面。大的放在后面，那最后的子表四就是这个样子。好，那这样我们就完成了第一趟排序的工作，我们把各个子表当中的这些元素分别呃进行了。直接插入排序。所以第一趟处理之后，这个数组当中各个元素的位置应该是这样的。

好，接下来进行第二趟的处理，第二趟的处理，我们会缩小这个增量d的值，我们会在之前第一的基础上除以二。那之前第一是四，所以四÷2=2，因此第二趟的处理当中，我们会把相距距离为二的那些元素把它们划分为同一个子表。那对于一号元素与它相距距离为二的应该是三，那三再往后两个位置应该是五啊七，所以四九。二七七六六五这几个元素组成了第一个子表，那剩下的二四六八这几个元素，它们之间的相距距离也都是二。

所以剩下的这些元素又会组成第二个子表，也就是这个样子。好，那接下来的处理还是和之前一样，我们会对各个子表分别的进行直接插入排序，那对于第一个子表，我们对这四个元素进行直接插入排序。最终得到的结果应该是这个样子，第二个子表进行直接插入排序，最终得到的结果应该是这个样子。好，那到此为止，我们完成了第二趟的处理，那表里的元素总体来看就是变成了这样的一个排列的顺序。好，

接下来第三趟的处理，我们会让这个增量的值继续的缩小，在第二趟的基础上，除以二也就是变成一。那这就意味着，所有的这些元素都会被划分为同一个子表，所以最后的这一趟的处理其实就相当于对所有的这些元素。进行一次总体的直接插入排序，而这个时候其实经过之前的两趟处理，我们在这儿得到的这个元素序列其实已经呃达成了一个所谓基本有序。这样的一个比较理想的状态好，那接下来对所有的这些元素进行直接插入排序，得到的结果就应该是这样的。好，所以我们经过三趟的处理之后就可以得到一个全局递增有序的啊，

一个排列。那再把刚才的这几个过程给全部罗列一下，这是最初的一个状态，在低碳处理当中，我们把相距距离为四的。这些元素把它们看作是属于同一个子表当中的元素，那对所有的这些子表内部我们都会进行一次直接插入排序，那得到的结果是这个样子。接下来，在第一趟处理的基础之上，我们会进一步的缩小增量d的值，所以在第二趟的处理当中，我们会把相距距离为二的这些个元素把它们看作是同一个。这个子表里的元素，那把这些子表当中的元素进行一次直接插入排序，

就可以得到这样的一个状态。好，最后基于第二趟排序得到的这个结果，在第三趟的排序当中，我们会让增量d的值变为一。也就是所有的这些元素，我们都把它看作是同属于同一个子表，那对这个子表当中的这些元素进行直接插入排序，最终就可以得到一个全局的有序序列。好，那这就是希尔排序的一个大致的实现思想。那在这个例子当中，我们选用的增量序列d是四二一这样的一个，就是每一次缩小一半这样的一个序列。那这也是希尔本人，

他自己建议的一种增量序列的一个选取方式，第一趟的处理中增量d，我们可以选元素的个数除以二这样的一个值。然后在之后每一趟的处理当中，都会让增量d的值每一次减半，直到最后d=1为止。那我们考研当中，对于希尔排序的考察啊，通常也会出现其他各种各样的一个增量序列，比如说我们可以规定第一趟我们选择的这个增量。d的值是三第二趟选择一。好，那在这种情况下，在第一趟的排序当中，我们会把相距距离为三的这些元素看作是从属于同一个子表的元素。

那像这儿，我们不透明的这几个元素，就是从属于同一个子表对它们进行直接插入排序得到的结果，应该是这样的。而接下来处理第二个子表当中的元素，对它们进行直接插入排序，应该是这样的一个结果好。接下来第三个子表只有这样的两个元素。对他们进行直接插入排序，得到这样的结果，好，那这就是第一趟的处理所得到的一个结果。接下来，按照要求，第二趟的处理，

我们选择增量的值等于一，也就是对整体进行一次直接插入排序，那最终就可以得到一个整体有序的序列。所以第二趟的处理结束之后，得到的状态就是这个样子，那这也是考试当中关于希尔排序最常考的一种考法，就是会给你一个特定的元素序列。然后告诉你说每一趟的这个增量到底是多少，然后让你判断每一趟的排序之后所得到的这个呃序列是什么样的一个状态。那类似的题型，大家会在课后习题中遇到好，那接下来我们来看一下希尔排序怎么用代码，具体实现我们会定义这样的几个辅助变量，一个变量是d。那它就是当前这一趟处理当中的增量，

初始时在第一趟的处理当中，我们会让呃，刚开始的这个增量等于元素的个数除以二。也就是四这样的一个值好，接下来进行第一趟的处理I=d+1，也就是说I刚开始会指向四+1=5的这个位置。是不是好奇怪，那指向这个位置的原因是因为我们在处理呃，第一个子表的时候按照直接插入排序的规则，其实我们只需要从第二个元素开始。就是检查这个元素是否需要把它插到前面就可以了好，所以刚开始我们会让I指向第一个子表当中的第二个元素这个位置。那接下来按照直接插入排序的规则，我们是不是需要对比当前指向的这个元素，还有它前边的这个元素，

它们之间的大小关系？那由于当前指向的元素要比49更大，所以我们并不需要改变76这个元素的位置。也就是这个if条件不满足好，那接下来会进行第二轮的for循环，我们会让I的值加加。也就是变成I=6，那现在我们要处理的是第二个子表，那由于当前指向的这个元素要比它前边的这个元素要更小。所以我们会把当前指向的元素的值放到零这个位置，然后接下来更深层的这个for循环做的事情是。我们会检查当前这个元素，前边的这些元素是否要比当前处理的元素更大，如果更大的话，那么我们就会把j所指的这个元素。

给它后移，当然这个元素的后移并不是移到j+1这个位置，而是应该移到对于这个子表来说的后一个位置。也就是应该移到j+d这个位置。所以会把38移动到六这个地方好，那接下来根据这个for循环的规则，我们会让j=j-d。其实也就是要检查当前这个子表当中前边还有没有其他的元素，需要处理好，那由于此时d的值是四，所以j-d应该是等于负二这样的一个值。那到这一步j的值已经不大于零了，所以这一层的for循环会被跳出，接下来要执行的是这一句。也就是会把刚才我们保存下来的这个数值放到j+d的这个位置，

所以这一步的处理会把13复制到二。好，那这样的话，我们就完成了对第二个子表的一个直接插入排序，接下来会继续爱加加。处理下一个子表，那对于这个子表的处理原理也是类似的，我们刚才已经把这些代码一句一句给分析了一遍，所以就不再赘述大家，自己看动画。好，接下来继续让I加加处理最后一个子表，同样的，我们会对这个子表进行一个直接插入排序。然后继续I加加好，

那到这一步是不是我们的第一趟的处理就已经结束了？接下来第二趟的处理，我们会让d的值变为以前的一半，也就是让d=2好，现在我们要对第一个子表进行一个直接插入排序。所以我们的I值刚开始会让它等于d+1，因为我们只需要从这个子表的第二个元素开始处理就可以。好，由于当前处理的元素27要比49这个元素要更小，所以我们需要把49往后移。然后把27放到49那个位置。好，那现在对于这个子表来说，是不是前边的这两个元素其实是已经有序了？按照我们之前学习的直接插入排序，

接下来我们应该处理的应该是76这个元素，对吧？但是这个地方的代码实现啊，并不是这么做的，这儿我们会让I的值继续加加，也就是变为四。所以接下来我们会切换为处理第二个子表的这个元素好，那由于49要比13更大，这个if条件不满足，所以我们不需要对49和13的位置进行调整。接下来，我们会继续让I的值加加，那这样的话，我们又回到了之前的那个子表要处理这个子表的76这个元素。那同样的，

由于不满足这个if条件，所以76这个元素的位置不需要做任何的调整，那继续让j加加。接下来又切换回了另一个子表，要处理38这个元素，那由于38比49更小，所以根据直接插入排序的规则，我们需要进行一系列的调整。由于当前元素前边的这个元素呃值要比38更大，所以我们会把它前边的这个元素往后移一位。好，接下来会让j指向当前子表当中的再往前的一个元素，也就是会指向13，那由于13的值不比38更大，也就是不满足这个for循环的条件。

所以接下来会跳出这个for循环执行之后的这一句。我们会把之前保存下来的数据把它复制到j+d，也就是四这个位置那处理之后就是这个样子。好，接下来又回到了这个地方，我们会让I的值继续加加，这就是要处理另外一个子表那处理的方法，和之前类似，我们就不再赘述，大家自己看动画。好，那到此为止，是不是第一个子表当中的所有元素都已经保证了有序？接下来，继续让爱佳佳处理最后的一个元素。

那由于这个元素比之前的一个元素要更大，所以我们不需要做任何的处理。那到此为止，我们就完成了第二趟的处理，我们保证了这两个子表都是有序的。好，最后我们就可以进行第三轮的处理，继续让d的值呃缩小一半，也就是d=1。那之后的处理是不是就变成了我们最熟悉的那种直接插入排序好，那接下来的过程就很简单了啊，最终。排序得到的结果就是这个样子，得到了一个整体有序的序列，好的，

那这就是希尔排序算法的一个代码实现。其实我们理解一段代码最好的方式就是自己搞一个例子，然后根据这个代码的执行，一步一步耐下性子来分析。那课本里边给的这种实现方式，我们是每次让I加加，也就是说我们会轮流的切换着来处理不同的几个子表。但是其实对我自己来说，我觉得逻辑更清晰的一种处理方式，应该是我们可以连续的把从属于同一个子表的这些元素都一个接一个的把它处理完，然后再来处理另一个子表当中的这个。元素那大家也可以自己尝试着实现一下，我们提出的第二种实现思路。其实实现起来也不难，并且我认为这样的实现方式可能更符合我们对这个算法的一个理解。

好，接下来我们来探讨一下这个算法的算法性能如何？显然，对于空间复杂度来说，我们只需要常数级的一个辅助空间。而对于时间复杂度的分析，就要困难的多，比如说刚才我们举的这个例子，我们设计了这样的两种不同的增量序列。第一个增量序列是四二一，第二个增量序列是三一，那当我们采用不同的增量序列的时候，我们排序的趟数会受到影响。同时，在每一趟排序当中，

各个元素的这个对比，还有元素的移动，这些次数也都会受到影响。所以，关于希尔排序这个算法的时间复杂度到底是怎么样的？这个问题其实分析起来很困难，到目前为止都没有办法用数学手段来证明确切的。时间复杂度哎对了，大家想一下，对于希尔排序来说，如果我们第一趟的这个排序当中，我们选择的增量d的值就直接等于一。那这种情况下，算法是怎么执行的？想一下。

其实这种情况会导致希尔排序算法退化为直接插入排序，对吧？好，所以希尔排序在最坏情况下，它的时间复杂度也可以达到和呃直接插入排序一个数量级。因为就像之前我们说的，我们直接选择让增量d=1，那它不就是一个直接插入排序吗？另外，如果我们的数据元素数量不是特别大呃，在某一个范围内的话。那希尔排序的这个效率可以达到on的一点三次方，这样的一个数量级，那相比于on的平方，其实还是提升了很多的，

所以希尔排序比直接插入排序还是要优秀不少。只不过我们没办法用形式化的手段，数学的手段来确切的证明它的时间复杂度是多少？好，那这是希尔排序的空间和时间复杂度。那接下来我们来看一下这个算法的稳定性如何？来看这样的一个比较简单的例子，假设我们原始有这样的一个序列。然后第一趟的排序当中，我们选择呃增量d=2，也就是说相距距离为二的这些元素，我们会把它划分为同一个子表，也就是说65和呃。49这两个元素，我们把它划分在同一个子表当中，

那对这个子表进行直接插入排序导致的结果就是49这个元素跑到了前面，65放到了后面。好，接下来第二趟的排序，我们让增量d的值等于一，那最终排序的结果就变成了这个样子。那可以看到，在原始序列当中，我们有下划线的这个49，它应该是在右边的，但是经过希尔排序之后，有可能它会到左边这个位置，所以这个算法显然是不稳定的。那另一个方面，呃希尔排序的实现，

我们只能基于顺序表，因为我们需要用增量d来快速的找到呃与之相邻的。就是从属于同一个子表的各个元素，所以必须要有随机访问的特性，才可以实现啊，这样的一个事情。因此，希尔排序只能基于顺序表来实现，不可能基于链表来实现。好的，那这个小节当中，我们学习了希尔排序这个比较呃神奇的算法。算法的思想就是，我们要把整个序列把它划分成一个一个特殊的子表，然后对各个子表进行直接插入排序。

每一趟的处理之后，我们都需要把增量d的值不断的减小，直到最后d=1为止，在这个时候所有的元素都会被划分在同一个子表当中。那对于希尔排序的考察，通常会给出一个特定的例子，并且给大家一个增量序列，然后让大家分析每一趟的排序过程是怎么样的，排序之后的状态又是什么样的。而关于代码的考察频率不是特别高，当然了，其实只需要理解了这个算法的一个实现的思想之后，我们考试的时候临场写代码应该也是难不住大家的。其实所谓的代码无非就是你对处理问题的逻辑，用计算机语言把它描述一遍，

仅此而已。所以核心还是要理解这个算法的一个运行的逻辑，只要算法的逻辑捋清了，那代码其实就是逻辑的一个外化体现而已。好的，那以上就是这个小节的全部内容。
