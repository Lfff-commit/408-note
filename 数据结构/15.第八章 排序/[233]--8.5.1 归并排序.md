好的，那这个小节中我们要学习另外一个比较重要的算法，就是归并排序，那什么是归并呢？其实啊，你看它的英文会更好理解，就是merge。merge的意思呃，可以翻译为归并，也可以翻译为合并，如果你现在已经有两个或者有多个已经有序的序列，那么我们把这两个有序序列合二为一，合并成一，整个有序序列。的过程其实就是所谓的归并，

那来看一下怎么做，现在我们左边这个数组和右边这个数组都已经是有序的，我们要把它合并成一个。那首先我们肯定得定义一个更大的数组，才能把所有的这些元素都存放在一起。好，那接下来要做的事情是我们可以设置这样的三个指针I。JK，接下来我们每一次会对比I和j所指的这两个元素，到底谁更小？那我们会把更小的一个元素放到k所指的这个位置，那此时I和j所指的元素当中，更小的显然是七，所以我们把七放到k所指的位置。同时让j和k啊往后移。

好，现在I和j所指的两个元素当中，十是更小的，所以我们把十放到k所指的位置，同时让j和k继续往后移。好，接下来12要更小，所以把12放过来。接下来是16。21。好，那现在I和j所指的两个元素都是24，那如果我们自己写代码的话，我们肯定可以在代码里边规定，你可以让I所指的这个元素优先放过来，

或者也可以让j所指的优先啊，都OK。那这个地方我们让I所指的这个数组优先，所以我们会把没有下划线的24先给放过来，那接下来就是有下划线的这个24放过来。好，接下来是33，然后是37，好到了这一步，我们的j指针是不是已经超出他原来这个数组的一个下标范围了？那就说明右边这个数组所有的这些元素，我们都已经进行了合并。那现在只有左边这个数组是没有合并完的，所以接下来我们就不再需要进行关键字的对比，我们可以直接把左边这个表里的所有这些元素通通都放到我们这个总表的表尾当中，

也就是。这个样子好，那到此为止，我们就完成了两个有序序列的归并或者叫合并。好，那这儿还要引入一个概念，叫做二路归并，什么是二路呢？二路公交车不是这样的啊，所谓二路归并，其实就是我们刚才做的这个呃过程。我们是把两个有序序列，把它们合二为一。这是二路归并，所以在进行二路归并的时候，

其实我们每当要选出一个更小的元素的时候，我们只需要对比I所指的元素和j所指的元素到底谁更大？我们只需要对比关键字一次，就可以选出一个更小的元素，这是二路归并，那既然有二路归并，我们当然也可以有多路归并，比如说四路归并。像这个地方，我们给出了四个已经有序的数组，那如果我们把这四个数组把它们合四为一四合一。那这样的一个四合一的归并过程，或者说合并过程，其实就是所谓的四路归并，我们在这设置了四个指针p1P2P3p4，

分别指向这四个有序数组当中啊，目前剩余的。小的一个元素，那现在我们要把这四个数字进行归并，我们就需要在每一次都从啊这四个指针所指的四个元素当中挑选出最小的一个元素。放到k所指的这个位置，那由于总共有四个元素，所以我们至少需要对比关键字三次才可以挑出啊，最小的一个。比如第一次我们可以让p1和p2所指的两个关键字进行对比，然后挑出一个呃最小的关键字，接下来用这个最小的关键字和p3所指的对比。如果p3的更小，那么就更新这儿记录的最小关键字，然后最后再用这个最小关键字和p4进行对比，

所以总共至少需要进行三次的关键字对比。才可以从四个关键字当中挑出最小的一个。好，那这是四路归并，所以这儿得出的结论是，如果我们进行m路归并的话，那么我们要从这m路当中选出一个最小的元素或者最大的元素。至少需要对比关键字m- 1次，因此归并的路数越多，那么我们每挑选一个元素的时候。需要进行的关键字对比次数也相应的会变多，那我们在进行内部排序的时候，我们的归并排序通常是使用二路归并来实现的。其实，归并排序的思路很简单，

如果给出一个初始序列，那么我们刚开始会把这个初始序列当中的每一个单独的元素。都看作是一个一个独立的，已经排好序的部分。因为每个部分只有一个元素嘛，所以你单独看这个部分，那肯定就是有序的，那接下来第一趟的归并排序，我们会把相邻的啊，这样的两个部分。分别进行二路归并，比如说零和一这两个部分进行一个归并就可以得到这样的一个有序序列。接下来，二和三进行归并，四和五进行归并，

那最后六这个元素它单出来了，所以嗯，单独对这个部分进行归并，其实就相当于什么也没做。好，这是第一趟的归并排序，那接下来第二趟的归并排序会基于第一趟的结果，再次的对他们进行二路归并，比如现在零一这个部分已经有序。然后二到三这个部分也已经有序，把这两个有序的子序列归并成一个就可以得到这样的一个结果，那右边的这两个部分也一样的。这两个子序列各自也已经有序。所以把它们进行归并，可以得到这样的结果好，

这是第二趟的归并排序，经过第二趟的归并之后，我们得到了两个，就是已经有序的子序列。所以最后一趟的归并排序，我们只需要把这两个子序列给合并起来，归并起来就可以得到一个整体有序的。一个序列。好，所以这就是归并排序的一个思想，我们都是采用二路归并，把相邻的两个有序的子序列两两进行归并。好，那通过刚才的学习，大家应该能够感受到，

其实归并排序最重要的一个核心操作，就是要能够把数组当中两个有序的序列。归并成一个，所以接下来我们先来实现这个呃核心操作来看一下代码怎么做，比如我们现在有这样的一个数组a。三到六，这个范围是已经有序的，然后七到九，这一个范围也是有序的，两个有序子序列相邻，那我们会用low指针指向最前边这个元素。然后MID这个指针指向第一个有序子序列当中的最后一个元素，然后再用hi指针指向第二个子序列当中的最后一个元素。那这样的话，我们就可以用low mi de hi来区分出我们要归并要合并的两个子序列的范围。

这两个子序列是相邻的，那这和我们之前提出的这个归并排序的需求是一样的，我们每一次要合并的肯定都是两个相邻的子序列。这些要合并的元素，他们数组下标都是连续的好，所以来看一下怎么合并这两个子序列。在这，我们会定义一个辅助的数组b，这个数组的大小啊，等于n也就是说和a数组。大小是一致的。那现在我们要merge，要归并，要合并的是a数组当中low到me的这个子序列和me的加一到hi这个子序列。这两个子序列原本就已经有序了，

好，那接下来这个for循环要做的事情是我们会把。a这个数组当中，low和high之内的这些元素，把它们复制到b数组当中，就是用一个指针k，让它先指向low，然后每一次把k所指的呃a数组的。这个元素复制到b数组中相对应的位置。每一次都是k加加，然后k要小于等于hi，所以把最后这个hi所指的元素复制过来之后，第一个for循环就结束，接下来会进入第二个for循环。这个for循环所做的事情就是进行归并，

这儿定义的I指针是指向了第一个有序序列当中的呃，第一个元素，然后这指针是指向了第二个有序序列当中的。第一个元素，然后k所指的这个位置就是我们接下来要把小元素放到哪儿，是指明了这样的一个信息，其实和我们之前演示的那个例子是一样的。好，那每一轮的for循环，我们会对比此时I所指的这个元素是否要小于等于j所指的元素，如果满足这个条件的话，那么就会把I所指的元素复制到。a数组当中k所指的这个位置，同时让I加加好，那刚开始这两个元素显然是I所指的更小，

所以我们会把I指的这个元素。复制到AK这个位置。好，接下来还要让k加加，那现在I和j呃所指的两个元素显然是j的比较小，也就是会执行else相关的逻辑，那我们会把j所指的元素。复制到AK这个位置，同时让j加加也就是这样。好，同样的，接下来我们还要让k加加。接下来I和j所指的两个元素，他们是相等的，那如果说两个元素相等，

我们会优先让I所指的这个元素复制到元数组a当中。那这么做的好处在于我们可以保证算法的稳定性，因为I所指的这个子序列在原本没有归并的序列当中是更靠前的，所以我们优先选择。原本就更靠前的这个元素，把它先合到啊，最终的这个数组当中，那我们就可以保证算法的稳定性好，所以我们会优先选择没有下划线的24。好，那接下来是把j所指的元素放过去。接下来是33更小，所以把33放过去好，那到这一步我们的j已经超出hi所指的范围了，不满足j小于等于hi的条件。

所以这个for循环会跳出此时j所指的这个此序列，其实已经合并完了。所以接下来我们会用这样的两个while循环来分别检查I和j所指的两个子序列是否已经合并完，对于没有合并完的子序列，我们会把。剩余的这些元素统统放到相应的位置，那现在我们的I是小于等于幂的，那说明I所指的子序列没有处理完。所以接下来我们会把37。45都给挪过去。好到这一步，我们就完成了原来的两个有序词序列的归并合并，那这个归并的操作就是我们归并排序算法的一个核心操作。好，那我们再来看一下归并排序的完整代码，

我们要对一个数组a进行归并排序，那我们会用low和hi来指明我们要进行归并排序的。这个元素的范围，那现在a这个数组零到六这个范围我们都是需要进行归并排序的。好，接下来要做的事情是我们会把这一整个呃无序的序列，把它从中间拆分MID=low+hi÷2，也就是等于三。接下来，我们会把原本的这一整个序列拆分成左右两个部分，左半部分是low到me的，右半部分是me的加一到hi。然后接下来我们会分别对左半部分和右半部分递归的进行归并排序。那经过这种递归的处理之后，左右两个部分都可以变得有序，

那接下来我们只需要再用刚才我们提出的这个merge函数。把左右这两个有序的子序列再进行一次归并就可以。好，那merge函数怎么执行？这个问题我们刚才已经分析过了，这儿就不再赘述，那这儿对于写代码不太多的同学可能不太好理解的是。如何对左右两个部分递归的进行归并排序，比如我们现在来看对左半部分怎么进行这个归并排序？这儿传入的参数是数组a，还有low和MID，也就是说在下一层的merge sort这个呃函数当中，我们传入的hi指向的是三。然后low指向的是零。它指明了当前我们要进行归并排序的呃，

这个序列的范围。好，那对这个部分进行归并排序，我们还会继续的往下拆分，同样的，我们会用MID=low+hi÷2。然后把这个序列再一次的拆分为左右两个部分，然后接下来同样的我们还需要再递归的对左右这两个小部分再进行归并排序。比如对于左边这个部分的归并排序，我们传入的参数是数组a的值，然后指明这个部分的范围应该是零到一好，所以再下一层的merge sort。啊，这个low和high就是指向了零到一这个范围，这种递归类的算法就像俄罗斯套娃一样，

一层套一层一层套一层。所以也使得很多初学者很怕这种递归类的算法好，那回到刚才这个算法啊，我们现在是要对零到一这个小范围进行一个归并排序。那按照这个代码的逻辑，当low小于high的时候，我们还会进行再下一层的拆分。MID=low+high除，以二向下取整，那么MID就应该是等于零。所以即便现在只剩下两个元素，我们还是会把它们进一步的拆分成左右两个部分，然后分别会对左右两个部分再递归的呃进行。归并排序好，但是现在左右这两个部分呃，

分别只剩下了一个元素，所以下一层的merge sort就什么也不会做。因为你的序列当中只含有一个元素，那你还怎么排序啊？不用排了，肯定就是有序的好，所以到这一步，我们的俄罗斯套娃终于到了尽头。然后接下来就会执行到这个merge函数对零到一进行一个归并MID，是指向零。那么，归并的过程就很简单了，有两个有序的子序列，每一个子序列当中都只有一个元素，那之前我们分析过这个merge函数所需要做的事情就是。

首先要把啊，我们此次要处理的这个部分全部都给复制到b数组当中，接下来对两个有序序列进行归并，归并之后是要写回a数组，所以归并之后的结果应该是。38在前，49在后，那具体这个merge函数怎么执行，我们就不再赘述，现在这种情况比之前我们分析的那个例子要简单的多。好，总之我们的归并排序算法其实是使用了递归的实现方式。对于一个无序的序列，我们要对它进行归并排序，那我们可以呃从中间把它拆分成左右两个部分。

然后分别对左右两个部分递归的进行归并排序，那当左右两个部分都有序之后，就可以对左右两个有序的子序列进行归并。那怎么实现归并？我们是使用了这样的一个merge函数。好，那接下来我们对归并排序的算法效率进行一个分析，那刚才我们给出的这个归并排序示意图，有的地方会把它称为归并树。因为你看它的形态是不是就像一颗倒立的二叉树？那我们把它看作是一棵树，我们就可以利用我们很熟悉的树的那些性质来对这个算法进行分析来看一下这个树高是h。那归并排序的趟数应该是h- 1趟对吧好，所以我们可以利用这些特性来推算出含有n个元素的表进行二路归并排序。总共需要进行多少趟那二叉树的第h层，

最多只会有二的h- 1个节点，所以如果有n个元素。它所对应的归并数数高为h的话，那应该要满足这样的一个条件，因为我们的n个节点都需要出现在这棵树的第h层。而刚才我们说了dh层最多只会有二的h- 1个节点。因此n要小于等于二的h- 1好，那把这个不等式解出来就可以得到h- 1可以取啊log 2n向上取整，这样的一个值。那之前我们又说了，这棵归并树的高度h再减一，其实就是归并排序的趟数，所以对于n个元素进行二路归并排序，那归并的总趟数就应该。等于h- 1，

也就是log 2n向上取整。而我们每一趟的归并时间复杂度，都是在on这个数量级，所以算法的总体时间复杂度就应该是每一趟的时间on再乘以总趟数log 2n。就应该是o的n×log 2n这样的一个数量级好，那有同学可能不理解为什么每趟归并的时间复杂度都是on这个数量级？我们来看一下左边这个具体的例子好，现在假设我们要进行最后一趟的排序，也就是基于第二趟排序的这个结果，要把这两个有序的子序列合二为一。那还记得我们是怎么合并的吗？我们会用两个指针I和j分别指向两个子序列的呃，当前还剩余的最小的一个元素。然后我们每对比关键字一次，就可以从中挑出一个更小的元素，

那最终我们把这两个有序子表合二为一，所需要进行的关键字对比次数。肯定是小于等于n- 1次，因为我们每对比一次，关键字都可以挑出一个当前还剩余的更小的元素。所以最多进行n- 1次的关键字对比，就一定可以完成这一趟的归并，所以你看我们最后一趟的归并，它的关键字对比次数啊，最多是n- 1是on这个。数量级对吧？好再来分析我们基于初始序列的第一趟归并，我们把这些个元素两两进行对比，那每一对元素我们其实都只需要进行一次的关键字对比。因此，

我们第一趟的归并排序啊，所需要的关键字对比次数应该是约等于二分之n。大概只需要对比关键字，这么多字就可以了，你看他也是o的N次方这样的一个数量级，所以不管是哪一趟的归并，我们所需要的关键字对比次数都是on这样的一个数量级。因此，每一趟归并的时间复杂度也是on这个数量级好。那这就回答了我们之前提出的一个疑问，那对于归并排序来说，无论我们给出的这个初始序列，它到底是有序啊？无序啊，还是任何一种状态，

最终时间复杂度肯定都是o的n×log 2n这样的一个数量级。也就是说，归并排序的最好，最坏和平均时间复杂度都是一样的好，这是时间复杂度。接下来聊一聊空间复杂度，那归并排序的空间复杂度其实主要是来自于我们之前说到的那个辅助数组。b我们定义的辅助数组b和原本用于存放那些元素的数组a是同样的大小。长度为n，所以空间复杂度应该是on这个数量级，不过有同学可能会有疑问，因为我们的归并排序算法，它是用递归的方式实现的。那既然涉及到递归递归工作站，是不是也需要一些辅助空间呢？

是的，确实没错，不过我们递归调用的深度其实不会超过log 2n这样的一个数量级。所以递归所带来的空间复杂度应该是o的log 2n，那么它和这个数组带来的on这个复杂度相比，把这两个部分的空间进行相加，我们只需要保留更高阶的一个，所以整体来看的空间。复杂度其实主要还是受辅助数组b的影响，是on这个数量级好，那这是时间和空间复杂度，那对于归并算法的稳定性，我们之前也说了。当我们对两个连续的有序子序列进行归并的时候，如果说在两边同时出现了关键字的值相等的情况，

那么我们会优先的让靠左边的啊，这个元素先给合并进去。所以归并排序肯定是稳定的一个算法好的，那这个小节当中我们学习了什么是归并排序，其实归并排序最核心的呃处理还是？如何把两个已经有序的子序列啊，合并到一起。只要你会实现merge函数，那归并排序的这个递归部分其实是很好理解的，如果low小于hi啊，不好意思，这个地方又打错了。如果low小于high的话，那我们会从中间点MID，把它切分成左右两个部分。

然后分别对左半部分和右半部分再递归的进行归并排序。当左右两个部分都已经有序之后，我们再对左右两个有序的子序列进行一次merge操作。那归并排序的时间复杂度可以达到n的log 2n这个数量级和堆排序和快速排序，是可以达到同样的优秀程度。当我们进行内部排序的时候，我们通常是采取二路归并的一个策略，而如果我们把归并排序算法用于外部排序的话，那我们还会使用到更多路的归并排序。那具体的内容，我们还会在外部排序，那个部分进行学习好的，那以上就是这个小节的全部内容。
