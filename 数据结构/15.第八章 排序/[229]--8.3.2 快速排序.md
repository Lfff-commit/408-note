好的，在这一小节中，我们会学习快速排序，这个算法你看这个算法，它本身名字就取得这么自信，它敢把自己称为快速排序，那说明它肯定是比较优秀的。事实上，在我们所有要学习的内部排序算法当中，快速排序的平均表现性能确实是最屌最优秀的。在上一节学习的冒泡排序当中，我们每一趟的处理都会使一个最大或者最小的元素确定最终位置。而在快速排序算法当中，我们每一次都会使一个中间元素确定它的最终位置。那算法的思想是这样的，

对于一个给定的待排序的表，里边总共有n个元素，那像我们这儿给的这个例子就是有八个元素的待排序表。然后我们会选择一个元素作为所谓的书轴，或者叫基准，比如说在第一趟的排序当中，我们可以选49这个元素作为基准。然后接下来再经过一趟排序之后，我们可以找到49这个元素应该摆放的最终位置。那49这个值在整个表当中看起来并不是最大的，也不是最小的，它应该是处于一个中间位置，所以我们要确定49这个元素的最终位置。那就意味着，其实我们需要对整个表进行一个调整，

我们需要把这个表划分成左右两个部分，左半部分所有的元素都要比49更小。而右半部分所有的元素都要大于或者等于49这个基准元素，那这样的一次处理，我们把它称为一次划分。因为是用49作为基准，把整个表划分成了左右两个部分，好来看一下怎么做这个划分的过程，我们用low和hi两个指针分别指向。此时，我们要处理的啊，这个序列的头和尾两个位置，然后我们选择low所指向的这个元素。让它作为所谓的基准元素，或者也可以叫输轴元素好，

接下来我们会让low和high这两个指针开始往中间移动。我们会用low和hi这两个指针，把所有的这些待排序的元素都给扫描一遍，在整个扫描的过程当中，我们需要保证hi所指的这个指针它的右边。都是大于等于当前的基准元素49的，而漏指针它的左边，我们要保证都是小于49的元素。好由于此时low所指的这个位置是空的，所以我们先让hi向左移动那hi当前所指的元素显然是要大于等于49的，所以下标为七的这个元素并不需要移动。那我们让hi减减，让它左移。此时hi所指的这个元素是要小于49的，那刚才我们说了，

所有比49更小的元素都应该放到low指针。它所指的位置的左边，所以我们可以把27这个元素让它移动到low所指的这个位置，也就是这个样子好，现在hi所指的这个位置空出来了，那接下来我们让low指针。往右移动，此时low所指的位置是要小于49的，所以不需要把它移到右边，接下来让low指针右移，那当前指向的这个元素同样要小于49。所以不需要把这个元素移到右边，继续让low指针右移，此时指向的元素65，它的值是要大于等于49的，

那刚才我们说了，所有大于等于49的元素。我们都需要放到hi指针所指位置的右边，所以65这个元素我们会把它放到hi所指的位置。好，接下来low所指的这个位置空了，所以接下来我们让hi指针继续左移。此时hi所指的元素是要大于等于49的，所以不需要移动，因此让hi指针左移。那当前hi所指的元素小于49，而刚才我们说了所有小于49的元素都需要放在low指针所指位置的左边好，所以我们会把13这个元素。把它放到low指针所指的这个位置，接下来切换回low指针，

让它继续右移。当前low所指的这个元素显然不需要右移。那下一个元素97是要比49更大的，所以我们会把97呃移动到hi所指的这个位置，也就是这个样子。好，现在又切换回hi指针，让它继续左移。此时hi所指的元素大于等于49，所以检查下一个元素，这个元素也要大于等于49。所以继续让hi减减当low和hi碰到一起的时候，就说明其实我们已经把所有的这些待排序的元素都给扫了一遍。所有比基准元素49更小的元素，我们都把它放到了low指针所指位置的左边。

而所有比49这个基准元素更大的或者相等的元素，我们通通把它放在了hi指针所指的右半部分。那到这一步，我们就可以确定我们的基准元素，49肯定是放在三这个位置，也就是low和hi相遇的这个位置好，所以我们把49放到三这儿。那这就是一次所谓的划分的过程呃，我们选定了待排序序列当中的第一个元素49作为所谓的输轴元素，或者叫基准元素。然后我们用low和hi这两个指针分别指向了大排序序列的最左和最右两个位置，当这两个指针往中间移动的时候，我们每扫描到一个元素都会对比这个元素和基准元素的大小关系。如果比基准元素更小的，我们通通会把它移动到左边，

如果比49更大的，我们会把它移动到右边，那当low和hi相遇的时候，我们就确定了这个基准元素。输轴元素的最终存放位置。好，那既然49的最终位置已经确定了，那在接下来的排序当中，我们是不是就不需要再管49这个元素了？我们只需要管左边这个子表和右边这个子表，对吧？好，所以接下来要做的就是我们会对左右这两个子表再次的用同样的方法进行划分。好，先来看左子表，

对它进行划分，那我们同样的是用low和hi两个指针指向当前我们要处理的这个序列的头和尾两个元素。选中最前边的一个元素作为基准元素，现在来检查hi指针所指的元素13是要小于基准元素27的，所以应该让13这个元素放在靠左的位置。所以13会把它放到low所指的地方好，现在hi所指的位置变空了，所以我们切换回让low向右移动。38它的值要大于等于27，所以需要把38放到靠右的位置，因此38放到hi所指的这个地方。好，接下来让hi向左移动，那low和hi就会在中间一这个位置相遇。此时说明呃，

这个子表当中所有的这些元素都被我们扫了一遍。那我们就可以确定27它的最终存放位置，应该是此时low和hi共同指向的这个位置好，那这样的话，零到二这一个子表是不是又再一次的被我们刚才选中的书轴元素？划分为了左右两个部分，那由于我们剩余的左右两个部分都只有一个元素，所以显然这两个部分我们不需要再进行别的处理。也就是说，零到二这个子表当中，所有这些元素的最终位置，我们此时都已经确定了。好，那这是对零到二这个子表的处理，接下来我们切换回对四到七这个右子表的处理，

同样的方法用low和hi分别指向我们要处理的这个。呃子序列的头和尾的位置，同时我们选中让low所指的这个元素作为所谓的基准元素。此时hi所指的元素要小于76，所以需要把它移动到low所指的位置，然后接下来我们让low向右移啊，下面一个元素97要大于76。所以我们需要把97移动到hi所指的位置好，接下来切换回让hi向左移65这个元素的值是要小于76的。所以需要把65这个元素移动到low所指的位置好，接下来让low向右移，那么low和hi就呃碰头了。所以76这个元素的最终位置，我们就可以确定了，它是放在六这个地方，

那这样的话，对于四到七这个子表，我们通过基准元素76。再次的把它划分成了更小的两个部分，对于左半部分的处理呃，思路是一样的，我们让low和high指向呃首和尾两个元素。然后让low所指的这个元素作为基准元素，接下来hi只需要进行一次左移，就可以让low和hi在四这个位置碰头，那么就说明带下划线的这个49，它就应该存放在四这个位置。好，那最后还剩65和97这两个元素，那显然这两个元素的最终位置此时也已经可以确定了，

所以这就是我们快速排序的整个过程。我们会不断的进行划分这个动作，每一次的划分会使得一个连续的序列被分为左边和右边两个部分。左边的元素都要比书轴元素，基准元素更小，而右边的元素都要大于或者等于我们的基准元素。那接下来我们只需要递归的对左右两个部分，两个子表再分别进行划分就可以。好来看一下具体的代码实现啊，快速排序，这个算法代码的考察频率应该是所有排序算法当中最高的一个。所以这个部分我们会讲的比较细，同时呃，很多跨考的同学对于递归类的算法其实会感觉比较懵。所以接下来我们会一步一步来分析整个递归发生的过程是什么样的，

这个地方我们给出的这个示例图是递归工作站或者叫函数调用站。好，现在我们指定了这样的一个数组a，此时我们需要排序的呃，这个序列区间范围应该是零到七，这个范围内，所以我们刚开始会传入整个数组a的值。同时，指明最左边的元素和最右边的元素下标应该是零到七，这个范围好，此时满足这个if条件low小于hi，所以我们会进入这个if条件的处理。那这一句我们会调用partition这个函数。这个函数要实现的事情就是进行一次划分，我们要把a这个数组的low到hi这个区间内的元素进行一个划分，

所以接下来在函数调用栈当中会压入。呃和partition这个函数相关的呃一些的信息，包括low变量和high变量的值，那此时我们传入的low和high。值是等于零和七的对了，在这个地方，系统还会在背后记录啊，我们的quicksort这个函数此时是执行到了第几行？那此时是执行到了96行，所以我们这写的是一个井号加96。记录这个信息是为了我们一会partition函数，它return返回之后，我们需要能够知道接下来我们应该执行的是哪一行，所以需要把这个给记录下来。好，

现在我们进入partition函数的一个处理，第一句我们会让呃low指针所指向的这个元素。用一个叫做pivot的变量，把它保存下来，在英文里的意思就是书轴。或者叫基准好，接下来我们会用这个书轴对整个表进行一个划分，此时low小于hi是满足的，接下来这个while循环，这我们会检查。如果此时low小于hi，同时如果hi所指的这个元素，它的值大于等于呃书轴元素49的话。那么，我们会让hi一直减减，

也就是说，当前这个while循环，我们想要做的事情就是让hi指针不断的左移，直到找到一个比书轴元素更小的元素为止。才会跳出这个y循环好，所以hi指针左移，那当它指向这个元素的时候，当前所指的元素是要小于数轴元素的，所以不满足y循环的条件就可以跳出y循环。可以执行下一句，也就是让hi所指的元素把它放到low所指的这个位置。好，继续往后，现在又有一个while循环当low小于high的时候，我们会检查此时low所指的元素是。

是否小于等于输轴元素49，如果满足这个条件的话，就会让low一直加加，也就是说这个while循环要做的事情是会让low指针不断的右移。直到找到一个比49更大的元素，才可以跳出这个while循环好，所以low指针右移，一直到65这个元素才可以跳出while循环。然后执行下一句，也就是让low所指的元素把它放到hi所指的这个位置，也就是这个样子。好，接下来，由于呃low同样还是小于hi，所以我们会让这个大循环继续执行下去。

好，接下来。又到了第一个while小循环，还记得这个小循环，它做的事情是什么吗？我们会让hi指针不断的左移，直到找到一个比49更小的元素为止。那显然，下一个比49更小的就是13这个元素，然后让13这个元素移到low所指的位置。好，接下来又让low指针继续右移，找到一个比49更大的元素，把它放到hi所指的这个位置。好！

此时同样满足low小于hi这个条件，所以接下来我们还会让这个大循环继续下去，接下来这个while小循环，我们会让hi指针不断的左移。而剩下的这些元素都要大于等于这个数轴元素，所以直到hi一直左移移动到和low相等的位置。才会因为不满足low小于hi这个条件而跳出这个while循环，那由于此时low和hi指向的是同一个位置，所以接下来这一句的处理其实相当于什么也没做。好，再到下一个呃，while小循环此时由于不满足low小于hi，所以我们的low指针也不会继续右移了。因此，可以到下一步，

那同样的，由于hi和low所指的位置是同一个位置，所以接下来这句代码的呃，这个操作其实相当于什么也没做。好！此时再回到这个外层的大循环。此时，由于low小于hi，这个条件不满足了，所以可以跳出这个循环。然后执行下一句，也就是把我们之前保存下来的这个基准元素，把它放到low所指的这个位置，也就是这个样子。好，

所以这样的话，我们就完成了一次所谓的划分，我们把所有小于49的元素都移到了左边。所有大于等于49的都放在了右边。那最后partition这个函数会return load的值当第一层的这个partition函数执行结束之后。再回到我们这个quicksort函数，我们就可以知道啊，之前的这一次划分，最终我们是以三这个位置作为一个呃基准。分为了左右两个部分好，我们让这个函数继续执行下去，接下我们会先处理左子表，由于刚才返回的这个值是三。所以呃，左子表的范围就应该是low啊，

零到三减一也就是零到二的位置。所以接下来会进入第二层的quicksort函数，同时也需要把上一层的这个quicksort函数执行到了什么位置，执行到了97行，把这个信息给记录下来。对了，这个地方我们是用了一个简写，我们用p来表示pivot position这个变量的值。好，现在我们进入了第二层的quicksort函数，我们此时要处理的是零到二这个范围的子表low=0，high=2，那同样的我们还是会调用刚才我们。实现的这个partition函数传入的low和high就是零到二好，同样的，

我们以low所指的元素作为基准。然后让小于27的元素移到左边，大于27的元素移到右边，直到搂和嗨在中间点相遇。那我们就可以把27这个元素放到一这个位置，由于刚才partition这个函数的每一句代码我们都呃分析的很详细了，所以这个地方我们讲的稍微快一点。好，那到这一步，我们确定了这个基准元素，它的存放位置是一，所以这个partition函数最终会返回low所指的位置。一。也就是说，对于刚才我们所说的第二层的quicksort对零到二这个区间内的元素啊进行划分之后，

我们的中间元素是放在了一这个位置。刚才处理partition函数是呃执行到了第96行，所以接下来我们需要执行的是第97行。好，97行要做的事情是对左子表呃再一次的进行划分，那此时左子表当中只剩下零这个元素。所以在第三层的quicksort函数当中，我们传入的low和high其实就是零和零这两个值。那由于low=high，就说明啊，此时这个子表当中其实只剩余一个元素，那一个元素你肯定不需要再继续的排序处理了。也就是说，不满足low小于high的这个条件。因此，

我们第三层的这个quicksort就什么都不用做，就可以直接返回上一层的这个递归调用。那呃上一层的这个递归调用之前是执行到了97行，所以呃这个函数返回之后，我们应该紧接着执行98行。那98行传入的参数p+1，应该是等于一+1，也就是二，然后hi的值是二，所以接下来98行的这个函数调用我们传入的。low和high的范围是二和二，那和之前一样，并不满足low小于high这个条件，所以这一层的啊，quicksort。

其实什么也不用处理就可以直接返回上一层好，那之前这一层呃本来已经处理到第98行了，那再往后是不是已经没有别的代码需要再运行？因此，又可以再进一步的再返回到更上一层的这个函数之前，这一层的函数是处理到了97行，所以接下来要运行的代码应该是98行的这个代码。这儿传入的参数p+1应该是三+1，也就是四，然后hi的值应该是七好，所以接下来的这一层函数调用当中，我们要处理的就是。四到七这个范围内的这些元素，而此时由于low小于hi，所以我们会执行96这一句。

也就是调用partition函数传入的参数low=4，high=7，那我们会让四这个元素作为基准元素，然后把所有小于76的放到左边。大于76的放到右边。知道low和hi在中间相遇，那我们就确定了76应该存放的位置是六这个地方。好，所以这个partition函数最终会返回的，值应该是六，也就指明了76，它所存放的最终位置，那返回到上一层函数之后，呃，我们就可以知道。

四到七，这些元素是以六作为一个呃中间点，划分为了左右两个部分，接下来应该先处理左子表。也就是处理四到六减一四到五这个范围好，所以我们会进入第三层的quicksort。low=4，high=5，同样的会调用partition函数经过处理之后，我们就可以确定49这个元素的最终位置应该是四这个位置。所以这个partition函数最终返回的return的值应该是四，因此对于它上面这一层的quicksort函数来说。这儿返回的值呃p应该是等于四好，接下来在这一层函数当中需要继续执行第97行。那传入的参数low应该是等于四，

然后p减一应该是等于四减一，等于三，也就说在下一级的quicksort函数调用当中。low的值要比hi更高了，那这其实是说明刚才我们的这个书轴元素49，它的左边已经没有待排序的元素了。所以在这种情况下，同样不满足low小于high的条件，所以这一层的quicksort就可以直接跳出。返回到上一层好，刚才执行了97行，所以接下来应该执行98这一行啊，传入的参数p+1应该是五，然后hi也是五。所以接下来这一层quicksort需要进行快排的，

其实也就是五到五这个区间，由于只有一个元素不满足这个条件，所以这一层的递归调用也。同样的，什么也不会做，会直接返回上一层好，那到此为止，在这一层的递归电路当中，是不是我们已经执行完了最后一句代码？因此，这一层的递归调用就运行结束，再一次的返回，再上一层，那在这一层的调用当中，我们之前执行到了97行，

所以接下来要执行98行。传入的参数p+1应该是六+1=7 hi，同样是等于七，所以98行的这一次函数调用我们传入的low和hi都是七。那和之前类似，low小于high，这个条件不满足，所以这一层的函数调用我们什么也不会做，会直接返回好现在第二层的这个调用我们也已经执行完了，最后一句98行代码。所以这一层的函数调用也可以执行结束，就可以返回再上一层，也就第一层，那第一层的最后一句代码98也已经执行完了。那到此为止，

整个递归执行结束，此时零到七这个范围内的所有的元素都变成了一个有序的序列。好，所以这就是快速排序代码的一个执行过程。接下来，我们对快速排序的算法效率进行一个分析，先来看时间效率。我们刚开始给的初始序列呃，有零到七这样的几个元素。所以按照刚才我们分析的这个过程，可以看到我们第一层的这个quicksort函数，其实就是要处理零到七这个范围内的这些元素，对它们进行一次划分，也就是对零到七调用一次partition函数。而对于partition函数，

其实要做的事情就是low和high两个指针，不断地往中间移动。因此partition函数的执行，也就是要把n个元素都扫描一遍，它的时间复杂度不会超过on这样的一个数量级。因此，用partition函数处理n个元素，那时间复杂度就应该是on这样的一个量级好。那经过第一层的quicksort处理之后。我们会确定一个元素的最终位置，这个元素把原表划分成了左右两个部分，所以在第二层的处理当中，我们需要处理的呃元素范围就应该是零到二和四到七这样的两个范围。分别是左子表和右子表，因此第二层的quicksort其实就是分别对零到二和四到七对这两个区间会分别调用一次partition函数。

那这两个区间内剩余的元素个数是要小于n的，所以我们对这两个区间分别执行partition函数，它的时间复杂度也肯定不会超过on这样的一个数量级。好，那经过第二层的quicksort处理之后，我们又可以进一步的呃，确定两个元素的这个最终位置。那接下来第三层的quick s是不是就需要处理零到零二到二，还有四到五和七到七这样的四个字表？同样的，这些子表当中剩余的元素个数，其实是要小于n的，所以对这四个部分分别的进行quicksort，其实时间复杂度肯定也是不会超过on这样的一个数量级。好，

那经过第三层的quick sour t处理之后，又可以进一步的确定四个元素的呃，最终存放位置。好，那最后我们只剩下一个元素，那在经过第四层的quick source处理之后，65这个元素的最终位置也可以得到确认。好，所以可以看到刚才这个例子当中，我们最多需要进行四层的quicksort函数调用，然后每一层的quicksort其实只需要处理。我们剩下的这些还没有确定最终位置的这些元素，而这些没有排好序的元素，它们的数量是要小于n的，因此对这些元素的处理。

总的时间复杂度肯定都不会超过on这样的一个数量级，那每一层的quicksort调用啊，时间复杂度不会超过on，所以总的时间复杂度我们就可以用on乘以递归的层数就可以得到。因此，快速排序算法的时间复杂度和我们的递归深度，递归层数是息息相关的。这是时间复杂度，而空间复杂度其实也和递归层数相关。刚才我们呃，一步一步很详细的演示了这个函数调用站递归工作站的一个状态。在这个函数栈里边，每一层的函数调用我们都需要开辟小片空间来保存。这一层函数调用相关的一些局部变量啊，还有什么返回地址啊之类的这些信息。

所以如果我们呃递归调用的层数深度越深，那么空间复杂度也会相应的越高，那由于每一层的函数调用，我们只需要定义呃固定数量的几个。变量就可以，所以如果用大o表示法来看，那空间复杂度应该呃和递归的层数就是正相关的。因此，如果要确定快速排序算法的空间复杂度，那同样的我们最重要的还是要确定它里边总共发生了几层的递归调用。因此，对于快速排序算法，我们要研究它的时间和空间复杂度，都必须先研究它的递归层数，递归深度有什么样的一个规律。

我们再来看一下，刚才我们给的这个例子，在经过第一层的quicksort排序之后，会得到这样的一个结果，49这个元素把原来的表划分成了左右两个部分。那第二层的quicksort需要处理的就是零到二这个区间和四到七这个区间。那对这两个区间分别进行处理之后，就会得到这样的一个结果，所以接下来第三层的quicksort应该处理的就是。这样的一个范围，那经过第三层的quicksort处理之后，我们又可以进一步的确定四个元素的最终位置。接下来，第四层的quick s呃就只需要处理呃65这个元素。所以可以看到我们每一层的这个quick s，

其实会把当前这一层需要处理的那些子区间。再一次的划分成左右两个部分，然后一直这样逐层的往下，所以其实整个图大家会发现我们画出来它就是一个二叉树。用刚才这种分析思路，最终我们会把n个元素啊，把它统一的组织到一棵二叉树当中。那这个二叉树的层数其实就是对应的递归调用的深度，递归调用的层数，所以快速排序算法，它的递归调用层数。可以有多深呢？这个问题我们就可以把它转化为对二叉树的高度呃上限和下限的一个判断。那这个大家很熟悉了啊n个节点的二叉树，最小高度应该是这么高，

最大高度应该是n这么高。所以对于一个快速排序算法，它最少的递归层数也应该是log 2 n+1这样的一个深度。而最多有可能需要经过n层的递归调用，才可以完成快速排序算法。好，那再回到我们之前给出的这个结论，时间复杂度应该是n。乘以递归层数，那刚才我们说了递归层数最少应该是log 2n这样的一个数量级最大，有可能是n这样的一个数量级，所以快速排序算法的最好时间复杂度和最坏时间复杂度。就可以达到all的log 2n和all的n方这样的一个复杂度，那空间复杂度也是一样的，递归层数最好的情况是log 2n最坏的情况是n。

所以这是最好和最坏的空间复杂度，像我们之前给的这个例子，其实就是比较好的一种情况，我们每一次选中的这个基准元素。都可以比较均匀的把整个表划分成左右两个部分，左右两个部分的元素个数，相对来说是比较平均的。每一层的划分都是这样，所以这样就会导致我们整个递归深度最小，算法效率也最高，我们再来看一个最坏的情况，假设我们原本给的这初始序列，它本来就是有序的。好，那按照快排的规则，

我们呃选择第一个元素作为基准元素，接下来hi指针不断的左移。尝试着找到一个比一更小的元素，但是啊，整个过程都没有发现，所以直到hi和low相遇的时候。我们才可以确定这个元素，它的位置应该是零这个位置，那这是第一层的quicksort处理之后得到的一个结果，也就是说我们的这个基准元素。他把原来的这个表划分成了左右两个部分，但是左边部分只有零个元素，没有元素，而右边部分剩余了七个元素。所以这个基准元素对于整个表的划分是很不均匀的，

好接下来第二层的quicksort就需要对右边的这七个元素进行一个处理。也就是要以二作为基准元素，那么同样的low指向一，然后hi指向七这个位置，那hi指针不断的左移，需要把所有的这些元素都给扫描一遍。才可以确定这个基准元素的存放位置，同样的，这个基准元素对于整个表的划分是很不均匀的，左边什么也没有，右边剩余了很多很多。好，那在这种情况下，我们总共就需要进行八层的quicksort函数调用。所以有n个元素，

那最多可能会进行n层的quicksort呃函数调用，那这就是最坏的情况，每一次我们选中的这个书轴元素。都会把待排序序列划分为很不均匀的两个部分，最终导致递归深度增加，算法效率变低。所以，对于快速排序算法，如果我们给定的这个初始序列，它本来就是有序的，或者是逆序的，那这种情况快排的性能最差。因为每一次的划分都会很不均匀，所以结合刚才这个例子可以知道，如果我们每一次选择的输轴元素，

基准元素都可以把待排序序列划分成比较均匀的两个部分。那么，我们的算法效率是可以得到提升的，所以基于这个思路，我们可以对快速排序算法进行一个小小的优化。比如第一种方法，其实我们可以选择的头部，中部和尾部三个位置的元素，把这三个元素进行一个对比。然后选取一个中间值作为数轴元素。这样可以保证我们每一次的划分会比较均匀好，这是第一种方法，第二种方法，其实我们也可以，比如说随机的在整个大排序呃序列当中选取一个元素作为输者元素。

那既然是随机选取的，那从概率上来说，我们就不太可能每一次都选到最小的或者最大的元素，所以这种方式也可以保证我们的划分呃要更均匀一些。即便遇到完全有序或者完全呃逆序的这种情况，也可以得到一个不错的算法，效率好，这是两个小小的优化思路。好，我们再对这个算法的时间和空间效率进行一个总结啊，对于快速排序算法来说，最好的情况就是每一次我们选中的输轴元素。都可以比较均匀的把大排序序列呃划分成左右两个部分，是要比较均匀的划分，在这种情况下递归层数只会达到log 2n。

这样的一个数量级，而如果序列原本就是有序的或者逆序的，那这种情况下对于一个没有优化过的快速排序算法，我们的时间和空间复杂度就会达到最高。此时递归层数也最大，可以达到n层递归，这是最好和最坏两种情况。不过，在实际应用当中快速排序算法的平均时间复杂度，其实是要接近于这种最好的时间复杂度而。而不是接近最坏，所以在实际应用当中，快排这个算法是在所有内部排序当中平均性能最优秀的一个算法。好的，那最后我们再来看一下这个算法的稳定性如何？

来看这样的一个例子呃，只有三个元素，以零号元素作为输轴，那此时hi所指的元素要小于二。所以需要把它移动到low这个位置，接下来low指针右移，那由于当前low所指的元素和输轴元素是相等的，所以这个元素不需要移动。那low会继续游移，此时low和hi同时指向二，所以原本的这个二，我们会把它放到这个位置。好，所以到这一步带下划线的这个二，它的最终位置就已经确定了，

那整个呃快排运行结束之后，这几个元素的排序应该是。啊，这样的可以看到原本这个带下划线的r，它本来应该是靠左边，但是现在它跑到右边这个位置，所以这个算法是不稳定的。好的，那这个小节当中我们学习了快速排序，这个算法那这个算法它的时间和空间的效率表现主要取决于递归调用的深度。而递归调用的深度，又取决于我们每一次进行划分到底均不均匀，每一次的划分越均匀，那么递归调用深度也会越低，相应的算法效率也可以越高。

那这个算法的最好时间复杂度可以达到n×log 2n这样的一个数量级啊，不好意思，这个地方好像打错了，把这两个地方。把最好和最坏呃写反了，但实际应用当中，平均来看这个算法的呃，实际效率都是要接近最好的，这种状态也就是n×log 2n。因此，快速排序算法是所有内部排序算法当中，可以说是应用的最广泛的。那既然这个算法在实际应用当中经常被使用到。显然，我们的考试肯定也会重点的考察，

这样的算法。所以关于这个算法的呃代码，大家一定一定要自己能够完完全全的手写出来，所以我们在这个小节当中对这个算法的代码讲解也会比较详细。好，最后再给大家提一个问题啊，可能会比较有歧义，就是我们王道书上对于快速排序算法。所谓的一趟排序和一次划分，是画了等号的一次划分，等于一趟排序。但是大家在做课后习题的时候，会发现我们四零八的原题里边。会告诉我们，所谓的一趟排序，

指的是对所有的尚未确定最终位置的元素。进行一遍处理，这样的处理才叫一趟排序，所以按照四零八原题里给的这种定义啊，一次划分和一趟排序其实并不相同。如果用我们之前给的例子来看，四零八的原题里边呃给的关于一趟排序的定义，其实指的是我们这的一层的处理。你看我们第二层的处理，其实可以确定27和76这样两个元素的位置，因为整个第二层的quicksort。才是对我们还没有排好序的所有元素进行了一次完整的处理，都给他们扫了一遍。所以按照四零八里边对于所谓一趟排序的一个定义来看，每一趟的排序可能可以确定多个元素的最终位置。

而一次的划分，其实只是对一个连续的区间，对这几个元素调用一次partition。所以一次划分只能确定一个元素的最终位置，而一趟排序可能可以确定多个元素的最终位置。好，所以这个地方可能是大家在做课后习题的时候会比较疑惑的一个点啊，这儿先给大家打一个预防针，那虽然很多教材当中都认为一次划分就是一趟。呃，快速排序，但如果考四零八的同学，那我们最好还是以四零八真题给的这种呃，一趟排序的定义为准。好的，

那以上就是这一小节的全部内容。
