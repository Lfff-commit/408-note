好的，那从这个小节开始，我们会进入这门课的最后一部分的内容就是外部排序，这个小节当中，我们会给大家介绍一下啊，外存和内存之间的数据是怎么交换的？只有理解了这个原理，才可以理解外部排序和内部排序的本质区别，然后接下来我们会介绍外部排序的实现原理，然后来分析影响外部排序效率的一个因素。最后，会根据我们呃分析得到的这些结果来提出一些对外部排序的优化思路。好，首先来看第一部分的内容外存和内存之间是怎么交换数据的。那我们这儿的外存特指磁盘就是所谓的机械硬盘，

那磁盘这种设备啊，有一个特点就是这里边存储数据的那些存储单元是以。所备的磁盘块为啊单位的操作系统，也是以磁盘块为单位对这些磁盘的存储空间进行管理，比如说这样的一个磁盘块，可能它的大小是1 KB或者4 KB之类的。然后在这些磁盘块里边可以存放各种各样的数据，比如说磁盘块四和磁盘块11里边存放了这样的呃几个数据。好，那现在问题来了，如果你要修改你磁盘里边存储的这些数据应该怎么办呢？我们要修改这些数据必须做的事情是需要把对应的磁盘块。读到内存里边，也就是说我们需要在内存里边申请开辟一片所谓的缓冲区，那这样的一个缓冲区的大小可以和一个块的大小是保持一致的。

都是1 KB，然后接下来我们就可以把，比如说磁盘块四里边的这些数据给读到呃内存当中，也就是这个样子。那我们磁盘的读写都是以这种快为单位来进行的，也就说我们每次是读一块写一块。好，现在数据已经读入内存了，那接下来我们就可以用我们的程序代码对内存里的这些数据进行修改，比如说把这些数据修改成这个样子。好，那现在我们只是把内存里的这些数据修改了，如果要更改磁盘块的数据，那我们还需要把这些数据。写回磁盘，

同样的也是以快为单位进行写操作的，所以我们可以把这一整块1 KB的内容写回磁盘块四，把原先的数据给覆盖。当然，也可以写到其他的磁盘块，比如说写到磁盘块11把11这儿的数据给覆盖，那这就是外存和内存之间数据交换的一个原理。我们每一次是以块为单位进行读写的，好理解了这个原理之后，我们再来看什么是外部排序。那所谓的外部排序，是指我们的这些数据元素，它是存放在外存，存放在磁盘当中的。由于磁盘的容量很大，

而内存的容量很小，所以很多时候我们可能没有办法把所有磁盘中的这些数据都给读入内存，内存存不下这么多。所以我们要对存在外存中的这些数据进行排序，那这就是所谓的外部排序。好，现在来看一下外部排序应该怎么实现？其实实现外部排序的一个呃，基本的思想还是要基于我们之前学习的归并排序。采用这种方式，我们最少只需要在内存里边分配三块大小的缓冲区，就可以完成对任意大小的文件进行排序。也就是说，这我们在内存里边申请了一块两块三块，这三块缓冲区的大小都和我们一个磁盘块的大小是保持一致的。

也就是说，如果磁盘块是1 KB，那这三块缓冲区的大小也都是1 KB？对了，在这个例子当中，我们为了演示起来，方便一些，整个文件呃，总共只包含了十。16块的数据。这儿一个绿色的部分就是对应一块，总共有16块，另外每一个磁盘块当中会包含三个记录呃，那这儿我们给出的这个数字就是每一个记录的关键字。现在我们要对这整个文件里边所有的这些记录用归并排序的方式，

把它变成一个递增的序列。那还记得归并排序怎么做吗？我们每一趟的归并是要把两个已经有序的子序列合并成一个更长的子序列。所以在归并排序开始之前，我们需要构造一些已经有序的子序列好，可以这么做，我们内存里边现在已经有这样的两片输入缓冲区，可以存放两块的内容。那我们可以把第一块和第二块的内容先给读入内存，那这些数据只要被我们读入内存之后，你想怎么蹂躏它都可以。所以我们可以对这些数据进行一个内部排序，变成这个样子，就你把这两块的内容连起来看呃，第一块的内容到第二块的内容。

其实现在已经变成了一个递增的一个状态好，现在我们可以先把输入缓冲区一的内容先放到输出缓冲区当中。然后通过输出缓冲区，再把这些数据写回磁盘，那这些数据的大小刚好是和磁盘的一个块的大小是一样的。好，所以把八九二十六写回磁盘的第一个块，接下来把输入缓冲区二当中的这些数据放到输出缓冲区，然后写到第二个磁盘块当中。好，那现在第一个磁盘块和第二个磁盘块里边的这些记录是不是就变成了一个递增有序的状态？之后我们就可以用这个有序的子序列来进行归并排序，那我们可以把这样的子序列称为归并段。好，接下来我们读入后边两块的内容，

同样的，这些记录读入内存之后，我们就可以随便蹂躏它，可以在内存里边对它们进行一个内部排序。然后同样的把这两块的内容再给写回磁盘，那这样的话，我们又得到了第二个呃有序的子序列好，那后边的这些部分是不是都一样的，每一次我们可以读入两块的内容。然后把这两块里边包含的这些记录在内存里边进行内部排序，变为有序之后再给写回磁盘。因此，用刚才说的这种方式，我们就可以得到八个呃初始的归并段。之后我们可以用这八个归并段来进行归并排序，

由于整个文件它总共有16块的内容。那从刚才我们构造归并段的这个演示当中，大家可以看到，其实每一块的内容都会被读入内存一次。那在内存里边排好序之后，还会把呃对应的块再给写回外存，所以总共有16块的数据，那么这16块都需要分别被读一次，被写一次。所以在这个例子当中，我们构造初始的归并段总共是呃，发生了16×2，也就是32次的读写磁盘的操作。好，这是外部排序的第一个步骤，

我们需要先构造呃，这样的一些已经有序的初始归并段，接下来我们就可以使用这些初始的归并段来进行归并排序。好，现在我们进行第一趟的归并，接下来我们会把这给出的两个归并段进行二路归并。我们会先把归并段一和归并段二当中的呃，更小的这个块给读入内存，分别放到缓冲区一和缓冲区二当中。那接下来对这两份数据的归并，是不是就变成了内部的归并排序？那对比两个归并段的呃，最小关键字挑出一个更小的，先把它放到输出缓冲区。然后接下来再挑出一个更小的关键字，

那显然是八，所以把它放到输出缓冲区。好之后还没有归并的这些记录，当中最小的应该是九把它放到输出缓冲区，还记得吗？我们输出缓冲区的大小和磁盘块的大小是相等的，是1 KB。而我们读写磁盘，每一次只能读1 KB和写1 KB，那我们凑足1 KB之后，是不是就可以把这些数据给写回外存了？好，所以现在我们可以把这一块的内容给写回外存啊，写到这样的一个部分好，现在输出缓冲区已经清空了，

那我们就可以往里边继续写数据。同样的，还是使用归并排序。那挑出最小的关键字应该是25，接下来是26，好到这一步，我们输入缓冲区一当中的这些记录是不是已经全部归并完了？那当这个缓冲区空了之后，我们就需要立即用归并段一的后一个磁盘块里的内容来填补输入缓冲区一。也就是这个样子。因为这么做，可以保证我们的输入缓冲区一当中永远是包含了呃归并段一里边，此时暂时还没有被归并，但是数值最小的一个记录。好，

下面继续让两个归并段进行归并，那接下来更小的一个关键字应该是27把它写到输出缓冲区，然后现在输出缓冲区又满了，凑足了一块的内容就可以写回外存。那现在输入缓冲区二里边已经空了，那和之前类似，我们需要立即用归并段二里边的下一块的内容来填补这个空缺。好，接下来继续进行内部归并，挑出三个最小的记录，凑足一个块写回外存，接下来还剩下三个块把它们归并为一个呃有序的。整体，然后写回外存好，那到此为止，

我们的两个归并段是不是就归并为了一个更长更大的段？那为了美观一些，我们把这些数据给它挪到它以前的这个位置好，接下来我们就可以用和之前类似的方法，把后面的两个归并段给归并成一整个更长的有序序列。方法和之前是一样的，所以这就不再赘述，那之后剩余的这些部分都是一样的，我们都可以用类似的方法把它们二合一。那这样的话，经过第一趟的归并之后，我们把八个初始的归并段把它们归并成了四个。一二三四现在总共有四个。好，接下来我们可以进行第二趟的归并，

把这四个归并段分别进行两两归并。好，我们先来归并前边的两个。那这两个段归并之后，我们会把它写到外存的啊，另外一片空间当中，归并的方法和之前是一样的，我们先把归并段一和归并段二里边更小的两个块。分别读入内存放到输入缓冲区一和输入缓冲区二当中，接下来用归并排序的方法挑选出三个最小的记录。凑足一整块之后，把它写回外存好，接下来更小的两个关键字是八和九，那么我们先把它放到输出缓冲区，此时输入缓冲区已经空了。

那按照我们之前的说法，我们需要把归并段一当中的下一块内容给读入输入缓冲区一当中。也就是这个样子，然后接下来我们才可以继续往后归并。可能有同学会觉得奇怪，你把这一块的内容读入之后，那接下来放到输出缓冲区里的不也是13吗？13步也是更小吗？那你干脆把13先放进去，然后两块一起读入，是不是也OK呢啊？不对，这种想法是不对的。比如来看这样的一种情况，刚才我们读入的这三个记录是二五二六二七，

如果我们读入的第一个元素不是25。而是比如说是十，那是不是接下来我们要挑选的元素就是十这个元素，而不是13这个元素，所以如果刚才你没有先把这一块的内容给读入内存的话。那在这种情况下，接下来的归并是不是就会出现问题好，所以再次强调，每当一个输入缓冲区空了的时候，我们都需要立即把与之对应的那个归并段。下一块的内容给读住内存，然后才可以接着往下归并。好，我们继续接下来放到输出缓冲区的，应该是13凑足了一块之后写回外存，

那缓冲区二空了，我们再从归并段二当中读入下一块的内容。再继续进行归并，然后写回外存缓存区二空了，那再次读入，然后再次归并。写回外存缓冲区，移空了读入。归并。写回外存，然后继续归并，缓冲区一空了，就读入归并段一的下一块内容，然后继续归并。写回外存。

读入归并。写回外存，继续归并。写回外存，好到此为止。我们是不是把两个归并段，把它们归并成了一个更长更长的归并段？好，同样的，为了美观，我们把这部分的图像把它挪到下面来啊，不过大家需要知道啊，我们归并之后的这个更长的子序列其实是放在磁盘的另一片空间当中的。以前的这两片空间，我们会归还给系统，

这我们只是为了美观，所以会把上边这部分的内容给挪到下面来，看起来好像是同一片空间，但是逻辑上并不是这样。大家知道是什么意思就行。好，现在我们还剩余下面的两个归并段，那归并的方法是不是和之前是一样的，用同样的方法可以把它们归并为一个更长的归并段？里边的这些关键字是递增的，是有序的。好，完成了第二趟归并之后，我们得到了两个呃更长的有序的归并段，那接下来我们再进行第三趟的归并，

只需要再把这两个呃归并段给归并就可以了。归并的方法其实和之前都是一模一样的，只不过这儿每一个归并段的长度变长了而已好，所以这儿就不再赘述，总之经过第三趟的归并之后，这一整个文件就可以。得到一个整体有序的排列。好来回顾一下我们进行外部排序，刚开始是呃，先把原始的完全乱序的那些数据生成一个初始的归并段，也就是生成一小段一小段的呃，内部有序的。子序列。在我们的例子当中，文件总共有16块的数据，

而这16块的数据都需要被读写一次。另外，把数据读入内存之后，还需要进行内部排序。那经过这样的处理，我们是得到了八个呃初始归并段，每个初始归并段占两块的大小。那这是因为我们在这个例子当中只分配了两块大小的输入缓冲区，所以我们每次只能读入两块的内容，对他们进行内部排序。如果分配的内存缓冲区更大的话，那我们呃得到的这个初始归并段的长度相应的也可以变得更长。好，总之在我们的例子当中，我们会生成八个初始的归并段，

然后接下来我们会进行三趟的归并。把两个有序的归并段归并成一个更长的一个总体，每一趟的归并会基于上一趟得到的结果，再次把两个有序的归并段合并成更长的一个归并段。那经过三趟归并之后，就可以得到一个整体有序的文件。那通过刚才我们对归并过程的演示，不难发现，我们每一趟的归并，其实都需要把16块的数据都分别给读入内存。然后归并完了之后再写回外存，所以每一趟的归并这个读写磁盘快的次数都是16次，当然了，这些数据读入内存之后，我们还需要在呃内存的内部。

进行一个内部归并好，所以我们外部排序的呃，时间开销大致上可以分为这样的几个部分，第一个部分也是占大头的部分。读写外存的时间，这部分的时间是占很大比例的，然后第二个部分就是呃内部排序所需要的时间，也就是生成初始归并段的时候。我们需要使用某一种内部排序算法来生成一个初始的有序序列，那最后在每一次归并的时候，我们把呃数据读入内存了，我们还需要对这些数据进行一个内部归并。所以外部排序的时间开销可以由这样的三个部分来组成好，现在我们先把注意力放在读写外存的时间。其实读写外存的时间和我们读写磁盘的次数基本上是成正比的，

所以通过刚才的分析，我们知道我们整个过程。总共需要读写磁盘128次，那如果每一次读磁盘或者写磁盘都需要十毫秒的时间的话。那整体来看，所需要的读写磁盘时间就是1280毫秒，也就是一点二八秒。对于计算机来说，内部排序和内部归并，由于是在内存里边来处理这些数据，那这个速度是会很快的，肯定达不到秒这样的一个级别。好，所以可以看到读写外存的时间是占很大一部分的，那刚才我们这给出的32，

其实刚好就是文件的总块数乘以二。因为我们刚才说的这几个阶段，每一个阶段都需要把呃文件的各个块给读入一遍，然后再写出一遍。所以读写次数加起来就应该是32，那这标红的这个三指的是我们归并的趟数。显然，文件的总块数到底有多少啊？我们没办法改变，也就是32这个基数肯定就确定了，但是归并的趟数其实我们可以想办法让它缩小。那只要归并的趟数缩小了，我们读写磁盘的总次数是不是就可以下降？相应的，外部排序的时间开销也可以下降，

所以这是我们接下来会优化的一个思路。怎么减少归并的趟数？好，所以接下来我们会进入下一个话题，怎么对外部排序进行优化？刚才我们说了，如果我们能够减少归并的趟数，那么我们相应的也可以减少读写外存的时间。那怎么做呢？我们可以用多路归并，之前我们是用了二路归并，那现在来看一下，如果我们采用四路归并。那相应的，要进行四路归并，

我们就需要在内存里边分配四个输入缓冲区，然后把四个归并段的内容分别读入这四个缓冲区当中。接下来归并的原理其实是一样的，每一次从这四个归并段当中挑出一个最小的元素，那刚开始最小的显然是一，接下来是二。接下来是七凑足一整块之后，把它写回外存。接下来更小的是八九十三，凑足一整块写回外存好，那后续的过程我们就不再赘述。其实和二路归并是一样的原理。同样需要注意的是，每当一个缓冲区空了的时候，我们就需要把这个缓冲区所对应的归并段的下一个块给读入内存。

只需要注意这一点就可以了，好，总之我们进行四路归并之后，可以把这四个归并段进行一个四合一的操作，合并成一个更长的整体。那下边这四个归并段也是一样的，我们用四路归并，可以把它们合并成一个整体，所以在第一趟归并之后，我们整个文件就只剩下两个归并段了。好，那接下来我们再对这两个归并段进行一次二路归并，是不是就可以得到一个整体有序的文件？好，因此如果我们采用这种四路归并的话。

那我们的归并趟数就只需要两趟，第一趟的归并可以把四个归并段合并成一个，然后第二趟的归并可以把这两个呃比较长的归并段。归并为一个整体，所以总共就只需要进行两趟的归并。那在这种情况下，我们读写磁盘的次数就变成了只需要96次，之前是32+32×3嘛。现在不是乘三，是乘二，只需要进行两趟，所以我们采用多路归并的这种方式，增加归并的路数，那我们就可以减少归并的趟数。归并的趟数越少，

那读写磁盘的次数相应的也会变少，那这样就可以减少外部排序的总的时间开销。好，那如果说我们有r个初始归并段，有r个初始归并段，那如果我们采用k路归并的策略的话啊，我们的归并数就可以用k差数来表示。那对于一棵k叉树dh层，最多会有k的h- 1个节点，而我们的这二个初始归并段都需要在这个k叉树的呃最底层，也就是dh层。所以需要满足这样的一个条件，就是r小于等于k的h- 1，因为第h层最多只能有这么多个节点嘛，那解这个不等式就可以得到h- 1。

最小值有可能取得这样的一个值log，以k为底r的对数向上取整。那我们得到了h- 1，有可能得到的最小值，而刚好我们的归并趟数和h- 1是一样的。所以这个值也代表了呃，进行k路归并的时候，有可能得到的最小的归并趟数。那既然归并趟数等于这样的一个值，那根据对数的一个性质，我们可以知道k越大肯定这个呃归并的趟数会越少。也就意味着读写磁盘的次数会减少，另一个方面，如果我们能让r，也就是初始归并段的数量。

变少的话，那么归并的趟数也可以减少。所以我们要怎么对外部排序进行一个优化呢？第一种优化思路就是让归并的路数k啊变得大一些。第二个优化的思路就是让初始归并段的数量变少一些，不过这儿也需要强调，并不是归并的路数k呃越大越好。因为归并路数越多，其实也会带来一些负面影响，比如说啊，我们进行k路归并的时候，根据刚才我们的讲解，我们知道肯定需要开辟k个输入缓冲区。那这意味着，内存开销的增加。

另一个方面，我们从k个归并段当中挑出一个最小的或者最大的关键字，那按照之前我们提出的方法，每挑一个元素，我们需要对比关键字k- 1次才可以。所以归并的路数k增大的话，那么我们在进行内部归并的时候，每挑选一个元素，所需要对比关键字的次数会增加，相应的也会导致内部归并所需要的时间增加。所以如果归并的路数k太大，其实也不行，当然了，这个问题其实可以得到解决，我们在下个小节当中会学习一个东西，

叫做败者数。呃，通过败者数的方式，我们可以减少在k路归并的时候这种关键字的对比次数。好，总之这个地方想强调的是呃k越大，那么读写磁盘的次数会越少，相应的外部排序时间开销也会越少，但是也并不是k可以无限的增大。这个不能一概而论好，这是关于k接下来我们来看怎么减少r，也就是怎么减少初始归并段的数量。其实刚才我们在进行四路归并的时候，我们不是分配了四个输入缓冲区吗？那既然有四个输入缓冲区，

对于刚开始一个无序的文件。我们完全可以直接读入四块的内容，然后把这些记录在内存里边进行一个内部排序。再分别写回外存，这样的话，我们得到的初始归并段其实就包含了四块的内容。用这种方式构造初始归并段，就只会有四个，所以我们生成初始归并段的这个内存工作区，这片空间越大，那么我们可以生成的初始归并段的长度也会越长。那每一个归并段越长，是不是就意味着归并段的总数r就会越少，所以外部排序的整体效率就可以得到提升？好，

那在这种情况下，我们接下来是不是只需要对这四个初始归并段？进行一趟的四路归并就可以得到一个整体的有序文件，因此这个地方我们得出的结论是。如果我们能够增加初始归并段的长度，那么相应的初始归并段的数量r就可以减少。而r越小，我们读写磁盘的次数越少。好的，那这个小节当中，我们带大家初步的认识了什么是外部排序？需要分为这样的两大步骤，首先我们需要呃生成r个初始归并段，然后接下来我们需要进行s趟的k路归并，那这个s等于这样的一个值怎么算呢？

刚才已经说过。那进行k路归并的过程，其实和我们之前学习的呃归并算法并没有任何区别，只不过我们要归并的那些数据并不是一次全部读入内存的，而是分块读入的。仅此而已，那这个小节的最后我们也花了比较大的篇幅来介绍怎么优化外部排序，减少时间开销。大致上可以分为这样的两个思路，第一个思路就是可以用k路归并，就是增加归并的路数k。k越大那归并的趟数就会越少，相应的读写磁盘的次数也会减少，不过当归并路数太大的时候也会造成呃两个问题，第一个问题是。

内存的开销会增加另一个问题是，当我们要选出一个最小或者最大的元素的时候，我们需要进行k- 1次的关键字对比。那下个小节当中，我们会介绍败者数，用败者数来优化这个问题，可以减少在k路归并的时候，关键字的对比次数。好，这是第一个优化思路，第二个优化思路是可以减少归并段的数量r那这一小节我们介绍的这种生成初始归并段的方法。如果有n个记录，然后内存工作区也就输入缓冲区，总共可以容纳l个记录的话，那最终我们生成的初始归并段的数量r是等于n÷l。

也就是说，采用这个小结介绍的这种方法来生成初始归并段，那我们的呃归并段的数量r完全是取决于内存工作区到底有多大。那在之后的小节当中，我们会介绍一种叫做置换选择排序的算法，可以突破这个限制，我们可以用置换选择排序来生成更长的归并段，从而就可以减少归并段的数量r。那败者数和置换选择排序，就是接下来的两个小节需要学习的内容。好的，那以上就是这个小节的全部内容。啊对了，还没有结束啊，再跟大家说一个小问题，

就我们课本上对于所谓多路平衡归并的这个说法是有一些问题的。呃，课本上说对于r个初始归并段，我们做k路呃，平衡归并，那么归并数可以用严格的k差数来表示，也就是说这个数里边。只有度为k或者度为零的节点。但是大家又会发现，我们课本里边给的七点八这个图，它把这个呃归并的过程也称为了四路平衡归并。那是不是和课本里给的这个描述是有矛盾的，因为你看这个根结点，它的度是二而不是四。所以其实课本上对于这个所谓k路平衡归并嗯的一个定义是有问题的。

我们把这个问题纠正一下，所谓k路平衡归并呃，它需要满足这样的两个特性，第一就是最多只能有k个归并段，归并为一个。第二，就是在每一趟归并当中，如果说有m个归并段参与归并，那么在经过这一趟的处理之后。应该是得到m÷k向上取整这么多个新的归并段，如果不满足这个条件，那么就不能够称为k路平衡归并。来看一个反例啊，比如说这个例子，你看这个地方用了四路归并，

所以上边给的这个例子，我们把它称为四路归并排序，那是正确的。但是它并不是四路平衡，归并排序，为什么呢？因为我们第一趟的归并当中总共是有八个归并段呃，参与归并。但是在归并之后，得到了三个归并段。那对于四路归并来说，如果这一趟有八个归并段，参与归并的话，那么最终得到的新的归并段应该是八÷4向上取整。也就是说，

只能生成两个归并段，而这个地方，它生成了三个归并段，那这个归并，我们就不能把它称之为四路平衡归并。那再回头来看我们课本里给的这个例子，低碳硅并总共有八个归并段处理，那经过处理之后生成的归并段数量是两个。是满足四路平衡归并的这个要求的，然后第二趟的归并有两个归并段参与归并，为了一个那你代进去算一下二÷4，然后向上取整等于一，其实也是满足这个四路平衡。归并的一个要求，所以我们课本里给的这个图，

七点八的这种归并确实可以把它称为四路平衡归并，而我们刚才给的这个例子就不是四路平衡归并。好，所以这就是所谓的多路平衡归并好的，那以上真的就是这个小节的全部内容。
