各位同学大家好，在这个小节中我们要学习数据结构这门课的最后一个考点，最佳归并数，那我们首先来看一下之前我们学习的归并数，它隐藏的一个神秘的性质。通过上一小节的学习，我们知道，如果我们使用置换选择排序来构造初始的归并段的话，那么这些初始归并段的长度可能是各不相同的，那我们这儿给出了五个初始的归并段。这里边的数字什么五二一这些表示的是每个归并段，它总共占多少个磁盘块？像r2这个归并段，它就是占五个磁盘块r4是占六个磁盘块好，那来看一下，

我们现在要对这五个初始归并段。使用二路归并的方式，把它们两两合并啊，首先我们可以合并r2和r3，这两个归并段，那它们俩的长度一个是五，一个是一。所以我们要在内存里边对它们进行归并，那肯定需要把r2这五块的内容给读入内存。也需要把r3这一块的内容读入内存，而之前我们说过，我们读写磁盘是以磁盘块为单位的，所以把这六块的数据读入内存，需要读磁盘六次。那在内存中，

我们会把这两个归并段把它们合二为一，最终会生成一个总共占六块的归并段，那把这个归并段写回磁盘需要六次的写操作。所以我们把r2和r3进行一个二路归并，总共需要读六次写六次。好，接下来我们归并r4和r5那原理和之前类似r4这个归并段的六块数据，还有r5的两块数据，把这八块的内容读入内存，总共需要读八次。那在内存中归并，完了之后再把这些数据写回磁盘，又需要再写八次，所以r4和r5的归并需要读写各八次。好，

接下来我们可以把刚才得到的这两个比较长的归并段再次的进行归并，那其中一个归并段占六块，另一个归并段占八块，那我们把这些数据读入内存，总共就需要读磁盘14。四次那归并完了之后，把这些数据写回磁盘，又需要写14次好，最后我们再把r1这个归并段和刚才得到的这个比较长的归并段进行一个归并。那么，这次的归并需要读和写分别进行16次，所以按照这样的策略进行归并，那我们读磁盘的次数应该是六+8+14。再加16，总共需要读这么多次，

写磁盘也是一样的，需要写这么多次好，现在我们来做这样的一些事情，我们把这些绿色的节点，也就是这几个归并段。把它们看作是这棵二叉树的啊，五个叶子节点。那我们可以算一下这棵树的带全路径长度等于44和上边我们计算的这个结果是相同的，也就是说这棵归并树的带全路径长度。其实和他的读磁盘次数和写磁盘次数刚好相等。对了，可能有同学忘了树的带全路径长度怎么算？那如果忘了这个内容，那可以回去看一下哈夫曼树那个小节。这简单的说一下r1这个叶子节点，

它的权值是二，然后从这个节点到根节点的路径长度是一，所以r1这个节点的带全路径长度应该是二×1，也就是全值乘以。它到根结点的距离，然后剩下的r2到r5这四个叶子节点，它们到根结点的路径长度都是三。那这几个节点的权值分别是五一六二，所以这么一乘就可以得到这几个节点的带全路径长度之和，那么整体一加和就可以得到这棵树的带全路径长度。所以如果要算读写的总次数，也就是磁盘的IO次数的话，那就应该是88次，总共这么多，所以这地方我们得出的一个重要结论就是。

再把这些初始归并段归并的过程当中，磁盘IO的次数应该是等于归并数的带权路径长度乘以。二。那基于这个结论，不难想到，如果我们想要追求归并的过程，磁盘IO总次数最少，那么其实这个问题我们就是要求一颗带全路径长度。最小的归并数，那带权路径长度最小的数不就是哈夫曼数吗？这是我们在第五章学习过的内容。好，所以接下来我们会构造一棵哈夫曼树来优化这五个初始归并段的二路归并的策略。好，还记得哈夫曼树怎么构造吗？

刚开始我们会把这些节点看作是一个一个独立的树，只不过每一棵树只有一个根节点而已。那接下来我们会从这几棵树当中挑选出根结点的权值最小的两棵树，让他们成为兄弟。那刚开始，权值最小的是一和二这两个。那我们让他俩成为兄弟，然后新的这个根结点的权值是这两个孩子结点的权值之和一+2=3。好，现在还剩下四棵树根结点最小的两棵树，应该是三和二这两个，所以让他们俩成为兄弟。这根节点的权值应该是三+2=5好，接下来更小的应该是五和五，这两个让他们俩结合新的根节点的权值应该是五+5=10。

最后让这两个节点结合，连上一个新的根节点，全值是16好，那这棵归并树背后的含义就是说我们刚开始应该是先把r1和r3这两个初始归并段。归并成一个总长度为三的新的归并段，然后接下来用长度为三的这个归并段和r5这个归并段进行一个归并。得到一个长度为五的，再接下来用长度为五的和r2这个段进行归并，得到一个长度为十的，最后用长度为十的归并段和r4进行归并。得到一个总体长度有16块的有序文件，那这棵树就是一个哈夫曼树，我们计算它的带群路径长度可以得到34。也就是说，用我们这儿给出的这种归并方案，

总共只需要读磁盘34次，写磁盘34次，总的磁盘IO次数只需要68次。而我们之前提出的第一个方案，按照这种方式来归并总共的IO次数可以达到88次这么多。所以这棵树就是一棵最佳归并树，我们按这样的方案来进行二路归并，可以得到最少的磁盘IO次数。好，那这是二路归并的情况，接下我们再来看一下多路归并的情况，假设现在有这样的几个初始归并段，那和之前一样，每一个小圆点里边的数字表示的是这个归并段，它总共占多少？

多少个磁盘块？那假设现在我们要采用三路归并的一个策略，按照我们之前的方法啊，进行三路归并应该是这么操作的。那可以算一下这棵树的带全路径，长度应该是二四二这么多，也就是说按照这样的方案来进行三路归并的话，那我们总共需要242次的读磁盘和242次的写磁盘。总的磁盘IO次数就应该是四八四这么多次好，这是我们之前学习的方法，显然这并不是一个最佳的归并数。那三路归并的最佳归并数应该怎么构造呢？其实原理和二路归并是非常类似的，我们会选出权值最小的三个节点，让他们成为兄弟。

那最小的显然是二三六，这几个好，我们把这几个段进行归并，得到一个总长度为11的新的归并段。接下来，权值最小的三个应该是92，然后就是这个11，所以下一次的归并应该是让这三个段进行一个归并。得到一个长度为32的新的段好，再往后全值最小的三个应该是17，18，24。所以我们让这三个段进行归并。好，最后只剩下这样的三棵树，

我们再把它们进行一次三路归并，最终得到一个总长度为121块的，一个有序的。文件。那用这样的方法得到的一棵三路归并的归并树，它就是一棵最佳归并树，我们可以算一下它的带全路径，长度应该是二二三。也就是说，按照这样的方法来进行归并排序，那么我们总共只需要读写磁盘446次。啊，对了，同学们，可能现在还没有学过操作系统，

这个地方所谓的磁盘ioi指的是input就是输入。然后o指的是output，也就是输出，其实对应的就是读操作和写操作。因此这儿我们说磁盘IO次数其实是包含了读，也包含了写。好，这个点好像之前忘了和大家解释好，总之我们每一次会选出三个根结点的权值最小的数，让他们成为兄弟。然后新的根结点的权值应该是它的三个孩子结点的权值之和我们一直重复这样的一个过程就可以得到一个三叉的哈夫曼数。它的带圈路径长度最小，所以它就是一棵最佳归并树。是不是和二叉的哈夫曼数构造呃，没有太大的区别好，

接下来问题来了，我们刚才给了这样的几个初始段。现在假如我们减少一个归并段，比如我们把长度为30的这个归并段给干掉，也就是说我们总共只有八个归并段来参与这个三路的归并。那按照之前我们构造三叉哈夫曼树的规则，刚开始全职最小的三个应该是二三六，那把它们三个归并。接下来最小的应该是90，21这几个把它们归并，再接下来最小的应该是17，二四十八。把它们三个进行归并好，现在我们总共只剩下两棵树，那我们要把这两棵树归并，

肯定就只能进行二路的归并。也就是这个样子。最终，我们得到的有序文件总长度应该是有91个次方块好，我们来算一下这棵树的带全路径长度应该是幺九三。那么，按照这样的策略来进行归并，总共的磁盘IO次数就应该是三八六这么多次，那这儿要跟大家强调的是，我们刚才的这种构造方法是不对的。问题出在我们最后的这一次归并，大家会发现，其实只是两路的归并，而不是三路的归并。而如果初始归并段能够更多一个，

那刚好就可以在每一次归并的时候都保证是三路归并。好，所以正确的做法应该是这样的。那我们把这个问题广义化，对于k叉归并来说，如果初始的这些归并段的数量无法构成一个严格的k叉归并数，那么我们需要补充几个长度为零的。虚段那像刚才这个例子当中，我们只需要补充一个虚段就可以了。那这个长度为零的归并段，其实它并不存在，我们加上它只是呃，为了能够让这些节点的。个数能够刚好凑出一个严格的k叉归并数好，那来看一下我们加入了这个节点之后，

刚开始全值最小的三个节点就应该是零二三这几个。那我们把他们三个先进行归并，新的根结点权值为五二+3+0=5好，接下来权值最小的三个应该是五。六和九那我们把他们三个进行归并好，再往后权值最小的应该是十二十七和18，那我们把他们三个进行归并。最后我们刚好还剩下三棵树，那把这三棵树再进行一次三路归并就可以得到一个总长度为91的有序文件。那我们再来算一下这棵归并树的带旋路径，长度等于163，也就是说总的磁盘IO次数只需要。326次好，那这棵树它就是一个三路归并的最佳归并数，那这个地方再跟大家解释一下这个长度为零的虚段是什么意思？

我们的第一次三路归并应该是二三零，这几个归并段的一个归并还记得吗？进行三路归并的时候，我们需要设置三个输入缓冲区。那二这个归并段的数据会被放入第一个输入缓冲区，然后长度为三块的这个归并段，它的数据是放在第二个缓冲区。然后最后的这个长度为零的序段，其实意味着我们在第三个输入缓冲区，这儿什么也不用放。接下来，我们进行三路归并的时候，其实我们只需要把最后的这个归并段看作是一个已经把所有的元素都归并完了的，归并段就可以了。好，

所以这就是所谓的一个虚段，它背后的一个原理。总之，当我们在进行k路归并的时候，如果k大于二，那么我们就有可能会遇到初始归并段的数量无法构成严格的k叉归并数这样的一个情况。如果遇到这种情况，那我们需要补充几个呃长度为零的虚段，然后再进行k叉哈夫曼树的构造。那到底需要补充几个呢？接下来我们研究一下这个问题好，现在我们是要进行k路归并，那么k路归并的最佳归并数最终一定是一个严格的k差数。所谓严格的意思是说，这个数里边只能包含度为k或者度为零的节点。

那我们设度为k的节点总共有nk这么多个度为零的节点，总共有n0这么多个，然后总结点数等于n这么多个。好，那在这棵归并树当中，我们刚开始给出的初始归并段，还有补充的这些虚段，他们肯定都是叶子节点，也就是说他们都是度为零的节点。因此，初始归并到的数量，加上我们补充的呃虚段的数量，应该刚好是等于n0。好，另外根据k差数的性质，

应该有这样的两个结论，第一个n也就是归并数的总结点，数量应该是等于度为零的结点数，加上度为k的结点数。因为之前说了这个最佳归并数，它是一棵严格的k叉树，它只包含度为k和度为零的节点，所以把这两类节点的数量加和就是这棵树的总结点数量。好，另外还应该满足这样的一个式子。总共有nk这么多个分支节点，而每一个分支节点，它的度都是k，所以每一个分支节点，它会发出k个分叉。

那除了根结点之外，其余的所有结点头上肯定都会连着一个分叉，所以k×nk应该是分叉的总数量，那么n- 1就是呃总的结点数减掉一个根结点。这两个值应该是相等的好，那根据刚才得到的这两个式子，我们就可以推出。n0应该是等于这样的一个值，然后再往后一推，可以得到nk等于这样的一个值，那nk指的是度为k的节点的数量。它肯定是一个整数，所以式子的右边n0-1÷k-1，这个除是肯定刚好可以除得一个整数，也就是能除得尽。

所以按照我们刚才的这个推论，我们的初始归并段的数量，加上我们补充的这个虚段的数量。n0再减掉一，应该是刚好能够除得尽k- 1的。所以如果刚开始给的初始归并段数量减一，再对k- 1取于等于零的话，那么就说明其实我们。不需要再补充虚段，同样也可以构成一棵严格的k叉树。而如果这儿取于运算呃等于u不等于零的话，那么就说明初始归并段的这些数量没办法构成一颗严格的k差数，那我们只需要补充k- 1再减u。这么多个虚段就可以了，因为如果加上这么多个虚段，

那刚好可以让啊，这个值能够除得一个整数。那这是模运算的一个简单的数学问题，这就不再赘述。好，我们简单的把这两个结论进行一个应用，假设我们现在是要进行八路归并，那那如果初始归并段的数量。是等于比如说等于19。那我们要用这19个初始归并段构造一个最佳归并数，需要添加几个虚段呢？来看一下19-1呃18，然后对k-1，也就是对七取于应该是等于。应该是等于四，

那就说明这19个初始归并段是没办法构成一棵严格的呃八叉树的。那我们需要补充几个虚段呢？应该是k- 1，也就是七再减掉u也就减掉四=3。所以我们只需要再补充三个长度为零的虚段，那么这些虚段和初始归并段的总数啊，就应该是19+3=22。那由于22-1，然后再除以k-1，刚好可以除得尽，所以这就说明我们再补上三个虚段之后。就可以构成一棵严格的k叉树。好，那这是对这个结论的一个小小的应用和练习。好的，

那这个小节当中我们学习了什么是最佳归并数，那一颗归并数的带全路径长度，其实刚好就。等于归并过程当中读磁盘和写磁盘的次数。所以我们要让归并的过程读写磁盘次数最少，那其实就是要追求一个啊，带全路径长度最小的归并数。那这就涉及到我们之前学过的哈夫曼数相关的内容。不过和之前我们学习的二叉哈夫曼树的构造不太一样的是，当我们进行k路归并，也就是要构造一个k叉的归并树的时候。我们需要判断一下原始给的这些呃初始归并段的数量是否刚好能够构成一棵严格的k叉树，如果不能的话，那么我们就需要添加相应的。啊，

虚断。然后再来进行k叉哈夫曼数的构造，用这样的方式就可以得到在k路归并的时候一颗最佳的归并数，可以让读写磁盘的次数最少。好的，那以上就是这个小节的全部内容，也是这门课的全部内容。很荣幸能够陪伴大家走过学习数据结构的旅程，期待下一段旅程我们再会，同学们下课。
