各位同学大家好，在这个小节中，我们会介绍磁盘管理相关的几个很简单的知识点，那我们首先会介绍什么是磁盘初始化，磁盘初始化的过程中需要做些什么？另外，我们会介绍引导块，或者说起始块的概念，最后我们会介绍对于磁盘坏块的一个管理，那首先来看一下什么是磁盘初始化。我们的磁盘刚被制造出来的时候，其实是只被划分成了一个一个的磁道，那在磁盘正式出厂之前，还需要进行一个叫做低级格式化，或者叫物理格式化的过程。

这个过程就是呃，一个划分扇区的过程。那一个扇区可以分为头尾，还有数据区域，这样三个部分，那每个扇区的数据区域所能存放的数据数量都是相同的，比如说可以存放512个字节。所以咱们之前所聊到的一个扇区可以存放的数据的大小，其实指的是数据区域可以存放的大小。那像头和尾这两个部分可以做什么呢？还记不记得咱们在讲文件的物理结构的时候讲过一种链式结构，也就是把文件的那些啊数据块。用链接的方式把它连起来，那前一个数据块指向下一个数据块的指针，其实就可以保存在尾部这个部分。

也就是说，这个链接指针并不需要占用数据区域，那这样的话可以方便操作系统管理。另外呢，管理扇区所需要的那些数据结构一般也是放在头和尾的位置，比如说像山区校验码。这些校验码可以用来检查数据，区域当中存放的这些数据是否发生了错误？那这样的话，可以大幅度的提高磁盘存储数据的一个可靠性。总之，一个扇区由头数据区域尾三个部分组成，那扇区的划分其实是在出厂之前啊，低级格式化也就物理格式化的时候进行的。那在我们正式开始使用磁盘之前，

还需要对磁盘进行逻辑上的分区，每个分区呢，由若干个相邻的柱面组成。这所谓的分区，其实就是我们平时熟悉的C盘D盘E盘，那在划分了分区之后，需要进行下一步叫做逻辑格式化。逻辑格式化所做的事情主要就是创建文件系统，包括啊，要创建这些根目录的目录文件。并且要把那些用于存储空间管理的数据结构也进行初始化，比如说像咱们之前学过的位视图空闲分区表等等，也是在逻辑格式化这个步骤当中给建立的。那这是磁盘初始化的时候需要做的几个事情，那接下来我们来看一下什么是磁盘的引导块。

在我们的磁盘完成了呃物理格式化，还有磁盘分区和逻辑格式化之后，我们就可以把操作系统的那些相关数据把它写到这个呃磁盘中了，也就是我。我们所谓的自己安装操作系统的一个过程，那在计算机开机启动的时候，其实是需要进行一系列的初始化工作的，包括初始化CPU，初始化内存。还有初始化呃像寄存器之类的一些硬件部件，但是这个初始化的过程需要执行所谓的初始化程序，也叫自举程序。那一般来说，这个自举程序是存放在ROM，也就是只读存储器当中的，

而ROM中的数据是在出厂的时候就已经写到了这个ROM当中，并且以后就不能再修改。那一般来说ROM是在出厂的时候就直接集成在了我们的电脑主板上。也就是说，计算机开机的时候，它首先会读取ROM当中的这个，这些程序，并且执行这些程序来完成初始化的工作。但是我们来思考一下，我们把自举程序放在ROM当中，会有什么问题呢？假设我们的自举程序需要更新的话，那么就会很不方便，因为ROM当中的数据是无法更改的。但是由于自己程序本身又比较复杂，

所以我们不太可能保证自己程序啊，相关的数据永远不改变，那这个问题怎么解决呢？那现在的操作系统一般是只在ROM当中存放很小的一个字据装入程序。而完整的自举程序会存放在磁盘的启动块，也就是这儿所说的引导块，或者叫启动分区上。比如说存放在C盘的啊，这几个盘块上那么这些区域就可以称作启动分区，或者叫引导块启动块。启动块规定，必须在啊磁盘的固定位置，那当计算机开机的时候，首先会执行ROM中的自举装入程序。在执行这个自己装入程序的过程当中，

嗯CPU就可以知道接下来需要执行的自己程序是存放在硬盘当中的哪个位置的。那通过自己装入程序的引导CPU，就可以从磁盘中读取完整的自己程序。这样的话就可以完成初始化。那自己装入程序的复杂度不高，很小，所以其实是可以保证自己装入程序是不会出错的，不需要更改的。而自己程序需要更改的话，我们就把自己程序放在啊磁盘的固定位置，那这样的话，当自己程序需要更新的时候，就只需要啊重新把这些引导块当中的数据把它。呃，重写一下，

更新一下就可以了。所以这种方案带来的好处就是自己程序的更新会变得很方便，那一般来说，拥有这个启动分区或者叫引导快，启动快的这个逻辑磁盘。就被称为启动磁盘或者叫系统磁盘，那像平时咱们很熟悉的C盘就是系统磁盘。也就是说，其实C盘的某一个固定位置是装了我们的自举程序的，那这是引导块的作用。那接下来我们再来看一下对磁盘的坏块应该进行什么样的管理？所谓的坏块就是指那些坏了的无法正常使用的扇区就是坏块。那这属于一种硬件故障，操作系统是无法通过软件的方式把它呃进行修复的。所以为了防止我们错误的使用这些话块呃，

我们就必须把这些话块给标记出来。在简单的磁盘系统当中，可以在进行逻辑格式化的时候来标明哪些磁盘块是坏块，比如说我们可以把它在fat，也就是文件分配表上进行一个标注。那标记为坏块的这些块之后不再分配给任何一个文件就可以了，那由于操作系统在对存储空间进行管理的时候肯定是需要读取啊，文件分配表的内容的。而哪些块是坏块，是记录在文件分配表当中的，因此采用这种方式的话，坏块对这个操作系统是不透明的。而对于复杂的磁盘系统来说，磁盘控制器这样的一个呃磁盘内部的硬件部件会负责维护一个叫做坏块链表的一个链表。顾名思义，

就是把这些坏块用某种方式把它们链接起来，那在磁盘出厂的时候，其实就有可能已经有一些坏块了。所以在出厂之前，就会对这些坏块进行一个检查。在低级格式化也就物理格式化的时候，就需要把这个画块链进行初始化，另外磁盘控制器还会。保留一些好的备用扇区，用来替换这些话块，比如说此时操作系统想要使用这一个本来已经坏掉的块。那么，在硬件的层次，磁盘控制器这个硬件部件就会用其中的某一个好的，这种备用块来替换这个坏块，

但是这个过程对于操作系统是透明的操作系统。系统是不可知的。那这种方案呢，也被称为扇区备用，那这是对磁盘的坏坏进行管理的两种方法。好的，那么这个小结我们介绍了磁盘的初始化，需要做一些什么事情，分为低级格式化呃，磁盘分区还有逻辑格式化这样三个步骤，大家需要理解，并且记住啊，各个步骤需要做的是一些什么事情？那之后我们介绍什么是引导块，那引导块主要是用于存放自己程序的，

在开机的时候一定需要运行自己程序才可以完成初始化的过程。最后，我们介绍了两种画块的管理方式，那这个小节的内容都不难理解，大家只需要有个印象，能应付选择题就可以了。好的，那么这就是这个小节的全部内容。
