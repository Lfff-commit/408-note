各位同学大家好，从这个小节开始，我们会开始学习这门课的最后一个章节，设备管理那通过之前的学习，我们知道操作系统，它作为系统资源的管理者。既需要对上层的软件进行管理，也需要对下层的硬件进行管理，那在之前的学习当中，我们学习了操作系统，它需要对处理机还有存储器这些硬件进行管理。但是这些硬件其实是在计算机的主机内部的，那这个章节我们要探讨的所谓的设备管理，其实指的是。操作系统对计算机主机外部的那些啊，

硬件设备的一个管理。那在正式开始本章之前，我们首先要了解一下IO设备的基本概念和分类。那首先来看一下什么是IO设备。那IO设备又可以称为外部设备，所谓的IO其实指的就是输入和输出。I代表的是input。o代表的是output。因此IO设备的作用就和它的名字一样，它无非就是用来把数据输入到计算机当中的一种设备，或者是一种呃，可以接收计算机输出数据的一种外部设备。那像平时我们很熟悉的鼠标和键盘，就是一种很典型的输入型设备，比如说像键盘这种外部设备，

可以把一个一个的字符数据输入到计算机当中。那像显示器，它又是一种很典型的输出型的设备，显示器可以接收计算机主机输出的那些数据，那些图像的数据，并且把那些图像。解释出来。那像移动硬盘，还有大家很熟悉的U盘，就是属于呃，既可以输入又可以输出的设备。可以把移动硬盘当中存储的数据输入到计算机当中，也可以把计算机当中的数据输出到移动硬盘当中进行。存储，所以其实输入的过程无非就是把这个设备准备好的数据读入计算机当中，

而输出的过程无非就是把计算机当中准备好的数据。把它写出到这个输出设备上，因此unix系统会把这些外部设备把它抽象为一种特殊的文件。这样的话，用户就可以用读文件或者写文件的方式来对这些外部设备进行操作。像reit操作其实就是向外部设备写出数据，也就是一个数据输出的过程。而re的操作就是从外部设备中读入数据，也就是一个输入的过程。因此，在之后的讲解当中，大家也会经常看到啊，对一个设备进行write操作和read操作，它们所对应的其实就是数据输出和数据输入的过程。那么，

接下来我们来看一下IO设备有哪些分类？如果按使用特性来分的话，IO设备可以分为这样的三种类型，第一种叫做人机交互类的啊，外部设备。像平时我们经常用于和计算机进行交互的这些鼠标，键盘，打印机等等，这些设备就属于人机交互型的外设。那这些设备一般来说，数据的传输速度会比较慢啊，数据的传输都是以字节或者呃几十字节为单位的。那第二种叫做存储设备，这也是大家很熟悉的，像移动硬盘还有光盘，

这些就是存储设备，就是用于存放数据的。那像这种存储设备一般来说，数据的传输速度会比较快，第三种叫做网络通信设备，像大家平时熟悉的猫。还有路由器，这些就属于网络通信设备。那这种设备的数据传输速度一般来说是介于这二者之间会比人机交互类的更快一些，又会比存储类的设备要更慢一些。那这是按使用特性来分类，如果按照传输速率来分类的话，又可以把设备分为低速，中速和高速这样三种。低速设备就是像什么什么鼠标键盘这样的设备，

因为我们每敲击一次键盘，其实只是往啊计算机当中输入了一个字符。所以这种设备和计算机进行数据交换的速度是很慢的，一般来说每秒就是呃几个到几百个字节。那中速设备的话，一般来说传输速率是每秒钟数千至上万个字节，比如说激光打印机。而高速设备的话就是每秒数千个字节，甚至还要更快，像平时咱们使用的移动硬盘。那其实那其实高中低速啊，并没有一个很明确的这种界限划分，所以这个知识点大家也不需要特别的记忆。能有一个印象，了解就可以了，

基本不可能作为考点进行考察，第三种分类方式就是按照信息交换的单位进行分类。分为块设备和字符设备，那像平时咱们使用的移动硬盘，磁盘等等，这些就属于块设备，它的数据传输的基本单位是一块一块进行的。那经过上个章节的学习，大家对快的概念应该也已经有了很明确的了解，那这种设备的特点是传输速率会比较高，并且可寻址。所谓可寻指，就是指啊，这种设备可以随机的读写任意的一块，那像字符设备的话就是类似于鼠标键盘这种。

数据的传输，基本单位是字符或者字节。所以这种类型的设备传输速度一般会比较慢，并且不可寻址，在输入输出的时候，一般来说会采用中断驱动的方式。那中断的概念，咱们在第一章啊，也有比较详细的介绍，在下个小节中，我们还会啊继续介绍什么叫中断驱动方式。好的，那么这个小结的内容并不多，我们理解了什么是IO设备，所谓的I其实就是input o，

指的就是output，也就是输入和输出。那么之后我们又介绍了l设备，按照不同的呃分类方式，可以分为哪几种类型？那像前面的这两种分类方式，其实并不会有很明确的呃，那种划分的界限，所以这两种分类方式其实一般来说也不太可能进行考察。因此，大家需要重点关注的是，第三种分类方式，也就是按信息交换的单位分类，分为快设备和字符设备。大家需要注意嗯，

它们之间的区别。那这两类设备最本质的区别是，这种设备与计算机进行数据交换的时候，这种数据交换的基本单位，一个是跨，一个是字符。那除此之外，大家还需要结合上一章的内容啊，理解快设备是可寻址的，而字符设备是不可寻址的，这样的两个特性。那字符设备通常会采用啊，中断驱动的这种IO控制方式好的，那么以上就是这个小节的全部内容。
