各位同学大家好，在这个视频中，我们要学习叶框回收这个考点，那这个考点是二零二五年的大纲里边才新加入的一个东西。啊叶框回收和我们之前学过的叶面置换，这个考点有非常呃相似的一些地方。所以在这节课的开始，我们先把页面置换和页框回收这两个概念之间的区别呃，把它捋清楚，然后再来介绍页框回收。的一些相关机制，那来观察这两个术语，它们的英文原文页面置换，它用的是replacement这个词。而叶框回收它用的是呃reclaim拿回回收利用这个词。

那replacement它有替换更换的意思，所以当我们在谈论页面置换的时候，通常就是把某一个进程，比如说进程p1。它的一个页面从某一个页框里面就是置换出去，替换为另一个新的页面，对吧？那换个角度来讲，相当于就是把一个呃物理页框回收，然后立即再分配给这个缺页的进程去使用。所以页面置换的这个过程呃，通常就是即收即用的。那相比之下，页框回收这个过程，它并不是即收即用的，

就是操作系统隔一段时间可能会回收一部分页框。保持整个系统当中有比较多的空闲页框资源，那操作系统会保管这些页框资源。等未来有进程，需要更多的页框的时候，再把这些页框资源分配出去，好，那接下来用呃几个例子来感受页面置换和页框回收它们的区别。首先来看页面置换的一个事例，之前我们已经学过页面置换以及页面置换算法相关的内容，对吧？来看这样的一个例子，假设绿色的这块这块空间。表示的是操作系统当前拥有的空闲页框资源，那在这个图示当中，

这个空闲页框就跟我们的钱包一样，就空空如也，啥也没有了。好，现在有p1P2P三三个进程，每个进程都已经被分配了，若干个页框，比如说p1，它被分配了页框二六十一。这几个页框当中分别存放了p1进程的呃逻辑页面一五二。那P2P3进程也类似好，现在我们假设在这种场景之下p3进程，它想要访问自己的逻辑页面二号页面。那从这张图当中可以看到，目前p3进程只有零号和一号页面，

现在是在内存里边的，对吧？那么，它要访问二号页面，就会触发这个缺页中断的异常。接下来需要把二号页面调入内存，那么页面的数据是要放到物理页框里面的，所以操作系统需要给二号页面准备一个页框出来。呃于是操作系统摸了摸自己的口袋，发现没有任何空闲页框可以使用，那在这种情况下就只能进行页面置换。好，那么通过之前的学习，我们知道页面置换总的来说有两种策略，一种是局部置换，

一种是全局置换，对吧？那假设呃，在这个系统当中采用的是局部置换的策略。那局部置换意味着，当p3需要调入一个新页面的时候，操作系统会从p3的呃这几个页框里边。换一个页面出去，那我们不妨假设呃把p3的这个77号页框里边的这个页面一。给换到外存，然后再把页面二给换入页框77，那最终页面置换的效果就是这个样子。页框77当中换入了p3的页面二，而原本的这个页面一被换出了外存。好，

那这是第一个呃页面置换的例子，在这个例子当中，我们假设的是局部置换这种策略。好再来回顾一下全局置换的策略，如果在这个系统当中采用的是全局置换策略，那么当p3进程需要一个新的页框的时候。操作系统会从呃所有的这些进程的页面当中，通过页面置换算法，选择一个选择一个合适的页面，把它置换出去。那页面置换算法，我们都很熟悉了，对吧？什么aru算法clock算法之类的，那假设在p1P2P3的呃，

所有的这些页面当中。p2进程的页面五已经呃，最近最久没有被访问了，于是根据lru置换算法的策略。需要把p2进程的这个页面给淘汰掉，于是这个页框99就可以被p3所使用。那最终页面置换的效果就是p2手里的这个页框被呃抢夺了，然后把页框99分配给了p3。好，那由于页面置换的这个范围是呃覆盖全局的当p3，需要一个新页框的时候，操作系统可能从其他进程的手里边呃置换一个页面出去。好，那么无论是局部置换还是全局置换，我们都可以看到页面置换它的一个特征就是。

操作系统回收页框之后就立即再分配给这个进程，那通常来说页面置换的发生时机就是呃，当进程发生缺页。并且操作系统呃已经没有空闲页框可以使用的时候就会触发页面置换的这个过程。那么，根据页面置换的策略，就是局部置换和全局置换这两种策略操作系统选择的这个置换页框的来源是不一样的。如果是局部置换。那么就是从进程自己手里的这些页框资源当中，选择一个把其中的页面数据置换出去，对吧？而如果是全局置换的话，那么可能从全局就是所有的这些进程当中。挑选一个合适的页框，把其中的内容换出，

然后把这个页框分配给当前缺页的这个进程去使用。好，那么以上是页面置换的例子，接下来作为对比，我们来观察页面回收是什么情况？看这张图同样p1P2P三三个进程，假设目前这个操作系统它拥有一个空闲的页框资源，就是32号页框是暂时没有呃分配出去的。好，我们假设p1进程，它的页面二以及p2进程，它的页面五最近一段时间都没有被访问过。那么，在这种情况下，操作系统就可以呃使用叶框回收的这种呃相关策略。

把p1手里的这个页框和p2手里的这个页框给回收。反正他们最近已经没有再使用过这两个页框了，对吧？那不如回收给系统这样的话，如果接下来有其他的进程。呃，他有那个页框使用的这种需求，那么对于操作系统来说，就是手里有框，心中不慌。好，所以页框回收和页面置换还是有一些区别的，操作系统会回收页框，未来有需要的时候再把这些空闲页框分配给。相应的进程去使用。

那页框回收这件事情发生的时机呃，常见的有这样的两种策略，一种有可能是这种定式回收，比如说操作系统，它呃设置一个计时器。每隔十秒钟或者每隔多久就定时的去检查一次，哪些页框可以被系统回收？那还有另一种常见的策略，就是操作系统可能会监测在本系统内这个空闲页框的数量还剩余多少？如果空闲页框数量低于某一个下限值的时候，操作系统就会开始进行页框回收。确保系统中总有呃比较充裕的这个空闲页框资源好，那么这是页框回收的发生时机，我们回答了when的问题。什么时候进行叶框回收，

接下来我们再回答how怎么回收的问题？首先，系统中有很多页框都可以被回收，对吧？那么哪些页框应该被优先回收呢？那有这样的一些常见的策略。首先，用户进程的页框。通常优先于内核进程的一个框被回收，比如说呃p1P2P3这几个进程，前两个进程是用户进程。p3进程是内核进程，那么内核进程一般来说它是操作系统，自己的进程对吧？它的重要性肯定要比呃，

用户进程要更高，因此当操作系统要回收页框的时候，可以优先从用户进程的手中。去回收这个页框资源。好，这是第一个呃，比较常见的一种策略，那第二种策略如果说同时有多个进程，他们都可以被回收页框。那么，此时可以采用类似于页面置换算法的这种规则，比如说采用先进先出，最近最久被使用或者clock之类的。采用类似的算法，在各个待回收的页框当中，

选择一个或者多个页框进行回收。好，那这个部分由于和页面置换算法很类似，在这我们就不再赘述。好，现在我们已经知道了哪些叶框应该被优先回收，接下来我们进一步的探讨，当一个叶框被回收之后，应该怎么去处理？可以让整个内存系统变得更高效，那在这个环节，我们可以使用啊，缓冲式回收这种策略。在有的教材当中，也会把它称为页框缓冲算法。

比如说在国外的经典教材操作系统设计与精髓。在这本教材里边就使用了页框缓冲算法呃，这个术语，而另一本国外的经典教材恐龙书就是操作系统概念。在这本教材当中，使用的是缓冲式回收这个术语好，那么什么叫做缓冲式回收？或者说什么叫做页框缓冲算法呢？缓冲式回收可以有效的提供叶框缓冲机制的一个工作效率，那具体来说有这样的三种嗯，常见策略。首先，第一个策略就是可以保留最近被回收的页框内的数据，这样的话，如果页面再次被访问，

就可以快速恢复。好，同样的结合示例来感受假设p1的页面，2p二的这个页面五最近已经很久没被使用过了，那么操作系统可能会回收啊，这个页框11和页框99。那从这个图示当中可以看到，操作系统会管理一个空闲页框链表，把所有的已经被回收，可以被再次分配的空闲页框。全部都组织在这个地方，那从这张图当中可以看到叶框11里边原本保存的是p1进程的页面二对吧？那么叶框11被操作系统回收之后。你会看到里边的这个页面二的信息，并没有被清空，

那这么做的好处就是假如接下来一段时间。t1进程，它再一次的需要访问自己的页面二，那么这个页面二的呃数据就不需要去读磁盘来调入，对吧？操作系统会检查哎，页面二的数据刚好之前被我回收了，还在这个页框11里边。那现在既然你p1进程呃，需要再次使用这个页面，那么我把这个页框11呃携带着页面二再给你分配回去不就行了，对吧？所以如果操作系统保留最近被回收的这个页框内数据，那么在有些时候就可以快速的恢复这个页面的信息，而不需要去读磁盘。

好，这是缓冲式回收的第一个常用策略。好，接下来再看第二第三，这两个策略，第二个策略延迟写回脏页框，第三个策略维护两个呃页框链表。对页框进行分类管理，这两个策略，它们的作用都是一样的，都是为了优化IO时机聚合写入。以减少磁盘IO的开销，好来看一下什么意思？我们假设p1P2P3它们呃，各自都有一个页面，

最近很久没使用了。那么，操作系统把p1的这个叶框11p二的叶框99p三的叶框77全部回收。我们假设p1进程的这个页面二，它的脏位等于零，换句话说，这个页面二的数据没有被修改过。那么，既然这个页面的数据没有被修改过，那这个页面就不需要写回磁盘，对吧？不需要写回磁盘。而相比之下，我们假设呃p2进程的页面五以及p3进程的页面一这两个呃页面，我们假设它的脏位等于一，

也就是说这两个页面的数据。被修改过，那么这两个页面的信息肯定就得写回外存，对吧？好，接下来观察右边这张图，可以看到操作系统维护了空闲页框链表。以及修改页框链表这样的两个链表。上面这个维护的是脏位等于一的，也就是需要写回磁盘的这些呃页框的信息。而下面这个链表，它维护的是呃，要么就是呃，完全这种干净的一个呃页框，要么就是里边保存的数据不需要被写回磁盘的。

呃，这样的一个框。那显然，下面这个链表的这些页框，如果未来有一个进程需要使用的话，那么直接把这个页框分配给它，直接往这个页框里边。覆盖新页面的数据是不是没有问题，对吧？因为像页面二的信息完全不需要写回磁盘嘛？好，那相比之下，上面的这个页面五和页面一啊，它们分别存放在页框99和页框77当中。那么，

这些页框把它全部组织在一起，来管理的话，操作系统就可以优化这种写磁盘的。IO时机。怎么理解呢？你的磁盘肯定会有空闲的，以及忙碌的时候，比如说你往你的磁盘里边下载电影啊，或者呃下载文件的时候，你的磁盘可能就是很忙碌的，对吧？那如果说操作系统检测到哎，当前这个磁盘本来就处于忙碌的时期，那么这些。呃，

脏页面脏页框里面的数据是不是就可以先暂时不写回磁盘等你的磁盘？空闲的时候再把它写回磁盘，这样的话就可以让整个。磁盘的IO效率更高嘛好，这是所谓优化IO时机的意思。那第二句话，聚合写入以减少开销，这个怎么理解呢？我们知道呃，磁盘在写入数据的时候，它的磁头臂是需要移动的，需要去寻找磁道，那如果说我们把多个就是呃存储的位置。相邻的这种页面全部都聚合在一起，一口气去写入，

这样的话写磁盘的这个效率也会变高，就有点类似于什么呢？有点类似于你做收纳的时候对吧？你做收纳的时候。你有好几个这种小格子，你有好几个小格子，那么如果说你一次一次性搬运好几个物体，把它放到这种连续的几个小格子里边。那么，你的这个搬运效率是不是就会更高？你不需要在这些小格子之间来回来去跑吗？假如你一会儿哎是放到左边这个格子，一会儿是放到最右边这个格子。那么，当你在存入这个物体的时候，

是不是得来回来去跑？这样的话，你的存入效率就会变低。那么，对于磁盘来说也是一样的，如果把存储的位置，相邻的这些页面全部都。呃，聚合在一起就是一口气，把相邻的这些东西全部放进去，那么OK对于磁盘的写入效率就可以呃。就可以提升，从而减少磁盘IO的时间开销好，那么这是叶框回收的几种常见策略。好的，

那么在这个视频中，我们介绍了叶框回收这个考点，首先我们探讨了叶框回收和叶面置换之间的一个呃区别。页面置换本质上也是操作系统，把一个页框回收，但是立即再分配给某一个进程去使用。而对于页框回收这种机制来说，就是操作系统会保管它回收回来的这些页框，等未来有进程需要使用这种新页框的时候，再把它分配出去。好，所以页框回收和页面置换之间还是有一些区别的，页面置换通常发生在进程缺页，并且已经没有空闲页框可用的时候。就会进行页面置换，

而页框回收通常来说是定时触发的，比如说每隔十秒回收一次页框，或者当系统检测到空闲页框的数量。低于某一个下限值的时候，也可能会触发页框回收，所以页面置换这个过程，它属于是。问题发生了，我临时再去解决问题，而页框回收这个机制，它像是问题出现之前，我就提前预防，提前储备好这个空闲的页框资源。好，那么在这节课当中，

我们探讨了哪些页框应该被优先回收？通常来说，呃，会优先回收用户进程的页框，以及如果有多个用户进程的很多个页框，都可以被回收的话。那么，可以采用类似于页面置换算法的一些策略，比如说先进先出，最近最久未使用或者clock等等。采用这些策略。在多个页框当中选出一个或者多个页框进行回收，那最后我们介绍了页框回收的一些优化策略。我们把它称为缓冲式回收，常见的就是保留数据，

延迟写回和双链表管理这样的三个策略。最重要的是标红的这个保留数据就是被回收的页框数据暂时不清空，这样的话，如果里边的页面再次被访问的时候就可以快速恢复，而无需启动磁盘IO。那总结来说一句话，叶框回收它的核心思想就是操作系统会暂存。回收的页框减少缺页代价，优化磁盘IO的性能。好的，那么以上就是这个考点的全部内容。
