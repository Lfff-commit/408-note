各位同学大家好，本节课我们会学习操作系统的四个特征，本节课的内容通常来说会考察概念性的选择题。那操作系统有这样的四个特征，分别是并发，共享，虚拟和异步，其中并发和共享是操作系统最基本的两个特征，并且二者互为存在条件。接下来我们先认识并发这个特征和并发很相似的另一个概念，叫做并行来看一下二者的区别。并发指的是两个或者多个事件在同一时间间隔内发生，这些事件在宏观上是同时发生的，但是微观上是交替发生的。这叫做并发而并行，

是指两个或多个事件在同一时刻同时发生。好，举个例子，我家养了两只猫，左边这只叫招财，右边这只叫进宝，两只猫都很粘人。那如果我采用单手撸猫的这种方式，那为了照顾到两只猫，那我会在一段时间内左一下，右一下，左一下，右一下，就这样交替的抚摸。那由于是单手撸猫，

所以宏观上看我们可以说在这一段时间内，我是在同时撸两只猫。但是微观上看呃，在某一个具体的时刻，我只会撸其中的一只猫。好，这是并发撸猫，那再看并行撸猫同样是招财和进宝，这次我们用左右两只手分别撸一只猫。那么，此时我们就是在并行撸猫，撸招财和撸进宝这两个事件，在同一时刻同时发生。并不是微观上交替进行。好，

那么通过这个例子，我们可以感受到并发和并行的区别，理清楚这个概念之后，我们来看操作系统的呃，并发这个特性。操作系统的并发性指的是在计算机当中同时运行着呃多道程序，这儿的同时我们打了双引号，现在你知道了所谓的同时运行有可能只是宏观上同时，但微观上。是交替运行的，那这就是操作系统的并发性。那有同学可能会说，现在我们的这个电脑手机的CPU都是多核的，那多核CPU意味着同一时刻允许有多个啊程序并行的执行。每一个核心去执行一道程序，

所以多核CPU其实是支持呃，多道程序的并行执行的。然而，即便是多核CPU操作系统的并发性依然不可或缺。比如说，像英特尔的酷睿I3处理器，它就是一个四核CPU。这意味着，可以同时并行的运行四个程序。然而，我们在玩电脑或者玩手机的时候，我们会同时开很多个程序，比如说。在这四道程序运行的同时，我们还想同时使用QQ音乐，

那在这种情况下，四核CPU就没办法。同时，支持五道程序的并行执行了。那此时，我们只能让比如说QQ音乐和Word文档这两个程序。交替着上这个呃处理机去运行。好，所以即便是对于多核CPU来说，并发性依然是不可或缺的，那操作系统会负责完成这个呃程序或者说进程的调度。什么时候该让qq音乐上处理机运行，什么时候又应该让word上处理机运行，这个交替运行的过程就是操作系统在背后去实现的。那这就体现了操作系统的并发性。

关于操作系统是如何调度呃多道程序运行的，这个我们会在第二章进程管理呃那个部分去学习。在当前阶段，我们建立起一些粗浅的认知就行。好，接下来看操作系统的第二个特征，共享性所谓的共享，指的是资源共享。在一台计算机内部。硬件资源往往是有限的，那么有限的资源如何让多个并发执行的进程啊？共同去使用呢？这就是所谓的共享要解决的问题。操作系统需要协调系统内的有限资源，什么时候该把资源分给a进程，

什么时候该把资源分给b进程，这是操作系统需要完成的事情。那么有的资源，我们需要使用互斥共享的方式，而有的资源，我们可以使用同时共享的方式。所谓的互斥共享，就是同一时间段只允许一个进程访问，这种资源比如说你的手机摄像头这种资源。大家可以试一下，你打开系统相机的同时呃，有没有可能呃，用微信和你的家人或者朋友聊天？也就是说，摄像头这个资源有没有可能同时分配给啊？

相机这个进程以及微信这个进程去使用。大家可以试一下，至少我的手机的操作系统是不支持摄像头这种资源的，同时共享的。同一时间段内，只允许一个进程访问该资源。好，再来看。同时，共享的方式呃，有一些系统资源在一段时间内是可以让多个进程，同时啊，对它进行访问的。但这所谓的同时，也有可能是宏观上同时，

而微观上是交替进行交替访问。刚才我们说了摄像头这种设备，它属于互斥共享的资源，而多个进程访问硬盘这种设备。它就可以实现同时共享，比如说你可以用QQ呃给你的朋友发送文件a，与此同时用微信给你的另一个朋友发送文件b。那么，你的文件a和文件b都是存储在你的硬盘里边的，对吧？那么两个进程同时发送呃文件的时候，是不是意味着QQ和微信这两个进程它们？需要在一段时间内同时去访问硬盘这个资源。那这就是所谓的同时共享等，学到后面大家会知道所谓的两个进程，

同时访问硬盘资源，它在背后微观上其实是两个进程交替。访问这个磁盘的好，那当前阶段我们先不展开细聊，我们重点体会同时共享的这种呃方式。那还有一个典型的可以由多个进程同时共享的设备，就是扬声器。无论是手机还是电脑扬声器，它都属于IO设备，这个设备可以由多个进程同时共享，比如说你一边用一个呃音乐类的软件。去播放声音，播放音乐。与此同时，你打开了哔哩哔哩呃来观看我们的这个讲课的视频。

那么，音乐软件和哔哩哔哩这个软件，它们就同时共享着扬声器这种资源好，那么通过这些例子不难感受到操作系统的共享特性。操作系统需要对有限的硬件资源啊进行管理。在恰当的时候，把资源分配给某一个或者某几个进程去共享使用。好，这是共享，那么在这节课的刚开始，我们说并发特性和共享特性互为存在条件，对吧？来解释一下为什么呃，我们说并发性是指呃多个程序同时运行对吧，而共享性是指呃系统中有限的资源被多个并发的进程去共同使用。

那假如说QQ进程正在发送文件a，而微信进程正在发送文件b，这个体现了并发性对吧？那幺正是因为呃，两个进程会同时运行，而两个进程又都需要同时访问硬盘这种资源，所以有了并发性。他们俩才需要去共享某一个硬件资源。假设没有并发性，一段时间内，系统中只会有一道程序正在运行，那么系统内所有的资源都让给这个程序去使用，是不是就OK了？也就是说，如果没有并发性，

那么共享性其实就没有存在的必要，那相应的如果说操作系统。不提供这种共享的特性，那么QQ和微信想要并发的运行并发的去分别发送文件a和文件b是不是这个事情也没办法实现？所以我们在开篇才说呃，并发性和共享性是操作系统最基本的两个特性，二者互为存在条件。没有并发那么共享，就没有存在的意义，而如果没有共享，那么并发性也不可能实现。好，接下来看操作系统的第三个特征叫做虚拟性。所谓的虚拟，是指一个物理上的实体。

把它变成若干个逻辑上的对应物，物理上的实体是实际存在的，而逻辑上的对应物是用户感知到的。好，这句话读起来比较抽象，我们结合一个例子来理解，大家应该都知道哈呃，一道程序，它要运行之前。需要放到你的呃内存里边，先把程序放入内存才可以给这个程序分配CPU，这个程序才可以跑起来。所以当我们打开一个软件的时候，呃，通常来说，

第一次打开的时候，它会启动的比较慢，对吧？那启动的慢的那个过程，其实就是把这个软件或者说这个程序的数据从硬盘给装入到内存。而一旦这些程序的数据放入内存之后，就可以给它分配CPU，就可以让它运行起来了。呃，我在上大学期间呃，很喜欢玩一个游戏，叫做gta 5，这个游戏官方给的运行内存的下限是4 GB。大学期间我在打这个游戏的时候呃，我还会同时打开QQ，

挂个QQ，然后打开迅雷去下载一些东西，以及有可能在打开网易云音乐呃去听歌。然而，我的电脑只有4 GB的物理内存，只有4 GB。相当于我的电脑只够装呃gta 5的，这个运行数据对吧？然而，实际用下来我发现。我想要一边打gta，一边挂QQ，一边挂迅雷，一边听音乐，这几件事情同时进行是完全OK的。

所以对于我这个用户来说，我在逻辑上感知到的我的呃电脑的这个内存。似乎是有4 GB的内存，加上二五六的内存去支持QQ运行，再加上二五六的内存支持迅雷运行。再加上二五六的内存，还可以支持网易云音乐去运行。我作为用户感知到的这个呃电脑的内存要比它实际的这个物理内存更大。那这就是操作系统虚拟性的一个体现操作系统，它实现了虚拟存储器技术，这个我们会在本门课的第三章去进行学习。那引入了这种技术之后，虽然我的电脑物理上只有4 GB的内存，但是在我这个用户看来，它似乎远远大于4 GB。

虚拟存储技术是典型的空分复用的，这种思想所谓空分复用，就是把一个有限的物理内存。在空间上切分成很多个这个部分，然后各个部分可以独立的让各个进程去复用。比如说当gta 5需要使用到很多内存的时候，就可以把其中的很多空间先分给gta 5，而如果这个游戏它当前运行的这个过程当中不需要这么多内存。那么就可以把其中的一些空间给它清空出来，然后把这些空间分块分给其他的呃进程去使用，那这就是所谓空分复用的意思。等大家学完第三章，会对这个概念有更深的认知，这我们简要了解即可好，那么除了用这种空分复用的方式实现虚拟性之外。

在操作系统中，还会使用十分复用的思想去实现虚拟性。比如说某个单核CPU。用户同时打开了一二三四五六六个软件。然而，我们只有一个单核的CPU，对吧？物理上看，这台计算机似乎只能支持一个程序的运行。但是在我们用户视角看来，这个计算机似乎是拥有六个CPU一样，可以同时支持六个程序的运行。那么，这背后就涉及到了虚拟处理器的技术。一个单核CPU在我用户的视角看来，

它似乎是有六个CPU在同时为自己服务。相当于我们把CPU的服务时间拆分成了呃，很多个时间分片对吧？每一个时间分片我们可以给其中的某一个进程去使用。比如说第一个时间分片，我们给QQ使用，第二个时间分片去运行这个chrome。第三个时间分片去运行，这个QQ那么交替往复运行，这样在我们用户看来好像一段时间内所有的程序都在同时运行一样。那由于我们是把CPU的时间进行了拆分，然后把这些时间片让各个进程去复用，所以这种技术叫做十分复用技术。好，所以操作系统有可能用时分复用技术，

这种思想也有可能用空分复用这种思想去实现虚拟性。好，这是操作系统的第三个特性，接下来最后一个特性叫做异步性。所谓的异步，是指在多道程序的环境下。呃，由于我们多个程序是并发执行的。但是由于系统的资源有限，进程的执行并不是一贯到底，而是走走停停，以不可预知的这种速度往前推进，这就是进程的异步性。好，这句话不好动，

我们结合一个例子来解释，我现在有一台iPhone手机，那iPhone有一个云盘的软件叫做I。跟微软的one drive，这个云盘是类似的。我会把我所有的文件都存放到这个i cloud云盘里边。这样我的手机本地的存储空间就可以呃节省下来。好，接下来故事是这样的，有时候我会想用微信给朋友发送一张存储在i cloud云盘上面的图片。然而我想发的这张图片，它是存储在云盘里边的，还没有下载到我的本地。所以当我在微信上点击发送图片这样的一个操作的时候，我的i cloud云盘这个进程，

它会默默的开始下载这张图片。那由于我的手机系统上同时并发着运行很多很多个进程，所以I进程下载图片这个动作，它也是走走停停的。因为我的手机并不随时都在为他服务，对吧？好那么经过呃两三秒钟的下载，下载完成之后，我的微信进程才可以去发送这个图片文件。因为在这个时间点，我的图片文件才真正的从云盘来到了我的手机本地，然后微信是从手机本地。去读取这个图片的数据，再发给我的朋友。好，

现在思考这样的问题，对于我的微信进程来说，其实当我点击发送那个按钮的时候，微信就已经准备好，随时随地都可以发图片，对吧？那我的微信进程和我的i cloud这个进程，它们俩是并发运行的，各走各的，各干各的，那微信怎么会知道i cloud这个下载这个事情有没有做完呢？也就是两个进程都是在以不可预知的速度向前推进，对吧？都是走走停停的。那假设我的微信进程在中间的这个时间点就尝试着发送图片。

那这意味着什么？这意味着我的图片文件可能只下载了一半对吧？下载了一半就被微信发出去了。那这显然会导致错误的结果。好，所以在操作系统内部这种进程的异步性是我们必须去解决的问题。那在本门课的第二章，我们会学习操作系统的进程同步机制。有了进程同步机制，那么多个互不相干的，这种呃进程，他们之间想配合去完成一些事情的时候。就可以用这种同步机制去解决，以此去防止并发进程的异步性带来的这种问题。好，

那么这就是操作系统的第四个特征，叫做异步性好的本节课。我们介绍了操作系统的四个基本特征。其中，并发和共享是最基本的两个特性，二者互为存在条件。在我们谈到并发性的时候啊，我们强调大家需要注意并发和并行的区别。那关于共享性，虚拟性和异步性。我们在当前阶段有一个粗浅的认知即可，随着本门课的后续学习，大家会有更清晰的认知。好的，那么以上就是本节课的所有内容。
