输入输出指令是特权指令例如像x86架构 in/out 这两个指令可以在x86的CPU上面用汇编语言的形式来使用这两个指令 特权指令
IO指令，开/关中断（影响一个程序是否一直霸占CPU），修改PSW（程序状态字寄存器）（这个寄存器存放了很多重要的信息例如此时CPU是否是开/关中断 还有程序的一些运行状态 很重要的一些参数和信息记录在这个寄存器）指令

通道CPU的小马仔，通道可以执行IO指令，从而管理IO设备的工作还有一些普通指令

系统调用是用户主动陷入内核，不会主动执行屏蔽中断（关中断）这个特权指令

中断响应过程（中断引指令通过硬件实现），关中断，保存端点，引出中断服务程序，将CPU状态改为内核态

过程调用只需要保存PC的值不需要保存PSW的值

一般过程调用过程例如函数A可能运行在内核态也可能用户态

寄存器存了某些数据读写速度较快能够更好的匹配CPU工作，程序计数器（PC）是特殊寄存器
PC中的指向下一条即将执行的代码 不管是子程序还是中断程序当执行完之后还是要回到主程序继续执行 PC程序计数器内容压栈保护 当子程序或者中断程序执行结束之后回到主程序执行 在从栈点拿出并且回复到PC

程序计数器，

它也是一种特殊的寄存器，

不过它存的是这样一个一个东西啊PC是程序计数器的英文缩写是。

啊program counter的缩写，

大家可以下去查一下。

它的作用是嗯，

这个PC中存的内容是指向了下一条我们即将执行的代码。

比如说这个地方，

我们有一个代码的编号是n，

那么下一条应该执行n的话PC就会被置为n。

接下来，

代码n被执行，

而代码n涉及到的是一个子函数子程序调用的一个一个代码，

这样的话，

这个程序就会转到另外一个子程序上进行执行。

但是这个地方大家注意子程序中的代码m，

这个m和n的编号已经没有直接的关系了，

它是另外一个呃。

一个代码，

但是这个时候为了执行代码m，

我们依然需要把PC的值变为m，

指向新的这个即将执行的代码。

直到子程序执行结束之后，

再返回主程序，

接着返主程序呃，

刚才应该执行的那一行继续执行。

但是这个地方问题出现了，

既然我们这个地方已经把PC的值变成了m，

而m和n并没有直接的关系。

那么，

当我们的程序回到主程序之后啊，

它又是怎么知道我应该从n- 1这这块继续往下执行的呢？

其实这就是这一题的a答案，

要考察的内容。

不管是子程序还是中断处理程序，

当它们执行结束之后，

终归还是要回到以前的主程序继续执行，

而如何记录我们在主程回到主程序之后，

应该从哪一？

一行开始执行呢，

这就需要把PC程序计数器的这个内容进行，

在呃进行压榨保护保存下来。

当我们的子程序或者中断处理程序执行结束之后，

再从站点拿出啊，

我们之前保存的这个内容，

并且复回到PC。

这样的话就可以知道，

接下来我们应该执行的是哪一行代码，

因此PC这个寄存器里的内容。

不管是子程序调用，

还是中断处理程序调用，

由于它们调用完了之后终归还是要回到主程序继续执行，

因此PC的内容是都需要保存下来的。

所以a答案不正确，

下面看通用数据寄存器有什么作用？

假设有这样两个呃，

可以保存数据的寄存器分别叫a和b。

然后代码n- 1是有这样一个操作，

就是往a和b里边呃放了两个数，

不管它是什么，

然后代码n- 1 n+1。

的操作是把a和b中的数取出来，

相加之后再返回a，

也就是说代码n+1的操作是基于代码n- 1的，

这个操作的结果来继续执行的。

下面看这样一种情况，

假设代码m也就是这个子程序里的某一行代码，

它做了一个操作，

就是往a和b里边又存放了另外两个数。

这样的话，

当子程序或者中断程处理程序结执行结束之后，

返回主程序这个代码n+1的结果就会出现错误。

因为它已呃这它它已经不是我们以前期待的那两个数了，

而被篡改成了另外两个数。

所以这当然不是我们期待得到的一个结果。

因此，

既然我们在这块的调用中需要用到覆盖掉这两个寄存器里的内容，

那么在子程序开始之前。

就应该把这两个寄存器里的内容给保存下来，

然后在子程序结束之后再把这两再把保存下来的那两个数再给放回这两个寄存器用来恢复现场。

这样的话才能保证主程序中的代码n+1执行，

是我们以前期待的正确的结果。

所以啊，

有时候这个数据寄存器里的内容是需要被保存下来的。

再看另外一种情况，

假设现在代码m它并不是把AB两个覆盖了整个子程序当中的任何一个代码都不会涉及到对寄存器a和b的数据的操作。

这样的话，

有机也就意味着在子程序的执行当中，

呃a和b的内容不会被更改。

在这种情况下，

其实a和b的内容就没有必要再保存下来，

因为它不会变。

所以这块执行之后再返回它，

依然是以前的那个数，

也不会影响以前这个主程序里的呃执行结果。

所以在刚才说的这种情况下，

我们的数据寄存器里的内容又可以不被保存下来。

因此，

数据寄存器里的内容有时候是需要保存的，

有时候是不需要保存的，

具体需不需要就要看子程序或者中断处理程序中。

是否会用到或者覆盖掉这两个寄存器中的内容？

所以c答案也不对，

在中断处理程序中也不一定会保存这个通用数据，

寄存器里的内容，

而子程序调用中有时候是需要保存的。

c答案不对。

下面看d答案，

通用地址寄寄存器，

其实通用地址寄存器和c答案的这个数据寄存器是一样的，

只不过这个寄存器里放的是地址，

这个寄存器里放的是数据。

而对于通用地址寄存器，

也和刚才我们数据寄存器一样，

有时候它在这边会涉及到对这两个寄存器的内容的修改。

这样的话，

它需要保存下来，

有时候又不需要修改，

这样这种情况下又不需要保存下来，

所以d答案和c答案一样也不对。

所以剩下的答案就是b下面看一下b答案，

这也是这个题目中比较难理解的一个选项。

首先看一下程序状态，

自寄存器它有什么作用？

它有一个非常厉害的作用，

就是标记了当前处理器的状态，

这只是它其中的一个一个作用啊。

我们的处理器当前处于内核态还是用户态，

就是根据这个寄存器中的某些或者某个标志位来进行区分的。

因此，

这个这个寄存器里的这些数据是非常关键的，

就影响到我们整个系统的运行。

下面看这样一个例子，

假设现在正在执行的是一个用户程序，

这个用户程序在用户态执行啊，

那么这样的话，

这个psw的标志就应该是用户态。

此时，

在执行到某个代码的时候，

突然来了一个中断信号，

然后此时这个CPU就不得不停止用户程序，

转而执行中断处理程序。

这样的话，

就需要把CPU或者处理器的状态啊，

切换到内核态而psw的标志也会相应的改变为内核态。

最后再返回，

返回之后呃，

就需要把psw的标志从内核态再一次又变回用户态。

下面考虑这样一种更复杂的情况。

假设现在中断处理程序一正在执行的过程中，

又来了一个优先级更高的中断信号二。

那此时CPU也必须停止中断处理程序，

一转而执行呃，

中断处理程序，

二来响应这个呃优先级更高的中断中断信号。

那此时从从处理程序一到处理程序，

2 CPU的状态依然是内核态。

保持不变。

那么，

当这个处理程序二结束之后，

返回之后，

我们的psw的标志不应该把它变为用户态，

而应该。

继续保持内核态，

然后最后从处理程序一结束，

返回之后才把它呃变为用户态。

呃理清楚了，

上面这个这个过程之后，

我们会发现在我们的中断处理程序结束，

并且返回之后。

有时候我们的呃CPU处理器的状态psw的标识位是需要改变的，

从内核态变为用户态。

但又有的时候，

它是不需要改变的，

依然保持内核碳。

那么，

我们如何区分？

是否需要把psw变为呃另外一个状态呢？

这其实就需要用到所谓的这个压栈保存。

把psw的这这个数据给记录下来，

在也就是说在这个中断处理程序呃执行之前记录下来psw的。

呃，

标志标志位和其他相关的数据，

这样的话，

当我们的处理程序结束之后，

从站点拿出psw呃，

我们之前保存的数据，

这样的话就可以知道。

我们是不是应该把处理器切换回用户态，

还是说继续保持内核态不变，

所以这是psw标志的呃。

psw寄存器的一个很重要的作用。

因此，

在刚才这个例子中，

我们会发现，

在中断处理过程中，

我们一定要保存psw这个寄存器相关的内容。

只有这样，

我们才知道处理结束之后呃，

应该把处理器置为什么态？

啊，

刚才这个例子是一个中嵌套中断的一个例子。

所谓嵌套中断，

就是指啊，

这儿本来已经在运行一个中断处理程序了，

突然又来了一个优先级更高的中断处理程序，

这样的话就会嵌套的再进行一个中断处理。

这就是所谓的嵌套中断，

下面看另外一个例子，

为什么子程序调用不需要保存呢？

在子程序呃，

在普通的子程序调用中。

首先，

我们的程序是运行在用户态，

然后子程序调用，

其实还是用户的子程序。

这种子程序依然是在用户态执行的，

因此psw的标志位依然是用户态不变，

所以当这个子程序啊，

执行结束返回之后PS的。

psw的标志位依然是保持不变用户态就可以了。

所以在普通的子程序调用中，

我们psw的值其实是不会被改变的。

但是这个地方可能有的同学会问，

假如这个子程序中涉及到系统调用怎么办呢？

假设在这个子程序中，

涉及到了一个系统调用，

而这个系统调用的话，

呃，

它的处理程序一定是要切换到内核态才可以运行的。

既然是呃，

有用户态到内核态的一个切换，

那就意味着在系统调用过程中肯定也是经过了中断才可以完成这个呃。

处理器状态的一个一个转变，

所以其实在系统调用这这种比较特殊的程序调用里。

其实它中间也是掺杂着掺杂着一个呃中断处理，

而这个中断处理已经把psw的标志给保存下来了。

所以这个题目中提到的子程序调用，

其实是指我们狭义的子程序调用，

就是用户态到用户态的这种调用。

在这种调用下psw的标志，

还有其他的一些信息是不会改变的，

因此这个psw也不用被。

记录下来，

因为返回之后psw还和以前一样。

啊，

刚才这两个例子比较绕。

其实这个题目还可以有另外一种理解。

我们来看一下这个psw，

既然这么重要，

它标记了当前处理器的状态是内核态还是用户态？

那么，

考虑这样一种场景，

假设我们在用户态下可以改变psw的一些值，

比如说我，

我在用户态下。

我使用了某条指令，

让这个这个这个标志呃，

让这个psw的标志变为了内核态，

那这样的话。

既然已经切换到了内核态，

在我的用户程序里，

我就可以使用操作系统的特权指令。

而这种结果可想而知，

是非常严重，

极不安全的，

所以psw的呃值的改变肯定不可能在用户态进行。

它只可能切换到内核态进行，

因此回到刚才的这个例子当中，

我们在进行普通的子程序调用的时候。

都是从用户态进而继续保持用户态呃，

在这种情况下psw的值不可能被改变，

因此呃，

即使执行了子程序再返回之后psw还是以前的值。

不会影响到以前主程序的执行，

但是假如此时是进呃，

在假如此时我们进行了一个中断处理，

而中断处理又。

会涉及到从用户态转换到内核态这样一一种情况，

那在内核态中，

既然psw的值有可能会被改变。

那么，

我们就应该把用户态下的psw的值给记录下来，

这样的话，

当从这个内核态返回用户态的时候，

我们就可以恢复psw的。

以前的值恢复现场，

这样的话，

以前的子程序的执行就可以继续下去了，

所以。

这是另外一种比较简明的理解方式，

这样想也可以。

反正综上所述这个题目的答案就是b在中断处理下psw是一定要保存的，

因为它有可能会被呃改变，

而子普通的子程序调用是不需要保存的，

因为。

呃，

普通的子程序调用psw的值不可能被改变。

我们来看这道题，

这个题目中首先大家需要明确的是在做在对外部中断进行处理的时候，

我们需要保存的内容有哪些？

有这样一个原则，

我们需要保存的是中断处理程序结束并返回之后再。

源程序，

也就是被中断的那个程序当中还需要使用，

并且如果我们不保存就不可以恢复的内容。

在这个地方，

我为大家做了一个总结，

中断处理时需要保存的数据可以分为两个大类，

其中一类由硬件负责，

另外一类由。

呃，

操作系统也就是软件负责，

其中硬件负责保存的是呃PC和psw，

这两个必须不得不保存的。

啊，

寄存器的内容，

而操作系统也就是软件负责保存的是那些有可能需要保存，

有可能也不需要保存的寄存器的内容，

比如说。

这儿b答案提到的通用寄存器。

为什么有的通用寄存器需要保存，

而有的不需要保存呢？

假设在我们的源程序当中呃，

已经使用了某一个寄存器，

寄存器a。

那么，

假如在中断处理程序中也需要使用到寄存器a，

并且把寄存器a中的数据给覆盖。

那么，

我们在返回原程序的时候，

我们就需要把原程序中以前在寄存器a中存的那个数给它恢复回来，

因此在这种情况下。

原程序中寄存器a当中的内容就需要先把它保存下来，

而假如说源程序中使用的寄存器a在中断处理程序中并不需要使用的话。

那么，

其实因为中断处理程序不可能更改这个寄存器a的内容，

所以这个寄存器的内容也不需要被保存并恢复。

因为它一直是保持不变的。

所以通过刚才的讲解，

可以知道呃a答案程序计数计PC的内容，

这是非保存不可的。

这个由硬件来负责保存，

而通用寄存器的内容需要判断它到底需呃，

它到底是需要保存还是不需要保存，

因此它是由软件或者说操作系统来负责保存的。

而c和d两个答案快表和catchy这两个东西其实在里边存的内容是我们主存中的某一些呃数据的一个副本，

一个复制体。

所以即使这两块的内容丢失了，

其实我们也可以再复制一份也就完了，

所以它是可以恢复的内容，

因此c和d两个。

两个两个选项是不需要保存的内容。

所以这题的答案选b。

我们来看这道题，

首先大家需要明确的是CPU从用户态变为内核态，

只有唯一的一个途径就是通过中断。

而只要发生中断，

对于中断的处理肯定是在内核态中进行的，

所以可以这么说，

从用户态变为内核态。

其实等价于是否发生中断，

只要发生中断了，

肯定会变为内核态。

而只要从用户态变为内核态，

中间肯定也需要中断的处理。

所以这个题目我们可以呃，

用这种思路来判断abcd四个选项是否会引发中断。

那么，

从中段的大分类上，

这个这个逻辑框架上入手中段，

分为内中段和外中段。

这个地方给出的是一些汇编。

汇编语言的一些指令，

而这些指令肯定是在执行的过程中才会发生的。

一些异常中断。

这这种中断就被称为内中断，

因此我们看这些分类，

首先看a选项，

它是一个除法操作。

假如说呃，

正常的除法是不会导致异常的，

但是假如这这个r1它是零的话，

就会导致整数除零这种非法操作。

这种非法操作其实就会导致一个软件中断，

从而使用户态变为内核态。

因此a答案是有可能的。

而b答案。

int这个指令，

一般来说，

我们的防管指令或者叫陷入指令trap指令，

就是通过int加上某一个特殊的参数来实现的。

这个反管指令一般用在系统调用中，

是一种用户自愿中断的一个一个操作。

执行这个指令之后，

我们的CPU就会从用户态变为内核态，

因此b答案它是也是有可能导致CPU的这个状态的改变的。

而d答案呃d答案是把地址这个中的内存数据放入寄存器中。

首先，

在访问一个内存单元的时候，

这个内存单元相关的内存页面有可能并没有被调入内存中。

从而就有可能导致内存缺液的情况，

而内存缺液属于硬件故障导致的一种异常。

这这种故障其实可以通过修复的方式，

就可以让程序继续执行下去，

而具体内存缺页这块的内容，

大家在学习到存储管理那一章的时候，

就就就会有一个更深入的了解。

这地方如果暂时不懂，

也没有关系，

以后回头再过来看就可以了。

下面看c答案嗯，

这个指令是让寄存器r0的内容取非。

其实，

无论寄存器r0中有没有存数，

总之，

寄存器r0中肯定会有一些零一零一的二进制代码。

而取非的这个操作，

无非就是把零变成一把一变成零，

这种操作是不可能导致异常的，

因此c答案。

呃呃，

这个指令不可能导致异常，

也就是不可能导致内中断，

因此也不可能让CPU从用户态转变为内核态。

所以这题的答案选c。

当用户进行系统调用的时候，

就意味着呃，

这个处理机CPU会从用户态切换为核心态。

而这个切换的过程，

我们知道是通过线入指令或者叫房管指令来实现的。

只要陷入指令执行完之后，

我们的系统就会变为内核态，

这样的话，

用户就会失去了控制权。

然而，

当用户请求系统为其服务的时候，

他会向系统呃提出一些服务的参数。

这些参数肯定是只有用户才能知道的，

因此如果用户失去了控制权，

那么也就意味着用户不可能再把自己想告诉的参数告诉系统。

所以二和三两个选项陷入指令，

肯定是在传递参数之后才能进行，

因为如果先陷入的话。

那么，

我们的用户失去控制权，

就没办法再传递参数了。

所以三肯定是在二之前，

而三和二之间的顺序也是这一题中最容易让人不确定比较蒙的一个点。

下面看四和一两个选项。

首先，

在执行系统调用相关的一些服务的时候，

因为会涉及到使用各种各样的系统资源。

所以它肯定只能在核心态运行用呃，

以此来保证系统的安全性，

所以第四步就是执行相应的服务程序，

肯定是在进入内核态之后才能执行。

因此，

四是在二之后执行完呃，

这个陷入指令之后才会进入内核态，

而进入内核态之后才可能执行相应的服务程序。

下面看一。

其实一的话，

很容易想明白这个相应的系统服务，

如果没有执行结束，

没有执行完的话，

是不可能把呃。

操作权限交回给用户的，

只有系统做完所有的处理才可能返回用户程序，

也就是返回用户态。

所以四就是执行用户服务呃，

执行服务程序是在一之前，

因此通过刚才的这一系列，

我们就可以推出一个顺序是三二。

四一所以这题的答案选c。

但是根据刚才的这种方法，

大家依然不知道系统调用这个过程背后到底发生了什么。

下面用一种正儿八经理解法来告诉大家，

背后到底做了一些什么事。

首先，

我们要明确的第一点是陷入指令的作用。

它是在系统调用的时候使用完该指令之后。

处理机就会从用户态转换为内核态。

其次，

这个题目中我们容易蒙的是二三两个选项啊。

首先我们来看一下，

在用程序调用时，

一般是怎么传递参数的，

一般的用户程序调用，

因为是用户态到用户态的这种这种。

呃，

调用因此啊，

普通的程序调用的参数的传递是通过用户栈来进行的。

而对于我们的系统调用来说。

系统调用之后，

由于我们的系统状态会从呃用户态切换为内核态相应的。

这个栈也会从用户栈转变成内核栈，

所以在系统调用的过程中，

就不能像普通的程序调用一样。

使用用户栈来传递参数，

所以系统调用一般会用后面这三种方式来传递参数。

而第一种方式由用线入指令，

来自带参数的这种方式通常来说会对参数的长度啊，

个数啊，

会有各种各样的限制，

不太方便。

所以现在操作系统常用的是第二种，

就是通过通用寄存器来传递参数的方式来实现系统调用时候的参数传递。

那么，

这个参数是如何传递的呢？

嗯，

假设这有一个使用用高级语言写的，

一个系统调用，

然后这个高级语言经过编译之后的汇编语。

其实就会变成这个样子，

这个系统调用，

最后会变成这样一些代码。

其中上面这一行，

它其实是把参数放入了某一个寄存器中，

而下面这个int指令，

其实它是执行了一个线入指令。

让系统从用户态变为内核态。

因此，

从上面这个过程中，

我们也可以很直观的看到啊，

把参数放到寄存器中。

这一步其实是在陷入指令之前就执行的。

所以三传递系统调用参数呃，

这个步骤是在二之前。

这就是这题中最容易让人蒙圈的两个顺序，

然后四合一的话呃，

他的推理刚才我已经说过了，

这块其实。

呃，

没什么好纠结的，

四肯定是在线呃，

执行线入指令到了内核态之后才能执行相应的一些系统服务的功能，

所以四在二之后。

而只有这些服务结束之后才会返回用户态，

给用户返回一个结果，

所以一又在四之后，

因此用这种方式理解，

大家应该也可以明白。

这题的答案选c。

好的，

那么这道题讲解结束。

好来看这道题，

当定时器产生了时钟中断信号之后，

时钟中断服务程序更新的部分内容是。

我们在强化课里讲过一个例子时，

钟部件也就是这儿的所谓的定时器。

英文是timer翻译不一样，

有的地方叫定时器，

有的地方叫时钟部件，

有的地方叫时钟。

这个硬件部件的作用就是每隔一段时间会给CPU发送一个时钟中断信号，

比如说我们假设它每隔50毫秒会给CPU发送一个信号。

那假设刚开始CPU上面正在运行的是应用程序，

一它运行在啊用户态，

然后应用程序一的指令一条一条的被CPU处理。

然后处理了几条指令之后，

突然这个时钟部件，

他给CPU发出了一个啊时钟中断信号，

那接下来CPU在检测，

到时钟中断信号之后就会转向内核态。

然后接下来就要开始执行处理，

这个时钟中断的内核程序，

也就是他这所说的时钟中断服务程序。

那接下来CPU就是执行这个时钟中断处理程序的一系列的指令。

这个时钟中断处理程序会做的事情是，

首先会把时钟变量的值。

给更新修改时钟变量会记录当前的时间t，

每收到一个时钟中断信号，

这个t的值就会加50毫秒。

因为我们在这儿是假设了，

每隔50毫秒会发送一个时钟中断信号嘛，

所以第一个选项是正确的时钟中断处理程序，

会把内核中的时钟变量的值更新。

然后再来看第三个选项时钟中断服务程序，

还会把呃进程在当前时间片内的剩余时间呃进行一个更新。

比如说之前这个应用程序一这个进程，

它在当前时间片内剩余的时间就只剩下50毫秒了。

那么，

在这次时钟中断信号发出之后，

时钟中断处理程序会把他的这个剩余时间。

再减50，

然后判断之后会发现哎，

这个应用程序它的时间片只剩下零，

也就是说时间片已经用完了。

那既然他的时间片用完了，

是不是就需要切换为另一个进程，

所以接下来时钟中断处理程序，

还会调用进程，

调度进程切换相关的那一系列？

功能模块，

然后接下来就会让另一个进程上CPU运行好，

那在这个中断处理结束之后CPU又会从核心态切换回用户态。

那接下来就是执行另一个进程，

相关的指令代码。

所以第三个选项显然是正确的，

第二个选项说时钟中断服务程序还会更新，

当前进程占用CPU的时间。

那这个其实也是正确的，

他这儿所谓的进程占用CPU的时间，

指的是一个总时间的统计。

比如让大家看一个例子，

如果你手里有mac苹果的电脑的话，

那你可以打开你的活动监视器看一下他这有CPU这样的一栏。

然后这儿可以看到CPU时间，

其实这个CPU时间就是指各个进程，

它总共使用了多久的CPU？

这是一个累计的CPU使用时间，

那统计各个进程占用CPU的时间有什么作用呢？

比如举一个例子。

这我们统计了CPU使用时间，

然后如果操作系统还统计了各个进程，

对磁盘的使用时间的话。

那是不是系统就可以判断某一个进程，

它到底是CPU繁忙型进程还是IO繁忙型进程，

对吧？

那知道这些信息系统是不是就可以对啊，

进程的调度算法进行优化，

比如说如果系统发现某一些进程是IO繁忙型的进程。

那它可以适当的把IO反慢性进程的优先级提高，

对吧？

当然这只是其中的某一个应用场景的例子。

其实，

统计进程占用CPU的时间，

这个参数还会有其他的一些作用。

总之，

第二个选项也是正确的。

所以这个题目一二三都正确答案选d好，

nothing
