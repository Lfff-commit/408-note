好，接下来看信号，这个考点信号是二零二五届的考研大纲当中新增的考点。信号这个术语和我们之前学过的信号量非常像，但它们俩是完全不同的两个东西。信号量英文术语叫semaphore信号，英文术语叫signal。信号量主要用于实现进程之间的同步互斥，而信号这种机制主要用于实现进程间的通信。那么，在这个视频中，我们首先会探讨信号的作用，并且会简单介绍几个linux操作系统定义的信号。紧接着，我们会探讨信号的发送与保存，

然后要探讨当一个进程，收到信号的时候如何去处理一个信号。最后，我们会探讨一个疑难点，就是信号和异常有什么关系。好，首先来看信号的作用。考研大纲把信号归为了进程间的一种通信方式，我们可以用信号这种机制通知啊某个进程。某个特定的事件已经发生，那么当这个进程收到这个信号之后，就需要对这个信号进行相应的处理。具体怎么处理？我们之后会展开探讨，那在linux操作系统当中定义了30种信号类型，

这个地方罗列的是符合posix标准的30种信号。啊，事实上，除了这30种信号之外，linux操作系统还定义了更多的信号。这张表格不用怕，我们简单了解一下即可。好注意观察这个表格当中，每一行就是对应一种具体的信号，那可以看到每个信号都有一个序号，一个编号。并且会有一个信号名。不同的操作系统对信号类型的定义是不一样的，比如说在早期的unix操作系统当中，定义了19种信号。

不过，原理和linux是类似的，每一种信号都会有一个自己专属的序号，并且有一个信号名。所谓的信号名，其实就是宏定义的常量。比如说这个地方signal int，它对应的是序号二。那么，在linux的内核代码当中，这个信号名就是用一个宏定义去实现的，本质上一个信号名的背后对应的是这个信号的序号。当某些事件发生的时候，就会触发一个信号，比如说在linux操作系统当中，

如果我们在那个小黑框上啊，正在运行一个程序。在这个程序运行的过程当中，如果我们按下control c，那么我们的这个动作就会触发一个信号，会使得操作系统的内核。向正在小黑框里边运行的这个进程发送一个signal int这个信号。那么，当这个正在运行的进程收到信号之后，它会对这个信号进行相应的处理。对于signal int这个信号来说。默认的处理就是终止当前进程。那类似的道理，如果发生一个算术异常，比如说我们写的程序当中呃，

有bug发生了，除以零这种情况。那么，这个事件会导致操作系统的内核呃向正在运行的这个进程发送一个signal f PE这个信号。fpe是floating point exception的缩写。也就是对应浮点异常，那么当这个进程收到呃八这个信号之后，接下来会对这个信号进行处理。那默认的处理就是终止当前进程，并且转储内存。所谓的转储内存，就是把当前正在运行的这个呃出现了浮点异常的进程，它的内存数据。存储到硬盘里边。这一点大家是有体会的啊，

比如说当你正在使用某一个软件，这个软件突然崩溃，那么崩溃的这些信息其实会被存储到你的硬盘当中。总之，这地方是想强调当某一些特定事件发生的时候，我们可以向相关的进程发送一个呃对应的信号。通知他哎，这个事件发生了。对linux有兴趣的同学，可以研究一下这张表，这张表引用于深入理解计算机系统。那这本书又套娃引用了这个网站，有兴趣的同学可以自己看一下，好接下来我们探讨信号，这种机制的啊，

实现原理。首先我们看信号的发送与保存啊，我们知道每一个进程会有一个呃对应的PCB。也就是进程控制块，那么在PCB当中会有这样的两个呃位向量。我们分别把它称为pending和locked。pending这个词的意思就是待处理。blocked对应的是被阻塞。假设在某一个操作系统当中，总共定义了八种信号，我们用序号一到八对应这八种信号。那么，在这种系统当中，就需要用八个比特的位向量去表示当前呃待处理的信号。同样的，

用八个比特表示被阻塞的信号。一会我们会感受到被阻塞的信号是什么意思？那这个block的位向量也可以称为信号掩码。注意观察在八个比特当中，只有末尾的两个比特等于一，这就意味着进程一，它阻塞或者说屏蔽了七八这两种信号。那一个进程具体要屏蔽哪几种信号？这个是可以通过系统调用去自行设置的。好，接下来我们看其他进程如何给进程一发送一个信号。一个最常用，最灵活的啊，发送信号的这个系统调用函数是q函数。可以看到q函数当中有两个参数，

第一个参数指明的是PID，也就是要说明我的这个信号要发给谁？第二个参数是一个int型的值。它表示的就是信号的序号好，来看一下q函数怎么工作，假设进程p1的PID。也就是进程ID是二六六六，那么两个用户进程和一个内核进程都可以调用q函数给这个进程发送一个信号。比如说进程p2调用q函数指明，我要给二六六六这个进程发送一号信号。进程三指明我要给二六六六，这个进程发送八这种类型的信号，类似的内核进程指明我要给二六六六发送一这种信号。那么，执行这三个q函数最终的效果就是进程p1，

它的pending这个位向量。信号一这个地方会由零变成一信号八也会由零变成一。也就变成这个样子，很好理解吧，这意味着进程p1收到了一号这种信号，也收到了八号这种信号。由于pending这个变量，它是一个未向量，也就是说每一种信号类型只能用一个比特来保存它的信息。所以虽然进程p2以及这的内核进程都给进程p1发送了一这个信号，但是对于进程p1来说。它只能接收一个一这种信号。后到达的信号，一会被简单的丢弃。并且从进程p1的这个位向量当中，

其实无法区分出这个信号，到底是由哪个进程给我发过来的？好，那么这是信号的发送与保存，除了q函数之外，其实还有其他的函数也可以用于发送信号，只不过q函数是最灵活以及最常用的。所以我们主要以q函数为例。好，那么从这个例子当中可以看到用户进程和用户进程之间是可以相互发送信号的，同时内核进程也可以给用户进程发送信号。那值得一提的是，呃，用户进程之间允许发送的信号类型是有限制的。比如说在Linux系统当中，

signal kill就是九这个信号可以用于杀死一个进程，比如说一个负进程可以给自己的子进程。发送signal q这个信号。但是其他的用户进程不能随意的发送这个signal q呃信号。如果每个进程都可以随意的杀掉其他进程的话，那整个系统就会产生很多错误。好，所以这一点是值得注意的，进程之间允许发送的信号类型是有限制的，当然操作系统内核要给用户进程发送信号，这个就没什么限制了。因为操作系统内核拥有最高权限好，那么在这页课件当中，我们探讨了信号是如何发送的，以及当一个进程呃，

收到一个信号的时候，这些待处理的信号信息是如何保存的？本质上是用一个恩比特的位向量来保存好，接下来我们探讨呃，这个进程什么时候会处理？收到的这些信号。很简单，每一次进程从内核态转为用户态的时候，都会例行的检查是否有待处理的信号。如果有的话，那么就处理这个信号。比如说，当一个进程从系统调用返回的时候，是不是就会从内核态转为用户态？再比如，

当一个进程收到一个中断，对这个中断处理返回的时候，是不是也会从内核态转为用户态？总之，每次从内核态回到用户态的时候，都会去例行的检查是否有待处理的信号。那具体来说是怎么检查的呢？pending和blocked是两个恩比特的位向量，首先我们会把blocked这个位向量全部为按位取反。比如说在这个例子当中，全部为按位取反就变成了六个一末位，两个是零，那按位取反之后再和pending这个呃位向量进行逐位的与运算。那显然，这些比特主谓语之后，

得到的就是呃，第一个比特等于一，然后末尾的七个比特都等于零。值得注意的是，第八个比特。虽然进程p1收到了八这个信号，但是由于八这个信号是被阻塞的，因此把blocked按位取反，并且逐位与之后。第八个比特就变成了零。好，那么根据这个运算的结果就可以判断，此时我们需要处理的是第一个信号，而第八个信号可以不做处理。好，

现在我们已经检测出了当前待处理的信号，是信号一接下来就应该处理这个信号。那处理的原理是这样的，首先操作系统会给每一个信号配备一个默认的处理程序。比如说信号一有它的处理程序，信号二有它的处理程序，信号八有它的处理程序。那由于此时需要处理的是信号一，因此接下来会运行信号一的处理程序，如果用个图来表示的话，大概就是这个样子。我们知道一个进程的执行，本质上就是在运行一些指令序列。我们假设进程一执行的过程当中，在这个时间点。

他收到了一些信号，也就发生了刚才我们呃描述的这些事情，收到了信号，一收到了信号八。那当他收到这些信号的时候，并不会立即响应这些信号，直到这个进程从内核态。转为用户态的时候才会检测，并且处理这些信号，那我们不妨假设这儿的指令k呃，使得进程转为了内核态。比如说我们不妨假设这个指令k是trap指令，trap指令会引发系统调用，而系统调用执行结束返回的时候。就会让这个进程的状态从内核态回到用户态，

那此时就会例行的检查是否有待处理的信号？那基于刚才的例子，我们说此时只有信号，一是有待处理的。接下来就应该转移到信号一的处理程序。所以三这个过程就是执行信号一的处理程序，那信号一的处理程序执行完了之后才会返回到这个进程p1。继续往下执行那下一条指令，就是指令k+1。在刚才的讲解中，我们假定指令k是一个trap指令，它引发了系统调用，而系统调用返回的时候会导致。进程的状态从内核态转为用户态，那我们不妨换一种假设，

假设这个指令k它只是一条很普通的指令。在执行完指令k之后，假设这个进程p1它收到了一个IO中断。那类似的道理IO中断信号也会导致呃进程p1从用户态先转为内核态，而这个中断的处理完成之后又会使得这个进程。从内核态要回到用户态，那么同样的，在这个时刻也需要去例行的检查，是否有待处理的信号？如果有，那么就运行这个信号对应的处理程序。好，那么通过这一页课件，我们解释清楚了，when什么时候处理信号的问题？

并且从八这个信号当中，我们感受到了呃被阻塞的信号，这个变量有什么作用？那再次强调，具体要屏蔽哪些信号，这个可以由进程自己来决定。好，那么理解了信号处理的这个时机之后，接下来要探讨具体要怎么处理信号，也就是how的问题。首先，操作系统会给每一类信号都设置一个默认的信号处理程序。在这我用signal 1 default啊，去表示信号一的默认处理程序。那回到之前，

这个表格可以看到在linux操作系统当中，对于signal int这个信号的处理是会执行终止进程相关的，这个代码逻辑。对于signal fep这个信号的处理，是会执行终止进程，并且转储内存相关的代码逻辑。不过，有的信号可能会直接把它忽略，比如说signal window change就是窗口大小变化，这种信号的默认处理就是忽略。你可以理解为这种信号的默认处理程序是一个空函数。好，所以当进程检测到一个待处理的信号的时候，它可能会执行操作系统设置的默认处理程序。当然，

我们说有些信号会默认忽略，不做任何处理。那我们可不可以不执行这种默认的信号处理呢？当然可以。操作系统允许一个用户进程，为某些类型的信号设置用户自定义的信号处理程序。比如说进程一可以通过系统调用去设置信号二的，这个处理程序。这个处理程序可以由进程一自己去写，比如说当进程一收到信号二的时候。他希望输出hello world，那么输出hello world这个动作就是用户自定义的一个处理逻辑。那这个进程自定义的处理程序会覆盖掉系统的这种默认处理。那这种机制有什么作用呢？我们举一个例子来辅助大家理解嗯，

在linux操作系统当中，当一个应用程序，它的窗口大小发生变化的时候。操作系统的内核会向这个应用程序发送signal window change，这个信号对于这类信号的默认处理就是直接把它忽略就什么也不做。但是平时我们有这样体会，假如我们在电脑上呃去去调整某一个应用程序的这个窗口大小的话。那么，这个应用程序的内部UI往往需要根据窗口大小的变化去进行动态的调整，对吧？比如说窗口缩小了，或者窗口扩大了，那么有一些按键按钮的位置就需要重新改变一下。好，所以如果窗口大小变化，

我们什么都不做的话，那么UI的动态调整就很难实现。那此时信号这种机制就发挥了作用。当窗口大小变化的时候，进程一定会收到signal window change这个信号。此时，我们可以自定义一个处理程序，我们可以规定自己开发的这个应用程序，收到window change这个信号的时候。根据窗口大小的变化去动态的调整UI。好，所以这种用户自定义的信号处理程序在做开发的时候，其实是很有用的好，那么现在我们知道了信号是如何被处理的？如果我们没有自定义信号处理程序，

那么就会执行默认的这个处理逻辑操作系统，给每一个信号都配置了默认处理程序。有的信号默认会忽略这种情况，相当于操作系统配置的这个默认处理程序当中什么代码都没有，写它定义了一个空函数。那如果有自定义的信号处理程序，那么自定义的这些程序会覆盖掉默认的处理逻辑。好，那这就是对信号的处理方法，接下来补充信号处理的一些细节问题。首先，信号处理程序运行结束之后，通常会返回进程的下一条指令继续执行。除非这个信号处理程序把进程阻塞或者终止了，那结合之前这个图相信不难理解，

当信号处理结束之后，通常会返回到进程的下一条指令继续往后执行。除非这个信号处理程序直接就把进程给阻塞了，或者直接把它终止了。好，接下来需要补充的是，一旦处理了某个信号，就会将pending位重置为零。比如说当进程p1。它处理了这个呃信号一的时候就会把这个信号一的pending位重置为零，就这个意思好，接下来也是刚才我们强调过的东西，如果重复收到同类信号的时候。重复的信号将被简单的丢弃。因为每一类待处理的信号，

我们只用一比特去记录。所以之前我们说进程p2和呃内核进程都给这个进程p1发送了一这种信号。那进程p1只会把第一次收到的信号给记录下来，第二次收到的这种重复信号就会简单的直接把它丢弃。好，最后一点当同时收到多个不同类型的信号的时候，通常会优先处理序号更小的信号，比如说进程p1，此时可能有呃信号，一信号，二信号，三。都有待处理，那在这种情况下，通常会先呃处理信号，

一紧接着处理信号，二最后信号，三就这个意思。好，接下来我们要补充强调这样的一点，就是各个进程的信号，处理程序可以是不同的，我们说操作系统给每种信号都规定了一种默认的处理程序，对吧？那么，进程一它可以呃自定义，当我收到呃信号二的时候，我希望按照我自己写的这段呃程序去进行处理。那类似的道理，假设有一个进程二，

它也可以进行自定义，比如说它可以自定义呃信号四，当我收到信号四的时候，我希望按照我自己写的这段逻辑去处理。好，所以通过这个图，想要强调的是各个进程，它自定义的这个信号处理程序只作用于它自身，而不会作用于其他进程。每个进程都可以有自己的自定义信号处理程序，如果没有自定义这个东西的话，那么它就会执行操作系统配置好的，这个默认的处理程序。好，另外从这个图当中可以看到每个进程都有自己的pending微向量以及block的微向量进程p1阻塞了七八这两种呃。

信号而进程p2，它阻塞了一二三这几种信号。所以各个进程到底要阻塞呃哪种信号，这个也是由它们自己通过系统调用去进行设置的。各管各的，互相独立。好，最后我们再来探讨信号和异常的关系，先说结论信号可以作为异常的配套机制。让进程对操作系统的异常处理进行补充。我们知道，在进程运行的过程当中，有些特殊事件可能会引发异常，那操作系统内核会捕获这种异常，并且处理这些异常。

比如说缺液异常以及整数除以零的异常。不过有些异常可以由操作系统内核完成全部的处理，最典型的例子就是缺液异常。当一个进程运行的过程当中，呃，访问了某一些缺失页面的时候，会引发缺页异常，而操作系统内核会完成缺页异常的全部处理。然而，有些异常无法由操作系统内核完成全部的处理，可能还需要用户进程的配合。对这个异常进行补充处理。比如考虑这样的一个场景，假设你开发了一个呃应用程序。就是一个简单的计算器。

你的这个计算器应用会被各种各样的人使用，对吧？假设你的舍友在使用你的这个程序的时候，总喜欢用整数除以零。那么，整数除以零的这种操作就会触发一个异常。操作系统的内核会捕获这种异常。并且在异常处理程序当中会给你的这个计算器呃进程发送signal fpe这种信号。那这种信号的默认处理，就是会把进程终止，并且转储内存，也就意味着如果你不修改这种呃信号的默认处理程序的话。那么，每当你的设有进行整数除以零这种骚操作的时候，你的应用程序就会闪退崩溃。

这是默认的呃，信号处理程序带来的效果。这种体验如何？有同学说妙极了，其实一点都不妙。显然，这种用户体验会非常糟糕。好，所以在这个时候我们就可以通过自定义的啊，信号处理程序去处理这种情况，比如说当你的设有输入整数除以零的时候，你可以在这个计算器的页面打印出错了。这个信息。就提醒他的输入有问题，而不是直接让这个程序闪退。

好，所以从这个例子当中，我们可以感受到操作系统检测到呃一个异常的时候，有些异常可以被操作系统独立的完成全部处理。而有些异常比较复杂，可能需要根据这个应用程序的实际需求去进行个性化的处理。那么，在这种情况下，就可以使用信号机制对操作系统的异常处理进行个性化的补充。好，所以信号这种机制在开发当中还是很常用的好，那么在这个视频中我们探讨了信号，这个在二零二五届才新增的考点。信号的作用是可以用于通知进程，某个特定的事件已发生在考研大纲当中，

把信号这种机制归为了进程间通信的一种机制。那经过我们的拓展和补充，我们也看到了信号呃，也常作为异常处理的一种配套机制。一个进程可以调用q函数给另一个进程发送信号，只不过需要指明接收进程的PID。以及信号的序号。一个进程可以给另一个进程发送信号操作系统的内核，也可以给进程发送信号，同时一个进程也可以给自己发送信号。那当一个进程，收到一个呃待处理的信号的时候，会先把这个信号保存下来，具体来说，在每个进程的PCB当中，

会用两个n比特的位向量去表示待处理的信号。以及被阻塞的信号。被阻塞的信号不需要处理。那什么时候去处理这个信号呢？具体来说就是当进程从内核态转为用户态的时候，就会例行的检查是否有待处理的信号。如果有的话，那么就进行信号的处理。那具体来说又如何处理呢？首先，操作系统会给每一类信号都配置好呃，默认的处理程序。当然，有的默认信号处理程序是空的，也就是会直接把这个信号给忽略什么也不做。

而有的信号会有具体的呃默认处理动作，比如说默认终止这个进程。那除了默认的这种处理程序之外，操作系统也允许用户进程去自定义一个呃信号处理程序。如果有自定义的处理程序，那么就不再执行默认操作。也就是说，自定义的处理程序会把默认的处理程序给覆盖掉。好，最后有几个信号相关的特点，可能会在概念题当中进行考察，首先不同的操作系统对信号类型的定义是各不相同的。这个很好理解，另外当一个进程重复收到同类信号的时候，同类的重复信号将被简单的丢弃。

因为我们只用一个比特去记录一类待处理的信号。这是由数据结构的特性所限制的。好，最后需要强调的是，有些信号既不允许呃被用户自定义处理函数，也不允许被阻塞。比较典型的就是linux当中的signal kill以及signal stop这两个信号，从前面这一页课件当中也可以看到这一点。signal stop.以及signal q。这两个信号是不允许被阻塞，也不允许被用户自定义啊，处理函数的。好，那这是最后需要强调的东西，

好的，那么以上就是信号这个考点的全部内容。
