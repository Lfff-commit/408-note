各位同学大家好，在这个小节中我们会学习另一个经典的进程，同步互斥的问题，读者写者问题。这个问题的描述是这样的，在系统中有读者进程和写者进程这样的两种进程，读者进程可能有多个写者进程，也有可能有多个这些进程，在系统中并发的运行。那他们会呃访问一个共享文件，读者进程就是不断的读这个共享文件，而写者进程就是不断的写这个共享文件。那由于读和写这两个操作，对这个文件的呃影响是不一样的，所以对于读者和写者这两种进程，

我们有不同的呃要求。有这样几个要求，第一个要求我们可以允许多个读者同时来读这个文件。第二个要求，在同一时刻，我们只允许一个写者进程来写这个文件。第三个要求，如果说此时有某一个写者正在进行写操作，那么在这个写操作完成之前是不允许其他的读者或者写者。来对文件进行操作的，那第四个要求就是一个写者在写操作执行之前需要保证其他的读者和写者。都已经全部退出，也就是要保证没有别的进程，正在访问这个文件，那这个写者才可以啊，

对这个文件执行写操作。那我们来分析一下，为什么有这样的一些要求啊？首先来看为什么可以呃多个读者同时读文件。等大家学过第四章，会知道一个文件由一条一条的记录组成，那记录是什么概念？我们现在暂时不需要关心我们，只需要知道一个文件，它是由一坨一坨的数据组成。那么，这个文件可能同时会有多个读进程，想对它进行读操作，那由于这两个读进程在读这个文件的时候并不会更改啊，这个文件的这些。

数据的信息，所以多个读进程，同时读这个文件是可以被允许的，那这个地方需要注意一下这儿的读进程和我们呃，刚开始学过的生产者，消费者那种消费者进程是不一样的。不一样的消费者进程，他在消费一坨数据的时候，他会把这坨数据取走。也就是说，消费者进程的这个操作是会影响这些数据的。但是，读者进程和消费者进程不一样，他在读一个数据的时候，并不会把这坨数据给清空。

也就是说，他并不会改变这个共享文件的数据。所以我们可以允许多个读者进程来同时访问这个共享文件，那这样的话我们就理解了第一个要求。那我们的第二条，第三条和第四条要求其实呃，概括起来就是说当一个写者进程正在对文件进行写操作的时候。其他进程是不能访问这个文件的，或者换一个角度来讲，当一个写进程，想要写这个共享文件的时候，他必须先等到其他进程对这个文件的操作结束之后。他才能往里边开始写数据，那我们来看一下为什么那假设此时现在这个共享文件有这样的四坨橙色的数据，那一个写者进程，

他此时在系统当中运行。并且这个写者进程，他打算往这个位置来写一坨数据，那由于写者和读者进程在系统中是并发运行的。所以当他们俩并发运行的时候，有可能发生这种情况，这个读者进程他先读走了这个共享文件的前三坨数据，他取到的都是他想要的这个橙色数据。但是当他要读第四坨数据的时候，这个时候切换成了这个斜者进程，而这个斜者进程往里边写入了一坨新的数据，变成了一块绿色的数据。而接下来，这个读者进程，他往外读的第四块数据就变成了写者进程。

刚刚往里边写的这一块新的数据，所以。本来他想读的是那坨呃橙色的数据，但是如果他们俩并发运行的话，那么有可能读者进程他读到的并不是他想要的那块数据。因此，由于写者进程，它会改变这个呃共享文件的内容，所以写者进程是不可以和其他进程同时访问这个共享文件的。那同样的道理，我们再来分析一下，如果两个斜者进程并发运行，会发生什么情况？假设这两个斜者进程啊，此时并发运行，

并且在同时访问这个共享文件的话。那么刚开始写者一上处理机运行，他发现这个共享文件这个位置还空着，可以往里面写数据。那此时如果又切换回写者二这个进程，这个进程他也发现共享文件的这个位置是空的，也可以往里边写数据，那由于这两个写者进程都以为这个位置是空的。所以写者一会往这个位置写入自己的数据，而写者二同时也会把自己的数据写进去，因此就会发生后面那个写者把前面那个写者的数据给覆盖掉，这样的问题。所以如果两个写进程，同时对这个共享文件进行呃写操作的话，那么也会出现呃数据覆盖数据不一致的问题。

那讲到这，大家应该就可以理解，为什么要设置这样的一些限制了？那接下来我们要做的事情就是尝试用pv操作来解决这个问题，还是和以前一样，我们第一步是要分析这个题目当中这些进程的关系。看看它们之间是否存在同步或者互斥的关系，那很显然呃，经过刚才我们的分析，我们知道对于共享文件这个呃共享资源的访问。读者进程和读者进程是可以同时访问的，也就是读者和读者之间不存在互斥。但是，写者进程和写者进程之间需要互斥的访问共享文件。同时，

写者进程和读者进程之间也需要实现互斥。那这种互斥关系比我们之前接触到的那些互斥关系都要复杂的多，那我们怎么用pv操作来实现啊？这个互斥关系呢？那接下来我们要考虑的就是要怎么样设置信号量，然后用pv操作来解决这个问题，我们设置一个叫做rw的呃，互斥信号量用于实现。啊，各个进程对于共享文件的互斥访问，那写者进程他要做的事情就是不断的写文件，而读者进程他做的就是不断的读文件。那由于写者和读者之间需要互斥的访问呃文件，这个共享资源，所以写者在写文件之前需要对。

rw这个互斥机信号量执行p操作也就是啊，进行了加锁的动作，然后写完文件之后再解锁。呃，读者进程也一样，在读之前需要加锁，读之后把它解锁，这样的话我们就可以实现读者和写者之间互斥的访问这个文件，这个事情了。但是如果我们只是简单的在读文件之前加锁，读完了文件之后解锁，那么这种方案。呃，会导致读者和读者之间不可以同时访问这个共享文件。那这个问题怎么解决呢？

我们可以设置一个叫做count的变量，这个变量它记录了当前有几个读进程，正在访问这个文件。那count的初始值是零，就意味着刚开始并没有读进程。在读这个文件，那当一个读进程要对这个文件进行加锁动作之前。他需要进行一个检查，看一下自己是不是第一个想要读这个文件的进程。如果自己是第一个读进程的话，那么他就会执行这个p操作啊，执行加锁。之后呢，要对count的值进行加加的操作表示，此时正在访问文件的进程数量加一。

而当一个读进程读完了文件之后，它需要对count的值进行减减的操作表示，此时访问这个文件的呃，读进程数减一。那之后再对这个count的值进行一个判断，如果当这个count执行减减操作之后，count的值变为了零，那么就说明此时已经没有别的读进程，正在读这个文件了。那这种情况下就说明啊，我这个读进程是最后一个正在读文件的进程，那当我读完了这个文件之后，我需要把这个文件进行一个解锁。那用这种逻辑就可以实现读进程和读进程之间可以同时来访问这个共享文件这个事情。因为只有第一个读进程，

才需要对这个文件进行加锁，才会执行p操作，而只要此时有一个读者正在读这个文件。也就是说，只要此时count的值他大于零的话，那么接下来的读者进程就不会再对这个互斥信号量执行p操作，就不会再进行加锁，他可以直接跳过这个条件判断。而直接进行count加加操作。所以由第一个读进程负责加锁，由最后一个读进程负责解锁，这个方案看起来似乎是可以实现我们读者和读者之间可以同时共享这个文件，这件事情的。不过嗯，这个方案也存在一个问题，

我们来深入思考一下，如果两个读者进程，他们并发的执行的话。刚开始如果count的值是零，那么。读者一，当他在执行到这个判断语句的时候，发现此时count的值是零，所以这个条件满足。因此，读者一接下来会执行这个p操作。那如果此时切换成了读者二进程，那这个进程他在执行这句条件判断的时候也会发现count的值，此时是零，所以这个条件也满足。

因此，读者二接下来也会啊，来执行这个p操作，所以读者二对rw执行了p操作之后。rw的值就变为了零，那接下来如果再切换回读者一这个进程的话，它在执行这个p操作的时候嗯，会发现rw已经为零。这个读者一就会被阻塞在这个地方，因此这种情况下，虽然读者二已经可以开始读文件了，但是读者一又被阻塞在了啊这个。呃p操作这。那导致这个问题的原因在于，我们对count变量的检查和赋值这两个操作没有办法一气呵成。

当一个读进程对count的值进行检查之后，有可能切换成第二个读进程，它也进行检查操作，那这样的话就啊会出现问题。那为了解决这种无法一气呵成的问题，我们应该很自然的想到，我们可以让各个读进程互斥的来访问count这个变量。所以我们可以再多增加一个互斥信号量，叫mutex用于保证对这个count的互斥访问，然后再对count进行呃操作，之前执行p操作，操作完了执行v操作。那下面这个地方也一样，那我们来看一下，加了这个呃mutex互斥信号量之后，

情况会变成什么样？假设刚开始是读者进程，一呃上处理即运行，那当它执行这个p mutex之后，mutex的值就从一变为了零。而读者进程并不会被这个p操作阻塞，他会继续往下执行这个if判断，然后他发现这个count的值为零，接下来他又会执行对rw的p操作。那此时假如说呃，这个。读者进程二，被调度上处理机运行了，那读者进程二，它对这个mutex执行p操作的时候，

由于它的值是零，因此读者进程二会被阻塞在这个地方。阻塞在这个p操作这所以由于此时读者一他还没有完成对count的访问，因此读者二暂时不能进入这个mutex对应的临界区。而直到读者一把，这些p操作count减减这些操作都做完，并且对mutex执行了v操作之后，读者进程二才有可能被唤醒，然后。继续进行下面的判断，因此当我们加了mutex这个呃互斥信号量之后，各个读者进程，他们对count的呃访问就是互斥的。同一时刻，只有可能有一个读者进程正在对这个count执行检查和赋值的操作，

那这就避免了我们刚开始分析的这个问题。那讲到这儿，这个算法就呃，没有什么特别大的问题了，不过在这种情况下依然会存在一个潜在的问题。就是只要有毒进程还在毒的话，那血进程就会一直阻塞，等待导致血进程饿死的现象。所以我们这儿实现的这个算法当中呃，我们可以认为读进程，它是有更高的优先权的。我们来分析一下这儿所说的这种场景。假设此时系统中有一个读进程，它正在读这个文件，那么此时count的值。

是一而此时，如果有一个写进程到达的话，那由于第一个读进程已经对这个rw执行了p操作rw的值变成了零，因此写进程在对这个rw。进行p操作的时候会被阻塞在这个呃信号量上，因此它会阻塞在这个地方，没办法往下执行。那此时如果有读进程到来的话，由于呃，此时count的值是一不等于零，所以这个读进程可以跳过这个p操作。它可以呃，直接往继续往下执行，然后开始读文件。同样的，

如果有源源不断的读进程到来的话，他们都可以跳过这个p操作，都可以开始读这个文件，所以如果一直一直有这个读进程到来的话。那么，由于只有最后一个呃读进程，读完了之后才会对这个rw互斥信号量执行v操作，它才会解锁。所以在最后一个读进程，读完之前这个写进程，它会一直被阻塞在rw这个互斥信号量这。那如果说我们有源源不断的读进程，一直到来的话，也就是说这个文件永远都有呃读进程正在读，那么这样的话就永远不会对rw这个互斥信号量进行解锁。

所以导致了血进城，他饿死的现象，那我们怎么解决这个血进城饿死的问题呢？我们可以再设置一个互斥信号量w啊，这个题目的信号量确实有点多，这个信号量是用于实现写优先。然后我们可以在这两个位置分别对w执行p操作和v操作。那我们来看一下这个pw是怎么工作的？那来看第一种情况，假设刚呃是两个读者进程正在并发的运行。那如果第一个读者进程，他在啊，这两个pv操作之间啊，进行了进程调度切换成了第二个读者进程的话，那么这个读者进程他会被阻塞在w这个互斥信号两这。

直到第一个读者进程，对呃w执行了v操作嗯，第二个进程才有可能被唤醒，并且第二个读者进程在这个地方和这个地方，这两个p操作这都不可能被阻塞。所以这样就可以保证读者一和读者二，他们是可以同时读文件的。那再来分析第二种情况，假设两个斜者进程，它们并发的运行第一个斜者进程在写文件的时候，这个w和rw肯定都是零，它们的值都是零。所以第二个写者进程，他肯定会被阻塞在w这个呃互斥信号量这儿，直到这个写进程写完文件，

并且对w执行了v操作之后，第二个写进程才有可能来写这个文件。所以两个写进程之间并发运行是可以实现互斥的，写文件的，他们并不会同时访问这个文件。再看第三种情况，如果是一个写者和一个读者啊，他们并发的运行，那假设刚开始上处理机运行的是斜者，当这个斜者正在写文件的时候w这个互斥信号量的值肯定是零。因为他在前面进行了p操作，而此时如果有一个读者想要尝试读文件的话，他首先要做的第一步肯定是对这个w执行p操作。那由于它的值是零，所以这个读者进程会被阻塞在这儿，

直到写者进程对w执行了v操作之后，读者进程才有可能开始访问这个文件。所以写者和读者之间互斥访问这个文件也是可以实现的。再看第四种情况，假设刚开始是一个读者进程上处理器运行，那么这个读者进程进行了前面的这一系列操作之后，他开始读这个文件。那由于读者进程在读文件之前对w这个互斥信号量先执行了p，再执行了v。所以当这个读者进程，在读文件的时候w的值是等于一的。那此时如果切换回了斜者进程的话，由于w的值等于一，所以斜者进程在这个p操作，这不会被阻塞，

它可以顺利的跳到下一步。对rw执行p操作，而由于此时rw这个互斥信号量已经被第一个读者进程执行了p操作了，它的值是零。所以斜者进程，它会被阻塞在rw这个互斥信号量这儿，而由于这个斜者，他对w这个互斥信号量已经执行了p操作，因此此时w这个互斥信号量的值。变为了零。而不是一。那此时如果还有一个读者进程，他要上处理机运行，那当他对w这个互斥信号量执行p操作的时候。由于此时w的值已经是零了，

所以这个读者进程它会被阻塞在这个地方下不去。因此，这样的话，这个斜者和第二个读者都会被阻塞。斜者是阻塞在这个地方，而第二个读者是阻塞在了这个地方。那在这种情况下，只有读者一可以顺利的运行，那读者一当他读完了文件之后，他会对count进行减减。那由于此时只有这一个读者正在读文件，所以当count减减之后count的值就变为了零。因此，读者一发现他自己是最后一个正在读文件的啊读者，所以他会对rw执行v操作，

而对rw的v操作。可以唤醒这个斜者进程，因为它是被阻塞在rw这个互斥信号量上的，因此这个微操作导致斜者进程被唤醒。因此，写者进程接下来就可以顺利的往下执行，然后开始写这个文件。但与此同时，这个读者二它又被阻塞在了w这个位置。所以在这个例子当中，我们可以看到嗯，在增加了pw和vw这两个操作之后，如果刚开始一个读者啊，先开始读文件。那么，

在他读文件的过程中，如果是写者，在读者之前到来，那么接下来是这个写者先被唤醒。而只有当这个写者写完文件，并且对w这个互斥信号量执行v操作的时候，它才会把读者二唤醒，因为读者二是阻塞在这个地方的。所以我们增加了pw和vw这两对呃pv操作之后，我们就呃解决了之前所提到的邪者一直饥饿的问题。如果按之前没有这个pwvw的那种方案来看的话，当第二个当第二个读者到达的时候，管你此时有没有写者正在等，待我肯定都可以顺利的进入开始读文件。所以这个地方大家可以用这个例子来认真的体会一下这两对pv操作是怎么解决，

写进程饥饿的问题的。那再来看最后一种情况的例子，假设刚开始是一个斜者上处理机运行，那斜者在写文件的时候w和rw它们的值都是零。那此时如果有一个读者上处理机运行，那这个读者对w执行p操作的时候，由于这个w的值此时是零，因此它会被阻塞在呃pw这个地方。那此时如果还有第二个斜者进程上处理机运行的话，第二个斜者进程也会被阻塞在pw这个位置。那在之前信号量机制那个小节当中，我们讲到过信号量除了这个数字表示资源之外，它还有一个功能，就是排队的功能。由于这个读者先对w执行了p操作，

所以这个读者他会排在w这个互斥信号量后面的这个队列当中。它处于对头的位置，而接下来这个斜者进程二。它是之后对w执行p操作的，所以写者进程二，它会被排在读者进程之后，因此当第一个写者在写完文件，并且对w这个互斥信号量执行v操作的时候。它唤醒的其实是读者，而不是之后到来的写者。因为读者排在这个w信号量的对头，而写者是之后的一个之后才会被唤醒。所以可以看到，在这种算法当中，其实我们并不是真正的实现了写优先，

而是相对公平的呃，实现了这种先来先服务的原则。所以在有的书上也把这种算法称为读写工频法，那如果要真正的实现写优先的话，又应该怎么实现呢啊？这个有兴趣的同学，大家可以自己上网搜一下。网上有很多答案，但不管是什么方案，大家一定要能够耐下心来，自己来呃推一下，自己来分析一下这些pv操作，它到底是怎么实现？啊，我们之前所说的那一系列的要求的那读者写者这个问题啊，

可以给我们以后。解决复杂的互斥问题，提供一种思路。读者和读者之间不互斥。而写者和其他进程都互斥，这个事情实现的关键啊，在于我们设置了这样一个。计数器count。当一个读者要对这个共享文件进行访问之前，都需要先对这个计数器count的值进行一个判断。如果发现自己是第一个读者进程的话，那么他们会对rw那个互斥信号量执行p操作，也就是说对。这个共享文件，这个资源进行上锁。

而当读者读完文件之后，当他离开的时候，同样也需要对count的值进行一个判断，如果自己是最后一个正在读文件的进程的话，那么最后一个进程。需要对呃共享文件进行一个解锁。所以我们利用cunt计数器实现了读者和读者之间不互斥的问题。但同时，我们又可以保证写者和其他进程之间互斥，包括写者和写者，互斥写者和读者也互斥。另外，还需要注意的是，我们刚开始那个方案当中，由于对count变量的检查和赋值不能一气呵成，

所以导致了呃两个读者进程不可以同时读文件这个事情。所以，为了实现一气呵成，我们想到了用一个互斥信号量，用pv操作，把它给包起来。那这种用pv操作实现一气呵成的这种思路，也是我们非常值得借鉴的。那这个地方的一气呵成，指的并不是说在对count变量访问的时候不会进行进程切换。进程切换依然是会，但是我们用pv操作可以保证呃，各个进程对count的访问肯定是互斥的，同一时刻只有一个进程。再对这个count的变量进行检查和赋值操作。

所以这是第二个值得大家吸取的一个呃精华所在。那最后我们还要认真的体会我们是怎么用w那个互斥信号量解决写进程饥饿的问题的。那我们在考研当中遇到的绝大多数pv操作的大题，其实都可以用我们之前学到的生产者，消费者的那种思路来解决。不过有的时候我们确实有可能会遇到像读者写者这种很复杂的互斥的问题。当我们遇到同步问题的时候，我们更多的应该参考的是这个生产者，消费者这种模型的思想，而当我们遇到复杂的互斥问题的时候，我们应该想到的是啊，这个读者，写者问题。想一想它是怎么用这个计数器count来实现复杂的互斥规则的？好的，

那么以上就是这个小节的全部内容。
