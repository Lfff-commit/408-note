好来看，二零二五考研大纲当中新增的考点多，处理机调度所谓的多，处理机就是多核的CPU，每一个CPU都可以独立的运行一个进程。那么，与单处理机的呃调度相比，在多处理机这种情况下，我们会面临哪些新的问题呢？在多处理机调度中，我们需要追求哪些新的调度目标？这是我们首先要探讨的问题。那基于第一个问题的探讨，我们会提出两种解决方案，分别是公共就绪队列和私有就绪队列。

好，我们先探讨与单处理机相比。呃，多处理机调度需要考虑的新问题，我们知道在单处理机这种环境下，整个系统中只有一个CPU。在这种情况下，我们设计调度程序的时候，只需要考虑一个维度的问题，就是采用某一种策略，比如说先来先服务，或者最短作业优先，或者时间片轮转，以及优先级调度等等。反正就是采用某一种特定的策略，

根据这种策略来确定到底让旧序队列当中的哪一个进程优先上处理机运行。那这就是单处理机调度要解决的问题。那相比于单处理机调度，多处理机调度还需要考虑第二个维度的问题，因为除了按照调度算法去选择一个优先级更高的进程之外。我们还需要决定让这个进程上哪一个CPU去运行好，那么基于多处理机调度的这种呃新特性。我们需要追求两个新的调度目标，分别是负载，均衡以及处理机亲和性。首先，负载均衡很好理解，就是我们应该尽可能的让呃，这些CPU都同等忙碌。比如说在一段时间内CPU 1的负载是50%。

CPU 2，CPU 3的负载是80，而CPU 4它的负载很高，到了95%，那么在这种情况下，显然CPU 1太闲了，而CPU 4又太忙了。那么，在进行进程调度的时候，我们就应该去考虑解决这个问题，尽可能的让CPU负载均衡。比如我们可以考虑让CPU 1呃t cpu 4多分担一些任务，那具体要怎么做？我们之后会探讨。好，

这是第一个，我们需要追求的目标，负载均衡。那第二个需要追求的目标叫做处理机亲和性。我们需要尽可能的让一个进程调度到同一个CPU上运行，以发挥CPU中缓存，也就是cache的作用。好来解释一下，我们知道每一个CPU内部都有各自的cache，也就是各个进程的呃数据缓存。假设CPU 4的这个cache当中保存了呃p4p8p12和p16这几个进程的数据缓存。那么，在这种情况下，如果我们能够尽量的保证啊，这几个进程尽可能的在CPU 4上面运行。

那么就可以最大程度的发挥这个cache的作用，可以让这些进程的运行速度得到提高。那相比之下，假设CPU 1里边不存在p4的这个数据缓存。那么，假如我们在进行进程调度的时候，把p4进程调度到CPU 1上面运行。那么p4进程在刚开始运行的时候就无法从这个cache当中找到自己的数据副本，那这样的话就会使得p4在CPU上运行。需要有更多的访问主存的动作。而访问主存肯定要比访问cache要慢得多，因此在多处理机调度当中，我们应该追求处理机亲和性。尽量的让进程调度到同一个CPU上运行。那具体怎么做？

我们一会展开探讨。总之，现在我们明确了与单处理机调度相比啊，多处理机调度当中需要额外考虑的两个问题。接下来，我们提出两种不同思路的解决方案。第一种方案是采用公共就绪队列。好如图所示，所谓的公共就绪队列，就是说把系统内所有的就绪进程。都放在一个统一的就绪队列当中。那这儿所谓的p1P2P3p4这些东西，本质上指的是各个进程的PCB。我们把当前就绪进程的PCB用队列的形式把它们呃连在一起。显然，

就序队列应该位于内河区。好，接下来每个CPU空闲的时候都会运行调度程序，而调度程序会根据某一种调度算法，从公共就绪队列当中。选择一个进程，让它运行，那调度算法的原理和单处理机调度是一样的，比如说我们可以采用呃，先来先服务的策略。或者短作业优先的策略以及时间片轮转的策略等等，那无论采用哪种策略，总之就是在这个呃公共的就绪队列当中。去选择一个进程，让它上处理即运行好，

我们不妨假设呃，在这个系统当中采用的是优先级调度。那么，当CPU 1它空闲的时候，会运行调度程序，假设当前所有的这些就绪进程当中。进程p1的优先级更高，那么接下来CPU 1就会运行p1这个进程。好紧接着，假设p4进程也空闲了，那么它也会运行调度程序。假设在现有的这些就绪进程当中，优先级最高的是p4进程。因此，接下来CPU 4就会运行p4进程。

那由于这些CPU都是并行运行的，也就是说多个CPU有可能会同时运行调度程序。而每个CPU运行调度程序的时候，都会去访问这个公共的就绪队列。显然，我们需要确保这些CPU对公共就绪队列的访问是互斥的。每个CPU在进行进程调度的时候，需要对这个呃就绪队列上锁。这个不难理解吧，假设CPU 1和CPU 4都在运行调度程序，那如果它们的调度程序。不给就绪，队列上锁的话，是不是就会导致两个CPU同时去访问这个就绪队列？此时在这个就绪队列当中，

优先级最高的是p1进程，那么CPU 1的调度程序会把p1进程。调度到CPU上面，与此同时，CPU 4的这个调度程序也会把优先级最高的p1进程调度到CPU 4这里边。那显然，这种情况是错误的，对吧？好，所以调度程序在访问公共就绪队列的时候必须要上锁，一定要保证互斥访问。好，那这是这句话想要强调的东西。那公共就绪队列这种实现方案，它的优点就是可以天然地实现负载均衡。

因为每一个CPU 1旦空闲，它一定会主动的运行调度程序。而调度程序一定会从啊，公共的就绪队列当中选择一个进程啊，上这个CPU运行。所以并不会出现某一个CPU一直空闲，而其他CPU一直在忙碌的情况，那这是它的优点。那另一方面，它的缺点是各个进程可能会频繁的切换CPU运行，也就是处理机的亲和性不好。比如说p1进程，目前正在CPU 1上面运行，那么下一次p1进程被调度的时候，有可能被调度到CPU 2，

也有可能被调度到CPU 3上面。所以进程可能会频繁的切换CPU。好，那这是公共就绪队列的缺点。那如何去提升处理机的亲和性呢？有两种方案，首先是软亲和的方案，就是让调度程序尽量的去保证这种亲和性。比如说操作系统的调度程序，尽量的去呃，让p4进程上CPU 4运行，而p1进程尽量的让它呃，总是调度到CPU 1上面。调度程序只能尽量的去保证这种亲和性，也就是说在某些不得已的情况下，

呃，一个进程，它也有可能会换一个CPU去运行。因此，我们把这种亲和性称为软亲和。那有软亲和，就有硬亲和。所谓的硬亲和，就是呃，用户进程可以通过系统调用，主动的要求操作系统。给自己分配一个固定的CPU，确保这个CPU的亲和性，比如说p4进程的程序员在写代码的时候可以用系统调用。去要求操作系统确保硬亲和，

那么操作系统根据用户进程的要求，接下来p4进程，它每一次被调度的时候一定都是呃调度到CPU 4上面。那这就是所谓的硬亲和好，所以这是采用公共就绪队列的时候，我们确保亲和性的两种方式。那接下来我们提出第二种方案，叫做私有就绪队列顾名思义。操作系统会给每一个CPU配一个就绪队列。当CPU空闲的时候，它总是会从自己的私有队列当中调度一个进程来运行。好比如说CPU 1，此时空闲，那么它会运行调度程序。这个调度程序只会检查自己的私有队列。

从私有队列当中挑选一个优先级最高的进程，比如说PE进程让它上CPU 1运行。那类似的另一个CPU，假设CPU 4此时空闲，那么它会运行调度程序，调度程序会从自己的私有队列当中。挑选一个优先级最高的进程，比如说p4进程，让它上CPU 4运行。好，那么基于私有队列这种方案啊，我们如何去实现CPU之间的负载均衡呢？可以用两种策略实现，分别是推和拉。先来看推的方式。

推迁移push所谓的推迁移策略，就是会有一个特定的系统程序，它会周期性的检查各个CPU的负载。比如说在某个时刻，这个特定的系统程序，它呃在CPU上面运行。这个程序会检查呃，这几个CPU在最近一段时间内的负载情况。假设CPU 1的负载比较低，CPU 4的负载比较高，那么当系统程序发现了这种负载不均衡的情况的时候。它会把CPU 4的就绪队列当中，某一些就绪进程把它推到CPU 1的就绪队列里边。比如说系统程序把p16这个就绪进程推到CPU 1的就绪队列当中，那么经过这种进程任务的呃重新分配之后。

CPU 1的负载会变成75 CPU 4的负载会变成70，那此时各个CPU的负载就比较均衡了。那这就是推迁移的策略，这种策略有点类似于呃，整个系统当中它会有一个包工头，专门负责派活。那刚才提到的这个特定的系统程序就是包工头的作用，它会周期性的检查各个CPU，他们干活的这个负载情况怎么样？如果某一些CPU它的活特别重，有些CPU又一直在摸鱼，那么这个包工头就会用推迁移的策略进行负载均衡。好，那这是推迁移的策略，实现负载均衡，

接下来再介绍第二种策略，叫做拉迁移。如果采用这种策略，那么每个CPU它运行调度程序的时候，这个调度程序会周期性的检查。自身的负载以及其他cpu的负载，什么叫周期性的检查呢？比如说在一般情况下呃，调度程序的运行。只会检查自己的这个就绪队列，从自己的就绪队列当中选择一个优先级更高的进程，让它上处理即运行。那我们可以这样规定，比如说每调度十次，我就要检查一次自身的负载以及其他CPU的负载。

如果发现其他CPU太忙，而自己太闲的话，这个调度程序就会主动的拉一些进程到自己的就绪队列当中。好比如说，此时CPU 1发现自己的负载只有50%，而CPU 4的负载太高，那么CPU 1的调度程序就会主动的把CPU 4的。某一些就绪进程拉到自己的就绪队列当中。那这就是拉迁移的策略。那拉迁移的策略有点类似于呃，这几个CPU他们是一群互帮互助的同事，如果看到其他同事很忙的话，就会主动的把活揽过来。为其他人分担任务。好，

那么这就是基于私有就绪队列实现负载均衡的两种方式，分别是推迁移和拉迁移。那解决了负载均衡问题之后，接下来我们要解决第二个目标，也就是如何实现处理机的亲和性。事实上，私有就绪队列这种方式，天然的实现了处理机的亲和性。很好理解吧，因为在一段时间内，一个CPU它总是呃去调度自己的就绪，队列当中的这些进程。所以这些进程在一段时间内总是上同一个CPU运行，那这样的话就可以发挥cache的作用。当然，

在某些情况下，一个进程有可能会切换CPU，比如之前我们提到p16这个进程，原本是在CPU 4上面运行的，但是因为CPU 4的负载太高而被。呃，迁移到了这个CPU 1上面运行，那这样的话就会牺牲一些处理机的亲和性。那如果程序员想确保呃，这个亲和性的话，同样也可以采用硬亲和的方式。也就是可以采用系统调用呃，告诉操作系统，我的这个进程让它一直在一个特定的CPU上面运行，我不希望它在运行的过程当中发生迁移。

好，所以在私有就绪队列的这种方案当中，程序员同样可以采用系统调用的方式去保证硬亲和。好的，在这个视频中，我们探讨了二零二五届考研当中新增的考点。多处理机调度。与单处理机调度相比，在多处理机调度当中，我们需要考虑额外的两个目标，分别是负载，均衡和处理机亲和性。负载均衡就是我们应该尽可能的让每个CPU同等的忙碌，而处理机亲和性就是尽量的让一个进程调度到同一个CPU上运行。这样可以发挥CPU缓存的作用，

我们提出了两种不同的思路，分别是采用公共就绪队列和私有就绪队列。公共就绪队列意味着所有的CPU都共享同一个就绪队列。每个CPU运行调度程序的时候，都会从公共就绪队列当中选择一个进程来运行。那为了确保安全呃，每个CPU在访问公共就绪队列的时候必须上锁。那公共军事队列这种方式可以天然地实现负载均衡，那与此同时，它的亲和性不太好。那为了提升处理机亲和性，我们提出了软亲和和硬亲和两种方案，这两种方案在现实应用当中通常是混用的。所谓软亲和，就是让呃调度程序尽量的保证亲和性。

另一种方案叫做硬亲和，就是由用户进程通过系统调用，主动的要求操作系统给自己分配一个固定的CPU。好所以软亲和是由操作系统的调度程序，尽可能的去保障，而硬亲和是由用户进程主动的去提出。好，那这是提升处理及亲和性的两种方式。接下来，我们又介绍了私有就绪队列这种方案。每个CPU会有一个私有的就绪队列。每次发生调度的时候，总是从自己的这个就绪队列当中选择一个进程进行运行，那这种方式天然的实现了处理机的亲和性。在私有就绪队列这种方案当中，

我们需要重点关注如何实现负载均衡，分别是推迁移和拉迁移。我们说，推迁移的这种方式有点像是操作系统，会安排一个包工头。让这个包工头周期性的检查各个处理器的负载，如果负载不均衡，那么就从忙碌的CPU就绪队列当中推一些进程到。空闲的CPU的就绪队列当中。那相比之下，拉迁移这种方式就是一群团结友爱的同事，如果一个CPU发现自己的负载很低，而其他CPU的负载很高。那么就会主动的从高负载CPU的就绪队列当中拉一些就绪进程到自己的就绪队列当中。那这是私有就绪队列，

实现负载均衡的两种方式。好的，那以上就是多处理机调度这个考点的全部内容。
