此字幕由AI自动生成

接下来，

新考点锁锁是用于实现互斥的一种方法，

你可以简单理解为锁就是一个布尔型的变量。

只有true和FALSE或者零和一两种状态分别表示，

当前已上锁或者没有上锁。

有这样的两个函数可以操作锁。

acquire这个函数就是上锁，

获得锁而release可以释放锁好，

那用锁实现互斥的主要缺点就是盲等。

如果进不了临界区，

就会不断的while循环被卡在这儿等，

这就会导致CPU资源的浪费，

违反了让权等待的原则。

由于盲等的过程中需要不断的循环检查，

因此这类的锁通常把它称为自选锁，

自己在原地旋转跳跃，

闭着眼。

像我们之前学过tsl指令swap指令以及单标制法，

这些都属于自选索，

那无论是哪种自选索？

总之，

都是用于解决进程互斥的问题的同时，

所有的这些自旋锁，

包括这儿介绍的互斥锁，

都有盲等的问题。

都违反了让权等待的原则。

那值得一提的是，

如果一个进程在盲等，

暂时进不了临界区，

那么它并不会一直占用呃处理机。

如果这个进程，

它的时间片用完，

那调度程序依然会让它下处理机，

所以并不是盲等，

就会一直占用处理机，

它的时间片还是会用完的。

那这种盲等的特性，

其实也有它的优点，

因为在等待锁的期间，

不需要切换进程的上下文，

切换进程的上下文，

其实代价还是蛮大的，

对吧？

所以在多处理器的系统当中，

如果对临界区上锁的时间很短，

那么这种自选等待其实代价反而会很低。

在多处理器的系统当中，

如果某一个进程p1。

此时正在自选等待，

那么他只会吃掉这一个核的呃，

一个计算能力，

那如果此时。

上锁的进程p2也在某一个核心里边运行，

那p2是不是很有可能在接下来快速的使用好临界区，

然后快速的解锁，

解锁了之后这个p1。

在自选等待的过程当中，

是不是突然就会发现哦解锁了？

那它是不是就可以顺利的进入临界区？

而这个自旋盲等的过程，

由于没有发生进程切换，

因此盲等的代价反而是很低的，

所以自旋锁的这种策略。

通常会用于多处理器的系统当中呃一个核，

此时可能它的运算能力会被吃掉在盲等，

但是其他核可以照常工作，

并且快速的释放临界区。

这种自选锁就不太适用于单处理机系统，

因为如果是单处理机系统的话p1进程，

此时它吃掉了唯一一个核对吧，

它在这个时间片内有可能等到被解锁吗？

不可能只有他这个时间片用完，

并且上锁的那个进程啊，

上处理机，

然后解锁之后他才有可能获得这个锁。

所以在单处理机系统当中，

它不可能说这个自旋等着等着哎，

突然就获得锁了，

不会发生这种情况。

因此，

这种自选锁更适用于多处理机的系统好，

nothing
