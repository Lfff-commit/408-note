各位同学大家好，在这个视频中，我们要探讨进程的内存印象，也就是说一个进程在运行的时候，它在内存里边是什么样子？那为了方便大家理解，我在这儿写了一个简单的C语言程序，一会儿我们会结合这个程序去解释。啊，程序运行的时候，它在内存里边的样子，那假设这个计算机系统是32位的，也就意味着每个进程，它拥有4 GB的虚拟地址空间。那地址的范围就应该是从八个零一直到八个f，

大家可以自己算一下，刚好是4 GB。那可以看到，在这个示意图当中，最上面高地址的这1 GB会用于存储操作系统的一些内核数据，比如说进程的PCB。再比如说进程的页表。另外，操作系统的代码也是存储在高地址的，这1 GB当中。比如在第二章当中，我们学过进程调度，那进程调度就属于内核代码，这些代码就是存储在这片区域好，那这是高地址的1 GB。

那我们作为普通程序员，我们写的这个程序，它只能访问用户区的这篇数据，而不能直接访问操作系统内核区的这些数据。好，那接下来我们来研究一下用户区里边存储了哪些数据？首先，在低地址的这个部分会有一片我们不会使用的区域嗯，地址范围就是从八个零一直到呃这个地址。好，然后从这个地址到c七个零。这一整片的区域就会被我们的用户进程所使用。我们从低地址往高地址来看，那地址最低的这片区域会用于存储只读代码以及只读数据。我们知道我们写的这些呃代码在正式运行之前会经过编译链接等等一系列的处理，

把这些代码翻译为与之等价的机器指令。那这些机器指令在程序运行过程当中是不会改变的，所以它属于只读代码，在程序运行过程当中只能读这些机器指令，而不能写这些机器指令。好，因此我们在C语言里面写的这些呃程序代码最终会被翻译为机器指令，然后在程序运行的时候是被存储在紫色的这片区域当中。那除了这些指令代码之外，还有一些只读的数据就只能读，不能写的数据也是存储在这片区域。在C语言当中，有这样的一个关键字叫做const，如果你用const关键字去修饰一个变量，那么这个变量就是长变量。

这种变量的值在程序运行过程当中是不可以改变的，也就是说我们对于这类的变量只能读它的数据，而不能写它的数据。所以但凡const关键字修饰的这些啊，变量都会被存储在紫色的这一片区域当中。好，我们继续往上看，在这片区域之上，会有一片区域用于存储，可以读也可以写的那些数据。结合C语言程序来说，就是我们熟悉的全局变量和静态变量，那在右边这个程序当中。a这个变量是被定义在main函数之外，它属于全局变量，

所以a存储在绿色这片区域。另外，在main函数里边，这儿有个变量c，它用static关键字修饰，属于静态变量，所以c这个变量它是被存储在绿色这片区域的。那这片区域的数据就是既可以读也可以写。那刚才提到的这两片数据区域，在程序运行的时候，或者说进程启动的时候就可以确定这两片区域的大小是多少，并且在整个进程运行的过程当中。它们的大小是固定不变的。很好理解吧，因为一个程序，

它的代码一旦写完之后，这个程序对应的指令，它的大小是多少？是不是就确定了，就固定了，另外整个程序当中，它到底定义了多少个长变量，是不是在程序启动的时候就也可以确定这个长变量到底有多少个？另一方面，我们写的代码当中总共定义了多少个全局变量，总共定义了多少个静态变量，这些是不是也是在进程启动的时候就可以确定的。并且在整个程序运行的过程当中，这些数据的呃大小是不会改变的好，所以刚才我们提到这两片数据区域在进程启动的时候就可以确定大小。

并且啊，整个过程当中，这两片区域的大小是固定不变的。好，继续往上看。在读写数据这片区域上面，就会有一片区域称为堆英文，是hip。那结合C语言来看，在C语言当中，但凡用malloc和free这两个函数去处理的那些数据，就是存储在堆这片区域当中。那显然，堆这片数据区域，它的大小肯定是可以动态的增加或者减少的，

因为我们可以动态的malloc和free。那下一次大家写程序的时候也可以自己去尝试一下，你最大可以my lock多少空间？那对于32位的系统来说，堆区最多只能到啊四七个零这个地址。也就是说，堆加上刚刚提到的这三片区域，总共只有1 GB的大小。所以在你的程序当中，最多可以malloc多少空间，这取决于下面的这两片数据区域有多大。那除了这两片数据区域以及未使用的这片区域之外啊，低地址的1 GB当中，剩余的这些部分就全部可以被用于my lock。那有兴趣的同学，

大家可以写程序尝试一下，当然了，你的电脑可能是64位的系统，而不是32位的系统，那64位的系统，它可以malloc的空间还会更大。那这就不展开细聊了。那我们回到左边这个例子，整个地址空间总共有4 GB操作系统，用掉了1 GB，然后低地址的这些部分用掉了1 GB。现在中间还剩下两GB的部分，那这两GB的低地址部分是共享库的存储映射区。那结合我们熟悉的C语言来看，我们在C语言当中经常会调用一些啊，

系统提供的库函数。那这些库函数的指令代码就会存储在黄色的这片区域当中，比如说这个地方使用到的print函数。它的指令代码就是存储在共享库的存储映射区。显然，不同的程序，它有可能使用到的库函数数量是不一样的，你使用的库函数越多，那么这片区域肯定也会越大，越往上增长。好，那这是黄色的这片区域。接下来再往上看，从中间的这两GB高地址的部分，这个部分会有一片区域用来存储用户栈。

我们知道程序运行的时候，就是从main函数开始，main函数可能会调用其他的函数，那每调用一层函数。就需要保存上一层函数的信息，同时也需要开辟一片新的空间去存储被调用函数的呃相关信息。那所有的和函数调用相关的数据就存储在用户站这片区域当中，显然这片区域也是会动态的增加或者减少的。你调用的函数层数越多，那么这个用户占肯定也会越往下面这个方向去增长。那我们在每一个函数里边定义的呃局部变量以及函数调用的一些呃调用参数等等，这些东西就是存储在用户栈里面。好，那这就是进程的内存印象。好，

最后我们还需要解释一个东西，我们在C语言当中经常会用这种宏定义的方式定义一些常量。那很多同学可能会误以为呃，既然它是常量，所以它应该被存储在紫色的这片区域当中，比如说x的值等于一零二四，那么有没有可能在紫色的这片区域当中？分配出四个字节，用于存储x这个常量呢？好，事实上，这种理解是不对的。我们宏定义的常量不会在内存当中专门的分配呃，存储的空间。那在预编译的阶段，

你的编译器会把这个大x把它自动的替换为一零二四。那把x替换为一零二四之后，一零二四这个值会被隐含到程序指令当中。这个学过计算机组成原理的同学啊，应该能够理解在计组当中我们介绍指令的时候提到过一种寻址方式，叫做立即数，也就是说程序指令当中就直接包含了一零二四。而如果没学过计组或者不考计组的同学嗯，可以不用深究，总之这想强调的就是宏定义的常量，它并不会存储到紫色这片区域当中。用const关键字定义的这种长变量，它会被存储到紫色这片区域当中。大家只需要记住这个结论就可以好，那这就是进程的内存印象。

那这个部分个人认为很有可能考察的形式就是呃，有可能会给你一个C语言的代码，给你一段代码，然后问你。比如说呃a这个变量或者b这个变量，或者某一个变量，它应该存储在呃进程内存印象的哪一片区域当中，有可能会有这样的考题。那在后面这一页课件当中，我也给大家总结了呃，哪一些数据应该是存储在什么位置给大家做了这样的总结？那结合之前的这个例子，相信不难理解好，那接下来大家也可以结合这页课件再回忆一下好的，那以上就是这视频全部内容。
