好，下面来看IPV 4分组这个考点在大纲当中处于这个位置，那IPV 4 IPV 6这两个术语我们得把它拆分成两个部分来看。IP啊，是一个部分v4和v6，表示的是IP协议的版本，第四个版本和第六个版本是这个意思。IP协议在互联网当中处于核心地位，在网络层之上主要运行的传输层。协议有TCP和udp这两个协议。而这两个协议在网络当中的这个数据传输都要依赖于IP协议。IP协议定义了一个非常重要的数据结构，叫做IP数据报。也就是IP分组IP分组通过路由器进行存储转发，那一个IP分组可以承载TCP协议传过来的数据，

也可以承载udp协议传过来的数据。那同样的道理，像网络层当中也有一些呃，其他的协议会使用到IP协议的，这个IP分组进行传输，比如说ICMP协议，I gmp协议。这些协议的数据也会被封装在IP分组当中，再来传输那网络层的这个IP协议又会调用更底层的，也就数据链路层和。物理层的那些协议，比如说八零二点三八零二点幺幺八零二点五等等，底层有不同的这种呃数据链路的实现方式。所以可以看到IP协议是互联网的核心。在这个视频中，

我们先要探讨IP协议当中定义的最重要的这个数据结构，叫做IP分组。那IP分组也可以称为IP数据报，二者是同义词。一个IP数据报由首部和数据部分组成。那首部又可以进一步的分为固定部分和可变部分，首部这个位置一定会包含20个字节的固定部分的内容。那可变部分的内容有可能是呃，什么都没有，最大有可能到40个字节，在历年的考研真题当中，可变部分通常是没有。也就是说，在做题的时候一般呃，这个首部就是20个字节。

当然，这只是通常的情况呃，我们具体问题需要具体分析好，接下来我们看首部的第一行。这几个字段分别是什么意思？先把这几个字段的比特数给标注上去。版本这个字段占四个比特，这四个比特需要表明这个IP数据报它符合的是呃哪个版本的IP协议？我们现在互联网上呃，通常使用的是v4就是第四版本的协议，那在不久的未来有可能会过渡到v6第六版本的这个IP协议。所以现在的网络当中，前四个字段就这四个比特，通常它的值就是四。当然，

肯定是用二进制来表示的。也就是零一零零。好，这是第一个字段的含义。接下来，第二个字段叫首部长度顾名思义，它表明了这个IP数据报的首部。总共占了多少个字节，但是需要特别强调的是，首部长度是以乘四字节为单位的。好来解释一下，首先首部长度这个字段，它占四个比特呃，那四个比特的这个无符号数可以表示的范围就是零到15对吧？那什么叫以乘四字节为单位呢？

比如说有个IP数据报，它的首部不包含这个可变部分，只包含固定部分。也就是说，首部总共是20个字节。那么，对于这样的一个IP数据报来说，首部长度这个字段，它的值就应该是五。因为五×4字节。等于20字节刚好就对应了这个手部的长度。好，那么既然首部长度啊，这个值它只能表示零到55，那就意味着整个首部的最大长度只有可能是15×4字节，

也就是60字节，对吧？那也正因如此，可变部分的长度，它只能在40字节以内，因为整个首部是固定部分，加可变部分，对吧？两个部分加起来不能超过60字节的总长度。那另一方面，在可变部分最后的这个位置，我们会看到一个呃叫做填充的东西。这儿的填充是为了凑足四字节的整数倍。这同样是由首部长度的，这个计算规则带来的一个限制。

好，所以这几个东西要联合起来，看它们之间是有相互联系的。接下来后一个字段叫做区分服务占八比特，这个字段一般用不到考试也不会考，所以知道有这么个东西就行了，我们可以把它跳过。再往后一个字段，叫做总长度啊，顾名思义，它指的是整个IP数据报的总长度，也就是包含了首部和数据部分。两个大的部分加起来总共有多长？那可以看到总长度这个字段占了16个比特，那16个比特可以表示的啊，

这个数的范围就是零到六五五三五。那需要注意的是。这个总长度，它是以一字节为单位的。相比之下，刚才的首部长度这个是以四字节为单位，对吧？好，那么根据总长度这个字段的呃数值表示范围啊，不难知道数据部分理论上啊，理论上最短可以到零字节。也就只有一个首部。理论上，数据部分最长可以达到六五五三五减二十个字节，因为首部肯定要呃占二十个字节嘛。

所以，数据部分理论最长就是这样的一个值啊，当然这些都不用记，那在这儿需要强调这个前缀叫做理论最长，理论最短。一会儿我们会补充实际上呃一个IP数据，报它的总长度会被其他的一些因素所限制。那这个坑一会再来填好，接下来让大家看一个一二年的真题，通过这个真题想告诉大家，在真题当中它一般会给出IP分组头，也就是IP数据报的。首部它的结构会明确的，给你这样的一个结构图，所以你不需要去记忆每一个呃字段，

它到底是在呃第几个字节，第几个比特？考试会给你这种参考图，但是我们需要记住每个字段的含义，要能够根据他给你的参考图去分析这个IP数据报的呃，相应的一些性质。比如说一二年的这道题目，他就给了一个IP数据报的前40个字节。那么刚开始这个位置肯定是首部，对吧？IP数据报的首部，刚才我们说首部长度这个字段。它应该占四个比特版本，这个字段也是占四个比特，那这儿是以16进制的形式给出了IP数据报的具体内容。

那根据下面这个截个图，我们就可以找到这个IP数据报的首部长度，应该是等于五×4个字节，注意首部长度是以四字节为单位的。那算出来首部长度等于20个字节。前面的第一行总共刚好对应20个字节，所以第一行的这些16进制表示的就是首部的呃信息，那再往后就是数据部分的信息，那数据部分的长度等于多少呢？接下来，我们可以从总长度这个字段去推测，根据下面这个结构图呃，可以知道总长度应该是这一串。对应16比特翻译成十进制，对应的是48，

我们说总长度是以一字节为单位的，所以啊，整个IP数据报的总长度就是48字节。好，因此可以算出数据部分的长度，应该等于总长度减掉首部的长度等于28个字节，那这个题目它的数据部分只给出了20个字节。所以还差八个字节的信息题目没给完整好，那这是真题的呃，一种考察形式，他肯定会给我们IP数据报的这种结构图，但是我们要知道每一个字段。的含义是什么？这样我们才能够根据题目给的其他条件去推算我们需要的一些信息好，接下来我们回头填一个坑。

刚才我们说数据部分受到总长度这个字段的这个呃限制，它有一个理论最大值和理论最小值。然而，在实际的网络传输当中，数据部分的长度通常会受到下一段数据链路的最短，最长，真长的限制。比如说我们之前在介绍以太网真的时候。我们说过，以太网针有最短针长的限制，也有最长针长的限制，对吧？快速回忆一下一个以太网帧，它的构成是六六二n四收发协数页n是四六一千五。这儿的n对应的就是网络层交给数据链路层的这坨数据，

也就是IP数据报的总长度，对吧？IP数据报的总长度最小不能低于46字节，最大不能超过1500字节好，因此在实际的传输当中。IP数据报的真实大小会受到数据链路层的限制。那在这，我们需要引入一个重要的概念，一个数据链路层能够承载的最大数据量，我们把它称为最大传送单元。英文缩写叫mtu，这个英文缩写在题目当中非常常见，需要记住，那像以太网当中。它的mtu就等于1500字节。

这个参数限制了一个IP数据报，可以被允许的最大长度，那这样我们就引出了一个分片的问题。如果一个IP数据报的总长度超出了数据链路层的mtu的限制，那么我们就需要把IP数据报。进行分片。好，先看一个例子，假设呃，现在。这张图当中，网络层的这个IP数据报总长度是4000个字节。这个IP数据报是由呃原主机a的网络层去呃构造的好，现在a主机需要把这个IP数据报。扔给呃数据链路层去进行传输，

那么我们不妨假设从a主机到这个下一跳路由器的呃这一段数据链路层。它的mtu=4000字节。那这是不是就意味着？网络层的这4000个字节大小的IP数据报，刚好可以装到。一个帧当中。好，接下来问题出现了，经过第一段链路的传输。这台路由器可以收到数据报。整个IP数据报的大小是4000个字节。那接下来这个路由器需要把IP数据报通过下一段链路传递给下一个相邻的节点。然而，第二段链路，它的mtu=1500。

那此时我们就需要把这个大的IP数据报拆分成几个更小的分片。然后把这些小的分片依次传给呃下一个节点。等这些小的分片传到目的主机之后，再由目的主机的网络层进行分片的重组。那结合一个具体的例子来理解，假设现在主机a刚开始构造的这个IP数据报呃，它包含了20个字节的首部。以及三九八零字节的这个数据部分。总计4000个字节。那根据刚才分析，我们知道这个IP数据报在第一段链路当中的传输，不需要分片。4000个字节，一个帧直接带走，但是到了中间，

这个路由器的时候，就需要把它拆分成几个更小的片。这4000个字节的IP数据报会被分成三个片。每一个分片，又是一个独立的IP数据报。第一个IP数据报总长度是1500字节，第二个IP数据报总长度是1500字节，第三个IP数据报总长度是一零四零个字节。那用图来表示就是这个样子。这个大的IP数据报被拆分成了三个独立的，更小的IP数据报，然后封装成三个独立的帧。再进行传输，传到下一个相邻的节点。好，

接下来我们看一下这个图的细节。首先呃，原始的这个IP数据报它的呃，有效数据也就是数据部分总共占三九八零个字节。我们会把这个数据部分进行拆分，首先拆分出一四八零字节。让它作为第一个分片的数据部分。为什么是一四八零字节呢？因为首部固定的还要占20个字节，对吧？首部加上数据部分总计不能超过1500字节，这个是由第二段链路的mtu限制的。好，所以第一个分片当中可以承载的这个数据量就是一四八零字节。第二个分片当中可以承载的有效数据量也是一四八零字节。

那由于这些分片每一个分片都是一个独立的IP数据报，因此每一个分片当中，我们都需要给它呃添加一个。首部的信息。这么做带来的好处就是每一个分片在网络当中都可以独立的被存储转发。也就意味着，这些分片可能会按照不同的路径。进行转发对吧？那这就使得整个传输效率变得更高，那接下来我们需要解决一个什么问题呢？就是我们刚才说一个IP数据报完整的IP数据报可能被拆分成很多个分片，对吧？而这些分片在网络当中呃。各走各的，他们最终到达目的主机的时间，

还有顺序，可能是乱的。所以对于目的主机而言，当他收到这些分片的时候，需要通过某一些信息把这些分片重新按顺序。组装起来。那怎么去实现这些分片的组装呢？这就需要使用到IP数据报的第二行这三个字段，分别叫标识标志和片偏移。好，首先标识字段占16个比特，这个标识字段通常是IP数据报的发送方，也就是源主机去生成的。一般来说就是一个自增序列，比如说我a这台主机啊，

对外发送的第一个完整的IP数据报我，把我把它的标识编号为一。第二个呃，完整的IP数据报，我把它标识设为二第三个IP数据报设为三好，以此类推。呃，一个IP数据报在传输的过程当中有可能被分片对吧？有可能被分片。那为了让接收方收到这些分片的时候，能够区分出每一个分片原本属于哪一个IP数据报？因此，我们就设置了标识这个字段，如果数据的接收方收到好几个分片。并且这些分片的标识相同，

原地址也相同，那么就说明这几个分片原本从属于同一个IP数据报，对吧？那接收方的网络层就可以呃把这些分片进行组装，把它恢复成完整的IP数据报。好，这是标识这个字段的作用。好，接下来看标志这个字段。三个比特在这三个比特当中，我们只需要关注最低位和次低位。最低位通常把它记作MF。意思是more fragment。用于表示，呃，

在当前这个分片之后，还有没有更多的分片？比如说数据的接收方收到了一个IP数据报，这个IP数据报的标志字段是零零一。那最低位等于一，也就是MF这一位等于一，表示当前收到的这个IP数据报它并不是最后一个分片。在它的后面还有别的分片。接下来看标志字段的次低位df，这一位df是don't fragment的英文缩写，表示不要分片。如果df=1，那么就意味着当前的这个IP数据报是不允许被分片的。只有df=0的时候表示允许被分片。如果一台路由器，

它此时要转发的这个IP数据报被标记为不允许被分片。也就是标志字段的次低位等于一的话，那么假设这个路由器要把这个IP数据报。转发到下一条链路的时候。这个链路的mtu不允许IP数据报这么大，但是这个IP数据报本身又不允许呃被分片。那此时怎么办呢？就说明这个IP数据报就无法正常的呃，传给这个目的网络了。那在这种情况下，路由器会选择把这种不允许分片又发不出去的IP数据报直接给丢弃。与此同时，路由器会给这个IP数据报的源主机，也就是发送方发送一个ICMP报文。那ICMP报文的作用是通知原主机发生了某种异常情况。

就跟操作系统的异常机制是类似的，只不过这种异常的通知机制是通过报文传给原主机。让原主机根据ICMP报文的呃相关信息去了解到哦，原来我发出的那个IP数据报呃传输失败了。好，那这是df这一位的呃含义。好，那么这是标志字段，我们关注最低位MF和次低位df就可以最高位啊，不用管。接下来看片偏移这个字段总共占13个比特，这个字段用于表示当前的这个IP数据报的数据部分。在被分片之前，所处的位置。那需要注意的是，

片偏移这个字段，它是以八字节为单位的。好，那了解了标识标志，还有片偏移字段之后，我们再回到刚才这个例子看一下，这个例子当中的细节。上面这个部分是原始的IP数据报当一台主机构造一个IP数据报的时候。呃，需要给这个IP数据报的首部这个位置呃，附一个标识信息。标识总共占16比特，那像这个例子当中，标识等于七七七，这是随便写的一个值。

与此同时，标志信息也需要设置标志信息总共占三比特，那最低位是MF表示在这个IP数据报的后面还有没有更多的分片？那由于这个IP数据报没有被分片对吧？所以显然MF这个标志位应该等于零。接下来df表示的是呃，不要分片，如果df=0的话，意味着呃，这个IP数据报在传输的过程当中是可以被分片的。就是这个意思。那也正是因为df=0，所以中间这个路由器在转发IP数据报的时候，才可以把它给拆分，把它进行分片。

好，那分成了这样的三个片，刚才我们说过第一片和第二片的这个呃数据部分呃，分别是一四八零字节。那最后一片剩余的呃，这个数据部分就应该是三九八零减掉两个一四八零。所以最后这一片更小一点，它的数据部分占一零二零，那再加上20个字节的首部，最后的这个分片，它的总大小就是一零四零。那么，由于这三个分片都从属于同一个IP数据报，所以它们的标识都是来自于呃。原始的数据报本身。

复制一份。这样的话，这个目的节点在收到这些分片的时候就可以根据标识信息去判断，它们原本从属于同一个IP数据包这件事情。好，接下来看标志位第一个分片的，后面还有更多的more fragment，还有更多的分片，所以第一个分片的MF标志位设为一。第二个分片的MF标志位也设为一，第三个分片的后面已经没有更多的分片了，所以MF这个呃标志位设为零。好，这是三个分片的MF那df这个标志位。啊，

直接继承自原始的这个数据报就行了。好，接下来第三个字段出现了片偏移，这个字段之前我们说片偏移，这个字段占13个比特。并且片偏移是以乘八字节为单位的。片偏移表示的是这个IP数据报它自己呃承载的这坨数据在原始数据报当中的位置。比如第一个分片，在原始数据报当中就是从零这个字节开始的，对吧？所以它的片偏移记为零即可。在原始数据报当中，从第零个字节到编号为一四七九的呃，这些字节。就是总计一四八零个字节嘛，

这一四八零个字节都从属于第一个分片。好，接下来看第二个分片的片偏移等于185，那刚才我们说片偏移是乘八字节为单位的，所以185×8等于一四八零。意味着第二个分片的这个数据在原始的数据报当中是从一四八零。啊，这个字节开始的。一直到二九五九这个字节。好，那同样的道理，第三个分片，它的片偏移等于370，370×8等于等于二九六零，因此第三个分片承载的这些数据。

在原始数据报当中，是从二九六零这个字节开始的好，那这就是片偏移这个字段的原理。目的主机收到这些分片之后，首先根据标识去判断哪些分片从属于同一个原始的IP数据报。然后根据各个分片的片偏移这个字段，把这些数据拼凑成原始的这个IP数据报。就是完成分片的重装。那分片的问题是考试的重点，这还需要提醒大家注意一些东西，首先分片这个动作有可能在原主机或任何一个中间路由器发生。比如说原主机构造了一个很大的数据报，当他要发给下一段链路的时候，呃，发现这段链路的mtu不允许，

那么原主机就需要把这个大的IP数据报。拆分成多个分片对吧？所以原主机自己可以执行分片的动作，那当一个分片到达某一个中间路由器的时候，同样的由于。下一段电路的限制有可能还需要把这个分片拆分成更小的分片。所以中间路由器在转发的过程当中，也有可能会进行分片这个动作，而一旦某一个IP数据报被拆，分成多个分片之后。各个分片就可以沿着不同的转发路径去进行转发。那所有的分片最终到达目的主机的时候，才会被目的主机给重组。那由于每个分片都是独立的IP数据报，所以各个分片就有可能乱序的到达目的主机。

好，另外最后一点就是刚才我们说首部的片偏移这个字段，它是以乘八字节为单位的，所以这就意味着除了最后一个分片之外。其他的每个分片的数据部分都必须是八字节的整数倍。像刚才这个例子当中，最后的这个分片，它的数据部分占一零二零个字节，大家可以算一下一零二零÷8是除不尽的。也就是说，最后这个分片的数据部分，它并不是八字节的整数倍，而前面这两个分片，大家可以算一下一四八零÷8是可以除得尽的。所以前两个分片的数据部分都是八字节的整数倍，

那这样的要求也是为了配合。片偏移这个字段的含义，因为片偏移，它总是乘以八字节以这个为单位，对吧？好，所以这一点需要提醒大家注意，这个坑大家会在课后习题当中遇到。等你做题错一次就知道注意这一点了，好接下来我们再回到IP数据报的首部，再看第三行的这三个字段，这三个字段的含义就相对简单一些。生存时间这个字段占八个比特，在考试当中通常记为ttl这个英文缩写。这个字段表示的是一个IP数据报，

在网络当中可以通过的路由器数量的最大值。直接举一个例子，比如说呃主机a，它构造了一个IP数据报，然后把这个IP数据报发到网络上。主机a在发出IP数据报之前，会给这个IP数据报设置一个生存时间，也就是刚才我们提到的ttl啊这个字段。比如说我们把生存时间ttr设置为六，那接下来这个IP数据报首先到第一个路由器。当IP数据报到达一个路由器的时候，这个路由器就会把ttr- 1，所以经过这个路由器的处理ttr就会变成五。好，如果ttl没有减到零，

那么这个IP数据报就可以继续往后转发，那同样的下一台路由器也会把ttl- 1，那么ttl就变成了四。再往后ttr变成三，再往后ttr变成二。那到达最后一台路由器的时候ttl的值啊，依然还没有减到零，所以这个IP数据报就可以啊，顺利的到达b节点。那这是ttr设置合理，可以到达的情况，接下来看ttr设置不合理，不能到达的情况假设。同样的a主机创建了一个IP数据报，并且把ttr设置为四。

那a主机给第一台路由器，这台路由器会让ttl- 1，那就变成了三，再往后转发。下一台路由器又会把ttr- 1变成了二，然后再转化出去，下一台路由器把ttr- 1。接下来再转发出去，那最后这台路由器把ttl再减一，此时ttl就变成了零，对吧？而一旦ttl变成了零。那就会把这个IP数据报直接给丢弃，那丢弃IP数据报的这台路由器会给原主机。发送一个ICMP报文。

用于通知原主机发生了某一种异常情况。好，那这就是ttr这个字段的含义。那接下来的巴比特表示，协议意思就是我这个IP数据报当前正在为哪一个协议服务。比如说，当前正在为TCP协议服务，那么这个协议的值就设为六，当前正在为udp协议服务，那么呃，就可以把这个值设为17。那这么做就可以让呃目的主机的网络层知道这个IP数据报当中运输的数据部分。到底应该递交给上层的TCP协议还是udp协议还是其他的什么协议，对吧？所以协议这个字段主要是为了让IP数据报逐层往上递交的时候呃，

能知道它的去处。好，那再往后就是首部校验和16个比特。根据这个字段的名字，可以知道这个校验信息，它只校验IP数据报的首部。不需要校验IP数据，报的数据部分。那这样可以使得每一台路由器的这种呃校验的负担比较小，对吧？数据部分的差错，我网络层是不管的。如果说我们把首部校验和这16个比特设为全零，那么就意味着啊，这个IP数据报在转发的过程当中，

不用去校验。连手部的差错控制都不用管。好，这是校验核的一个特殊状态，设为全零表示不用校验。那关于这个首部的校验和啊，这几个比特如何去确定它的计算方法和udp协议的校验是完全相同的，我们会放在udp协议那个地方去学。反正它的原理和奇偶校验crc循环冗余校验，这些都类似。就是用整个首部的这些数据。去计算得到一个校验码，然后把这个校验码给复到这16个比特呃，这个字段当中。就是这么个意思。

那具体的我们在udp那再来解释。好，那最后的两行反而就变简单了，接下来的这两行分别是32比特的原地址和目的地址，表示的就是发送方的IP地址和接收方的IP地址。好那么到此为止，我们就了解了IP数据，报所有重要的东西。IP协议的第四个版本当中定义了啊，符合IPV 4标准的IP数据报或者说IP分组的格式。它由首部和数据部分来构成，其中首部的固定部分占20个字节可变，部分呃可能是零字节有可能最大不超过40个字节。那建议大家在听完课之后回去看王道书的素图，回忆一下每个字段的作用，

如果回忆不起来，那么再看看书。那在首部当中，我们介绍了三个和长度相关的字段。很容易记混给大家编了一个口诀，叫四幺八首总篇。为了辅助记忆，配了这样的一个图，把脑袋。给呃偏着啊，手总偏。四一八首总篇的意思就是说首部长度这个字段是以四字节为单位总长，这个字段是以一字节为单位。片偏移这个字段是以八字节为单位。好，

所以四幺八首总篇这三个字段很容易混，需要记住。那在考试当中最常考的是分片问题，还有ttr字段的问题。在分片问题当中，我们介绍了mtu这个概念。它表示，一个数据链路层能够承载的最大数据量。如果IP数据报的总长度大于下一段链路的mtu，那么就需要把IP数据报的数据部分进行分片。每一个分片作为一个独立的，新的，更小的IP数据报去进行传输。那很容易错的一个点，就是除了最后一个分片之外，

其他每个分片的数据部分都必须是八字节的整数倍。好，另外在IP分组的首部当中有两个字段呃，它的名字很容易混呃，需要特别的记忆一下标识字段16个，比特标记字段三个比特。标识对应的是啊，每个分片原本属于哪个IP数据报，而标记字段我们需要关注的是最低位和次低位，也就是MF和df。这两个术语在题目当中很容易混淆，需要特别的记忆。那最后ttr字段也很常考，一个IP分组每经过一台路由器，这个路由器就会把ttr- 1。

如果ttr值已经减到零，那么这台路由器就会直接把IP分组给丢掉，并且向原主机发送ICMP报文。ICMP报文的作用就是通知源主机啊，出现了某一种异常好，那以上就是IPV 4分组的所有内容。
