各位同学大家好，在这个视频中，我们要学习地址解析协议，英文缩写叫ARP。那通过之前的学习ARP协议，我们已经并不陌生了，它可以用于查询同一网络中的啊，这些节点的IP地址和MAC地址之间的映射关系。ARP协议中定义了一个数据结构，叫做ARP分组。那类似的IP协议当中也定义了一个数据结构，叫做IP分组，对吧？IP分组会交给呃下一层数据链路层，把它封装为一个mac帧。

那类似的道理，一个ARP分组也会呃被交给这个数据链路层封装成一个mac帧。比如说数据链路层呃使用的可能是八零二点三，这个协议标准。也就是使用以太网作为呃数据链路层。以太网max帧的格式是六六二n四，收发斜数验。其中n这个部分指的就是上层网络层呃交过来的这一坨数据，也就是网络层的pdu。如果mac帧的数据部分携带的是IP数据报，那么收发斜数页的斜也就是类型这个字段。就会被写成啊零八零零，这个是16进制表示总共两个字节零八零零表示的是它携带的是IPV 4的这个啊。pdu也就是IPV 4协议定义的这个数据报。好，

另一方面，如果这个mac帧，它的数据部分携带的是呃ARP协议的pdu，也就是呃携带了一个ARP分组的话。那么，收发斜树叶的斜类型这个字段就会被设置为零八零六。好，所以通过这两张图是想让大家看到呃ARP分组和IP分组之间的一个关系。这两个东西是两种不同的数据结构，分别在ARP协议当中呃被定义以及在IP协议当中被定义。好，另外一点需要复习的是在一个mac帧当中，如果目的地址也就是接收方的MAC地址，我们写为权一。那么表示，

这是一个广播帧，广播帧就意味着在同一个网络内，同一个网络内的呃，所有的这个节点都会收到这个广播帧，对吧？另外，再回顾这样的一些知识啊MAC地址总共占48比特，这个MAC地址是网络适配器在出厂的时候就被呃厂家给分配好的。并且可以确保是全球唯一的。一台主机至少会有一个网络适配器，比如说网线插口背后的那块芯片，就是啊，网络适配器。那也正因如此，一台主机至少会有一个MAC地址。

另一方面，对于路由器来说，由于路由器有多个转发接口。每个接口的背后都会有一个网络适配器，那这就意味着一台路由器它会有多个MAC地址，对吧？每一个转发接口呃，都会对应一个呃，唯一的MAC地址。好，回顾完这些知识之后，接下来正式进入ARP的学习。我们说ARP的作用是可以在一个局域网的内部啊，去查询到一个IP地址对应的MAC地址等于多少。在ARP协议当中，

还定义了一个很重要的数据结构，叫做ARP表。ARP表被存储在ARP缓存当中。那这个数据结构的作用是什么呢？在这个数据结构当中，它记录了IP地址和MAC地址之间的映射关系。而每台主机，每台路由器都有自己的ARP表。并且为了保证ARP表象的正确性，这些主机路由器都需要定期的去更新ARP表象。好，那如果用图来表示的话，大概就是这个样子，比如说这个是h3这台主机的ARP表。这个ARP表记录了IP地址和MAC地址的映射关系，

那同样的道理h6也有自己的ARP表。路由器也有自己的ARP表。这些主机还有路由器都需要定期的去更新自己的ARP表的表象。当一台主机刚开始接入啊网络的时候，它的ARP表是空的。好，接下来我们探讨ARP协议工作的过程。ARP协议是通过IP地址找到这个IP地址对应的MAC地址等于多少对吧？其实，就类似于从人群中找人的过程，假设这是一个人群。这个是你。你希望在人群当中找到三年二班的呃咸鱼同学，但是你不知道到底谁才是咸鱼同学？那这个时候你可以怎么办？

很简单，你在这个人群中大喊一句，三年二班的咸鱼同学，你在哪儿？你可以这么问对吧？好那么假如人群中真的有三年二班的咸鱼同学，那么这个同学听到了之后他就会啊告诉你说啊，你好你好，我就是你想要找的三年二班的咸鱼同学，我的身份证号是多少多少我的。性别是什么什么，对吧？那ARP协议通过IP地址去查MAC地址的过程也是类似的。首先，找人的一方呃会构造一个ARP请求分组。

这个ARP请求分组最终会被封装到一个mac帧里边，并且mac帧的呃目的地址会写成全一。那么，写为权一意味着这个mac帧会被广播给整个局域网内部的所有节点，对吧？好，那另一方面，在这个ARP请求分组当中，还需要指明我是谁？我想找的是谁？要指明这两个信息。那关于我是谁，就是要告诉对方，我的IP地址是x我的MAC地址是y。你得自报家门，

对吧？那我想找谁？这个信息就是要指明我想找的这个家伙，他的IP地址是等于多少？最终，我希望呃，通过这个IP地址z去找到对应的对应的这个节点，它的MAC地址。好，我们不妨结合这个例子来梳理一下ARP啊，请求分组是怎么被发出去的？我们先来看第一个训练。现在我们假设h3这个节点呃，要给internet上的某一个节点发送IP数据报。啊，

不好意思，这个课件这个地方打错了啊，那我们知道，如果IP数据报想要发到internet上，那么这个IP数据报首先应该发给本网络内的这个。默认网关对吧？而本网络内的默认网关是b2这个接口，它对应的IP地址是幺二八点幺。好，现在问题发生了h3的内部，虽然配置了默认网关的IP地址，但是它还不知道默认网关的MAC地址。此时ARP协议就登场了，首先h三会构造一个ARP分组。具体来说是ARP请求分组。

在这个分组当中呃，需要指明我是谁，也就是要填写自己的IP地址，我是幺六六点幺点幺二八点二。那除此之外，还需要自报家门呃，告诉对方呃，我的MAC地址是等于多少？那MAC地址总共48比特，我们用16进制表示，假设它是aabbccddee。呃，结尾是三三。我们剪辑为这样。好，

那除此之外，还需要说明我想要找的是谁？把我想要找的这个节点的IP地址写进去。也就是默认网关幺二八点幺把它写进去好，现在这个ARP分组是网络层的呃pdu对吧？网络层的pdu。接下来，这个网络层的pdu会被交给数据链路层，数据链路层封装成mac帧。那么，由于我们是要广播找人，所以这个mac帧的目的地址需要写为全一。好，另一方面，这个mac中的原地址就写成自己的，

这个地址就OK了，也就是什么ee三三呃，我们就只记最后的这。两个16进制啊，就把它记为三三。好，那这样mac帧封装好了。接下来，这个mac帧先发给交换机，交换机检查由于呃，目的地址是权一，所以它会把这个mac帧。呃，转发到所有的其他接口。反正最后这个ARP请求分组啊，

肯定会被默认网关接收，也会被h4收到。也会被h6和h5收到所有的节点都会收到，但是只有默认网关b2这个接口啊，收到这个呃mac帧之后。检查哦，他要找的人哎，没错，正是在下幺二八点幺。于是b2接口会给h3这个节点回复一个单波帧，在单波帧当中告诉h3。我的MAC地址等于多少？好，所以ARP协议的第二个过程就是被寻找的那个节点会返回ARP响应分组。注意ARP响应分组是一个单波帧。

因为在ARP请求分组当中找人的那个节点，已经自报家门了，对吧？已经告诉对方自己的MAC地址是等于多少？所以，当被寻找的那个节点返回ARP响应分组的时候，把ARP分组封装成帧的时候，就只需要封装成单波帧。广播找人单播回复就跟刚才我们说的人群中找人，这个过程是一样的，你在人群中广播大喊一句，二年三班的咸鱼在哪？对吧？那咸鱼听到之后，他可以呃跟你私聊吗啊，

我就是那个咸鱼。同样的道理，那显然在ARP响应分组当中需要回复对方说啊，你好，我就是你要找的那个靓仔，我的IP地址刚好就是呃，你想要找的那个z。我的MAC地址是v，告诉他这样的一些信息。好，接下来回到这个例子，我们梳理b2接口，返回ARP啊，响应分组的这个过程。对了，

在刚才这个过程当中b2接口，它收到了h3的ARP请求，对吧？在这个ARP请求当中h3已经自报了家门。呃，自己的IP地址和MAC地址等于多少？已经告诉b2了，所以当b2收到这个请求分组的时候。它会把h3的这个IP地址，还有MAC地址的映射关系顺手给记录到自己的ARP表当中。因为当一个节点给另一个节点发送了这个ARP请求分组的时候，意味着这两个节点之间接下来很有可能。即将开始这个通信对不对？所以我不如顺手就把啊对方的这个IP地址MAC地址也先记录下来。这样的话之后，

如果b2想找h3，是不是直接查表就可以了？好，那这是b2节点会顺手做的事情。好，接下来b2节点会构造ARP呃响应分组。在响应分组当中告诉对方，呃，自己的IP地址正是你想要找的幺六六点幺点幺二八点幺。以及告诉对方呃，自己的MAC地址是aabbccddee。b2 IP地址32比特MAC地址48比特。好，这是ARP呃响应分组。那么ARP响应分组是网络层的东西，

接下来需要把网络层的数据封装成mac帧。mac真的目的地址应该写h3的，这个MAC地址。因为b2之前收到这个请求，分组之后可以从中知道h3的MAC地址等于多少，对吧？好，那目的地址是以三三结尾的，我们只记录末尾的两个三。好，这是目的地址，那源MAC地址。要写的是自己的MAC地址，对吧？那b2它自己的MAC地址是什么？

eeb 2那简单起见，我们只写末尾的b2。这样个东西。好封装成了mac帧。接下来，这个mac帧会被转发到这个呃交换机这儿，对吧？那由于mac帧的目的地址非常明确，是三三这个地址。而交换机知道呃三三这个MAC地址连接的是这边这个接口，所以这个呃ARP响应分组会被精准的转发给h3。这个计算机。好，现在h3收到这个ARP响应分组之后，是不是就可以知道刚才查的这个IP地址和呃MAC地址之间的映射关系了？

于是呃，就可以把这个。查询的结果呃，记录到自己的ARP表当中好，那从此以后h3就知道默认网关的MAC地址等于多少了。那回到最开始的故事，接下来假设h3要给互联网上的呃某一个节点发送IP数据报。那么，对于h3来说，他现在已经知道默认网关的这个MAC地址等于多少了，对吧？所以h3发到外面的这个IP数据报。在封装成帧的时候，这个mac帧的目的地址是不是直接填默认网关的MAC地址就OK了？好，

那这是训练一。接下来看，训练二基于训练一的这个结果，也就是呃，路由器还有这个h3的ARP表都已经有内容了。基于这样的一个结果，假设现在有个IP数据报，从呃internet发往h3那么分析ARP工作的过程。发给h3的这个IP数据报呃，它的目的IP地址。目的IP地址肯定写的是幺六六点幺点幺二八点二对吧？那么，当这个IP数据报到达路由器的时候，首先这个路由器会查自己的路由转发表。我们在这个图里边没有画路由转发表，

但是通过之前的练习，我们知道路由转发表的查询结果就是呃，决定了这个IP数据报。最终，应该从b2接口给它转发出去，对吧？那么，对于这个路由器来说，要把这个IP数据报转发到下一段链路的时候，肯定得把这个IP数据报封装成帧，对不对？封装成一个mac帧。那现在这个路由器知道了啊，接收方的IP地址等于多少？于是他会先查自己的ARP表，

哎，发现刚好有一个表象能够匹配，从中可以查到呃，这个接收方的MAC地址。于是查完ARP表之后啊，这个mac帧的封装就变得很容易了，可以直接把目的地址写成h3的这个地址，也就是什么意义？第三三，这个东西。那原地址呢？写的就是b3接口，自己的地址也就是什么eeb 3这个东西好，所以封装成mac帧之后，这个mac帧就可以直接。

从b2接口出来到达呃，这个交换机，然后直接精准送达h3。好，那这是训练二。那最后还有一个训练三就交给大家，自己进行假设h3想要给h6这台主机发送IP数据报。但是现在h3只知道h6的IP地址，还不知道它的呃MAC地址，那么背后这个ARP协议工作的过程到底是什么样的？大家可以自己模拟一遍训练三啊，背后的过程。简单说一下啊，首先h三会封装一个ARP，请求分组对吧？

然后ARP请求分组最终会被封装到一个广播帧里边，所以这个ARP请求分组啊，会被路由器的b2接收。会被h4接收，也会被h5。以及h6所接收。那由于这个ARP请求要查找的是呃幺五零点幺，因此只有h六会做出ARP响应。并且这个ARP响应分组是被封装到一个单播mac帧里边。可以直接指明这个帧的啊，接收方是一一三三这个节点，但是啊，这儿有个小小的细节，提醒大家注意。一个单播mac帧从h6发出去之后。

由于这个节点，这个节点，它是一个集线器，对不对？所以这个集线器会无脑的，把这个单播帧转发给h5，以及再从左边这转发出去。那交换机它不会无脑的转发，它会精准的呃，把这个单播帧呃投递给h3，所以事实上呃h6回复的这个单播帧。它会被h5收到，也会被h3收到，只不过h5检查单播帧的这个目的MAC地址。目的MAC地址和自己的对不上，

对吧？所以h5的数据链路层不会接收这个单播帧，更不会检查这个ARP响应分组内部的呃东西。好，所以h6回复这个ARP响应分组的时候有这样的一个小细节，需要注意。h3和h5都会收到这个单播帧，但是h五会丢弃这个啊单波帧。像这种小细节，如果我是出题人的话，我可能就会啊，故意的这么考考你在背后给你挖一些坑，故意让你掉进去。那这就是ARP协议再次强调ARP请求分组会被封装到一个广播帧里边，而ARP响应分组会被封装到一个单播帧里边。

那背后的细节，大家可以再结合这张导图进行回顾。最后，给大家拓展一个东西，这个东西考试不会考这么深哈，就是ARP分组的格式展开来看，到底是长什么样？首先这张图的呃一行对应的是两个字节。整个ARP分组的总长度呃，总共只有28字节。好，现在看第六个字节和第七个字节，这个字段叫operation，它表示的就是呃，这到底是一个请求分组还是一个回复分组？

如果这个字段设为一，那么表示这是一个ARP请求，如果设为二，表示这是一个ARP回复。也就是lp响应。好，然后看这个地方呃，浅绿色的这个部分总共是六个字节，48比特。那这个部分对应的就是啊，找人的那一方，他自报家门，自己的MAC地址等于多少？就是填在这个地方。再往下更绿的这个部分总共占四个字节，

也就是32比特在这儿填的，就是找人的那一方啊，他自己的IP地址等于多少？那接下来紫色这个部分这儿48比特对应的就是被寻找的那方，它的MAC地址等于多少？被寻找的那一方呃，返回这个ARP响应分组的时候就是在这个地方填写自己的MAC地址。然后末尾的这四个字节，也就是32比特写的，就是被寻找的那一方，它的IP地址等于多少？那前面这几个字段，我们就呃先不做拓展，有兴趣的同学可以自己去查一下。对了，

给大家分享一个出题的思路啊，假如我是出题人的话，那我可能会这么考，你就是说一个ARP分组，它的长度只有28字节。这个ARP分组会被封装到以太网的mac帧里。那么想一下，28字节封装到以太网mac帧里，这背后有什么可以考的东西啊？是不是会涉及到ARP分组的填充啊？我得把一个28字节的ARP分组至少填充到46字节才可以封装到以太网mac帧里边，对不对？好，所以如果我是出题人的话，这个可能会成为我考察呃大家的一个角度啊，

一个命题的角度。反正记住这句话就行了，以太网mac帧是六六二n四收发协数验n是四六一千五。意味着以太网mac帧的数据，部分最短不能低于46字节。好，那以上就是ARP的全部内容。
