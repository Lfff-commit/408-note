各位同学大家好，从这节课开始，我们会学习OSPF路由协议，它的英文全称是。这样的啊，中文译名叫开放，最短路径优先协议，那open开放的意思就是说这个协议它不被任何呃私人或者私人组织所拥有。有点像开源软件那样。所以叫开放最短路径优先协议，那之所以称为最短路径优先，是因为它的呃底层使用了狄捷斯特拉算法。会尝试找到总代价最低的一条路径。好，那这是这个路由协议，

它名字的含义在这个视频中，我们先介绍OSPF协议的特点。首先看OSPF在协议栈当中所处的位置，那OSPF协议它属于网络层。并且从左边这张图当中可以看到，OSPF协议它使用的是IP协议提供的服务，那如果一个IP分组当中携带的是。OSPF协议的数据那么呃，需要在IP分组的首部把协议字段写成89。不知道大家还记不记得IP分组的协议这个字段反映了啊？这个IP分组的数据部分携带的是哪一个协议的数据？好那么OSPF协议，它的pdu也就是协议数据单元这一整块，我们把它称为一个OSPF分组。有的地方也会翻译为OSPF数据报。

看英文术语的话，它使用的是packet这个词和IP packet，也就是IP分组，那在考研以及国内的主流教材当中啊，通常是称为OSPF分组。可以看到，一个OSPF分组由首部和数据部分组成。OSPF协议定义了五种分组类型，在这个OSPF的首部这个地方有一个字段叫做type。根据这个type字段的值等于一二三四还是五去判断后面携带的数据部分到底是属于哪一种类型。那么，第一类的分组称为问候分组，第二类叫做数据库描述分组，第三类叫做链路状态请求。第四类链路状态更新。

第五类链路状态确认关于这五类OSPF分组分别会在什么时候被使用。这一点我们会通过后面的学习再进行详细的探讨，现在我们先混个眼熟，对这几个术语大致有个印象就可以。好，接下来我会用一个简单的例子带大家感受OSPF协议，它的大致的工作原理。假设在这个框内是咸鱼电信这家公司构建的一个自治系统，其中有六台路由器，分别叫r1到r6。以及在自治系统内有六个网络，分别是net 1到net 6。这个自治系统会和外界的互联网相连，那我们可以把整个外界的互联网视为net 0。如果用cidr记法表示的话，

外面这一整个网络可以用零点零点零杠零这种默认路由去表示。好那么，假设咸鱼电信这家公司规定在这个自治系统之内使用了OSPF路由协议。那之前我们有过简要的介绍OSPF协议，它是基于链路状态路由算法。那本质上就是使用狄杰斯特拉算法去计算最短的转发路径。比如说对于r1这台路由器。它首先要尝试着建立整个网络的拓扑结构图，如果用数据结构的知识来解释的话，就是要创建一个带全有向图。把每一个路由器以及每一个网络视为顶点，而路由器和路由器之间的链路或者路由器和网络之间的链路把它视为代权，有向边。那么，只要建立起整个网络的代权，

有向图之后就可以基于这个代权，有向图去运行。狄杰斯特拉算法。这样就可以知道，从自身r1出发到达每一个目的节点的最短路径应该怎么走。那在OSPF协议当中，我们可以把带权有向图的这个呃存储理解为临接表，这种存储结构。那迪杰斯特拉算法，我们在呃数据结构那门课里面有过详细的介绍，所以接下来要解决的核心问题就是。怎么让每一台路由器都能够了解到整个网络的拓扑结构，也就是怎么去建立这个带全有向图的临界表。这个问题是OSPF协议需要解决的，那背后的原理其实并不复杂。

首先，每一台路由器。当它开机运行的时候，它可以直接探测到的信息是哪些节点和自己直接相连。比如说r1这台路由器，它可以探测到自己和r2相连。r3相连net 1相连以及net 0相连。与此同时，r1路由器也可以用某一种算法去计算出自己到身边的这些邻居啊，这条有向边的权值是多少？所以只要r1路由器一开机，那么它就可以独立的探测到这样的信息，就是说从我r1这台路由器。从这个节点出发，可以到达r2这台路由器，

并且全值是三，那在这你会看到r2全值是三。与此同时，从r1可以探测到与自己相连的另一台路由器叫r3全值，距离等于一，那你可以看到r3全值，距离等于一。好，那与此同时，还有r4对吧？全值等于三对应这个节点。和net 1这个节点相连，全值等于一，那么对应的就是灰色的这个东西，与此同时，

还有net 0。net 0对应的是自治系统外的整个外部网络，这条边的权值是二，那对应的是这个节点。这个数据结构的样子，大家很熟悉吧？是不是本质上就是一个单链表，对吧？r1这台路由器，它所对应的这个节点。作为单链表的头节点，与此同时，在这个单链表当中，记录着从这个头节点r1出发呃，与之相邻的各个节点。

以及各条边的这个权值信息，那由于和路由器直接相连的节点有两种类型。所以我们在这个图当中，用白色和灰色这呃两种链表结点去进行区分。好，这是r1这台路由器通过自己的努力，不需要别人配合啊，通过自己的努力就可以探测出的一些信息，可以把这些信息存储为一个单链表。那类似的道理，比如说对于r6这台路由器来说，它是不是开机了，也可以立即探测出和自己相连的有r4。全值为四有r5，全值为二以及有net 6，

全值为三，那么r6这台路由器它是不是也可以建立这样的一个单链表？好，所以每一台路由器都可以独立的探测自己的邻居分别是谁以及和邻居之间的距离，分别等于多少？那么，我们只需要把每一台路由器探测到的这个所谓的单链表的信息，把它全部组合起来。那这不就组成了呃，带全有向图的临街表吗？而只要有了这个图的临界表，那么基于这个临界表去运行d阶斯特拉算法。是不是就水到渠成了，对吧？好，

那么刚才我们说每一台路由器可以独立探测到的信息，其实就是这台路由器呃，它所对应的这个呃，我们把它称为单链表吧，结合大家熟悉的这个知识来讲解会更容易理解一些。比如说r6，它只能建立自己的这个单链表，对吧？r1只能建立自己的这个单链表，那么其他路由器各自呃独立建立的这些单链表。是怎么被每一台路由器所知道的呢？这就需要OSPF协议发挥作用了。好，所以接下来我们要解决的问题就是一台路由器，怎么把自己探测到的这个信息，

也就是自己构建的那个单链表的信息。说给其他的所有路由器。好，那OSPF协议是这么规定的，首先刚才我们说啊，一台路由器，它可以探测到的信息，如果用我们的大白话来讲，就是它可以知道。我是谁？我的邻居都有谁？我到每一个邻居之间的代价是多少？这是一台路由器，可以独立探测出的信息。那我们不妨把这些信息称为链路状态信息。

好那么一台路由器，探测出与自己直连的所有的链路状态信息之后，它就会把这些链路状态信息通过宏泛法。发送给整个自治系统内的所有路由器。结合前面这张图来解释的话，就是说比如说r1这台路由器，它探测到的呃链路状态信息就是这个单链表。我们不妨把这种单链表数据结构取一个名字，叫做lsa。ls指的是链路状态，而a指的是advertisement，也就是广告通告的意思。因为我们要把这个单链表广而告之，告诉其他所有路由器嘛，所以这种单链表这种数据结构，

我们就把它命名为lsa。link state advertisement好，接下来路由器r一会把这个lsa这个数据结构。把它封装到一个OSPF分组当中，然后OSPF分组会携带着lsa呃这一块信息。用红泛法迅速地覆盖到全网，可以看到红泛法的英文叫做floating，就是洪水泛滥的意思。宏泛法的工作方式呃，说白了就是一传十，十传百，好比如说r1，先把呃这个携带lsa的这个。OSPF分组。先发给自己的邻居r2r3r4，

这几台直接相邻的路由器好，各台路由器收到这块信息之后。接下来，他们又会帮忙把这个信息再度的转发给自己的呃，相邻的邻居，比如说r3从上面这个接口收到lsa的信息。那么，它就会把这个rsa的信息转发给与自己相邻的其他的路由器，也就是r4和r5。那类似的r4在收到r1发来的这个信息之后，他也会帮忙转发给自己的所有邻居。包括r3和r6。那类似的其他路由器收到一个需要被洪泛的lsa信息之后，他们也会把这个信息迅速的转发给与自己相邻的其他呃路由器。好，

那从我画的这些箭头当中，大家可以看到洪泛法的一些特性。首先，一台路由器收到一个需要被洪泛的信息之后。它会把这个信息转发到除了入口之外的所有的出口。比如说r1给r3发来了这条信息，那么r3就不应该把这条信息再重新丢回给r1。而是把这条信息从右边这条路和下面这条路进行转发。好类似的道理，像r4这台路由器，它从左上方这条路收到了，需要被洪泛的信息，那这条信息不应该回流回去。而是要从其他的所有出口转发出去，也就是左边这条路和下面这条路。

好，所以这我们要强调的是啊，被洪泛的信息是不可以回流的好，另外我们还可以看到，如果收到重复的链路状态信息。那么，路由器不应该重复的转发，比如对于r4这台路由器来说，它第一次收到这个需要被红泛的信息是从r1这边发过来的。那他帮r1红饭转发过一次之后，很快的他也会收到r3红饭给他的同一条信息，但是重复收到的这种信息，他就不会进行二次的红饭，二次的转发。好，

那这也是红泛法的特点，这两个特点可以防止红泛法呃无休无止的去呃无限转发。那可以看到宏泛法的这种特性，可以使得一台路由器它探测到自己呃直连的这些链路状态信息可以迅速的被广而告之，迅速的被其他所有路由器都知道。那同理，其他的每一台路由器一旦开机，一旦构建好自己呃，身边的这个链路状态信息之后。都是通过红泛法迅速的发给全网其他的路由器。所以各台路由器可以迅速的构建出整个网络的这种呃带全有向图的临界表存储。那有了这个临界表，存储就可以进行下一个环节。好，接下来解决另一个事情，

我们知道网络的这个结构是千变万化的，有时候可能会有新的路由器加入网络。有时候可能会因为一些线路故障或者硬件的故障，导致某一些节点或者某一些链路，它的状态发生变化。那OSPF协议是如何解决这种问题的呢？它规定，一旦一台路由器探测到身边，与自己直连的这些节点，或者与自己直连的呃，任何一条链路。啊，发生变化的时候，这台路由器就需要迅速的更新自己的lsa，也就是迅速的。

修改自己构建的这个单链表。与此同时，一旦这个单链表发生变化，那么就立即再一次洪泛给全网其他的路由器。好，所以一旦探测到变化，就立即再次红泛，这种特性就使得OSPF协议不存在rip协议的那种坏消息传播的慢的问题。因为无论是好消息还是坏消息，一旦一台路由器探测到它，就会迅速的向全网广而告之。好，那么以上就是OSPF的主要特点。接下来再介绍OSPF的其他一些特点。首先OSPF协议允许一个自治系统。

自己去设计规则，去定义一条链路的权值代价等于多少，那这样的好处就是呃，各个自治系统可以根据自己的业务属性，业务类型。去设计链路代价的计算方法。那默认情况下OSPF是呃用带宽作为参考，去度量一段链路的代价的。一段链路的代价等于参考带宽除以接口的实际带宽。那这儿的参考带宽指的是100 mbps。好比如说r1到r3的这段链路，我们给它写的代价等于一，这就意味着r1和r3连接的这个接口。这个接口的实际带宽也会到达100 mbps，这个速度。

好，另外像r5和r6它们之间的这段链路，我们把它的代价写为了二，那么按照默认的这种啊，链路代价计算规则的话说明。这段链路的带宽应该是等于50 mbps，参考带宽是100 mbps。这是一个常数值，然后50 mbps是这段链路的实际带宽，所以这段链路的权值，我们把它设为二就是这个意思。那值得说明的是。在我们的这个示意图当中，我们把这一整个网络的拓扑图把它画成了带全的无向图。这是为了让这个示意图看起来更简洁，

但是在现实应用当中，两个节点之间的链路，我们应该把它抽象为。图的两个带权有向边。并且在现实应用当中，这两个方向的有向边，它们的权值可以是不相等的。那在本课程当中，为了简化图示，除非我特别说明，否则我们就认为两个相邻的这个呃路由器之间。两个方向的边的权值都是相同的。这样可以让示意图更简洁一些。在历年真题当中，比如二零一四年的呃那道大题。

它就是采用了我这种图式的风格。好，另一方面，值得一提的是，在现实应用当中，这种路由器到达直连网络的这条边。这个方向的边，它是有权值的，而直连网络到达路由器这个方向的边，我们通常会把它的权值视为零。所以，基于这样的规定r1，这台路由器到达net 2的距离应该是三+1=4。而net 1到net 2的距离应该等于零+3+1，同样等于四。

就是这么个意思。那这些都是OSPF协议的规定。在考试当中，如果你看到图示是这种两个路由器之间的无向边，那么你就认为两个方向的权值都相等。而对于路由器和网络直连的这种情况，我们最好是按照OSPF协议的官方规定去度量它的边的权值。注意网络到直连路由器的这条有向边权值，需要视为零。好，接下来看OSPF的下一个特点。OSPF协议呃，支持多条等价路径共存，比如说呃，对于r3。

到达net 6这个网络如果走红色箭头这条路线，那么它的总代价应该是一+4+3=8。那如果换一条路走黄色的这条路等于三+2+3，同样等于八，所以对于r3这台路由器来说。上面这条路和下面这条路，它都会使用，比如最简单的一种策略就是，但凡转发到net 6的。IP数据报第一个往上面转发，第二个往下面转发，第三个往上面转发，第四个往下面转发，那这样的话就可以把IP数据报的这种转发路线。让它负载均衡，

从不同的线路去转发。好，那这是OSPF支持的特性。好，接下来看第三个特点，这个特点涉及到安全性OSPF分组，具有鉴别功能。从而可以保证，仅在可信赖的路由器之间交换正确的链路状态信息好，比如说看左边这张图。假设有一个黑客，他潜入了咸鱼电信的机房。然后把一台黑路由器连接到了r1这个地方，与此同时，这个黑路由器伪造了自己的链路状态信息。

比如说它可以利用OSPF的这个红泛功能。去欺骗其他所有的路由器，就告诉其他人说从我这走到达net 2 net 3 net 4 net 5 net 6的距离都等于一。那这样的话，其他所有的路由器是不是都会误以为从这台黑路由，这可以到达这几个目的网络。那可想而知IP数据报的转发会受到非常大的影响。甚至会直接让这个自治系统瘫痪。不过还好OSPF的分组是具备鉴别功能的，如果是一台未经授权的呃不可信赖的路由器发来的这些信息，那么其他的路由器可以鉴别出来啊，这是一条假消息。并且把它无情的抛弃，那这样就可以防止黑客的攻击。好，

再比如这种鉴别功能，也可以防止中间人篡改OSPF的呃泛分组。比如说r3这台路由器，假如说它被黑客攻击了呃，俗称中病毒了，那么其他路由器让r3帮忙洪泛转发的呃链路状态信息。是不是有可能被这个黑客被这个病毒给修改，故意改成一个错误的信息，对吧？那这样的话，其他路由器收到错误的信息之后。他一看，哎是r3发过来的，那r3是好人啊，他不是黑路由，

那如果像这样错误的相信了r3这台路由器是不是也会导致整个自治系统崩溃？不过还好OSPF的这种鉴别功能可以让这些路由器们不仅抓出黑路由，这种外来黑客与此同时也可以鉴别出一台看似合法的路由器发来的错误的信息。好，所以OSPF的鉴别功能保证了自治系统的安全性。好，接下来第四个特性，它可以支持可变长度的子网划分和无分类编址cidr。这特点没什么好说的，比如说呃咸鱼电信就这家公司，它拥有这样的一整块地质资源。然后从这一块地址资源当中。划分出了不等长的子网NAT 1 NAT 2 NAT 3 NAT 4 NAT 5 NAT 6，那这些子网都是呃不定长的。与此同时，

我们可以用这种cidr记法去表示这个子网。好，这是第四点，接下来最后一点，每一台路由器在洪泛自己的链路状态信息的时候，都会带上一个32位的序号。用序号去表示呃链路状态的新旧。相当于标注了信息的版本号好，举个例子，对于r5这台路由器来说，目前呃，它探测到的链路状态信息是。和r3，r6以及net 5相连全值分别是三二一。好，

假设这是r5这台路由器，刚开机的时候探测到的链路状态信息。也就最初始版本的一个信息，那此时就可以把这个链路状态信息序号啊设置为一。当然，也可以设置为从零开始，这个无所谓，知道它的逻辑就行好，接下来假设我们在r5和r4之间拉了一条新链路。另外r5和r3之间的这条链路呃全值变成了二，那这个时候r5可以迅速的探测到这种变化，于是r五会。迅速的构建最新的这个链路状态信息。此刻r5和r3r4r6 net 5直接相连。全值分别是二一二一。

好，那么这个链路状态信息，它的版本进行了更新对吧？所以序号字段就应该在原先的基础上加一。那我们知道路由器构建好，这个自己身边的链路状态信息之后。这个信息是需要洪泛给其他的路由器的，对吧？那么在洪泛的过程当中，如果一台路由器。他重复收到了r5红泛过来的这个信息，那么他就可以根据他所收到的这个呃序号的大小去判断到底应该以哪条信息为准。那显然，序号越大，版本就越新。

那么，其他路由器就应该使用更新这个版本的信息，对吧？好，那么这是序号字段的作用。好的，在这个视频中，我们介绍了OSPF的特点，我们说OSPF属于网络层的协议，它使用IP协议提供的服务。那OSPF路由协议，它是基于链路状态，路由算法事实上就是狄杰斯特拉算法。用这种算法去计算最短的转发路径，那么要运行迪杰斯特拉算法，

需要有一个前提。就是每一台路由器都能够清楚的知道整个网络的呃拓扑结构，结合数据结构的知识，我们可以用临街表去表示一个带全有向图。用这种方式去记录整个网络的拓扑结构。那接下来要解决的问题就是每一台路由器怎么去完善，去构建整个临界表的信息呢？那过程就是首先每一台路由器开机的时候，探测与自己直接相连的所有的链路状态信息。然后把自己探测到的这些信息用宏泛法，这种方式迅速的发给其他所有的路由器。那为了让其他路由器了解到自己发出的这个链路状态，信息版本的新旧。所以在发送这个链路状态信息的时候，也会附带上一个序号，

这种呃字段。序号越大，表示链路状态，信息版本就越新。好，那每一台路由器一旦开机迅速红泛，这样的话就可以让全网的所有路由器都迅速的构建出整个网络的呃拓扑结构。那在网络运行的过程当中，每一台路由器都会保持探测自己身边的那些链路的状态。一旦探测到身边链路有变化，那么就会再一次立即洪泛最新的链路状态信息。这样的话，全网的路由器就可以迅速的知道呃，网络当中哪些地方发生了变化？那这是OSPF的大致原理，

那在这节课当中，我们也介绍了OSPF的其他一些特点。首先。呃，这种协议允许自治系统灵活的自定义在本自治系统内这个链路的代价如何计算？另外OSPF支持等价多路径转发，这样带来的好处就是可以实现负载均衡。好，另外，为了保证自治系统的安全性，OSPF的分组还支持鉴别功能，这样就可以识别出非法的路由信息。这个非法路由信息可能是呃，非法的路由器发来的信息也有可能是合法的路由器发送的一些错误信息。那最后OSPF还支持变长子网划分和cidr无分类编制。

好的，那么通过本节课的学习，我们已经对OSPF协议的呃大致原理有了一个框架性的认知。在后面几节课中，我们还会深入探讨OSPF的其他原理。好的，那以上就是这节课的全部内容。
