各位同学大家好，在这节课中我们要探讨bgp路由选择的策略。首先，什么是bgp路由选择呢？就是说，对于一个处于外部自治系统的目的网络x来说。假如知道了有好几条呃自治系统的路径都可以到达这个目的网络，那么到底要选哪一条路去走？这个就是bgp路由选择要探讨的问题。如果有多条路都可以走，那么首先会选择本地偏好值最高的路线，这个策略不行的话，就采用第二个策略。就是选择as跳数最少的路线，二步行再采用策略，

三使用热土豆路由选择算法，三步行再选择四。选择bgp标识符最小的一条路由。好，具体是什么意思？我们结合实例来理解。首先看上面这个例子，假设as 5这边有一个目的网络x。那么，通过之前的学习，我们知道as 5这边的边界路由器会把本自治系统的这个可达网络信息。逐层的往外传递，最终远方的as 1这边的路由器就可以知道。到达目的网络x可以走上面这条路，上面这条路需要经过as 2 as 3和as 5。

那与此同时，还有一条路可以选，可以走下面，这条路需要经过as 4和as 5。好，那么对于as 1这边的路由器来说，假如要转发一个IP分组到目的网络x。那么，到底要选上面这条路还是选下面这条路呢？这就是所谓的路由选择要探讨的问题。首先要看上面这条路和下面这条路。谁的本地偏好值更高？所谓的本地偏好值，就是这两条路的优先级谁更高。那这个优先级通常是由呃自治系统的管理员去设置的。

通常会考虑成本，安全等因素，比如说上面这条路，虽然看起来好像更长一些，对吧？但是如果说as 2这边它收取的服务费比as 4更低一些，那么as 1的这个管理员。它就可以设置asr这边发过来的路由信息，它的本地偏好值更高。可以做这样的设置，也就是说优先走上面这条路。除非上面这条路走不通，比如说网络故障，此时才会选择下面这条路，因为下面这条路，

它的服务费用更贵嘛。好，所以本地偏好值是呃管理员他人为设置的。管理员偏好哪条路就优先选择那条路好，这是路由选择的第一步。如果用这个策略，可以选出一条路。那么，后面的步骤就不需要再继续了，但是如果呃，管理员没有设置本地偏好值，也就是上下两条路，我都喜欢。走哪条都行，那这种情况下，

我们就需要用第二个策略去进行路由选择。第二个策略最符合我们的直觉，要选择as跳数最少的，比如说在这个例子当中，我们假设管理员。对各条bgp路由没有本地偏好，那么对于as 1这边的路由器来说，它会优先选择as跳数更少的那一条路。那显然，走下面这条路，只需要再经过一个两个as。而上面这条路需要经过一个两个三个as，所以下面这条路的as跳数更少，因此走下面这条路。那这个地方值得注意的是as的跳数最少，

不意味着路由器的总跳数最少，比如说as 4这个自治系统，假如它里边的路由器呃连接的非常长。在这个自治系统内部，需要经过很多很多个路由器的层层转发，那么虽然看起来下面这条路，它的as跳数更少，但实际上路由器的跳数可能反而会更多。好结合这个特性，我们也能够理解为什么我们说bgp协议它只会选择一条比较好的路线。而不是说选一条最佳的路线。这条路看起来比较好就行了，它到底实际上是不是最好的这个问题过于复杂？bgp协议不追求完美。好，

这是第二个策略。接下来，在有的情况下。用一二两条策略，可能也无法选出唯一的转发路线，那这种情况下，我们就需要采用第三条策略。去进行路由选择。比如说我们把这个例子改造一下。对于目的网络x来说，如果ase这边要转发一个IP分组到目的网络x，那么这个IP分组可以走上面这条路，也可以走下面这条路。上下两条路，它们的本地偏好值都一样，

以及as的跳数也都一样。都只需要经过三跳as。那在这种情况下，我们会使用热土豆路由选择算法，这种算法的理念就是呃，我们的一句俗语。就是烫手的山芋，你要尽快把这个山芋扔出去，对吧？那烫手的土豆也是一样的，如果你手里有一个烫手的土豆，你得把它尽快的扔出去。说到这里，给大家介绍一个云南的小吃啊，当地人叫做烤洋芋，

洋芋就是土豆的意思，把这个土豆放在烤架上，一整个烤。你可以把烤好的土豆拿过来啊，蘸料吃，那这个土豆虽然好吃，但是它很烫手。反正不能一直拿在手上。那对于ase这个自治系统来说，这个IP分组如果还没有走出呃本自治系统的话，它就是这个自治系统烫手的土豆，需要尽快的把它脱手，尽快的把它。从本自治系统内丢出去。所以，

所谓的热土豆算法，它就是要结合内部网关协议，比如说rip或者OSPF协议。去判断呃，这么多条路线当中到底走哪一条路可以让IP分组以最小的代价去离开本资质系统？那代价的度量和呃内部网关协议相关。rip以路由器的跳数作为代价的度量，而OSPF是以路径总长度作为代价的度量。好结合实例来理解呃，我们假设在这个例子当中as 1的内部采用的是rip路由协议。现在左边这台路由器，它有一个IP分组要发往x呃，这个目的网络。那么，这个路由器从右上角这台路由器这学到了呃一条路，

它需要经过三跳。以及从右下角这个路由器，这也得知一条路同样需要经过三条路由器。如果走上面这条路，那么IP分组就应该转发给右上角这个路由器。如果走下面这条路，那么这个IP分组就应该转发给右下角这个路由器。那基于rip的规则，如果IP分组转发给上面这个路由器，那么经过一跳两跳。才可以把这个烫手的土豆扔到本自治系统的外面，对吧？尽快脱手，而如果走下面这条路，那么这个IP分组需要经过一跳。

两跳三跳才可以扔出本自治系统之外。好，所以基于热土的路由算法的这种思想显然走上面这条路所需要付出的代价更小，对吧？就说这个烫手的土豆，这个IP分组可以用更小的代价尽快的离开本自治系统。所以在这个例子当中，这个IP分组就会走上面这条路。好，那类似的道理，假如下面这台路由器，右下角这台路由器，它也有一个IP分组。需要发往呃x这个目的，网络可以绕一大圈，

从上面这条路出去，对吧？也可以直接从本路由器这。往外丢。那显然，直接把IP分组丢给as 4的这个邻居路由器，这样可以让IP分组尽快的脱手，对吧？因此，这台路由器如果要转发这样的IP分组。那这个IP分组就没必要往上面去兜圈子了，好，那这就是所谓的热土豆路由算法。接下来我们换一个例子，假设as的内部采用的是OSPF路由协议，

并且在这个图当中，我们用橙色的字体标注出了每一条边，它的呃代价等于多少？那同样的左上角，这个路由器需要发IP分组到x这个网络，如果走上面这条路，那么这个转发的代价是六对吧？如果走下面这条路，那么转发的代价是二+3=5，所以虽然下面这条路，它的路由器跳数更多，但是根据OSPF对于所谓代价的一个评估来看。下面这条路的总长度反而是更小的，所以在这个例子当中，你会看到IP分组最终选择了下面这条路转发出去。

同样是热土豆，路由算法的呃，这种思想以最小的代价离开本自治系统。只不过我们对所谓代价的这种度量方式呃发生了变化。好，那大家可以好好体会一下这两个例子，接下来如果热土豆路由算法还选不出呃一条最佳路线的话。最后一招，我们会选择bgp标识符最小的那一条路由。在上面这个例子中，我们同样假设ase采用OSPF，不过我们把上面这条边的代价改了，现在改成了五对吧？所以对于左边这台路由器来说，如果IP分组走上面这条路转发，

那么把这个烫手的土豆呃发出本自治系统的代价是五。那如果走下面这条路转发，那同样的这个烫手的土豆走出本自治系统所需要付出的代价也是二+3=5。好，所以在这种情况下，热土豆路由算法也不行了，那此时这台路由器它会选择一条bgp标识符更小的路由信息。首先解释一下什么叫bgp标识符，就是说每一台运行bgp协议的这个路由器，它都会有唯一的一个bgp标识符。相当于每一台路由器都会有自己的一个身份证号，或者你也可以理解为是员工的工号。那我们知道在一个公司里边，每一个员工入职的时候都会有一个工号，对吧？

那假如你是呃两个员工的这个同事。呃，你像这两个员工打听一些呃事情，那如果这两个员工他说的都有道理的话，你会优先选择呃信老员工的。老员工嘛，经验更足，对吧？也就是你会优先选择相信功耗更小的那个员工，他告诉你的这个信息。那对于上面这个例子来说，我们假设as 1的右边这两台路由器，它们的bgp标识符。分别是六六六一和六六六二，各占32，

比特哈那么上面这个bgp路由器，它的标识符更小。因此，上面这台路由器告诉左边这台路由器的路由信息会优先的被采纳。相对于黄色的这个路由器是这个自治系统的老员工，而紫色的这个路由器相比于黄色的来说，它更年轻一些。所以对于左边这台路由器来说，如果走上面这条路，这个方案和下面这条路，这个方案都完全一样的话，那么它会优先选择功耗更小的这个路由器。告诉他的这个信息，也就是选择上面这条路。好，

那这是路由选择的第四个策略，由于每一个bgp路由器，它的bgp标识符都是唯一的，不重复的。所以采用第四个策略，一定可以做出最终的路由选择。那通常来说，一台bgp路由器，它的bgp标识符呃是用这个路由器各接口当中更大的IP地址来定义的。我们知道一个路由器，它可能会有很多个接口嘛，对吧？每一个接口的IP地址都不一样，那么会选择更大的那个IP地址作为这台路由器，它的bgp标识符。

另外，路由器和路由器之间，它们在建立bgp会话，发送bgp报文的时候。就会告诉彼此自己的bgpid等于多少，所以左边这台路由器知道黄色这台路由器的呃ID。也知道紫色这台路由器的ID。那这样就可以做出最终的路由选择。好的，那么在这个视频中，我们介绍了bgp路由，选择什么时候需要进行路由选择呢？就是如果一个目的网络x，它处于外部啊，资质系统。

并且到达这个目的，网络有多条路径可以走，那么到底选哪一条？这是bgp路由选择要解决的问题，对了，为什么我们在强调呃这个目的网络x它是处于外部网络的呢？假如一个目的网络，它处于as的内部，那么根本就不需要用bgp路由选择的这套规则去去寻找转发路径，对吧？在as内部，只需要用rip或者OSPF这种内部网关协议就可以找到最佳的转发路径了。好，所以这地方我们强调的是外部的目的网络，并且是有多条转发路径可以走，

这种情况才需要进行路由选择。我们会采取四个策略，一步行就采取二二步行，采取三三步行，采取四第一个策略，是选择本地偏好值最高的路线。那么，本地偏好值是由管理员手工设置的，如果多条路线，它的本地偏好值都相同，那么就进入第二步。选择as跳数最少的路线，如果有多条路线as跳数呃都同样最少，那么就采用第三个策略，热土豆路由算法。

就像烫手的土豆，要尽快脱手一样，要选择一条合适的路线，让IP分组以最小的代价去离开本自治系统。那最后如果热土的路由算法还找不出最佳的这个路线，那么就采取最后一个策略，就是选择相信bgp标识符更小的那个路由器。发来的这个路由信息，也就是要听老员工的好的，那以上就是bgp路由选择的所有内容，大家可以再结合课件自己回顾一下。
