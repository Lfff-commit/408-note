各位同学大家好，在这节课中我们要继续探讨OSPF协议的基本工作原理，那通过之前的学习我们知道。对于一个使用了OSPF协议的自治系统来说，每一台路由器可以直接探测到啊，自己和哪些节点相连？以及自己和相邻的这个节点之间的边全值等于多少，这些都是一台路由器，可以独立探测到的信息。比如说r1探测到这样的一些信息。r6探测到这样的一些信息，每一台路由器。会把自己探测到的这个链路状态信息通过宏泛法迅速的广而告之。那所有的路由器都这么做，最终就会使得各台路由器都能够迅速的收到呃，

其他路由器发来的这些链路状态信息。那么，把所有路由器探测到的链路状态信息全部都整合起来之后，就形成了我们非常熟悉的连接表。那临界表是数据结构课程当中学习的一种图的存储方法。接下来，我们把临接表这个术语啊，换一个名字，在OSPF协议当中。这个临界表会被称为链路状态数据库，英文缩写叫lsdb。link state database.链路状态数据库。其实就是把每一个路由器探测到的这些信息全部整合在一起，就组成了一个完整的链路状态数据库。

那我们知道，由于使用了这种宏泛机制，所以每一台路由器都可以迅速的建立一个完整的链路状态数据库。好比如说r1这台路由器，它构建好自己的链路状态，数据库之后就可以基于这个链路状态数据库。运行迪杰斯特拉算法，从而就可以算出从r1出发到达net 0的最短路径。路径的总长度是二。到达net 1的最短路径是直达，那这条边的权值等于一。好，再比如到达net 4这个网络，最短的路径是r1先到r3，然后再到r4，

最后到达net 4。就是这样的一条转发路径。好，再比如到达net 6这个网络。可以找到的最短路径有两条，首先是从r1到r3再到r5r6，最后到达net 6，这是第一条可选的路径。那还有一条路径呃，是从r3到达r4r4到r6，最后再到net 6。也就是说，从r1出发到达net 6这个目的网络，我们可以找到两条呃等价的路径。不过，

对于r1来说，无论走哪条路径，下一跳路由器总是r3。这台路由器。好，那么到达其他目的网络的最短路径呃，这就不再赘述。对于狄杰斯特拉算法，它的运行过程忘了的同学可以回顾数据结构相关的内容。好总之，狄杰斯特拉算法可以计算出从一个单一节点出发到达每一个目的网络的最短路径等于多少。那么，基于狄杰斯特拉算法运行的这个呃，结果接下来r1路由器就可以构建自己的路由表。那r1的路由表最终应该长这个样子。

可以看到OSPF协议，它所规定的路由表格式至少包含这样的三个关键信息呃，首先是目的网络，然后是下一跳路由器。以及到目的网络的总距离。那目的网络这个部分我用了net1net2呃，什么net0？和这张图是对应的，这是一个简写，本质上这样的一个目的网络，我们是用IP地址加子网掩码，这两个信息去表示的。比如说net 1这个目的网络，我们可以用这样的一个IP地址，加上子网掩码26比特的一用这种方式去表示。

那么，由于r1和net 1是直连的，所以从路由器r1出发到达net 1的下一跳路由器，我们没有写任何路由器的地址。它属于直接交付这种类型。好再看。对于net 2这个目的，网络r1先要把IP数据报转发给r2r2，再转发给net 2。总距离是三+1=4，所以这个地方距离信息写为了四，并且下一跳路由器是r2。好，再看最后一行net 0这个网络，我们把它呃对应了自治系统外部的整个互联网。

可以用一个默认路由去映射net 0这个网络。如果路由器r1收到一个IP数据报，那么它会根据这个IP数据报的目的IP地址。分别去对比啊，这些目的网络号是否能对得上？如果所有的目的网络号都对不上，那么最终一定能和这个默认路由。呃，相匹配，那最终这个IP数据报就会被发往net 0。那么，在net 0这边，通常是会由一个呃其他自治系统的路由器和本自治系统的这个r1路由器会有直接相连。所以下一条路由器的信息通常会记录为呃，与之相连的外部路由器，

它的IP地址。那由于这个图当中没有画出这个外部路由器，所以我们就直接记为外部路由器，大家知道我想表达的意思就行。好，那么从这个路由表当中可以看到，虽然运行狄杰斯特拉算法之后呃，路由器r1它完全可以知道，从自身到达任何一个目的网络的。完整的这个路径应该怎么走？但是在路由表当中并不需要存储完整的路径。只需要存储下一跳路由器的地址就可以了。比如说到达net 6这个目的网络。有可能是走r3r5r6这条路，有可能是走r3r4r6 net 6，

有可能是这条路。但是对于r1来说，这个IP数据报最终到底要往下走还是往右走，它其实不用关心。对于r1来说，无论走哪条路，反正它只需要把IP数据报丢给r3这台路由器即可。那这两条路路径的总长度，总距离都等于九。好，这是值得注意的一点，虽然迪杰斯特拉算法的运行结果会得到完整的转发路径，但是路由器只记录下一跳的信息，而不会存储完整的路径。另一方面，

在这个自治系统内，一旦发生了网络拓扑的变化，比如说链路的权值发生了改变，或者某些链路断开了，某些路由器坏掉了。一旦发生这些事情，那这种网络拓扑结构的变化都会立即引发红泛。无论是链路的变化，还是路由器的变化，这些变化总能被自己直接相邻的。这个路由器所探测到对吧？那么，一旦这个邻居探测到变化之后，它就会立即红泛。从而就会引起每一台路由器，

它的lsdb也就链路状态数据库发生变化。而一旦这个链路状态数据库发生了变化，那么路由器就会重新运行。迪杰斯特拉算法。会基于最新的网络拓扑结构去重新计算最短路径，并且更新路由表。好，那么现在我们已经了解到了，对于一台路由器来说，它要构造自己的路由表，第一步是要先构造本地的这个链路状态数据库。换句话说，就是要构造自己的临界表，那基于自己的临界表再来运行。狄杰斯特拉算法。

计算出最短的路径。最终再根据狄杰斯特拉算法的运行结果来构造我们的路由表。就是这样的一个过程。好，那么在这个地方呃，有一个细节值得跟大家呃提一嘴，我们在数据结构课程当中学习的迪杰斯特拉算法。呃，就是基础版的迪杰斯特拉算法，它只能找到从起点到达这个终点的一条最短路径。但是在这个地方，你会看到嗯，我们OSPF协议它运行的是改进版的迪杰斯塔算法，这种改进的算法可以支持找到多条等价的最短路径。那这个地方有必要跟大家解释一下，

以免大家回去看数据结构的时候啊，产生疑惑。好，接下来我们考虑这样的一个问题。我们说自治系统内的每一台路由器都需要在本路由器内部保存一份，这种链路状态数据库。那我们举的这个例子，咸鱼电信这个自治系统，它里边的节点链路的数量。不算多，对吧？所以这个临接表的大小看起来还能够接受，但是如果是一个超大型的自治系统。那么里边就会有很多很多的路由器。比如说一些公有云服务商，

他们构建的自治系统规模内部可能包含五万甚至十万台的路由器。想一下这么大的资质系统，如果我们用一个统一的临界表来表示它的呃拓扑结构的话。是不是会让这个数据库非常庞大？并且每一台路由器都要存储一份，这个数据库的信息。这显然会让路由器们有点压力，对不对？那除了数据库会变大，会变复杂之外，其实自治系统变大之后，洪泛的压力也会变大。因为我们说洪泛法本质上就是一个路由器，把自己探测到的呃邻居的信息一传十十传百传到全网，对吧？

但是如果全网有五万到十万台的路由器，那么每一台路由器探测到的信息都需要全网广播。那这就会使得自治系统内的这个洪范消息数量变得特别巨大。所以，自治系统太大了之后，洪范的压力也会变大。那另一方面，狄杰斯特拉算法，它的运算开销也会变大，对吧？那么对于这个问题OSPF协议是如何解决的呢？说来也简单。OSPF协议啊，支持区域划分的这种功能。比如说外面的这个方框是咸鱼电信构建的自治系统，

在整个自治系统内使用了OSPF协议。那么OSPF协议支持我们把自治系统划分成多个区域。比如说在这个例子中，我们划分为了区域零区域一区域二。区域三划分成了总共四个区域。每一个区域都会用一个呃32比特的区域标识符去标记。那么，划分区域之后，宏泛法需要交换的链路状态，信息的范围就局限在了各个区域的内部。比如说r2，他要把自己探测到的链路状态信息洪泛出去，那么他在洪泛这个信息的时候。也会指明自己所属的区域号，区域标识符等于多少，

那么他红泛的这个信息显然会被r3收到，也会被r1收到。r3收到这个红泛信息之后，由于这个红泛信息仅在区域一的内部呃进行广，而告之所以r3。虽然它同时也和区域零相接，但是它并不会把这个红方信息往区域零这边发送。并不会因此红泛的链路状态信息被局限在了这个区域的内部。那这样就可以减少洪范带来的网络通信量。另一方面，也可以让每个路由器的呃lsdb更小。比如说对于r2这台路由器来说，它只需要关心本区域内的呃网络拓扑结构就行，换句话说r2内部存储的临接表。只需要包含这个区域内的各个节点以及节点之间的边的权值，

只需要包含这些信息就行了。至于其他区域的呃拓扑结构是怎么样的？r2路由器并不需要关心，那这样就使得r2路由器的lsdb变小。那相应的，狄杰斯塔拉算法的运算效率是不是也可以得到保证？好，接下来对这张图的一些信息进行解释，首先我们会看到零号区域，这个区域比较特殊，它是所谓的主干区域。在整个OSPF自治系统当中，主干区域只会有一个。它处于中心的位置。在有的教材中，

主干区域也会被呃翻译为骨干区域，因为它英文术语是backbone array。backbone就是脊骨的意思对吧？中枢骨干。那从这张图当中可以看到，主干区域它的作用是连通其他的非主干区域。可以看到我这标了，高亮绿色的这几台路由器，它们既属于主干区域，同时也属于呃某一个非主干区域。那r3r4r7这几台路由器我们就可以把它称为区域边界路由器，英文缩写叫abr。在一个非主干区域内，如果一台路由器想要把IP数据报转发到其他的区域，那么就可以把IP数据报转发给。

本区域的边界路由器。好，所以对于一个非主干区域来说，至少会有一台呃区域边界路由器和主干区域直接相连。好，另外值得一提的是呃，任何一个非主干区域通常都是和主干区域直接相连的。假如这儿有一个区域，四它用一个路由器r10和区域三相连。这种结构是不被允许的。如果要在这个自治系统内新划分一个区域，那么这个区域四同样它也只能和呃主干区域直接相连。好，所以区域划分的越多，显然这个主干区域它的压力也会越大，

对吧？因为如果区域和区域之间相互传递IP数据报的话。那么，这个IP数据报肯定得通过主干区域进行转发，所以非主干区域的划分也不能无限制的往上增长。在现实世界中，一个大型的自治系统内部可能会划分为30到50个，这种呃普通的区域以及一个主干区域。好，这是一个小小的拓展，那刚才我们说过呃，划分区域之后，区域内部的路由器。比如说r1或者r2，这两台路由器，

它们只需要知道本区域内的节点以及节点和节点之间的边。当然r3也属于这个区域，那另一方面r3这台特殊的路由器，它也属于主干区域。他脚踏两只船，两边的情况都需要了解。好，所以像r3r7r4这种呃区域边界路由器，它们既需要知道。自己直连的这个区域，它的网络拓扑是什么？与此同时，也需要知道主干区域的这个网络拓扑结构是什么？换句话说，区域内部的这种路由器，

它构建的lsdb更小，它的临接表更小，只需要包含这些信息，对吧？但是这种区域边界路由器，它构建的lsdb就要更大。好，这是给大家拓展的一些信息。那我们来总结一下OSPF协议，它支持把呃自治系统的内部划分为多个区域。其中，主干区域只有一个，非主干区域可以有多个。根据路由器，它所处的位置不同。

大致可以分为这样的三类路由器，首先是自治系统边界路由器，比如说这儿的r6。如图所示，自治系统边界路由器会和其他的自治系统啊相连。会负责本自治系统和外部自治系统之间的IP分组的路由转发。好，这是最特殊的一个路由器。那如果说这个自治系统内和其他自治系统的网络通信量很大，那完全可以设置第二台，第三台就是可以有更多台的这种呃自治系统。边界，路由器。这种设置是呃被允许的好，接下来第二大类的呃，

路由器就是我们这标了高亮绿色的。叫做区域边界路由器。这种路由器的作用啊，刚才说过，这就不再赘述。最后，第三类的路由器叫做区域内部路由器。也可以称为非边界路由器啊，也就是我这没有高亮的这些r1r2r5r8r9。这些都属于啊，区域内部路由器。这些路由器只负责本区域内的IP数据报的路由转发。如果要发往其他区域的话，那么IP数据报会先转发给本区域的边界路由器，然后由这个边界路由器再选择合适的路径继续往外转发。

好的，那么在这节课中，我们进一步探讨了OSPF的工作原理。首先，我们探讨了啊，使用OSPF协议的路由器怎么产生路由表？首先第一步就是需要先构建lsdb。没台路由器会把自己探测到的链路状态信息用宏泛法迅速的广而告之，发给呃其他的路由器。并且一旦探测到身边的链路有变化，就会立即再一次洪泛。由于宏泛法一传十，十传百可以迅速的通告全网，所以每一台路由器都可以迅速的构建自己的lsdb，也就是链路状态数据库。

本质上，链路状态数据库你可以理解为是图的邻接表存储。每一台路由器都会独立的根据自己的lsdb去运行。迪杰斯特拉算法。从而就可以计算出从自身出发到达每一个目的网络的最短路径应该怎么走。那基于最短路径的运算结果，最终就可以生成路由表。在路由表当中，并不需要保存完整的最短路径信息，只需要记录目的网络以及下一跳路由器。然后再记录到达目的网络的总距离是多少就可以了。好，另外OSPF协议为了防止每一台路由器的lsdb过大，以及为了防止宏泛法导致的网络通信量过大。所以提出了区域划分的这种功能。

OSPF支持把自治系统划分成一个主干区域以及多个非主干区域。这样在降低了洪范压力的同时，也让每一台路由器呃，它的lsdb变小了。那lsdb变小，也就是图的临界表变小了，那狄杰斯塔算法的呃运算速度也可以得以保障。好，那引入区域划分的这个概念之后，在自治系统内部的路由器就出现了几种类别。分别是自治系统边界路由器。区域边界，路由器和区域内部路由器。这几类路由器，我们结合图示可以一目了然，

迅速回顾。最后，给大家拓展几个呃，可能会出现的术语，这几个术语是容易混淆的lsdblsalsi。它们都有一个共同的前缀ls，也就是链路状态lsdb链路状态数据库嗯，它指的就是这整个临接表。lsa它指的是链路状态通告。这是一种OSPF协议定义的呃，一种数据结构。比如说r1路由器，它探测到的自己身边的这些链路状态信息，就会用一个lsa这种数据结构来保存。从逻辑上看，

它其实就是一个链表，对吧？每一台路由器会把自己探测到的这个lsa通过OSPF分组用宏泛法。迅速的广而告之，发送给呃其他的路由器。好，这是lsa这个术语，最后还有一个术语叫lsi。I指的是information也就是链路状态信息。这个术语它是一个广义的概念。你可以说r1探测到的这些信息，它是lsi。你也可以说r1r2r3，他们探测到这些信息是lsi。你也可以说整个lsdb它们都属于lsi。

反正都是在描述链路状态信息嘛。好，所以lsi这个术语它是一个广义的概念。甚至你可以认为呃某一条边的信息，它也是链路状态信息。那做一个类比lsdb就像是一本书，而lsa就像是书里的某一个章节。而lsi泛指书的内容，你可以说书里的一个字是书的内容，可以说书里的几个章节是书的内容，也可以说一整本书，它也是书的内容，对吧？好，那这几个术语大家在看书做题，

或者去网上查资料的时候可能会遇到。比如说二零一四年的呃一道真题，就遇到了lsi这个术语。那这几个缩写都是值得注意的。接下来最后再拓展一个东西。让大家看看lsdb更完整的样子，你会看到在这个自治系统内。除了路由器节点之外，其实每一个网络都会被我们视为一个节点，对吧？但是不知道大家有没有发现，之前我们在讲的时候，其实这个临街表它是不完整的，我们只描述了从路由器出发。到达呃，

与之相邻的其他节点。就是往外走的这些边全值等于多少对吧？在之前给出的这些信息当中，并没有表示。从一个网络节点到达与之相邻的路由器节点，它这条边呃全值等于多少？那作为拓展知识呃，可以看这个图可以看到我们对这个临界表进行了扩充，我们增加了net 1到net 6。从这几个节点出发，往外走的呃各条边，它的权值的信息，比如说net 1到达r1，这条边的权值，这条有向边的权值。

OSPF协议会把它视为零。一个直连网络到达与之相邻的路由器，这个方向的边通常都会视为零。好，所以如果lsdb啊，我们把它画的更完整一些，应该是这个样子，当然除了我这画的信息之外，其实lsdb还有更多的信息。不过最主要的还是我们前面给出的这部分临界表，因为对于一台路由器来说，从自身出发到达其他任何一个目的网络的呃，这个最短路径。其实，通过这个部分就完全够用了，

完全可以基于这部分的信息去运行。狄杰斯塔算法。好，那这只是作为拓展学习，简要了解即可。好的，那以上就是本节课的所有内容。
