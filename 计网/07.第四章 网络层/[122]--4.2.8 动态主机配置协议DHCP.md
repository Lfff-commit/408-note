好的，在这节课中，我们要探讨动态主机配置协议啊，英文缩写叫dhcp。首先直接给结论啊，所有的dhcp相关的重要考点我都在这个思维导图当中，用彩色字体给大家标注出来了。啊，我们要了解dhcp的一些基本概念，以及要理解dhcp啊背后工作的一个过程。主要分为一二三四这样的四个步骤，一会儿我会结合具体的例子带大家理解背后的过程。好，首先看dhcp它的作用。当一台主机刚接入网络的时候，

它肯定是还没有IP地址的，对吧？所以我们需要给这种刚接入网络的主机分配一个IP地址，当然除了IP地址之外，还需要给它配置默认网关配置子网掩码这些东西。那所有的这些和网络相关的这些呃参数的配置都是通过dhcp协议来自动的完成的。好，那么首先来看一下dhcp在协议栈当中处于什么位置，可以看到dhcp协议它其实属于啊应用层对吧？啊，那为什么我们的考研大纲把dhcp协议放在网络层这个部分来学习呢？因为dhcp协议它的存在。主要就是为了给这个网络层所需要的一些东西进行初始化的工作，所以才把dhcp放在网络层这边来学习。但是我们要知道，

它其实属于应用层，那么应用层的dhcp协议会使用传输层的udp协议。提供的服务udp协议又会使用网络层的IP协议啊，提供的服务，而IP协议又会使用数据链路层当中的某一种。就是链路层的协议提供的服务。所以，当dhcp协议相关的数据在网络当中传输的时候啊，其实需要经过层层的封装。需要先把应用层的这个dhcp协议所定义的这个数据结构，也就是dhcp报文啊，作为一个整体封装到udp里边。那udp协议封装的这一整坨数据又要交给网络层，再封装成IP数据，报网络层的IP数据报。

再交给链路层封装成mac克。好，接下来用一个具体的例子来感受dhcp协议工作的过程。首先，在这个例子当中啊，学校这个单位。它划分了两个局域网。右边是一个局域网，左边是一个局域网，换句话说，这是两个相互独立的子网，对吧？那么两个子网啊，它们可以呃分配的这个IP地址是不一样的，对吧？

为了管理每一个子网当中的这个IP地址资源，那么我们通常来说会给每一个这种局域网。啊，配备一个dhcp服务器，你可以理解为这是一台专门的主机，好那么不同的子网当中的这个dhcp服务器。他所负责管理的IP地址资源显然是不同的，那在这个例子当中啊，我们可以看到。学校的这两个子网啊呃，它的网络前缀总共是占17个比特。左边这个子网，它的网络前缀应该是呃，这样的一串前16个比特是一六六点一，第17个比特是零。

因此，左边这台dhcp服务器，它有权去分配的啊IP地址资源就应该是这个子网当中的所有的IP地址。比如说啊，他给自己分配了幺幺六点幺点零点九，他给自己分配了这样的一个IP地址。然后给h1分配了呃点零点幺，给h2分配了点四点二，所有的这些IP地址都是。这个dhcp服务器从自己的IP地址池里边拿出来的分配给这些主机。那类似的道理，对于右边的这个子网来说，我们也会给这个子网安排一台dhcp服务器，那这台服务器它负责管理的IP地址池。的资源就是啊，

前17个比特是哒哒哒，第17位等于一对吧？跟左边那个子网不一样。然后末尾的这15个比特啊，就是可以由这个dhcp服务器自己去分配的，当然了呃，末尾的15个比特全零和全一这两个特殊的地址是不能分配给某一个呃。特定的主机去私用的，对吧？这一点我们之前强调过很多次。好，那么这是dhcp服务器在一个呃局域网内的地位。好，接下来我们做这样的一个训练，假设h3这台主机是呃，

刚接入网络的主机。接下来，我们会分析dhcp协议工作的一个过程。好，我们来看一下这个图啊h3这台主机是新接入网络的。那它本身拥有一个MAC地址，但是它此时还没有被呃分配IP地址，没有被配置子网掩码，也没有被配置默认网关，这些东西对吧？好，那么当它新接入网络之后，它会向dhcp服务器请求给自己分配自己需要的呃这些东西。那具体来说，分为四个步骤，

我们看这张图，左边这条线client啊，表示的是客户。也就是刚加入这个网络的主机，右边server表示的是服务器。它对应的就是dhcp服务器。那么，动态配置IP地址的这个过程需要经过四个步骤，第一个步骤是啊，这个客户。也就是新接入网络的这台主机要给服务器发送一个dhcp的discover，也就是dhcp的发现报文。这是第一步。第二步，服务器收到这个呃dhcp发现报文之后，

要给这台主机返回一个dhcp offer。那offer这个词一般来说是翻译为提供。就是服务器给客户主机返回dhcp啊，提供报文。第三步，客户主机要给服务器发送dhcp request报文，那request 1般来说翻译为请求，也就是dhcp请求报文。第四步，服务器又给客户端返回dhcp acknowledge，这个一般翻译为dhcp确认报文。好，这是一个总的流程，分为四个步骤，接下来我们展开看一下每一个步骤的背后发生了哪些事情？

先给大家看最终的这个图啊，做一个解释，接下来大家会看到一。二三。四会看到这样四块呃很大的东西啊，那这儿的一二三四分别对应的就是刚才我们提到的。啊，发现报文提供报文请求报文和确认报文。并且每一个dhcp报文，它都需要被封装到udp协议里边。然后再交给网络层封装成IP数据报。网络层再交给这个链路层，最终会封装成mac帧。好，所以一二三四这几个报文我们都是呃详细的描述了它从应用层到传输层。

到呃，这个网络层，再到这个呃链路层分别是怎么层层封装的？考试中比较喜欢考察的是呃，各个dhcp报文的作用以及这些dhcp报文被封装成IP数据报的时候。这个目的地址原地址应该怎么填？还有就是呃，在链路层被封装成mac帧的时候，目的地址和原地址又应该怎么填？所以我在这儿拓展的这些细节啊，虽然看起来很细碎，但是考试考的就是这么深。因此，还请大家耐下心来，我们一起把内部的这些细节给抠一抠。

好，现在我们回到这张图h3是新接入网络的啊，一台主机，它此时还没有被啊分配IP地址，没有被配置子网掩码和默认网关这些东西。好那么显然呃，应该给h3提供这个IP地址分配服务的，应该是呃，右边这台dhcp服务器对吧？那在这台dhcp服务器上会运行着一个进程，我们姑且就把它称为dhcp服务器进程。这个进程对应的这个通信端口号是67，而h3这台主机里边也会运行一个呃进程叫做dhcp客户端进程。它所对应的端口号是呃68。接下来h3内部的这个客户端进程会和dhcp服务器的这个呃服务器进程。

二者相互通信，最终完成呃IP地址子网掩码，还有默认网关的配置。好，那先来看第一步，客户主机首先要给dhcp服务器发送一个呃，这个dhcp发现报文discover dhcp发现报文。那发现豹纹长这个样子。那么，在这个dhcp发现报文当中，h3这台主机需要说明自己的这个MAC地址等于多少？相当于给这个dhcp服务器自报家门，对吧？把身份证号告诉他，我的身份证号是什么aabbccddee三三啊？

这是我的身份证号。初来乍到新接入这个网络，请给我分配一个IP地址好，那这是dhcp发现报文当中需要包含的啊，比较重要的一个内容。那由于这个dhcp报文，它属于应用层对吧？它属于应用层，因此接下来这个应用层又会交给传输层的udp协议。udp协议把这个报文封装成一个udp数据报。那么，在传输层需要关注的是端口号的信息。h3和这个服务器的通信本质上是h3上面的一个进程和dhcp服务器上面的另一个进程的通信，对吧？那么h3的这个进程，

它的端口号是68，而呃dhcp进程，它的端口号是67。所以在这个dhcp发现报文当中，目的端口号应该指向的是dhcp服务器当中的那个进程的端口号67。而源端口号应该指向的就是h3啊，它的这个进程所对应的端口号68。好，这是传输层的封装，接下来重点来了呃，传输层又交给网络层封装成IP数据报，那么由于这个h3节点。此时暂时还没有IP地址，对吧？所以在IP数据报当中，

这个源地址就只能先填零点零点零点零。还记得全零这个特殊的IP地址有什么作用吗？它表示的是本网络上的本主机，如果一台主机暂时还没有自己的这个IP地址的时候。他就只能说我我的IP地址是零点零点零点零I am nobody对吧？我谁都不是，我现在还暂时没有IP地址。好，所以这是源IP地址，需要注意的地方。接下来，目的IP地址应该写成权一，那么权一的这个IP地址，它表示的是呃。这个IP数据报希望向本网络内的所有节点去广播。

好思考一下，为什么？我们想一下啊，因为h3刚加入这个网络对吧？他连自己的IP地址都不知道，那他有可能知道dhcp服务器的这个IP地址吗？也不知道对不对，所以当他想要呃给dhcp服务器发送这个呃dhcp发现报文的时候，他就只能用广播的形式。广播的形式去寻找对不对好，所以这个目的IP地址要写成广播IP地址32比特权益。好，那么这是h3主机的网络层做的一个呃封装，接下来网络层又需要交给数据链路层，对吧？

网络层又要交给链路层啊，把IP数据报封装成mac帧嘛，好那么来看一下，在这个mac帧当中，首先源地址不用说了。h3本身就拥有自己的MAC地址。所以原地址就写h3自己的。那目的地址这块会写成48比特权一。权一的呃目的MAC地址表示的是，这是一个广播帧对吧？这是一个广播帧，那道理是一样的，因为h3不知道这个dhcp服务器，它的MAC地址等于多少？它的这个IP地址等于多少？

所以。我只能广播去找啊，没办法好，所以这样的话h3这台主机就完成了这个呃，第一个报文的封装。我们详细阐述了第一个报文从应用层到传输层再到网络层，再到呃链路层，每一层分别应该如何封装。好，那么由于最终是被封装成了一个呃广播mac帧，对吧？因此呃这个报文。它会被广播到网络当中的各个节点，对吧？每一个节点都会收到这个呃。

携带着dhcp报文的这个mac克真，但是最终只有啊dhcp服务器的这个啊服务器进程。才会真正的接收这个dhcp，发现报文好来解释一下背后的过程，我们先分析h6这台主机。当它收到这个mac帧的时候，它是怎么处理的？首先h6主机，它的链路层。它的链路层收到这个mac帧之后，显然会把这个mac帧接收下来，对吧？因为它的目的地址是全一嘛。权益意味着这是一个广播帧网络中的所有的，这个节点的链路层都会接收它好。

接下来h6的链路层。啊，收到这个mac帧之后啊，首先把这个mac帧掐头去尾，然后就得到了IP数据报对吧？那检查IP数据报。哎，发现也比较奇怪，这个目的IP地址也是一个广播地址，对吧？那广播地址是不是意味着？我的网络层，我的网络层也应该把这个IP数据报给接收下来，好接下来继续去拆解。网络层接收这个IP数据报之后，

呃，把IP数据报的首部给去掉，然后就得到了这个udp数据报，也就是到达了传输层。到了传输层，从udp数据报的首部当中可以看到，这个udp数据报它的目的端口号是67。但是h6这台主机上并没有任何一个进程呃，绑定着67这个端口号。所以，到达传输层这一步h6终于发现问题不对了，这个udp数据报肯定不是发给我的，对吧？因为我没有端口号为67的进程。于是，

到达传输层之后啊，这些数据就会被h6给丢弃。好，那你可能会觉得奇怪h16不能把67这个端口号绑定到自己的某一个进程上吗？那结论就是这个特殊的端口号是不可以被使用的，因为在互联网技术当中，一些比较重要的。特殊的这种啊进程。会专门被分配一个特定的这种端口号，这是互联网的技术标准去制定的一个规范。也就是说，67这个端口号，它只能被dhcp服务器进程这种特殊的进程去使用，而不能被一台主机上面自己编写的这个普通进程去使用。好，

所以这就确保了啊，普通的这些主机，当它收到这个udp数据报，并且检查目的端口号的时候。在它自己的内部肯定找不到一个和67相互绑定的进程。好，所以虽然所有的这些呃主机节点都会收到这个mac帧，但是最终只有dhcp服务器会把这个报文给接收下来，并且做出响应。好，那么现在来到dhcp服务器这边，它从这个mac帧当中呃，就像剥洋葱一样，一层一层的剥开，最终就拨到了这个dhcp发现报文。

好从这个发现报文当中呃dhcp服务器知道哦，现在有一个呃新人加入啊，这个新人的MAC地址是什么三三啊，这个MAC地址。那么接下来我会尝试着给他发一个offer，发一个offer，就是给他提供一个啊，这个IP地址。好于是就到达了第二步，那么dhcp服务器它从自己管理的这个IP地址池里边。找到一个暂时还没有被分配出去的空闲IP地址，比如说幺二八点二这个IP地址，现在还空闲。那么，接下来会尝试着把幺二八点二这个IP地址分配给这台呃新主机h3好，

那么来看一下这个dhcp提供报文里边需要呃。指明的一些东西首先肯定要指明给这个新主机分配的IP地址等于多少，另外还需要指明这个IP地址的租用期等于多少。租用期的意思是说这个IP地址你可以使用多长时间？所有的主机对IP地址的使用都是有这个时间限制的，如果租用期快到了，租用期快到了。那么，一台主机可以续租这个IP地址，也就是需要再跟这个dhcp服务器啊，打一个招呼。你给我分配的这个IP地址，我希望续租啊，续租一年或者续租几个小时，类似于这样啊好，

所以租用期指的就是啊，给这个新主机分配的IP地址，它可以使用多长时间？就这么个意思。接下来还需要呃告诉新主机，你所处的这个子网子网掩码等于多少？那来看一下啊，在学校的右边这个网络当中。我们可以看到它的末尾的15个比特是主机号。前边的更高位17个比特是网络前缀，对吧？所以在这个网络当中的所有的节点，它的子网掩码肯定就是17比特的一。加15比特的零对不对？那用十进制表示的话，

就是二五五点二五五点幺二八点零。好，另外还需要告诉这个呃新主机默认网关的IP地址等于多少？那默认网关是呃b2这个接口。它的IP地址是幺二八点幺好，所以这个东西也需要告诉他，那么这是dhcp提供报文里边包含的内容。接下来需要把应用层的这个数据交给传输层进行封装，那传输层同样使用的是udp协议。udp协议会把这些数据封装成一个udp数据报，并且写入目的端口号和源端口号。那由于这个报文是要从服务器发给呃，这个客户端的，所以源端口号应该写的就是67目的端口号是68。就是这个样子。

好，接下来传输层再把这些呃数据交给网络层，封装成IP数据报。那么，由于此时这个h3节点暂时还没有IP地址，所以目的IP地址就只能先写成广播。那另一方面，原IP地址直接写dhcp服务器，它自己的IP地址就行了，对吧？虽然对方还没有IP地址，但是我自己是有IP地址的，所以原IP地址写自己的。目的IP地址写成广播IP地址。好，

接下来又到了链路层，链路层这个地方出现了区别。由于啊，由于这个啊h3。在刚开始的时候已经告诉dhcp服务器，自己的MAC地址等于多少了对吧，所以这个dhcp服务器。它可以直接把目的MAC地址写成h3的地址。而源MAC地址就是写自己的，写自己的呃，这个MAC地址也就是说这个dhcp啊，提供报文。它在网络层是被封装成了一个广播IP数据报，但是在链路层，它其实是被封装成一个单播帧的。

好，所以对于这个呃单波帧来说。它经过这个交换机的转发，会精准送达h3，其他节点都不会收到这个单播帧，对吧？好，那这样我们就完成了第二步dhcp服务器给这个客户主机发了一个dhcp offer。好，现在h3收到了这个dhcp提供报文呃，接下来要进入第三步。需要给dhcp服务器发送一个request，也就是请求报文，那么第三步是这个样子。好，

首先来看一下，请求报文如何构造在dhcp，请求报文当中h3这台主机需要说明啊，我是谁谁谁。我接受的IP地址是多少？这个IP地址幺二八点二是刚才这个dhcp服务器给他返回的这个报文当中告诉他的，对吧？幺二八点二。你就去用这个IP地址吧，好，所以h3要告诉这个dhcp服务器我是谁，我接受的IP地址是多少？那接下来又到了传输层封装成udp数据报啊，这就不再赘述好重点看呃，网络层和数据链路层。

到网络层这个部分啊，由于h3现在它虽然说已经在走这个IP地址申请的这个流程了啊，但是这个IP地址。呃，直到第四步完成之前，这个IP地址还暂时不属于h3，所以h3现在其实还是一个nobody。还是一个nobody，所以在网络层这个地方h3封装的这个IP数据报原地址还是只能先写成全零啊？零点零点零点零。那么，目的IP地址要写成权一一个广播数据报。为什么要这么做？我们一会再来解释，大家先记住这个结论，

这个有可能在选择题或者大题当中作为一个小问来考察。好，最后mac帧这个地方啊，也需要把它包装成一个广播帧，广播mac帧也就是目的MAC地址需要写成48比特权一。好，你现在可能会觉得有点奇怪啊，这个dhcp服务器在第二步给h3返回的这个报文当中。难道就不能告诉他自己的IP地址和MAC地址等于多少吗？为什么非要让h3在第三个步骤的时候还是去进行广播呢？我点对点的发送不好吗？好原因是这样的。dhcp服务器这个东西啊，对于一个比较大型的局域网来说，比如说在你们学校，

有可能会有很多台dhcp服务器。去负责IP地址的分配。好，那么在这种情况下，我们来思考一下，如果一个学校内部啊，有多台dhcp服务器。不同的dhcp服务器，它所负责管理的IP地址资源各不相同。比如说，这个服务器，它负责管理1000个IP地址。另一台服务器负责管理。另外的1000个IP地址。每个服务器的管辖范围各不相同，

那在这种情况下会发生的一种情况就是。当一台新主机。在第一个步骤，广播这个dhcp发现报文的时候。这个dhcp发现报文会被网络内的所有dhcp服务器所呃，收到对吧？那么，每一台dhcp服务器都以为。新节点是在向自己申请IP地址，因此所有的dhcp服务器都会给这个新节点。返回dhcp offer报文就是哎，我给你分配一个IP地址，另一个呃服务器说那我也给你分配一个IP地址。好，所以对于h3来说，

它有可能会收到多个dhcp服务器呃，返回的这个呃，提供报文对不对？好，那在这种情况下h3这个节点是不是得广而告之？告诉这些服务器，我最终选用的IP地址是。多少不然的话就乱套了，对吧？每一台服务器都以为自己已经把IP地址分配出去了。但是事实上h3最终选用的IP地址只有一个。那这也是为什么这个dhcp请求报文需要用广播的形式告诉全网的原因，因为在现实的这个网络当中。同一个局域，网内部可能会存在多台dhcp服务器，

所以这个新节点需要广而告之，告诉所有的dhcp服务器，我最终接受的。是谁给我分配的IP地址好，就是这个原因好，所以这个地方IP地址要写成广播IP地址呃，目的MAC地址也要写成广播的MAC地址。好，那么这是第三个步骤，好，接下来就可以进入到第四个步骤。这台dhcp服务器收到了第三个报文，接下来要返回第四个报文，就是确认报文。那第四个报文长这个样子。

其实第四个报文和第二个报文，它俩的内容基本上是一样的啊，相当于再把这个呃给h3分配的IP地址租用期子网掩码默认网关这些东西。再一次给h3发回去，就相当于告诉他啊，我现在确认了这些东西都是你的。这些东西都是你的。好，那么这个确认报文被封装到udp里边，然后又被封装到这个呃IP数据报里边。好，那么原IP地址写的是服务器，自己的IP地址，而目的IP地址写的还是呃。广播地址。

原因和之前一样，对吧？h3现在啊，正在走这个申请IP地址的流程，但是这个流程还没走完，所以它其实还暂时没有拥有IP地址。好因此啊，第四个报文，它所对应的这个IP数据报目的地址也只能先写成权一。好，那最后到了这个链路层，链路层会把这个IP数据报封装成一个单播帧。因为这个dhcp服务器它本来就知道啊，新主机的MAC地址等于多少对吧，所以就只需要用一个单播帧把它发回去。

所以第四个报文被封装成帧之后，先交给交换机，交换机精准送达给到h3。好，那么h3在收到这个确认报文之后。它就真正的拥有了IP地址，拥有了子网掩码，拥有了默认网关的配置。好，那么这就是dhcp协议在背后工作的过程，那大家可以再结合这张图进行回顾。那对于考试来说，比较重要的东西，我都总结在了这张思维导图里边。dhcp协议属于应用层，

它是基于udp的。并且它使用了呃客户服务器模型也可以缩写为CS模型c，对应的是client s，对应的是server CS。客户服务器模型那么dhcp的客户指的就是新接入网络的那台主机，也就是希望获得IP地址的那台主机。而dhcp服务器的话，就是负责分配IP地址的那台主机，每一台dhcp服务器会负责管理一系列的IP地址池。那刚才我们说在一个大型网络内部，有可能会有多台dhcp服务器，这个是可以被允许的，只是不同的dhcp服务器，它所负责管理的IP地址池也各不相同。各管各的。

那值得一提的是，在家庭网络当中啊，通常是由我们的家用路由器去兼职dhcp服务器的一个作用。就相当于你们家的这个路由器啊，它的内部其实正在运行一个dhcp啊，服务器进程。内部正在运行这样的一个进程。所以我们家用路由器，它的功能其实是很综合的啊。那么dhcp这个部分一定要记住这四个过程，每一个过程使用的dhcp报文类型不一样啊，第一个步骤是客户端给服务器发送dhcp发现报文。第二个步骤是服务器给客户返回dhcp提供报文。在这个提供报文当中，需要给呃这个客户主机指明。

我给你分配的IP地址是多少？租用期是多少？子网掩码是多少啊？这个默认网关需要配置为多少？这些信息都在dhcp提供报文里边啊，要说明清楚。接下来第三步，客户会向服务器呃发送这个dhcp请求报文。相当于要告诉整个网络内的所有的dhcp服务器，我最终哎选择使用的IP地址是多少？好，最后第四步服务器给客户端返回dhcp确认报文。那在这四个步骤当中，网络层和链路层的源IP地址，目的IP地址源MAC地址，

目的MAC地址。每一个步骤应该如何去设置？这个是很喜欢去考的一个点哈，所以啊，还请大家注意。好的，那么以上就是dhcp的所有内容。
