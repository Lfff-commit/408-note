好的，接下来看子网划分和子网掩码在大纲当中对应这个位置，我们先把NAT这个部分给跳过，因为之前我们说IPV 4地址的发展是。刚开始被简单的分为了abcde这样的几类，后来啊，发现这样的分类方式不够满足这个时代的需求，于是提出了子网划分技术。那子网划分技术的背后，需要有子网掩码的支持。那随着技术的发展，在子网划分技术的后面，又出现了这个呃变长子网，也就是cidr这种技术。呃，

然后才出现了NAT这种技术，所以我们这节课先来看子网划分和子网掩码，在这个视频中，我们不仅要学这些概念。同时，我会带大家做一些训练，同样也是基于一个很综合的图来进行训练，只要把这几个训练做好了。那么你就能解决非常多的啊，考研真题的大题，所以这节课可能拓展的会比较多，但是相信我对你的做题解题一定有用。好，我们先看一下为什么要提出子网划分技术？呃，

在之前的视频中，我们介绍过最开始IPV 4的这个地址，分为了abcde这样的几类。比如说某一个学校，它申请了一个b类地址，那么这个IP地址的管理机构会给这个学校分配固定的16比特。这16比特是b类地址的网络号，而末尾的16个比特可以由这个学校，由这个单位自己去啊分配。那在这种情况下，一个学校的这个局域网内部最多可以支持的主机数量就应该是二的16次方减二这么多台。非常非常多，对吧？但是很多时候一个单位的呃，一个小局域网内部可能根本用不到这么多的这个主机号，

用不了这么多地址。那能不能让这个主机号的利用更充分一些呢？于是就有人提出了子网划分技术。比如说还是以这个学校呃，已经申请的这个b类地址为例，前16个比特是由IP地址的管理机构分配给它的。这些东西不能动，那后16个比特当中，比如说我们可以把呃最高位的这一个比特抠出来作为子网号。然后把末尾剩下的这15个比特作为每一个子网内部的主机号。那这样的话，这个学校的网络就被划分成了两个子网。大概就是这个样子，可以看到我这儿写的子网零，它连接的是路由器的一个接口。

而子网一是通过路由器的另一个接口去连接的，那我们知道路由器的每一个接口呃，对应的就是一个单独的独立的网络，对吧？那相比之下，在没有划分子网之前，大家看一下之前的这个图就是这个样子，一个学校的路由器它接出来之后。就是连接了一整个局域网，那这两个子网，我把它称为子网零和子网一，如果从IP地址的这个视角来看。啊，所谓的子网零就是啊，对应这个比特等于零的这种状况，

子网一就是对应这个比特等于一的状况。子网划分的好处就是可以把呃一整块很大的IP地址块拆分成两个更小的部分。然后每一个地址块。让它作为一个呃子网，其实就是变成了两个独立的网络，对吧？那从现实意义上来讲，假如说一个学校，它要开设两个校区。那么，他购买了一个呃IP地址，呃号段之后，他是不是就可以把这个号段进用这种子网划分的方式拆分成两个连续的号段？这两个连续的号段分别对应两个网络，也就是对应两个校区，

对不对？因此，子网划分可以提高这个IP地址的利用率。在这个例子当中，我们只把子网划分成了两个。如果我们要划分更多子网的话，很简单，只要把更多个比特抠出来作为子网号就可以了，比如说看这个图。在这个图当中，这个学校他就把两比特作为子网号，那两比特可以表示的是零零零一一零和一一对吧？有这样的呃，四种子网号那这样的话，这个学校购买的这个IP地址块就可以被分为拆分成四个部分。

分别作为子网，零子网，一子网，二子网，三。在子网零当中，所有的这些节点可以被分配的IP地址就是啊子网号是零零，然后后面的话就是随便自己去安排，只要不是全一或者全零就可以了。那类似的道理，像子网二当中可以去分配的这个IP地址的范围就是呃，让子网号等于一零，然后后面的。随意的去分配，只要不是全一或者全零就OK好，

那这就是子网划分的作用，可以让一个很大的IP地址块划分成几个相互独立的这种地址块。从而提升IP地址资源的利用率。那可以看到，在子网划分之前IP地址以前只有两级结构，就是网络号和主机号。而引入了子网划分技术之后IP地址的结构就出现了三级结构，网络号子，网号和主机号。如果某个单位租用了一个IP地址段，那么可以从啊，原本的n比特主机号当中抠出前k个比特来作为子网号。然后用剩余的n-k个比特作为主机号，这样的话就可以划分出二的k次方个呃大小相等的子网。这的大小相等指的是每一个子网呃被分配的这个IP地址呃块的大小是相同的。

那需要强调的是呃，每个子网当中主机号不能分配为全零或者全一。因为通过之前学习，我们知道当主机号这些个比特全零的时候，它表示的是一个网络本身。而权一表示的是，在这个网络当中的IP数据报的广播地址。好，那么进行子网划分之后，这个路由器还有各台主机的这个网络层的功能，就需要进行一些拓展。那先看主机，需要有什么变化？在传统的这种呃没有划分子网的网络当中，我们只需要给每一台主机配置一个IP地址，

以及配置好默认网关的地址就OK了。那在引入了子网划分技术之后，一台主机除了IP地址和默认网关之外。还需要给它配置一个东西，叫做子网掩码。子网掩码，我们通常是用十进制的方式来记录，但是在计算机内部，这个子网掩码其实对应的是32比特的二进制。比如说对于子网一内部的这几台主机而言，我们需要给它们呃配置子网掩码，等于这样的一个值。那把这串十进制展开，为32比特的二进制应该对应的就是17位一。呃，

再加上15位零。这的17位一对应的刚好就是网络号和子网号的，这个总比特数。我们可以用子网掩码和一个IP地址进行逐位的与运算。从而得到呃一个IP地址的这个网络号和子网号这个部分。好，具体怎么算？我们一会再来解释。我们先从一个宏观的视角去解释，为什么要有子网掩码的存在？好来思考一下，对于这种传统的网络来说，一台主机在发出一个IP数据报的时候。如果这个IP数据报是发送给自己网络，本网络内的其他主机，

那么这个IP数据报是不是不需要发给默认网关？可以直接发给本网络内的呃，另一台主机精准送达。但是如果这个IP数据报是要发给其他网络内的呃，这个主机的话，那么就需要把IP数据报传给这个默认网关。让他帮忙转发出去。好，所以你会看到当一台主机发出IP数据报的时候，它首先要做的一个很重要的事情是。先判断目的IP地址与自己的这个IP地址是否从属于同一个网络，对吧？那么，对于传统的ABC，这种分类的网络来说。

我如何判断目的IP地址和自己的IP地址是否从属于同一个网络呢？是不是很简单？如果我自己的这个网络地址，它属于c类地址，那么我就用我自己的前24比特。和目的IP地址的前24比特。进行对比，如果二者相同，那么就说明我要发的这个目的IP地址和我自己从属于同一个网络，对吧？那这是传统的分类方式，那现在如果我们把一个传统的这种呃分类的地址。再把它拆分成了两个子网，那我们还能够用这种呃传统的方法去判断吗？比如说对于这个学校。

一台主机要判断自己的IP地址和另一个IP地址是否从属于同一个网络，就不能只看前面的这16个比特。还要把子网号也考虑进去，所以只有网络号和子网号完全相同的两个IP地址才属于同一个子网。好，捋清楚这一点之后，我们就可以引入子网掩码的作用了。我们刚才说子网掩码是需要配置的。如果这个学校划分为两个子网，那么它的子网掩码就是11位七，加上15位零。刚好对应的就是网络号加子网号的这个部分，以及主机号的这个部分好，接下来介绍子网掩码的用法。假设某一台主机，

它的ip地址是这样的，什么零一零一呃。点点叉叉叉啊，我我随便举的一个例子哈。好，那么这样的一个IP地址，我们想要取出它的呃网络号和子网号等于多少？只需要和这个子网掩码。逐位相与。逐位相与。那我们知道零与上任何比特都等于零，对吧？零与上任何比特都等于零，所以主谓相与的结果就是末尾的这15个比特。全部会被置为零，

而更高位的这17个比特相与的结果刚好就是这个IP地址的前17个比特。因为一和任何比特相与都等于这个比特，它本身对吧？一和零相与这等于零，一和一相与等于一，一和零相与等于零。好以此类推，总之有了子网掩码之后，一台主机可以用自己的IP地址和这个子网掩码相与。得到自己的网络号和子网号，另外还需要把目的主机的IP地址。和这个子网掩码也是呃，同样的规则进行相与，这样的话就可以得到目的，主机的IP它的网络号和子网号等于多少对吧？

那把网络号和子网号提取出来之后，再进行对比，如果这17个比特都相等，那么就说明目的主机的IP地址和我自己的IP地址从属于同一个网络。那这就是子网掩码的作用。由于这个学校内部啊，它是取了易比特作为子网号，所以我们给各个子网内的这些主机配置的子网掩码。都是呃等于17位一和15位零都是相同的。那需要注意的是，除了给主机配置子网掩码之外，也需要给呃和各个子网相连接的这个路由器的接口。也配置上子网掩码。好捋一遍子网掩码的作用是在进行子网划分之后，我们呃，

需要用子网掩码和IP地址逐位相与。用这样的方式去提取出网络号和子网号，那这两个部分可以合称为网络前缀，所以接下来我会用网络前缀这个术语去。啊，代指这个网络号加上子网号，这些个比特那么只有网络前缀相同的两个IP地址。才归属于同一个网络。那每一个子网，其实就是一个独立的网络，对吧？另外，刚才我们说如果一个网络的内部进行了子网划分，那么这个网络当中的每一台主机以及呃。每一个路由器的接口都需要配置IP地址，

默认网关以及子网掩码。子网掩码的值配置为多少，主要就取决于在这个子网之内。网络前缀总共是多少比特？好，另一方面，如果一台路由器支持这种子网划分技术的话，我们需要呃把它的转发表进行呃一个改造。我们需要在每一个转发表项当中增加子网掩码这样的一列。那具体来说就是这个样子，在不支持子网划分功能的这种路由器当中，它的转发表只需要包含目的网络和转发接口这样的两个部分。而支持子网划分的路由器需要增加子网掩码这样的一列。举个例子，假设现在有个IP数据报，

从互联网上呃发到了这个学校的路由器，这个IP数据报的目的IP地址。是等于幺六六点一点零点一好，那么这个路由器收到IP数据报之后。它会用这个目的IP地址。和自己的每一行进行对比。比如说呃和第一行进行对比，那么路由器会用这个子网掩码，会用这个子网掩码。和目的IP地址进行逐位的与运算，这样的话就可以提取出这个目的IP地址的。前17个比特，对吧？再和这个呃目的网络号进行对比，如果能够对得上，

那么OK就从b3这个接口把它转发出去。好，那换一个IP数据报，假设现在这个呃路由器它收到的是这样的一个呃IP数据报。那么，这个目的地址先和第一行进行对比，和第一行的子网掩码逐位相与。取出前17位。显然，这个IP地址的第17位等于一，所以和它的这个呃第一行匹配不上。于是就要对比第二行，那么这个子网掩码和这个IP地址逐位与之后，那么与运算的结果和呃这个东西。刚好能够对得上，

因此这样的一个IP数据报就会从b2接口转发出来。好，接下来我们进行一些画图训练来捋清啊，背后的一些过程，首先第一个训练假设h3要给h6发送一个呃IP数据报。那h3构造的这个IP数据报源地址应该是写成它自己的地址，而目的地址。应该是写成h6的地址。h3要检查这个目的地址与自己是否从属于同一个网络，那它的做法就是用配置好的这个子网掩码。和目的地址逐位相与。另一方面，也要和自己的这个IP地址逐位相与，如果网络前缀都相同的话，那么就说明这个目的地址和自己从属于同一个网络之内。

那在这种情况下h3就可以直接啊，把这个IP数据报封装成mac帧，并且在mac帧当中直接说明h6的。这个MAC地址。于是这个mac帧通过交换机呃再到达集线器，集线器会把这个mac帧无脑的转发给h5和h6两台主机。h5主机不会接收这个mac帧，因为目的地址和自己的MAC地址不相同，而h6主机会接收这个mac帧。然后拆掉mac帧的首部和尾部，得到IP数据报。由于IP地址和自己的IP地址是匹配的，所以h6这台主机就会接收这个IP数据报。好，这是第一个训练，

没有涉及到路由器的转发，在局域网的内部就完成了这个呃IP数据报的传送。接下来第二个训练h1给h3发送。好h1和h3分别属于两个不同的子网，首先h1构造IP数据报原地址写自己的。目的地址写h3的。好，接下来要判断目的地址和自己的IP地址是否存属于同一个网络，于是把目的地址和自己的IP地址。和呃本机内配置好的这个子网掩码进行逐位与运算，得到两个IP地址的网络前缀。由于二者的网络前缀不相等。所以就说明这个目的节点和我不在同一个网络内。于是h1需要把这个IP数据报发送给默认网关，让它帮忙转发。

好那么h1可以根据ARP协议知道这个默认网关的MAC地址等于多少，那这样就可以把这个IP数据报封装成mac帧。MAC地址写的是b3，这个接口。也就是默认网关地址对应的这个接口，它的MAC地址，那这个mac帧先发给交换机，交换机再交给路由器。路由器的数据链路层把帧的首部，尾部控制信息拆除之后。再把IP数据报。交给路由器的这个网络层。接下来，路由器的网络层需要决定这个地址应该往哪转发。用目的地址和每一个表象。

进行对比。首先要和每一个表象的子网掩码进行相与。相与的结果再和目的网络号进行比较，那第二个表象可以匹配成功，所以就会从b2接口把这个IP数据报转发出去。那同样的，这个IP数据报需要被封装成帧。好封装成帧之后，就可以从b2接口转发出去。交换机根据MAC地址又转交给h3这个节点。最终h3成功收到从h1呃过来的这个IP数据报。接下来第三个训练，我们看h1给h7发送。在这个例子当中，我们会看到一个进行了子网划分的网络和一个没有进行子网划分的传统网络。

它们之间的IP数据报传输是完全兼容的。在互联网技术的发展过程当中，新技术的出现可以兼容老技术，这点是非常重要的，这有助于进行技术的过渡，对吧？你不能说当我提出一个新技术的时候，我就要求全世界所有的路由器，所有的主机都支持这个什么子网划分的功能。我们很难做到全世界范围内同时完成这个技术升级，很难对吧？所以当一个新技术提出的时候，它又能兼容老技术，那这样的话，我们就可以让这些。

比较老的设备用几年的时间慢慢的过渡到这种新技术。好来看一下h1要给h7发送数据报，那么呃，这个IP数据报的源地址是h1的地址，目的地址是h7的二零零点幺点幺点二。接下来，我们加快速度，首先h1通过子网掩码和目的地址，原地址的这个逐位与运算可以判断出这两个地址不属于同一个网络。于是h1需要把这个IP数据报封装成帧，然后把这个帧发送给默认网关。好，那么这个帧到达呃路由器之后，路由器要用目的地址和自己的这个转发表进行逐一的对比。那么，

到第三行的时候呃，可以对比成功。那这儿我们引入一个新的概念，叫做默认子网掩码。在这个例子当中，呃，这家公司的网络，他之前申请的是一个c类的网络号，对吧？c类的网络号。也就是前24比特表示的是网络号，然后末尾的呃八比特表示的是主机号。那对于这种没有进行子网划分的网络来说，我们可以给这样的网络配置一个默认子网掩码。如果是c类网络的话，

就是配置24比特的一八比特的零。如果是一个b类网络，它没有进行子网划分，那么这种b类网络的子网掩码就配置成呃二五五。点二五五。点零点零也就是前16个比特是一后16个比特是零。而如果是一个A类网络，还没有进行子网划分，那么我们可以给它配置默认子网掩码为二五五点零点零点零，也就是呃，前八个比特是全一。后面的24比特全零。这个应该不难理解，对吧？像一个c类网络，

如果它没有进行子网划分的话，那我们要判断一个目的地址是否是发给它的？是不是按照呃以前的这种呃网络地址结构就是只对比前24比特就行了，那要提取出这个目的地址的前24比特，我们是不是只需要用？24比特一和八比特的零逐位相与，就可以把它的前24比特就是提取出来。那这么做的好处就是当路由器收到任何一个IP数据报的时候，用它的目的地址去查表的这个算法都是一致的。不管是一个已经划分了子网的网络，还是没有划分子网的网络。我都是用相同的规则去处理。先和子网掩码相与，然后再和前面的这个目的网络号进行对比好，那由于第三条能够对比成功，

所以啊，这个路由器会把IP数据报封装成真。然后发给下一个路由器。那这个路由器还是传统的路由器，它的路由表呃只包含目的，网络号和转发接口这样的两个部分。那首先这个路由器会根据目的IP地址的，前几个比特去判断这个地址到底是一个A类还是b类还是c类地址？那由于它是一个c类地址，于是就需要把前24比特全部取出来。然后末位的巴比特。全部置为零。其实本质上也是子网掩码的那套逻辑，对吧？那取出这个地址的网络号之后，

再和自己的这个转发表啊表象进行对比，第一条能够对得上。于是从c2接口发出。那最后这个针就被逐层的转发就到达了h7的手上。好，这是h1给h7发送，接下来我们逆过来，让h7给h1发送。首先h7封装一个IP数据报。检查目的IP地址和自己的IP地址是否从属于同一个网络，那由于这个网络是一个传统的，没有进行子网划分的网络。所以h7不需要去管什么子网掩码，那些东西由于自己属于c类网络。所以就需要用自己的前24比特IP地址的前24比特和这个目的地址的前24比特进行对比。

好对比失败，那么说明这个目的地址和自己并不在同一个网络之内，于是h7需要把它封装成真。这个帧是要发给默认网关的。那么，这个针经过交换机到达了公司的路由器好，这个路由器接下来检查目的地址。目的地址属于b类网络，于是会把这个目的地址的前16个比特啊保留下来，后16个比特全部置为零。这样就得到了目的地址的这个网络号，然后用这个目的地址和自己的表象进行对比，由于和第二个表象对比成功。所以会从c口转发出去。好把它封装成帧之后。

呃，从c口转发出去到达了呃，这个路由器现在这个路由器是呃有子网划分功能的。那他检查目的IP地址的方法是，首先用目的IP地址和每一行进行对比。对比的方法是先用子网掩码和这个目的IP地址进行逐位相与，如果相与的结果和网络能够匹配成功，那么就从相应的接口啊转发出去。那显然，这个目的地址和第一行转发表的第一行是能够匹配的，所以它会从b3接口转发出去。由于转发接口b3的IP地址和目的IP地址从属于同一个网络，所以这个路由器在把IP数据报封装成帧的时候。就可以直接把mac帧的目的地址写为h1的MAC地址，于是这个mac帧可以精准的投递到h1这个地方。

那可以看到，在刚才例子中划分了子网的这个节点给没有划分子网的节点发送这个IP数据报是可以呃正常送达的。那反过来也是一样，这就说明了这种新技术和这种老的策略，它是相互兼容的。好，接下来最后一个训练，我们看如果h1节点给这个互联网上的某一个节点发送IP数据报啊，会发生什么情况？假设这个目的地址是一一一点，二点，三点四。好，首先h1要构造IP数据，报IP数据报的源地址是自己的。

目的地址是互联网上的某一台主机。好封装成帧之后，首先用子网掩码和这两个地址逐位相与进行对比。对比之后发现，这两个地址不属于同一个网络，因此这个IP数据报需要发给啊默认网关。好，那么封装成帧之后就逐层的。经过交换机到达了这个路由器。好，现在路由器需要查表，那显然这个目的IP地址和我们这配置的第一行，第二行，第三行。都对不上，

对吧？那现在注意这个地方，这儿有一条默认路由的信息，默认路由的呃目的网络号配为了32比特全零。子网掩码也配为了32比特全零。那这就意味着，用目的地址一一一点二点三点四就是32比特的目的地址。和32比特全零的子网掩码主谓相与，那么相与的结果肯定是32比特全零，对吧？所以相与的这个计算结果肯定能和这条默认路由匹配的上，那这条匹配的上。就可以从b1接口发出去。而于是这个数据报封装成真之后，到达了下一条路由器。

这个路由器还是一个老式的路由器？先检查目的地址，它属于A类网络。于是会把目的地址的前八比特啊保留下来，然后后24比特全部设为零。用这样的一个表象和自己的这个转发表逐一的对比，第一条对比无上，第二条对比无上好，到了最后一条。就是都对比不上的时候，我默认会从a1这个接口把它发出去，于是这个路由器会把它封装成真，然后发送到再下一跳的路由器。好，那么在整个互联网上啊，

还会有别的很多路由器，反正逐一的转发，最终就可以到达目的主机。好，那么通过这个例子，主要是想让大家感受到默认路由的作用，默认路由的配置通常就是网络号全零。子网掩码也是全零。全零的子网掩码和任何目的IP地址呃逐位相与，结果一定都是全零。所以当所有的这些呃转发表象都匹配不上的时候，默认路由的这条表象一定是能够匹配上的。好，那这是最后一个训练好，那么通过刚才学习，

我们认识到，如果一台路由器呃支持子网划分技术，那么它的转发表当中。每一个表象会包含目的网络号子，网掩码和转发接口这样的呃三个部分，那对于一个。传统网络来说，如果它内部没有进行子网划分的话，我们可以给它呃配置默认的子网掩码。A类地址的默认子网掩码是八比特权，一+24比特权，0b类是16比特，16比特零。c类是24比特一和八比特零。结合传统的ABC类地址的这种结构应该不难理解。

好，那最后我们还引入了默认路由这个概念，默认路由是路由器的转发表当中的默认项。通常来说，就是设置为目的网络号全零子网掩码也全零就可以了。当转发表当中所有的表象都不匹配的时候啊，肯定可以和默认路由，这个表象能够匹配。那这就确保了路由器的正常工作。那通过上节课和这节课的学习，我们也详细介绍了一台主机在发送IP数据报的时候啊，发生的事情。主要分为这样的两大步骤，首先主机要判断目的IP地址和本机是否从属于同一个网络。那对于进行了子网划分的这种网络来说，

就是要和呃本机配置的子网掩码逐位相与，然后检查目的IP地址和自己的IP地址网络前缀。是否相同。只有网络前缀相同的两个IP地址是属于同一个网络，如果网络前缀不同，那么就属于不同的网络。好，接下来主机会把IP数据报封装成mac帧，并且传送到链路上。如果说目的IP地址呃和自己是同一个网络。那么，就通过ARP协议找到目的，主机的MAC地址，然后再把IP数据报直接封装成真。这个帧可以直接传给目的主机，

而不需要经过路由器的转发。另一种情况，如果说目的主机和本机不属于同一个网络，那么就通过ARP协议找到默认网关对应的MAC地址。然后把IP数据报封装成阵，发给默认网关。好，那么对于一个路由器来说，它转发IP数据报的过程，我们也捋得非常清楚了。首先，路由器从某个接口收到一个IP数据报，紧接着对IP数据报的首部进行校验。还记不记得在IP数据报的首部这个位置有一个字段叫做首部校验和就是说路由器需要检查一下这个首部有没有比特错误？如果首部校验没有问题，

那么接下来就从首部当中找到目的IP地址。然后用目的IP地址去查转发表，对于一个呃支持子网划分功能的路由器来说，它的转发表向包含了目的网络号。子网掩码和转发接口这样的三个部分，我们需要用IP数据报当中的目的。IP地址和子网掩码逐位相与。然后和每一个转发表项当中的目的网络号去进行匹配，哪一条能匹配上，那么就从对应的转发接口给它转发出去。在转发表当中会有一条呃默认路由的这个表象。这条表象可以用来兜底，无论如何默认路由，这条表象一定是可以匹配成功的。那查表结束之后就可以把IP数据报从呃匹配的这个接口转发出去，

那有一种特殊情况是不需要呃把IP数据报转发出去的。如果查表的结果啊，发现转发接口和这个数据报的入口是相同的，那么就不用再转发回去。什么时候可能会发生这种情况呢？比如说h1要给h2发送一个IP数据报，那这个IP数据报当中的目的IP地址就应该是h2的IP地址对吧？但是h1的这个呃网络协议站出了一些bug。它把这个IP数据报封装成了一个广播帧。有这种可能性吧，出bug的时候封装成了一个广播帧，那么这个广播帧显然就会呃，通过这个交换机，然后到达路由器的b3接口。这个路由器根据呃目的IP地址去查表，

发现诶它的转发接口刚好是b3。出口和入口是相同的，那在这种情况下，它就不用再把这个IP数据报重新转发回去了。直接把它丢弃即可。好，这是这个地方要强调的东西。那最后还要补充一个东西，就是子网掩码的另一种记法，大家可以关注这个图的绿色字体部分。看一下h1主机写了IP地址，然后一个斜线，后面跟了17对吧？这个时期的意思就是说h1这台主机配置的子网掩码前17个比特全部是一，然后后面的比特全部是零。

就是这个意思，所以这种技法其实是比较简洁的一种技法，也是我们考试当中常见的技法。那上面这儿也是一样的意思，就是说给b3接口配置的IP地址是这样的，然后给b3接口配置的啊，这个子网掩码是17个比特的一后面。全部是零。接下来再看转发表这个位置，我们可以这么去写，就是目的网络号后面斜杠呃，跟一个数字。这种记法的意思也是一样的，就是说这个表象呃，它对应的这个子网掩码是17比特一。

剩下全部是零，那第三行的意思就是说这个表象，它配置的子网掩码是24比特一，后面全为零。那默认路由这儿就是有零个一，剩下的32位都是零。好，所以这种记法啊，很方便我们人类阅读。更容易书写，但是你要知道，在计算机内部，它其实呃还是这样的结构。好的，那么听完课之后，

大家可以直接去完成这几个题目。另外，为了让大家有更多的训练素材，在这个图当中，我把学校的子网划分成了四个。然后也给出了这个学校路由器的路由表，有兴趣的同学可以基于这张图再一次去尝试，刚才我们提到的呃训练一到训练五。最后再拓展一张图。呃，你会发现在前面这张图当中这个学校啊，这个学校虽然划分成了很多个子网，但是所有的这些子网都是直接和这个总路由器相连的。事实上，划分成多个子网之后，

我们完全可以多增加几个路由器，比如说像这个样子。我们可以用一个总路由器和外面的这些路由器进行呃通信，然后学校内部再多增加几个呃路由器。这么做的好处是什么呢？比如说h1要给h9发一个IP数据报，那么在前面这种只有一个总路由器的这种，这种拓扑结构当中。h1给h9发的数据报需要总路由器去转发h1给h3发的也需要总路由器去转发。只要跨子网的这种通信，肯定需要麻烦这个总路由器，对吧？那这样有可能会使得总路由器的这个负担比较重。所以如果我们增加这种更多的路由器。那总路由器是不是就可以轻松一些？

h1给h9发送的这个呃IP数据报通过这个路由器。去转发就行了h1给h3发的数据报通过下面这个路由器去转发就行了，那像这个圈内的路由器们还有主机们。还有交换机们。这些节点构成了一个自治系统。英文缩写叫as。这个概念之后也会涉及到。在这个自治系统中，只有这个路由器会直接负责呃，本自治系统与其他自治系统之间的一个通信。好的，那有兴趣的同学还可以再完成这两个挑战。那以上就是这节课的全部内容，大家辛苦了。
