好，接下来看TCP这个部分的最后一个考点，拥塞控制在大纲当中，处于这个位置。在TCP协议中实现拥塞控制的算法，主要有这样的三种。对于考试来说，最重要的是慢开始算法，还有拥塞避免算法。往年出现过的呃，这个考点相关的题目几乎都是这两个算法，那除了这两个算法之外，解决拥塞控制，还有一种算法叫做快恢复算法。这种算法的触发条件是呃，

当发生了快重传的时候，也就我们之前介绍过的三个冗余ack那种情况。当发生快重传的时候，就会触发快恢复这个算法，那在这节课当中，我们主要探讨呃，前两个重点算法。而后面的这个快恢复算法，我们放在下节课再来探讨好。首先，初学者很容易把拥塞控制和流量控制啊混起来。接下来，我们会探讨这二者的区别与联系来看，这样的一个例子，假设这是一个呃比较复杂的网络，

网络当中有很多路由器组成。那我们知道一个网络上面可能会连接很多台主机在这儿，我们用abcdef来表示。假设现在a主机和d主机啊，上面的两个进程正在进行，这个TCP啊连接。那么，流量控制这个功能就是要控制TCP连接的双方啊，给彼此发送数据的这个速度。也就是说，流量控制这个功能，它只管局部的问题。好，再来看什么是拥塞控制。拥塞控制，

它要控制的是整个网络当中每台主机的数据发送量。当发生拥塞的时候，希望尽快的降低网络中这些路由器的负载。所以拥测控制它是一个全局的问题，不知道大家平时上网的时候有没有遇到过啊，突然一下子就是家里的网络特别卡，或者整个学校的网络都特别卡。有没有遇到这种情况？假如啊，假如这是你们学校的网络。在高峰期的时候，比如说呃，睡觉之前可能整个学校所有人都在用这个网络刷视频啊，打游戏啊之类的。那在这种时候，

整个学校的这些主机是不是都在疯狂的给这个网络注入新的这个数据啊？那此时网络中这些路由器的负载就会变得很大，如果说啊，这些主机注入的这个数据量太大，超出了整个网络的传输能力的话。那么，网络当中就会经常出现丢包的情况，此时你就会感受到整个网络变得很卡，对吧？那当你感受到整个网络好像都变得很卡的时候，这个时候我们就说发生了拥塞。对了，有时候我也会把这个词读作拥塞啊，因为平时我们说堵车塞车的时候都是念塞。但是正规的读法应该是读拥塞在接下来的讲解中，

我可能偶尔会把它念作拥塞好，那么回到这个网络。当这个网络出现这个拥塞的时候，我们应该如何去缓解这个拥塞的情况呢？最直接的办法就是迅速的减少各台主机往这个网络上发送的这些数据量，对吧？只要所有的这些主机发送的这个数据量都往下降的话，那么整个网络的负载是不是就可以变低？那么，网络的拥塞就可以得到改善？那对于TCP协议来说，我们应该如何去啊？迅速的减少一个进程，或者说一台主机，它发送的数据量呢？

是不是可以用我们之前学过的发送窗口这个东西去作为限制啊？一个进程的发送窗口越小，那么就意味着这个进程往网络上啊，发送这个TCP报文段的速度。也会受到限制，对吧？那之前我们在学习流量控制的时候说过呃，发送方的这个发送窗口。受到接收方的接收窗口的大小限制。那接收窗口和发送窗口，这二者配合就可以实现收发双方的这个端到端的流量控制。那现在我们还想用发送窗口去解决网络拥塞的问题，因此我们引入一个新的概念，叫做拥塞窗口。这个拥塞窗口是每一个发送方都会维护的一个变量。

数据的发送方，它的发送窗口大小不能超过接收方的接收窗口大小，也不能超过拥塞窗口的大小。接收窗口这个值反映的是数据的接收方，它接收数据的能力对吧？而拥塞窗口这个值反映的是当前这个网络，它传输数据的能力。那有了拥塞窗口这个变量之后，我们就可以怎么做呢？当数据的发送方检测到网络拥塞之后。它就迅速的降低自己的拥塞窗口，那拥塞窗口降低就意味着自己的发送窗口也会呃降低，对吧？这样的话，相当于就限制了自己往这个网络上继续丢数据的这个速度。

那显然，网络拥塞这种问题并不是一台主机的问题。当一台主机检测到网络拥塞的同时，其他主机。也会同时检测到这个网络拥塞，那每台主机在检测到呃网络拥塞之后都主动的降低自己的这个拥塞窗口。那所有的主机都这么做，之后整个网络的负载是不是就降低了？就像那首歌唱的，只要人人都献出一点爱，世界将变成美好的人间，对不对？好，那每一台主机，他们都降低自己发送的这个数据量，

那么整个网络就会变成一个美好的啊，不庸色的网络。好，那现在拥塞控制的总体思路，我们已经呃捋出来了，接下来我们应该思考的就是如何去判断网络有没有发生拥塞。首先看第一种情况，假设对于一台主机来说，它的传输层TCP这边发出的每一个TCP报文段。都能够顺利的收到ack，都没有超时。那这种情况下，数据的传输是很顺利的，对吧？此时说明整个网络肯定没有发生拥塞。

好，再看第二种情况，如果说呃，一台主机的这个呃传输层，它的TCP发出的报文段。出现了超时重传。那么，此时我们可以认为，整个网络发生了比较严重的拥塞。通常来说，发出一个报文段之后，没有按时收到ack。导致这种现象的主要原因是路由器发生了丢包的状况。什么意思？比如说c这台主机，

它发出了一个TCP报文段，那这个TCP报文段在网络层是被封装成一个IP数据报的，对吧？那假如这个IP数据报到达中间的某一台路由器的时候，由于这台路由器此时负载很大。所以他来不及接收呃，这个主机发来的这个呃TCP段。那这样的话，对于c主机而言，它发出的这个TCP段是不是就会迟迟收不到ack？那路由器丢包，说明此时路由器的负载已经很重了。因此，我们可以判定为发生了严重的拥塞。好，

所以如果检测到严重拥塞的时候，这个数据的发送方是不是就应该迅速的尽可能快的缩小自己的这个拥塞窗口？也就是需要迅速的把自己的这个呃数据发送量给压下来。好，这是严重拥塞的情况，再来看另一种情况，假如啊，数据的发送方收到冗余ack。也就之前我们学习的快速重传那种机制啊，收到冗余ack，那么此时我们可以判定为网络可能有一点拥塞。收到冗余ack意味着什么？意味着数据的发送方可能发出了四个TCP报文，其中三个TCP报文可以顺利的到达。另外一个TCP报文可能是丢失了，

对吧？那么，在这种情况下，既然发出四个可以到三个，只丢了一个的话，我们可以判定此时网络可能有点拥塞，但是还没有拥塞到特别严重的情况，对吧？好，所以如果发送方收到冗余ack，并且引发了快速重传，那么此时就判定为网络有点拥塞。那既然网络有点拥塞，接下来这个数据的发送方是不是应该自觉的啊？适当的缩小一下自己的这个呃拥塞窗口。

那这就是拥塞控制的总体思想。首先，数据的发送方要判断当前这个网络呃到底是没有拥塞还是发生了严重拥塞，又或者是呃有一点拥塞。当发生严重拥塞的时候，数据的发送方应该自觉地迅速缩小自己的拥塞窗口，从而迅速的限制。自己的这个数据发送量。而如果此时网络有点拥塞的话，那么数据的发送方就适当的缩小自己的拥塞窗口。那么慢开始这种算法可以用于呃网络严重拥塞的时候。这种算法可以迅速的把拥塞窗口呃变小。好，另外快恢复这个算法，它用于解决当网络有点拥塞的时候。

适当的降低这个呃拥塞窗口的大小。而拥塞避免算法主要是在网络还没有发生拥塞的时候，小心翼翼的去调大这个拥塞窗口。好，这节课我们主要讨论的是慢开始和拥塞，避免这两种算法。那大家可以翻开王道书，找到这张图，只要我们把这张图理解清楚了，那么呃，拥塞控制这个考点就难不住你。那为了辅助大家理解这张图，我制作了一个和这张图完全呃配套的一个呃，另外的一张图。我做的这张图是用大家更熟悉的这种方式呃，

带大家看到背后发生的一些过程，那接下来的讲解我会尽量的贴合呃真题风格。所以我认为有必要先跟大家解释一下拥塞控制的考题特点，因为接下来的例子也会尽可能的去贴合真题的，这种考题特点。好，首先第一个特点，拥塞控制这个考点，它经常与流量控制综合考察，也就是说在真题当中发送窗口的大小，我们得综合考虑接收窗口有多大。以及拥塞窗口有多大。不过，在这节课的例子中，为了聚焦于这个拥塞控制相关的功能，

我们接下来讨论的这张图。会假定接收方的接收窗口足够大，会基于这样的前提条件去呃进行探讨，也就是说在这节课的例子中，我们可以认为发送窗口的大小。等于拥塞窗口的大小。那关于拥塞控制和流量控制是怎么呃综合来考察的？这个我们在学完之后会用一道真题来感受。好，接下来拥塞控制的第二个考题特点就是在历年真题当中，以及在绝大多数的教材当中。探讨拥塞控制问题的时候，通常只涉及单向传输，也就是说TCP连接的双方通常只有呃，其中的一方在发送数据。

那这样去考察的话，呃，相当于考察的难度就不会过高，也不会变得太复杂。所以在这节课要讲的这个例子当中，我们也会假设只有客户进程呃，在给服务器进程不断的发数据。服务器进程没有数据，要发给客户端，它只会返回纯粹的ack啊报文端。好，这是真题的第二个特点。接下来真题的第三个特点通常来说呃，会默认每个TCP报文段都是以最大段长，也就是满载数据的。

比如说像零九年的这个题目，他就说一个TCP连接总是以1 KB的最大段长去发送TCP段。然后后面布拉布拉给了一堆条件，最后考呃，这个拥塞窗口的大小应该等于多少？类似的一五年这道题，他说，甲乙双方建立了TCP连接，然后甲向乙发送数据，始终以这个最大段长等于1 KB。去发送这个TCP报文段。最后问甲的发送窗口等于多少？好所以拥塞控制这个部分呃，几乎所有的真题都会呃给大家，这样的一个条件，

每一个TCP报文段都是满载数据的。接下来真题的第四个特点，拥塞窗口的大小通常来说是以这个最大段长的倍数作为单位。比如说假设最大段长等于1 KB，而拥塞窗口的大小等于二。那等于二的意思是说这个拥塞窗口的大小是两KB。两倍的mss是这个意思。那在接下来的讲解中，我们也会用这样的方式去呃记录呃拥塞窗口的大小。你要知道拥塞窗口的大小一二，这个值表示的是它是呃最大段长的多少倍是这个意思。好，最后一点，在所有的拥塞控制相关的考题当中。我们都会默认接收方收到一个报文段之后会立即确认。

哪怕收到失序报文段，也会立即返回一个ack。那这就会导致什么？就会导致冗余ack对吧？最终就会引发我们之前学习的快重传。好，这是真题的第五个特点，接下来我们也会呃默认数据的接收方，它采用呃立即确认的这种策略，而不会推迟确认。好，接下来观察这张图，在这张图当中横坐标，表示的是传输轮次。本质上，

它体现的是时间的概念。它以往返时延作为单位，比如说二这个刻度表示的是从零时刻开始。到两倍的rtt这个时间点应该是什么样子？然后六这个刻度表示的是到六倍的rtt，这么长的时间的时候，这个拥塞窗口的大小是什么样子？好，所以横轴表示的是时间，它是以rtt的倍数作为单位的。接下来看，纵轴纵轴这边表示的就是拥塞窗口的大小。比如说在四倍的rtt这个时间点，拥塞窗口的大小是16，那根据刚才的解释，

我们知道拥塞窗口等于16。它表达的是呃拥塞窗口等于16倍的mss，对吧？它想表达是这个意思。好，接下来看左边这个地方，你会看到一串很奇怪的呃，英文缩写叫做ss thresh。这个东西正规的翻译应该把它翻译为慢开始门限，但是在真题当中，通常把这个值称为拥塞控制的阈值。所以接下来我会用阈值这个术语去指代啊ss thresh去指代这个东西。好，我们来解释一下这个阈值有什么作用？可以看到这儿对应的是阈值的初始值等于16。

这就意味着，拥塞窗口大于16还是小于16，这是一个分界线。当拥塞窗口的大小小于这个阈值的时候，我们会让拥塞窗口的增长速度尽量的快一点。而一旦拥塞窗口大于这个阈值，我们就需要采用一种更谨慎的做法，我们不能无限制的让拥塞窗口快速增长。一旦超过这个呃阈值，那么拥塞窗口每经过一个rtt，就只能让它加一。好，所以这个阈值是拥塞窗口的大小如何变化的一个分水岭，一个分界线。好，

那么现在我们假定呃，这个阈值的初始值等于16。接下来我们看零这个时刻到四倍的rtt，这个时刻在这段期间我们会采用慢开始算法。顾名思义，当一台主机或者一个进程，它刚开始呃往一个网络当中去发送数据的时候。刚开始不能发的太快，对吧？就像一个从来不锻炼身体的人，他如果刚开始想要尝试锻炼的时候，这个运动量不能太大，如果说第一天的运动自己能承受，那么第二天。翻一倍第三天运动量再翻一倍第四天运动量再翻一倍第五天运动量再翻一倍。

那假如你是那个锻炼的人，每天你的运动量都会在前一天的基础上翻一倍。那么这么翻下去，你很快就会累死，对不对？好，所以你作为运动的人，你会给自己设置一个呃阈值。就是当你某一天的运动量达到16，这个阈值的时候，再往后你就不能每天翻一倍了，如果前一天的这个运动量你能承受的话，那么你会选择呃后一天。这个运动量再加一对吧？逐步的往上走就是这样一个意思。

好，那么对于TCP协议数据的发送方来说也是一样的，当自己刚开始往一个网络当中发送数据的时候，因为刚开始自己还不确定这个网络到底有没有发生拥塞，对吧？所以刚开始得谨慎一点。先把拥塞窗口的值设置为一，从一开始。接下来，每收到一个ack，就让呃拥塞窗口的值加一。那这种策略就会导致每过一个rtt这个拥塞窗口的呃大小就会翻一倍。好，我们用这张图换一个视角来理解我们，说当一个TCP节点刚开始呃，

发送数据的时候，首先会把自己的这个拥塞窗口的值设置为一。这是零这个时刻设置为一。那另外，我们假设这个初始的阈值等于16好拥塞窗口设置为一，意味着此时这个进程的发送窗口大小也等于一。别忘了我们假设的前提条件，我们是假设接收方的接收窗口足够大，对吧？那基于这样的条件，发送窗口的值就永远等于拥塞窗口的值。好，所以零这个时刻数据的发送方可以给数据的接收方啊，发送的这个数据量如果按照满载数据这种形式来发送的话。它就只能发送一个TCP报文段，

对吧？所以在这个图当中，你只看到一条线，这条线对应的就是一个满载数据的TCP报文段。其中，携带的数据就是mss这么多。好，那么数据的接收方收到这个TCP报文段之后，他会立即确认。于是返回一个ack。那刚才我们说采用慢开始算法的时候，我们每收到一个ack，就让这个拥塞窗口的值加一。那由于这个时刻收到了一个ack，对吧？

所以，拥塞窗口的值就在原有基础上加了一就来到了二这个大小。那拥塞窗口变为二，意味着发送窗口也变为了二，对吧？那这就意味着接下来这个数据的发送方。它可以连续发出两个TCP报文段，每个报文段都是携带mss这么多的数据。好，所以在这个地方你会看到两条线，对吧？好，接下来到接收方这边，由于采用的是立即确认的这种呃方式。所以每收到一个TCP报文段，

它就会返回一个ack，因此返回了两个ack之后。在二倍rtt这个时间点，发送方这儿它的这个呃拥塞窗口是不是又可以加二了？每一个ack会加一对吧？那么两个ack就加二。好，现在拥塞窗口的值变成了四，那接下来发送方就可以连续的发出四个TCP报文段。相应的接收方也会返回四个ack，对吧？连续返回四个ack。好那么，在三倍rtt这个时间点，发送方收到了四个ack。

于是拥塞窗口又可以加四到了八这个值。所以在这个时间点发送方可以连续发出八个段。那相应的接收方这边又会返回八个ack。好，所以到四倍rtt这个时间点呃，发送方的这个拥塞窗口又会加八到达16这个值。那16刚好就是我们之前设置的那个初始阈值。好那么，拥塞窗口的大小到达这个分界线之后，再往后就得切换为拥塞避免的算法。接下来，拥塞窗口的增长，我们得谨慎一点，不能每隔一个rtt就让它翻倍。那来看一下拥塞，

避免算法规定在一个rtt内，即使收到多个ack，我们也只能够让拥塞窗口的值加一。当拥塞窗口大于等于阈值的时候，就会使用这个拥塞，避免算法。那相比之下，前边这个慢开始算法。它每隔一个rtt拥塞窗口就翻倍，对吧？如果一直这么翻倍下去，那拥塞窗口可能会变得特别大。这就有点害怕，对吧？好，

所以你会看到在四倍的rtt这个时间点，由于此时拥塞窗口的值等于16。所以发送方可以连发16个报文段，而接收方也会连回16个ack，那由于此时我们已经切换成了拥塞，避免算法。所以虽然收到了16个ack，但是在这一个itt内，我们只能让拥塞窗口加一。所以这个地方让它增加的慢一点，谨慎一点。好，所以到五倍rtt这个时间点拥塞窗口的值只增加了一来到了17这个值。好，那接下来就可以连发17个报文段，

然后又会连续收到17个ack。那同样的，由于拥塞窗口已经超过阈值了，所以虽然收到多个ack，我们也只能让它加一好，那后面的就呃，以此类推。一直到12倍的rtt的时候。在这个时间点呃，发送方收到了23个ack，但是这个拥塞窗口的值只能加一变成24这么大。好，现在又可以连续发出24个报文段，此时故事发生了在12倍的rtt到13倍的rtt，这段时间内。

发送方发出的呃某一个或者某几个报文段发生了超时，那么按照之前我们分析的逻辑，一旦发生了超时重传。说明此时网络可能发生了严重的拥塞，对吧？而当网络发生严重拥塞的时候，我们应该干什么？应该迅速的缩小拥塞窗口，也就是应该迅速的压制自己啊，发送的这个数据量。好，所以在这个图当中，你会看到在12倍的rtt这个时间点。一旦发生了网络拥塞或者更具体一点来说，是发生了超时重传这个事件，

那么当这种事件发生的时候，我们会迅速切换成。慢开始算法，而慢开始算法规定，这个拥塞窗口的值是从一开始的，对吧？所以你会看到拥塞窗口的大小，从24这么大，一下子回归到了一这个起点。那么，在这张图当中，你也会看到这一点，在13倍的rtt这个时间点，拥塞窗口的值又恢复到了一。那除了拥塞窗口的值要发生改变之外。

门限这个值也应该发生改变，我们会把门限的值设为拥塞窗口值的呃一半。那由于发生这个超时重传之前拥塞窗口的值是24对吧？那24÷2=12，所以发生超时重传的时候，我们会把这个门限值。让它从16变成12。那在这张图当中，对应的就是这个东西。当发生网络拥塞的时候，我们迅速的减小了拥塞窗口，也适当的减小了这个门限的值。其实这个门限值降低，本质上也是为了缓解拥塞，对吧？

因为这个门限值越小，意味着接下来这个拥塞窗口的呃增大趋势会变得更慢一些，对不对？那大家也需要在王道书上补上这几个字，这个点它对应的网络拥塞，指的是超时重传这种事件。只有超时重传的时候会一下子把拥塞窗口变成一。如果不是超时冲穿，而是收到冗余ack，那么这个拥塞窗口的值不会下降的这么快。好，所以要在自己的书上补上这几个字。好，现在在13倍的rtt这个时间点，我们又回到了慢开始算法。

在接下来这段时间内，每收到一个ack，就会让呃拥塞窗口加一，这就意味着在接下来这段时间内，每隔rtt这么长的时间，拥塞窗口的大小就会翻倍。好，那回到这张图，首先发出一个TCP，返回一个ack。每收到一个ack，就会让这个拥塞窗口加一，所以在14这个时间点，拥塞窗口到了二。接下来发出两个呃TCP报文段，

然后返回两个ack。每个ack导致呃拥塞窗口加一，所以15这个时刻拥塞窗口变成了四。接下来发出四个报文段，返回四个ack，在16这个时刻拥塞窗口变成了八。好，接下来不一样的东西来了，16这个时刻可以发出八个报文段，然后返回八个ack。那照理来说，我们现在是在慢开始算法的阶段，对吧？照理来说，八个ack就应该对应呃拥塞窗口加八。

好，但是这儿有个特殊的情况，由于刚才我们已经把阈值降为了12。所以当发送方收到这八个ack的时候，它这次的拥塞窗口不能加八，只能加四。也就是不能超过阈值的限定。好，所以在这张图当中，你也会看到这一点，照理来说呃，17倍的rtt，这个时间点应该是在16这个时间点的基础上。拥塞窗口翻倍对吧？但是翻倍之后会直接超过这个阈值。

所以这个翻倍的动作会被限制。因此，17这个时间点拥塞窗口的大小再一次来到了这条分界线。那既然到了这条分界线，再往后是不是又得切换成拥塞，避免算法了？接下来拥塞窗口的呃，增大我们得谨慎一点。每一个rtt最多只能让它加一。好，所以从17这个时刻开始就切换为拥塞，避免算法。那根据拥塞窗口的大小，可以发出12个段返回12个ack，那么在18这个时刻，

虽然收到了12个ack。但是由于采用的是拥塞避免算法，所以我们只能让拥塞窗口加一来到了13这个值，那再往后就不再赘述。反正每隔一个rtt最多只能加一。那最终到21这个时刻。拥塞窗口的大小到达了16。那对应这张图，就是来到了这个终点，那再往后的过程就不再赘述，都是一样的。好来总结一下，当一个TCP节点刚开始传送数据的时候。需要执行慢开始算法。慢开始算法会把这个呃拥塞窗口的初始值设为一。

接下来，每收到一个ack，就让拥塞窗口加一。那这就意味着，在慢开始阶段，拥塞窗口每经过一个rtt就会翻倍。那么，当拥塞窗口的大小大于等于这个阈值的时候，就需要切换为拥塞，避免算法。每一个rtt最多只能让这个拥塞窗口加一，那如果某一个时刻发生了超时重传这种事件。此时说明，网络当中出现了严重的拥塞。那在这个时刻，

首先我们得重启这个呃慢开始算法，会把拥塞窗口的值重新回归到一。另一方面，我们会把这个阈值设为发生拥塞的时候，这个拥塞窗口的大小的一半。设为这样的一个值。那再往后的过程就是类似的。拥塞控制这个考点，最重要的就是这张图，把这张图理解透了，那任何题目都难不住你。好，接下来用两个真题来练手，看零九年的39题，说一个TCP连接总是以1 KB的最大段长。

去发送TCP段，发送方有足够多的数据要发送当拥塞窗口为16 KB的时候。发生了超时。问，发生了超时之后，接下来的四个rtt时间内。所有的TCP段传输都是成功的。那么问，当第四个rtt时间内发送的所有的TCP段都得到肯定的应答的时候，拥塞窗口的大小应该是多少？好来整理一下mss=1 KB，我们用小写字母c代表这个拥塞窗口的大小。那么，拥塞窗口的大小等于16倍的mss？那么，

在这个时刻发生了超时，那超时这个事件会导致什么？首先，阈值我用小写的s来代替啊。阈值会等于当前的这个拥塞窗口值的一半，那当前拥塞窗口值是16呃，除以二就等于八对吧？阈值变成了八。另外，拥塞窗口要变成一。好，那么这就是发生超时的时候会做的一个调整。那我们不妨把超时的这个时刻记为零时刻。零时刻拥塞窗口等于一阈值等于八好，那么在一倍的rtt以后，

这个拥塞窗口是不是会翻倍啊？变成二对吧s=8，接下来再经过一个rtt，在两倍的rtt的时候，拥塞窗口又会翻倍变成四。阈值同样等于八。接下来，在第三个rtt的时候，拥塞窗口变成八。阈值同样等于八好，那么在第三个rdt这个时刻，拥塞窗口的值已经到达了，这个阈值对吧？所以到第四个rtt的时候，虽然会收到八个ack，

但是这个拥塞窗口只能再加一了，对不对？因为我们强调过，只要它超出了这个阈值，那么每隔一个rtt最多只能加一，所以到四倍rtt这个时刻。呃，拥塞窗口就变成了九。阈值同样等于八。所以这个题目选择c选项，对吧？从零这个时刻开始，这是第一个rtt，这是第二个rtt，第三个rtt，

第四个rtt，那题目问的就是在第四个rtt时间内。发送的这些TCP报文段都得到肯定应答，也就是都收到ack之后拥塞窗口的大小应该等于多少？再次强调c=9的意思是拥塞窗口是九倍的mss，假如这个题目啊，假如这个题目它把mss设置为。2 KB那么答案应该等于多少？就应该等于二×9就应该是18 KB。就得选这样的选项好，所以不要忘了拥塞窗口的这个数字和mss之间是有联系的。这些细节不注意，很容易掉到坑里。好，这是零九年的题目，

在这个题目当中，发送方的发送窗口，它只受到。呃，这个拥塞窗口的限制。因为这个题目没有给任何条件啊，告诉你说这个数据的接收方，它的接收窗口的大小是怎么变化的，对吧？它没有给这样的条件。所以在这个题目当中，发送窗口的大小就只受限于这个拥塞窗口的大小。那接下来我们再看一五年这道题，这个题目我们就得把啊拥塞窗口和接收窗口，同时考虑进去。

好来看一下，说主机甲和主机乙建立了一个TCP，然后甲的拥塞窗口初始阈值为呃32 KB。然后甲向乙始终以mss=1 KB的大小啊，去发送数据。好，我们先记下来啊mss=1 KB。初始的阈值等于32倍的mss。好，继续往后说，这个主机甲一直有数据发送，主机乙为该连接分配了16 KB的接收缓存。接收缓存的大小会影响什么？是不是会影响接收窗口啊？好再往后读，

说这个主机乙会对每一个数据段进行确认，什么意思啊？他这想表达的就是它采用的是立即确认对吧？而不是推迟确认。好，并且告诉我们忽略断的传输时延。传输时延怎么理解？大家有没有发现，当我们在画这个TCP的传输过程，这个示意图的时候，我们所有的这些线，它都是一根很细的线。我们没有画出这条线的宽度，对吧？如果这条线有宽度，

比如说一条线画这么宽的话，那么这个宽度其实就对应着呃TCP报文段的传输时延。那一般来说，我们在探讨TCP的这个传输过程的时候，我们只考虑TCP报文段的传播时延。传播时延而不考虑TCP段的传输时延，那历年真题也基本上都是这样。好，所以真题说话还是很严谨的，它把该提到的边边角角条件都会给你交代清楚。好，接下来说，如果主机已收到的数据全部存入缓存，并且不被取走。那么，

问主机甲从连接建立成功的时刻起。并且没有发生发送超时的这种情况。那么，经过四倍rtt之后，甲的发送窗口应该等于多少？好，我们回头琢磨一下这句话，说主机甲收到数据存入缓存之后不被取走，不被取走是什么意思啊？就是数据放到缓冲区里，但是暂时不交付给应用层。是这个意思对吧？也就是说在这道题目当中，我们可以认为这个接收缓存，它只会往里边填入新的数据。

而不会去清空这个缓存那，这意味着16 KB的接收缓存，剩余的空间会越来越小，对吧？那接收缓存的剩余空间变小，会影响的是什么？是不是就会影响接收窗口的大小？好，所以从刚建立起连接的那个时刻开始算起，我们把这个时刻记为零。在这个时刻，阈值等于32，接收方的接收窗口大小等于多少？我们用小写的r来表示。接收窗口大小呃只有16对吧？

整个接收缓冲区就只有这么大。好，那么刚开始传送数据的时候，主机甲的拥塞窗口应该设置为多少？应该设为一对不对？这是慢开始算法规定的。那么，在零这个时刻，主机甲的发送窗口大小就应该等于拥塞窗口和接收窗口当中更小的那个值，对吧？现在我们得综合的考虑接收窗口和呃拥塞窗口的限制了好，那么既然发送窗口等于一。因此，在零这个时刻，主机甲是不是只能给主机乙发送一个TCP报文段？

那此时主机甲的拥塞窗口就会加1c，就变成了二对吧？那这个阈值肯定没有发生改变，同样等于32。那接收窗口现在还剩多少公斤啊？16的接收窗口收到了一个报文段，因此接收窗口的剩余容量就只剩下了15。好，所以在一这个时刻发送窗口的大小就应该等于二。那发送窗口等于二，意味着甲可以向乙发送两个TCP报文段，对吧？好，所以到两倍的rtt这个时刻s的值没有发生改变，接收窗口的值。

会从15变成13，因为主机乙会把两个报文段的数据放到缓存里。并且这些数据是不交付给这个应用层的。好，那主机甲的拥塞窗口是不是会变成四又翻了一倍？那此时发送窗口就变成了四。好，下面到第三个rtt s的值不变r的值变成了13-4，只剩下九的大小。而拥塞窗口经过一个rtt之后再次翻倍变成八。那此时发送窗口的大小，是不是就等于八？那此时意味着，主机甲还可以给主机乙连发八个报文段。因此，

在四倍的rtt这个时刻。阈值s不变，主机乙的这个接收缓冲区只剩下一的空间了，对吧？九个空位当中，又消耗了八个空位。只剩下一个空位好，另一方面，主机甲的这个拥塞窗口再次翻倍。因为这个拥塞窗口还没有超过阈值，所以每隔一个rtt，它总能翻倍。好那么到四倍的rtt，这个时间点发送窗口的大小等于多少就等于一了。因为我们说发送窗口的大小同时受限于接收方的接收窗口大小以及拥塞窗口大小，

要取更小的那个值，对吧？好，所以经过四个rtt之后。假的发送窗口a选项。好，那这是一五年的这个题目。那之后大家做拥塞控制分析的这种题目也可以参考我这儿的这种写法，首先一定要提醒自己把mss。专门写下来，因为mss的值决定了这个crs，它们真正的值等于多少？我们最终都要把这些数字乘以mss。那在分析的过程当中，我们从零这个时刻开始，

每隔一个rtt你都要梳理出。这个阈值接收窗口，拥塞窗口和发送窗口要梳理出这些东西。那这样顺着往后分析，最终你就可以得到题目啊，要你求的这个答案。好的，那么在这个视频中，我们主要介绍了慢开始和拥塞避免。截至目前，所有的历年真题都只考这两个算法，还暂时没有考过快恢复算法。而关于这两个算法的复习，其实只要理解这张图就完全够了。好，

所以这节课就不写思维导图了，用这张图复习比什么都好。好的，那么以上就是这节课的全部内容。
