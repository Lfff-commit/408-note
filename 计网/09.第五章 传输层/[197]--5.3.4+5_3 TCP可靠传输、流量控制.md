好在这个视频中，我们要补充可靠传输的最后一个内容。在上节课的学习中，我们提到，由于TCP的这个呃确认机制，它具有累计确认的这种规则。所以数据的接收方如果稍微的推迟一些返回ack的时机，那么这么做可以尽可能的去发挥累积确认规则的一个优势。比如，当数据的接收方收到第一个TCP报文段的时候，它会尝试着先等待零点五秒。如果在这零点五秒之内，还有其他的TCP报文段到达，那么就可以采用累积确认的这种规则。最终只需要返回一个ack，

就可以完成对多个啊报文段的确认。那这是推迟返回ack带来的好处。然而，推迟确认的优点背后也有缺点。我们来看这样的两个例子，在左边这个例子中，我们采用推迟确认的策略。而右边这个例子中，我们会采用立即确认的策略，也就是每收到一个呃正确的报文段。就立即返回一个ack。那采用立即确认这种策略之后，就可以引入一种新的机制，叫做快速重传。好，

那由于这个图比较大呃，我们把它放大一点来看，先看左边这张图。我们假设左边这条线对应的是客户端，右边这条线对应的是服务器。好，那么从这可以看到连续出现的四条线，意味着客户端连续给服务器发送了四个报文段。我们书接上回在上节课的，最后我们说这个服务器，它的接收窗口接下来只能接收四个字节的数据了。那相应的客户端这边的发送窗口也只能为四。接下来，我们构造四个TCP段。每个TCP段携带一个字节的数据。

那么，第一个TCP段，它的序号就应该是等于呃622。第二个等于623。那后面的两个同理。好，现在我们假设六二三这个TCP段，它在呃传输的过程当中丢失了。就是这个东西，它丢失了，而另外的三个呃TCP段都可以顺利的被呃接收方接收。因此，六二二。六二四六二五这三个数据都可以被顺利的接收。注意，

我们现在探讨的这个前提条件是说数据的接收方采用推迟确认的策略。那推迟的时间不能超过零点五秒。那么，假设在零点五秒之内。数据的接收方收到了第一个，第三个和第四个报文段，那么推迟了零点五秒之后，在这个时间点。它就不得不呃给发送方返回一个ack，那此时返回的确认号应该等于六二三发送窗口的大小应该等于三。那结合这个图不难理解。现在六二三之前的所有的呃，这个数据都已经收到了。那么，现在服务器这边还可以接收从六二三开始，

再往后的三个字节。好确认号等于六二三。而接收窗口应该等于三。在这个例子当中，接收窗口等于三，这一点是非常值得去关注的。因为很多初学者会有一个误解，就是说。接收方这边它的接收缓冲区还有多大的空位？我的接收窗口就应该返回多大对吧？但事实上不是这样的。接收窗口和确认号这两个东西是组合起来看的，当客户端收到这两个东西的时候，他就会知道。在六二三之前的所有的数据都已经被按序接收了，

而目前可以给服务器发送的，应该是从六二三开始。往后的三个字节。好，所以通过这个例子啊，一定要注意理解服务器这边返回的确认号，还有接收窗口的值等于多少，特别是接收窗口的值这个东西。那对于客户端而言，它之前发出这四个报文段的时候，是不是分别给这四个报文段都设置了各自的这个计时器？超时计时器对吧？那现在根据服务器返回的这个ack，可以确定六二二这个字节肯定是已经被接收了。因此，

六二二这个字节不必再占用发送缓冲区。那与此同时，由于现在对方的接收窗口大小等于三。因此，客户端的发送窗口也得缩小。变成三的大小。好，那现在你会发现这样的现象，客户端这边发送窗口框住的这些数据都已经发出去了，对吧？而且这些数据现在还都没有超时。那既然这三个报文段暂时还没有超时，那对于客户端来说，现在需要立即重发吗？不需要对吧？

得等到他们三个都超时了，再依次的把它们重发。好，所以回到这个图，刚才我们说服务器返回了这个ack之后，客户端这边它的发送窗口当中已经没有可以发送的数据了。那直到这个超时重传计时器，在这个时间点六二三这个报文段终于超时了。那在六二三超时之后，之前紧接着六二三发出的六二四六二五这两个呃报文段是不是也会接连的超时对吧？所以接下来这三个报文段会依次超时。然后这三个报文段又会被依次的呃超时重传。但实际上，通过刚才的例子，我们知道后面两个报文段其实早已经被呃顺利的接收了，

对吧？其实大可不必重发。好模拟一下这个过程。这三个段依次超时，于是引发超时重传。这三个报文段又会再次的从呃客户端。发往这个服务器，那么假设这次服务器这边呃能够顺利的收到三个报文段。服务器会检查自己的接收，缓冲区发现后面两个报文段是重复的报文段。我只需要把六二三这个报文段接收进来。那么，由于接收缓冲区已满，所以这些数据啊，应该尽早的交给这个。

应用层。那接下来服务器这边期待接收的下一个序号，就应该等于六二六对吧？所以返回的确认号等于六二六。而接收窗口现在已经变成了八。好，这个ack端顺利的传给呃客户端。这次客户端传出的三个报文段都没有超时，于是根据ack的值，可以确定这三个报文段都已经顺利的接收。把它们移出缓冲区。那空出来的缓冲区读入新的数据。发送窗口调整为八。好，那么这就是结果，

那刚才这个例子中，我们看到了一个呃最严重的问题就是。呃，我们采用推迟确认的这种机制。确实能够尽可能的发挥累计确认的这种特性，但与此同时，如果多个报文段当中。其中的呃个别报文段，它的传输出现了问题，那很有可能会导致几个报文段陆续的超时。而其中的某些报文段本来是没有必要重传的，对吧？好，那这个问题怎么解决呢？接下来我们引入呃快重传机制。

快重传机制需要基于立即确认的啊，这种策略去进行立即确认，就是推迟确认的反面。看右边这张图还是同样的故事呃，客户端发出了一二三四，连续发出了四个呃TCP报文段。其中，第二个报文段它丢失了。那由于现在我们采用了立即确认的机制，因此服务器这边每收到一个报文段，就立即给客户端返回ack。好，接下来携带六二三这个字节的报文段，不巧在中途丢失了。那再往后服务器这边收到的就是携带六二四那个字节的报文段。

虽然这个报文段是失序到达的，但是收到这个报文段的时候啊，也需要立即给客户端返回一个ack。ack等于六二三意味着在六二三之前的所有的呃字节我都已经收到了。好，那同样的道理，服务器收到六二五之后，它也会立即确认。并且确认号也只能是六二三好，所以对于客户端进程来说，你会发现。他在短短的呃，这一段时间之内连续的收到了三个ack，并且他们的确认号都是六二三，对吧？

那此时，我们引入一种新的机制，叫做快重传机制。如果数据的发送方这边收到三个呃确认号，相同的冗余ack。那么就立即重传啊，这三个确认号对应的那个报文段，所以在收到第三个ack的这个时间点。发送方这边就立即重新传输了这个携带六二三这个字节的报文段。你看这个报文段之前是从这个时间点才发出的，对吧？粉色的这个部分意味着呃，它需要在这个时间点才会超时，但是由于引入了快重传机制。这个报文段的重传。

大大的提前了，对吧？当收到三个冗余ack的时候。客户端就迅速重传，那客户端快速重传服务器，这边尽早的收到缺失的这个报文段。然后尽早的给客户端回复这个ack。所以对于客户端来说，这个计时器还没有超时的时候，它就已经把这些报文段都顺利的送达了。中间传输失败的报文段也通过快速重传进行了处理。在整个过程当中，已经顺利送达的两个报文段并没有被重复的传输，对吧？那这一点和左边这个策略你看。

还是有很大的进步的。那这就是快速重传机制。通过冗余ack去触发。而冗余ack又需要立即确认这种机制去支持。所以但凡支持快速重传的这个TCP。它都是采用立即确认的机制，而如果不支持快速重传的TCP，它就会采用这种推迟确认的机制。那关于快速重传的这个例子，它背后的这个数据是怎么跑的？我就不画了。那值得一提的是，在我设计的这个例子中，为了方便大家理解每一个TCP报文段携带的数据都很少，对吧？

只有一个字节。你可能会觉得重复传这样的两个字节，其实代价也不是特别高，但是如果结合实际应用来看的话，通常来说。一个TCP报文段，它携带的数据量可能是1 KB这么多。甚至比1 KB更大，比如说1 MB都有可能，那如果说TCP报文段当中携带的数据很多，而这种无效的重传。又存在的话，那显然这个效率就会很低，对吧？而相比之下，

如果引入这种立即确认的策略，然后呃，再引入快重传的机制。那这种无效的重传就会变少，整个TCP的呃，这个传输效率就可以大幅度的增加好，那这就是快重传机制。但凡要支持快充船，它背后肯定需要有立即确认这种配套机制呃去配合。那立即确认是推迟确认的反面。意味着接收方每收到一个TCP报文段，就应该立即返回一个ack。即便是收到一个失序的报文段，也应该立即返回ack。那这种失序到达的报文段会导致冗余ack。

如果数据的发送方连续收到三个确认号，完全相同的冗余ack的时候，就应该立即重传和这个呃冗余ack的确认号。相对应的那个报文段。所以引入了快速重传机制，就可以让传输过程当中呃出错的这些报文尽早的重传，而不是非要等到超时的时候再重传。好，那补齐了快速重传之后呃，整个TCP的可靠传输以及流量控制就全部结束了。大家可以再结合这两页导图进行回顾，好的，那么以上就是TCP的可靠传输和流量控制相关的所有内容。
