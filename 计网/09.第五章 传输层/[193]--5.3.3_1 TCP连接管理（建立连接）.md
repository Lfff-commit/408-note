各位同学大家好，在这节课我们要探讨TCP连接管理这个考点，在大纲当中处于这个位置。那我们知道TCP它是提供啊，面向连接的服务，所以在上节课当中我们介绍过TCP协议，它会经历啊三大阶段。第一大阶段就是需要先通过三次握手来建立连接，那这三次握手分别对应三个TCP报文段。那主动发起第一次呃，握手请求的这一方，我们把它称为客户端而被动响应连接请求的这一方，我们把它称为服务器。当连接建立完毕之后，双方就可以进行双向的这个数据传输，

那如果其中一方呃已经传输完所有的数据。那么，他就可以向对方发出呃挥手。经过两次挥手，可以关闭一个单向的数据传输通道。而另一个方向的这个数据传输通道还会继续保持，直到另一个进程，它也发出挥手。那总共经过二+2次挥手之后，双方的通信就彻底结束，也就是连接也彻底的呃断开被释放。好，这是TCP协议工作的三大阶段，那另外在上节课我们也详细学习了一个TCP报文段的构成。分为TCP首部和呃携带的这个数据部分，

其中在TCP的首部啊有acksyn和fin。这样的三个标志位，我们需要特别注意，在这节课当中，这三个标志位也是我们探讨的核心。那除了这三个标志位之外。在这节课当中，我们还会频繁的涉及到序号和确认号这两个字段。所以如果不熟悉这三个标志位，以及这两个字段的同学啊，可以先暂停来看一下这页PPT。在这页PPT当中，我已经把最重要的东西都整理进来了，好，接下来我们正式进入TCP连接管理这个考点的学习。

这个考点本质上就是要考察三次握手和四次挥手，他背后的一些细节那么通常来说有这样的三种命题角度。第一个命题角度，题目很喜欢考察，在这三次握手和四次握手当中，这三个标志位和呃sequence ack sequence这两个字段。它们的值分别是多少？我们要能够根据题目给的初始条件去推算这些字段的值。好，这是最常考的一种命题角度。在大题小题当中都有可能考察好。接下来，第二个命题角度。通过接下来的学习，我们会认识到，

在三次握手和四次挥手期间啊进程的这个TCP状态是会发生转换的。每发出一个呃握手或者挥手报文段，或者每收到一个握手挥手报文段，都会影响进程的TCP状态。那么，他的状态如何？受到这些报文段的影响，这个有可能在选择题或者大题当中作为一个小问来进行考察。那第三个命题角度，题目有可能让我们去分析，在建立连接和断开连接啊，也就释放连接的那个过程当中。最短耗时等于多少？那这节课的讲解也会以这三种考法作为一个核心的主线，掌握这三种考法的同时，

你也就理解了TCP连接管理背后所做的一些事情。好，首先我们看建立连接这个阶段，要经过三次握手。我们来看这三次握手当中syn ack fin这三个标志位以及。sequence序号还有ack sequence啊，就是确认好这两个字段，它们分别是怎么变化的？我给大家设计了一个详细的例子，那由于每一页课件啊，想让大家体会到的重点不一样。所以我会在一张图的右侧这个地方去提醒大家，你在这张图当中应该重点关注的是什么东西？比如这张图体现的是三次握手，在这张图当中，

想让大家重点关注的是syn ack sequence和ack sequence这几个字段。我也会在这个图示当中用一些高亮颜色来吸引大家的这个注意力。好，再换一张图，在这张图当中，我想让大家重点体会到的是TCP的状态是如何变化的。所以我把中间的这些信息全部变成了黑白的字体。好，所以大家在听课，还有回顾课件的时候一定要注意，我这儿给大家写的这个文字，提示每一个例子，聚焦于一个具体的小问题。好，那么首先看这张图，

在这张图当中，我们要重点关注syn和ack这两个标志位。首先，通过之前的学习，我们知道ack这个标志位只有握手，一是等于零的，其他所有的TCP报文段ack都等于一。而syn这个标志位只有握手一和握手二是等于一，其他的报文段都等于零。那最后fin finish这个标志位，只有挥手一和挥手3 fin=1，其他的所有报文段fin都等于零。那我们在这个图当中也体现了这三个标志位的细节。首先syn这个标志位表示的是啊，当前的这个TCP报文段，

它是不是在请求连接？呃，或者是不是在同一请求连接？那只有握手，一是在请求连接，只有握手，二是在同一请求连接，因此只有这两个TCP段，它的sin=1。那再往后，所有的TCP段syn就都等于零。好，接下来ack这个标志位，它表示的是ack sequence，也就是确认号这个字段是否有效。

确认号这个字段是希望向对方反馈当前已接收的数据有多少，那么在刚开始建立连接的时候，由于通信双方还没有任何的这个数据传输的动作，对吧？所以握手一的这个确认号肯定是无效的。客户进程暂时还没有接收服务器进程发来的任何东西，对吧？所以他给服务器进程发的这个确认号肯定就要设置为无效。那对应的ack这个标志位就得设为零，这两个是配套的东西嘛，对吧？好，接下来握手一当中还有一个字段叫做sequence。这个字段的值是客户端进程，它自己决定的。

握手一当中的这个sequence表示的是起始序号。那我们不妨假设客户进程啊，他规定接下来自己发送的这些数据是从六六六开始编号的。好，这是握手一那接下来服务器进程，收到握手一之后就立即回复一个握手二。其中也包含了sequence和ack。我们先来看确认号这个字段。可以看到服务器给呃客户返回的这个确认号，刚好是客户发过来的这个序号。再加一。那有这样一点需要特别强调握手一和握手二这两个呃TCP报文段都是不能携带数据的。但是仍然要消耗一个序号，什么意思呢？上节课我们说一个tcp段，

由首部和数据部分组成。那握手一和握手二这两个TCP报文段，它们的数据部分都为空，也就是只有TCP首部。那正常来说，首部当中的序号这个字段，它表示的是呃，在这个TCP报文段当中的第一个字节。它的序号等于多少，对吧？那相应的数据的接收方返回的这个确认号，它反馈的是我希望接收到的下一个字节。它的序号等于多少，对不对？照理说，

握手一当中，既然没有携带任何数据，那么呃，服务器进程返回的这个ack。也应该等于六六六才对，是不是？但是TCP协议特别规定。握手一一定要消耗一个序号。所以这个地方服务器返回的确认号在握手一的这个起始序号的基础上，呃加了一。好，再来看握手二当中的序号，这个字段握手二当中的序号是服务器进程，向客户端进程说。接下来我给你发的这些数据的起始序号从50开始。

这个值是由服务器进程，它自己设定的。注意体会，别和握手一的序号搞混了，握手一这个序号是客户进程给自己。的数据确定的一个起始序号。而握手二当中的这个序号是服务器进程，给自己的数据呃确定的一个起始序号。好，那么继续往后当客户进程，收到握手二之后会立即回复一个握手三。首先观察握手三当中的这个确认号。是在握手二的序号基础上加一对吧？那刚才我们说过，握手二当中不能携带任何数据。

也就是只包含一个首部。但是握手二仍然要消耗一个序号。所以客户进程收到握手二之后，他返回的这个确认号应该是在上面这个起始序号的基础上加一。它要固定的消耗一个序号。好，接下来看握手三的序号字段。六七七就是客户端进程呃，数据部分的真正的起始序号。梳理一下客户进程，在握手仪当中选定了自己的起始序号等于六六六。虽然握手一不携带任何数据，但是它依然会消耗一个序号。那接下来客户端进程真正开始发送的这些数据，它的呃序号就是从六六七开始的。

也就是对应这个值。好，所以一定要注意这一点，握手一握手二，虽然不携带数据，但是它们仍然要消耗一个序号。好，接下来在这一页课件当中，我们会看到握手三，它可以不携带数据，也可以选择携带数据。那和握手一握手二不一样的是，如果握手三不携带数据，就不用消耗序号。来看左边这张图，

我们假设这个握手三，它也只有手部。没有携带任何数据。那么，当服务器进程收到这个TCP报文段的时候。它给客户端返回的确认号就是六六七。你看这个握手三没有消耗序号，对吧？它和前面的握手一不一样，握手一同样没有携带数据，但是返回的这个握手二当中确认号就变成了六六七。要消耗一个序号。好，再看右边这个例子，在右边这个例子当中，

我们假设握手三，它携带了数据，并且是100个字节。那你会看到此时握手三具体消耗了多少个序号呃，就跟它的这个携带的数据量是完全相关的。携带100个字节，那么它就需要消耗100个序号。我们可以结合之前这个视角来解释。在握手一这儿客户端进程规定自己的起始序号是六六六。那么，虽然握手一没有携带任何数据，但是它也需要消耗一个序号。因此，客户端进程接下来真正要发送的这些数据，应该是从六六七才开始呃编号的。

那在握手三当中携带了100个字节的数据。那当服务器进程收到这100个字节之后，它给呃客户端反馈的确认号应该表达的是什么？应该表达的是？接下来，我期待收到的那个呃数据，它的序号是从多少开始的，对吧？那显然，再往后一个字节的这个数据，它的编号就应该是从七六七开始的。这就意味着，序号在七六七之前的所有数据。已经被我服务器进程给顺利的接收了。好，

所以通过这张图左右两个例子，我们要重点强调的是握手三，它可以不携带数据，也可以携带数据。如果它不携带数据，那么它就不会消耗序号。这一点和握手一二是有区别的。好，接下来我们看一个例题，二零一一年的真题39题。说主机甲向主机乙发送了这样的一个TCP报文段，期望与主机乙建立TCP连接。问，我们如果主机已接受连接请求，那么主机已向主机甲发送的这个正确TCP段可能是什么？

好，大家先暂停做一下，十秒钟之后公布答案。三二一这个题目选择c选项。好来看一下，为什么说主机甲向主机乙发送这样的一个呃TCP报文段，那么由于syn这个标志位等于一。我们可以推断出这个TCP报文段，要么是握手一，要么是握手二，对不对？在TCP协议的全过程当中，只有这两个啊，报文段的syn是等于一的。好，

再往后看，说主机甲期望与主机乙建立TCP连接，这句话的背后隐含的信息是什么？意思就是说主机甲是主动发起连接请求的那一方，对不对？也就意味着主机甲肯定是客户进程嘛。那既然他是客户进程，他给主机已发送的，肯定就只会是握手一对不对？那么，服务器也就主机乙这边他收到呃，握手一之后，如果他接受该连接请求的话。它应该给主机甲返回的TCP段，就肯定是握手二，

那既然是握手2 syn，就肯定等于一了。所以a第两个选项首先排除它们的xyn都等于零。好，接下来比较有迷惑性的是b和c两个选项，我们说握手，二是服务器给客户主机呃返回的TCP报文段。在握手二当中。sequence序号这个字段表示的是服务器进程的起始序号，对吧？它表达的是这样的一个含义，那服务器进程的起始序号到底等于多少？这个是由服务器进程去决定的，对不对？所以这个值到底等于多少，

其实无所谓，具体就看这个服务器进程，它如何去设置自己的起始序号。那有所谓的是什么呢？就是这个确认号。在握手二当中，服务器给客户端返回的这个确认号应该是等于握手一的，这个起始序号。再加一，因为之前我们强调过，虽然握手一它不携带数据，但是它固定的会消耗一个序号。所以握手二返回的这个确认号就应该是一一二二零，再加一对不对？要固定的消耗一个序号好，

那这样的话b选项是不是就排除了，剩下的就是c选项？这个确认号的值是正确的。好，那么这是一一年的39题。这个题目当中，服务器给客户端返回的这个序号字段，它其实是一个干扰信息。如果你没有理解序号，确认号背后的啊，那些逻辑的话，那么这种干扰选项很有可能就把你带到坑里去。好，所以这个部分考的还是很细的，一定要能够推算出握手一二三当中的啊，

这几个字段的信息分别等于多少？好，那现在我们已经搞定了，第一种考法，接下来我们来关注第二种考法，有可能会考察呃进程的TCP状态转换。直接给个图，在建立连接的过程当中，客户进程刚开始是连接关闭的状态。在真题当中，通常用英文表示就是close状态，当客户进程发出握手一之后。它的状态就从close变成了psych ronizecent，就是同步已发送。而当客户进程收到握手二之后，

它就会进入established已建立连接这样的一个状态。好，这是客户进程，在建立连接的阶段，会经历的三种状态。再来看服务器进程。刚开始，服务器进程也是close这个状态。接下来，如果服务器进程允许其他的客户进程啊，建立连接的话。那么，它就会监听自己的端口，于是服务器进程进入了listen监听的状态。好，

那么当服务器进程收到握手一之后，它又会从监听状态转换到同步已接收的状态。sys对应的是synchronization啊，同步，然后rcvd对应的是received。好，这是同步已接收的状态。那么，再往后当服务器进程，收到握手三的时候，它就进入了established已建立连接的状态。好状态转换的这种题目，一般来说会怎么考呢啊？来看二一年的38题，这个题目它考的是释放连接阶段的状态转换。

但是从这个题目当中可以看到，建立连接的这个过程也有可能出现类似的考题，对吧？你看这个题目探讨的就是呃，在四次挥手的那个阶段，如果客户端服务器巴拉巴拉巴拉发了一堆这个挥手信息之后。客户的这个TCP状态会转换成呃abcd会转换成哪种状态？它的这些选项就是用英文来表示的。那类似的考法也很有可能在建立连接这个阶段啊，来进行考察，比如说我可以这么考，你当服务器进程收到握手一之后。它会转换，为什么状态？那答案就是进入同步已接收状态。

好，那么这种考点它就属于记忆型的考点，建议大家考前几天回顾一下，利用短时记忆去做题就可以了。说实话，这个状态转换的过程，如果你下周问我，我很有可能也忘了。所以这种考点，它本身逻辑不是特别复杂啊，关键在于你记不记得住这种东西，给自己写一个备忘录，考前几天回顾一下就可以了。好，那么这是建立连接阶段，

这个呃TCP状态的转换。接下来看，建立连接这个阶段的最短耗时分析这种考法。可能会这么考察，比如说题目可能会问你从客户端发出握手，一到客户端进程可以开始发送数据。至少需要多久？那答案就是一倍的rttrtt，是往返时延，对吧？对于客户端进程来说，当它收到握手二的那一刻开始，它就可以发送数据了。如果用进程的TCP状态去解释的话，就是呃，

一旦这个进程，它的TCP连接进入到了established这个状态。那么，它就可以开始给对方发送数据了，所以从发出握手一一直到它可以正式的发送数据。整个过程最短耗时就是一个itt，对吧？ratt对应的就是这个TCP报文段的往返时延嘛。好，我们换一个进程，对于服务器进程来说，从客户端发出握手，一开始一直到服务器进程，可以开始发送数据，至少需要过多久。

那显然就是一点五倍的rtt。从发出握手一开始。握手一到达服务器。握手二到达客户端。这儿总共花费了一倍的rtt，对吧？接下来握手三还需要经过单向的传播时延，到达服务器b。所以握手三的传输过程又需要消耗零点五倍的rtt，那加起来不就是一点五倍的rtt吗？那如果考这种考题的话，题目肯定会给你rtt的值等于多少？把它代进去算就可以了。那我是不是还可以这么问，从服务器进程发出握手二开始，

直到服务器进程可以开始发送数据？整个过程耗时是多少？那么显然就是一倍的rtt对吧好？所以其实这个耗时分析不要去记忆，关键是理解它背后的逻辑。最核心的点就是客户端进程是从收到握手二才可以开始发送数据的。而服务器进程是从收到握手三这个时间点才可以开始发送数据的。只要理解了这点，那么管它题目怎么去变，你肯定都能自己分析。好，所以不要记忆，要理解。好的，那么这节课当中，

我们主要探讨的是在连接建立的阶段，也就是三次握手的阶段啊，这三类题型我们需要注意什么？由于内容比较多啊，释放连接也就四次，挥手的阶段，我们留在下个视频再来探讨好的，那以上就是这视频的全部内容。
