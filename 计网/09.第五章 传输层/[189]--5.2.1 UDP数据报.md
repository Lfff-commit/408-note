好的，接下来看udp数据报这个考点在大纲当中，位于这个位置。udp的考题一般来说难度都不高啊，但是udp经常和TCP协议进行对比，所以接下来我们先啊总结一下udp和TCP的一些显著的区别。好，先来看udp协议，在下面这张图当中，我们要弄清楚udp协议udp数据报，还有udp首部这几个概念之间的关系。首先，我们知道，在每一台主机的应用层都会运行着啊，很多个进程，

那么假设主机a的进程一。和主机b的进程二需要传送呃一块数据，那么进程和进程之间要传送的一个数据单元就叫做报文。那发送方也就是进程一在准备好报文数据之后，会把这个报文数据交给呃传输层的udp协议进行处理。udp协议会在这个报文的前面添加一个udp首部总计八个字节。那这样就得到了一个完整的udp数据报。紧接着udp协议，再把udp数据报呃交给网络层进行处理，那网络层的IP协议。会在udp数据报的前面再添加一个呃IPV 4首部，这样的话就得到了一个IP数据报。那接下来IP数据报就可以经过路由器的转发啊，最终到达目的主机。那目的主机的网络层在收到IP数据报之后检查，

如果这个IP数据报当中携带的是udp的这个信息。那么，主机b的网络层把IP首部呃拆掉之后，就会把呃剩下的那些信息。交给传输层的udp协议进行处理，而udp协议又会根据这个udp首部当中记录的源端口号，目的端口号啊这些信息。去决定到底要把呃udp数据报当中携带的这个报文信息交给应用层的哪个进程。好，那么有这样的一些要点啊，值得强调。首先从这个地方可以看到udp的首部是很小的，它只有八个字节。那相比之下，假如采用的是TCP协议的话，

TCP协议的首部啊，最小都要占20个字节，所以TCP的首部。带来的额外开销就会比udp更大。好，那这是第一点。值得强调的好，第二点我们要知道啊。udp协议每一次传输的就是一个完整的报文。并且udp协议并不支持报文的拆分和重装，那这就意味着如果应用层的进程。他的这个报文太大的话，那么当他把一个超大的报文交给udp协议进行传输的时候，udp协议会直接拒绝这种啊请求。因为udp协议的运输能力是有限的，

如果应用层的报文太大，那么就直接拒绝应用层的这个请求。所以当我们在写这种网络程序的时候啊，如果这个数据的传输采用的是udp协议，那么假设我们要传输的数据太大。那么我们得自己在应用层这个部分，也就是在自己的程序当中，把这个报文先拆好，拆好之后再交给udp来帮我传输。它并不支持啊，自动的拆分和重装的功能。好，那么相比之下，如果两个进程，它们之间的通信在传输层采用的是TCP协议的话。

那么呃TCP它是支持这个长报文的自动拆分和自动重装的。所以即便进程一它的这个报文很大，并且这个很大的报文已经超出了TCP协议呃一次运输的能力。那么，在这种情况下TCP协议会自动的把这个报文拆分成若干个报文段。然后给每一个报文段添加TCP的首部。当这些报文段到达接收方的传输层的时候，那接收方的TCP协议又会把这些报文段进行呃重装。排序最终就可以给目的进程交付一个完整的长报文好，所以TCP是支持长报文的拆分和重装的。这点和udp协议有所区别。好，接下来第三个区别udp协议是无连接的，不可靠的，而TCP协议是有连接的可靠的。

那在之前的学习中，我们简要的介绍过所谓有连接，指的就是当通信双方正式开始传输数据之前。双方会先呃彼此打好招呼，让对方准备好接收数据。只有确认对方准备好了之后。发送方才会把呃真正要传的数据传输给对方。另一方面，在数据传输完成之后，通信双方又会去释放链接，也就是要明确的通知对方，我的传输已经传完了。接下来这个链接可以断掉了。好，所以TCP是有连接的，

那相比之下udp就是无连接的，如果进程一要通过udp协议给进程二发送一个报文。那么，在发送这个数据之前，这两个进程之间并不会啊，先打招呼，不管对方有没有准备好接收这个数据？数据的发送方都会直接把报文啊封装到udp数据报里，然后直接丢给呃数据的接收方。所以我们说udp是无连接的好，接下来TCP是可靠的，而udp是不可靠的。在TCP协议当中，会采用确认机制来确保这个传输的可靠性。那相比之下udp协议就没有采用确认机制。

所以传输是不可靠的，那如果两个进程之间采用udp协议去呃进行数据的传输。但是二者的通信又希望保证可靠，那怎么办呢？事实上，我们可以让应用层去呃，保证这个传输的可靠性。传输层的udp协议啊，只保证尽最大努力去传输这些数据。但是这些数据有没有残缺？有没有错误？等等啊，这些方面的检查啊，需要由这个进程自己去实现。好，

接下来TCP协议，它可以支持拥塞控制，而udp协议不支持拥塞控制，具体来说就是。当两个进程呃相互之间使用这个TCP协议去进行数据传输的时候。如果数据的接收方检测到了网络拥塞，那么它会用一种机制去通知数据的这个呃发送方，让它发的慢一些。从而就可以减少网络当中的数据量，最终就可以缓解网络拥塞，那具体怎么实现拥塞控制？这个我们会在TCP协议当中有进一步的探讨。现在我们只需要知道TCP，它是支持用塞控制的，而相比之下udp，

它并不支持用塞控制，知道这点区别就可以了。好最后TCP协议，它仅支持一对一的数据传输，而udp协议不仅支持一对一的传输，还支持一对多的传输。好来解释一下什么意思？假设在同一个网络当中有AB。CD这样的四台主机。并且这四台主机上都分别运行着一个进程好，现在假设a主机上面的进程和d主机上面的进程。想要使用TCP协议来进行数据传输。那么，由于TCP协议啊，它只支持有连接的这种传输方式，

所以这就意味着在两边的进程正式开始通信之前，需要先完成建立连接的动作。只有连接建立完成之后a主机才可以给d主机啊发送数据，那当然连接建立完成之后d主机也可以给a主机发送数据。那这个数据发送的过程只局限于呃a主机和d主机上面的两个进程。a和d建立的这个TCP连接，只能满足二者之间的通信，而不能让a和b或者a和其他的节点进行通信。如果a要和其他的节点进行TCP传输的话，那么还需要再建立另一条TCP连接。好，所以这是TCP，只支持一对一传输的原因。好，接下来再回来看udp协议，

为什么说udp既支持一对一，也支持这个一对多呢？首先，一对一很好理解。假如a和d之间要通过udp来传输数据，那么a在进行这个udp数据报的呃封装的时候。只需要把udp数据报封装成一个单播IP数据报就可以了，也就是说在这个IP数据报当中指明啊，数据的接收方，它的IP地址等于多少？以及数据的接收方，它的进程对应的端口号等于多少，指明这些信息就可以实现udp的一对一传输。好，所以一对一传输不难理解，

那来看一下udp如何实现一对多的传输。其实也很简单，只需要把udp数据报封装到一个广播IP数据报当中，或者一个多播IP数据报当中就可以了。那还记不记得？如果我们在IPV 4首部这个地方，把目的IP地址写为32比特权一。那么，这样的目的IP地址就表示，这个IP数据报需要广播给同一网络内的所有节点，对吧？而这样的一个广播IP数据报当中，封装的是一个udp数据报，所以就意味着所有的节点。都会收到同一个udp数据包。

那这样的话，岂不就完成了一对多的数据传输，好说的具体一点，假设abcd是网络中四台主机上的啊，四个不同的进程。我们不妨假设a进程的端口号是九九六，而bcd进程它们的端口号都是七幺幺。那么，对于a进程来说，如果它想要用udp协议实现一对多的通信，它可以怎么做？它是不是可以在呃自己的这udp数据报当中？指明目的端口号是七幺幺，这个端口紧接着在网络层这个地方，再把目的IP地址写成一个广播地址。

那这样的话bcd的网络层都能够接收到这个呃IP数据报，与此同时，由于udp首部当中的呃目的端口号。都是七幺幺，所以bcd它们的传输层也都会接收这个udp数据报。那这样的话就实现了udp的一对多传输。那这其实也是呃udp在传输之前不需要先建立连接所带来的好处。好，那么这是udp和TCP的区别，udp不仅支持一对一的传输，也可以支持一对多的传输，只需要把udp数据报封装到一个广播IP数据报，或者一个多播IP数据报当中。当中就可以了。其实刚才举的这个例子就可以用于实现直播软件，

假如a对应的是直播的服务器。而bcd对应的是呃直播的客户端。那么，当服务器要给所有的这些客户端啊，都推送这个直播的画面啊，声音这些信息的时候。这个直播服务器就可以把画面，声音这些数据封装到一个udp数据报里边。然后通过这种广播或者多播IP数据报的方式，实现一对多的传输，也就实现了一台服务器给多台客户端。同时，推送数据的这样的一个呃功能。好，所以udp协议的这种功能还是很实用的。

好，那么接下来我们详细看一下udp数据报的格式。首先，一个udp数据报由udp的首部八个字节以及数据部分由这样的两部分来组成。那在这个图当中，八个字节的首部对应的就是前面的这些东西，而数据部分对应的就是后面这个东西。那可以看到首部的长度是固定的啊，占八个字节。其中前两个字节表示的是源端口号，也就是发送方那个进程所对应的端口号。那假如发送方发送完呃udp数据报之后，不需要对方回复的话，那么可以把源端口号设为全零。这个可以理解，

对吧？如果a给b发送一个udp数据报。他希望b在接收udp数据报之后，也给自己回复一个udp数据报，那么a给b发送的这个udp数据报当中。肯定得告诉b，如果你给我回信，那么你应该回到我的哪个端口对吧？就得携带这个信息，但是如果a呃。不需要b给自己回信，那么这个源端口号设为全零就是OK的。好，接下来后面的两个字节呃，表示的是目的端口号，

这个信息是必填的指向了呃，接收数据的那个进程，它的端口等于多少？好再往后两个字节表示的是udp的总长度，也就是八字节的首部，加上数据部分。总共占多少个字节？那由于长度这个字段总共是两个字节，16个比特，那我们知道16比特可以表示的最大的一个呃数就是六五五三五。因此，这就限制了udp数据报的理论，最大长度也就只有六五五三五这么多。好，为什么要加理论最大长度这个定语呢？

好，是这样的原因，我们知道udp的这一整块数据。最终是要封装到IP数据报里面的，对吧？然而，一个IP数据报可以运输的最大数据量其实也有一个限制，对不对？在一个IP数据报中，携带的数据部分不能超过六五五幺五字节这么多。好，所以虽然这个字段它可以表示的udp的最大长度是六五五三五，但是受限于IP协议的呃运输能力。事实上udp数据报的呃实际的最大长度不能超过六五五幺五字节这么多。好，

所以这个地方我们加了理论，这个前缀好，接下来首部的最后两个字节表示的是udp校验和。这16个比特是由发送方的传输层在发送udp数据报之前计算得到的。通过某一种算法计算得到校验和，然后把这个校验和啊写到udp的首部里边。那这样的话，当接收方收到一整个udp数据报的时候，他就可以基于这16比特的校验和去检查。整个IP数据报是否有呃比特错误？那具体是怎么生成校验和的？怎么检查有没有比特错误？背后的原理我们会在下节课进行补充。那假如数据的发送方啊认为这个udp数据报它错没错，其实都无所谓，

那在这种情况下。可以给呃校验和这几位填入呃16比特的全零表示无需校验。好，那这就是udp数据报的首部。它的构造还是比较简单的，那在首部的后面啊，紧接着就是数据部分了。那通过之前学习，我们知道udp的数据部分，它包含的就是一个完整的应用层的报文。好，接下来用这张图来表示udp数据报传输的一个呃，具体的细节首先假设进程一要给进程二。发送一个报文，这个报文的总长度是96字节。

并且进程一绑定了三六六六这个端口，进程二绑定了九八八八这个端口。好，那么发送方进程，一会把报文交给这个呃传输层。并且进程一需要指明这个数据的接收方，它的IP地址等于多少端口号等于多少？好，那接下来传输层的udp协议就会把整个报文封装成一个udp数据报。那具体来说就是要添加八个字节的udp首部，其中前两个字节表示的是源端口号呃，也就是进程一的端口三六六六。接下来的两个字节指向目的端口号，九八八八。也就是接收进程的这个端口号好，

那再往后两个字节表示的是udp数据报的总长度，以字节为单位的总长度。那由于报文这个部分啊占96字节，而首部这个部分呃总共又是八个字节，所以整个udp数据报它的总长度就是104个字节。所以udp长度这个字段应该填104，那最后还有udp校验核的信息，现在我们还没有讲校验核的具体算法，所以先暂时不做探究。反正udp校验和也占16个比特，它的存在是为了检测udp数据报当中的比特错误。好，接下来udp数据报交给IP协议封装成IP数据报，那其中最重要的两个东西就是要注明原IP地址和目的IP地址。原IP地址对应的是数据的发送方，

目的IP地址对应的是数据的接收方。好，那这样IP数据报就可以经过路由器的层层转发，最终到达目的主机的这个网络层。那再往后就是逐层的往上递交，那后面的过程就不再赘述。通过这个例子，主要是想让大家看到这个udp数据报首部应该怎么填，那么结合这个图示，相信不难理解。好的，那么在这个视频中，我们探讨了udp协议，还有udp数据报相关的一些东西，首先我们用udp协议和TCP协议进行了一些对比。

分别从首部的大小以及是否支持长报文的拆分和重装。还有就是两种协议，它们是否有连接，是否可靠，以及是否支持拥塞控制，从这几个方面进行了对比。那另外，我们也强调了udp协议，它不仅支持一对一传输，也支持一对多的传输，那相比之下TCP协议由于啊，在正式开始传输之前，通信双方都需要先建立连接。所以每一个TCP连接都只支持一对一的传输。那么udp和TCP在这些方面的区别是啊，

很喜欢在选择题当中进行考察的。好，那除了与TCP协议的对比之外，我们也详细介绍了udp数据报的格式。具体的大家可以在用这张图进行回顾。好的，那么以上就是这个视频的全部内容。
