好，继续探讨TCP报文段的格式，这是TCP报文段首部的样子，可以看到前面这有20个字节是固定的。而后面这个部分是可变的。这一点是不是和IP数据报的首部很像啊？IP数据报的首部固定部分占20个字节。而可变部分最多不能超过40字节，所以IP数据报的首部啊，它的大小可能是20到60字节。那对于TCP数据报来说也是一样的，它的首部大小也是最小不低于20字节，最大不超过60字节。大家可以把它们俩呃结合起来，记忆。

好，另外在这个图当中啊，这样的一整行对应的就是32位，也就是四个字节。上面这些小刻度表示的就是比特。接下来，我们会从上往下一行一行的去探讨这些字段的作用。首先看第一行的两个字段，分别叫源端口和目的端口，这两个字段我们很熟悉了，对吧？源指的就是发送方进程，它对应的端口号。而目的指的就是接收方进程，它对应的端口号。

像之前这个例子，进程一要给进程二啊，发送四个这个TCP报文段，那么每一个TCP报文段。的首部这个位置都需要记录源端口号，应该是进程一的端口三六六六，而目的端口号应该是进程二的端口。也就是九八八八。源对应的是发送方目的，对应的是接收方。那如果反过来是进程二给进程一发送的呃TCP数据报的话，源端口和目的端口就应该倒过来，对吧？因为此时进程二变成了发送方进程，一是接收方。

好，这是前两个字段的含义。从这个图当中可以看出来，每个字段是占16个比特，因此源端口号和目的端口号的取值范围显然就应该是零到六五五三五这个范围。好，接下来看第二行这个字段叫做序号。这个字段用于标记当前的这个TCP报文段。它的数据部分的第一个字节在原始字节流当中的位置。这个字段的存在是为了把原始的字节流拆分成若干个分段，然后在接收方那边又可以根据序号的信息把这些分段又重组成原始的字节流。那值得注意的是，起始序号是发送方自己设置的，不一定从零开始。好，

接下来用一个例子来感受序号这个字段的作用。进程一要给进程二发送两个报文。那么，在传输层的TCP协议看来，它并不关心啊，这些数据到底是由几个报文组成的？在TCP协议看来，这些数据就是一连串的呃字节流。1600+1800呃，总计就是3400个字节，对吧？由于mss的限制，这3400字节的数据至少需要拆分成四个TCP段来运输。好，那么数据的发送方，

也就是进程一。它可以设置一个起始序号。比如说它规定。这些字节流的第一个字节，它的起始序号等于500。那这样的话，第一个TCP段当中携带的这些数据啊，它的编号就应该是。500一直到一四九九对吧？好，那么类似的道理，第二个呃TCP段当中，它所携带的这些字节编号就应该是一五零零。一直到二四九九，这个范围对吧？

好，那么回到刚才我们介绍的序号这个字段。我们说TCP首部当中的序号字段，它标记的是这个TCP段当中。它携带的第一个字节的序号等于多少？所以第二个TCP段，它的序号字段就应该是一五零零。那同样的道理，第三个TCP段，它的序号字段。就应该是二五零零，对吧？而最后一个TCP段，它的首部啊序号就应该等于三五零零。好，

回过来再补充一下第一个TCP段它的序号。它的序号就是五零零。好，现在暂停来体会这两句话，相信你已经能够理解了。在考研真题当中，序号这个字段经常被简写为小写的seq。那可以看到序号这个字段总共是32比特。好，接下来这一行叫做确认号，在考题当中确认号这个字段通常会简记为小写的ack。或者ack sequence。这两种记法都可能出现。与确认号这个字段关联比较大的，还有一个呃标志位。

这个标志位也叫ack，只不过在真题当中，这个标志位通常会记为大写的ack。好，所以审题的时候要注意大写的ack对应的是这一个比特，而小写的ack或者ack sequence这种记法表示的是32比特的确认号。好，接下来解释这个大写ack和小写ack的作用。首先看大写的ack。这个比特用于标记当前的这个TCP报文段当中确认号这个字段是否有效？如果大写的ack=0的时候表示确认号这个字段是无效的。只有ack=1的时候表示确认号这个字段是有效的。那么，在整个TCP协议通信的过程当中。只有握手一对应的这个TCP报文段，

它的ack=0。除了握手一之外，其他的所有的TCP报文段包括握手二，握手三，它们的ack。都等于一。也就意味着往后所有的TCP报文段当中。确认号这个字段都是有效的。好，接下来探讨确认号的作用。那么，确认号这个字段的作用是数据的接收方可以通过确认号这个字段。给数据的发送方反馈。在这个确认号之前的所有序号的字节都已经被我正确的接收。好，

我们还是结合前面这个例子来理解。假设进程一给进程二发送一二三四这样的四个TCP报文段。那如果说主机b已经顺利的接收一二两个报文段。那么，接下来右边这个TCP协议。给左边这个TCP协议。返回的TCP报文段当中，可以携带这样的信息。首先，大写的ack=1。然后小写的ack sequence。也就是确认号可以记为二五零零。二五零零意味着在序号为2500的这个字节，前面的所有的字节。都已经被我顺利的接收。

那进程一的传输层在收到这样的呃报文段之后，它就能够确定啊。第一个TCP报文段，第二个TCP报文段已经顺利的被接收。至于第三个，第四个有没有被顺利的接收，现在还不得而知，对吧？好，那这是确认号的作用，表示在确认号之前的所有的字节都已经正确的收到。事实上，这个确认号它具有累计确认的这种特性，对吧？那这意味着什么呢？

假设进程二这边它收到了一二四这几个TCP报文段。那么，此时它给这个进程一回复的呃TCP报文段当中。这个确认号应该填多少？是不是还是得填2500，对不对？虽然我已经提前收到了第四个TCP报文段。但是序号为2500到3499的这些字节，我暂时还没有收到，对吧？所以进程二给进程一返回的这个确认号应该填2500，表示序号小于2500的所有字节我都已经顺利的收到了。好，所以这是确认号这个字段需要注意的地方，它具有累计确认的特性。

那序号确认号，还有ack位这三个东西是非常常考的很重要。不管大题还是小题都会用到。好，接下来看下一个字段，下面这个字段叫做数据偏移。英文的原文叫data offset，所以翻译成数据偏移，不过这个翻译我觉得是比较有迷惑性的。它可能会让你误解为哎，它指的是不是呃TCP报文段当中这个数据部分在原始数据当中的偏移量对吧？很容易产生这样的误解，但事实上，数据偏移这个字段，它表示的是TCP首部的长度。

并且是以乘四字节为单位的。那结合这个图可以看到数据偏移这个字段，它的长度是四个比特，那么四个比特可以取值的范围就是零到15，对吧？所以TCP首部的长度最长不超过60字节。并且TCP首部的长度一定都是四字节的整数倍。那也正是因为这样的限制，所以最后这一行我们看到可变部分的末尾处有一个东西叫做填充。其实就是为了把TCP首部凑足四字节的整数倍。好，所以数据偏移和填充这两个字段要联合起来看，它们俩是有关联的。对了，讲到这儿，

不妨回顾一下在IP首部这个部分，我们编过这样的一个口诀，叫做四一八首总篇，对吧？意思是在IP首部当中有一个字段表示的是IP首部的长度。它是以四字节为单位的。那和TCP的数据偏移这个字段是不是如出一辙？并且由于IP首部是以四字节为单位，所以在IP首部的可变部分，我们也会有类似于填充这样的字段存在。也就是必须把IP首部凑足四字节的整数倍，所以在IP协议当中，首部长度这个字段和TCP协议的数据偏移这个字段其实是一模一样的，可以把二者联合起来记忆。好，

另外在IP首部当中，首总篇的总指的是在其中有一个字段表示的是IP数据报的总长度，对吧？而总长度这个字段，它是以一字节为单位，总长度包含了首部和IP的数据部分。那现在回头观察TCP段的首部，你会发现在整个首部当中没有任何一个字段。去专门的指明这个TCP报文段的总长度等于多少，也没有任何字段去指明它的数据部分的长度等于多少。那这个问题是怎么解决的呢？是这样的。我们可以知道IP数据报的首部占多少个字节，以及IP数据报的总长度是多少个字节。那么，

二者一相减，是不是就可以得到IP的数据部分占多少个字节？那这样也就计算出了呃TCP报文段总共占多少个字节？好，现在对于TCP报文段当中，如果我们找到数据偏移这个字段，是不是又能够知道它的首部占多少个字节？那最终我们要确认TCP报文段的数据部分是多少个字节，那还不简单吗？好，所以在TCP报文段当中，只有这样的一个字段，用于表示首部的长度，但是没有任何字段去表示数据部分的长度或者它的总长度。这些信息是结合IP数据报的，

这个结构啊，进行推算的。好，那么这是数据偏移这个字段，它和IP首部当中的首部长度，总长度这两个字段是有联系的。需要把二者结合起来理解。好，接下来看后面的这个保留字段六个，比特这字段是最没有排面的啊，没有任何作用。通常来说，会把它全部置为零。好，继续往后。

接下来我们会接触到几个标志位。每一个标志位都是一个比特，首先urg，这个标志位。这个缩写来自于英文单词urgent。表示紧急的，急迫的好，所以这个标志位在中文教材当中呃，通常把它翻译为紧急位。不过，在真题当中，如果出现这个标志位，那它大概率是用缩写urg，而不会用紧急位去表示。那么，

当这个比特等于一的时候，表示当前这个TCP报文段当中携带的是紧急数据。应该尽快插队发送。那与此同时，当urg=1的时候，表示紧急指针这个字段是有效的。好，我们先来解释什么叫紧急数据。假设进程一和进程二是我们开发的一个软件，叫做微信。这个软件可以支持文件的传输。以及聊天文字的传输。假设现在进程一正在给进程二传输两个文件，这两个文件对应的就是报文外和报文z。那在这两个文件传输的过程当中，

假如呃，左边这个微信进程想要给右边这个微信进程。发送一些即时聊天的文字，那这些文字对应的也是一个报文，对吧？那么，对于TCP协议来说，它之前收到的呃应用层传过来的这个原始字节流是不是总共有3400个字节需要按顺序把它传过去，对吧？但是现在显然文字聊天的这个数据应该被优先的传递给进程二，对不对？这个不难理解吧，你想一下，如果你和一个人呃，正在用微信传那个文件。

与此同时，你还想给他发一段文字信息，那如果你的文件还没有传完，你的文字就发不出去，这样是不是很难受啊？好，所以在两个进程的通信当中，有些数据可能没那么紧急，这些数据我就按照先来后到的顺序，依次把它发出去。但是如果有一些紧急数据到达，那么这些紧急数据把它封装成TCP报文段之后。就应该让他尽快的。从发送方发出去。好，

那现在再回来感受这句话，当urg这个标志位等于一的时候，表示这个TCP报文段携带的是紧急数据。应该尽快插队发送。那另一方面，当urg标志位等于一的时候，表示紧急指针有效。紧急指针这个字段，它指的就是紧急数据的序号。我们之前也学习了序号这个字段的含义，对吧？上面这个序号标记的是非紧急数据。而紧急指针这个字段，它标记的是紧急数据的序号。回到刚才这个例子。

对于一二三四这四个非紧急数据来说，我们是用sequence序号那个字段去给它们啊，进行编号的。那像文字这一类紧急数据，我们会另外编一组序号。比如说这段紧急数据，它的紧急数据编号啊，我们可以让它从100开始。假设这些数据总共100字节，那么它的编号就应该是100到一九九。相应的，携带这些数据的TCP报文段，它的紧急指针就应该设为100。假设还有后一段文字，也就是后一个紧急数据呃，

需要发送，那么后一段紧急数据的这个紧急指针就应该是从二零零开始的。好，所以紧急指针这个字段本质上体现的也是序号的概念。只不过所有的紧急数据啊，会有独立的一套序号，而普通的非紧急数据是另外一套序号。好，那这是urg标志位以及紧急指针。二者是有关联的，要结合起来理解。再往后psh这个标志位通常翻译为推送位。它来自英文单词push。当这个标志位等于一的时候，表示希望接收方尽快回复。

一般来说，这个标志位会用于交互式的通信。比如你在打游戏的时候，当你按下某一个按键的时候，你按下按键的这个操作最终会被封装到呃TCP报文段里边。这个TCP报文段会被发送给游戏的服务器。显然，你按下按键之后，肯定希望服务器尽快给你返回，结果对吧？比如说你释放了一个技能，放了一个大招。那么，服务器得尽快的通知你，你的这个大招到底有没有命中给敌方带来了多少伤害？

对不对？好，所以像这种通信就是交互式的通信一个进程，希望另一个进程尽快的回复我发出去的这个TCP报文段。那此时就可以把这个TCP报文段的PS h push这个位设为一。好，继续往后下面这个标志为叫rst，中文通常翻译为复位位。当这个比特等于一的时候，表示啊，发送方出现了呃，严重的差错，比如说发送方的主机呃，崩溃了。或者发送方进程被强制杀死了之类的。

那么，此时就必须释放TCP连接。好，另一方面rst=1这种TCP段也可以用于拒绝一个非法报文段。比如说有一个恶心的黑客，他故意的给你的电脑发一个非法的TCP报文段，他的格式是乱七八糟的。那么，你的电脑传输层检测出这种非法报文之后，就会给对方呃发送一个rst=1。这样的报文段。这样的话就可以让双方尽快的释放TCP连接。好，那么在这一页当中介绍的这几个标志位啊，包括紧急指针，

这些东西是不常考的，所以优先级没有特别高。好，继续往后后面这两个标志位就很重要，很常考了。首先syn这个标志位。它来自于英文的synchronize同步这个词。通常翻译为同步位，当sin=1的时候，表示这是一个连接请求或连接接受报文。直接把最重要的结论给大家。只有握手一和握手二这两个报文的syn标志等于一，其他所有报文的syn都等于零。回到这个图记录一下，只有握手一握手二这两个呃报文段，

它的syn=1。其他的所有报文段syn都等于零。记住这个结论，你就能做题。那握手一是客户端发起连接请求的呃，一个报文段对吧？而握手二是服务器同业连接请求的报文段。好，所以再回来体会这句话呃，就能够理解了，接下来看fin这个标志位，这个标志位来自于英文单词finish。表示终止结束。当fin这个标志位等于一的时候，表明此报文段的发送方的数据已经发送完毕啊，

并且要求释放传输连接。好，这个记不住也没关系，我们记住最重要的这个结论，记住结论你就能做题。只有挥手一和挥手三的fin=1，其他所有TCP报文的fin都等于零。好，我们在这标注一下，只有挥手一以及挥手三。只有这两个报文段fin=1。其他的报文段包括挥手二和挥手四呃，它们的fin都等于零。我们来体会一下挥手一和挥手三的作用。挥手一意味着进程a已经没有更多的数据想要发给进程b了，

对吧？全都传输完了。所以进程a会给进程b发送挥手一报。文段告诉他，我给你传送数据，这件事情已经finish了。已经结束了。所以这个报文段的finish标志位就应该设为一。类似的道理进程b的数据都发完了之后，它要给进程a发送挥手三这个报文段。告诉进程a我给你的数据已经发完了，已经finish了，所以挥手三的finish这个标志位应该设为一。好，这两个标志位的文字描述啊，

看一看知道个大概就行了，对于考试来说，重要的是记住这两条结论。这两个结论很常考。好，另外还需要给大家补充呃，在考题当中常见的术语。如果一个TCP报文段，它的syn这个标志位等于一的话，那么我们可以把这样的TCP报文段称为syn段。所以当你在考题当中看到syn段的时候，你就能知道它肯定是要么是握手一，要么是握手二，对吧？那类似的道理，

如果fin标志位等于一，那可以把它称为fin段，那fin段对应的肯定就是要么挥手一，要么挥手三。那类似的，如果ack这个标志位等于一的话，我们可以把这种TCP段称为ack段。一个TCP报文段，它有可能既是ack段，又是syn段。比如说握手二这个报文段，它的ack=1，它的sin也等于一。所以对于握手二这个报文段来说，我们既可以称它为syn段，

也可以称它为ack段，反正你要知道，当题目告诉你某一个TCP段，它是syn段的时候，它其实是想告诉你syn标志位等于一。当它说这是一个ack段的时候，它是想告诉你它的ack标志位等于一。这些标志位之间并不互斥，有可能同时都是一。好，这是真题当中很常见的一种表达方式，大家需要注意。好，那么所有的标志位我们都已经过完了，接下来看窗口这个字段。

这个字段也很重要，也很常考。在真题当中，窗口字段经常被呃简写为rw nd或者rcvwnd。这两种缩写来自于receive和window这两个词。rcvwn d.receive window意味着这个字段，它表示的是接收窗口的大小。换句话说，就是接收方在给数据的发送方反馈，我还可以再接收多少个字节的数据？就是这个意思。我们结合前面这个例子来解释。现在进程二正在接收进程一发过来的这些数据。假设传输层的TCP协议给进程二分配的接收缓冲区。

大小是2500字节。好，那么如果现在一二两个报文段已经到达呃，接收方的这个接收缓冲区。那么显然，如果这两个报文段还没有给上层交付的话，那么这个接收缓冲区就只剩下500字节的呃空间对吧？第一个报文段，第二个报文段已经占去了呃2000字节的这个空间。那此时接收方给发送方返回的呃，下一个TCP报文段当中。它就会填入这样的一些信息，首先确认号应该填2500对吧？表示在2500之前的所有数据，我现在都已经收到了。

与此同时，还需要告诉这个发送方，我的接收窗口只剩下500字节的空间。那这样的话，下一个要给他发送的这个TCP报文段，我是不是就不能携带1000字节的数据？我只能携带500字集的数据，对吧？我给他发1000字节，他收不下这么多。好，那么这是窗口字段的一个作用。这个字段是实现流量控制的关键。流量控制就是说数据的接收方要用某一种机制去限制数据的发送方。发送这个数据的速度。

那么，关于流量控制的实现细节，我们还会用之后的小节进行更进一步的探讨。现在先建立起一个框架，我们要知道窗口这个字段就是实现流量控制的关键。好，那么继续往后，接下来到了校验和这个字段，可以看到这个字段占16个比特。那么TCP的差错校验和udp是雷同的。计算校验和的这个逻辑也一模一样。在计算之前，也需要先添加12个字节的尾首部，只不过我们需要把udp尾首部当中的协议字段从17改为六。把udp的长度字段替换成TCP的长度好，

具体来说就是这个样子，之前我们在学习udp校验，或者说udp检验的时候我们说。在计算这个校验和之前需要添加12字节的尾首部，尾首部当中包含四字节的源IP，四字节的目的IP以及一个字节的全零。还有一个字节的协议字段。17表示的是，这是udp协议，而TCP协议对应的序号是六，所以如果我们要计算TCP的这个校验和，那么我们需要把这个17。改成六那最后这儿还有两个字节，我们用这两个字节去表示TCP的总长度。也就是TCP的首部和数据部分加起来总长等于多少要填到尾，

首部的最后两个字节。好，那么在添加了TCP的尾首部之后。计算校验合作方法和udp是一模一样的。对于数据的接收方来说，检验比特差错的算法也和udp 1模一样，所以这就不再赘述。其实TCP的检验和这个东西对于考试来说不是特别重要。因为考研大纲当中明确规定，我们要掌握udp的检验，但是TCP这个部分你看它没有强调TCP检验，对吧？既然二者的原理都是一模一样的，那么按照大纲的要求，如果想要考察这种呃检验的算法的话。

它肯定是放在udp这边来考，而不会在TCP这边来考好，所以这个字段你知道它和udp的逻辑是一样的，知道这点就行了。那在检验和后面紧急指针这个字段的含义，我们已经和urg这个标志位一起啊聊过了，所以接下来就到了最后这一行选项这个部分。这个部分的长度是可变的，可以为空，也可以非空。之前我们提到过一个很重要的概念，叫做mss最大断长。最大断长是呃两个进程，在建立连接的时候，具体来说就是在握手一握手二这两个报文段当中。

去呃，通过选项这个字段协商接下来的这个传输当中mss。最大段长等于多少？比如说客户端给服务器发的这个握手一里边可以在选项字段当中指明。接下来我要给你发送的这些数据mss=1000字节。这意味着，往后a给b发送的所有TCP报文段当中携带的数据，有效数据都不会超过1000个字节。那这样的提前协商可以让进程b提前准备好缓冲区。比如说进程b的接收缓冲区可以设置为64倍的mss。这个64是我随便举的一个例子啊。反正就是基于mss的大小去设置自己的接收缓冲区，到底需要预留多少？那这么做可以让呃预留的缓冲区大小适中，对吧？

对内存的利用率会更高。如果进程a告诉进程b接下来的mss=50000字节，那么用同样的这种方法去预留接收缓冲区。是不是总能保证这个缓冲区的大小能够满足接下来的这个通信，对不对？好，所以在建立连接的时候提前商定mss是有必要的。那值得一提的是，两个进程通知对方的这个mss是可以不相等的，比如说进程b可以告诉进程a说。接下来我给你发送的最大断长是800个字节。那如果进程a采用类似的方法去呃，预留接收缓冲区的话，它的接收缓冲区就应该等于64×800对吧？可以预留的更少一些。

这是现实的情况，两边设置的mss啊，可以不一样，不过在考试当中，为了降低难度，一般来说，这个mss都会设置为统一的值。并且最常见的大小就是1 KB。好，那么这是选项这个字段，虽然这个字段它本身不是特别重要，但是通过选项字段去协商确定的mss大小。这个东西是考试很常考的。那在现实应用当中，通常来说mss都不会设置的太大，

以免IP数据报被分片。好，这句话稍作了解就行。好的，在这个视频中，我们详细探讨了TCP报文段的格式。我们需要理解，并且记住各个字段的作用。那给大家画一下重点，简单来说，这节课全部都是重点。那特别注意序号，确认号，还有窗口这三个字段，在考题当中的常见的这种简记方式。

另外，对于中间这六个标志位来说，呃，我这画了红框的这三个标志位是最重要的。没有画红框的另外三个标志位啊，以及紧急指针这些东西考评不高，当然还有校验和这个字段。在TCP这边不是特别重要，如果要考校验的话，肯定会在udp那进行考察。那当然，还有保留这个字段，没有任何含义。最后选项这个字段，其实和这个字段呃关联的考试重点只有mss这个东西。

所以关键在于理解mss是什么。你要知道mss是在握手一和握手二啊，那两个报文段当中通过选项这个字段去进行协商的，知道这点就行了。那数据偏移这个字段，它表示的是TCP的首部，重点是记住它是以乘四字节为单位。记住这一点就行，那源端口号和目的端口号这两个字段呃，它的含义是很简单的，属于传输层的基础。所以这两个字段其实也不用花太多功夫，那总的来说，考试最常考的是序号确认号。还有窗口这三个字段，

以及这儿画了红色框的这三个标志位大写的ack syn，还有fin。其他打了叉叉的这几个呃字段，还有标志位，不用花太多时间去抠细节。这种考评不高的考点，你只需要在考试前几天啊，迅速的回顾一下，利用短时记忆，知道它们有什么作用就可以了。好，所以其实还是有重点的啊，序号确认号，窗口号，还有ack标志位syn标志位fin标志位。

这几个是考试的重中之重。好，那么大家可以再看图回忆一下啊，这些字段分别是什么含义？可以像背单词一样去复习这个部分。如果想不起来这些字段的作用，那再回去翻一下课件。当然，在后续的学习当中，我们还会结合一些实例。带大家更深刻的去理解这六个重要字段的用法和含义，所以现在你理解个大概就行了。好的，那么以上就是TCP报文段这个考点的全部内容。
