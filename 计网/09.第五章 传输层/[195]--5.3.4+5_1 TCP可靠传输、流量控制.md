各位同学大家好，在这个视频中，我们要探讨TCP协议如何实现可靠传输和流量控制。在大纲当中，对应这个位置。为什么我要把可靠传输和流量控制放在一起来讲呢？因为无论是可靠传输还是流量控制，都需要数据的接收方。和数据的发送方相互配合来完成。那可靠传输就是要告诉数据的发送方。目前已经被正确接收的呃数据有哪些？而流量控制就是呃数据的接收方要告诉数据的发送方。发送数据的速度应该控制在什么范围？那相比之下，拥塞控制并不是由数据的接收方啊，

去给发送方做反馈的。拥塞控制主要是发送方要监测网络情况，根据网络是否拥塞来调整自己发送数据的速度。那这是我们把可靠传输和流量控制放在一起来探讨的原因。这节课的内容比较多，首先我们要快速回顾TCP首部当中和可靠传输以及流量控制相关的这些字段。紧接着，我给大家设计了一个具体的例子，我们结合例子让数据动起来，在这个过程当中。去理解和体会可靠传输以及流量控制啊，到底是如何实现的？那可靠传输的实现。离不开序号这个概念。在TCP协议当中啊，

进程在建立连接的过程当中，会先设置一个自己的起始序号。然后在数据传输的过程当中，每个字节对应一个序号。那基于序号这个重要的概念，数据的发送方和接收方之间会相互配合使用确认机制。以及重传机制去确保数据的可靠传输。其实这两个东西大家会觉得似曾相识，对吧？因为我们在第二章数据链路层那个部分也聊过确认机制和重传机制。只不过，在数据链路层那一章ack确认还有这个重传，是以数据帧为单位的。而本节课介绍的这个确认和重传，是在传输层。

以TCP报文段为单位，那二者的原理有很多相似相通的地方。好，这是可靠传输，那对于流量控制来说，呃TCP协议也是通过滑动窗口机制来实现流量控制的。具体来说就是接收方要维持一个接收窗口，我们通常把接收窗口记为。rcvwnd或者rwnd这个rcv或者r它表示的是receive就是接收的意思。这个接收窗口的大小反映了接收方呃接收数据的能力，如果接收窗口越大，那么说明我可以继续接收的数据也越多。那么，接收方在给发送方返回ack报文段的时候，它需要顺道告诉发送方自己的接收窗口大小等于多少？

那发送方就可以去基于啊，接收方的接收窗口调整自己的发送窗口的大小，要确保发送窗口不能大于接收窗口。好，那么这些东西我都会用实际的例子带大家感受对了，对于考试来说，这节课要学习的内容是大题和小题的高频考点。虽然这节课的内容会比较多，但是相信我，大家把我设计的这个例子弄明白了，那么不管是大题还是小题，你一定会有很清晰的思路去分析。所以静下心来，把这节课的例子给弄明白，是一件投入产出比很高的事情。

好，那么首先我们来回顾这样的几个呃重要的字段，第一个序号字段啊，我们通常用se q sequence这种方法来简记。这个字段用于标记。当前，这个报文段当中携带的数据部分的第一个字节在原始字节流当中的位置。好，接下来确认号这个字段，它需要和ack这个标志位啊进行配合。如果大写的这个ack=0的时候呃，表示这个确认号字段是无效的，那通过之前的学习，我们知道。在整个TCP通信的过程中，

只有握手一的ack=0，其他所有的报文段ack肯定都等于一。那么，当ack=1的时候，表示确认号有效，而确认号这个字段是数据的接收方，给数据的发送方反馈。就是告诉数据的发送方，在这个确认号之前的所有字节我都已经正确收到了。好，那这是确认号这个字段在这节课的例子中啊，我在表示确认号的时候会用小写的ack去记录。然后大写的ack这个字段，我在这节课的例子当中就不会专门的把它写出来。因为它的逻辑很简单，

除了握手一之外，所有的TCP报文段ack都等于一。好，再来看窗口这个字段。这个字段就是接收方给发送方反馈自己的接收窗口的大小等于多少，也就是表明。从这个确认号之后啊，我还可以再接收多少个字节的数据，注意窗口这个字段是以字节为单位。那在这节课当中，窗口字段我会简记为rwnd。那么，接下来我设计了一个例子，从建立连接开始，一直到逐步的传送数据。

我会给大家标注每一个TCP报文段当中和可靠传输，以及呃流量控制相关的这些字段分别是什么值？另外，这是第一张图，在这张图的后面呃，我画了点点点后接的就是这张图，相当于这节课的例子，这些小图和小图之间其实是连着的。只不过由于这个图太长了，所以我把它拆分成了一小段一小段的部分好，还有一点有必要解释一下，就是在每一张图当中。如果涉及到重要概念的，我会用这种黄色高亮去进行标注。当你把黄色高亮的这些呃地方都理解了之后，

你也就能够理解可靠传输和流量控制背后的逻辑了。好，那么我们正式开始左边，这是客户端进程，右边这个是服务器进程，那首先发出握手的是客户端进程。假设客户端进程给自己呃设定的这个初始序号是五九九。那通过之前学习，我们知道握手一虽然不携带数据，但是它会消耗一个序号，所以服务器进程回复的挥手二当中，这个ack啊要加一。那与此同时，服务器进程，它给自己设置的这个初始序号是一九九，

那你会看到在握手二这个地方服务器给客户端返回了自己的接收窗口大小。对吧，接收窗口的大小等于八，意味着现在服务器进程，它的数据接收能力只有八个字节。那在现实应用当中，接收窗口的大小一般是以KB为单位的，比如说呃，接收窗口可能是8 KB或者16 KB之类的。但是在这个例子中，为了简化理解，我们把接收窗口啊设置的小一些，我们的重点是理解它的工作原理背后的逻辑。好，那么这是握手二，

那么当客户进程收到握手二之后，又会立即返回握手三。虽然握手二不携带数据，但是它呃依然需要消耗一个序号，所以握手三这边返回的ack是200。并且在握手三当中呃，客户进程也给服务器进程返回自己的接收窗口大小等于十。意味着接下来如果服务器进程给客户进程啊，发送数据的话，那么最多不能超过十个字节。好，现在我们暂停探讨一下啊，完成三次握手之后两个进程，它从内存的视角来看长什么样子？看这张图，

接下来我会以这张图为框架去模拟后续的这些TCP报文段当中。数据是如何呃挪动的？好，先来理解这张图，左边这是一个客户主机。这是它的IP地址，右边这是服务器的主机，这是它的IP地址，那我们知道每一台主机上面会运行着很多进程。我们假设客户主机上面运行的客户进程一正在和服务器主机上面运行的服务器进程。它们俩正在进行TCP连接。我们不妨假设这个客户进程是你写的一段程序，而服务器进程是你设友写的一段程序。你们俩写的程序都属于应用层。当这两个进程想要进行网络通信的时候，

就需要使用到传输层的TCP协议。好，所以这儿有个分界线，分界线的上面就是应用层，分界线的下面就是传输层。那应用层和传输层是从网络协议站的视角呃进行的解释。如果从操作系统的内存视角来解释的话，那你的这个进程，你写的这段程序，它是运行在内存的用户区。而传输层的呃TCP协议，它是运行在操作系统的内核区。那你设有写的这个程序也是一样的。好，那么既然要进行网络通信，

那你和你的舍友首先得通过系统调用给自己的这个进程。绑定一个通信用的端口号，假设你的进程绑定的是幺幺幺，而你的设友绑定的是九九六。好，那接下来客户进程要首先啊，发出建立连接的请求。你可以在程序当中请求操作系统。新建一个socket套接字对象，那很多跨考同学可能还没有接触过。面向对象编程这个概念。没关系，你不妨把对象理解为数据结构这个东西，你很熟悉了，对吧？

那么，你需要告诉这个操作系统，你想要连接的呃，那个服务器，它的IP和端口号等于多少？那对应的就是你设有的IP和它绑定的这个端口号，对吧？这些信息就是记录在socket这个数据结构当中的。那除此之外，在socket这个数据结构当中，也记录了本机的端口号，也就指向了你这个进程。好，接下来你可以在自己的呃，这个程序当中进行系统调用。

让操作系统的呃TCP协议这个部分。根据你填入socket的这些信息，去发起TCP连接，那具体来说就是向对方发出一个TCP报文段。对应的就是握手一对不对？那对应的就是这条线。那服务器进程这边收到握手一之后。也会生成一个socket对象。其中包含了和自己建立连接的对方的IP地址，还有端口号这些信息。另外，还需要记录本机进程的这个端口号。好，现在看下面这个地方。每一个socket对象，

除了这些简单的基本信息之外。它还会包含两个缓冲区，分别是接收缓冲区和发送缓冲区。顾名思义，接收缓冲区就是用于接收。呃，另一方给我发过来的数据而发送缓冲区，就是要把自己即将发送的数据先写到这个发送缓冲区当中。然后再发给对方。那我们不妨假设服务器这边操作系统给它分配的接收缓冲区和发送缓冲区都等于八个字节。而客户进程这边呃给他分配的是十个字节。大家不要被接收缓冲区发送，缓冲区这个浮夸的名词给吓到啊，本质上这个所谓的缓冲区就是操作系统。给这一次TCP连接分配的char型数组。

长度为八的char型数组。这个char型数组呃被包含在socket对象里边。客户端这边同理。那回到这张图，可以看到在握手仪当中客户进程告诉服务器进程，我的起始序号是五九九对吧？虽然握手翼不携带数据，但是它固定的要消耗一个序号。所以对于服务器来说，接下来他希望收到的呃后一个字节的编号应该等于多少？是不是应该等于600？那600这个值其实就是要给对方回复的呃，确认号对吧？那这个确认号是从握手二当中携带的。在图中对应这条线。

ack=600。好，那除了ack以外，我们看到握手二当中也告诉了这个客户进程，我这边的初始序号等于一九九。我这边的接收窗口等于八。初始序号是随便设置的一个值。那虽然握手二不携带任何数据，但是它固定的要消耗一个序号。所以当客户进程这边收到握手二之后，他就知道。接下来，我期待接收的第一个字节，它的序号应该是等于二零零，对吧？

好，这是根据握手二的序号加一得到的一个值。而这个值就应该是握手三当中。携带的确认号。那握手三，我们先放一放，我们回头看这个值接收窗口等于八，意味着什么？意味着服务器这边的接收缓冲区只能够接收从600这个序号开始的八个字节。那这个图体现的其实很直观了。好，那么对于客户进程来说，他收到握手二的这个接收窗口大小，意味着虽然我的这个发送缓冲区。有十个字节这么大。

但是我的发送窗口不能超过八。八个字节是对方接收数据能力的上限。超出八个字节，它的这个接收缓冲区就不够用了。缓冲区就会溢出。也就会导致数据丢失。好，这是服务器返回的握手二，接下来我们的客户端又需要给服务器。发出握手三。那握手三的这个确认号应该是等于200，这个刚才我们说过。除此之外，握手三当中还需要包含序号以及呃客户进程，这边的接收窗口大小。

你看接收窗口等于十。我们假设握手三不携带数据。好，那么现在服务器进程这边收到握手三。那握手三的这个接收窗口等于十，意味着呃客户进程这边他最多可以接收。从200这个序号开始的十个字节的数据，对吧？而服务器进程这边，它的发送缓冲区都只有八个字节这么大。所以服务器进程这边的发送窗口。可以把整个发送缓冲区给框下来。因为即便我把发送缓冲区的数据一口气全部丢出去，对方肯定都能够接得住。好，

那么从这个框架图当中可以看到TCP协议支持双向的数据传输，因此无论是客户进程还是服务器进程。它们都拥有各自的发送缓冲区和接收缓冲区。这两个缓冲区呃，具体分配多少取决于操作系统的内核。本质上一个十字节的缓冲区，它就是一个呃长度为十的char型数组。那发送窗口的大小不能超过发送缓冲区的大小。另一方面，发送窗口的大小也不能超过对方的接收窗口的大小。好，那么基于握手一二三我们知道。呃，客户进程这边，他接下来真正要开始发送的这些数据序号，

应该是从600开始起的，对吧？而服务器进程这边，它接下来真正要发送的这些数据。序号应该是从200开始的。我们不妨假设客户进程，接下来要发送100字节的数据，本质上这100字节的数据就是你自己定义的一个。长度为100的char型数组，你可以自己决定这个char型数组里边呃填入的是什么东西。好，这是你要给你的舍友发送的数据。好，接下来看你的舍友，这边程序应该怎么写？

由于他要接收你的100字节数据，那为了放得下你的这100字节。你的舍友这边他可以呃，提前定义好一个长度为100的叉二型空数组。这样的话，你这边发过来的呃，这些字节就可以呃，一个字节一个字节的填到这数组当中。那类似的道理，你的舍友要给你传50个字节的数据，那你这边是不是也得呃预留一个空数组用来接收这些数据，对吧？这个数组到底预留多长？这个是由你自己来决定的，为了简化大家理解，

我在这个例子当中是假定了你和你的舍友提前已经商议好了。呃，你们要给彼此传输的这个数据有多少？所以你们可以根据对方要传送的这个数据。去预留一个长度合适的呃空数组。但是在现实应用当中，其实两个呃进程之间的通信，它们到底要传多少数据往往是不可预知的。那在这种情况下，其实可以用动态扩容的方法去解决，就是说如果你的这个呃预分配的数组，它已经填满了，那么你完全可以用数据结构里边学习到的那些技巧。去给它进行扩容。好，

接下来我们不妨用零到99去标记呃，你准备好的这个数组。然后用零到49去标记你的舍友准备好的这个数组。零到99对应的是你的这个char型数组的呃下标对吧？数组下标。那如果从TCP协议的视角来看的话。你这儿的第零个字节，它应该对应的序号应该是多少？应该是600+0对不对？而你的数组当中，下标为一的那个字节，它对应的应该是601这个序号。下标为99的那个字节，它的序号应该对应的是六九九。那六零零到六九九这个序号是从TCP协议的序号，

这个视角来进行的解释。那在接下来的演示中，我会用零到99这个范围的编号去分别对应你的这些待发送数据。比如说你可以在自己的程序当中调用send函数。你需要在send函数当中指明。你要发送的数据。它在哪一个char数组当中？以及你要发送的这个数据是要发送到哪一个socket对象里边？那socket对象，它背后其实指向了一个发送缓冲区，对吧？所以当你调用了send函数之后，操作系统这边就会把你的这100个字节的数据。按顺序填到发送缓冲区里边，那由于发送缓冲区只有十个字节的大小。

所以刚开始只能把零到九这些字节给填到发送缓冲区当中。等发送缓冲区这边有新的位置，再把后续的字节给读过来，那这个是操作系统在背后给你做的事情。对于程序员来说，只需要指明自己要发的这个数据是从哪开始的，以及指明这个数据要发到哪个socket对象里边。指明这些东西就行了，那由于在建立连接阶段，socket对象这边其实已经记录了对方的这个IP地址和端口号，对吧？所以只要你把数据发给这个socket对象，接下来这个呃操作系统的TCP协议。就会根据socket对象里边的这些这些信息去构建TCP报文段。然后帮你把这些东西发给对方。

好，现在我们再来体会这个发送窗口的作用，是不是就很直观了？发送窗口框住的是这样的八个字节。意味着我最多就是一口气把这八个字节丢过去。对方暂时只能接收八个字节的数据，如果我一口气把零到九这些字节全部丢过去，那么对方的接收缓冲区受得了吗？受不了，对吧？好，所以从这个地方你已经开始体会到流量控制的呃，实现原理了。数据的接收方给数据的发送方返回自己的接收窗口的大小，而数据的发送方根据这个接收窗口的大小去调整自己的发送窗口的大小。

好，那么接下来我们会让这些数据动起来当服务器，这边的接收缓冲区收到呃一些数据的时候。这些数据是如何交付给啊？应用层的这个进程的呢？我们来解释一下背后的过程，你的设友可以在他的程序当中先定义一个长度为100的空数组。然后调用receive函数。在调用这个函数的时候，告诉操作系统。它的这个空数组的指针是什么？以及要告诉操作系统，我接收的这个数据来自于哪一个socket对象？那这样的话，当这个socket对象，

它的接收缓冲区这边有新的数据，到来的时候。操作系统就可以把这些数据。按照应用层的进程的指示。把它复制一份到这个空数组当中，对吧？那这就是数据从客户端呃传到服务器所经过的一个过程。那反过来也是一样的，服务器这边如果准备好了数据，那么它就通过send函数把这些数据。交给操作系统填到自己的这个发送缓冲区当中，然后从这个发送缓冲区又发到这个客户端的接收缓冲区当中。紧接着，客户端这边又通过receive函数从这个接收缓冲区当中读出这个呃，收到的这些数据。

对了，有一点值得补充的是一个端口，其实可以啊，建立多个TCP连接。比如说这个服务器进程，它绑定的端口号是九九六。那么，它可以用左边这个socket对象。和客户端进程一的这个socket对象。来进行TCP的传输。那类似的道理。同样采用本机的九九六这个端口。使用另一个socket对象就可以实现和客户进程二的这个TCP传输。所以通过这张图，想给大家补充的一点是一个端口，

它是可以支持多个TCP连接的，但是每一个TCP连接仅支持一对一的全双工通信。另一方面，在右边这个TCP连接当中，可以看到一个socket对象里边的这个接收缓冲区和发送缓冲区。其实，它们的大小可以不相等。具体分配多少空间，这个是由操作系统内核去决定的。那对于这个服务器进程来说。他想给右边这个客户进程发送的数据可以定义另外的一个char型数组，对吧？它的这些数据在调用send函数的时候，只需要指向另一个socket对象就可以了。好，

那么这是通过这张图，想要给大家补充的东西，一个端口号其实可以建立多个TCP连接。好，那么这个视频先到此为止呃，我们重点是体会这张图，理解TCP传输的底层原理。知道发送缓冲区，接收缓冲区是怎么回事？以及发送窗口和接收窗口，它背后的本质是什么？那基于这个框架，在下个视频中，我们会让这些数据真正的动起来。好的，

那以上就是这节课的全部内容。
