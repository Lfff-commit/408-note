好继续来看TCP连接管理，这节课我们主要探讨释放连接的阶段啊，这四次挥手背后的一些细节。同样的，我们需要关注在四次挥手期间啊，这几个标志位还有序号字段和确认号字段，它们的值分别是多少？以及要了解每发出或者每收到一个挥手报文段之后啊进程的TCP状态会如何转换。最后，我们还需要掌握在释放连接也就四次挥手的这个阶段，最短耗时需要多少？好，首先看第一张图，这张图我们重点关注在四次挥手过程中，每一个TCP报文段的fin，

也就是finish这个标志位。以及ack标志位还有啊序号以及确认号分别是什么样的值？首先，通过之前的学习，我们知道syn这个标志位只有在握手一和握手二当中会等于一。在其他所有的报文段当中，包括挥手一二三四当中啊，syn都等于零好，另外ack这个标志位除了握手一之外啊，都是等于一。所以挥手一二三四当中ack肯定也等于一。这意味着，同一报文段当中的确认号这个字段是有效的，再看fin finish这个标志位。当这个标志位等于一的时候，

意味着其中一个进程正在和另一个进程主动的挥手告别。也就是告诉对方，我的所有数据都已经发完了，已经finish了，所以只有挥手一和挥手三这两个啊TCP报文段。finish标志位等于一。而挥手二和挥手四当中finish都等于零好，这是三个标志位的信息，接下来。我们假设客户进程是首先发起挥手的那一方，那么当客户进程发出挥手一服务器，返回挥手二之后。客户进程向服务器进程发送数据的这个通道就被关闭了。但是服务器向客户端发送数据的这个通道还没有被关闭，所以我们会看到在挥手二之后，

服务器进程还可以继续单向的传输数据。那接下来需要注意的地方是，挥手一和挥手三，也就是两个finish报文段，即便不携带数据，也要消耗一个序号。这点和握手一握手二是类似的，好来看一下挥手一这个地方挥手一的这个序号字段等于九九九。我们假设挥手一没有携带任何数据。但是挥手一依然需要消耗一个序号，所以你会看到服务器这边返回的呃，确认号等于一万。类似的道理，我们注意看挥手三，在挥手三当中，

服务器给客户端返回的这个呃序号等于7800。虽然挥手三不带数据，但是它依然需要消耗一个序号，所以呃，客户端给服务器返回的这个确认号。会加一也就变成七八零幺好，这是需要注意的地方。对了，这个地方值得一提的是，挥手一和挥手三其实是可以携带数据的，但是在现实应用当中，我们往往不会让挥手一和挥手三去携带数据。所以考试的时候也通常会默认不携带数据这种情况。好那么挥手一挥手三其实可以携带数据，挥手二也可以携带数据，

但是挥手四不可以携带数据，我们分析一下挥手二和挥手四的情况。当挥手一发出的时候，意味着这个客户进程，他想要单方面的断开这个数据传输的通道，对吧？但是并不意味着服务器进程向客户端进程，这边的数据通道已经断开了。所以服务器进程在返回挥手二的时候，可以在挥手二当中携带一些数据，这个是OK的。而对于挥手四来说，虽然这个呃TCP报文段是客户端返回给服务器的，但是由于前边的挥手一和挥手二。呃，

之后客户端向服务器的这个数据通道其实已经提前关闭了，对吧？所以挥手四这个TCP报文，它只能用于啊释放连接。而不能再携带数据好，所以挥手一二三都可以携带数据，但挥手四不能携带数据。好，这是这张图需要注意的地方。最后解释一个细节呃，假设在挥手二及挥手二之后，服务器进程陆续的向这个客户端进程。传输了2200字节的数据。那么，到挥手三的时候，

你会看到呃服务器进程的这个序号字段已经从5600。加了2200。变成了7800，对吧？好，所以挥手二和挥手三之间的序号差值取决于呃，这两次挥手中间总共还需要传输多少数据？那这一点结合序号这个字段的呃，本质含义应该不难理解。好，接下来看，释放连接阶段的这个进程状态转换。先来看客户进程，在客户进程发出挥手一之前呃，一直都是established，

也就是建立连接的状态。而客户进程发出挥手一之后，就会进入到呃finish wait 1啊，finish wait 1终止等待一这样的一个状态。等客户进程收到挥手二之后，又会进入到finish w two啊，终止等待二这个状态。好再往后，如果客户进程收到挥手三，那么他就进入了time weight时间等待的状态。与此同时，在收到挥手三的时候啊，这个客户端进程会启动一个计时器。倒计时等待两倍的msl，这么长的时间呃msl这个概念，

我们一会再来补充，反正就是启动一个计时器。当这个倒计时为零的时候，客户进程就进入了最终的连接关闭状态。好，这是客户进程的状态转换。在考试当中，通常是用这个英文的术语去呃，表示这些状态。好，接下来看服务器进程的状态转换。首先，建立完连接之后，服务器进程就一直处于这个established这个状态。当服务器进程收到挥手一之后，

它就进入了close weight关闭等待状态。而当服务器进程发出挥手三之后，它就进入了呃last at tec最后确认状态。好，那么在收到挥手四之后，服务器进程就直接进入关闭状态。好来看道真题。二一年统考的38题。大家先暂停自己做一下。说客户首先向服务器发送了呃finish段。请求断开TCP连接。finish段是什么意思啊？就是finish标志位等于一的TCP报文段。那finish=1的只有挥手一和挥手三，对吧？

结合前面给的这个条件，说客户首先向服务器进程发出了这个呃finish段，那么首先发出的那次挥手肯定就是挥手一对不对？好，那么题目问当客户收到服务器发送的finish段，并且向服务器发送了ack段之后。客户的TCP状态转换为什么？好客户收到服务器的finish段，那么呃，这个finish段肯定对应的就是挥手三。然后客户又向服务器发送了ack段，ack段的意思是ack=1的报文段，那么在这个地方它对应的肯定就是挥手四对吧？好题目问我们的是发出了这个ack段，也就是发出了挥手四之后客户进程的TCP状态转换，

为什么？那回到之前，这个图发出挥手四之后。客户进程，他的状态变成了time weight，所以这个题目选择b选项，你看这个考点其实是一个呃纯记忆性的考点啊。首先，你要知道，挥手一挥手，二挥手，三挥手，四它们之间的这个逻辑关系。与此同时，你还要知道，

每发出一个挥手，每收到一个挥手之后，这个进程的状态是怎么转换的。那像状态转换这个东西，反正我是记不住啊，对于这种考点，我一般是考前先回顾一下。利用短时记忆，在考场上能做题就行了。那还有一点需要补充的是我们讲的这个例子当中，以及在这个考题当中都是客户首先向服务器进程发出了这个挥手的请求。事实上，也可以是服务器进程先发出挥手。也就是说，这张图的左边，

你也可以把它换成服务器进程，而右边换成客户端进程。那这种情况就是说服务器进程首先发出挥手。这两个角色互换之后，它们的这个状态转换是不是也会调换啊？所以一定要注意审题，比如说我们把这道题目啊，稍加改造。假设是服务器进程，首先向客户端进程发送了这个finish段请求断开TCP连接。在这种情况下，当客户进程收到服务器发出的呃finish断，并且向服务器呃发送了ack断之后。客户的TCP状态应该转换，为什么？

你看角色一变，一切都变了，对吧？服务器进程首先向客户进程发出这个finish段，也就是对应这个挥手一。那么，客户进程向服务器进程返回的这个ack段。这个ack段就变成了呃，对应这个挥手二对不对？那此时问我们客户的TCP状态转换，为什么是不是就得回答close wait啊？所以如果这么改造这个题目的话，它就变成了a选项。好，所以这一点是值得注意的，

会完全改变一个题目的啊，这个分析过程。好，接下来我们分析释放连接的过程需要消耗多长时间，接下来的这个耗时分析，我们是基于客户进程。首先发出了挥手，基于这样的前提条件啊来进行的。好，那首先填刚才留下的一个坑，这个地方我们引入了一个新的概念，叫msl，它是maximum e segment lifetime的缩写。代表最长的呃报文段寿命。它是由TCP协议规定的一个固定时间长度。

比如说我们可以规定msl等于呃800毫秒，那这就意味着当客户进程收到挥手三的时候。它会启动一个计时器，这个计时器会持续二×800，也就是1600毫秒，当1600毫秒的倒计时结束之后。这个客户进程才会真正的进入close关闭状态。好解释一下，为什么收到挥手三之后还要等待一段时间才会关闭这个连接？原因是这样的，我们知道网络的传输有时候不可靠，对吧？比如说客户进程给服务器进程返回的这个挥手四。它的传输失败了。服务器进程迟迟没有收到，

挥手四那么在这种情况下，服务器进程就会重新发一个挥手三。因为他会以为是不是客户没有收到我的挥手三对吧？好，所以这个地方我们要留一段等待的时间，就是为了防止这种情况发生，因为挥手四不一定能够顺利的传给服务器b。所以客户进程需要等待比较长的一段时间，如果在这一整段时间之内，哎，我一直没有收到服务器进程重复发送的这个挥手三。那么我就可以猜测他应该是收到我的挥手四了，对吧？此时客户进程就可以放心的关闭连接。好，

所以这段时间的等待是有必要的。那另外需要补充的是，如果在等待的期间重复收到了挥手三重复收到了挥手三。那么，这个计时器会重置。像刚才这个例子，假设在这个时间点，我又重复收到了挥手三，那么从这个时间点开始，那么我又需要重新倒计时1600毫秒。接下来，如果倒计时的过程当中没有再重复的收到挥手三。那么，客户进程就可以顺利的关闭连接。好，

就是这样的一个逻辑，那msl和之前我们介绍的一个呃术语mss注意别混淆。mss的前两个字母也是对应maximum segment最后一个s，它对应的是size。所以mss它描述的是这个最大的报文段长度。也就是一个TCP报文段当中，最多可以携带多少字节的数据，它表达的是这个东西。因此mss这个术语，它的单位对应的应该是字节。而msl它对应的应该是时间长度秒或者毫秒之类的。好，那么引入了msl这个概念之后，我们就可以分析啊，这个耗时到底等于多少了？

我们考虑最短的耗时。首先看这个地方，如果服务器进程在收到挥手一的时候已经没有更多的数据需要发送了。那么，服务器进程可以连续的发出挥手二和挥手三如图所示，挥手二和挥手三几乎是呃前后脚就是一起出去的。那这意味着，服务器进程的中间就是close weight，这个呃阶段，它的持续时间极短。接近于零号时。那相应的，在客户进程这边，他收到挥手二的时候进入呃，这个终止等待二这个状态。

而收到挥手三的时候，又立即进入这个time weight状态。所以对于客户进程来说，这段时间也是极短暂的，无限接近于零。好，那在这种情况下，客户进程从发出挥手一一直到进入close状态，总共需要的时间是不是就只需要？这儿是一个rtt。然后下面这我需要再等待两倍的msl对不对好，所以总共就只需要一倍的rtt，加上两倍的msl。只需要这么长的时间，因为我们说中间这个部分，

它已经无限接近于零了，对吧？好，那么对于服务器进程来说，如果从客户进程发出握手，一开始算，一直到它进入这个关闭状态，所需要的耗时就应该是一点五倍的rtt。我们是从客户进程发出挥手，一开始算的，那挥手一过来挥手，二挥手，三过去，这儿就是一倍的rtt，对吧？

接下来客户进程的挥手四还需要经过一次单向的传播时延，当挥手四到达服务器的时候。服务器立即进入这个关闭状态，而挥手四所需要的这个单向传播时延就是零点五倍的rtt。因此，从发出挥手一开始一直进入到close状态，总计就是一点五倍的rtt。这就是至少需要的时间好，接下来看一个真题，二二年的39题，假设客户c和服务器s已经建立了TCP连接。呃，往返时延rtt题目给了等于50毫秒，最长报文段寿命msl给了等于800毫秒。数据传输结束后，

客户进程主动的请求断开连接。也就是说，挥手一是由客户进程发出的。问，从客户进程发出挥手，一开始一直到客户进程进入到close状态以及服务器进程进入到close状态。分别需要多长时间？回到前面这个图，对于客户进程来说，他总共需要的时间就是一倍的。rtt加上两倍的呃msl。而对于服务器进程来说，从发出挥手一这一刻开始，一直到服务器进程进入到close状态，它总计需要一点五倍的rtt。

需要这么长的时间。所以把题目给的条件代进去，就可以得到答案，选择d选项。这是客户进程所需要的时间一倍的rtt，加上两倍的msl。而服务器进程呃所需要的时间等于一点五倍的rtt。好，这是二二年这道题，如果这个题目要做一些改编的话，无非就是啊，把这个时间的起点和终点这些条件。嗯，给你变一变对吧？比如换种问法，

我可以问你从服务器进程收到挥手一开始。到服务器进程呃，关闭连接这个部分所需要的耗时等于多少？显然一倍的rtt嘛。好，再换一种问法，从服务器进程发出挥手二开始，一直到客户进程进入到close状态，总计需要多长时间？那此时就应该是零点五倍的rtt。加上二倍的msl，对吧？好，所以这儿给大家总结了两个例子，不用去记忆，

关键是要理解往返时延，还有这个两倍的msl分别是在哪些阶段需要使用的？理解了背后的这些逻辑，你才能够以不变应万变。好的，那么通过上节课和这节课的学习，我们详细探讨了TCP连接管理这个考点，首先需要注意在三次握手和四次挥手当中。acksyn和fin这几个标志位的值分别应该如何设置？只有握手一的ack=0，只有握手一和握手二的sin=1，只有挥手一和挥手三的fin=1。好，这是三个标志位。那么TCP连接管理主要探讨的是建立连接和释放连接的两个阶段呃，

分别需要注意的一些事情。在建立连接这个阶段，我们需要注意的是，握手一和握手二是不能携带数据的，但是它们也要消耗一个序号。那由于握手一和握手二，它们的syn=1，所以在题目当中也经常把握手一和握手二称为syn段。那么，当客户端进程收到握手二之后，就可以开始向服务器发送数据了，因此握手三呃这个报文段。它可以选择携带数据，当然也可以选择不携带，那假如握手三不携带数据的话，

它就不消耗序号。这点是和握手一握手二有区别的。关于建立连接阶段的耗时分析问题啊，我们关键是要理解客户端进程，它是在什么时候可以开始传送数据？服务器进程是从什么时候才可以开始传送数据？只要理解了这一点，那么任何呃耗时分析的题目都难不住你。好，这是建立连接的阶段，那么在释放连接的阶段，首先要注意的是挥手一和挥手三啊。即使不携带数据，也要消耗一个序号。挥手一和挥手三实际上可以携带数据，

但是在现实应用当中，通常不会让它们携带数据。所以考题当中一般来说也是这样的条件。那由于挥手一和挥手三，他们的finish这个标志位等于一，所以这两个报文段也可以称为finish段。那接下来挥手，二是可以携带数据的，而挥手四不可以携带数据。那释放连接这个阶段呃，如果考察耗时分析的题目。首先，要想到msl最大报文段寿命，要想到这个东西，这个东西是TCP协议规定的一个固定时间长度。

它的作用是当客户进程收到挥手三并立即返回，挥手四之后，客户进程需要立即进入到倒计时的状态。需要倒计时两倍的msl，然后才可以进入close关闭状态。耗时分析的题目，如果要考的话，肯定考的是最短耗时，那什么情况下这个呃连接关闭的时间最短呢？肯定就是服务器进程，收到挥手一的时候已经没有待传送数据了，在这种情况下，这个连接释放的耗时会最短。此时，服务器进程可以连续发出挥手二和挥手三。

其实只要抓住这两个关键点，那么管它耗时分析的题目如何改变，我们都会有思路去解决。好，那最后还要提醒大家的是在考试的前几天一定要记得回顾建立连接释放连接呃，特别是释放连接阶段的这个呃进程的状态变化。这个东西不看几天肯定就忘，所以考试之前快速回顾一下就可以了，好那么以上是TCP连接管理这个部分。
