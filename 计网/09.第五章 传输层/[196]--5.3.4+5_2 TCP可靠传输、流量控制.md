好，继续来看TCP的可靠传输和流量控制。上节课我们介绍了前三次握手背后发生的一些事情，接下来我们正式开始传输数据，假设客户端进程首先给服务器进程发送了三个字节的数据。那么，从发送窗口这边取出呃三个字节，把这三个字节封装成一个TCP报文段。那在首部这个位置序号字段应该设为多少啊？应该设为600对吧？600意味着，当前这个报文段当中第一个字节，它的序号等于多少？那除了序号之外，还需要返回这个确认号。

以及客户进程，这边的接收窗口大小。那由于序号和接收窗口大小啊，和上一个握手三没有任何改变。所以在这个图示当中，我们就不画进去了，好接下来这个TCP报文段啊，会封装成IP数据报，然后经过路由器的转发，最终到达服务器的传输层。那这些数据会被放到接收缓冲区里边。零一二这三个字节。那接收方的TCP协议还会对这些数据进行比特校验。不知道大家还有没有印象TCP首部，这儿有一个校验和字段，

我们之前说过TCP的校验规则和udp的校验规则是一样的。所以，当数据的接收方收到一个报文段之后，它需要用这个TCP校验去检查里边有没有比特错误。那假设这三个字节当中都没有比特错误，那接下来服务器进程是不是就可以给啊？客户端进程返回一个ack了。那么，在这张图当中，就对应下面这条线。为了方便探讨，我们假设现在服务器进程这边还没有准备好，要发送给客户端的数据。你可以理解为在现在这个时刻，你的舍友这边虽然已经和你建立起TCP连接了，

但是它的这50个字节的待发送数据。还没有准备好，以及还没有呃调用send函数，所以服务器进程这边的发送缓冲区暂时还是空的。暂时还没有数据，要发给这个客户进程好，所以此时服务器进程啊，给客户进程。发送的这个TCP报文段当中，它只有首部而没有携带任何的数据，对吧？相当于这个TCP报文段，它纯粹就是为了返回ack而存在的，那来看一下服务器返回的这个TCP报文段当中。序号，

这个值等于200 ack确认号等于六零三好，我们重点关注这个确认号和接收窗口。确认号等于六零三，是因为现在我接收到的这三个呃字节。它的序号应该是六零零到六零二，对吧？那接下来我期待接收的这个字节就应该是六零三。同时，当服务器返回这个确认号的时候，就意味着它告诉这个数据的发送方六零三之前的所有呃字节我都已经收到了。好，这是ack确认号的含义，再来看这个接收窗口的大小，我们看整个接收缓冲区只有八个字节，对吧？

接下来我还能接收从六零三这个序号开始，往后五个字节的数据，所以接收窗口的大小就只剩下五这么多。好，那么这个TCP报文段被客户端收到之后，由于这里边还没有携带数据，所以呃，客户的接收缓冲区这边我们暂时不需要填任何东西。那根据确认号ack客户进程，这边是不是就可以确定零一二这三个字节已经被顺利的接收了，对吧？接下来要发送的数据应该是从六零三这个序号开始的。好，那么既然前三个字节已经收到了，他们就没必要再留在这个呃发送缓冲区里边了，

对吧？所以已经确认接收的这些数据，可以把它啊从发送缓冲区当中清除。那这样的话，发送缓冲区是不是就可以接收后面那些字节了？那后面的字节就是六一零六一一。六一二。好，这是客户端，根据服务器的确认号做的一些操作。把已经确认接收的那些数据全部从发送缓冲区清除。那接下来还有一个信息需要处理，由于对方的接收窗口变成了五，所以接下来我还能够给对方发八个字节的数据吗？他已经收不了这么多了，

对吧？所以客户端这边需要实时的去调整自己的发送窗口大小。好，所以客户端的发送窗口，我们应该缩小为五。意味着接下来我最多只能发送六零三到六零七这几个字节。好，那么通过这个例子，我们能很直观的感受到确认号以及接收窗口这两个字段呃，它们的作用。好，我们让故事继续假设，接下来这个客户进程向服务器进程连续的发送了两个TCP报文段。第一个报文段携带了两个字节，第二个报文段携带了两个字节。

那服务器这边连续收到两个报文段之后，它可以采用累积确认的这种方法。只返回一个ack。因为确认号的含义是说这个确认号之前的所有字节我都已经收到了，对吧？所以如果它连续收到的这两个报文段。它们所携带的这些字节，刚好是连续的，那么服务器只需要返回更大的那个ack值就可以了。好理一下这个过程，客户端构造了两个TCP报文段，第一个报文段携带的呃字节序号是六零三到六零四。第二个报文段携带的数据序号是六零五到六零六。好，所以第一个报文段，

它的序号字段应该是六零三，第二个它的序号字段应该是六零五对吧？那么，这两个报文段呃到达服务器这边之后。无论这两个兄弟他们是谁先到达。服务器这边的TCP协议总能根据这个序号字段去决定它携带的这些数据应该填到什么位置，对吧？比如，假设第二个报文段先到达，那么由于这个TCP协议，它知道哎，接下来六零三应该填的是这个位置，六零四是这个位置。而六零五应该填这个位置对吧？好，

所以六零五六零六这两个字节肯定就应该填在这样的位置，对吧？那相应的收到前面这个段之后也可以根据它的这个序号字段去决定它所携带的这些数据应该填到哪？那么，显然就应该是这样的位置好，那么假设刚才这两个TCP报文段呃差错校验都没有问题。那么，接下来服务器返回的这个确认号就会到六零七，对吧？意味着六零七之前的所有的这些字节我都已经顺利收到了。好，所以服务器这边会构造一个ack段。它只有手部不携带任何数据。其中有两个关键信息，首先是确认号等于六零七，

其次接收窗口只有一。因为接收缓冲区不够了，对吧？接下来最多再接一个字节好，那么这个东西被客户端进程收到之后。它就可以知道六零七之前的所有字节，六零三六零四零五零六这些字节都已经被顺利的接收，对吧？那既然已经顺利接收。这些东西我就可以把它清出去，不再占用这个发送缓冲区。好，与此同时就可以再读入后面一些字节的数据。就变成这个样子好，这是收到确认号之后呃，

在背后要做的事情。接下来还要根据接收窗口去调整自己的发送窗口大小，那显然接下来发送窗口就只能等于一。最多只能再发送一个字节的数据。好，那么通过刚才这个例子，我们可以深刻的体会到累积确认到底是怎么回事，服务器这边。连续收到了两个呃TCP报文段。但是它只需要返回一个ack。好，接下来让故事继续。由于接收窗口只有一的大小，所以再往后一个TCP报文段当中，最多就只能再携带一个字节的数据，

对吧？接下来你会看到服务器进程，在收到这一个字节之后，它的接收缓冲区呃就满了。那当接收缓冲区满了的时候，就应该尽快的把数据按序交付给应用层。清空接收缓冲区。这样又可以使得接收窗口重新变大好，我们来模拟背后的过程，现在发送窗口的大小只有一。所以接下来的这个TCP报文段只能携带一个字节的数据。那这个字节，它对应的起始序号啊，就应该是六零七。好，

现在这个TCP报文段来到服务器这边。服务器这边把这个字节放到呃对应的位置，也就变成了这样。好，现在你会看到整个接收缓冲区已经满了，对吧？此时如果再不清空接收缓冲区，那么发送方这边就没办法再啊，继续发送数据了，对吧？好，所以服务器这边的TCP协议，它会把目前已经收到的这些数据按序交付给应用层。本质上就是把这些数据。从下面这个char型数组复制到呃。

用户区的这个char型数组。好，现在接收缓冲区空了之后，这边的接收窗口就可以变大了，对吧？那接下来服务器这边。期待接收的这个呃序号应该是从六零八开始的，六零八之前的所有的这些数据都已经接收，并且已经先交给这个应用层了。好那么，此时服务器返回的这个呃报文段当中。确认号应该就是等于六零八，然后接收窗口等于八，太好了，我又行了，

对吧？好，那么客户这边他知道六零八之前的这些字节都已经收到了。那么，已经确认被接收的。这些呃数据。就可以从发送缓冲区删掉。与此同时，发送缓冲区的这个新空位，可以把后面的字节先给复制过来。好，接下来由于接收窗口的大小恢复，为了八所以发送方这边的发送窗口也可以恢复成八。好，那么通过刚才这个例子，

我们就能够理解什么叫传输层按顺序把数据交付给应用层。本质上就是把接收缓冲区里收到的那些数据按顺序复制到应用层去。好，这是这个例子。好，那么我们让故事继续。在接下来的这个例子中，你会体会到捎带确认这种机制它的作用。我们假设接下来客户给服务器发送了两个字节的数据。与此同时，服务器这边刚好已经准备好了五个字节的呃，待发送数据。在这种情况下，当服务器进程返回ack的时候，就可以在其中捎带上自己要传的这五个字节数据。

好来看一下背后的过程，客户端构造了一个新的呃TCP报文段，携带了两个字节的数据。那这些数据的起始序号是六零八。现在这些数据到达了，这个服务器这边。我们需要把这些数据放到接收缓冲区呃，正确的位置。好，那么接下来服务器应该给这个客户端返回ack了，对吧？好，现在故事发生了，假设你的舍友他写的这个程序，哎，

已经准备好了五个字节的数据。并且它通过线的函数，把这五个字节的数据发到这个发送缓冲区这边。那由于这边的这些数据，它的起始序号是二零零对吧？所以这些数据的第一个字节，它的序号应该是二零零第二个字节应该是二零一第三个字节是二零二。二零三二零四好，这就是你设有的前五个字节所对应的序号。它是从二零零开始的。好，那么既然服务器这边刚好也有数据要发给客户端，那服务器构造的这个TCP报文段。是不是除了返回确认消息之外，也可以顺带的把二零零到二零四这些呃字节给带回去啊？

那这就是所谓的捎带确认，在返回ack的同时，捎带上自己已经准备好的一些数据。好，那么我们来看一下TCP首部当中需要填的一些啊内容，首先由于这次携带的这些数据啊，它是从二零零开始编号的，对吧？所以序号字段应该填二零零。好与此同时，确认号这个东西我们来看一下，他接下来期待收到的是六幺零呃，往后的那些字节对吧？六幺零之前的都已经顺利接收好，所以确认号是六幺零好，

除此之外还需要返回这个接收窗口的大小，接下来还能够接呃六个字节，对吧？还能够接六个字节。所以接收窗口大小等于六。那这个报文段最终被客户进程收到。那由于这个报文段当中携带了数据。所以这些数据会被复制到呃客户进程的接收缓冲区里边。总共五个字节，第一个字节的序号是二零零，那接下来对于客户进程来说，他往后想要接收的后一个序号就应该是二零五了，对不对？二零五之前的所有字节我都已经顺利接收了，好，

这是对数据的接收。接下来，客户端还需要处理这个确认号，还有窗口大小这两个东西。根据确认号可以知道六幺零前面的所有字节已经被顺利接收。所以这些字节没必要再占用发送缓冲区。当发送缓冲区空了之后，就可以把呃后续的那些字节给读过来。好，另一方面，发送窗口不能大于接收窗口的大小，所以发送窗口应该调整为六。好，这是客户进程，收到啊，

服务器进程的这个捎带确认消息之后所做的一些处理。我们分析完了这两条线。好，现在客户进程，他收到服务器进程发来的数据之后，是不是也应该给服务器？返回这个确认的信息啊，那与此同时，客户进程也有待发送的数据，所以接下来客户进程给服务器进程返回的这个TCP报文段当中。是不是也可以用捎带确认的方式返回ack确认号的同时，也把自己的这些数据给带上？好，所以故事就来到了这条线。我们假设客户进程也采用了捎带确认的方式，

并且在这个TCP段当中携带了六个字节的数据。好来看一下客户进程，把发送窗口内的所有的这些呃数据都一次性带出去了。那起始序号是六幺零。好，除此之外，还需要携带ack确认号的信息。由于二零五之前的所有的这些呃数据已经收到了，所以确认号就应该返回二零五。好，另一方面，由于客户进程这边他还没有把这些数据从接收缓冲区拿走，所以接收缓冲区现在是不是只能再接收五个字节的数据了？因此，客户端进程的这接收窗口啊，

它就得设为五。好，现在这个报文段啊，到达了。服务器进程。拿服务器进程，把这些数据复制过来。好，现在既然这个接收缓冲区已经满了，那么里边的数据应该尽快清空，对吧？也就是要把它呃。尽早交付给这个应用层。说白了就是复制到你设有定义的这个数组当中。那接下来这个服务器进程，

它期待接收到的呃下一个序号是不是就变成了六幺六对吧？好，这是对报文段携带的这些数据的处理。接下来还需要对确认号以及接收窗口进行处理。首先确认号等于二零五，那么就意味着二零五之前的呃，所有的这些数据都已经被顺利的接收。那么，已经被接收的数据是不是没必要继续占用这个发送缓冲区了？对吧？好，另一方面，由于对方的接收窗口缩水了啊，缩到了五。所以服务器这边的发送窗口也得调整大小。

也就是设置为五。意味着接下来如果准备好数据的话，最多只能发五个字节过去。好，那么我们假设你设有写的这个程序啊，后续的那些数据暂时还没有准备好，还没有send过来。也就是说，接下来服务器给客户端返回的呃TCP报文段当中，已经没有数据可以捎带了，对吧？好，那么在服务器返回的这个报文段当中，它的序号字段应该变成二零五对吧？意味着接下来如果我给你携带数据的话，

那么第一个字节的数据是从二零五开始编号的。好，那除此之外，还有确认号，刚才顺利的接收了六幺零到六幺五，这个范围内的字节。所以接下来服务器进程这边，期待收到的呃下一个序号应该是六幺六。所以ack确认号这个字段就应该填六幺六。好，另一方面，由于接收缓冲区已经清空了，所以啊，接收窗口又变成了八。那么，

服务器进程在收到这个反馈之后。它就可以确认在六一六之前的所有字节都已经被顺利的接收。那么，这些字节就呃可以移出发送缓冲区。同时，把客户进程已经准备好的呃数据再给读过来。好，这是根据确认号做的一些反应，接下来由于接收窗口恢复为了八，所以客户端的发送窗口也可以啊扩大。好，那么刚才我们探讨的是这两个TCP报文段，对了，这段文字我发现打错了，这应该是服务器进程，

收到数据后。缓冲区已满。此时应该把缓冲区的数据尽快交付给应用层。在前面这些例子中，我们都是让接收缓冲区满的时候，才把呃接收缓冲区清空，对吧？但在现实应用当中，并不是只有缓冲区满才会把这个数据交给应用层。即便缓冲区没有被填满，只要缓冲区里的这些数据是有序的，那么我们也可以把这些数据。提前交给应用层。好，所以在接下来的这个例子当中，

是想让大家体会到这一点，我们假设接下来客户进程要给服务器进程发两个字节的数据。那这两个字节的序号就应该是六幺六和六幺七起始序号六幺六，那除了序号字段之外，由于这个确认号还有接收窗口，这两个字段。和前面没有发生任何改变，所以另外这两个字段我们就不再特别的强调。好，现在这个报文段到达服务器。服务器把其中的数据放到接收缓冲区。此时序号在618之前的所有的数据都已经被顺利的接收。现在虽然接收缓冲区没有满，但是由于收到的这些数据是有序的，所以服务器这边可以提前把啊，

有序的这些部分数据。先交付给应用层。那这样带来的好处就是接收缓冲区总能有呃更多的空间。那相应的，这个接收窗口也能尽量的大。好，所以通过这个例子，主要是想解释这一点，并不是接收缓冲区满了才可以交付应用层。在现实应用当中，即便缓冲区没满，只要这些数据是有序的，那么也可以提前交付给应用层。对了，可能有必要解释一下什么叫数据有序，

什么叫数据无序，假设在现在这个情况之下，如果服务器进程。他乱序的收到了二零二一二二，他提前收到了这些数据，也就是前边的六幺八六幺九这些呃，前置的序号他还没有收到。那么，此时接收缓冲区里的这些数据，就不能提前交给这个应用层。因为我们需要确保有序交付，按序交付对吧？如果我们把二零二一二二这几个数据直接放到。17的后面，那显然应用层收到的数据就变成乱序的了，

对吧？而TCP协议一定要保证。我给应用层交付的这些数据必须是有序的，不能是乱序的好，所以如果接收缓冲区，现在只有这几个数据，那我还不能交给应用层。我至少得确保前边的18，19，这些数据也到达了之后，我再把这一整块的数据。按序交付给应用层，对吧？好，所以这是按序这两个字想要强调的东西。

好，那接下来我们让服务器进程返回ack包文段，这个报文段只包含首部，其中确认号是六一八。而接收窗口等于八。意味着接下来服务器有能力接收从六幺八这个序号开始啊，往后的八个字节。好，那么客户进程收到这个东西之后，他就可以调整自己的发送缓冲区幺六幺七已经被收到了。那么，已经确认接收的就移出缓冲区。缓冲区的新空位可以读入呃，后面的数据。好，

接下来再检查接收窗口，对方的接收窗口等于八，所以呃，这个客户端的发送窗口的大小，它不用缩水，对吧？继续保持这个值就可以了。好，那么这是服务器返回ack之后的呃一个效果。好，接下来让故事继续在后面这个例子中，你会体会到超时重传机制是怎么工作的？为了确保传输的可靠性，TCP协议规定，在每发出一个报文的呃同时。

就要给这个报文设置一个呃超时重传计时器。这样的话，如果这个报文在传输过程当中，因为网络原因而丢失了，那么这个计时器时间到了之后。发送方就会进行超时重传，当然在重传的同时也要重置这个计时器。好，那么我们来模拟这个例子背后的过程。假设这个报文段携带了两个字节的数据是从序号六一八开始的，那报文段就应该长这个样子，那我们在发送窗口这边画一个圈。表示这两个字节被包装到同一个报文段当中，好，现在当这个报文段发出的时候。

这边就会启动一个超时重传计时器。假设这个报文段没有顺利到达呃，服务器这边。那么，这个计时器肯定会超时，对吧？而只要它超时，客户端这边就会重新构造啊，这个TCP报文段。与此同时，重置这个计时器，那么假设这次这个报文段顺利的到达了接收方这边，那么接收方这边收下数据之后。会返回ack报文段。其中确认号应该是等于呃六二零。

另外，接收窗口等于六。好，那么这一次在计时器超时之前，客户端这边顺利的收到了这个ack。所以就能够确定六幺八六幺九都已经顺利收到。那已经收到的就移出发送缓冲区，与此同时，根据接收窗口呃调整自己的发送窗口。调整为六。好，那么这就是超时重传机制。接下来再看一个例子，假设客户端传送两个字节给服务器。服务器收到报文段之后返回ack，

但是ack在返回的图中丢失了。那对于客户端来说，由于它发出的这个报文段迟迟没有收到ack，所以它呃一定会超时。那一旦超时，客户端又会重传这个报文段。而服务器又会再次返回ack好来看这个过程，客户端呃发送两个字节。起始序号是六二零。当这个报文段发出的时候，会设置一个计时器。现在这个报文能够被顺利的接收，那其中的数据会被写到呃，接收缓冲区这边。接下来，

服务器期待收到的呃下一个序号应该是六二二。那服务器会返回ack报文段，其中确认号等于六二二。接收窗口只剩下四的空间。那遗憾的是，这个ack报文段并没有被顺利的接收啊，它在传输的过程当中丢失了那客户进程的这个报文段，它的计时器肯定就要超时，对吧？超时之后需要重置计时器，与此同时再重新构造一个一模一样的这个TCP报文段，那同样的把这个报文段发出去。现在服务器这边收到了重复的这个数据，对吧？服务器这边会先检查自己现在呃保存的这个确认号是六二二，

意味着六二二之前的所有的数据我都已经收到了，对吧？那么，现在收到的这个TCP报文段，它里边携带的数据就是重复的数据。好那么，此时服务器进程这边，它会再次构造ack报文段。确认号同样是六二二呃，接收窗口同样是四，没有变。那这次这个ack被客户端顺利的接收，这次这个计时器还没有超时，所以客户端进程判断。六二二之前的所有数据已经收到。

那么，已经确认接收的就移出缓冲区。与此同时，根据对方的接收窗口去调整自己的发送窗口大小。变为四。好，那么这是通过这个例子，要让大家感受到的东西，无论是发送方的这个呃报文段丢失。还是接收方返回的这个ack端丢失。超时重传机制，它总能正确的处理这些问题。那这就保证了TCP协议的可靠性。好的，那么通过这节课的例子，

我们感受到TCP协议当中可靠传输和流量控制是如何去实现的？首先，可靠传输的实现需要基于序号这个概念。在两个进程建立连接的时候，需要确定一个起始序号，那么在数据传输的过程当中，每个字节都会对应一个序号。我们引入了确认机制。并且确认机制当中啊，包含累积确认的规则，也就说如果数据的发送方收到呃，这个确认号等于n。那么就说明序号在n之前的所有字节都已经正确的接收。数据的接收方返回确认消息的时候，有可能会构造两种ack段，

一种叫做捎带确认，一种叫做专门确认。那专门确认这个词是我自己自造的一个术语哈，在我们的教材当中，只有捎带确认这个说法，捎带确认的意思就是啊，一个ack段里边。顺道呃携带了自己要传给对方的数据。而专门确认就是说这个ack段，它只有首部而不携带，具体的数据。那这是确认机制当中有可能出现的两种ack端，那在TCP协议当中呃数据的接收方应该在什么时候返回ack呢？通常来说，会采取推迟确认的这种啊，

这种思想因为确认机制有累积确认的规则，对吧？所以如果数据的接收方返回ack的这个时间，让它稍微推迟一些，就是拖上一段时间。那么就可以通过一个ack段去累积确认很多很多个数据，对吧？不过，对于具体可以推迟多久TCP协议是有一些限制的。首先，推迟的时间最多不能超过零点五秒。相当于什么呢？如果服务器这边收到客户端的这个数据，那么服务器不会立即返回ack。其实可以让它间隔零点五秒，

万一在这零点五秒之内，还有连续的数据到达呢，对吧？这样的话，服务器就可以利用累积确认的这种机制。啊，用一个ack段去确认多个报文段。就有点类似于前面讲的这个例子一样，在这个例子当中，客户端连续的发来了两个TCP报文段。照理来说，这两条线应该画的非常接近，但是因为要写这些文字，所以不得已把它们画的比较开。反正对于服务器来说，

如果它在收到第一个呃TCP报文段的时候，它能够等一等，等个零点五秒。那么，在这零点五秒之内，假如还有另一个TCP报文段到达，那么它就可以二合一用一个ack段去累积确认前边收到的所有的这些数据，对吧？好，所以呃，稍微推迟一点再返回ack，这个是呃，有一定的好处的。好，这是推迟时间的上限，

那另一方面，如果说这个服务器进程，它自己刚好也有数据要传送的话，那么就不用再推迟了。就应该立即返回ack端，并且捎带上自己的数据，这点可以看这个例子，我们在讲这个例子的时候说。呃，客户进程给服务器进程发来了一个TCP端，与此同时，服务器进程刚好准备好了五个字节的数据，也想要发给对方。那么这个时候服务器进程不需要推迟返回ack，而应该立即返回顺道，

就把自己的这个数据给带回去。那相比之下，在下面这个例子当中，如果服务器进程暂时没有数据，想要返回给对方的话，那么它就可以先等零点五秒。零点五秒之后再返回这个ack。好，最后关于推迟返回ack这件事啊，如果数据的接收方收到了两个长度为呃，最大段长的这种报文段。那么就应该立即返回ack。我们可以思考一下，为什么？如果一个报文段，

它的长度是mss，那么意味着这个报文段当中携带的数据量是比较大的，对吧？那么，这种很长的报文段，如果传输的过程出现了问题，那对他进行重传的这个代价是不是也会比较大？因此，如果连续收到两个最长的报文段，那么数据的接收方应该尽早的给对方返回确认的消息，让对方知道，哎，这个东西已经顺利的收到了。不需要超时重刷。要尽早的让这个事情尘埃落定。

好，所以是出于这样的一个考虑，那以上是关于确认机制相关的东西，关于重传机制，我们在这节课的例子当中，主要是探讨了超时重传。也就是每发出一个报文段，就设置一个计时器，如果计时器到期还没有收到呃，这个报文段的确认，那么就需要重传这个报文段，并且重置计时器。这个就是超时重传机制，在下个视频当中，我们会补充一种重传机制，

叫做快重传，通过冗余ack实现。那为了支持快速重传，我们还需要一个配套机制，叫做立即确认，你看立即确认和推迟确认这两个东西，我把它标成了同样的颜色，说明二者是有关联的。立即确认就是推迟确认的反面，如果采用立即确认这种呃方式的话，就意味着当数据的接收方收到一个TCP报文段的时候。一定会立即返回一个ack，而不会再推迟零点五秒或者呃，必须等到自己有数据要捎带。不需要再等待这些条件，

只要收到一个TCP报文段，就立即返回一个ack。所以在下个视频中，我们会把推迟确认这种方式改为立即确认这种方式，然后再引出快重传这种呃新的机制。那具体的我们放在下节课再来探讨。好，这是TCP协议实现可靠传输的一些呃原理。那在这节课的例子中，我们也体会到了呃，流量控制是如何实现的？接收方要维持一个自己的接收窗口。接收窗口的大小不能大于接收缓冲区的大小。另外，接收窗口框住的是接收方，

还允许接收的序号范围，对吧？那另外，对于发送方来说，它也需要维持一个发送窗口。首先，发送窗口的大小不能大于发送缓冲区。其次，发送窗口的大小不能大于接收窗口的大小，那这一点也是实现流量控制的关键。通过接收方的接收窗口大小去限制发送方的发送窗口大小，这样就实现了由接收方去控制。发送方发送数据的速度就实现了这样的一个事情。那发送窗口框住的是发送缓冲区当中已经发送，但尚未接收到确认的数据以及可以发送，

但是。还没有发送的数据，那这点结合这节课的例子已经很好理解了，好的，那么这就是TCP协议实现可靠传输以及流量控制的原理。在下节课，我们再来补充一个快重传机制，那以上就是这节课的全部内容。
