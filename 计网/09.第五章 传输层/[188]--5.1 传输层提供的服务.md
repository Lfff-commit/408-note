好的，接下来进入第五章传输层的学习。呃，内容比较少。首先，第一个部分讲的是传输层提供的服务，那这个部分主要是介绍传输层的一些基本概念。通常来说，不会单独的去考察啊，第一个大标题一般来说就是结合udp或者TCP协议啊来考察。啊，那么传输层只有两个协议，一个是udp协议，相对来说更简单一些，历年考察过三个小题。

其中，二零二四年考察了一道呃udp校验的题目，而其他的年份基本上都是考udp数据报或者是呃。啊udp相关的一些基本概念基础知识好。udp这边考的就是更简单一些。接下来重点是TCP协议。TCP协议有史以来考过20道小题以及两道大题考评，还是非常高的。那TCP相比于udp来说，它的啊，实现原理背后的过程都会复杂的多。那接下来，我们会依次探讨这三个部分，在这个视频中，我们先探讨啊，

传输层提供的服务。那对应王道书的五点一小节。好那么直接上总结。对于传输层的啊，这些基本概念最重要的就是要先理解端口它的作用。那理解了端口的作用之后，我们才能够理解传输层提供的这些功能到底是怎么实现的。那我们不妨先回顾一下传输层在协议栈当中的位置，那么从上往下看，在所有的这些协议的啊上面。是用户，我们可以给应用层的这些应用程序啊，提供我们想要传输的数据。比如说我们想用微信传一张图片，或者想用QQ发一串字符，

那这些数据是由我们用户直接提供的。那么，我们的数据交给了应用层的呃某一个进程之后，这个进程可能会在我们提供的这个数据前面。加一些啊，应用层的首部信息。应用层的首部h5，这个部分有可能存在，也有可能不存在，具体来说就是取决于应用程序，它的呃实现逻辑。比如说用微信给微信传一张照片，那么也许微信的开发者认为。在这个照片的数据的前面。我需要增加一些呃，

有必要的信息，比如说这张照片是什么时候发送的？什么时候发送的，以及这张照片的大小。还有这张照片的格式等等，有可能微信的开发者认为啊，这些信息有必要写在h5，也就是应用层的首部这个位置。那在这种情况下h5这个部分的数据就会存在。那换一个例子，假设QQ的这个呃应用程序开发者认为如果用户给我丢来一张啊，照片的数据。那么，我不需要在这张照片前面加任何东西，我只需要直接把这张照片通过这个传输层的TCP协议或者传输层的udp协议。

传给另一个QQ进程就可以了。所以如果说应用程序的开发者，他认为在这个呃用户数据的前面没必要加任何东西的话，那么事实上这个应用程序。可以直接把用户数据直接交给这个传输层的TCP或者udp进行传输。那如果传输层使用的是TCP协议。那么h4这个部分添加的就是TCP协议的首部。而如果采用的是udp协议，那么h4这个部分添加的就是udp的首部，那具体来说，这两种协议的首部，它的呃格式定义是不一样的。我们会在之后的学习当中看到这两种手部啊，分别长什么样子？那在传输层这个地方，

无论使用的是TCP还是udp协议，反正最终这整块的数据，传输层的这整块数据都会交给网络层进行下一步的封装。那具体来说就是交给网络层的IP协议IP协议啊，会把。传输层交过来的这一整块数据，封装成一个IP数据报。好，那么这就是传输层在协议栈当中的啊地位，它为上层，也就是应用层提供服务。同时，传输层又需要使用到网络层的IP协议所提供的服务。那之前我们在网络层那个部分学过NAT路由器，当时我们就引入了传输层端口号的概念。

我们说一个IP地址可以标记网络当中的某一台特定的主机。所以网络层可以通过原IP地址和目的IP地址这两个信息去完成IP数据报。在主机和主机之间的这种传输。然而，一个IP数据报到达一台主机还不够，最终IP数据报所携带的数据肯定还得交给上面的某一个应用进程。那此时传输层的端口号就会发挥作用，我们可以给主机上面的一个进程。绑定一个端口号。并且同一台主机上的不同的进程，它们的端口号都是不重复的。那这样的话，我们就可以用呃主机的IP地址，加上这台主机上某一个特定进程的端口号。用这两个信息去精确地指向网络当中的某一台主机，

上面的某一个特定的进程。那么，传输层就是通过端口号来区分数据来自哪个进程以及去往哪个进程的？所以传输层在网络层之上，基于源端口号和目的端口号这两个特性。实现了啊，端到端的通信，也就是进程到进程之间的通信。好，接下来我们需要补充一些和端口号相关的知识。在这个图当中，我们会尝试着捋清进程端口号，还有传输层协议之间的这种关系。假设这儿有一台主机a和主机b，它们分别拥有各自的IP地址。

假设主机a上面的进程一正在和主机b的进程五进行，这种网络通信。并且这两个进程的通信是使用了TCP协议。好紧接着进程二和进程六也在进行通信，它们俩之间的通信也使用TCP协议。再往后进程三和进程七之间也正在进行这个啊，数据通信，只不过它们俩之间采用的是udp协议。最后进程四和进程八之间也在进行通信，它们俩之间使用的也是udp协议。下面解释一下应用层和传输层中间的这几个数字，这几个数字代表的就是端口号。也就是说，进程一。它绑定的是TCP协议的二五零，

这个端口号。而进程二，它绑定的是TCP协议的七七七，这个端口号进程三绑定的是udp协议的32，这个端口号。就是这个意思。好，那么从这两个图当中，想跟大家强调这样的一些东西，首先两台主机的端口号是相互独立的。比如说你会看到左边这台主机a它的进程二绑定了七七七这个端口，右边这台主机。它的进程七也在使用七七七这个端口，然后左边这台主机，它在使用幺幺三这个端口，

右边这台主机，它的进程六也在使用幺幺三这个端口。所以这想表达的是端口号，这个东西是每一台主机，它自己的一个资源。只要确保主机内部端口号别重复就行，但是不同的主机当中端口号重复是可以被允许的。所以这句话想表达的是这一点。接下来第二句话TCP和udp的端口号也是相互独立的。那要理解这句话，可以看右边主机b的啊，进程五和进程八。可以看到，这两个进程，前者使用的是TCP协议，

后者使用的是udp协议。前者使用的是八八六端口，后者使用的也是八八六这个端口。二者使用的端口号是相同的，但是由于它们啊，在传输层对应的这个协议各不相同。所以这两个看起来相同的端口号其实并不冲突。比如说当进程一和进程五需要通信的时候，它需要指明这样的呃三个信息。首先要指明我们这两个进程的通信采用的是什么协议。那么，按照这个图来说，就是采用TCP协议。第二，还需要指明啊，

本进程绑定的端口号二五零，这个是进程一绑定的端口号。绑定了这个端口号，就意味着接下来如果进程五想要呃给我发送数据。那么就往这个IP地址的TCP协议当中的二五零这个呃端口。给我发送数据就行了。所以一个进程要和另一个进程通信的时候，需要给自己绑定一个这个端口。用于接收对方给我发过来的这个数据。好，这是第二个要指明的东西，第三个要指明的啊，信息就是。我要和对方通信，那么对方的这个IP地址还有端口号是多少？

那么在这个图当中。进程五的IP地址是二三幺点二点二点二呃，端口号是八八六。那由于双方采用的是TCP协议，所以进程一给进程五发送的这个数据。会从进程一的这个TCP到达进程五的TCP。然后再去检查这个端口号，八八六。于是这个数据就可以顺利的送到a进程五。也就是说，虽然udp这边同样也在使用八八六端口，但是在传输的过程当中，可以根据现在使用的是TCP还是udp协议。去区分哎，我这个数据到底是应该从TCP呃往八八六这边去呃递交还是从udp往八八六这边去递交？

好，所以可以看到两个协议，它的这个端口号是相互独立的。好，那这是这个地方想要强调的东西。同时，在刚才这个过程中，我们也梳理了两个进程，要通信的时候啊，需要指明哪些信息？好，那接下来思考这样的一个问题。两个进程之间的通信有可能会呃传输一系列的数据。那如果每一次发送数据给另一个进程的时候，我都需要在自己的这个呃代码当中。

写明对方的IP地址端口号，并且在写明使用的是呃什么协议，如果每一次都重复去写这些东西。是不是会很麻烦？对于写代码的人来说会很麻烦，对吧？那为了让代码变得简洁，因此在C语言C加加当中定义了这样的一个呃数据结构，叫做套接字。英文叫socket。那套接字分为TCP套接字和udp套接字，比如进程一和进程五之间，它们要采用TCP协议进行通信。那么，在进程一的这个呃代码当中，

就可以创建一个TCP套接字。在这个数据结构当中，去指明接下来要和我进行通信的这个进程，它的IP地址等于多少？以及它对应的端口号等于多少？那这样的话，接下来如果两个进程之间需要有多次这种数据传输。那么，在写代码的时候，是不是就可以直接用套接字？这个数据结构去说明我的这个传输方向是什么就可以了？也就是说，程序员在写代码的时候，就不必每一次都说我使用的是什么协议，我要跟哪个IP哪个端口通信。

不用每次都去重复这些参数。好，那这是套接字的作用，本质上它就是一个数据结构，而这个数据结构，它的作用就类似于一个网络指针。可以去这么理解。它指向了网络当中。将要和本进程通信的另一个进程。就是这么个意思好，那这是套接字的概念。那值得一提的是，在套接字这个数据结构当中，除了包含啊IP地址和端口号，其实还包含了其他的一些重要的信息。

那由于我们现在主要是在学习理论，而不是在学习编程，所以其他的东西我们就暂时不做拓展。有兴趣的同学可以自己去搜一下C语言或者C加加的这个socket套接字啊，在实际写代码的时候应该怎么使用？好，那么通过刚才学习，我们认识到了端口的作用，我们可以用端口号去标识主机上的一个特定的进程。那值得注意的是，每台主机的端口号都是相互独立的。另外，在同一台主机上，TCP和udp两种协议的端口号也是相互独立的。那无论是采用TCP还是udp协议，

我们在C语言和C加加语言当中啊，进行网络编程的时候都会使用到这个套接字，这个数据结构。一个套接字包含了啊IP地址和端口号，这两个重要的信息，那通过这两个信息就可以唯一的去标识网络中的一台主机。上面的一个特定的进程。那有了端口这个概念之后，传输层就可以实现端到端的通信，这个功能也就是实现进程到进程之间的通信。接下来简单介绍一下这个端口号的分类。首先，传输层的端口号啊，是用16比特来表示的，它的取值范围是零到六五五三五。

那么，所有的这些端口号被分为了两大类，第一大类是服务器啊，使用的端口号，第二类是客户端使用的端口号。什么叫服务器？什么叫客户端呢？所谓服务器，就是被动通信的一方。而客户端一般是主动发起通信的一方。比如你打网络游戏的时候，你的手机上安装的这个王者荣耀啊，它就是属于客户端。当你启动王者荣耀这个客户端的时候，它会主动的和王者荣耀的服务器去建立连接，

发起通信。所以客户端一般来说是啊，主动发起通信的一方，而服务器就是等待着被通信的那一方。服务器是被动响应的，客户端是主动发起通信的。那么，在早期的互联网标准文档当中，规定大于四九一二五的这些端口号。一般来说就是让客户端这种呃进程去使用。而小于四九一二五的这些端口号，一般来说就是会用于服务器进程。但是现在这种标准其实已经不流行了。那到目前为止，还依然在遵循的一种啊，

约定俗成的标准就是零到一零二三这个范围内。一般来说是熟知端口号。这个范围的端口号通常只能用于被熟知的重要应用程序。比如说FTP文件传输协议。FTP这种应用几乎在每个人的电脑上，每个人的手机上都会安装这种应用程序。属于装机必备好，另外当我们在发电子邮件的时候，发email的时候都会使用到SMTP这个协议。所以这种协议也是被熟知的，被全世界范围内普遍使用的协议。因此，像25这个端口号就专门被留下来给SMTP协议去使用那类似像DNS这个协议。在每个人的电脑上，每个人的手机上也都会安装。

这个协议的作用是可以把网址转换成IP地址，比如说平时我们上网的时候，我们通常输入的就是网址，对吧？网址是很方便记忆的3w点，百度点com，那么当我们要去访问这个网站的时候。其实在背后，我们最终需要访问的是这个网站的服务器。而DNS这个应用就可以呃，把这个网站的网址用一些方法转换成这个网站的服务器所对应的IP地址。所以DNS这个应用也是装机必备的，每一台电脑，每一台手机，上面一定会安装这个DNS协议。

好，所以像这种在全世界范围内被广泛使用的应用程序，我们的互联网管理组织就会规定。呃某一些特殊的端口号哎，一般人不能用，需要专门预留给这种重要的应用程序。所以如果我们自己开发一个应用程序的话，那么零到一零二三这个范围的端口号呃，通常来说是不能使用的。那如果你开发的应用程序想要使用呃零到一零二三这个范围内的端口号，其实也可以。如果你能让你的应用程序在全世界范围内超级流行，也成为装机必备，每个人的电脑离开你的这个应用程序，它都工作不了。

如果你开发的应用程序能够达到这种境界，那么事实上你也可以向这个呃互联网的这个管理机构去申请。专门给你的这个应用程序分配一个这种呃特殊的端口号，这个端口号独你所用。所以如果你开发的应用程序，想要使用这个范围内的端口号，那理论上也是可行的，只是有一点点难度而已。好，那么除了这个范围之外的其他端口号，在实际的开发当中其实并没有特别严格的限制。只要在你的本机上呃一个端口号没有分配给其他进程，就是别冲突就行了。所以刚才我们提到的这个端口号的分类，它是属于呃早期的，

这种互联网标准，并且这种标准它只是一种建议性的标准，而非强制性的标准。建议性的标准就是说，互联网的这个呃管理机构，它建议啊，所有的这个服务器进程使用这个范围的端口。同时，也建议所有的客户端进程使用这个范围的端口。但是建议终归还是建议你也可以不听。因此，关于端口号的分类，这个部分啊，我们重点关注熟知端口号啊的范围是零到一零二三。知道这一点就可以了，

好那么这是端口这个部分啊，需要了解的一些东西。那有了端口之后，传输层就可以实现端到端的通信，也就是进程到进程的通信。同时，也可以实现复用和分用的功能。首先来看什么叫复用。如果结合协议栈来看的话，复用的过程指的是数据从上面到下面。我们知道一台主机的应用层可能会同时运行多个进程，而这些进程。都需要同时使用传输层的呃，同一个协议，比如说进程一和进程二，

他们想要发送数据的时候。都会使用到传输层的这个呃TCP协议。所以复用的意思就是说啊，同一台主机上的多个进程。可以复用这个传输层的同一个协议。好，再看什么叫分用当主机b的，这个TCP协议接收到一些数据的时候，它可以根据这些数据的目的端口号去判断。每一块数据应该交给上一层的，哪一个进程？也就是说，数据从下往上传递的这个过程当中，在传输层这个地方，同一个协议接收到的这些数据。

会正确的就是分派给上层的进程，那这个过程就叫分用，所以可以看到复用它描述的是。数据从啊应用层往下面往传输层走的这个过程，这个过程叫复用。而分用指的是从传输层往上走的这个过程。好，那没跟上的同学可以暂停看一下这个文字描述。OK，接下来再看传输层的差错，检测功能无论是采用TCP协议还是udp协议，这两种协议在收到数据之后。都会进行差错检测。也就是检查收到的这个数据有没有发生比特错误？那如果说检测出比特错误，

那么就会直接把数据丢弃。无论是TCP还是udp，都是这样的，检测出错误就直接把数据丢掉，但是二者的区别在于TCP在丢掉数据之后。会通知这个数据的发送方，让它重传。而udp是不通知发送方的，因此当两个呃进程采用TCP协议进行数据传输的时候，呃通常来说都可以确保数据都是正确完整的。而如果两个进程采用udp协议去进行数据传输，那么呃传输的这些数据有可能出错，有可能丢失。好，那这是差错检测功能，

两种协议在检测出差错之后，它们的处理方式各不相同。简单来说TCP就是靠谱一些，udp就是不太靠谱。那TCP协议可以向上层应用层提供面向连接的可靠的端到端传输服务。而udp协议向应用层提供的是无连接的，不可靠的，端到端传输服务。好，我们把这两个定语拆分来看，首先来看什么叫面向连接，什么叫无连接。所谓面向连接，就是有连接，就这么个意思。

好，接下来看一下什么叫做有连接的这个传输服务，什么叫无连接的传输服务来看这样的一个例子。假设现在小帅要给小美传输一些信息，那么它们俩之间的信息传输可以采用有连接的，这种传输方式也可以采用无连接的传输方式。先来看什么叫有连接的传输。小帅想给小美发一些信息，那么他首先会建立连接。比如说刚开始先给小美发这样的一些数据，在吗？说点事情。接下来，小美会回复在的请讲。然后小帅又给小美说好的，

那我要说了。那他想说的具体内容是v我50，我去解救肯塔基老爷爷，这两句话是小帅要给小美发送的数据。那么，当数据发送完了之后，小帅会说，说完了拜拜，小美说拜拜。好，这是有连接的传输，再来看无连接的传输，小帅真正想给小美说的话只有两句，第一句叫做v我50，第二句叫做我去解救肯塔基老爷爷。

如果采用无连接的传输方式，那么就意味着小帅并不会提前跟小美打招呼，没有这种打招呼的过程。直接把数据丢给对方。而相比之下，采用有连接的这种传输方式，小帅在给小美传输数据之前，会先跟对方打招呼。先确认对方已经准备好接收我的数据了，并且在传输结束之后还要告诉对方啊，我已经说完了，我的数据已经传完了。那就这样，拜拜。先跟对方打招呼，

让对方准备好接收数据，这个过程叫做建立连接，而数据传输完了之后。跟对方说再见，通知对方，我的数据已经传完了，这个过程叫做释放连接。好，那这是有连接的传输和无连接的传输。接下来再看什么叫可靠的传输，什么叫不可靠的传输。通常来说，可靠的传输都需要基于呃有连接的这种，这种方式也就是说在传输之前需要先建立连接，传输完了之后还要释放连接。

好，所以左边这个图表示的是可靠传输，右边这个图表示的是不可靠传输。好看，左边这个图。在建立连接之后，小帅给小美说了第一句话。给它编号为一，告诉它vo 50，第二句话我要去解救肯塔基老爷爷当小美啊，确认接收了这两句话之后。它会给小帅返回一个确认消息。那小帅获得这两条确认消息之后，他是不是就可以确定哦？我之前传过去的那两句话一定都是精准送达了。

他已经收到了，对吧？所以对于小帅来说，这个传输的过程很可靠，非常可靠。那相比之下，如果采用不可靠的传输，就意味着小帅给小美说了两句话，但是小美不给小帅任何回应。那这样的话，小帅能够确定这两句话是可靠的，交给了小美吗？他也确定不了，对吧？只是说我已经尽力了，

我已经告诉他这两句话了。如果说这个传输的过程出现了问题，那我也没办法，那这就叫不可靠的传输。好，所以所谓的可靠性啊，是用这种确认机制来保证的。在数据链路层，我们也接触过啊，这种确认机制也就是ack。反正就是发送方呃，发了几条消息之后，这个接收方收到了哪些一定要给这个发送方？进行反馈。这样的话，

发送方就可以知道哪些数据已经被正确的接收了。以此就保证了数据传输的可靠性。那对于不可靠的传输接收方，无论是收没收到数据，无论这个数据是否正确，反正这个接收方都不会给发送方任何的反馈。好，那这是可靠与不可靠的区别。好，现在再回头来看这两句话，相信已经没那么陌生了。TCP协议，它呃向应用层提供的是面向连接的。可靠的端到端传输服务，而udp向应用层提供的是无连接的，

不可靠的端到端传输服务。所以在现实应用当中，TCP协议啊，通常来说会用于对于数据的准确性啊，要求很高的地方。比如说两个进程之间传文件。我们不能容忍文件的数据有任何的差错，对吧？所以像这种场景就是对准确性要求更高的。此时，我们会采用TCP协议。而udp协议呃通常来说会用于准确性要求不高，但实时性要求比较高的这种呃网络通信的场景。比如说你跟别人视频通话的时候，你在跟别人视频的时候，

呃，那个画面偶尔不清晰一点，或者偶尔有一些马赛克什么的，其实。是可以忍受的，对吧？但是我们在视频通话的时候，对于这个实时性的要求会很高，如果对方说了一句话，你得等十秒钟才能听见，那么这个视频对话就很难受了，对吧。那除了视频通信之外，像现在的直播一般来说，也是用udp协议，

比如说某一个直播平台的服务器。它需要给成千上万的手机。给成千上万的手机呃发送这个直播的画面，还有声音这些呃这些数据对吧？那你在直播的时候，如果说这个画面偶尔变得没那么清晰，或者有那么一两秒钟，甚至这个画面丢失，你都是可以忍受的。但是在看直播的时候，你希望实时性要尽可能的高，对吧？所以对于这种场景啊，我们就会使用udp协议。好，

因此TCP和udp这两个传输层的协议啊，各有各的优势，各有各的劣势，我们在进行应用程序开发的时候。可以结合实际的这个呃需求去判断到底应该采用TCP还是udp。好的，那么在这个小节中，我们对传输层有了一个大致的了解，首先我们详细探讨了端口的作用。紧接着，我们又详细探讨了传输层支持的一些功能，那这些功能的具体实现，我们还会在呃TCP和udp协议当中再进行进一步的探讨。好的，那以上就是这一小节的全部内容。
