各位同学大家好，在这个视频中，我们要探讨udp检验这个考点，那所谓的udp检验就是说。数据的发送方给数据的接收方呃，发送一个udp数据报之后。接收方如何去检验出这个udp数据报当中是否有比特错误？其实也就是udp协议的差错控制，就这个意思。好，接下来我们先提出一种啊，新的差错检测方法。假设左边这个a结点要给右边这个a结点发送32比特的二进制。好，那么为了让b节点在收到这些数据的时候，

能够检测出其中是否发生了这个比特错误？我们需要在这个原始数据当中再携带一些呃校验信息，然后把原始数据校验信息一起发给这个接收方。这样的话，数据的接收方就可以根据这些校验信息去检查到底有没有出错了？好，接下来看这个校验信息应该如何生成？我们看这个式子。假设我们最终要生成的校验信息啊，总共是16个比特，那么我们可以把原始数据以16个比特为一组。让这些二进制数呃进行二进制加法。那最低位两个一相加本位和等于零往更高位进一个一。好，接下来次低位零+0，

再加上呃来自低位的这个进位零+0+1本位和就等于一。同时，往更高位近零。好那么二进制的加法，我们在计算机组成原理里边已经训练了很多了，具体的细节就不再赘述。总之，把这32比特的原始数据拆分成每16比特为一组，然后让这些分组啊进行二进制加法。那相加的结果就是这样的一串数字，大家可以自己算一下好，最后我们再把这个呃加法的运算结果逐位取反。也就是一变零零变一逐位取反，取反之后得到的这16个比特，就作为我们的校验和。

好，所以这32比特的原始数据，再加上这16个比特的校验和数据一起发给呃接收方b节点。那么b节点在收到这些啊内容之后就可以啊，用类似的方法去检查有没有发生比特错误？显然，如果说没有任何一个比特发生错误的话。那么，这三组16比特的数。依次进行二进制加法，最终得到的结果肯定是全一。这是没有发生比特错误的情况。很好理解吧，因为我们的这个校验和它是把呃前边的这些原始数据加和的结果。逐位按位取反对不对？

所以如果数据的接收方把这些16比特数呃依次进行加法运算的话，肯定就是16比特的权益啊。那假如这三组数相加的结果不是全一不是全一，那么是不是就说明了其中肯定发生了某些比特错误？好，那么这就是udp校验的一个呃核心的思想。在刚才这个例子当中，原始数据我们假设只有32比特，那如果原始数据更多一些，其实也是同样的道理。比如说原始数据假设有48比特。那么，同样的，在计算16比特校验和的时候，我们把这48比特的原始数据拆分成16比特，

16比特为一组。然后让这些16比特的数依次进行这个二进制的加法。那首先是前两个16比特数进行二进制的加法，那相加的结果是这样的。接下来再把这个呃加法的结果和第三个16比特进行呃相加的运算。那么，加法运算得到的结果是这样的。那在这个加法运算当中，出现了一个比较特殊的情况，就是最高位的这个呃二进制加法运算。产生了往更高位的进位。那当我们遇到这种情况的时候，需要把往更高位的进位，让它加回到最低这一位。我们把这个过程称为回卷。

把最高位的一。加回到。最低位。那么，一+1本位和等于零往更高位进一。一+1本位和等于零往更高位进一零+1本位和等于一。往更高位进零，也就是说再往前的这些比特就不会再发生改变了，好，那这是最高位回卷的意思。好，那么现在这个运算结果就是原始的三个16比特数啊，依次相加，并且考虑了回卷啊，得到的一个计算的结果。

那么，把这个计算的结果再进行逐位的取反。零变一一变零主谓取法，那这样的话就得到了16比特的校验和。好，那么发送方就可以把这个原始数据携带上16比特的校验和一起发给b节点。那对于接收方来说，他去检测比特错误的方法也是雷同的，反正就是把他收到的这些内容以16比特为一组。依次进行二进制的加法，但是需要注意，我们这说的二进制加法，如果最高位产生了进位的话，需要把它回卷到呃最低位。要考虑这个运算，

那么显然如果没有任何一个比特发生错误的话，那么前三个16进制数。相加的结果肯定还是这样的一个结果，对吧？那最终再加上呃16比特的这个校验和那得到的运算结果，肯定就是16比特的权益。好，所以如果接收方啊加法运算的这个结果为16比特权一，那么就说明没有发生比特错误，而如果不是16比特权一，那就说明发生了比特错误。好，所以从这个例子当中，我们可以看到我们前边设计的这种方法，即便原始数据啊变多。

也是完全适用的。另一方面，在这个例子当中，我们强调了呃，进行二进制加法的时候，一定要注意最高位产生的进位需要回卷。好，那么理解了这种啊，差错校验的方法之后，接下来就可以正式的介绍udp校验它背后的一个过程了。首先，在udp数据报的首部这个位置呃，末尾的两个字节表示的就是udp数据报的校验和。两个字节对应的就是16个比特。在计算校验和之前。

数据的发送方会首先在udp数据报的前面增加12个字节的尾首部。那展开来说，这12个字节包含四个字节的源IP地址，四个字节的目的IP地址。然后一个字节的全零，一个字节的无符号数17。最后还有两个字节表示的是udp长度。那尾首部当中的udp长度这个字段和真正的首部当中的这个udp长度字段，二者是相等的。而原IP地址和目的IP地址这两个字段的信息，就取决于发送方的IP地址和接收方的IP地址，就把它填进去就行了。关于为什么要添加尾首部呃，大家不用纠结，反正你只需要记住在计算校验和之前。

会添加12个字节的尾首部一起去进行这个校验和的运算，知道这一点就行了。好，那么添加了为首部之后，接下来就可以计算呃，16比特的校验和那计算的方法和刚才介绍的如出一辙。我们会把尾首部首部还有udp的这个数据部分，反正把所有的东西全部用于校验和的计算，以16比特为一组。啊，依次进行二进制的加法，那再次强调在进行二进制加法的时候最高位，如果产生了进位的话，那么需要回卷到最低位。加回最低位就是这个意思。

好，那么经过这一步处理，最终就得到了16比特的呃加法运算结果。那我们还需要把加法运算的结果逐位取反，取反之后就得到了16比特的校验和。然后把这16比特的校验盒填到udp的首部这个位置。好，那么校验和确定好之后，发送方的传输层就可以把刚才临时添加的这个尾首部给去掉。把它给拆掉，拆掉之后再把整个udp数据报交给网络层，然后让网络层封装成IP数据报呃进行传输。好，这是发送方的传输层要做的事情。比较特别的点就在于计算校验和之前需要先呃添加一个临时的尾首部。

而计算完校验和之后，再把这个尾首部给拆掉，所以这个尾首部实际上并不会在网络层进行传输。好，那接下来看接收方的传输层要如何去进行udp校验？接收方的传输层会收到一个完整的udp数据报。其中包含了首部和数据这样的两个部分，在首部的末尾，16个比特啊，携带了校验盒的信息。那接下来接收方会检查整个udp数据报有没有发生比特错误，显然由于发送方在计算校验和的时候。添加了一个尾首部。所以，接收方在进行差错检测的时候，

也需要把这个尾首部重新给构造出来。那这个为首部当中包含的信息呃和发送方那边构造的为首部是一模一样的。好临时添加了这个尾首部之后，接下来就可以把尾首部，首部还有这个udp的数据部分。把所有的这些部分全部放在一起，进行这个差错的检测，那具体来说就是以16个比特为一组。进行二进制加法。最高位产生的进位需要回卷，那如果最终加法的结果为全一，就说明没有比特错误。于是就可以接收这个udp数据报，并且把udp数据报的数据部分根据目的端口号的信息。交付给啊，

应用层当中的某一个进程。那另一种情况，假如差错检测没有通过，也就是呃，刚才说的那个加法的结果不是全一的话，那么就说明这个udp数据报在传输的过程当中发生了比特错误。此时，接收方的传输层就会直接丢弃这个udp数据包。那它里边携带的这个数据也就不会再交给应用层。好，那么这是接收方的传输层进行udp检验的一个过程。好，那最后二零二四年的真题39题，这个题目啊，大家可以暂停来练练手，

自己动手算一遍，把二进制代进去一个比特，一个比特的去计算。那你算出来的结果是什么呢？显然应该是c选项。其实二四年的39题，这个题目就是我刚才这个例子的来源。所以二四年的这道题目检验和的计算方法和刚才罗列的这个式子如出一辙，如果算错的同学可以结合课件再检查一下是哪个环节出了问题。好的，那么在这个视频中，我们介绍了udp检验的方法，首先我们介绍了一种呃新的差错检验的算法。首先，我们需要用原始数据去计算检验。

和具体来说，就是把原始数据以16比特作为一个组。把各组数据进行二进制加法，那在加法运算的过程当中，如果最高位产生了进位，需要进行回卷。加回到最低位。那加法运算的最终结果，我们再把它逐位取法，就得到了16比特的校验和。那这是校验和的计算，那在检测差错的时候也是类似的方法，接收方会把收到的内容以16比特为一组。同样的也是进行二进制的加法，那如果加法的结果为全一，

就说明没有差错，如果加法的结果不是全一，就说明有差错。那基于这样的呃差错检测算法，我们又补充了udp校验当中的一些要点。udp校验的核心思想和上面介绍的这种呃。差错检测的方法是如出一辙的，只不过在udp校验当中，发送方在计算校验和之前。需要先添加一个临时的尾首部。在计算完校验盒之后，再把尾首部拆掉。那相应的，对于数据的接收方来说，在进行差错检验之前，

也需要添加这个尾首部，让尾首部一起参与呃差错的检验。检验完了之后再拆除为首部。对于数据的接收方，如果说检测出这个比特错误，那么就直接把udp数据报给丢弃。如果说没有比特错误，那么就会接收这个udp数据报。并且根据udp数据报当中的目的端口号去决定这个数据。报当中携带的啊，这些数据应该交给应用层当中的哪个进程？好的，那么这就是udp检验这个考点。那最后我们回顾一下网络层，当我们在介绍IP数据报的时候，

我们说IP数据报的首部这个位置啊。添加了首部校验和。同样也是16个比特。那IP数据报在传输的过程当中，只检查首部当中有没有发生比特错误。那检验的方法其实和这节课介绍的呃udp的这种检验方法如出一辙。具体来说，就是把IP数据报的整个首部，整个首部分别以16比特为一组。进行二进制加法。在进行二进制加法的时候，也需要考虑到回卷这种情况，也就是如果最高位产生进位的话，那么需要呃把最高位的进位加回到最低位。那加和的结果最终在逐位的取反就可以得到IP数据报的首部校验和这16个比特的信息。

注意IP数据报的首部校验和的计算，只有IP数据报的首部。会参与。也就是IP数据报的数据部分，我们是不参与到首部校验和的计算当中的。并且在IP数据报当中计算首部校验和的时候，不会在IP数据报的前面再添加一个尾首部。没有这样的多余步骤。所以IP数据报的这个首部校验和它的呃计算方法本质和udp是一样的，但是背后的细节有一些区别。udp会添加一个尾首部，另外udp的校验范围是包含了首部和整个数据部分。好的，那么大家可以再消化一下。
