好的，在这个视频中，我们会分析电路交换，报文交换和分组交换的数据，传输性能。我们会用这种图来具体分析每一种交换方式，它在每一个时刻发生了什么事情？那在计算机网络这门课当中，这种风格的图示是很常见的。同时，这种风格的图示也有助于我们解决真题。好，所以理解这种风格的图示是很重要的，那我们的课本上也有这三个图，分别对应电路交换，

报文交换和分组交换。那为了让大家对这种图示的理解更直观，更具体一点，所以我在画图的时候呃，给这几个图示添加了这种背景小格子。那纵轴这个方向是时间t增加的方向。每一个小格子呃，我们不妨假设它对应一毫秒的时间。好，接下来我们展开来研究一下这三张图的啊，含义首先来看电路交换，假设现在计算机a。通过中间节点b和c要和计算机d建立连接，建立好连接之后，通过电路交换的方式去传送一个报文的数据。

那整个豹纹的大小，我们不妨设它为4 KB。那这张图纵轴表示的是时间递增的方向，每一个小格子对应一毫秒的时间。另外，横向来看，这张图有abcd这样的四根线。这四条竖线分别对应了abcd这样的四个结点。那我们知道电路交换的第一步需要先建立连接，那么首先a这台计算机需要向b这台呃交换机。发出这种建立连接的请求。那我们知道信号在线路上传输是需要一定的时间消耗的，我们假设这个信号从a点传送到b点。也就是跳到下一个位置，需要一毫秒的时间。

所以你会看到这有根箭头，从a这条竖线指向b这条竖线，意味着这是a发送给b的呃，建立连接的这种请求信号。而这根箭头是斜着往下走的。可以看到，从它的起点到它的终点，刚好中间差了呃，纵向的一个小格，那刚才我们说这样纵向的一个小格代表的是一毫秒的时间。纵轴表示的是时间。好，所以经过一毫秒之后a发出的信号到达b。那b这个电路交换机，它接收到请求连接的这个信号之后。

接下来，它需要在交换机内部建立一条呃通信线路，对吧？那么这条通信线路的建立，也是需要一定的时间消耗的。我们不妨假设这个交换机建立连接的耗时，刚好也是一毫秒，所以你会看到在b这台交换机接收到这个信号之后。他等了一毫秒，才往下一跳，也就是c这边继续发送请求连接的信号。那中间等待的这一个小格子，也就是这一毫秒的时间，就是用来建立。交换机内部的这个连接的。

好，那再往后的过程是类似的，从b到c这台交换机也需要发送一个信号，那这个请求连接的信号也需要一毫秒的时间才可以被c所接收。所以你会看到在这张图上b到c发送了一个信号，并且这根箭头从起点到终点的位置，中间刚好耗费了呃一毫秒的时间。也就是对应我们这的一个纵向的小格子好，那么当c这个呃节点接收到b发来的请求连接信号之后。是不是也是一样的，它也需要在交换机内部建立起啊，这种连接那同样的它建立连接的过程也要耗时一毫秒。所以你会看到c这个节点，它接收到b发来的呃连接请求之后，经过了一毫秒的处理。

也就是对应了这样的一个纵向的小格子。然后才会从c这往d下一个节点发出请求连接的信号。好，那后面的故事就是类似的c到d也需要一毫秒的这种时间。好，那么d接收到这个请求连接的信号之后，它也需要对这个信号做一定的处理。我们不妨假设呃，这个信号最终的接收方处理连接请求需要两毫秒的时间。所以d接收到这个信号之后，你看这儿经过了两个格子，也就是反映了两毫秒的时间。他才会给a发送应答的信号。此时d和a之间已经建立好了呃，这个物理的线路连接，

所以d节点给a节点。返回这种应答信号的时候，它的这个应答信号就可以顺着电路很快的传回到点。那由于这个信号的传播，每一跳传播时延是一毫秒，所以你会看到d结点给a结点。返回这个应答信号的时候。首先，经过了一毫秒的时间，到达了c从c再经过一毫秒的时间，又到达了b从b又经过一毫秒的时间，到达了a。好，那么a这个节点在这个时刻接收到应答信号之后，就说明连接已经建立好了，

对吧？所以从这个时刻开始a这个节点可以开始向d这个节点发送报文。那整个报文的大小是4 KB，而a节点往线路上发送数据的，这种数据传输率。等于每毫秒零点五KB。所以4 KB大小的报文以这种速度发送到线路上，总共需要八毫秒的时间。因此可以看到a结点的这个位置，大家可以数一下啊，总共有八个小格子。一二三四五六七八八个小格子，对应的是八毫秒的时间。g8毫秒的时间之内a一直往b发送报文数据，每毫秒可以发送零点五KB。

那最上面的这条线表示的是发送报文的第一个比特，它呃中间的一个过程。那我们知道一个比特从a传送到b，它中间需要经过一毫秒的传播时间，对吧？而b传到CC传到d中间又需要一毫秒的传播时间。所以你会看到豹纹的第一个比特，从a传送到b这个位置，它刚好呃经过了一毫秒的时间。那在这个图当中，对应了纵向小格子的一格。好，那同样的道理，数据从b发送到c，它中间也经过了一毫秒的时间，

那类似的c到d也是一样的，中间经过一毫秒。所以第一个比特的数据经过了一毫秒，两毫秒，三毫秒之后。从a到达了d。那么，在接下来的八个毫秒之内，数据会不断的从a发送向d？直到第八毫秒最后一个比特，从a发送到d为止。那从报文开始传输的这个时间点算起，一直到整个报文都被d结点所接收。总共消耗的时间就是八毫秒，再加上一二三三毫秒，

也就是共计消耗11毫秒的时间。完成了报文从a到d的传输工作，好那么当a节点发送完整个报文之后。它就可以开始释放连接，那释放连接的过程和建立连接的过程是类似的。a通知b释放连接这个动作也需要经过一毫秒的时间。所以这个箭头从a指向b中间经过了一毫秒的时间。好那么b结点断开中间这条线路，它也需要耗费一毫秒的时间。紧接着b还需要向c发出断开连接的呃，这种信号c又向d发出断开连接的这种信号。那后面这些箭头的含义都是类似的，那这个地方有必要跟大家解释一下呃，当a传输完报文的最后一个比特之后。事实上a结点就可以向b结点c结点和d结点呃发出。

释放连接的这种信号了。所以其实这个箭头它可以呃无限的靠近豹纹的呃底部，这根线只不过如果把这根箭头呃画到这个位置，那可能会和上面这个图发生重合。导致大家看不到这个箭头，所以我就把它们俩之间画出了一些距离。好，那这是用电路交换传送一个报文的过程，在传送报文的整个过程当中，呃，数据是从a结点。直送到d节点。中间经过了一条物理线路，那接下来看豹纹交换的性能和之前的条件一样，我们同样假设一个豹纹的大小是4 KB。

并且数据传输速率是零点五KB每毫秒，所以从一个节点将报文转发到下一节点所需要的耗时。就至少需要八毫秒的时间，那首先报文是从发送到那节点，把报文数据全部。发送到这个线路上，总共需要八毫秒的时间，这儿刚好对应了八个呃，表示时间的小格子。这就是八毫秒的时间，那第一个比特从点传送到点，总共需要一毫秒的时间。这是线路传输呃所需要的一个传播实验，所以数据从a点发出，被b点接收，

中间经过的这个时间就是一毫秒。对应纵向的一个小格子，那经过八毫秒之后a已经把所有的数据都发送到这条线路上了。到第九个毫秒，也就是这个位置b节点就接收到了整个报文的数据。接下来b节点需要对他接收到的报文呃进行处理，要检查报文的控制信息。这样才可以决定爆文的下一跳，应该发送到哪个节点上？那b节点对报文的这个呃处理，我们不妨把它称为存储转发时延，总共需要消耗两毫秒，这是我们假设的一个数据。好，所以可以看到b节点在接收到整个报文之后，

经过了两毫秒的这个呃处理。在这个时刻开始，就可以把报文呃发给下一个节点c。那和之前类似，整个报文的发送需要八毫秒的时间，而报文中的每一个比特数据从b到达c都需要消耗一毫秒的时间。好，那么同样的道理c这个节点在这个时刻接收到了整个报文，那它也需要经过两毫秒的存储转发时延。才可以确定这个报文，接下来应该传送到哪个位置好，所以经过两个格子，也就是一二两毫秒之后。节点就可以把报文发给节点，那整个报文发送的时间也需要经过八毫秒。

并且报文当中的每一个比特，从c点到达d点，都需要经过一毫秒的时间。好，那么我们不妨来数一下呃，使用报文交换这种这种方式从a节点。开始发送数据，一直到d节点接收到所有数据，整个过程总共经过了多少个毫秒呢？大家可以自己数一下，总共经过了29个呃，小格子，也就是经过了29毫秒。好，那这是报文交换的性能分析，

最后我们来看分组交换，我们把一个4 KB的呃报文拆分成。四个大小为1 KB的更小的分组。那么，在数据传输速率不变的情况下，每一个分组被发送到线路上，所需要的时间就只需要两毫秒。每毫秒零点五KB嘛，所以1 KB的分组只需要两毫秒的时间。那来看这个部分a结点，把第一个分组转发给b结点，整个分组的大小是1 KB。把整个分组发送到传输线路上，总共需要消耗两毫秒的时间，也就是对应纵向的两个小格子。

两毫秒的时间。可以把分组一全部发送到线路上，那么分组一的每一个比特从a结点传送到b结点都需要消耗一毫秒的时间。所以我们画这个分组的示意图的时候，它都是斜着往右的，并且它的这个倾斜度刚好对应纵向的一小格，也就是一毫秒的时间。好那么b节点在这个时刻接收到分组一之后，它需要对分组一进行处理。那由于分组的大小比报文更小，所以对分组的存储，转发，处理会比对报文的存储，转发更快一些。我们不妨假设对一个分组的存储，

转发，处理需要消耗零点五毫秒的时间，所以这地方我中间空了零点五个小格子。也就是对应零点五毫秒的呃分组存储转发时延好，那么经过零点五秒之后。b节点就可以把分组一呃发送到下一个节点c这儿，与此同时b节点也可以继续接收。呃来自于a结点的下一个分组。所以a节点给b节点发送这些分组，它中间只需要间隔零点五毫秒，就是确保b节点已经把呃上一个分组。能够处理完，我就可以紧接着给他发下一个分组的数据了。好，那么后面这些分组转发的原理也都是类似的。

对于b节点来说，他给c节点转发这些分组呃，也遵循了同样的规则，用两毫秒的时间把一个分组呃传送到。b和c的这条线路上。然后中间间隔零点五毫秒，零点五毫秒，让c这个节点有时间去处理它刚收到的这个分组。紧接着就可以继续给c节点发送下一个分组。那可以看到，采用分组交换这种方式，完成所有分组，也就是完成整个报文的呃数据传输。从开始到结束，总共花费了十七点五毫秒的时间。

大家可以自己数一下这些小格子。好，那么我们对三种交换方式的呃，数据传输效率进行一个对比。从a开始，数据传送一直到d呃完成，所有数据的接收如果采用电路交换的方式，那么整个过程持续11毫秒。如果采用爆文交换，需要消耗29毫秒，而如果采用分组交换，总共需要消耗十七点五毫秒。因此，完成数据传输所需要的时间是电路交换最少。报文交换最多分组交换要比报文交换更优秀，

但是又呃比电路交换更慢一些。另外，电路交换是呃没有存储转发时间的报文交换，由于报文的信息量大，所以存储转发一个报文。需要的这个呃，时间消耗就会更高，而分组交换需要的存储转发时延呃会更低一些。好，那么在这张图当中呃，给大家总结了这三种交换方式，各个维度的一个对比。大家可以结合这张表格，对这三种交换方式进行一个对比和总结。那在某一个维度的对比当中，

最优秀的一种交换方式，我给他标注了这种戴墨镜很得意的一个表情。最糟糕的呃一个交换方式，给他一个哭脸的表情，然后还不错的呃交换方式，给他一个笑脸微笑的表情。每一行的这些表情符号呃都是相同的含义。好的，在这个视频中，我们对电路交换，报文交换以及分组交换的性能进行了呃对比。主要是通过这种画图的方式来进行分析，那这种风格的图示大家一定要能呃理解，要能看懂。在计算机网络这门课当中，

以后还会出现很多类似这种风格的图示。另一方面，这种画图方法也有助于我们解决某一些真题，某一些课后习题。所以还请大家认真体会啊，这三个图的含义好的，那以上就是这个视频的全部内容。
