好的，继续来看计算机网络的分层结构，在之前的学习中，我们介绍了计算机网络的分层结构以及各层之间的关系。我们把工作在某一层的软件和硬件模块称为这一层的实体，那为了完成这一层的某些功能。同一层的实体和实体之间需要遵循某种通信协议。也就是说，协议这个东西，它是约束了水平方向的呃通信规则，那为了让大家体会到协议的作用。我们不妨从水平的视角来看一下数据是怎么传输的。之前我们简单的介绍过数据传输的过程。从垂直的视角来看，发送方这边的数据会从最顶层开始，

逐层的往下传递。每一层都会对数据进行相应的处理，然后再放到物理传输媒体上啊，进行传输，那在接收方这边。当他收到数据之后，又会从最底层开始进行逐层的逆处理，最终获得原始数据。我们不妨假设应用层需要实现lmn这几个功能，那n这个功能啊，我们不妨假设它是呃，要把数据进行压缩。然后再传输，从而可以节省流量。那基于这样的设计，

发送方的第五层实体需要把数据进行压缩，压缩完了之后再交给下面的层次去处理和传输。而接收方的第五层实体对等的这个实体，他又需要把数据进行解压缩。解压了之后再把它呃上交给上层的用户，因此从水平视角来看。这两个用户，他们感受不到数据被压缩和解压的过程。在他们俩看来，数据似乎是被原模原样的传输的。但事实上，发送方的第五层实体和接收方的第五层实体分别做了压缩和解，压缩的过程。好，那么为了支持这样的功能，

我们不妨制定一个协议，这个协议的名字叫做yscs协议。这是压缩传输的首字母的缩写啊，比较土的一个取名方式。那之前我们说过一个协议，它是规定了水平方向上的两个对等实体。他们之间的通信规则对吧？那么，为了实现压缩传输这个功能，我们需要在协议当中规定。发送方的第五层实体。首先需要把这个数据进行压缩。那数据压缩的算法有很多种，对吧？那为了让这个接收方能够用呃，

对应的这个解压算法去解压，因此我们需要增加一个手部的信息。也就是一部分控制信息。发送方的应用层实体先把数据用某一种压缩算法进行压缩。然后在手部这个位置指明自己采用了哪一种压缩算法，那这样的话，当接收方的第五层实体。收到这个数据之后，它是不是就可以从手部这个位置？去获取压缩算法的信息，紧接着再采用对应的解压算法，把数据进行解压再交，还给呃上层的用户。好，从这个例子中，

我们可以感受到呃，从水平视角来看，对等的两个实体之间。他们的通信需要遵循某种协议。而遵循这个协议的目的，是为了实现某一种功能。那一个协议可能会规定在数据之前增加一个手部。当然，有的协议也可以再增加一个尾部的信息，反正无论是手部还是尾部，这些都属于协议的控制信息。是为了实现协议的功能而存在的控制信息。那具体首部和尾部需要多少个字节以及每个字节具有什么含义，这些都需要在协议当中去给它规定清楚。好，

那理解了这些之后，我们就可以更深刻的体会到为什么要分层以及为什么要制定协议了。我们说计算机网络功能是很复杂的，所以我们需要先把计算机网络需要支持的功能abcdefg先给它呃罗列出来。然后再把这些呃功能安排到合适的层次当中，那处于同一个层次的对等实体。需要去配合着实现这一层当中的呃，这些功能。因此，我们需要去制定协议，去约束对等实体之间的这种通信规则。好，那了解了协议存在的意义之后，接下来我们再从啊垂直的视角再来看一下数据传输的整个过程。假设左边这个用户要给右边这个用户发送一些数据。

那这些数据首先会被交给应用层进行处理，应用层负责实现某一些功能，那为了实现这些功能，应用层和应用层之间的通信。需要遵循某种协议，因此就需要给这些数据增加协议的首部，我们不妨把它称为应用层的首部。好，那么应用层按照协议的规定做了这些处理之后，又会把这一整块的数据作为一个整体，把它交给。下面这一层传输层，那么传输层和传输层之间同样需要遵循某一些呃通信协议，因此传输层也会增加协议，控制信息，

也就是手部的。呃，这部分数据。好，接下来传输层又把这一整块的数据作为一个整体交给下面这一层网络层，那同样的道理，网络层和网络层之间也需要遵循某种通信协议。因此，网络层的实体也需要增加一个手部的信息好。接下来，网络层把这些数据交给数据链路层。那么，数据链路层为了实现本层的功能，它需要增加手部和尾部的信息，无论是手部还是尾部，

它最终都是为实现协议的功能去服务的。好，最后数据链路层再把这些数据交给啊物理层，那在这种五层的体系结构当中，物理层并不会对这些数据进行。任何处理，他只负责把这些二进制的数据原模原样的传输到这个物理传输媒体上。这样就可以让数据到达下一个节点，好下一个节点接收到这些数据之后，首先交给物理层，紧接着物理层再交给数据链路层。那这层的实体会根据本层的协议规定。去检查手部以及尾部的这些信息，然后根据这些控制信息完成数据链路层需要完成的一些事情。比如说完成差错控制的功能。

好，那如果这些数据没有什么问题的话，这层的实体会把本层的首部和尾部拆掉。然后再交给上一层进行处理。那第三层网络层，它通过呃本层的这个协议控制信息去实现本层应该负责的功能。那具体来说，这一层要实现的就是路由，也就数据分组的存储转发，所以第三层的这个首部信息，它其实会包含发送方的IP地址以及接收方的IP地址这些信息。好，那么路由器的第三层实体就可以根据这些信息去决定诶，这个数据分组。下一跳应该把它转发到什么位置？

那路由器的第三层实体会对h3这个部分的呃控制信息进行一些简单的处理。具体是什么处理，我们现在先不展开，总之处理完了之后，他又会把呃这一整块的数据交给下一层，也就是数据链路层。那数据链路层又会根据对等实体之间的这个协议去增加首部和尾部的信息。然后再把这些数据交给物理层，物理层再把这些数据通过物理传输媒体传给下一个节点。那接收方的主机呃，他的第一层物理层收到这些信息之后，就会把这些信息交给数据链路层，那后面的呃，这个过程就是类似的。逐层的往上处理每一层的实体，

只关注本层的这个手部信息，那逐层往上处理逐层的把这些手部信息给拆掉。那最终交给用户的就是一个干净的，没有手部信息的呃一个数据。那这样我们就完成了数据的传输，这就是从垂直视角来看，数据的一个传输过程。同一层次的对等，实体之间，他们需要遵循某一种通信协议，而这个协议本质上是为了某一些功能而服务的。那要让这个协议规定的，这些事情可以落地，我们就需要在啊数据之前增加一个手部的信息。每一层只关心自己的手部信息。

好，那捋清这个过程之后，接下来我们引入几个概念，分别是协议，数据单元，服务数据单元和协议控制信息。英文缩写是pdu sdu和PCI首先看协议数据单元。对等层次之间传送的数据单位。就是这一层的协议数据单元。好比如说第五层和第五层之间，他们传送的数据就是原始数据，加一个h5这个头部对吧？所以这一整块东西就是第五层的协议数据单元。好类似的第三层和第三层之间，它们传送的数据是这一整块对吧？

所以这一整块就是第三层的。协议数据单元。那协议数据单元由服务数据单元和协议控制信息组成。协议控制信息指的是控制协议操作的信息呃，说白了就是刚才我们呃提到的这个手部。第五层的协议控制信息，也就是第五层的PCI，就是第五层的首部。那第四层的PCI就是第四层的手部。那第二层的PCI包含了手部和尾部。两部分的这个信息，所以首部和尾部加起来，共同组成了第二层的PCI，也就是协议控制信息。那去除协议控制信息呃之外，

剩下的这个部分就是本层的服务数据单元。也就是sdu这个概念。服务数据单元是为了完成上一层实体所要求的功能而传送的数据。这句话读起来比较拗口啊，但是呃，通过之前的讲解，相信不难理解好，那没有跟上的同学，我在这儿给大家罗列了一些例子。大家可以暂停来感受一下，总之这三个概念之间的关系就是。某一层的呃服务数据单元，加上这一层的协议控制信息，共同组成了这一层的协议数据单元。而这一层的协议数据单元，

又会作为下一层的呃服务数据单元。大家可以暂停来体会一下。好，最后我们再来介绍协议的三要素。我们要制定一个完整的协议，那需要规定这个协议的语法语义和同步分别是什么？这就是所谓的协议三要素。那首先，第一个要素所谓的语法指的是啊，数据与控制信息的格式。比如说我们需要规定这个协议的控制信息，也就首部哎，总共几个字节，每个字节的含义是什么，以及协议的数据部分。

也就是sdu那个部分最多有多少个字节？这就是所谓的语法要规定数据的格式。那第二个要素语义指的是需要发出何种控制信息，完成何种动作以及做出何种应答，听起来比较抽象啊。举个例子啊，比如刚才我们提到压缩传输的功能AB两台主机的第五层应用层的实体，要实现压缩传输功能。那么，他们之间就需要遵循压缩传输协议，除了规定，这个协议的数据控制信息格式之外。我们还需要规定，在双方进行数据传输的过程当中，双方需要完成哪些动作以及做出哪些应答。

比如说当发送方发完数据之后。接收方是否需要给他一个应答的信号，以及这个应答的信号有哪些种类？比如说呃，我给他应答一个传输成功，或者给他应答一个传输失败，那这就是所谓的语义要描述清楚的东西。好，最后第三个要素叫做同步，又叫做时序。在历年真题当中，呃出现的是时序这个术语。那时序这个要素需要描述清楚，通信双方呃执行各种操作的条件，时序关系等等。

是对事件实现顺序的详细说明。比如说AB双方要遵循压缩传输的这个协议，那么这个协议当中我们要求。发送方首先要给接收方发送数据，那么接收方接收完数据之后。需要立即给发送方一个回应，比如说传输成功，当然也可以回应传输失败，对吧？那如果发送方在十秒之内。十秒之内还没有收到这个接收方的呃，传输成功应答的话，发送方会再次尝试。重发重新发送这个呃文件的数据。所以我们用这样的一个时序图去描述清楚了发送方和接收方，

他们的这些动作之间的时序关系。好所以语义这个要素是描述了发送方和接收方有可能需要做的动作有哪些？而时序这个要素是需要描述清楚这些动作的发声顺序。那这三个要素相辅相成，共同的组成了协议。那在这门课的后续章节中，我们还会详细学习很多种协议。那学完整门课之后再回来看这几个概念，相信大家也会有更清晰的认知。如果现在没有办法完全理解协议三要素的含义，那啊，这很正常，等大家学完整门课再回来看这个部分会觉得很简单。好的，在这两个视频中，

我们介绍了计算机网络的分层结构。我们说计算机网络的功能十分复杂。为了让计算机网络看起来呃结构清晰，并且易于维护，易于实现，因此我们引入了分层设计的思想。把计算机网络分成多个层次，那每一个层次负责实现呃若干个功能。那对于同一层次中的活动元素，我们把它称为这一层的实体，我们可以把处于同一层次的实体称为对等实体。那为了实现一个层次中的各种功能对等，实体之间需要根据某种协议的规定。去进行通信，所以协议规定的是水平方向对等，

实体之间的这种呃通信的规则。在一个分层结构的计算机网络当中。上一层的实体会通过接口去请求下一层实体的服务，而所谓的服务就是呃，为上一层提供的功能调用。所以接口和服务这两个概念是垂直方向的，它描述的是相邻的上层和相邻的下一层之间呃发生的一个交互关系。那服务可以按照不同的维度进行分类，比如可靠的，不可靠的，有连接的，没有连接的。以及有响应的和没有响应的，那关于服务的分类，在初学这个阶段可以先不了解，

随着这门课后续章节的学习。我们对服务的分类会有更直观的认知。那在这节课当中，我们也介绍了协议，数据单元，服务数据单元和协议控制信息这几个概念。处于同一层的对等实体之间，以协议数据单元为单位呃去交换数据。那协议数据单元由服务数据单元和协议控制信息组成，所谓的协议控制信息就是呃首部和尾部。而服务数据单元就是上一层的实体委托，给下一层的实体传输的呃。那部分数据。而某一层的协议数据单元，又会作为下一层的服务数据单元。

那这几个概念比较容易混淆，建议大家看图复习，反而会更清晰。好，那在这节课中，我们还介绍了协议的三要素，分别是语法，语义和时序。语法描述了呃协议的。数据与控制信息的格式，也就是描述了一个协议的sdu和PCI的格式。那协议的第二个要素语义描述了呃使用协议的这些对等实体之间需要发出什么样的控制信息以及需要完成什么样的动作，做出什么样的回应。简言之，语意描述了对等实体之间需要做什么。

而最后一个要素时序，它需要描述。这些对等实体做的这些动作。触发条件是什么？以及先后顺序是什么？也就是需要描述清楚什么时候做。那由于到目前为止，我们暂时还没有接触任何一个具体的协议，所以这几个概念现在听起来比较抽象是很正常的。通过这门课，后续章节的学习对这几个概念的理解会更加深刻好，那么在上一节课当中，我们也介绍了呃计算机网络体系结构的概念。当我们要设计计算机网络体系结构的时候，我们需要精确的定义，

层次的划分以及各层的功能。各层的协议等等，只需要给出精确的定义就可以，而不需要涉及到具体的实现。相当于我们设计一个体系结构，只是给计算机网络的软硬件开发者搭建了一个框架。开发者们只需要按照体系结构的这些定义去进行开发，那么你开发的软件和硬件就可以在呃我这个网络上去正常的运行。好，所以具体的实现这个问题是交给软硬件开发者的。体系结构的设计者只需要给出定义就可以，那在上节课中我们也简单介绍了三种常见的呃网络体系结构，分别是osi参考模型TCP IP模型以及我们教学使用的。五层模型。osi参考模型是法律标准，

是由一个国际标准化组织提出来的，但是现在的互联网世界。网络的体系结构是TCP IP模型，也就是分为了四层，所以TCP IP模型它是事实上的标准。在下节课中，我们会展开介绍osi参考模型和TCP IP模型，而在这门课程的呃后续几个章节中，我们会更详细的去研究五层模型的各个层次。好的，那以上就是这个视频的全部内容。
