好的在这个视频中我们要探讨tcp ip模型，那对于tcp ip模型考试的要求是这样的首先我们需要记住它各个层次的名称和顺序，以及我们需要了解tcp ip模型和osi参考模型它们在设计理念上有哪些区别，那设计理念的区别又导致了tcp ip模型和osi参考模型的各个层次之间功能上也会有一些区别，那接下来我们会依次展开探讨这些问题好首先我们尝试记住tcp ip模型的各个层次，tcp ip模型总共只有四个层次第一层叫做网络接口层它的作用类似于osi参考模型的第一层和第二层，好接下来tcp ip模型的第二层叫做网络层它的作用和osi参考模型的网络层是类似的，紧接着第三层呃传输层它和osi参考模型的传输层也非常类似，那最顶层是应用层这一层的作用相当于osi参考模型的会话层表示层以及应用层，从第一层到第四层我们分别标红一个字把这几个字连起来接网输用，那我们不妨书接上回上回说到一个无助的年轻人问了一句物联网谁会使用，此时人群中出现了一个大叔他说物联网书会使用，紧接着年轻人又问了大叔这样的一个问题叫做书怎么用大叔回答了一句，接网输用意思就是说只要接上网络那书就可以使用这个物联网，那接网输用这四个字分别对应了网络接口层网络层，

传输层和应用层，好这是tcp ip模型的四个层次好接下来我们要探讨tcp ip模型和osi参考模型在设计理念上有哪些区别，那首先我们来回顾osi参考模型的五六七这三个层次，重点关注第五层和第六层，我们知道在计算机网络的分层结构当中，上面一层会使用下一层提供的服务，或者说会使用下一层已经实现的功能那么在osi参考模型中应用层会使用表示层，的功能也就是数据格式转换而表示层又会使用会话层的功能也就是会话管理，之前的视频中我们说过绘画管理的效果类似于文件的断点续传好那么现在思考这样一个问题，会话管理的功能和数据格式转换的功能对于每一个应用程序都是必要的吗？显然不是对吧比如说我一个应用给另一个应用呃传文件，那如果左边这台机器它的数据编码格式和右边这台机器数据编码格式是完全一样的，那数据格式转换的功能是不是就没有必要存在了对吧，好另一方面如果传文件的过程当中网络出现了问题也就是文件的传输啊可能在中间某个点被中断了，那么当网络重新恢复的时候我们必须断点续传吗？

也不一定对吧事实上我们也可以选择在网络恢复的时候重新传整个文件这也是ok的也是一种可行的解决方案，好所以绘画管理这个功能对于有些应用程序来说它也是可有可无的，好那么既然对于某些应用程序来说数据格式转换会话管理这些功能可有可无的话，那表示层和绘画层这两个层次是不是就也没有必要呃非得让它存在，如果我们去掉这两个层次其实可以让整个网络体系结构更简洁，层次越少其实效率也会越高，好所以这就是tcp ip模型和osi参考模型在设计上的第一个呃很大的不一样，他把表示层和会话层给干掉了，因为表示层和会话层所提供的这些功能并不是所有的网络应用都需要的，所以这两个层次不需要存在，那如果说某些应用程序它确实需要数据格式转换会话管理这些比较个性化的功能怎么办呢，很简单我们只需要让呃这些比较个性的功能啊用应用层的某些协议去实现就可以了，假设微信和微信之间的通信需要使用会话管理的功能那么这两个应用程序，在通信的时候使用包含会话管理功能的协议就可以，那相反的如果不需要使用会话管理功能我们就换一个不包含会话管理的协议，

也就是说我们把这两种功能放在应用层去实现那么应用程序对于这两种功能是，使用与否的选择是非常灵活的，那相比之下在osi参考模型中一个应用程序不管你是否需要使用数据格式转换是否需要使用会话管理的功能它一定都会经过表示层和会话层的处理，对于这两个层次功能的选择是不灵活的，你非选不可，好所以tcp ip模型的应用层在功能上其实包含了osi参考模型的表示层，以及会话层，只不过tcp ip模型的应用层在实际使用的时候呃功能的选择会更灵活，好接下来我们再来回顾osi参考模型的第一层和第二层，第一层物理层呃需要去定义电路接口的参数比如说网线的接口长什么样子光纤的接口长什么样子是什么尺寸，以及电信号光信号分别是什么含义等等这些东西都需要在物理层中去进行说明，另外数据链路层呃需要实现差错控制流量控制的功能，也就是说osi参考模型对于第一层要干什么第二层要干什么分别需要支持哪些功能，具有哪些特性都需要有清晰明确的呃规定，那这就意味着呃一个网络硬件厂商他在研发物理层和数据链路层的设备的时候，

必须遵循osi参考模型的规定对吧，从某种意义上来讲osi参考模型对于第一层第二层的规定太死反而会导致这些网络硬件厂商，厂商在进行设备研发的时候其实更不灵活我本来可以有更多的创新但是为了贴合你这个参考模型的要求，我的创新被限制了，好所以tcp ip模型在呃底层的这个部分它有不一样的这种设计思路，tcp ip模型认为呃网络硬件的种类很繁多并且这个通信技术其实日新月异，可能每隔几年就会有更高性能的设备呃更先进的这种通信技术出现所以tcp ip模型认为呃在网络层之下的这些部分，到底如何实现相邻的两个节点之间这种呃比特流的传输，这个其实不应该有太多的限制那这是tcp ip模型的一个呃理念，那基于这样的理念tcp ip模型在网络层之下啊只有一层叫做网络接口层，tcp ip模型只规定这个网络接口层的任务是什么就是要实现相邻节点之间的这种数据传输，那如果从垂直视角来看就是为网络层传输分组网络层会把分组交给这个网络接口层，那网络接口层可以用比较灵活多样的方式去传输这个分组的数据那具体是怎么传输的，我不做特别的规定，那这样的设计思想使得tcp ip呃体系结构拥有更强的灵活性和适应性，

每一个网络设备商都可以去发挥自己的呃创造力，去研发更先进的更好的网络接口层技术，只要我研发的这个通信设备通信技术能够和呃网络层正确的交互就可以了，好我们做一个阶段性的小总结osi参考模型的第五层第六层会话层和表示层，主要的功能是会话管理和数据管理因为这两个功能并不是所有的应用都需要的，所以这两个层次可以直接把它干掉tcp ip模型会在应用层去灵活的呃支持数据格式转换和会话管理这些，呃比较个性化的功能，好另一方面osi参考模型的第一层物理层和数据链路层详细的规定了这两个层次需要支持哪些功能，那规定的太多反而会导致呃，物理层和数据链路层的这个设备厂商他们在生产研发设备的时候其实不太好发挥，有很多限制那相比之下tcp ip模型的网络接口层它在功能上虽然类似于物理层和数据链路层，但是tcp ip模型对于网络接口层要实现哪些功能需要支持哪些协议并没有做具体的规定，而是交给网络设备商去自由的发挥那这也使得tcp ip模型能够被更多的设备厂商所支持，最终也使得tcp ip模型在商业上取得了巨大的成功，好那到目前为止我们探讨的是这两种模型在呃层次结构上的明显区别，

那现在我们关注中间的两个层次可以看到两种模型都有网络层网络层之上也都是传输层，那么这些层次之间又有哪些设计理念上的区别呢我们不妨先回顾一下osi参考模型的网络层和传输层，分别需要实现哪些功能首先osi的网络层和传输层都需要实现，差错控制流量控制连接管理和可靠传输管理这些功能，只不过网络层针对的是数据报而传输层针对的是报文段，另外数据链路层也需要支持差错控制和流量控制的功能只不过数据链路层，它针对的是针，那之前的视频中我们说过呃各个层次传输数据的单位是不一样的，越上面的层次它每一次传输的这个数据单位呃也会更长比如说应用层以报文为单位，而报文交给这个传输层之后传输层会把报文拆分成多个报文段，当然如果豹纹很小的话也可以不拆分，好那类似的当传输层呃把报文段交给网络层之后网络层会把呃比较长的报文段，拆分成多个数据包，或者说拆分成多个分组，那类似的网络层把一个分组交给数据链路层之后数据链路层又会把比较长的分组拆分成多个帧，

也就是说越靠下的这些层次它传输数据的单位是越局部的，而越靠上面的层次它传输的数据是越接近全局的，那显然数据的局部正确不可以推出数据的全局正确也就是说数据链路层它只能确保每一个帧的传输是没问题的，但是它无法确保每一个分组的传输是没有问题的，因此osi参考模型认为在网络层这儿又需要对分组进行差错控制流量控制，以此去保证分组的数据传输没有问题，那同样的道理网络层保证了分组这个局部数据的正确性但是它无法保证报文段这个全局数据的正确性，因此在传输层这osi又会再一次呃进行差错控制流量控制以确保报文段的传输是没有问题的，那连接管理和可靠传输这两个功能也是类似的道理就是说网络层这儿可以确保数据分组的呃传输，有序不重复不丢失但是不意味着报文这个全局数据的传输有序不重复不丢失，所以网络层这儿实现过的功能在传输层这儿又需要再重新去支持重新去实现，好这是osi参考模型的设计思路哈，数据的局部正确不可以保证数据全局正确，那我们不妨换一个思路如果我们能够确保数据的全局是正确的那是否可以通过，推出数据的局部也是正确呢，

显然这个逻辑也是成立的，也就是说事实上下面这几个层次它可以不管呃自己的这个呃数据传输到底有没有出现错误可不可靠，反正最终都让这个传输层以报文段为单位去确保数据的传输是没有出错的，是有序的是可靠的那就ok了，因为只要我们检测出数据全局正确那么数据的局部肯定也是正确的对吧好所以基于这样的思路，tcp ip模型的网络层它去除掉了差错控制流量控制呃连接的管理以及可靠传输管理，它去除掉了这些呃功能，那网络层去除掉这些功能之后就会使得网络层的这个数据传输变得不再可靠，比如说现在传输层把一个报文段交给网络层呃让它去传输那网络层会把报文段拆分成，若干个分组然后再把分组交给网络接口层去传输那网络接口层的数据传输是不可靠的，也就说一个分组从a结点传输到相邻的b结点，在这个传输的过程当中分组的数据就有可能出现错误了对吧，那这个中间节点的网络层接收到一个分组之后，即便这个分组有可能出错，甚至分组有可能会丢失但是这都没有关系反正我这个中间节点接收到一个分组之后，

我把它尽最大努力转交给下一个节点就可以了，所以最终这个目的节点它接收到的这些分组序列有可能是有错误的也有可能是失去的，那这些分组最终会被合并成呃报文段再转交给上层的这个传输层，只不过刚才我们说由于中间的这个传输过程网络层并没有进行差错控制流量控制连接管理还有可靠传输管理，因此最终这个目的结点收到的报文段就有可能是有问题的，所以tcp ip模型的网络层它的理念就是呃主打一个摆烂我只尽我最大的能力去交付这些分组去传输这些分组，数据的传输我知道是不可靠的但是我就是摆烂，好那摆烂的结果就是目的节点的传输层他收到的报文段数据有可能是错的当然也有可能是对的，那接下来目的节点的传输层就会对这个报文段进行差错控制，流量控制连接管理可靠传输管理，反正就是增加这些功能去确保报文段的传输这个全局数据的传输是没有问题的，所以tcp ip模型它并不是完全不管这个数据传输的正确性可靠性这些东西，只不过这些任务都会由传输层来完成，那相比之下osi参考模型的网络层也需要实现差错控制流量控制吧啦吧啦这些乱七八糟的功能，那这些额外的功能就会使得网络的核心部分，

这些路由器它需要承载更多的事情功能变得更复杂负载变得更高造价也会变得更高，而tcp ip模型它的网络层功能相对简单那这也使得网络的核心部分这些路由器，它的呃负载变得更低功能变得更简单造价也可以变得更低，那刚才也说了呃tcp ip模型的这个传输层会负责保证数据的正确性有序性可靠性等等这些方面，也就是说数据传输的正确性可靠性的保障，是交给了这个呃网络的边缘部分交给了这些主机，所以相当于把网络核心部分的压力降低了然后把压力给到网络的边缘部分这些主机那这就会使得网络的核心部分这些路由器它，传输分组的整体性能可以得到大幅度的提升因为它不需要去管什么插错流量这些乱七八糟的东西嘛对吧，好所以tcp ip模型和osi参考模型在网络层这个层次啊设计理念上是有很大的区别的，好的那么在这个视频中我们介绍了tcp ip模型tcp ip相比于osi参考模型来说层次更少也更简洁，在这页ppt当中总结了tcp ip的一二三四四个层次分别需要完成什么样的任务以及各个层次具体需要实现哪些功能，那这儿标蓝字的这些部分是tcp ip模型和osi参考模型有不同的地方，注意在网络层这个地方差错控制流量控制连接管理可靠传输管理这几个功能点都是画了删除线的，这就意味着在osi参考模型中这几个功能是存在于网络层当中的但是在tcp ip模型当中这几个功能，从网络层当中删除了，

我们在这个视频中也简单介绍了osi和tcp ip它们的层次结构有什么区别osi参考模型的会话层表示层和应用层，在tcp ip模型当中对应应用层，而osi参考模型的物理层和数据链路层的功能对应了tcp ip模型的网络接口层，另外在这个视频中我们也详细介绍了osi参考模型和tcp ip模型网络层的一个呃设计理念区别，osi参考模型要求网络层必须实现插座控制流量控制连接管理还有可靠传输管理这些功能，而tcp ip模型的网络层不需要支持这几个功能那也正是因为这个差别，在osi参考模型中网络层可以向呃上层也就是向传输层提供，提供有连接可靠的服务以及无连接不可靠的服务，之前我们提到过分组交换或者说数据报交换呃其实就是无连接不可靠的服务，网络层在传输分组的时候不确保分组有序到达也不保证分组不丢失，所以当不使用网络层的这些功能的时候它所能提供的就是数据报服务是无连接不可靠的，而如果网络层的这些功能全部被启用那么它就可以为上层传输层呃提供有连接可靠的服务，也就是虚电路交换的服务，在虚电路交换中可以保证分组是有序到达的无差错的以及不丢失的，好那再看tcp ip的网络层由于它不支持差速控制流量控制这些乱七八糟的东西所以tcp ip，

呃它的网络层只能向上层提供无连接不可靠的服务，也就是不保证分组的正确性有序性，好另外在tcp ip模型中呃数据传输的可靠性是由传输层去呃实现的，如果传输层使用tcp协议，那么就可以保证数据的传输是有连接的可靠的也就是说数据的正确性有序性等等呃都可以得到保证，那当然tcp ip模型的传输层也可以向上层应用层去提供无连接不可靠的服务，如果使用udp协议去传输数据的话那么数据的传输就是无连接不可靠的，那关于tcp和udp的呃具体内容我们会在之后的章节中进行更详细的探讨这儿大家先有个印象就可以，事实上有的应用程序呃进行网络通信的时候，是可以允许数据的传输不可靠这件事情的，比如说当我们看直播的时候我们可能对直播的呃流畅性要求很高但是直播的画面丢失了几个帧，或者直播的这个声音啊停了几秒钟这个其实是可以忍受的对吧所以在现在的网络应用当中，无连接不可靠的这种数据传输服务也是有必要存在的那相比之下osi参考模型是大概是上个世纪八十年代提出来的，在那个年代其实并没有预见网络世界能够发展的如此迅速应用如此广泛，所以那个年代的呃设计者认为传输层只能向上层提供有连接的可靠的这个数据传输服务，

也就是说数据传输出现一些错误或者出现丢失是不被允许的是不可以容忍的，那这也是osi的传输层和tcp ip传输层的一个区别，在考试中这两种体系结构的网络层和传输层是最喜欢放在一起来考察的所以大家对这页ppt的总结呃也需要有个印象，随着这门课后续章节的学习大家对于这些功能到底有什么作用以及是如何确保数据传输的正确可靠的，会有更直观的理解，所以在当前阶段看到这些什么有连接的可靠的服务无连接不可靠的服务看见这些东西感觉比较模糊是很正常的，请不用担心好的那么以上就是这小节的全部内容，
