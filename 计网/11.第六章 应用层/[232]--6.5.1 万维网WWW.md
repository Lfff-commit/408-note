各位同学大家好，接下来让我们进入四零八的最后一个小节，万维网在大纲当中处于这个位置。这个部分理解起来是有难度的啊，特别是对于跨考的同学以及没有做过web开发的同学来说，可能这个小节学起来会比较抽象。所以，为了降低大家理解的门槛，我给大家制作了一个啊网站的实例啊，我们先用这个实例来感受，平时我们去访问的那些网站页面。它的背后到底包含了哪些东西？我们先把感性的认知建立起来，然后再基于你的这种感性认知去建立理论体系，这样你的吸收会更好，

而且也可以知道它背后到底发生了什么。在发布这个视频之前，我已经让我的同事帮我把这样的一个压缩包上传到了大家的那个课件啊，网盘里边。建议大家把这个压缩包下载下来，下载到你的电脑上，然后解压，解压之后你会看到这样的一些东西。首先，最外层的这个文件夹叫做网站根目录，下面有一个肯德基点png这样的图片。然后还有一个啊点MP 4的小视频。另外还有一个二级目录，里边存了一张麦当劳的图片。那大家可以双击打开这个默认首页点HTML，

打开之后你就会看到这样的一个呃网站页面。那在这个简单的网页里边，你会看到一些文字信息，会看到两张图片啊，还有这种。就是一个文本框。你可以在里边输入数据。以及还包含了这种呃视频视频的信息。这就是一个很简单的网页。所以我们可以得出这样的结论，一个网页由一个HTML文件，再加上n个网页元素组成，那所谓的网页元素就是像图片，视频。还有就是音频文件等等。

在这个HTML文件当中，会说明啊，网页当中需要引用的这些元素，或者说需要引用的这些文件。它的存储路径是什么位置？一会儿我们会看到背后的过程。那我们的浏览器会根据HTML文件以及其中引用的这些网页元素。把它渲染成最终你能看到的这个图像的样子。于是你就可以看到一个内容丰富的网页。那接下来我们来探究一下这个HTML文件内部长什么样子，大家可以右键选择呃打开方式。然后WINDOWS用户用记事本打开mac用户的话，用那个文本编辑器去打开，这样的话你就能看到HTML文件背后长什么样子。好，

那不方便操作的同学也可以直接看我这张图，打开之后就是长这样可以看到HTML文件的背后，本质上就是一些字符信息。只不过这些字符，它有特殊的格式要求。比如说你会看到这个网页当中标题一封求助信，这几个文字，它的大小字体是不一样的，对吧？那观察在这个HTML当中呃，这段文字我们是用呃h1这样的尖括号，然后反尖括号用这样的标记。去把它框起来了，那这一对标记它的作用就是告诉你的浏览器。在渲染这些信息的时候。

把它渲染成一个一级标题。用比较大的字体以及粗体去表示，那这是这两个标签，它的一个作用啊，那这两个标签之后。正文这儿这段文字我没有套任何的标签，对吧？也就是说浏览器在显示这段文字的时候不需要做任何特殊的处理。所以你会看到这儿就有一个呃比较正常的字体。好，再往后下面这儿写了一个尖括号br，这个标签对应的是换行符。如果我没有写这个标签的话，那么呃，下面这张图片它会被渲染到这段文字的后面。

就不会有这种换行的效果。再来看这个标签IMG的意思是image图片图像。这个标签是在告诉浏览器在这个位置处，需要显示一张image，一张图片。这张图片它的宽度是300个像素，它的高度也是300个像素。然后图片的来源是来自于同一个呃目录下的肯德基点png。只要指明了文件的存储路径，浏览器就可以知道去什么位置，找到这个图片文件。然后再往后这儿有一个label，一个标签，还有一个input输入框。这两个东西组合起来显示的效果就是这个样子，

好心人请输入你的文字，后面这个位置你可以输入一些内容。那在这两个元素之后，我们又可以看到一个视频文件。那视频文件对应的就是这个标签video，这个标签就是告诉浏览器在这个位置，我要显示一个视频。这个视频的宽度是300，像素高度要显示为400像素。然后在这个位置，我们又说明了这个视频存储在什么位置，文件名叫什么，以及这个视频的格式是什么？那最终显示的效果就是这个样子。好，

那么在视频之后又有一个换行，又插入了一段文字，在这个文字之后又有一个换行。最后这个位置在结尾处，我们又引用了一张呃图片，这个图片的存储路径是呃HTML文件的呃同一级目录。下面的MC这个文件夹，下面的麦当劳点png。你看和HTML文件同一级的这个目录，下面有个MC文件夹，对吧？它的里边又存了麦当劳点png。好，现在再回头来感受我们说浏览器打开一个网页，本质上就是先打开一个HTML文件。

然后根据这个HTML文件当中包含的这些文字信息以及引用的这些网页元素。它的存储路径等等，这些信息去渲染出啊，我们用户看到的图像，最终显示在用户的眼前。好，那刚才我们看到的例子是打开了一个本地网页，那如果我们的浏览器打开的不是本地网页。而是互联网上的其他网页，那背后的原理也是类似的，比如说当你用浏览器打开3w点d哩d哩点com。打开这个网页的时候。本质上，在背后浏览器做的事情，就是去找到这个网页所属的WEB服务器。

然后从那个WEB服务器那边。下载这个网页的HTML以及下载HTML当中引用的所有的这些文件，这些数据。把它下载回本地之后，再进行渲染。最终就把这个页面呈现给了用户。那接下来我会用一个综合的例子带大家弄清楚，当你在访问一个网站的时候，背后发生了什么？捋清楚这个过程，你就能解决大题以及计算题。假设闲鱼做了一个很知名的网站，叫3w点d哩d哩点com。我把这个网站相关的程序，还有数据都部署在了一台WEB服务器上，

这是服务器的IP地址。我会通过一个WEB服务器进程，对外提供网站服务，这个进程绑定的TCP端口号是80。好与此同时，在互联网的另一端，假设这是你的主机，你的主机从属于你们学校的这个局域网。这是你的IP地址，你的子网掩码是17个比特的一。当你的这台主机刚接入学校的局域网的时候啊，会通过dhcp协议动态的获取一个啊IP地址。以及配置一个子网掩码，还有dhcp协议也会给你配置默认网关。另外，

同样还是dhcp协议还会给你的这个主机配置啊，你们学校的这个DNS服务器的地址。这些参数的配置都是通过dhcp服务器在背后默默的替你完成的好，那么接下来。假设你要用你的chrome浏览器去访问我的网站3w点d哩d哩点com。当你输入网址并且敲下回车键之后，接下来你的浏览器会通过DNS服务器先去查询。你所输入的这个域名，这个域名对应的IP地址等于多少？那么根据你的这台主机呃，里边配置好的这个DNS服务器的地址。你的这个浏览器进程会构造一个DNS请求报文。其中要说明清楚，你要查询的这个域名，或者说这个网址到底是多少？

那么这个DNS请求报文。又会被包装成一个udp数据报。udp数据报又会被网络层包装成一个IP数据报，那么在IP数据报当中会指明原地址是你的这台主机的地址。而目的IP地址是你的主机内部配置好的，这个DNS服务器的地址也就是幺二八点九八。好，那么根据这个目的IP地址，最终你的IP数据报会被这台啊本地域名服务器所接收。这台本地域名服务器拆掉IP数据报的首部得到了udp数据报检查。udp数据报的目的端口号。目的端口号应该填写的是53，这个端口号就是呃DNS服务器专用的，所以你的这个udp数据报的目的端口号会固定的写为53。那这样的话，

这台DNS服务器就会顺利的收到这个udp数据报，那在从里边拆出DNS请求报文之后，它就能够知道。你查询的网站这个域名到底是多少？于是这台本地域名服务器会检查自己的缓存。如果在自己的缓存中能够找到这个网站对应的IP地址，那么它就可以把呃这个查询结果。包装成一个DNS响应报文。然后再经过传输层的udp协议封装，最后再经过网络层的IP协议封装，反正就是呃逐层的封装。封装好了之后，这个东西最终就会发回到你的这个浏览器进程好，现在你的浏览器进程收到这个DNS响应报文之后，它就能够知道。

自己刚才查的这个网址，所对应的IP地址等于多少？好，所以当我们访问一个网站的时候，其实第一步应该是DNS，也就是要先查到这个域名所对应的那台呃服务器，它的IP地址等于多少？那一旦查到这个结果之后，你的浏览器会把这个查询结果缓存在本地，那这么做的好处就是呃，如果近期还要呃继续访问这个网站的话。就不用再进行DNS查询了，好，总之现在你的浏览器已经知道了这个网址。对应的服务器，

它的IP地址等于多少？接下来，为了和这个网站服务器进行通信，你的浏览器进程会和这个网站服务器上面的WEB服务器进程。也就是TCP的八零端口号建立起TCP连接，这个八零端口号是WEB服务器呃专用的一个熟知端口。也就是说，全世界的程序员都有这样的一个共同约定啊，只要是WEB服务器进程，那么我默认绑定的是八零端口。好，所以你的这个浏览器刚才通过DNS查到IP地址之后，它就可以用这个IP地址，还有八零端口。去尝试建立TCP连接，

对吧？那通过之前学习，我们知道TCP连接的建立需要经历三次握手，对吧？其中第三次握手就可以开始携带客户端啊，想要传输过去的数据。那么，你的浏览器进程会在握手三当中携带一个HTTP请求报文。好来观察这张图。这是一个http，请求报文。ht TV协议规定了两种数据结构，一种就是请求报文，另一种叫做响应报文。那无论是请求报文还是响应报文，

它在传输层都会使用到TCP协议的服务，也就是会在传输层封装成一个TCP报文段。那回到刚才说的握手三握手三这个TCP报文段就可以携带HTTP请求报文。那在这个请求报文当中，需要说明我现在想要访问的是哪个HTML页面？比如说可以这么告诉对方呃，我这个浏览器现在想要访问的是你呃d哩d哩这台服务器上面的。默认首页点HTML。在握手三当中告诉服务器，这个信息好，那服务器收到这个HTTP请求之后。它就可以从中看到你想要的东西是什么，于是这台服务器会在这个网站根目录下面。找到默认首页点HTML，找到这个文件，

然后把这个文件。封装到一个http响应报文当中，于是这个http响应报文就携带着HTML文件。最终，通过TCP协议给你的进程返回了。好，现在你的这个浏览器进程收到了，这个响应报文之后就可以从中找到这个点HTML文件。那刚才我们说这个文件，它的内部就是长这个样子。其实，对于浏览器来说，这个东西就是一些字符信息，对吧？那浏览器会解析这个HTML文件，

他发现在这个HTML文件当中还引用了呃网站的根目录，下面的肯德基。这张图片以及小视频点MP 4这个文件。另外，还引用了这个网站根目录，下面的MC文件夹，下面的这个麦当劳点png。好，所以接下来就是啊，获取这个HTML当中引用的其他n个元素。那n个元素的获取需要通过n组HTTP请求和响应来完成。像刚才这个例子当中HTML当中引用了一二三三个网页元素，对吧？所以接下来我们就需要三组HTTP请求和响应，才可以从服务器这边获得这三个东西。

那在这个过程当中，根据http的工作方式，到底是持续连接还是非持续连接？以及如果是持续连接的话，到底采用非流水线方式还是流水线方式，会有一些区别。那区别在哪呢？给大家画了一个对比图。最左边这张图是非持续连接的工作方式，中间这张图是持续连接非流水线的工作方式。右边这张图是持续连接流水线的工作方式。好，我们先来看，非持续连接的这种呃http工作方式。之前我们已经探讨了握手一二三以及第一个http响应相关的过程，

其中握手三这个TCP报文段当中。携带了第一个HTTP请求报文，在这个报文当中，向WEB服务器指明了。我这个客户端进程想要get获取啊，3w点d哩d哩点com。这个网站根目录下面的默认首页点HTML。于是服务器这边通过http响应报文，把这个默认首页点HTML这个文件给携带回去了。那浏览器进程在收到这个HTML文件之后啊，去解析里边的内容发现还需要引用啊。肯德基小视频，还有MC下面的麦当劳点png需要引用这样的三个元素。那如果HTTP协议工作在这个非持续连接这种模式之下，那么就意味着在获得了第一个HTML文件之后。

就会迅速的释放连接。把前边已经建立好的这个TCP连接给断掉，紧接着又进行第二次的TCP连接建立。通过第二次TCP连接，建立去获得下一个元素，也就是肯德基点png。好，这个元素获得了之后，第二个TCP连接又会被迅速的释放。然后再建立第三次TCP连接，在第三次TCP连接当中。又获得了下一个元素小视频点MP 4。好获得元素之后，同样的迅速释放连接，再进行最后一次TCP连接的建立。

获得网站根目录，下面的MC下面的呃麦当劳点png，最终这个服务器给客户端返回了麦当劳点png。好，那现在你已经体会到什么叫非持续连接了，这儿的连接指的是TCP连接，就是说当一次HTTP请求。和响应。动作完成了之后，这个TCP连接到底要不要继续？像非持续的这种方式，就是每完成一次HTTP请求和响应，就断开这个连接。那相比之下，我们现在再来观察这个持续连接。

如果采用持续连接非流水线的方式，那么可以看到，刚开始通过三次握手建立了TCP连接，其中第三次握手就可以开始携带HTTP请求报文。其中指明了自己要访问的那个网页是呃网站根目录下面的这个呃默认首页点HTML。那同样的客户端，这边需要收到HTML文件之后才可以知道这个HTML文件还要引用哪些元素，对吧？那接下来你会看到浏览器进程，并没有把前边刚建立好的这个TCP连接给断掉，对吧？而是继续利用这个TCP连接去发出下一次HTTP请求，那这次请求当中会告诉啊这个。服务器我想要get肯德基点png这个文件，于是服务器返回了这个文件。

好，收到肯德基图片之后，再构造HTTP请求，获得小视频点MP 4。获得小视频之后，再构造最后一个HTTP请求，获得MC文件夹下面的麦当劳这张图片。好，最终所有的文件都获取完毕之后，呃，我们才会去释放这个连接。那从这个例子当中，你就能感受到持续连接和非持续连接它的背后到底有什么区别，对吧？当TCP连接建立完成之后，

会让它持续一段时间，不会很着急的把它断开。好，这是持续连接非流水线方式，最后再来看持续连接流水线方式观察这个地方。在这个位置，浏览器进程获得了HTML文件。并且从中知道了呃，我还需要引用的元素有哪几个？接下来浏览器进程会连发三个HTTP请求。那每一个请求呃，都是用一个TCP报文段去包装的对吧？所以这其实就是发出了连续的三个TCP报文段。那每一个请求的内部会指明呃，自己想要获得的资源到底是什么？

那服务器进程收到这三个请求之后。它就会构造三个响应报文。每一个报文携带一个文件，那这三个报文也会连续的发给这个客户端。当客户端收到所有的文件之后，才去释放连接好，所以这就是流水线的方式。好，最后图中的有一些细节，有必要跟大家呃额外解释一下，首先如果http采用的是持续连接的这种工作方式。那么，在释放连接的这个地方，其实并不是说这个网页呃，相关的这些东西传输完成之后就要立即释放连接。

这个TCP连接也可以多保持一段时间，那具体保持多长时间是通过这个WEB服务器啊去设置的？得看服务器那边呃，程序员怎么去写代码？怎么去配置参数？那无论是非流水线方式还是流水线方式啊，它的这个逻辑都是一样的，反正只要是持续连接，那么这个呃连接保持的时间都是由程序员去设定的。那这是第一点要补充的细节，好再回来看这个非持续连接这个部分。在这张示意图当中，为了让大家看到呃，每一次TCP连接之间的这种清晰的边界。所以我在制作这张图的时候，

故意的把这个释放连接的这个呃，这个部分。和握手一握手二就是分开了，但事实上并不需要等到前一个连接释放完毕之后。再开始建立下一次连接。也就是说，其实这个握手一。我们本来应该画在这个位置。画在这个位置。但是如果我这么去画图，那你可能就呃感受不到这几次连接之间清晰的边界在哪了。所以这儿我也用文字啊，进行了解释，实际上可以在还没有释放完连接的时候就迅速建立，下次连接，

所以采用这种非持续连接的啊，这种方式。客户端向服务器，每请求一个文件，至少需要两倍的rtt的时间，因为对任何一个文件的获取都是经过握手一握手二握手三。然后再来一个http响应对吧，至少要经过两倍的rtt之后才可以开始建立下一次的连接。然后才能获得下一个文件嘛，对不对？好，所以如果我们把握手一挪到上面去，让它从这个位置呃，就立即开始的话。那么就意味着我们每过两倍的rtt就可以获得下一个文件嘛，

对吧？好，那这是这个地方需要补充的细节。好，接下来在这张图当中，你会发现我们画握手一二三的时候。这个线都非常的细，对吧？很细的一条线，意味着我们并没有考虑它的传输时延等于多少。我们认为这一条很细的线，对应的TCP报文段。是很快就传输到这个呃网络上的。那另一方面，我们观察到每一个包含http响应的这个TCP报文段。

我们都把它画的比较宽，对吧？也就是说我们在强调这个TCP报文段的传输时延。而不仅仅是它的传播实验，对吧？好，那原因是这样的。观察上面这张图，我们之前说过HTTP请求报文，我们主要是要向服务器说清楚，我要请求服务器做什么东西？所以请求报文的体积一般是很小的。那这就意味着把它封装成一个TCP报文段，这个TCP报文段体积也很小，对吧？

而相比之下http响应报文当中有可能会携带一个文件，比如说一张图片甚至是一个视频，那么我们知道一张高清图片。或者一个分辨率比较大的视频，这些文件的体积都是很大的，所以携带了一个大文件的http响应报文。它的传输时延，我们就不能把它忽略了好，所以在这张图当中，所有的响应报文，我们都故意的把它画出了呃宽度。而所有的握手报文以及呃携带HTTP请求的这种TCP报文段，我们都是把它画成很细的一条线。那现在你应该知道是为什么了好，那这个细节也是值得注意的。

有一些考题也会要求你考虑到这个东西。如果一个题目告诉你呃，某一个http响应报文携带了一个小文件。它专门强调了小文件这个词，那它其实就是在暗示你传输时间，你不用考虑了，就是这个意思。那具体的大家会在课后习题碰到。好，那最后呃非持续连接这个部分，我们还需要补充一个东西，事实上在这个例子当中，我们可以采用并行TCP连接的这种呃方式。去呃加快获取后三个网页元素的速度。什么意思呢？

当浏览器收到第一个响应报文之后，它就能够知道HTML文件当中还要引用的元素到底是哪几个，对吧？那总共有三个网页元素，也就是三个文件。那刚才我们获取这三个文件的方式是串行获取的，每获取一个文件之后。再建立下一个TCP连接。那如果我们想让这个过程加速的话，其实可以把后三个文件的获取。通过并行的方式去加速，也就是可以并行的建立三个TCP连接，那这样的话后三个网页元素的获取速度。就可以缩短为原先这种方案的三分之一，对吧？

对了，为什么刚才我们强调的是让后三个TCP去并行的完成呢？为什么我们不能把第一个TCP连接也并行的去建立呢？为什么？这个逻辑不难理解，对吧？因为只有获得了HTML文件之后，我才能够知道这个网页当中到底还需要引用多少个文件。所以第一次的TCP连接和后三次的TCP连接肯定没办法并行完成，就是这么个原因。好，那再回到之前这张图，现在你已经知道呃，在第六个步骤这个地方，我们需要做什么了，

对吧？浏览器进程会根据HTML文件里的内容去确定，接下来要获取的n个元素分别在什么地方？每一个元素都需要一组HTTP请求和响应来完成传输，那么根据HTTP协议的工作方式不同。获取这n个元素的那个背后的通信也会有所区别，那具体的我们结合这张图来回顾就可以了。那恭喜你理解了这两张图之后http这个部分的大题以及计算题已经难不住你了。那结合之前的理解，我们再来看这张总结思维导图，所谓的万维网world wide web。这个3w就是一个全世界范围内的分布式，联机式的信息存储空间。那为什么说万维网是全球范围的，分布式的和联机式的存储空间呢？

我们知道呃，在整个网络上，除了我这台嘀哩嘀哩服务器以外，肯定还有别的服务器，对吧？比如说哔哩哔哩的服务器也在这个网络上，以及百度的这个WEB服务器也在呃互联网上。那么，每个WEB服务器存储的这些资源，或者说存储的文件都是分布式的。所谓分布式，就是并不是集中存储的，对吧？B站的服务器上存储了很多B站的那个呃视频资源。而我的这个d站服务器上存储了肯德基，

麦当劳，还有小视频这些东西。所以万维网这个巨大的系统，它是分布式的存储系统。那刚才还有一个定语叫做连机式的，这个不难理解吧，对吧？所有的这些服务器，所有的这些客户端都是通过这个互联网把它们连接起来的。好，所以再回来体会这句话，所谓的万维网就是一个全球范围的分布式的联机式的信息存储空间。每一台服务器上面都会存储自己的一些资源，然后普通的用户可以通过HTTP协议。去各个网站获取这些资源。

那万维网的组成有这样的三大元素，第一大元素叫做统一资源定界符，英文缩写叫URL。很简单，这个东西大家其实都很熟悉啊URL的构成是这样的，首先要指明这个应用层的通信协议。然后指明服务器的域名或者IP地址以及端口号，最后是路径。一个URL指向了万维网上的某个特定的资源，比如说当你想要获得呃，我这个d站上的肯德基点png的时候。你就可以用这样的URL指向这个资源。冒号的前面指明了这个应用层的通信协议。双斜线的后面指明了呃，我的这个服务器主机它的域名那本质上域名对应的就是IP地址对吧？

那你会看到在主机域名的后面，我并没有跟端口号对吧？因为对于WEB服务器来说，端口号这个信息都是默认为八零端口。所以端口号这个信息是可以省去的。好，最后肯德基点png这个部分的信息是指明了你想要获得的这个文件。它在d哩d哩服务器上面的存储路径到底是什么好？那这就是一个典型的URL。好，再看上面这个例子，当我们去访问B站的首页的时候，你可以看一下自己的浏览器地址栏那个部分。它的前边是https。简单来说，

https就是在HTTP协议的基础之上增加了加密，还有其他的一些功能。反正它就是http协议的promax auto版啊，但原理都是类似的，然后后面这个部分呃3w点bili bili点com。这个指明的是你想要访问的那台服务器，它的呃域名是多少？对吧？那本质上域名就是IP地址。那在这个URL当中，我们也没有写端口号，因为B站的这个服务器，它同样是一个WEB服务器，同样也是默认使用八零端口。好，

最后你会发现，当你登录这个B站首页的时候，它的这个网址后面没有跟首页点HTML。没有跟这样的后缀，对吧？也就是说路径这个部分也被省去了，那背后的原因是每一个网站。都会有自己的默认首页。那一般来说，默认首页的那个名字叫index点HTML。当你用URL去访问网站的时候，如果你没有在后面指明你要访问的是它的哪个页面？那么，它会默认的给你返回它的这个首页。但是大家可以去试一下，

在B站那边，如果你跳转一个链接之后，你再观察你的浏览器，它的后面肯定就会跟一个呃那个路径的后缀了。好，有兴趣的同学可以自己去看一下，那这就是所谓的URL统一资源定位服务。一个URL唯一的指向了万维网上的某个特定资源。好，那这是万维网的第一个组成部分，那第二个和第三个组成就是呃，我们这节课一直在学习的http。又叫超文本传输协议，还有HTML，又叫超文本标记语言，

那这两个东西我们这节课有过，具体的例子，这就不再赘述。大家可以读一下思维导图，上面的这个描述。我相信你现在完全能理解啊，这儿想表达的是什么意思？好，那么在这节课当中，我们也用一个呃比较综合的实例去分析了HTTP协议工作的一个呃流程。关于访问网页背后发生了什么，以及HTTP协议的呃这几种工作方式，最好的方式就是看我给大家做的那个图去复习，一目了然。那这儿的文字部分，

我就不再赘述，大家可以自己暂停看一下，那最后一点需要补充的是，对于HTTP协议的一点零版本来说。它默认采用的是非持续连接的方式，而对于HTTP协议的一点一版本来说，它默认采用的是持续连接的方式。如果不想采用持续连接的方式，那么可以在HTTP请求报文当中设置一个参数叫做connection，把它设置为close。那具体的例子，我们会在下节课当中见到好的，那以上就是这节课的全部内容。
