各位同学大家好，从这节课开始，我们会学习DNS这个考点，那DNS是域名系统的一个缩写。domain name system在这个视频中，我们先对DNS进行一个概述，我们结合大家的生活经验啊，先对DNS相关的一些啊，基本的概念。进行一个感性的认知，那域名这个呃术语其实离我们并不遥远，我们平时习惯把域名这个词称作网址。所以网址就是所谓的域名，比如说百度的网址是3w点，百度点com。

那换一个术语来描述，我们可以说百度的呃网站域名是3w点百度点com。我们熟悉的域名有很多，比如说b站的呃，域名是3w点哔哩哔哩点com对吧？还有一个很著名的四零八的网站，叫做王道论坛，它的域名是3w点cs考研点com。这儿的cs是computer science的一个缩写，所以3w点cs考研点com这个就是专门为计算机考研啊，去设置的一个论坛。那通过这样的解释，相信你已经能够把这个域名给记住了，我们会感受到域名是很便于人类记忆的。那么，

当我们用浏览器去访问一个域名的时候，本质上是在访问这个域名背后所对应的那个网站服务器。那网站服务器本质上就是一个主机和你的电脑没有呃，太大的区别，只不过它比你的电脑更强悍一些而已。所以，当我们访问3w点，百度点com的时候，本质上是访问这个域名对应的一台主机。当我们访问3w点cs考研点com的时候，本质上是在访问这个域名对应的另一台主机。好，所以我们得出这样的结论，所谓的域名其实就是给互联网上的呃某一台主机，也可以是路由器去取一个名字。

比如说王道论坛的服务器，我们给它取了一个好听的名字，叫做3w点cs考研点com。b站的这个网站服务器，我们给它取了一个好听又好记的名字，叫做3w点哔哩哔哩点com。好，所以感受这句话域名是给互联网上的一台主机或者一台路由器取一个名字，访问域名本质上就是在访问这个域名对应的主机。和路由器。好，现在问题来了，你的电脑你的主机要访问呃，某一个域名的时候，比如说访问王道论坛的时候。

那你的主机肯定得知道这个王道论坛服务器的IP地址是多少，对吧？但是你只给呃你的这个电脑提供了域名的信息，并没有给它提供这个服务器的IP地址信息，那么你的主机是怎么知道？这个服务器的IP地址的呢，这就需要我们的主角DNS域名系统来发挥作用了。一句话描述DNS的作用。DNS域名系统可以把便于人类记忆的域名转换成便于计算机处理的IP地址。所以你给你的浏览器，你给你的电脑提供了域名的信息，那你的主机，你的浏览器会在背后。利用域名系统，把这个域名转换成对应的IP地址，

最终你的主机就可以和域名所对应的这个呃服务器去进行。同性了。做一个不太严谨的类比，一台主机就像是一个人，那一个人他有自己的身份证号，也有自己的人名，对吧？有自己的名字。那主机的IP地址就相当于人的身份证号，而主机的域名相当于就是人名。而我们的DNS域名系统就类似于公安的那个户籍系统，往这个公安的户籍系统里边输入一个人名。输入一个人名，这个户籍系统就可以把人名啊，对应的身份证号给找出来对吧，

所以本质上公安的户籍系统也是把名字和身份证号做了一个映射。那类似的域名系统就是把主机的域名和主机的IP地址做了一个映射，这样的话，当我们要访问一个域名的时候，总能通过DNS系统。去找到与之对应的IP地址好，接下来思考这样的一个问题，我们使用公安的户籍系统去查人名对应的这个身份证号的时候。怎么样才可以确保不重名呢？比如说全国各地可能叫咸鱼的人有很多个，对吧？那当我们查咸鱼的身份证号的时候，你怎么知道此咸鱼到底是不是彼咸鱼，对吧？好，

那如何解决这个问题？如果我们能够对这个人名提供一个呃完整的，层次化的信息，那么就可以避免重名的这种情况。比如说公安局在查案的时候呃，他很明确他们要找的这个咸鱼是湖北省武汉市。咸家村的咸鱼这个人，所以如果能够提供这个完整的，层次化的名字信息。那么，发生重名的这种可能性是不是就被大幅降低了？那么，类似的道理在DNS域名系统当中。为了让这个呃各台主机的名字不重复不重名，因此我们也对域名进行了层次的划分。

呃，从右到左用这个点分号，用这个小点进行分隔，分别是顶级域名，二级域名，三级域名。当然，前面还可以有四级域名，可以分得更细好，所以一台主机的域名是具有层次化特性的。好，现在我们已经对域名以及域名和IP地址之间的关系呃有了一个初步的认知。接下来，我们结合一个简单的例子来感受dns域名系统的作用，假设你在你的浏览器输入了3w点，

百度点com这个域名。并且敲下了回车键，那么此时你的浏览器呃，只知道你要访问的是这个域名，但是不知道这个域名呃，它背后对应的这台主机IP地址等于多少？此时DNS域名系统呃需要发挥作用。在你们家附近ISP会呃部署至少一台本地域名服务器，那这个域名服务器也可以称为DNS服务器。本质上，域名服务器也是一台主机。在这台主机的内部，它保存了很多域名和。和ip地址的这种映射信息，比如说其中可能就保存了3w点，

百度点com这个域名对应的ip地址等于多少？保存了这样的信息好，所以你的浏览器会先向呃这个域名服务器发出DNS查询。询问对方接下来要访问的这个域名，所对应的IP地址到底是多少？好域名服务器这边。它查询之后，得到这个对应的IP地址信息，于是会给你的浏览器返回一个DNS响应报文。就告诉他说啊，我查到了，你要找的这个域名对应的IP地址是什么？六八点六六也就这个东西。于是你的浏览器进程就可以根据这个IP地址和百度的网站服务器建立TCP连接，并且呃，

发出这个HTTP请求。请求对方返回百度的这个网页。最后，网站服务器给你的浏览器返回了百度网页的这个呃文件。于是你的浏览器进程会把收到的这个网页渲染成你所看到的这个样子。好，那么通过这个例子，我们粗略的感受到了DNS域名系统的作用，那细心的同学可能会有这样的疑问。就是你的这个浏览器，或者说你的这台主机，它是怎么知道域名服务器的地址IP地址等于多少的？因为只有知道这个服务器的IP地址，才有可能对这台服务器发出DNS查询报文，对吧？

好原理是这样的，通常来说，当你的主机在呃联网的时候，它不是会通过dhcp啊这个协议去自动配置，自动获取呃本机的IP地址以及什么？子网掩码这些信息吗？那与此同时dhcp这个协议也会给你的主机。配置一个默认的域名，服务器地址，比如说在这个例子当中呃，就会把什么幺六六点幺点幺二八点九八。就是把这个本地域名服务器的IP地址给你的主机。配置上去，那这样的话，当你的这台主机要查询一个域名对应的IP地址的时候，

就可以向这一台域名服务器发出DNS查询的。好，就是这样的一个原理。好，接下来介绍DNS的基本特点，首先DNS它采用的是CS，也就是客户端服务器的这种工作方式。在刚才这个例子当中，你的浏览器啊，对域名服务器发出了DNS查询报文。域名服务器收到这个报文之后，对这个报文进行响应，在这一次交互当中。你的浏览器是c客户端，而域名服务器，

它是s服务器的角色。客户端发出请求服务器给出响应。这是典型的CS模式。好，那这个地方值得一提的是一台域名服务器，它的角色有时候是扮演DNS服务器，有时候可能会扮演DNS客户端。比如说这台域名服务器，假设呃，它的内部并没有保存3w点，百度点com。这个域名对应的IP地址，也就是说在这台域名服务器这儿没办法完成呃，这个域名的解析工作。那这种时候，

这台域名服务器就需要向另一台域名服务器啊，发出DNS的查询报文。而另一台服务器收到查询之后，又会给出响应，所以在这次交互当中。下面这台域名服务器又扮演了DNS客户端的角色，而上面这台域名服务器又扮演了服务器的角色。客户端发出请求服务器给出响应好，所以DNS客户端和服务器的这个角色是相对的。有时候同样是一台主机，那这台主机可能会扮演服务器的角色，也可能会扮演客户端的角色。好，那这是DNS的第一个基本特点，第二个特点DNS，

它是应用层的协议，通常是基于udp的五三端口。所以如果一个DNS客户端要发出DNS查询报文，那么这个查询报文封装在udp里边，而这个udp的。目的端口号就应该填五三。好细心的同学会发现，我这加了通常这个前缀，对吧？在现实应用当中DNS呃，在某些特殊情况下，可能会基于HTTP协议。也有可能会基于TCP协议。不过，大多数情况通常还是基于udp协议。

那如果遇到选择题说DNS呃基于udp协议对于考试来说，这样的选项我们认为它是正确的。那不基于udp的这些可能性，我们就不再拓展。我们不妨思考一下，基于udp有什么好处？首先udp协议，它不像TCP还需要进行三次握手，然后才开始数据传输，对吧？udp协议是无连接的，那这就意味着如果浏览器要给服务器域名服务器发出DNS查询报文，那直接把这个报文封装在udp数据报里边，直接丢给他就行了，对吧？

不需要进行什么握手啊，建立连接啊，这些呃比较麻烦的前置工作。那这样就可以使得DNS查询的这个过程更高效，更有效率。你想全世界那么多的呃，浏览器那么多的应用都要查这个呃域名的信息，对吧？那肯定这个域名系统越高效越好，所以DNS通常都是基于udp的。这样可以保证效率好，最后再补充DNS的第三个特点DNS属于分布式系统，要理解什么叫分布式系统，我们不妨看它的反面。集中式的这种系统，

这两类系统是完全相反的两种设计思路，集中式系统就是把所有的数据和服务。全都集中在一台或者少数几台服务器上来处理，而分布式系统就是把数据和服务。全部都分散在多台服务器上，让这些服务器协同工作去完成任务。前者是中心化的，后者是去中心化的。集中式的系统类似于把鸡蛋放在同一个篮子里，而分布式系统相当于把鸡蛋放在不同的篮子里。所以对于DNS这么重要的呃，互联网服务来说，我们肯定要把鸡蛋放在不同的篮子里，对吧？这样的话，

万一其中的呃个别的篮子。坏了，那整个DNS系统，它至少还能够正常的运行。好，所以DNS的设计思路是采用分布式系统，这样的话，即使单个计算机呃，个别的这种域名服务器发生了故障。也不会妨碍DNS系统的正常运行。好，那么这是DNS的第三个基本特点，好的，在这个视频中，我们对DNS域名系统啊，

构建起了粗略的认知。所谓的域名，就是给互联网上的某一台主机或者某一台路由器取一个名字，而这个名字通常具有层次化的特性。当我们访问一个域名的时候，本质是在访问这个域名背后对应的主机。那正因为要访问这台主机，所以我们需要知道这台主机它的IP地址等于多少。那这个时候DNS就需要发挥作用，域名系统可以把域名转换成对应的IP地址。那域名很便于我们人类记忆，现在再考大家一个问题，王道论坛的域名是多少？3w点。cs考研点com对吧？

你看我一说你就记住了。所以域名便于人类记忆，而IP地址是便于机器去处理。那我们也介绍了DNS的一些基本特点，首先它属于应用层，基于udp，另外DNS它属于CS模型。并且是分布式系统，分布式系统相比于集中式系统来说，它的健壮性更强一些。不会因为个别的呃域名服务器损坏而导致整个DNS失效好，那以上是这节课的全部内容。
