好，继续来看HTTP协议。基于上节课的理解，在这节课当中，我们会补充一些和http相关的呃边边角角的一些概念。首先，我们展开看一下http报文啊，它的内部长什么样子？左边这张图是请求报文的样子，右边这张图是响应报文的样子。那由于http这几个字母念起来呃比较烫嘴，所以这节课的讲解中我就不加这个前缀了，反正在这节课当中探讨的所有的呃请求报文和响应报文。指的都是HTTP请求和响应报文好，首先来看请求报文，

它的第一行由一系列的字节组成。最前边这几个字节表示的是方法，那我们不妨换一个大家熟悉的名字，在C语言当中，我们把函数称为函数。而在JAVA python当中，我们会把函数称为方法本质上http的请求和响应。它的作用相当于远程的函数调用。响应报文相当于函数调用的返回。可以结合上节课这个例子来理解，当浏览器进程向服务器进程发出第一个HTTP请求报文的时候。本质上就是远程调用了服务器的某一个函数，对吧？浏览器进程需要向服务器进程指明，我要调用的是你的哪个函数？

这个函数的名字叫get，另外我还需要告诉它这个函数调用的参数是什么？就是这个东西，对吧？那服务器这边收到这个HTTP请求之后就会执行get这个函数对应的那些代码，你可以这么理解。那最终，这个get函数会给呃客户端返回一个函数的执行结果，那具体来说，这个返回的执行结果就是。下面这个东西会把默认首页点HTML这个文件给携带回去。好，所以请求报文的这个方法字段其实就是在指明我想要调用你的哪个函数，对吧？每个函数就对应着相应的功能。

像上节课的例子，我们调用的都是服务器的get函数，你可以这么去理解，当我们调用函数的时候，还需要指明参数，对吧？那这个参数是怎么指明的呢？就是用它这儿下面这几行所谓的首部字段，这个东西去指明的。那一会我们会看到具体的例子好，另一方面这儿还有一个URL，这个URL指向了服务器当中的某一个具体的资源，比如说指向了一张图片。指向了一个视频，等等。

本质上，这个URL你也可以把它理解为是函数调用的一个参数。那在URL和方法中间会加一个空格字符URL，后面又加一个空格字符。那再往后就是版本这个字段，比如说http一点零http一点一。好，另外在请求报文的每一行末尾，你都会看到一个crle。这个东西也是一个ask字符。就是回车换行符。好，这是请求报文的第一行包含的东西，那第一行的后面跟的就是若干个字段名以及字段的值。那刚才我们说每一个字段，

本质上就是在告诉服务器，我的这个函数调用的参数是什么？要让服务器根据请求报文当中的这些参数去做出呃，相应的这个处理。好，最后这个地方会有一个实体主体，这个部分通常来说不会使用，除非一些比较特殊的场景啊，比如说当你要用浏览器。上传一张图片的时候。那你的这个图片文件最终会被封装在这个请求报文的实体主体这个部分。不过，大多数的请求报文后面这个部分都是空的。好，那么接下来我们简单了解一下这个呃，

请求报文当中常见的一些方法。首先get这个方法，上节课我们已经接触了很多了，就是要向呃服务器请求呃URL。所指向的那个资源。比如说向服务器请求一张图片资源，或者一个视频资源等等。好，接下来第二个方法叫做head head方法，可以理解为是get方法的呃阉割版。当服务器收到一个呃get方法的请求报文的时候，最终服务器呃构造的这个响应报文当中。会携带着具体要get的那个文件的信息。比如说图片，视频那些东西就是在实体主体这儿携带回去，

但是如果服务器收到的是had这个方法。那么，服务器返回的响应报文当中，就不会把文件携带回去，而是只返回响应报文的首部，这个部分的信息。所以这个方法通常来说是在get方法之前先去询问一下服务器，你到底有没有这个资源？就是这个作用。比如说先用head方法向服务器询问你到底有没有肯德基点png这个资源，那么服务器收到这个head方法之后。它会把这个资源到底有还是没有这些相关的信息，通过一个比较小的首部返回给客户端。那这样的话，客户端就可以通过比较小的这种呃代价去获取到服务器那边相关的一些信息。

好，那这是head方法，接下来post方法呃，它的作用是给服务器添加信息，如注释好这句话你看不懂没关系。大家上网的时候登录过自己的账号密码，对吧？那么你的这个账号密码这些信息就是通过post方法。发给服务器，让服务器去处理，你给他post发过去的这些内容。好，所以get方法是浏览器向服务器索取一个东西，而post方法是。是浏览器向服务器发送一些东西。

就是这样的区别。那最后还有一个方法叫connection啊，这个涉及到代理服务器。考察的概率不大，有个印象就可以。好，那么这是请求报文，接下来看响应报文，响应报文的第一行最开始是版本信息，就是要指明我的这个响应报文。它符合的是http一点零标准还是一点一标准？第二个部分是状态码，大家最熟悉的状态码应该是四零四。有时候我们访问一个网页的时候，它可能会返回四零四not found会返回，

这样的信息对吧？那四零四就是服务器给你返回的一个状态码，这个状态码表示的是你想要去访问的那个URL所指向的资源是不存在的。就是这样的一个意思，那状态码的后面短语这个部分其实就是要给客户端返回的一些信息。那四零四是我们经常能见到的，还有一些状态码，它不会被显示在你的浏览器上，比如说二零零表示请求成功。当你访问一个网页，去访问一个URL资源的时候，如果这个资源真的存在，那么在这个响应报文当中。就会把你想要访问的那个文件，比如说HTML呃，

给你返回回来，与此同时，这个状态码可能写的就是200。表示你的这次请求是成功的，但是你的浏览器不会把这个状态码显示给你看。有兴趣的同学可以去查一下你的浏览器怎么打开调试模式，如果打开调试模式，你就能看到除了HTML文件之外的其他这些东西长什么样子了。好，那么下面这个地方给大家整理了响应报文当中可能会出现的状态码，状态码通常是三个十进制数。比如说幺零零或者二零零或者二零幺等等。可以根据第一位数字去判断这个状态码属于哪个大类，比如说以二开头的就表示呃，这次收到的HTTP请求是成功的。

而如果是四开头，那么表示客户端错误，比如说刚才提到的四零四not found，意味着客户端那边提供的URL。是错误的，有问题。另外，如果是五开头的，那么表示服务器这边有一些错误好，那这些东西啊，随便了解一下就可以好，接下来我们可以看到。呃，在后面这几行包含的就是一些字段名，还有值字段名值，

每一行的后面都有一个回车换行符。那所有的这些字段值交代清楚之后，这儿会插入一个空的换行，再往后就是呃实体主体。比如说我们之前提到的要返回一个文件，就是在这个位置去携带的。好，接下来看一个实例啊，大家可以在自己的浏览器去输入我的这串东西。来看一下我手动输入的这个URL，是想在百度服务器上。找到服务器根目录，下面有没有一张叫做闲鱼点png的图片？那我想要访问这个东西，结果服务器给我返回了一个四零四note fund。

说the request这个请求的URL啊，咸鱼点png这个东西was not found。on this server，在这个服务器上没有找到，对吧？这是浏览器最终给我呈现的一个效果，那当我输入完这一串东西，并且敲下回车键的时候，我的浏览器在背后其实是给呃百度的那个服务器。发送了一个http，请求报文，那我在自己的浏览器上呃使用调试模式，把这个请求报文给截下来了。看一下里边长什么样子，请求报文的第一行，

刚开始这几个字母表示的是方法，对吧？你看get方法表示，我这次的呃HTTP请求是希望从服务器那边。get获得一个网站根目录下面的啊，闲鱼点png这个文件。好，所以第一个部分是get get的，后面有一个空格，空格的，后面就指明了，我要get的那个东西，它的URL是什么？然后URL结束之后，这儿又有一个空格，

空格的后面指明了我的这个http，请求它采用的是什么协议？采用的是一点一版本的HTTP协议。好，回来对一下这个东西，第一个部分方法方法的后面空格，然后是URL再空格。指明版本最后一个回车换行。那接下来回车换行之后，就到达了这些什么呃字段，还有值这个部分对吧？我们可以携带很多很多个字段，比如说在我发出的这个HTTP请求当中，携带的第一个字段名叫做accept字段。冒号的前面是字段名，

冒号的后面是这个字段的值。第二个字段叫accept in coding。第三个字段。第四个字段。第五个字段。那来看一个大家熟悉的东西，这个字段叫connection。它指明的是，这次http传输。想要采用哪一种工作方式？我这儿发出的是keep alive，意味着这个http传输采用的是持续连接的方式。如果这次http传输想要使用的是非持续连接，那么只需要把connection这个呃字段的值。把它改成close。

改成close，就意味着采用非持续连接。好，那么在connection的后面这儿有一个东西叫做cookie，一会儿我们来补充cookie有什么作用？好，再往下，这儿有个字段host，这个字段就是指明了我要访问的这个主机的名字叫做3w点百度点com。好，所以对于这个hd dp请求报文，大家重点关注第一行这三个东西以及connection字段。还有cookie这个一会再讲，以及host字段。好，

这是我的浏览器，给百度的服务器发出的HTTP请求报文。接下来再看右边这个地方，这是百度的服务器给我的浏览器返回的http响应报文，它的首部信息。我们回来看一下这张图，响应报文的第一行，它首先是版本信息，然后是状态码，接着是短语。呃，然后就是一个回车对吧？换行那看一下这个，这是版本信息。表示采用的是HTTP协议的一点一版本。

四零四四零四是状态码，对吧？表示你这个客户端让我找的这个东西，它不存在。好，最后not found这个是短语，对吧？状态码和短语的信息会被我的浏览器显示在这个地方。而下面这个网页具体显示的这个内容。它其实是一个点HTML文件，对吧？这个HTML文件是在这个响应报文的。实体主体这个地方携带回来的，所以我的浏览器收到它的这个HTML之后。就会把HTML里边的这些呃内容渲染出来，

然后展示在用户面前。好，那这是一个http报文格式的实例。接下来我们聊一聊cookie有什么作用？简单来说cookie就是服务器给客户端办的一张临时身份证。有了这张临时身份证，服务器就可以区分当前和自己通信的客户端到底谁是谁。好比如说，当我用浏览器登录我的淘宝账号的时候。服务器这边就会生成一个呃cookie。比如说服务器记录下来啊，现在登录的这个用户名叫咸鱼老登啊，我给他生成一个cookie一二三四五六。服务器会把这个cookie存储下来，与此同时在给客户端的这个响应报文当中，

也会把cookie。返回给客户端进程，那么客户端进程收到cookie之后，它也会把cookie存储在本地。那这样的话，再往后如果我的这个浏览器再给这个WEB服务器发送一个HTTP请求的时候。就可以把服务器给我生成的这个cookie字段携带过去。那服务器根据这个cookie的值一二三四五六，它就能判断出哦，我现在收到的这个HTTP请求，它是来自于咸鱼老登这个用户。那当服务器区分了这个用户到底是谁？之后服务器是不是就可以给这个用户做一些个性化的处理？比如说，当我们逛淘宝的时候，

不知不觉会被推荐一些东西，被推荐一些你感兴趣的东西，对吧？给你的账号推荐的东西和给我的账号推荐的东西，那肯定是不一样的，而服务器可以通过客户端呃携带的这个cookie字段的值去判断。现在和我通信的这个到底是谁？好，那这就是cookie的作用。在现实应用当中，cookie字段非常复杂，你看这是百度的cookie。不过原理都是类似的。那值得一提的是，cookie是有有效期的。

我们说这个cookie是一个临时身份证，对吧？临时身份证的作用范围可能就是一天两天之类的，所以你会发现当你用浏览器登录了淘宝之后。可能过个两天或者三天，你就得重新登录了，对吧？这背后的原因其实就是服务器在两天前给你生成的这个cookie已经过期了。过期之后，它就会要求你重新登录你的账号密码。就是这样的一个原因。好，接下来看一五年的真题第40题。说某浏览器发出HTTP请求报文如下，问我们下列描述当中错误的是啊，

不好意思，这儿直接显示了答案啊，选c选项。不过，选出答案不是目的，我们重点是看一下从这个题目当中我们能学到哪些东西。好把这个请求报文拆解一下，第一行的呃，第一个部分get它表明的是方法对吧？表示。这个浏览器想要向服务器get获取一个什么资源？那获取的资源是什么呢？要获取的就是3w点test点edu点cn，要获取这台服务器。上面的这个index点HTML要获取这个文件。

然后采用的是http一点一版本。好，那来看一下AB两个选项a选项说该浏览器请求浏览呃index点HTML。没错，对吧？另外b选项说index点html存放在呃3w点test点edu点cn上。没错，好，接下来看connection这个字段，我们之前说connection这个字段表示的是。这个浏览器希望和呃服务器采用什么样的http工作方式？如果这个字段的值是close，那么表示采用的是非持续连接，而如果是keep a life。那么表示采用的是持续连接。

那这儿给的值是close，所以c选项说浏览器请求使用持续连接就是错误的。好，再来看最后一行这个字段的名字叫cookie值，是一二三四五六。如果浏览器给服务器发的HTTP请求报文当中包含cookie字段，意味着什么？意味着这个浏览器之前肯定已经和服务器发生过交互了，对吧？只有服务器给它返回了cookie之后。在接下来，浏览器才可以带上这个cookie。好，所以d选项说该浏览器曾经浏览过这个网站，就是正确的。

如果没有浏览过这个网站，那么它就不可能拥有cookie。好，所以这个题目选择c选项。对了，在上节课中我们提到过这样的一点。http的一点零版本默认采用的是非持续连接，而http一点一版本默认采用的是持续连接。默认的意思就是如果这个请求报文当中没有显示的，指明connection到底是什么？那么，对于一点一版本的HTTP协议来说，它就会采取持续连接的方式。就是这个意思。好的，

那么接下来就是计算机网络的最后一页总结，在这节课中我们补充了HTTP协议相关的一些东西，首先我们学习了http报文的结构。请求报文和响应报文格式各不相同。对于请求报文来说，我们重点关注第一行以及在各种字段里边，我们需要重点关注的是host字段。host字段指明了这台主机的域名地址是多少？以及需要关注connection字段。这个字段指明了http采用持续连接还是非持续连接，那最后还需要关注cookie字段。所以请求报文当中最重要的几个字段呃，大家通过一五年的这道题去复习就可以了，一个好的题目胜过千言万语。好，

那么对于响应报文来说，呃，我们重点关注的是它的第一行就是状态行。后面的这些字段，有兴趣的同学呃，可以自己去了解一下。好，另外，对于请求报文来说，最常见的方法，大家重点关注这三个。get方法意味着客户端想要从服务器那获得指定位置的资源。而head方法相当于残缺版的get服务器，在响应报文当中只返回状态行和首部行。也就是不返回这个实体主体。

最后post这个方法是客户端向服务器发送，待处理的数据，比如说把账号密码发给服务器。那在这节课当中，我们也补充了cookie的作用。cookie相当于服务器给客户端制作的一张临时身份证。当客户端获得一个临时身份证之后，他可以在自己的请求报文当中携带上这个cookie识别码。这样的话就可以给服务器亮明身份，而服务器这边就可以根据客户端的具体身份给它提供一些个性化的服务，个性化的响应。好的，那么http这个部分除了这节课的内容之外。还有上节课提到的http工作过程以及工作方式，这两个内容也非常重要。

别忘了放在一起来复习好的，那这就是计算机网络最后一节的内容。大家知道，今年由于我的这个产能不够，所以课程更新的速度慢了许多，如果你听到了这节课，那我相信你应该是在等我。所以非常想对屏幕背后的你说一声谢谢。感谢你对我的包容和信任，希望我做的内容能对你的理解带来帮助。很荣幸能以这样的方式参与你的人生，参与你追求梦想的过程。当然，在接下来的时间，我也会尽我最大努力。

多制作一些可能对你有帮助的内容。好的，那再一次感谢大家，我是王道咸鱼老师，我们下节课再见。
