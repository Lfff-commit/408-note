好，继续来看DNS这个考点，在这个视频中，我们要介绍域名服务器的四种分类，以及这四种域名服务器如何相互配合着，完成域名解析的这个。呃，动作那所谓域名解析就是把域名转换成对应的IP地址，这样的一个过程。那通过之前的学习，我们已经了解到域名是具有层次结构的，比如说3w点qq点com。从根域名到顶级域名，二级域名，三级域名，

四级域名等等，各级域名之间的这种关系呈现出树状结构。那接下来我们要解决的问题是怎么把这样一个层次化的域名转换成对应的IP地址呢？比如说。3w点qq点com这台主机啊，它的ip地址是六六六，我们怎么根据这个域名信息一步一步的转换出它的ip地址？好，我们先看一种简单粗暴的思路。我们可以试着部署一台超级域名服务器，那全世界范围内的所有的这些域名。以及域名和主机IP的这种对应关系，全都存储在这个超级域名服务器这。并且任何一台计算机，比如说你的计算机想要查一个域名信息的时候，

以及你设有的计算机想要查域名信息的时候，都会像这台超级域名服务器。发出DNS查询请求。那这种思路就是一个典型的集中式DNS系统。我们把所有的数据以及所有的这个服务都集中在了这台超级服务器这。那为了让全世界各地的计算机都能够找到这台超级服务器，我们可以给这台超级服务器分配一个固定的IP地址。比如说一点一点一点一这样的话，所有的计算机发出DNS查询报文的时候都往这个一点一点一这个呃地址去发送就可以了。不过，逻辑上可行，但现实中是不可行的。首先第一点，全世界的这个域名数量非常大，数以亿计。

那数以亿计的域名以及域名和IP地址的这种呃映射信息信息量很大，对吧？全都集中存储在一台服务器上，那这台服务器。它的存储负担就会比较大，并且由于信息量很大，所以它的检索速度也会很慢好。其次，这种集中式的呃系统。抗风险性是很弱的，比如说这台服务器，它突然被老鼠给咬了，或者被一个呃什么导弹给炸了。那只要这台服务器损坏，那全世界的互联网都会跟着完蛋，

一旦它宕机，那你就无法访问你喜欢的那个网站，我也无法访问我喜欢的王道论坛。好，另外大家可以做一个无奖竞猜，全世界范围内每一秒钟会发出多少次DNS查询请求？猜一下，据相关机构统计，每秒钟大概有1500万次DNS查询请求。在高峰期也有可能达到数千万次。那一秒钟要处理这么多次请求，那你想如果全世界只有这样的一台超级域名服务器。他每秒钟都要处理呃，全球各地的数千万个，这种呃查询的任务。

那无论这台服务器，它再强大肯定都顶不住的，对吧？好种种迹象表明，这种呃简单粗暴的思路不可行。那接下来我们把这种思路进行一个优化改造。解决一二三这三个问题。首先，解决第一个问题就是所有的信息都集中存储在一台服务器。这。存储负担大，检索速度慢，这个问题怎么解决呢？接下来，我们结合域名的这种层次特性，

先引入三类域名服务器，这三类域名服务器分别是。根域名服务器顶级域名服务器以及权限域名服务器。首先，这个根域名服务器我们可以给它分配一个固定的IP地址，就像刚才所说的那个超级域名服务器一样。有固定的IP地址，好处在于全世界各地的计算机，它想要进行DNS查询的时候，总能在这个固定的位置去找到这个根结点。好世界各地的计算机找到根域名服务器之后，这台根域名服务器内部它保存了所有的顶级域名服务器的IP地址。比如说CN这个顶级域名服务器，它的IP地址是九九九com，这台顶级域名服务器，

它是三三三。其它的顶级域名服务器类似。比如说某台计算机，它想要查询3w点qq点com这个域名的时候。根域名服务器是不是可以先拆解出它的顶级域名是多少？如果顶级域名是呃com。那么OK下一步可以去询问com这一台顶级域名服务器好，那么一台顶级域名服务器。它的内部记录并维护了这个顶级域名之下的所有的权限，域名，服务器的IP地址。比如说qq点com这台呃权限域名服务器，它的ip地址是四四四。然后cs考研点com这台权限域名服务器，它的ip地址是五五五。

好，那么只要知道完整的域名信息，那么这台顶级域名服务器是不是就可以再拆分出二级域名？根据这个二级域名，到底是QQ还是CS考研？他就可以确定下一步应该向哪台域名服务器去发起这种查询，那对于3w点qq点com来说。下一步显然应该呃去询问这个呃qq点com就是左边这台权限域名服务器。那么，在我的这个图示当中，这个绿色的小框框笼罩的这些主机的域名和IP地址的映射信息，就是存储在这一台权限域名服务器当中的。那类似的cs考研点com这台权限域名服务器里边保存了3w点cs考研点com home点cs考研点com，就是保存了这些主机的。域名和IP地址的映射信息。

另外，像左边这台呃权限域名服务器edu点cn，它的内部保存了所有以edu点cn。结尾的啊，这些域名和IP地址的映射关系。包括四级域名，3w点pku点edu点cn这种四级域名对应的主机，它的ip信息啊，也是存放在这个权限域名服务器当中的。好，那么回到qq点com这台权限域名服务器，它现在可以知道哎，你现在要查询的是3w点qq点com对吧？把三级域名拆分出来。三级域名叫3w，

那么这台权限域名服务器就可以查到最终的这台主机，它的IP地址到底是多少？然后把DNS的查询结果逐步的返回好，那么可以看到我们现在利用这个域名的层次化信息设置了三层域名服务器。自顶向下，分别是根顶级和全线域名服务器。那么，通过这三级服务器的逐层查询，最终一定可以找到任何一个域名对应的主机的IP地址。那相比之下，前面这种简单粗暴的方案当中，超级域名服务器的内部需要保存全世界。所有的这些域名和IP地址的对应关系，对吧？它的信息存储量，

存储的负担是很大的，检索速度也是很慢的。而在后面，这种方案当中，我们的根域名服务器，它的内部只需要保存这些顶级域名服务器的IP地址。而不需要保存这些终端主机的IP地址信息。那现在根域名服务器的信息存储量被降下来了，那存的东西少，它的检索速度就可以变快，对吧？好，接下来我们还需要解决集中式的系统单击损坏，整个系统就跟着完蛋这个问题。那么，

在后面这种方案当中，如果根域名服务器全世界只有这样的一台。那这个根域名服务器一旦损坏，是不是整个互联网系统的DNS查询服务也会中断，对吧？那现实世界中这个问题的解决方案是在全世界各地分别设有13组根域名服务器。任何一组根域名服务器的内部都完整的保存了所有的顶级域名服务器的IP地址。那每一组根域名服务器都会给它分配一个固定的IP地址，比如说一点一点一点一。这是其中的一组根域名服务器。另一组跟域名服务器给它分配一个十点，十点，十点十之类的，反正13组跟域名服务器分别绑定一个固定的IP地址。那这样的话，

当我们要进行DNS查询的时候，这个查询的动作从任何一组根域名服务器开始。它总能找到下一级的顶级域名，服务器以及更深一步，又可以找到对应的权限域名，服务器。好，那么通过设置多组根域名服务器就解决了，一旦单个节点损坏，整个互联网就完蛋这个问题。使得DNS系统的抗风险性大幅度升高。好，那这个地方值得一提的是，我们说全世界有13组根域名服务器，对吧？

那每组根域名服务器，它在背后并非对应单台主机。通常是对应多台，并且这些主机会被部署在全球各地。比如说一点一点一点一，这是13组根域名服务器当中的一组，那么这组根域名服务器在物理上可能对应五台主机。这五台主机共享一点一点一点一这个IP地址。好，那么这些主机它会被分散部署在世界各地，比如说亚洲，我部署一台非洲，我部署一台。澳洲部署一台北美和南美在各部署一台接下来任波技术就会发挥作用。什么叫认拨技术呢？

比如说某一台计算机，它要发出一个DNS啊，查询报文，那么这个查询报文。他是想发给一点一点一点一这一组根域名服务器，那么当这个查询报文发出去之后。路由器会把这个报文就近投递。比如说这台计算机，它在亚洲对吧？那么就是部署在亚洲的这一台主机，一点一点一点一，这台主机离这个原主机更近。那么，这个报文就会派发给最近的这台主机。那相应的，

如果有一个南美洲的计算机，它也要往一点一点一点一发出一个DNS查询报文，那么这个查询报文就会呃就近的转发给这个南美洲的这台主机。那这就是认拨技术。可以实现就近访问，并且可以冗余容灾。什么叫冗余容灾呢？比如说亚洲的这台机器坏了，那么发往一点一点一点一的这个呃报文。可能就会在剩余节点当中选择一个更近的，比如说发给非洲。这就是冗余容灾，那认拨技术有点类似于你在饭店吃饭的时候，有时候你遇到问题会喊一声服务员，对吧？

那么一二三四五。这五个人都叫服务员，谁最先响应你呢？一般来说是离你最近的那个服务员会响应你，对吧？那这的一点一点一点一其实就类似于你喊服务员。最终哪个服务员会响应你，取决于谁离你更近。这样可以提高DNS查询的效率吗？好通过设置13组根域名服务器，我们解决了这种集中式的系统单机损坏，导致整个系统完蛋的这个问题。使得抗风险性增高好，接下来我们解决最后一个问题。全世界的这个DNS查询量很大，

对吧？即便我们设置了13组根域名服务器，但是相较于全世界每秒钟数千万次的这个呃DNS查询请求来说。如果每一次查询都需要在这13组跟域名服务器这呃进行的话。那么，这些跟域名服务器同样压力山大。好，那么为了解决这个问题，接下来我们引入最后一类域名服务器，叫做本地域名服务器。刚才我们分层级引入了根域名服务器，顶级域名服务器和权限域名服务器。那本地域名服务器是干嘛的呢？本地域名服务器数量非常多，像中国电信，

中国移动这些ISP，它会在各个地区部署大量的本地域名服务器，比如说你家附近呃，就会部署一台本地域名服务器。专门为你家附近的那一片的这个计算机去服务。另外，像很多高校以及某些大型企业，内部也会自己部署一个本地域名服务器。本地域名服务器会为用户就近的提供DNS解析服务。是用户侧的默认DNS入口。比如说当你的这台主机啊，在接入网络的时候是不是会用dhcp配置一些网络信息？那其中你家附近的这个本地域名服务器IP地址是多少？也是通过dhcp。去给你家的主机配置好的，

那这样的话，当你的这台主机要进行DNS查询的时候，他就知道噢，我应该去呃什么？幺二八点九八应该去这个地方去查就行了。好，那么本地域名服务器这个小老弟是怎么为呃根域名服务器去分担这种呃DNS查询压力的呢？说来也简单，本地域名服务器内部会保存DNS的缓存信息。当某一个域名被重复查询的时候。只要缓存命中，那么本地域名服务器就可以直接给用户返回DNS查询结果。举个例子，假设这台本地域名服务器刚开机。它的DNS缓存刚开始为空。

那么下面这有一台主机呃，向他去查询3w点qq点com。要查这个域名的IP地址信息好，这个本地域名服务器会先检查自己的缓存里边有没有？这个域名的IP地址信息。现在显然没有，对吧？那于是这台本地域名服务器，它就只能去麻烦这个根域名服务器。从根域名服务器开始，根域名顶级域名权限域名逐层的去查找，最终就会找到3w点qq点com这台主机的ip地址。那查到IP地址之后。就会把域名IP地址的信息缓存下来，并且设置一个呃，

有效时间，比如说30分钟内。这个缓存信息是有效的。好，接下来本地域名服务器给下面这台主机返回查询的结果，3w点qq点com ip地址是六六六。那刚才这个过程当中，本地域名服务器，它不知道呃，这个域名的IP信息。所以他不得已还是得去麻烦这个更域名服务器，对吧？好，接下来不一样的来了，现在还有一台主机，

它也想要访问3w点qq点com这个域名。那这台本地域名服务器收到这个查询请求之后，这一次它在自己的缓存当中可以直接找到。3w点qq点com对应的ip信息对吧？并且检查这个ttl生存时间，这个有效时间还没有过期。于是，在这次查询当中，本地域名服务器不需要再对外查询，而是直接把结果返回给右边这台主机。所以引入了DNS缓存这种机制之后，根域名服务器的压力就被大幅度的降低了。事实上，大多数的dns查询可能查的都是那几个热门的域名，对吧？

比如说你喜欢3w点哔哩哔哩点com，我也喜欢3w点哔哩哔哩点com。很多人高频访问的域名可能就那几个，而一旦一个域名被高频的访问，那么这个域名的信息就很容易啊，在DNS缓存当中被命中。这样的话，大量的重复查询工作其实就可以被避免，对吧？于是，根域名服务器的负荷就没那么高了。那除了本地域名服务器拥有DNS缓存之外。一台主机内部也会拥有DNS缓存。在这，我们引入一个新概念，

叫做解析器。r.就是负责发出DNS查询报文，以及解析DNS响应报文的那一段程序。通常来说，在一个操作系统内部，会设有一个系统级的解析器，比如说当你自己开发了一个应用程序之后。你的应用程序，如果想要使用DNS查询这个服务，那么你没必要去自己实现DNS查询的这一套逻辑，只需要调用操作系统给你提供的这个解析器。就行了。当然，有一些需要高频呃DNS的应用，

比如说浏览器呃，你每上一个网站，它可能都需要进行一次DNS查询，对吧？那像这类应用。它可能会在呃应用的内部自己实现一个DNS解析器。就是说它可以不调用操作系统的这个解析器。所以在我的课程当中，有时候我会说浏览器啊，对这个本地域名服务器发出了DNS查询报文。这种表述是正确的，因为浏览器的内部，它可能自己就实现了DNS的这个解析器。有时候我也会说，一台主机向这个呃域名服务器发出了DNS查询报文，

那这种说法也是正确的，因为在一台主机内部就是在操作系统这个级别。会有一个公用的解析器。好，那这是解析器的概念，反正就是一段程序用来发出DNS报文和解析呃，收到的这个DNS报文。那么，在这个解析器的内部，也会保存一个本地的DNS缓存。那这么做的好处是进一步的，降低DNS查询的频率。比如说右边这台电脑，它的这个解析器啊，最近查过qq点com的这个ip地址信息。

那么，在这个解析器的内部，就会把3w点qq点com。它对应的IP地址信息六点六点六点六。给缓存在本地。并且同样的也会设置一个呃，有效时间，比如说有效时间是30分钟，那么在30分钟之内，如果这台电脑。需要再一次的访问，3w点qq点com，那么这台电脑甚至就不需要向本地服务器发出查询请求。而是直接根据本地的这个缓存，直接去访问六点六点六点六。

好，所以本地的DNS缓存是用于降低啊，这个终端主机向本地域名服务器发出DNS查询请求的频率。而本地域名服务器的这个DNS缓存，是用于降低本地域名服务器向。根域名服务器发出啊，这个查询请求的这个频率。好，那么引入了DNS缓存机制之后，我们在前面这个方案当中遗留下来的问题终于解决了。虽然全世界每秒会有数千万次的DNS查询请求。但是得益于部署在全世界各地数量庞大的本地域名服务器，以及得益于这种。DNS缓存机制。根域名服务器，

它实际上并不会每秒钟收到数千万次的DNS查询请求。很多DNS查询请求可以被本地域名服务器直接处理。只要缓存命中即可。好，接下来我们一鼓作气，详细探讨域名解析的过程。域名解析就是把域名转换成对应的IP地址的这个过程。域名解析是通过DNS查询来完成的，那么DNS查询的方式有两种，分别是递归查询和迭代查询。在这页课件当中，我们先介绍递归查询。用一句大白话来表述所谓递归查询，就是如果你问我问题，那么呃，

即便我不知道这个问题的最终答案，我也会想方设法的去帮你找到最终答案。而迭代查询的话，就是你问我问题，如果我不知道最终答案，那么我会告诉你呃，你下一个应该问谁？好结合例子来体会。现在这台主机想要访问mail点qq点com。于是他向本地域名服务器发出了DNS查询报文。好，这台服务器检查自己的缓存mail点qq点com这个域名的信息嗯，此时还没有。缓存没有命中。那现在我们假设采用递归查询的方式。

再来读一下这句话，递归查询，就是说你问我问题，我会想方设法的帮你找到最终答案。现在这台本地域名服务器显然还不知道妹儿点qq点com的最终答案，对吧？好，那怎么办呢？他会向根域名服务器发出dns查询，问他mail点qq点com你知道吗？并且这次查询请求也是指明了采用递归查询好，那么这台根域名服务器收到这个请求之后。他也不知道mail点qq点com到底是多少，那由于采用的是递归查询这种方式，那么对于这台更域名服务器来说，

是不是也是用一样的原则？你问我的这个问题，我虽然不知道最终答案。但是我会想方设法的帮你找到最终答案好，那么既然根域名服务器不知道，但是它根据这个顶级域名。com的信息他可以判断呃，既然要找到这个最终答案，那么下一步我应该问的是。com这台顶级域名服务器。而根域名服务器是知道这台顶级域名服务器的IP等于多少的，对吧？所以根域名服务器就会像三点三点三点三这台服务器。发出一个DNS查询请求。问他你知不知道mail点qq点com的ip好，

这台服务器现在也不知道。根据域名信息，它拆出了二级域名，对吧？二级域名是qq，于是这个顶级域名服务器会像qq点com这台权限域名服务器。发出DNS查询。问他mail点qq点com他的ip地址是多少，你知不知道好那么到权限域名服务器这一层？显然，他已经能够知道最终答案了，对吧？七点七点七点七好，他查到最终答案之后返回给上一层。com这个顶级域名服务器。

好，这个顶级域名服务器，它又会返回给再上一层根域名服务器。好，现在根域名服务器，它知道了最终结果，那么它又会返回给上一级的本地域名服务器。好本地域名服务器会把查询结果缓存在本地。与此同时，把最终的这个IP地址查到的IP地址返回给呃这个主机。查询结果是七点，七点，七点七。好感受这个递归查询的过程，本地域名服务器问根域名服务器，

你知不知道mail点qq点com？根域名服务器虽然不知道最终答案，但是它会想方设法的找到最终答案，然后再把最终答案返回给。对方对吧？这个查询的过程也有点像函数的递归调用a函数调用b函数b函数调用c函数c函数调用d函数d函数调用e函数。一函数返回之后，先把结果返回给呃dd，返回给CC，返回到BB，又返回到a，对吧？所以跟函数的递归调用是很像的。因此也把它称为递归查询方式。如果指明采用递归查询方式，

并且被请求的这台主机，它也支持这种递归查询方式的话，那么就会按刚才我们所说的这套方法。逐层的查查到最终结果之后再返回给呃发出查询的这台主机。好，这是递归查询的方式。接下来第二种方式迭代查询，在接下来的例子中，本地域名服务器会用迭代查询的方式。去完成域名解析。我们还是从这台主机开始，主机问本地域名，服务器mail点qq点com，你知不知道？本地域名服务器检查自己的缓存，

缓存没有命中，于是它只能向根域名服务器发出查询请求。这次我们采用的是迭代查询的方式。他会问根域名服务器mail点qq点com ip地址是多少，你知道吗？根域名服务器呃，它的内部其实也会有dns缓存。如果它的缓存里边哎，没有命中找不到这个结果，也就是他不知道最终答案是什么。那么，在迭代查询的这种方式下，根域名服务器不会替这个呃本地域名服务器去询问com。而是会回复这个本地域名服务器，你要查的这个域名我不知道。

但是下一个人你可以去问com这台顶级域名服务器，并且这台顶级域名服务器的IP地址是三三三。好，现在本地域名服务器知道下一步该找谁了，对吧？于是同样的，他会问三三三这台呃顶级域名服务器，你知不知道mail点qq点com的ip？好，这台服务器它也会先检查本地的缓存，如果这个内部缓存没有命中，那么呃，它就只能跟对方说。抱歉，我也不知道你要问的这个域名，

它的IP是多少？但是呃，下一步我可以告诉你，你可以去问四四四这台主机。好，现在皮球又踢回到本地域名服务器了，最后本地域名服务器就可以向四四四这台主机发出DNS查询请求。那么，来到权限域名服务器这一步，它的内部肯定就已经保存了魅二点qq点com。于是，这台服务器向本地域名服务器返回了最终的答案。是七七七。本地域名服务器把查询结果缓存起来，并且把查询结果返回给呃最开始发出请求的这台主机。

好再次感受迭代查询的这个特点。你问我的这个问题，如果我不知道最终答案，那么我就告诉你，你下一步应该问谁？好所以迭代查询，递归查询是两种截然不同的呃域名解析思路。那在现实应用当中，根域名服务器通常呃只提供迭代查询的这种服务。所以，即便本地域名服务器发出DNS查询请求的时候指明说，呃，我需要的是递归查询服务。那抱歉，根域名服务器这边提供不了，

我只能按照迭代查询的这种方式告诉你，你下一步去问谁？好，因此到底采用什么样的查询方式？这个通常是在DNS查询报文当中去指明的，但是这种查询方式。到底会不会被支持？具体还要看接收查询报文的这个服务器，它是否提供这种查询服务？对于考试来说，它会明确的告诉你呃，采用的是什么样的查询方式？好，另外在这个图示当中，我们可以看到终端主机向本地域名服务器发出的这个呃查询请求。

我们默认都是采用递归查询的方式。递归查询的方式，意味着本地域名服务器会想方设法的直接把最终答案告诉这个终端主机。好，接下来看两道真题，首先看二零一零年的40题。说如果本地域名服务器没有缓存，那么当采用递归的方法去解析另一个网络的某主机域名的时候。用户主机和本地域名服务器发送的域名请求消息数分别是多少？好，大家思考一下，十秒钟之后公布答案。三二一这题选择a选项。用户的主机想要去访问另一个网络当中的某主机域名。好，

那么用户主机肯定需要向本地域名服务器发出这个DNS查询请求，对吧？那么，由于本地域名服务器没有缓存，因此本地域名服务器肯定需要向根域名服务器发出这个查询请求，但题目明确告诉我们说采用的是递归的这种方法。那递归方法意味着根域名服务器会向顶级域名服务器去寻找答案。顶级域名服务器又会向权限域名服务器寻找答案。权限域名服务器返回，结果顶级域名服务器返回给根根域名服务器返回给本地，本地域名服务器返回给用户主机。好，所以根据题意，这个用户主机，它整个过程当中只发出了一次DNS的这个查询请求，

对吧？同样的，本地域名服务器也只发出了一条查询请求。那我们不妨把这个题目改造一下，如果本地域名服务器对外都是采用迭代查询的这种方法，迭代查询的方法，那么这个题目答案应该选多少？是不是就应该选择b选项？用户主机向本地域名服务器发出的这个查询请求默认都是递归查询。而本地域名服务器如果对外采用的都是迭代查询，那么它得先问根，然后再问顶级。再问权限，整个过程会发出多条域名请求信息。好，

所以注意审题递归方法和迭代方法得到的答案是完全不同的。那这是一零年的40题。接下来再看一六年的40题的改编题。说假设所有的域名服务器都采用的是迭代查询的方式。当h4这台主机访问呃规范域名3w点xyz点com，这个网站的时候。本地域名服务器。就是这一台。在完成域名解析的过程当中，可能发出DNS查询的最少和最多次数分别是多少？好，大家把答案打在公屏上，十秒钟之后公布答案。这个题目应该选择a选项。好首先来分析最少的情况h4要查这个域名的信息，

那么它得向本地域名服务器发出DNS的查询请求，对吧？那么，这台本地域名服务器，如果说它的缓存可以命中的话，它就不需要再对外查询了，对吧？所以这台服务器它对外发出的DNS查询次数最少可以是零次，只要缓存能够直接命中，那么它就可以把呃查询的结果。直接返回给这个呃用户主机。好，这是最少的情况，再来看最多的情况，用户主机发出DNS查询请求，

如果缓存没有命中，那么这台本地域名服务器。首先，需要向根域名服务器发出查询。那由于采用的是迭代查询的方式，迭代查询的方式，因此根域名服务器。不知道最终的这个呃查询结果，那么他只能告诉本地域名服务器说你接下来去找com这台服务器。好，接下来本地服务器向com发出请求com告诉他，我不知道你的答案，但是你接下来可以去找xyz点com这个权限域名服务器。好，最后这台本地域名服务器又向权限域名服务器发出查询，

找到最终结果之后，再把最终结果返回给用户主机，那么可以看到在这个过程当中，本地域名服务器发出了一。二三三次DNS的查询请求。综上所述，当缓存没有命中的时候，如果采用迭代查询，那么最多会发出三次DNS查询请求。题目选择a选项。好的，那么递归查询和迭代查询是考研的重点，大家可以再结合这两道题目再来消化吸收一下。由于这个部分的内容比较多啊，所以这节课就先讲到这下节课，

我们还会补充一些呃，边边角角的东西。那这个部分的总结我们也放在下节课的结尾，一起进行好的，那以上就是本节课的所有内容。
