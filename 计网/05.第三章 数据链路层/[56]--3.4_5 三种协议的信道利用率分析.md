好的，在这个视频中，我们要探讨三种协议的信道利用率，包括停止等待协议后退，延伸协议和选择重传协议。那后退认真协议和选择重传协议的信道利用率分析思路是完全一致的，所以这两种协议我们会放在一起来探讨。另外，在这个视频中，我们也会补充几个重要的术语，这些术语可能在看书做题的时候会碰到。好，那由于信道利用率这个部分是考研小题的命题重点，所以在这个视频中我们也会穿插一些例题来帮助大家了解这个部分的考察风格。好，

首先我们来看，停止等待协议的信道利用率，那我们知道在停止等待协议当中，发送方和接收方啊，发送数据的过程是这样的。首先，发送方要给接收方发送一个数据帧。接收方收到这个数据帧，并且检查无误之后会给发送方返回这个数据帧的确认帧对应的ack。由于停止等待协议的呃发送窗口和接收窗口的大小都等于一，所以每一次只能发一个数据帧。返回一个确认帧，再发送下一个数据帧，再返回下一个确认帧，就是这样的一个过程好，

那么为了方便探讨信道利用率，我们不妨假设这样的一些前提条件。现在假设发送方和接收方之间的这个信道数据传输速率是1 KB PS。数据帧的长度是4 KB，那显然发送一个数据帧就需要四秒钟的时间。一个确认帧的长度是1 KB。所以发送一个确认帧需要耗时一秒钟好，另外我们再假设啊信道的单向传播时延等于七秒钟。那基于上面这些条件，发送方和接收方之间使用停止等待协议去传输这个数据帧的过程就是这样的。在这个图当中，上面这条线表示的是发送方，下面这条线表示的是接收方，横轴表示时间t。并且横向的一个小格子对应的是一秒钟的时间。

好，那么来看一下，首先发送方给接收方发送第一个数据帧耗时，总共需要四秒钟。所以，对应横向的一二三四四个小格子，那由于信道的单向传播时延是七秒钟。所以这个数据帧的最后一个比特，从被发出到被接收，总共中间经历了七秒钟。好，那么收到这个数据帧之后，假设这个数据帧没有检测出任何的差错，那么此时接收方就可以给发送方返回一个呃。确认帧ack那刚才我们说了确认帧的呃，

发送时延是一秒钟的时间好，那么这个确认帧在信道上传播也需要七秒钟。因此，从确认针的第一个比特被发出，到第一个比特被发送方接收，总共中间经过了七个小格子。好，那么再经过一秒钟的时间发送方就可以完整的收到呃，这个ack帧。接下来，从这个时间点开始，发送方就可以发送第二个数据帧，那第二组数据帧和确认帧的这个传输过程是完全一致的。所以理想情况下，之后会一直重复前边的这个过程。

那可以看到，从发出一个数据帧到收到这个数据帧的ack，整个过程至少需要画。四秒，再加上二×7，再加上最后的一秒，这是总耗时。在这一诊断时间内，发送信道上只有四秒钟是有数据通过的，所以发送信道的信道利用率就应该是四除，以刚才的这个。呃，总时间就等于21%，那这就是发送信道的利用率。注意这个地方，

我们探讨的是发送信道的利用率。也就是说，发送方和接收方之间的数据传输，我们把它看作是两条单向的信道。发送方给接收方发送数据的这条信道，叫做发送信道，而接收方给发送方返回ack的这条信道，我们把它视为接收信道。那由于我们探讨的是发送信道的信道利用率，所以我们认为发送信道上只有四秒钟的时间是有数据通过的。传输ack的这一秒钟是接收信道上有数据通过的时间。好，那理解清楚这一点，接下来我们对信道利用率的这个计算啊，进行一个小总结。

我们把一个数据帧的传输时延记作TD。把两倍的单向传播时延记作rtt。然后把一个确认帧的传输时延记作ta，那么在停止等待协议当中。呃，理想情况下，信道利用率u就应该等于呃数据帧的传输时延。除以数据帧的传输时延，加上两倍的单向传播时延，再加上这个确认帧的传输时延。那结合下面这张图应该不难理解。对了，这个地方为什么要强调是理想情况下呢？因为我们知道信道的传输，有时候是不稳定的，

对吧？那所谓理想的情况就是说。这个信道的传输不会丢帧，这个帧的内部也不会出现比特差错。每一个数据帧和每一个呃确认帧总是能顺利的发送和接收。所以这个地方我们是基于这种理想情况来探讨信道利用率的。好，那么这是停止等待协议的信道利用率。好，接下来我们探讨另一种情况，我们假设确认针非常短，那确认针非常短，意味着。确认真的这个呃，传输时延也会非常短，

我们可以把这个传输时延忽略不计。那在这种条件下，我们可以把这个确认针的传输画成这样的一条线，也就是说它是没有宽度的。我们把这个确认针的传输看作是一瞬间就可以完成，因为它非常短嘛。好，所以改变这样的一个条件之后，信道利用率就变成了这个样子。发送信道上有四秒钟的时间是有数据通过的，那从第一个数据帧发出到下一个数据帧可以发出，总共需要经过四+14这么长的时间。所以信道利用率就应该等于百分之二十二点二。在这种情况下，刚才我们总结的这个信道利用率的计算公式同样适用，

只不过这儿的ta我们把它视为零就可以了。好，接下来看一个真题，二零一八年的36题假设主机甲采用呃停止等待协议，向主机乙发送数据。数据的传输速率是3k bps单相的传播时延，单相的传播时延是200毫秒。忽略确认帧的传输时延，这个什么意思啊？就是刚才我们说的那个条件对吧？就是说确认帧非常短。我们可以认为它的传输是在一瞬间就完成的。也就是说，我们可以忽略确认帧的传输实验啊，但是呢，

我们不能忽略确认帧的传播实验，对吧？这是两回事。好题目说，当信道利用率等于40%的时候，数据帧的长度应该是多少？好，那么停止等待协议的这个信道利用率，我们刚才总结过，等于数据帧的传输时延，加上数据帧的传输时延。加两倍的单向传播试验，再加确认针的传输试验。在这个题目当中，确认帧的传输时延等于零，

那数据帧的传输时延应该是等于数据帧的长度。除以呃这个信道的速率，我们最终要求的是数据帧的长度，对吧？那把题目给的条件带进去之后，我们应该算得这个信道利润率要等于40%，这也是题目的条件。好，那接下来就可以解这个方程，最终可以算得数据帧的长度应该等于800比特。那提醒大家需要注意，我们这儿带入的单向传播时延单位是毫秒得把它换算成秒为单位。才可以和前边这个呃bps相匹配。好，那这是一个很典型的例题，

考察停止等待协议的信道利用率。那有必要提醒啊，部分同学很多同学看到这样的一个公式，他会选择去背。其实很简单，信道利用率的分子表示的是这个一个数据帧的传输时间，对吧？而它的分母指的是从一个数据帧刚开始发出到下一个数据帧可以发出。中间总共经历的总时间。在发送信道上，这一段总时间内，只有其中的一小部分有数据通过，那有数据通过的，这个时间的比例就是信道利用率。所以我们要理解这个公式背后的逻辑，

而不是去死记硬背，死记硬背的公式很难去应对考试当中呃变化的条件。好，那这是要提醒大家注意的，要理解，而不是死记硬背。好，接下来看。后退原针协议和选择冲撞协议的信道利用率，在理想情况下，这两种协议的信道利用率分析方法是完全一样的。这二者有一个共同点，就是发送窗口的大小要大于一。虽然这两种协议的接收窗口大小不一样。但是在理想情况下，

我们认为发送窗口内的n个数据帧可以被连续的发出，并且被接收方呃顺利的接收。因为不用考虑真丢失，真错误这些问题，所以在理想情况下，这两种协议的信道利用率其实都只受。发送窗口的大小影响和接收窗口的大小没有关系，那为了讨论方便，我们也假设一些前提。我们不妨假设发送窗口的大小是四，那信道的传输速率还有数据帧，长度确认帧，长度以及信道的单向传播时延。呃，和之前的那个假设都是一致的。

那在这种情况下，数据的传输会变成这个样子，可以看到一个数据帧的传输时延，应该是等于四秒。那由于发送窗口的大小是四，所以无论是采用后退n帧，还是选择重传，反正最多可以连续的发出四个数据帧，对吧？那我们不妨把这些数据帧呃编号为零一二三。每一个数据帧的传输时延都是四秒钟，那由于我们讨论的是理想情况，所以这些数据帧接下来都可以顺利的被接收方所接收。好，那么第一个数据帧经过七秒钟的时间之后，

被这个呃接收方所接收。那接收方会立即返回第一个确认帧。这个确认针的传输时延也是一秒钟，而传播时延同样是七秒。那对于数据的发送方而言，当他收到第一个数据帧的ack之后，他的发送窗口就可以往前滑动一格，对吧？也就是说，收到第一个确认帧之后，就可以紧接着发送下一个数据帧。那同样的道理，收到第二个确认帧之后又可以发送呃，再往后一个的数据帧，那后面的过程就是完全一致的。

所以我们要分析信道利用率，我们只需要看呃这样的一个周期就可以了，也就是关注这个灰色的虚线框内。发送信道上有百分之多少的时间是有数据通过的好，那结合这个图示可以得到。发送信道的利用率应该是等于四×4。也就是总计16秒的时间发送信，道上是有数据通过的，这16秒传输了n个数据帧。好，那么从发出呃这一组数据帧到下一组数据帧可以呃被发出。中间所经历的时长就应该是第一个数据帧的传输时延，加上两倍的单向传播时延，再加上这个确认帧的传输时延。也就是这个式子的分母部分，

那计算结果是84%。那么，不妨把这个式子进行整理，同样的，用TD表示一个数据帧的传输时延用rtt去表示两倍的单向传播时延。用ta去表示，确认真的传输试验，那这样的话，信道利用率就应该等于n倍的TD÷TD+rtt再加ta。同样的，这个式子不用记，关键是理解它背后的逻辑，上面这个n×TD表示的是把发送窗口内的这n个数据帧。连续的发出所需要的呃，这个时间而下面分母的这个部分表示的是从发出第一个数据帧。

到接收到第一个呃，这个确认针中间所需要的总号时，因为收到第一个确认针之后。接下来就可以顺利的往后发送后一组数据帧，对吧？之后的过程就是不断的在重复。所以分母我们取了这样的一个值好的，那这是发送窗口等于四的情况，接下来我们把条件改一下，让发送窗口的大小等于五。其他的条件不变好，改变这样的条件之后，大家可以带进去算一下。发送窗口等于五算出来的信道利用率会大于一，但是我们知道信道利用率的定义是指。

呃一条信道上有百分之多少的时间是有数据通过的，对吧？所以信道利用率这个指标，它不可能大于一。因此，生搬硬套这个公式就不太适用了。好来看一下发送窗口变大之后，呃，具体发生了什么？发送窗口大小等于五，意味着可以连续的发出五个数据帧，对吧？那每个数据帧的传输时延是四秒钟。也就是说，这五个数据帧全部发出，

都需要经过20秒的时间。那回头看第一个数据帧。它的ack在20秒之前就会被收到，对吧？也就是说，从这个时间点开始，理论上发送窗口就可以往后滑动。但现实情况就是，在这个时间点这儿，第五个数据帧还没有发完，所以第五个数据帧还需要再继续发一段时间。那只有第五个数据帧发完了之后。才可以接着发后一组的数据帧，那在这种情况下可以看到发送信道上总是有呃，这个数据正在通过。

所以发送信道的信道利用率等于一。好，因此要提醒大家注意这个信道利用率是不能超过一的，在这个公式当中，如果分子的值大于分母的值。那最终我们应该取最大信道利用率等于一。好，这是做题的时候需要注意的地方，接下来看一个例题。一四年的36题。假设主机甲和乙之间使用后退n帧协议传输数据发送窗口的大小是1000。数据帧的长度是1000字节。注意字节是大b。新到的带宽是呃100 Mbps。主机乙每收到一个数据帧，

会立即用一个短帧进行确认，题目明确的告诉我们呃，这个ack帧确认帧的传输时延是可以忽略不计的。如果甲乙之间的单向单向传播时延时50毫秒，那么主机甲可以达到的最大。平均数据传输速率约等于多少？好！根据题目条件，一个数据帧的长度是1000大b。而信道的带宽是100 mbps，所以传输一个数据帧所需要的时间就是零点零八毫秒。注意大臂要乘以八换算成小臂，那如果画图表示就应该是这个样子，一个数据帧它的传输时延应该是等于零点零八毫秒。那由于发送窗口的大小是1000，

所以就意味着呃主机甲最多可以连续的发送1000个数据帧。那我们在这个图当中表示为零到九九九，那发送这1000个数据帧的耗时就是80毫秒，也就是从第一个数据帧。发出一直到最后一个呃数据帧发送完毕，这个过程总共耗时80毫秒。好那么在理想情况下呃，主机乙每收到一个数据帧就会立刻用一个呃短帧进行ack。所以第一个数据帧被接收之后，立即返回一个ack，那么由于单相的传播时延是50毫秒，因此这个ack帧从发出到被接收。中间应该是经历了50毫秒，50毫秒。那同样的道理，

当接收方收到第二个数据帧的时候，它又会立即返回第二个ack，对吧？那理想情况下，从这个时间点开始，发送方就可以连续的发送呃，后面的1000个数据帧对吧？也就是再次重复前边这个周期所发生的事情。好，所以我们只需要考虑这样的一个周期之内呃，它到底传输了多少数据对吧？用这样的一个周期内实际传输的数据量，除以这一整个周期的呃时长就可以得到呃。发送方的最大平均数据传输速率。好，

那么发送的数据总量是1000个数据帧，每个数据帧的大小是1000大b。刚才提到的这一整个周期啊，时长应该是两倍的，单向传播时延，再加上零点零八毫秒。结合这个图示，应该不难理解为什么要这么计算好，那么最后的计算结果应该是80 mbps。所以这题目的答案选择c选项。可以看到，一四年的这个题目，它并没有直接考什么信道利用率这个东西对吧？也就是说，如果我们只会生搬硬套公式。

而没有理解这个公式背后的这个分析过程，那么这个题目肯定是做不出来的，所以最重要的还是要理解我们在背后分析的这种方法和过程。好，那么对于这个题目来说，我们不妨用我们计算出来的这个呃，实际的数据传输速率。去除以这个信道的最大带宽80÷100。用这样的方式也可以算出信道利用率等于80%，所以信道利用率的计算方法有很多。我们可以用呃，实际的数据传输速率除以最大带宽，用这样的方式计算出信道利用率。当然也可以用，我们刚才总结的u=n倍的TD÷TD+rtt。

再加上ta也可以用这样的方式计算算出来的结果是一样的，这两种算法在背后表达的是同一个意思。好，那么这是一四年的36题，好，接下来再看一个题目，一五年的呃35题。说主机甲通过这么快的一一条卫星链路，采用滑动窗口协议向主机乙啊发送数据。给了链路的单向传播时延，给了数据帧的长度。并且告诉我们不考虑确认真的开销。为了确保这条链路，也就是这条信道的利用率不小于80%，问我们帧序号的比特数至少应该是多少？

好，这个题目是有一定难度的，它不仅考察信道利用率，而且还结合帧序号的比特数来。来进行出题好，另外这个题目提到一个术语叫做滑动窗口协议。那此处我们需要给大家补充所谓的滑动窗口协议，它指的是后退n帧协议或者选择重传协议。我们学过停止等待后退n针和选择重传这样的三个协议，那为了便于大家理解，所以在讲解这几种协议的原理的时候。我们把这三者视为都采用了滑动窗口机制，对吧？只不过这三种协议的发送窗口和接收窗口的大小有所区别而已。所以在原理上，

我们可以把这三种协议都视为采用了滑动窗口机制，但是在考试的时候。如果遇到滑动窗口协议这个名词，那你要知道它指的是发送窗口大于一的啊，这两种协议。也就是说，停止等待协议不属于，不属于滑动窗口协议的范畴，他虽然也使用了滑动窗口机制这种原理。好，所以这个术语是需要给大家补充的，那顺道的我们再补充另一个术语叫做arq协议和连续arq协议。所谓的arq指的是automatic repeat request。通常会把arq翻译为自动重传，请求协议自动重传，

这个特性我们在这三种协议当中都有感受到，对吧？无论是哪种协议，都有这样的一个呃规定，就是发送方给接收方发送一个数据帧之后。如果长时间没有收到ack，长时间没有收到ack，那么发送方就会自动的去重传这个数据帧。好，所以这三种协议都有自动重传的特性，因此如果大家在看书或者做题的时候看到说两个节点之间采用arq。那么你要知道，它在背后指代的，有可能是停止等待，有可能是gbn，

也有可能是选择重传。好，那与之相关联的还有一个术语，叫做连续arq协议，为什么叫连续arq？因为后退n帧和选择重传协议。它的发送窗口都大于一对吧？也就是说，在这两种协议当中，如果连续的发出多个数据帧。但是这些数据帧的ack都没有被呃顺利的接收，那么这两种协议会连续的自动去重传多个数据帧。所以如果遇到连续arq协议这个术语，那你要知道它指代的有可能是后退n针和选择重传。停止等待协议啊，

不属于连续arq的范畴好，那这是需要给大家补充的术语，接下来我们回到刚才这个题目，这个题目说主机甲和主机乙之间采用滑动窗口协议。那现在你知道了，它指的是主机甲和乙之间呃，可能使用的是gbn，当然也有可能使用的是选择重传，对吧？那无论是gbn还是选择重传，在理想情况下啊，这两种协议的信道利用率计算的方法都是一样的。题目要求，这个信道利用率不小于80%，所以这个式子最终我们希望它大于等于80%，

对吧？好，接下来我们把这个式子里边的变量进行一个填充，首先这个大n它指的是发送窗口的大小。题目并没有给这个条件，但是我们知道发送窗口的大小，它受到帧序号的比特数影响，对吧？所以只要我们确定了这个发送窗口的大小，当它等于多少的时候可以满足这个条件。那这样的话，我们就可以继续去推算真序号到底应该啊，占多少个比特好，那这个坑我们一会再来填，我们先把后面这几个变量给填上。

啊，这儿的TD指的是发送一个数据帧所需要的时间。那数据帧的长度是1000个字节，1000大b除，以这个链路的带宽幺二八k bps，那计算结果是这样的。好，接下来在分母，这也用到了TD，刚才已经算出来了。rtt指的是两倍的单向传播时延，那题目告诉我们呃，单向传播时延是二五零毫秒。两倍的单向传播时延就应该是零点五秒。好，

最后这个变量ta指的是啊，确认真的发送时延，那这个题目告诉我们不考虑确认真的开销对吧？也就是说确认真非常短。所以ta我们可以忽略不计，因为认为它的发送时延等于零好，现在在这个式子当中。只有大n的值是未知的，其他几个变量的值都是已知的，那解这个不等式得到的结论就是大n应该大于等于七点二。满足这个条件的时候，就可以确保这个链路的呃利用率不低于80%。那大n表示的是发送窗口的大小，这个题目最终是让我们求帧序号的比特数，至少应该为多少？

那在之前的学习中，我们知道gbn协议的发送窗口大于一接收窗口等于一，而选择重传的发送窗口和接收窗口都要大于一。并且无论是哪个协议，它们都必须满足这样的一个条件，当我们用n个比特给针进行编号的时候。我们需要满足发送窗口加接收窗口的大小，要小于等于二的N次方，对吧？好，接下来出现了一些区别gbn协议的接收窗口，大小等于一，而选择重传协议接收窗口是大于一的。所以显然，如果n比特这个n确定的情况下，

那么g比n协议的wt也就是发送窗口可以更大，对吧？我们不妨把接收窗口等于一给代进去，就意味着呃，发送窗口wt+1应该小于等于二的N次方。那刚才我们已经计算出发送窗口的大小，应该大于等于七点二。那发送窗口的大小不可能是一个小数，对吧？肯定是一个整数，所以这个式子意味着发送窗口的大小至少应该等于八。那我们不妨把八代进去，八加一小于等于二分之n。这里的n也必须取整数，所以这个式子推出的结果就是n要大于等于四。

因此，真序号的比特数至少应该等于四这个题目选择b选项。好，那么在这个题目当中，我们要强调这个结论。当我们用n个比特给针进行编号的时候，gbn协议的发送窗口肯定要比呃选择重传协议的发送窗口更大。对吧，因为选择重传协议的wr要更大，那相应的在满足这个式子的条件下啊，选择重传协议的wt就会更小。在这个题目当中，无论是gbn协议还是sr协议，反正当n=4的时候肯定都能满足题目要求的这些事情。但如果我们把条件稍微改造一下，

我们就可以体会到呃刚才强调的这个结论，它的作用我们不妨把这个80%呃变小一点。我们把它变成75%好了，反正就是把这个要求变低一点，那这样的话，我们推出的这个大n，他可能大于等于七就可以了。改成75%之后，是不是大于等于七？这个我不确定啊，反正就是我们假设改了条件之后得到的结论是。发送窗口要大于等于七，那基于这样的条件，如果采用gbn协议。那么gbn协议应该满足发送窗口，

加上接收窗口要小于等于二的N次方，对吧？要满足这个条件。那推出来的结果就是n大于等于三就可以，也就是说如果采用gbn协议，那么我们只需要用三比特给呃针进行编号就OK。好，现在再来看sr协议，我们要确保发送窗口的大小，至少要等于七，再加上接收窗口的大小。要小于等于二的N次方，那我们知道选择冲穿协议，它的接收窗口肯定是要大于一的，对吧？

所以接收窗口的大小至少应该等于二。那七+2小于等于二的N次方，那这个式子推出来n应该要大于三才对。也就是说，如果采用选择重传协议，并且要保证发送窗口的大小大于等于七的话。那么，我们至少要用四个比特给针进行编号，所以如果这个题目的条件稍微做一下更改。那么，采用gbn协议和采用sr协议得到的这个结果，结论就是完全不一样的。好，所以通过这个真题要理解这一点，好的，

那么在这个视频中，我们探讨了三种协议，在理想情况下，信道利用率等于多少？其实，信道利用率的计算本身并不复杂，但是从刚才那几个真题当中可以看到，这个部分的考察还是可以做的比较综合的。停止等待协议的信道利用率考察，相对来说会简单一些。那后退n帧和选择重传协议，就很喜欢结合用n比特给帧编号。这个知识点进行考察，当题目对信道利用率做出限制的时候，本质上就是对发送窗口做出了一定的限制。

那么，发送窗口的大小又受限于n比特，就是给帧编号的这个比特数。同时，当我们采用gbn协议或者呃选择重创协议的时候，因为二者对于这个。接收窗口的大小要求不一致，所以采用这两种协议的时候，我们去判断这个比特数也需要做出一定的调整。同样用n个比特给帧编号的时候gbn协议的发送窗口会更大，相应的gbn协议的信道利用率也会更高。好，另外在这个视频中，我们也补充了几个大家看书做题的时候可能会遇到的术语，分别是滑动窗口协议，

它指的是gbn协议或者sr协议。那arq协议包含了呃停止等待后退n帧和选择重传，这三个协议而连续arq。指的是后退n帧和选择重装协议好的，那大家可以再结合这个思维导图来回顾一下，那以上就是这视频的全部内容。
