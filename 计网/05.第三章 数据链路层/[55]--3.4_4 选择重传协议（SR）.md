各位同学大家好，在这个视频中，我们要探讨选择重传协议，那选择重传协议和停止等待协议以及后退n针协议有很多相似的方面。选择重传协议的英文缩写叫sr selective repeat，它的滑动窗口大小有这样的特点，首先发送窗口和接收窗口都要大于一。其次，它在滑动窗口上有一个特殊的要求，就是接收窗口不能比发送窗口大，一会我们再解释为什么要这么规定。好，另一方面，和停止等待协议后退n帧协议类似，如果我们用n个比特给啊帧进行编号，

那么必须满足wt+wr小于等于二的N次方要满足这样的一个条件。好，另外在确认机制方面。首先，由于选择重传协议，它的发送窗口的大小呃是大于一的，所以发送方可以连续的给接收方发送多个帧。在后退n针协议当中，接收方如果连续接收到多个正确针，那么可以采用累计确认的这种方式。也就是只返回最大的正确帧的帧号就可以，这是后退n帧协议，而对于选择重传协议来说，它并不支持这种累计确认的方式。必须每收到一个正确针，

就返回一个ack。好，那这一点是选择重传协议和后退原真协议不同的地方，另一方面在选择重传协议当中，如果数据的接收方收到的这个帧。检测出差错，那么接收方会丢弃这个帧，并且给发送方返回否认帧。用否认针nak主动的去请求发送方重新发送I号针。那这一点也是选择重传协议，和之前的两个协议不同的地方，之前学习的两个协议在重传机制方面只有超时重传这种规则。也就是发送方超时没有收到acki的时候再重传I号帧，而选择重传协议当中，这个重传机制除了支持超时重传这种。

规则之外，还支持请求重传，就是会由数据的接收方主动的用nak这种否认帧请求发送方去呃重传I号帧。这样的话，发送方就不用非得等到这个超时再进行重传，可以让重传的效率啊，进一步的得到提升。好，那这是选择重传协议的一些要点。接下来，我们会结合一些例子，带大家感受这些要点。首先，我们来探讨啊，选择重传协议的这个窗口大小的限制条件。

那假设我们用三个比特给这些针进行编号，所以这些针的编号就是先从零再到七，然后再回到零。一二三四五六七用这样的方式给这些帧编号，那接收方这边也是一样的，给每个帧用零到七再回到零这样的方式进行编号。好，那么刚才我们说在选择重传协议当中，窗口大小的限制有这样的两个条件，必须满足。首先第一个条件。如果用n个比特给帧编号，那么呃发送窗口和接收窗口的这个总大小要小于等于二的N次方。第二条件，接收窗口的大小要小于等于发送窗口的大小，

那么对于刚才给的这个例子，如果我们用三个比特给帧编号的话。我们可以取发送窗口呃，大小等于四接收窗口的大小等于三二的三次方等于八，满足第一个条件发送窗口和接收窗口的大小之和小于等于八。同时，也满足第二个条件啊，接收窗口要小于发送窗口，所以如果这么设计，那么就符合选择重传协议的这个窗口大小的两个限制条件。好，接下来假设我们把接收窗口的大小改为四，那么此时同样满足条件一和条件二。那这种设计也是合法的。好，

再接下来，如果我们把发送窗口的大小改为三，那么此时不满足条件二，我们说接收窗口必须小于或者等于发送窗口的大小。那由于不满足第二个条件，所以这样的设计是呃不被允许的好，我们简单解释一下为什么要规定接收窗口一定要小于或等于发送窗口？回到前面这个例子，接收窗口等于三发送窗口等于四，那在这种情况下，发送方显然可以连续的发送四个帧，对吧？连续的发送四个帧。那么，接收方在接收到第一个帧的时候，

它的这个接收窗口是不是就可以往右？开始往右滑动了，对吧？那只要往右滑动一格，接下来后面的这三个帧陆续到达，也可以被顺利的接收。那这是接收窗口小于发送窗口的情况，我们可以充分的利用发送窗口和接收窗口的这个容量。好，再回到这个例子，在这个例子当中，接收窗口要比发送窗口更大，那在这种情况下，发送方最多只能呃，给他先发三个帧，

对吧？那么，接收方连续接收到这三个帧之后，它又会给发送方依次返回这三个帧的ack。确认的信息那么只有发送方接收到这些确认信息之后，发送方的窗口才可以向右滑动。只有窗口向右滑动，它才可以继续发送后一个，也就是三号帧，所以在刚才这个例子中，我们可以感受到。如果接收方的窗口大于发送方的窗口，那么接收方更大的这些小格子，它就会长期保持为空利用率不高。所以如果接收窗口大于发送窗口的话，

那么这两个窗口的性能就没办法得到呃完全的发挥。好，所以在选择重传协议当中，我们必须满足第一个条件和第二个条件。好，那么在实际应用当中呃，选择重传协议的。发送窗口和接收窗口通常会取相等的这种情况。比如说用三比特给帧编号，那么我们可以取发送窗口和接收窗口的大小都等于四，在这种情况下，两个窗口相互配合效率会更高。好，接下来我们用几个事例来感受，选择重传协议的工作过程。

发送窗口和接收窗口的大小都等于四。那么，此时发送方可以给接收方连续的发送零号，一号，二号和三号这几个帧。好，那么接收方会陆续的收到这几个帧，并且要给发送方返回这几个帧的ack。同时呃，接收窗口可以向右滑动。那可以看到接收方每收到一个正确针，都要给发送方返回，这个正确针所对应的ack。那相比之下，在后退n帧协议当中，

接收方可以累积确认，如果是后退n帧协议，像刚才这种情况，连续收到零一二三这几个帧的话。那么，后退n帧协议可以只返回ack 3对吧？好，那这一点大家需要注意，两种协议的确认机制是有一些区别的。好，我们让故事继续发送方，接下来会陆续的收到ack零一二三那么确认了，这几个帧都已经被正确的接收之后，发送方的发送窗口就可以向右滑动。那接下来发送方就可以发四五六七这几个帧。

下面我们看一种异常情况的处理，假设在接下来这个发送过程当中产生了数据帧的丢失。那么情况就是这样的，假设接下来发送了四五六七这几个帧啊，每发送一个帧也会给这个帧设置计时器。也就是一个倒计时。假设其中的五号针因为通信线路的故障而呃丢失了。那这样的话，接收方就只能接收到四六七这几个帧，那么收到这几个帧之后，接收方的这个接收窗口可以向右滑动一格。与此同时，把四六七这几个帧的ack啊返回给发送方，那么发送方接下来会收到四六七的ack。在这种情况下，

发送窗口只能往右滑动一格，因为五号针此时还没有被正确的接收。好，此时由于零号帧已经在发送窗口之内，所以可以发送零号帧，并且设置零号帧的计时器。那随着时间的推移，五号针迟迟没有收到ack，所以五号针的计时器会超时。那在这种情况下，发送方会重传五号帧，并且重置五号帧的呃计时器。好，接下来接收方就可以收到零号针和五号针，并且顺利的把这两个针接收。

好，现在接收窗口可以向右滑动，与此同时，接收方给发送方呃返回ack 0和ack 5。那这一次ack 0和ack 5都可以在超时之前被发送方收到。那确认这两个帧被接收之后，发送窗口就可以继续向右滑动好，那刚才这个例子当中展示了数据帧丢失的时候选择重传协议会如何处理？主要就是依靠超时重传机制，每个帧被发出的时候都会设置一个计时器，如果计时器超时还没有收到。啊，对应的ack那么就重传这个帧。好，那这是数据帧丢失的处理，

接下来看另一种情况，我们退回到前面这一步，接下来我们假设接收方可以收到数据帧。但是，由于检测出数据帧的错误，而把数据帧主动的丢弃，那在这种异常情况下，选择重传协议又是如何处理的呢？那在这个图示当中，现在发送方可以呃，连续的传输四五六七这几个帧，并且每个帧被发出的时候都会设置一个计时器。那现在假设所有的针都可以被接收方收到，但是五号针因为检测出差错而被呃，这个接收方丢弃。

那此时接收窗口，只可以往右移动一格。那与此同时啊，接收方会给发送方。返回四号，六号，七号针的ack以及五号针的nak。nak指的是否认帧，就是告诉发送方说，哎，五号帧出问题了，你给我重新传一个就是这样的一个作用，那nak是negative acknowledge的缩写。acknowledge是确认的意思negative是呃否定负面的意思，那英文缩写取了nak这几个字母。

好，接下来发送方会陆续的收到四号针的确认五号针的否认和六号，七号针的确认。当收到四号针的确认之后，发送窗口就可以向右滑动一格。那与此同时，由于收到五号针的否认，所以发送方会在这个计时器超时之前。主动的把这个五号针重传一遍。那重传五号帧的同时也会重置五号帧的这个计时器。好与此同时，由于发送窗口刚才已经向右滑动了一格，所以零号针此时也落入了这个发送窗口之内。因此，零号针也可以。

呃，紧接着被发出。那发出零号针的同时，也要设置零号针的计时器，接下来接收方可以顺利的收到五号针和零号针。那这一次重新收到五号针之后，我们假设并没有检测出差错，那么五号针就可以被正确的接收。好在这种情况下，接收窗口就可以向右滑动四格，那由于是先收到五号针，所以会先返回五号针的ack。紧接着收到零号针，那再返回零号针的ack。那发送方收到ack 5之后，

滑动窗口可以向右移动。在收到ack 0之后，滑动窗口可以继续往后移动。好，那刚才这个例子当中，我们展示了，如果数据帧因为出现差错而被主动丢弃的时候会进行的处理。在选择重传协议当中啊，除了超时重传机制之外，还引入了请求重传机制。如果接收方发现一个有差错的帧，那么呃，除了丢弃这个帧之外，还会主动的给发送方返回这个帧的nak。主动的去请求发送方重传这个I号帧，

那这样的话，发送方就不必等到呃计时器超时再重传，而是收到否认帧就立即重传，这样的话就可以提升选择重传。协议的效率。接下来我们再看第三种异常的情况如何处理。我们假设在接下来的例子当中确认针有可能会丢失。好回到最初的起点，现在发送方可以连续发送零号，一号，二号，三号帧接收方也可以顺利的呃，陆续收到这几个帧。那根据收到这些帧的顺序，接收方会给发送方呃陆续的返回ack 0 ack 1 ack 2和ack 3。

那现在情况发生了，假设ack 1和ack 3因为通信故障而半路丢失了，那发送方就只能收到ack 0和ack 2。那在这种情况下，发送方的发送窗口只能往右滑动一格。好往右滑动之后，现在四号帧又落入了这个呃发送窗口之内，所以可以发送四号帧。好与此同时，如果一号针和三号针的这个计时器超时了，那么发送方就会重传一号针和三号针。重传的同时也会重置这两个计时器。好，那么回到接收方这边，首先接收方收到了呃，

这个四号针那四号针刚好也落在这个接收窗口之内。所以四号帧可以被正确的接收，并且会给发送方返回ack 4，与此同时，这个接收窗口可以在向右滑动一格。好紧接着，接收方还会收到一号和三号数据帧，那么由于这两个数据帧落在了接收窗口之外。所以接收方会认为这两个数据帧肯定是重复的，那收到这种重复帧的时候，接收方会给发送方返回这两个重复帧的ack。好，那么这一次确认真并没有在中途丢失，所以发送方可以顺利的收到ack 4 ack 1和ack 3。那此时发送窗口就可以往右滑动。

好，那刚才我们探讨了确认真丢失的时候，选择重传协议是如何处理的？首先确认真丢失会引起发送方超时重传。超时重传之后，又会导致接收方收到重复帧。那接收方判别重复帧的这个方法就是看这个帧是否落在了接收窗口之外，如果在接收窗口之外，那么就说明它肯定是一个重复帧。每收到一个重复帧接收方，就会给发送方呃再一次返回这个重复帧的ack。那用这样的方式就可以解决确认帧丢失的问题。好，那么接下来我们要探讨这个窗口的大小，如果不满足呃，

这个条件会有什么问题？假设帧序号是三个比特，现在我们故意违反这个规定，我们让发送窗口等于五接收窗口等于四。二者之和，大于八也。就是不满足这一条件，好在这种情况下，我们看会发生什么？首先，发送方可以给接收方发送零一二三四这几个帧。那接收方首先会收到零号针，只要零号针被正确的接收，那接收窗口就可以向右滑动一格。与此同时，

给发送方返回ack 0。好，那么接下来一二三四这几个帧也会陆续的到达，那接收方也会陆续的接收到啊，这几个帧。与此同时，接收窗口向右滑动，滑到五这个位置。好，那么接下来意外发生了假设，因为通信故障ack 0到四全部都丢失了，那么发送方这边零一二三四这几个帧的计时器会陆续的超时。超时之后，它又会重传零号，一号，

二号，三号，四号帧。此时，零号帧的重传就出现了问题。按照刚才这个例子，我们接收窗口往右滑动之后，零号是在接收窗口之内的。所以当接收方收到零号针的时候，他会认为这个零号针啊，是合法的，并不是重复针。那此时接收方就会错误的接收零号帧，那对于一二三四这几个帧，它们是落在了这个接收窗口之外。

所以接收方会认为这几个帧是重复帧。好，那么回到刚才这个问题，如果不满足这个条件，那么就会导致发送方和接收方无法正确的判别，哪些是重复帧？好，那么这就是必须要满足这些条件的原因，好的，在这个小节中我们介绍了选择重传协议。它和停止等待协议后退n正协议有很多相似的地方，那比较特别的点，我用这种呃红色的这种框框给它框起来了。选择重传协议的确认机制比较特别，当接收方收到正确帧的时候会返回这个帧的ack，

而当接收方收到一个错误的，有差错的帧的时候。会返回nak否认帧。用nak否认帧来主动的请求发送方重传对应的这个I号帧。好，另外，在选择重传协议当中，要注意这个条件，接收窗口不能大于发送窗口的大小。那选择重传协议，最需要注意的要点就是，首先是刚才说的nak否认真用nak来主动的请求重传。另外，我们强调选择重传协议，不支持累计确认必须一真一确认，

这点是和后退n帧协议不一样的。好的，那么这就是选择重传协议。
