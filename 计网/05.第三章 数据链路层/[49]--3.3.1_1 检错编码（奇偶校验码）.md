各位同学大家好，从这节课开始，我们会探讨数据链路层的差错控制功能，那差错控制功能的主要目标是要发现并且解决一个帧内部的。位错误，我们需要使用特殊的编码技术去发现帧内部的位错误，那当我们发现位错误之后，通常来说有两种解决方案。第一种解决方案就是接收方只负责发现这种比特错，如果一个针有位错误，那么就直接把这个针丢弃。并且想办法通知发送方，让他重新传输这个帧那关于如何通知发送方重传真这个问题，我们会在。啊，

可靠传输那个功能里边再进行探讨，在差错控制这个部分，我们先把注意力放在如何发现比特错误上。那使用检错编码技术可以发现呃比特错，我们会学习奇偶校验码和crc，也就是循环冗余校验码这两种检错编码技术。好，那么除了直接把针丢弃重传之外啊，还可以有第二种解决方案，就是接收方。可以发现并且纠正真内部的比特错误。那如果要纠正这种比特错误，我们就需要使用纠错编码的技术。那之后我们会探讨海明校验码，这就是一种纠错编码。

既可以发现比特错，同时还可以纠正比特错。好，那么在这个视频中，我们先探讨最简单的奇偶校验码。我们会首先介绍奇偶校验的这种校验原理，如何检测出比特错误？紧接着，我们会为跨考的同学补充异或运算的一个规则，那很多同学考的都是四零八。在四零八统考中学过祭祖的同学，对于异或运算肯定是很熟悉的，但是考虑到有一些自命题的同学可能不学祭祖。所以在这个视频中，我们依然需要对异或运算进行一个简单的补充，

因为除了奇偶校验之外，接下来要学习的crc校验码以及海明校验码。都需要使用到异或运算好，那接下来看奇偶校验码的校验原理，那奇偶校验具体来说可以分为呃，两种，一种就是奇校验，另一种是偶校验。二者的原理类似假设有n个有效信息位，那我们会在这n个信息位之前或者之后。增加一个比特的这种奇偶校验位，如果我们采用的是基校验的规则，那么我们添加这一个比特之后。需要确保这n+1位当中总共有奇数个一。那相应的，

如果是偶校验的规则，那么我们需要确保添加了这一位之后，整体来看总共需要有偶数个一。好，举个例子，假设这儿给出了两个比特串，我们分别求啊，这两个比特串的鸡叫页码和偶校页码。我们假设这个校验位是添加在有效信息位之前，那如果我们采用的是鸡校验的规则。呃，那先来看第一个比特串，这个比特串总共有七个呃信息位。其中包含一个一两个一三个一四个一，我们需要在这七个信息位之前增加一个校验位。

并且要确保整体来看总共有奇数个一，那么原本有四个一，因此这个校验位我们就得添一。这样的话，整体就有五个一，也就是奇数个一。好，再来看第二个比特串，同样有七个比特的信息位，那么其中包含一个两个三个四个五个一。好，所以前边这个校验位，我们只需要添零。那这是采用鸡校验的规则，如果采用的是偶校验规则原理，

类似前边这个比特串，总共有四个一。那添加这个校验位之后，需要确保整体来看总共有偶数个一，因此这个校验位我们只能添零。那对于第二个比特串，总共有一个两个三个四个五个一，所以为了使整体拥有偶数个一，我们需要在校验位，这再添加一个比特一。那这就是基于偶校验规则的呃，校验位的确定。在计算机网络中，发送方给接收方发送一个帧，那么发送方的数据链路层。

会在帧的数据部分添加这个校验位的信息，然后把校验位和信息位一起发给接收方。而接收方的数据链路层又会基于奇偶校验的规则去检查整个帧有没有出错。好，那么接收方的数据链路层是这么来检错的，如果发送方和接收方呃共同约定使用机校验的规则。那么，接收方的数据链路层收到这个针之后会检查呃，这个针的校验位和信息位。里边含有的呃比特一是不是奇数个？如果是奇数个，那么就认为没有错误，如果不是奇数个，就认为有错误。那在现实应用中，

偶校验要比鸡校验更常用。一些原因是偶校验很容易用简单的异或门来实现。我们简单介绍一下异或运算的规则，异或运算又称为摩尔加运算，如果两个比特进行异或运算。那么当且仅当两个比特都相异不同的时候，运算的结果才等于一。比如说这儿的第二个式子零和一两个比特相异，它们俩异或运算得到的结果是一第三个式子一和零两个比特相异，所以它们俩异或。得到的结果等于一。那相反，如果说两个零进行异或得到结果是零两个一进行异或得到结果也是零。那这就是异或运算的一个规则。如果我们把呃几个比特的信息位呃全部一起来异或就可以得到偶校验位。

比如我们以这一串比特串为例，刚才我们用手算的方式得到了呃，它的偶校验位等于零。那如果用机器计算偶校验位，我们只需要把这七个比特依次进行异或好，首先一和一异或等于一。那么，这个一和后一个零异或同样等于一这个一和后一个一再进行异或等于零。那这个零和后一个一继续异或等于一好，这个一再和后一个零进行异或等于一。最后还有一个一再进行异或最后的结果就是零好，所以把这七个信息位。呃分别异或最终计算的结果是零，那可以看到这个计算的结果和我们刚才呃首算求得的偶校验位是一致的。所以如果要用硬件来求偶校验位会非常快，

只需要用异或门就可以实现。好，那第二个例子也是类似的，我们把所有的这七个信息位啊，分别异或最终得到的结果就是一和我们首算求得的偶校验位是相同的。那这就不再追述。那这就是数据的发送方要做的事情，它会把真的这n个信息位。分别进行异或求得一个偶校验位，然后把这个偶校验位加入到信息位的前面。当然，也可以加入到信息位的，后面这个都OK，那么发送方会把这个校验位和恩比特的信息位。呃，

一起传给数据的接收方，而数据的接收方，它的数据链路层会对收到的这n+1个比特进行偶校验。那进行偶校验，也可以用异或运算来实现，接收方会把所有的比特呃依次进行这个异或运算。那我们总共收到八个比特，大家可以自己算一下这八个比特进行异或得到的结果等于零，那么此时说明这八个比特并没有发生错误。那这个式子对应的是啊，这个比特串八个比特都没有发生错误，好现在假设数据的接收方接收到的是啊。后面的这八个比特，并且这八个比特当中最后一个比特一跳变成了零。那此时对所有的比特进行异或运算，

得到的结果就是一那结果，唯一说明出现了比特错误。我们可以用人类视角数一数，总共有几个一一个，一两个，一三个，一四个，一五个一。接收方收到的这八个比特当中，包含奇数个一那，这并不符合偶校验的规则。所以我们从人类视角来看，也能发现呃，这八个比特当中肯定有某些比特出现了错误。好，

接下来再看一个例子，同样是发送这八个比特，假设最后的两个比特发生了跳变。都是从一变成了零，那么此时数据的接收方对这八个比特进行异或运算之后得到的结果等于零。结果为零，意味着啊，这一串比特符合偶校验的规则，对吧？我们从人类的视角来数一下，里边包含一个一两个一三个一四个一。有偶数个一，因此这串二进制数是符合偶校验的规则的。然而，我们从上帝视角可以知道，

这一串二进制数当中，有两个比特发生了错误。因此，如果有偶数个比特发生错误，那么数据的接收方将无法检测出这种错误。那刚才我们主要介绍了偶校验的一个呃硬件实现的原理呃，对于基校验来说，硬件实现原理也是类似的，同样可以通过异或运算去实现。对于计算机网络的考试来说，我们只需要能够用人类的算法确定这个校验位等于多少，以及能够分辨遗传二进制数。是否符合校验的规则就可以了，大家不需要深究机器，实现原理好的，

在这个视频中，我们介绍了一种最简单的检错技术。旧校验码我们提到了信息位，校验位这两个概念。信息位指的就是真内部的有效数据。而校验位在有的教材中又会把它称为冗余位，因为校验位是我们呃，为了给真的数据部分纠错或者检错而附加的一些冗余比特信息。好，那么对于奇偶校验来说，校验原理很简单，我们只需要在呃n比特信息位的首部或者尾部添加一个比特的校验位。如果数据的发送方和接收方约定的是机校验规则，那么就需要确保添加了这个校验位之后。整体来看，

总共会有奇数个一，而如果采用偶校验的约定，就需要确保添加校验位之后。整体来看，有偶数个一。那需要注意的是，这种奇偶校验码只能检测出奇数位的错误，如果刚好有偶数个比特发生了，这种比特跳变。那么，奇偶校验码是没办法检测出这种呃错误的。同时，奇偶校验码只能检错，不能纠错。好，

那么在这个视频中，我们也为跨考的同学补充了异或运算，又叫摩尔加运算的。这个运算规则。当两个比特进行异或运算的时候，只有二者相异的时候，计算的结果才等于一，否则为零。当我们采用偶校验规则的时候，数据的接收方会把所有的信息位和校验位全部进行异或如果异或的结果等于零，那么说明没有错误。如果等于一，那么说明有错误。当然，如果恰巧有偶数个比特发生错误，

那么此时数据的接收方呃，这个异或的结果也会等于零。也就是说发现不了偶数比特的错误。好的，那么以上就是这些视频的全部内容。
