各位同学大家好，在这个视频中，我们要探讨数据链路层组帧的原理。我们说数据链路层在网络层的呃下方，它为网络层提供服务，网络层会把IP数据报，也就是分组。交给数据链路层，请求数据链路层，然后传送给下一个相邻的节点。那下一个相邻节点的数据链路层，需要从收到的帧当中恢复IP数据报的原始数据。然后把IP数据报交给第三层的实体进行下一步处理。那么，数据链路层把网络层交给它的这些数据打包成帧的这个过程，

就是所谓的组帧，也就是封装成帧。考研大纲里边使用的是组针，这个说法好，那么要把数据封装成真，主要要解决两个问题，第一个问题就是真定界。也就是我们应该想办法让数据的接收方能够确定真的界限。因为我们知道物理层在进行数据传输的时候，是以比特为单位来传输的，如何从一系列二进制串当中？区分出真和真的边界呢？这就是真定界要解决的问题。好，那么为了完成真定界。

数据的发送方会把这个呃网络层的这些数据进行一系列的处理，比如说增加首部，增加尾部。同时，还有可能对这个IP数据报呃里边的一些比特进行一些变换处理。因此，接收数据的这个节点，它的数据链路层。除了区分针和针之间的边界之外，它还需要从一个针内部还原出IP数据报的原始信息。并且递交给第三层网络层进行处理，这就意味着网络层和网络层实体之间感知不到数据链路层对IP数据报。做的这些啊，处理和逆处理，也就是说组帧的这个动作对于网络层而言是透明的，

看不见的。好，所以实现真定界的同时，我们也要实现透明传输，用大白话讲，就是说呃数据的接收方要能够去除真定界的附加信息。把帧的数据部分，也就是IP数据报给恢复原貌，这就是透明传输好，那么接下来我们会介绍字符计数法，字节填充法。零比特填充法和违规编码法这四种组针的方法。好，首先来看第一种组帧的方法，字符计数法，

这种方法的思想很简单。假设灰色的这些部分是帧的数据部分，那么我们会在一个帧的首部这个地方。用一个固定长度的计数字段去表示帧的总长度，比如我们可以用八个比特，也就是一个字节。去表示整个针的长度。那么，这八个比特翻译成十进制，就应该等于七，也就意味着这一整个帧的大小是七个字节。那需要注意的是，真的数据部分本身只有六个字节，而这个地方我们记录的总长度是包含首部这一个字节的总长。所以需要把这个技术字段的长度给加进去，

那显然这个技术字段的比特数会影响整个帧的呃最大长度。比如八个比特可以表示的无符号数范围就应该是零到255，所以如果我们只用一个字节去表示帧的总长度，那么整个帧的最大长度就不能超过255。好另一点。值得一提的是，采用这种字符计数法啊，有一个最大的缺点，任何一个计数字段出错，都会导致后续所有的帧都无法定阶。比如在刚才这个例子中，原本第一个字节的值等于七，现在假设中间的这个比特发生了跳变，从一变成了零。那么，

第一个字节的值就从七变成了三，在这种情况下，数据的接收方。会误以为第一个帧的总长度是三个字节，同时会把第四个字节。视为第二个帧的长度，那这串二进制数对应十进制的27，这也就意味着数据的接收方会认为。从这个字节开始，往后的27个字节属于第二个帧，那可以看到，由于第一个帧的长度信息啊，发生了一个比特的错误。就使得后面所有针的这个边界都被打乱，就像多米诺骨牌一样，

第一块骨牌倒了，后面的骨牌也全部会被呃推倒。所以这种字符计数法的健壮性不太好好的，那么这是第一种组织的方法，字符计数法。好，接下来我们看第二种组帧的方法，字节填充法。这种方法也很简单，我们会约定两个特殊的字符，用soh和eot这两个字符。分别去标记一个帧的起始和结束的位置。那这两个字符都属于ASCII编码的呃，控制字符。soh的意思是start of header，

也就是首部的起始，这个意思它对应的。ask编码就是呃七个零+1个一。如果用16进制表示，那就是零一h。那相应的eot的全称叫做end of transmission，表示传输结束。那它在ask编码当中对应的二进制就是五个零，一个一再加两个零，如果用16进制表示就应该是。零四h。好，总之soh和eot属于ask编码里边的控制字符。好，现在新的问题发生了，

当我们使用这两个特殊的字符作为真的开始和结束标记的时候。如果一个帧的数据部分里边刚好包含soheoh这两个特殊字符。那么，是不是就会导致真定界出现错乱？比如绿色的这个是uh字符，那么数据的接收方是不是会误认为？呃，前三个字节属于第一个帧，对吧？但事实上不是这样的，这个eoh字符它属于帧的数据部分。好，那么为了解除这种误解，我们可以引入转义字符。就是esc这个字符esc的全称是escape character，

就是转义字符的意思。在ask编码当中，esc字符啊，对应零零零一一零一一，如果用16进制表示应该是对应1 bh。好，可以看到，在帧的数据部分呃，绿色的这个是eoh字符，蓝色的这个是soh字符。只要数据部分包含这两个特殊的字符数据的发送方，就会在呃他们之前插入一个转义字符esc。那这样的话，当数据的接收方检索到一个esc转义字符的时候，他就会知道啊，

这个转义字符后面跟着的并不是控制字符。而是一串普通的数据，于是数据的接收方可以把这个转义字符给呃剔除。同时也不会把后面这个字符误以为是啊，真的开始或者结束。好，所以插入转义字符可以解决对eoh和soh的这种误解。好，现在问题看似解决了，不过当我们引入转义字符之后，又有可能出现。数据部分包含转义字符的这种情况，比如说中间的这一个比特，它刚好就是转义字符esc。那么，

当数据的接收方检测到这个转移字符的时候，他是不是也会把这个转移字符删掉呢？如果是这么处理的话，显然也会有问题。好，这个问题的解决也不难，可以采取同样的思路，如果在一个帧的内部包含这种转义字符，那么我们。也可以在转义字符的前面再插入一个转义字符。那对于数据的接收方来说，它可以从第一个字符开始往后检索，如果发现soh。那么就意味着这是一个真的开始。好，

继续往后解锁。当发现一个转义字符的时候，就可以知道这个转义字符的后面。它属于普通的数据，而不是一个控制字符。好，所以把这个转义字符去掉，把后面的这一个字节原模原样的接收下来，紧接着继续往后检索。如果又发现一个转移字符，那么同样的数据的结束方会认为，在这个转移字符的后面，这一个字节是属于数据部分。因此，接收方的数据链路层可以把前面这个转移字符呃删掉，

并且原模原样的保留后一个字节。好同样的道理，继续往后检索。每当发现一个转移字符的时候，就认为这个转移字符的后面。属于数据部分，需要把它原模原样的接收，同时把前面这个转移字符给去除。好，那这就是字节填充法，如果真的数据部分包含特殊字符，也就是包含开始结束和呃转义这几个字符的话。那么，发送方的数据链路层会在这些特殊字符的前面，前面填充转义字符esc。

然后再把这些比特发给数据的接收方，那数据的接收方需要做的就是逆处理，那处理的方法刚才已经详细的说过。好，那这就是第二种组织方法，字节填充法，接下来介绍零比特填充法很简单。我们约定以一串特殊的二进制串作为真的开始和结束。比如，在实际应用当中，通常会用一个零六个一再加一个零这样的八个比特。作为一个帧的开始和结束标记，那显然这种方案会面临和上一种方案呃类似的问题。如果在这一个帧的内部刚好有一个零六个一，再加一个零的呃，

这种呃比特串。那么就会让数据的接收方误以为这是一个帧的开始或者结束，但我们知道这些比特属于帧的数据部分。好，那么为了解决这个问题，发送方是这么做的，它会扫描帧的数据部分，每当遇到连续的五个一。就会填充一个零。好比如说从左往右扫，扫到这个位置的时候，出现了一个一两个一三个一四个一五个一。好，每当发现连续的五个一，我就在第五个一的后面填充一个比特零。

好同样的道理，继续往后扫描，后面这儿也会遇到一个一两个一三个一四个一五个一。那么，在这五个一的后面，又需要添加一个比特零好，那么用这种方案填充了零比特之后。就可以确保一个真的数据部分不可能和开始结束的这个比特串一样。因为开始和结束的这个比特串，它会有连续的六个一对吧？会有连续的六个一。而我们刚才对帧的数据部分处理的规则是从左往右扫描，只要扫描到连续的五个一，我们就强行填入一个比特零。也就是说，

把这些数据处理完之后，我们可以确保这个数据部分最多只会出现连续的五个一，而不可能出现连续的六个一。那这样就可以保证数据部分不会出现开始和结束的这种特殊比特串好，那么对于数据的接收方来说，它的处理也很简单。当数据的接收方检测到一个零+6个一，再加一个零的时候，他就知道这是一个真的开始。当这一串二进制数再次出现的时候，他就知道这个帧已经结束了好，那么对于帧的内部数据。接收方的处理方法是从左往右扫描，每当遇到连续的五个一，就会把后面紧跟的这个零。

给删除。每当遇到连续的五个一，就会把后面紧跟的这个零删除，也就是做了发送方的逆处理。这样就可以实现透明传输，我们可以确保数据的接收方能够把这个帧的数据部分恢复原样。好，那这就是零比特填充法，这种方法在现代计算机网络当中非常常用，因为这个处理过程很容易用硬件，很快速，很高效的去实现。有助于提升数据链路层的处理效率。那数据链路层有两个协议的名字，大家需要记一下呃，

一个叫hdlc协议，另一个叫ppp协议。这两个协议的组征使用的就是零比特填充法好，那么关于这两个协议的具体原理，我们现在先不展开。现在我们只需要知道这两个协议使用的是零比特填充法来组针就可以了，好那么接下来我们介绍最后一种组针方法叫做违规编码法。好，这是真的数据部分假设在物理层，我们采用曼彻斯特编码去传输二进制比特串。那么，根据I triple e标准的曼彻斯特编码，我们需要观察每一个时钟周期的中间点。如果中间点是上跳，那么表示这是二进制零下跳，

表示一好，比如说这个帧的前四个比特是零一零零。那么，观察这个周期，它是上跳表示零第二个周期，下跳表示一第三个周期，上跳表示零，上跳表示零。那这是前四个比特，再来看整个帧的后三个比特是三个一，那么最后三个周期下跳，表示一下跳，表示一下跳，表示一。好，

那么中间的这些比特串都是同样的道理。基于曼彻斯特编码的规则，每一个时钟周期的中间点一定会发生信号的跳变，要么上跳，要么下跳。而如果中间点没有发生呃，这种信号的跳变。就意味着不符合曼彻斯特马的规定，也就是一个违规的信号好，那我们可以利用这一点。比如，在传送真的第一个比特之前。先用一个时钟的违规信号去表示，哎，这是一个真的开始，

那当一个真结束的时候，我们也可以插入一个违规信号去表示，这是一个真的结束。那显然，这种方法需要物理层的配合，发送方的数据，链路层会把整个帧的数据原模原样的交给物理层。那物理层会在这些帧的前面和后面分别插入一个时钟周期的违规信号。那这样的话，数据的接收方的物理层也可以根据这个违规信号去分辨出一个针的边界在哪？好，那这就是违规编码法，这种方法在现代计算机网络当中也非常常用好的，那么这个视频中我们介绍了组针的原理。我们说数据链路层组帧主要要解决两个问题，

第一就是要让数据的接收方能够区分出帧的边界在哪。第二，就是要能够让数据的接收方去除真定界的附加信息，把真的数据部分恢复原貌。我们介绍四种方法，分别是字符计数法，字节填充法，零比特填充法和违规编码法。其中，字符计数法会在每一个帧的开头，用一个定常的计数字段。去表示整个帧的总长，那需要强调的是帧的总长度是包含计数字段的长度，再加上帧的数据部分的长度。这点做题的时候容易出错，

大家需要注意。好，那么第二种字节填充法在这个方法中，我们引入了三个特殊的字符，分别是标记帧开头的soh字符。标记真结尾的eot字符以及转义字符esc。如果真的数据部分包含这三个特殊字符，那么就需要在每一个特殊字符前面填充一个转移字符。这是发送方的数据链路层需要做的事情，那接收方的数据链路层要做的就是逆处理，也就是要把转义字符给去掉。好，那第三种方法零比特填充法，我们会用一个特殊的比特串一个零+6个一，再加一个零作为真的开始和结束的标记。

那数据的发送方会检索帧的数据部分，每当遇到五个连续的一就填充一个零。这样就可以确保在帧的数据部分不可能出现这种开始和结尾的特殊标记，那数据的接收方要做的就是逆处理，每当遇到连续的五个一就删掉后面紧跟的一个零。那同时，我们要再次强调hdlc协议和ppp协议采用的就是零比特填充法。好，最后我们还介绍了违规编码法，这种方法需要物理层的配合，物理层会在每个帧的开头和结尾。插入一个不符合编码规则的违规信号，那接收方的物理层就可以根据违规信号去判断每个帧的边界在什么位置。好的，那么这就是四种常见的组成方法，

内生就是这一小节的全部内容。
