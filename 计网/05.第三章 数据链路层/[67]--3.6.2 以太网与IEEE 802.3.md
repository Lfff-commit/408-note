各位同学大家好，在这个视频中我们要展开探讨，以太网和I triple e八零二点三那八零二点三这个工作组主要负责的就是以太网技术的标准化工作。之前我们说过，八零二委员会把数据链路层拆分成了逻辑链路，控制子层和介质访问控制子层，这样的呃两个部分。介质访问控制子层英文缩写叫mac子层，而逻辑链路控制子层英文缩写叫llc子层。那八零二点三这个工作组，他研究的以太网技术呃探讨的范畴是物理层和max子层这两个部分。那接下来我们先探讨八零二点三工作组啊，推出的一些物理层的啊，相关的技术标准。然后再去探讨max层啊，

相关的一些技术标准好，首先看物理层的一些技术标准。这两张表格来自于王道书上面这张表格。总结了I triple e八零二点三刚开始啊，那几年推出的一些比较重要的这种物理层的以太网标准。包括以同轴电缆作为传输介质的10 base，50 base 2，以及以双绞线作为传输介质的10 base t。还有以光纤作为传输介质的10倍f。那这几种经典的以太网技术啊，它的速率都只能到10 mbps，只能到这样的一个速度。后来又出现了高速以太网，如果一个网段的带宽可以超过100 mbps，那么就可以称为高速以太网。

那下面这张表展示的就是啊，几种高速以太网，并且在这张表格当中罗列了这些高速以太网的啊速率。以及传输介质，我们要重点关注的是这些以太网技术标准是否支持全双工通信或者半双工通信。那只有使用半双工通信的这种呃，以太网技术标准才需要去使用csmacd协议去解决信道征用的问题。如果工作在全双工模式下，就不需要使用csmacd协议。好，那关于各种技术标准到底支持全双工还是半双工还是二者都支持给大家做了一个呃，梳理和总结。很简单，只要使用同轴电缆，那么肯定只能支持半双工通信，

而如果使用双绞线作为传输介质。那么，当这个双绞线的速率小于二点五gbps的时候。速率在这个范围内的技术标准是可以支持半双工和全双工两种模式的。我们知道一根双绞线，它只能连接两个节点，只能连接两个节点，那么用双绞线连接的两个节点。到底是使用半双工还是全双工通信呢？这件事情是在节点相互连接的时候。进行协商的，比如说如果用双绞线，其中一端啊，连接了一台计算机，另一端连接了一个集线器。

那么，由于集线器这种设备，它只能工作在半双工模式下，所以如果双绞线的一端连了集线器。那么，集线器和连接的节点啊，经过协商之后就会决定接下来会使用半双工模式去通信。而一旦使用半双工模式去通信，就肯定需要用csmacd协议去啊实现这个。介质访问控制。好，再举一个例子，假如我们用一根速率小于二点五gbps的这种双绞线啊，去连接一个计算机。另一端连接了一个交换机，

那么交换机通常都支持这个全双工通信。因此，当这两个节点连接起来的时候，二者会相互协商。如果两个节点都支持全双工通信，那接下来二者的通信。就会采用全双工的模式，这样的话就不需要再使用csmacd协议。好，接下来如果某一个技术标准，它的速率大于等于二点五gpps，那么这种类型的双绞线就肯定只支持全双工通信。所以你会看到在高速以太网这张表格里边啊，这种。速度为100 mbps的双绞线以太网，

它既支持半双工，也支持全双工。而如果说1 gbps的啊，这种双绞线它也是支持半双工和全双工，但是大于二点五gbps的。啊，这种技术标准，比如说10 gbps的这种双绞线，以太网它就只有全双工模式。好，所以双绞线的这种啊，技术标准是以二点五gbps作为一个分水岭。好，最后还有光纤这种啊，传输介质这种传输介质即便是十倍f就是只有10 mbps的。

呃，光纤它也只支持全双工通信，只要是I triple e八零二点三工作组推出的。关于光纤的这种啊，技术标准肯定都只支持全双工通信。所以在这个表格当中啊，这儿可能会让大家产生一些误会，就是双绞线或光纤可以支持半双工和全双工方式。如果说使用一个1 gbps的光纤作为传输介质的话，那么光纤连接的两个节点之间肯定是工作在全双工模式下。也就是不需要使用csmacd协议。那光纤以太网，通常来说考的不多，所以大家重点关注呃，同轴电缆和双绞线以太网。

好，下面这儿基于刚才的总结给大家啊，画了几个图，首先这是一个同轴电缆以太网。同轴电缆和同轴电缆之间通常是用中继器进行连接。中继器的两端对应的是两个不同的网段，而从这张图当中可以看到，不同的网段。可以采用不同的这种物理层技术标准。比如说最左边这个网段使用的就是十贝斯五，而中间这个网段使用的就是啊，十贝斯二这种技术标准。那刚才我们说过用同轴电缆实现的，这种网段只支持半双工通信。也就是说，

只要一个节点，在发送数据，那么其他的节点就暂时不能发送数据，否则会产生呃信号的冲突。好，这是同轴电缆以太网的特性。呃，接下来再看一个双绞线，以太网的例子。当我们使用双绞线作为传输介质的时候，一般会配合交换机和集线器去连接各个节点。以后我们在做题的时候可以默认，只要是交换机连接的终端节点，注意我这说的是终端节点，也就是指计算机这一类的节点。

呃，都可以工作在全双工模式下。所以你看这台交换机和这四台电脑直接连接，它们之间用双绞线作为传输介质都可以工作在全双工模式下。但是交换机和集线器啊，连接的这个端口由于集线器，它只支持半双工通信。所以交换机和集线器之间的这个数据通信，也只能在半双工模式下进行，也就是说这两个节点。在使用中间这根双绞线传输数据的时候，还需要采用csmacd协议。CD协议去争抢这个双绞线，这个信道。因为工作在半双工模式下嘛。

好，另外集线器这种设备连接的其他电脑啊，其他的节点都只能工作在半双工模式下。好，这是双角线，以太网那光纤以太网，由于不是考试的重点，所以这儿就不再赘述。反正大家在做题的时候，能够分析出啊，这个节点和节点之间到底是采用半双工通信还是全双工通信就行了？好，那刚才我们探讨的是八零二点三工作组呃推出的一些以太网的物理层技术标准。接下来，我们要探讨mac层，

也就是介质访问，控制子层的啊标准，那mac层的技术标准主要有两种。第一种是dec英特尔和施乐这三个公司联合推出的dix。ethernet以太网v2标准，而这个标准也是现在市面上最常用的呃技术标准。那基于这三个公司推出的这个以太网v2标准I trip e八零二点三工作组。抄了一份小小的作业，然后把这个技术标准改了一些小小的细节，然后就推出了I trip oe八零二点三的标准。总之，这两种技术标准其实差异不大，并且现在市面上流行的反而是这个v2标准，所以我们重点关注前者。接下来，

我们重点探讨这两种标准，在呃以太网针的格式上面有什么区别？先来看一个总图。这是v2标准的帧格式。这是八零二点三标准的真格式，你会看到，其实只有这个部分这个部分。的含义不一样。再看一眼，这是vr标准。这是八零二点三标准。好，那接下来我们先展开探讨v2标准的以太网mac帧，它的各个部分有什么含义等，最后再来解释八零二点三的这个字段。

和v2标准的这个字段为什么含义不一致？好，首先一个以太网mac帧分为一二三四五这样的五个部分。每个部分占多少个字节，分别是什么含义？这个是需要记住的，考试会考。所以给大家编了一个记忆口诀，叫六六二n四收发斜数验。六六二n四分别指这五个字段分别占多少个字节？第一个字段占六个字节，第二个字段占六个字节，第三个字段占两个字节，第四个字段。它的这个长度是可变的，

所以我用n来表示。最后一个字段占四个字节。好，这是六六二n四的意思。其中n的范围是四六一千五，最少46字节，最多1500字节。好，这是六六二n四。那我们知道数据链路层为网络层，也就是这儿标注的这个IP层服务，那网络层会把呃要传输的IP数据报。也就是IP分组。IP分组交给数据链路层，让数据链路层把IP数据报封装成一个帧。

然后再把这个针交给物理层传输到下一个节点。那这我们会看到在这张图式当中，第二个层次它并没有写数据链路层，而是写了mac层。那这是因为八零二点三工作组把数据链路层拆分成了llc子层和mac子层。但是由于llc子层不太争气啊，现在已经不存在了，所以在现在的网络世界当中，逻辑上看mac子层。它的上面已经没有llc子层了，而是mac层直接为网络层服务。所以在现在的网络世界当中，你可以认为mac层就等价于数据链路层。好，所以在这个图示当中mac层的上面就直接是网络层，

也就这标注的IP层。好，接下来我们关注前两个字段六六的含义。mac真的前六个字节表示的是目的地址，也就是要说明这个mac真要让谁接收。那我们知道网络当中每一个节点都会有自己的MAC地址，并且每个节点的MAC地址都是唯一的。48比特。也就刚好六个字节，所以如果a节点要给b节点发送一个。mac针。那么，它需要在这个真的前六个字节处。写明b节点，也就是接收方的MAC地址。

好，那接下来在后面这六个字节处，我们又需要说明这个针是谁发送的，也就是要指明发送方。a节点的MAC地址好，所以前两个六六分别对应收发，也就是谁接收谁发送。好，接下来需要注意的是，如果我们把这个目的地址全部写为一。48比特全为一，那么就表示这个mac帧是一个广播帧，那一个广播帧会被局域网内的所有节点所接收。那一会儿我们再来探讨广播帧的传播问题，好，

接下来我们把目光放到二这个地方，这两个字节表示的是网络层协议。什么意思呢？刚才我们说过mac层的上面就是网络层，为网络层服务。那网络层有很多协议，比如说IPV 4或者IPV 6。那mac层传播的这个帧到底是为哪个协议去服务的呢？或者换一个角度来解释，如果数据的接收方mac层收到了一个帧。那么，他收到的这个针。IP数据报拆出来之后，到底应该交给呃网络层的哪一个协议去处理呢？要交给IPV 4处理。

还是要交给IPV 6去处理，我们得说明清楚，对吧？这个IP数据报应该交给网络层的哪个协议？好，所以这两个字节对应的是协，指的是网络层的协议。好，那讲到这个地方，我们可以去解释v2标准和八零二点三标准的呃，这个字段的含义区别了。vr标准是现实中的标准。mac层的上方直接就是网络层，所以需要在这个地方指明我服务的对象是网络层的哪个协议？需要指明这个东西。

那对于八零二点三标准的这个mac帧来说，中间这两个字节，它表示的是数据部分的长度。实际有多少个字节表示的是这样的一个信息，难道八零二点三标准不需要考虑这个数据部分？应该交给网络层的哪个协议吗？其实不是的，我们说八零二委员会设计的这个层次呃，划分当中。mac层的上方还有一个aac层，对吧？所以基于他们最初的这个设计，其实mac层。并不直接为网络层提供服务。直接和网络层对接的，

应该是llc子层。所以当收到一个数据帧的时候，到底要把这些数据交给网络层的哪个协议？在I triple e的这种层次划分当中，是由lac子层去决定的，所以mac子层。不需要关心我到底是为哪个网络层协议服务这件事情，交给lc子层去解决就行了。因此才会出现这样的区别。好，那这个字段的区别是我们需要了解的，接下来我们再回到v2标准继续往后探讨。这两个字节指明了网络层的协议。接下来，这个部分就是网络层交给mac层传输的这个IP数据报。

也就是IP分组通过之前的学习，我们知道以太网技术当中，如果涉及到信道征用的话，那么会采用csma。CD协议去实现介质访问控制，而基于csmacd协议的一些特性，我们需要限制。一个帧的最短帧长，另外，为了防止一个节点啊，长时间的占用传输信道，所以我们也需要限制一个帧的最长的长度。因此，在以太网麦克针啊设计的时候，就考虑到了最短针长和最长针长的问题。

数据部分的大小，它的范围是46到1500字节，那如果说这个网络层要传输的IP数据报。太长的话，就需要把IP数据报分片，每一个片不能超过1500字节。那相应的，如果IP数据报太短，比如说一个IP数据报的呃，长度都小于46字节的话，那么我们就需要对IP数数据报进行填充。比如说用比特零去填充，反正数据这个部分不能小于46字节，必须满足这个特性。好，

这是n数据部分n的范围是四六一千五，最后还有四个字节，这四个字节用于差错控制。那在以太网当中使用的是crc循环冗余校验，所以这四个字节就是crc校验码。好，那这就是一个以太网mac帧的完整构成六六二n四收发协数验。那如果我们考虑整个mac帧的长度范围的话，呃n这个部分是可变的，剩下的这四个部分六+6+2再加四。应该是等于18对吧，所以综合来看一个以太网mac帧的啊，长度范围应该是64字节，这是最小的。一直到一五一八字节，

这是一个mac帧的长度范围。好，那接下来mac层把IP数据报封装成帧之后，会把这个帧交给物理层进行传输。那以太网的物理层传输，都是采用曼彻斯特编码。物理层会在以太网帧的前面插入八个字节的前导码。而这个前导码又可以拆分成两个部分，分别是七个字节的前同步码和一个字节的真开始定界符。好首先来观察这个钱同步嘛，这七个字节都是一零一零一零，如此往复交替对吧？相当于什么呢？在以太网当中，如果a要给b发送一个呃mac帧，

那么a节点的物理层。首先呃，会给b节点发送七个字节的一零一零一零。其实相当于给b节点打一个节奏，对吧？相当于a跟b说呃哥们儿，接下来我给你传一些数据。啊，我传数据的这个节奏大概就是一打二打三打四打是这样的一个速度。那b节点就可以根据他收到的这个钱同步码去调整自己接收数据的这个频率。好，那发送了这些前同步码之后，二者的节奏同步了。好，接下来看这个部分真开始定界符。

它的构成是一零一零一零一一。最后两个比特变成了两个连续的一。那这么做的好处就是b节点一直在接收一零一零一零，对吧？那b节点在收到两个一之后。就知道再往后收到的那些曼彻斯特编码表示的就是以太网mac克。啊，实际的那些数据了。所以这一个字节的作用就是啊，作为真开始的应届服。好，接下来继续用曼彻斯特编码去传输这个以太网mac帧，那么如何去表示这个以太网mac帧的结束呢？我们会采用违规编码法，同时传输完一个mac帧之后会留一段间隙。

所以相当于如果a给b呃发送一个mac帧，那么b节点呃连续的收到一些比特，对吧？那如果突然收不到比特了，那就说明。呃，当前的这个以太网麦克帧传输结束。所以数据的接收方完全可以根据流出的这段空隙去判断哦，以太网帧已经传输结束了。好，那除了这段空隙之外，其实也可以采用违规编码法去标记这个真的结尾。那之前我们学过曼彻斯特编码。它的特点是啊，跳零反跳一看中间中必变，

就是每一个节拍的中间，它肯定会要么上跳，要么下跳，对吧？在考研当中比较常见的一种标准是，上跳表示零，下跳表示一好。那么，既然曼彻斯特编码的中间每个节奏的中间一定会发生跳变。那如果说我们在传输曼彻斯特编码的过程当中，信号的中间没有发生跳变，那我们就可以把这种信号视为违规编码。所以如果a给b传输数据的时候，要让他知道哎，这个以太网真已经到结尾了，

那么当这个以太网真的最后一比特。发送完了之后。在这一个比特的后面，我们插入一个呃曼彻斯特编码的违规码，违规码也就是中间没有跳变的这种，这种呃节拍信号。那么，接收方b节点就可以根据违规编码判断哦，已经到了真的结尾对吧？好，那这就是以太网mac真的。结尾定界。好，那么这是物理层传输以太网mac帧的一个原理，没跟上的同学可以暂停吸收一下这张图。

那刚才我们留下了一个坑，目的地址这个部分，如果呃我们填写的是某一个特定节点的MAC地址。那么，这样的帧就是单波帧对吧？它是一对一的传输，而如果目的地址我们写了48比特的一。那么就意味着，这是一个广播帧会被整个局域网内的所有节点所接收，所以接下来我们要探讨单波帧和广播帧在一个局域网内部的啊传播过程。那这也是一个考试的重点，好，我们引入这样的一个模型。路由器可以连接多个网络，在路由器的这一端，

我们用交换机集线器连接了很多节点。那么，这些节点共同组成了一个局域网，而路由器的呃另外两个端口又分别连接了其他的网络。那通过之前的学习，我们知道MAC地址这个概念其实是数据链路层才拥有的东西，对吧？物理层是没有MAC地址这个概念的。那我们知道集线器它工作在物理层，而交换机它的功能包含了物理层。和数据链路层。那既然交换机涉及到了数据链路层，因此交换机肯定也有MAC地址这个东西，对吧？那同样的道理，

路由器啊，需要实现物理层数据链路层甚至网络层的功能，所以对于路由器而言。它涉及到了数据链路层的功能，因此它也会有MAC地址这个东西好，所以在网络当中不只是这种啊，终端节点拥有MAC地址。其实，交换机路由器也拥有MAC地址，甚至交换机路由器这两种设备，它们会包含多个MAC地址。比如一个交换机，可能是这么设计的，每一个网线接口的背后都会有一个网络适配器，而我们知道每一个网络适配器都会有一个MAC地址，

对吧？所以如果交换机，它有多个网线接口，那么每个网线接口都会对应一个MAC地址。路由器也是一样的道理，当然有些情况下可能多个网线接口会共用一个MAC地址。也有这样的设计啊，不过这儿我们就暂且先不展开好，总之这儿我们想强调的是交换机有MAC地址，路由器有MAC地址。还有这些终端节点有MAC地址，但是集线器没有MAC地址，我想强调的是这个东西。好，接下来我们看一个单波帧如何传输，

假设现在a节点要给c节点发送一个单波帧。那么，根据刚才学习的这个以太网针的结构，六六二n四收发斜数验对吧？收发斜数验。那就是说a节点在构造一个帧的时候，最开始的六个字节需要写明接收方，也就是c节点的MAC地址。紧接着，后面的六个比特需要写明发送方，也就是a节点。自己的MAC地址。好用这样的方式构造一个mac克之后，这个mac帧首先会发送给交换机，交换机会检查这个mac帧的。

啊，接收方的地址，那根据接收方的MAC地址，这个mac帧会被转发到相应的端口。好，所以a给c发送一个单播帧，这个动作的背后只有c节点会收到这个单播帧。好，接下来如果a要给f发送一个呃单波帧，那么同样的在这个帧里边。也要写明这是a发给f的，对吧？好，那么这个针同样的也需要先交给呃交换机，那么交换机经过检查发现。

数据的接收方f。它连接的是右边这个端口好，所以它会从这个端口把数据帧转发出去，好接下来对于集线器来说。他不会去判断数据帧的接收方MAC地址到底是多少，他只会无脑的把自己收到的这些曼彻斯特编码，这些物理层的信号。无脑的转发给自己连接的。各个端口，因此a发给f的这个数据帧会被e会被f会被g都给收到。但是只有f这个节点会接收这个帧。因为这三个节点收到针之后都会检查接收方是不是自己对吧，如果跟自己没关系，那么我就直接把这个针给丢弃。好，

这是a给f发送单步针好，接下来看e给a发送单步针，那e发送的针。先传给集线器，那我们说集线器会无脑的，把这个他收到的这些物理层的信号全部转发出去，对吧？所以f和g这两个节点肯定也会收到e给a发送的单播帧好，那么这个帧呃从集线器的左边这儿转发给交换机之后。交换机，它有点脑子啊，它会检查接收方是谁，接收方是a节点，那么交换机就会把这个呃帧这个单波帧从。这个端口转发出来。

所以a节点会收到帧，并且把帧给接收下来好，这是e给a发送好接下来假设e给f发送一个单播帧。那么这个针。到集线器这无脑的转发出去，无脑的转发出去f会收到g也会收到。但是只有f节点会把这个针接收下来好，另外这个交换机也会收到e给f发的这个针。但是检查接收方的地址f它连接的端口刚好就是呃数据进来的这个端口，所以交换机并不会把收到的这个针。重新转发回去，没有必要，对吧？那当然也不会转发到其他端口好，所以e给f发送的帧。

就是fg能收到，然后f会接收这个针好，这是单波针，接下来看两个广播针的例子。我们说六六二n四收发协数验，如果把接收方的地址也就是目的地址全部写为一的话，那么表示这个帧是要广播给局域网内的每一个节点的。好，现在假设a节点a节点发送了一个广播帧呃，这个广播帧的目的地址全部为一对吧？那么，这个广播针首先到了交换机这个地方，交换机检查哦，它是一个广播针，那接下来它会把这个广播针。

转发到呃，其他所有的呃端口，也就是上面这条路会转发这个广播帧b节点。c节点d节点还有集线器，这边都会收到这个广播帧那那对于集线器来说。他会无脑的把这些呃物理层的比特信号。再一次的转发，转发给与他相连的所有的节点，所以a节点发送的广播帧会被bcd。efg还有路由器r会被所有的这些节点所呃接收。那值得注意的是，当路由器接收到一个广播帧之后，它并不会把这个广播帧。转发到其他的网络。不会这么做。

所以我们说只有同一个局域，网内的各个节点才属于同一个广播域，也就是说a节点发送的广播帧只能传播到呃。同一个局域，网内的各个节点对吧？一旦到路由器这一块，路由器就不会再转发到其他的局域网了。就是这个意思，好，那了解了这些之后，我们看二零年的35题，问我们在这个网络当中冲突域和广播域的个数分别是多少个？那冲突域我们之前介绍过，如果两个节点同时发送数据的时候会导致冲突，那么二者就处于同一个冲突域。

那我们知道，只要集线器相连接的节点就肯定只能半双工通信，对吧？也就是说连接同一个集线器的两个节点肯定是属于同一个冲突域。好，这是冲突域的概念，而广播域的概念就是说如果一个节点发送广播帧，可以被另一个节点所接收。那么，二者处于同一个广播域，那通过刚才的学习，我们知道这个以太网交换机，它会呃把广播帧转发到所有的端口。而路由器收到广播帧之后，它就不会再继续转发广播帧，

那显然冲突域和广播域的个数。分别是四和二。这两台电脑连接了同一个集线器，它俩属于同一个冲突域。这儿是一个冲突域，这儿是一个冲突域，这儿是一个冲突域，总共有四个冲突域，那交换机是可以隔离冲突域的，对吧？因为交换机这边收到一个数据之后，他不会无脑的把它转发到另一个端口，不会无脑的转发，他会先检查自己收到的这个数据帧。哎，

有没有必要去转发对吧？所以有了交换机的存在。当左边这台电脑。在发送数据的时候，其实右边这台电脑也可以发送数据，二者是不会冲突的。所以我们说交换机隔离了冲突域，那接下来再分析广播域有几个，我们看如果这个节点发送一个广播帧。一个广播帧那么这个广播帧首先到集线器这儿，集线器无脑的转发，哎，这个节点会收到好，除此之外，这个广播帧也会发到交换机这个地方。

那交换机检查发现，这是一个广播帧，所以它会把它转发到其他连接的端口，那么通过下面这台集线器又会转发给。这两个节点，而对于路由器来说，当它收到这个广播帧的时候，它不会继续转发广播帧好，所以左边这个部分应该属于同一个广播域。那相应的，右边这个部分也属于同一个广播域，因此广播域的数量就是两个，这个题目选择c选项。好，因此我们可以得到这样的结论，

以太网交换机，它可以隔离冲突域，但是不隔离广播域。而路由器这种设备，它既隔离冲突域，也隔离广播域。集线器这种设备，它既不隔离冲突域，也不隔离广播域。这是三种设备的特性好，那这是二零年的35题好的，那么这个视频中我们介绍了以太网与I triple e八零二点三。八零二点三工作组的主要任务是推行以太网的这个标准化工作。我们首先介绍了物理层的一些啊，技术标准。

命名规律是这样的，速率加base加传输介质主要有同轴电缆，双绞线和光纤这三种传输介质。我们需要重点关注的是呃，每一个网段，它可以工作在半双工模式下，还是全双工模式下？当工作在半双工模式下的时候，就需要使用csmacd协议去实现介质访问控制。那在做题的时候，如果看到一个网段是由同轴电缆组成的，那么它肯定只支持半双工通信，也就是需要使用csmacd协议。而如果一个网段是用双绞线啊组成的，那这样的一个网段只能连接两个节点，

对吧？只能连接两个节点，那我们要观察。啊双绞线连接的这啊两个节点当中，如果其中一端有集线器，那么就肯定只能工作在半双工模式下。而如果连接的是交换机，那我们默认工作在全双工模式下，光纤网断只工作在全双工模式下，但是考的比较少，大家有个印象就可以。好，另外以太网的这个物理层，我们使用的是曼彻斯特编码，跳零反跳一看，

中间中必变。那这是我们需要了解的一些物理层标准，那对于mac层的呃技术标准，我们主要关注啊，对于这个mac帧的格式的定义。有两种标准，一种是v2标准，一种是八零二点三标准。vr标准的呃mac帧，它的构成是六六二n四收发斜束音。而八零二点三标准的构成是六六二n四收发常数验。也就中间这两个字节，八零二点三标准表示的是长度，就是数据部分的数据部分的长度。而v2标准的这两个字节，

它表示的是网络层的协议。我到底是为哪一个网络层协议服务的？标注的是这样的一个东西，那中间这个n是可变的数据部分，它的范围是四六一千五。好，所以大家可以把这两句话连起来呃背诵啊六六二n四收发斜数验n是四六一千五。记住这句话，你就记住了，以太网针的组成好，那么以太网针呃交给物理层之后，物理层会在以太网针的前面加八个字节的前导码。八个字节的千刀码包含七同步一定界，其中七个字节是同步码，由一零一零。

交替组成而一定界指的是最后一个字节是真开始的定界符。好，这是八字节前脑码的记忆，七筒布一印结，那在这个视频中，我们也强调了呃，收发协数验这个接收方的地址，如果我们全部写成一的话。表示这是一个广播帧，而交换机会把收到的广播帧转发到其他端口，也就除了接收广播帧的那个端口之外，其他的端口都会被。呃，转发出去。而路由器收到广播帧之后不会转发广播帧，

所以我们说交换机它不隔离广播域，而路由器可以隔离广播域。好的，那以上就是这期视频的全部内容。
