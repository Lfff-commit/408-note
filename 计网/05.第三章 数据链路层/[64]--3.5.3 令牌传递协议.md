各位同学大家好，在这个视频中，我们要探讨用轮询访问的方式去实现介质访问控制。在考研大纲里，轮询访问的这种介质访问控制方式，我们只需要了解令牌，传递协议就可以。好，那么首先介绍一下历史背景啊，在一九八四年IBM公司啊，发明了一种局域网技术，叫做令牌环网技术。那么，这种技术的核心特点就是啊，整个局域网呈现出环形的拓扑结构，

各个节点会轮询访问信道。只有获得令牌的节点可以访问信道，所以这种局域网它不会出现信道冲突的情况。那为了实现这些特性，需要引入一种介质访问控制协议，叫做令牌传递协议。一会我们会展开探讨。好，那当时这个令牌环网技术的最大一个竞争对手就是以太网技术。以太网技术是由施乐公司，英特尔公司还有一个叫做第1c的公司。digital equipment corporation啊，这三个公司联合开发的，当时的以太网一般来说是用一个同轴电缆去连接啊，多台计算机。

再往后出现了集线器去连接多台计算机，但无论是同轴电缆还是集线器，在逻辑上都是呈现出总线型的拓扑。那总线型的拓扑肯定会出现信道冲突，对吧？那之前我们学习的csmacd协议就是在这个时期去解决总线型以太网的这个呃信道冲突问题。那转机出现在交换机这种呃以太网设备的发明，以太网交换机被发明之后，以太网的逻辑拓扑，就从总线型变成了新型的结构。也就是结点和结点之间不会再出现信道冲突，那从此就彻底解决了呃，总线征用的问题。那因为这种方案非常的优秀，所以出现了交换机之后，

令牌环网技术就逐步的推出市场，推出舞台。所以，令牌环网技术以及其中的这个令牌传递协议，其实是一个过时的技术啊。因此，这个部分在考试当中不算重点，并且考评很低好，接下来我们用一个例子带大家感受令牌传递协议的这个工作原理。一个令牌环网呃呈现出这种环形的拓扑，整个网络当中，同一时刻只有一个节点可以获得令牌。而只有获得令牌的节点才可以去啊，传输自己的数据，比如在这个例子当中，

我们引入了四个节点。分别编号为一二三四，那假设现在一号节点获得了令牌。那么，获得令牌的这个节点会持有一个令牌帧。在这个令牌帧的内部，指明了当前获得令牌的节点编号等于一。好，此时如果这个一号节点想要发送数据，那么它会把这个令牌帧转换为一个数据帧。比如说现在一号要给三号节点发送数据，那么把这个令牌帧转换成数据帧之后，大概就会包含这样的一些信息。首先，在数据帧的内部，

也要指明当前获得令牌的节点编号是一号节点。另外，还需要指明这个数据帧要从哪儿发送，到哪儿我们知道，每一个计算机都会有自己的一个物理地址。在令牌环网当中，呃，一个物理地址占48比特，相当于每一台计算机都有一个呃，唯一的不重复的身份证号。那这儿的原地址和目的地址就是要指明发送方的呃，身份证号以及接收方的身份证号。好，那除了这些信息之外，当然也需要把呃真正要发送的这些数据部分，

把这些信息呃附加到数据帧里。那最后还有一个比较关键的字段，叫做是否已接收，刚开始这个字段应该设置为FALSE，也就意味着这个数据帧现在还没有被正确的接收。好接下来的故事是这样的。一号节点会把数据帧先传递给下一个节点。二号节点收到数据帧之后会检查这个目的地址，哎，发现目的地址和自己的呃身份证号对不上，那么他就不会接收这个数据帧。紧接着，他会把这个数据帧传递给下一个相邻的节点三号节点，那三号节点检查目的地址之后，发现这个数据帧是传递给自己的。

因此，它会把这个数据帧的数据部分复制一份就留下来。同时，修改是否已接收这个字段，把它从FALSE改成true，表示这个数据帧已经被接收方顺利的接收。好，那么把这些数据部分留下来之后，他就可以把数据帧转发给下一个节点，那下一个节点同样的也会检查目的地址和自己能不能对上，如果对不上，那么这个数据帧就原模原样的再转发给。下个节点好，那可以看到这个数据帧绕了一圈之后又回到最初的起点啊，回到了最初的起点好，

那回到一号节点之后它会检查是否已被接收这个字段。这个字段等于true表示，此次的发送是成功的，而如果这个字段等于FALSE，那么表示这次发送是失败的。什么情况下，有可能是FALSE呢？比如说三号节点呃，对数据部分进行crc循环冗余校验发现，哎，某些比特出错了。那么，三号节点就有可能会呃，拒绝接收这个呃数据帧。所以如果数据帧在传输的过程当中出现了一些比特错误之类的，

这种异常情况，那么这个数据帧在绕一圈回来之后就有可能依然是FALSE。那如果发生了呃FALSE这种情况的话，数据的发送方就可以尝试重新发送。再让它绕一圈。当然，我们也需要给重发的次数设一个上限，比如说只能重发两次，或者只能重发三次，如果到达了重发次数的上限，那么这个数据帧就会被放弃发送。好，那么在刚才这个例子当中，我们假设数据帧是发送成功的，那当数据帧回到原点之后。

这个源结点就会把数据帧又转换回令牌帧。同时，要把这个令牌的控制权传递给下一个节点，也就是要修改这个令牌号，把令牌号修改为二。那需要注意的是，一个节点获得令牌之后，只能传一个数据帧。传完之后就需要立即释放令牌。换句话说，也可以把它解释为要生成一个新的令牌，因为令牌号已经变了，对吧？好，那么令牌号修改为二之后，

接下来一号节点就会把这个令牌针传递给下一个节点。好，下一个节点获得这个令牌帧之后，检查令牌号等于二。和自己的编号能够对得上，那么就说明这个节点，如果接下来想要发送数据的话。那么，它就可以把令牌帧转换成数据帧，并且呃，发送自己的数据就像刚才一号节点那样。而如果这个节点暂时没有什么数据需要发送，那么它会立即释放这个令牌的控制权。换句话说，就是生成一个新的令牌。

把令牌号变成三，紧接着把这个新的令牌传递给下一个节点，那同样的，如果这个节点。此时暂时没有什么数据需要传输，那么它又会放弃这个令牌，生成一个新令牌，把令牌号变成四。紧接着又把这个令牌再传递给下一个节点。好，总之这个令牌会在整个环形网络当中逐一的传递，如果某一个节点。需要传输数据帧，那么当它获得这个令牌之后，就可以像一号节点那样，

把令牌帧变成数据帧。然后再传输数据帧，那需要强调的是，无论是令牌帧还是数据帧，都只能沿单向传递。比如说在这个例子当中，我们只能逆时针的传递，而不能顺时针倒过来传递。好，那接下来的故事是这样的呃，假设四号节点也没有数据，想要传送，那么它会把令牌呃放弃。再把令牌号改成后一个节点的编号等于一，紧接着把令牌传递给下一个节点。

好，现在令牌绕了一圈，又回到一号节点，如果一号节点又有呃数据要传输的话，那么它又可以把这个令牌针。变成数据帧，然后再用相同的逻辑把数据帧传递出去，数据帧会在整个环形网络当中逐一的传递。那之后的过程就是类似的，我们就不再追述好，那么为了让大家对令牌帧和数据帧的真实样子有一个更直观的认知。我找了一些资料呃，供大家参考，这是一个令牌针的真实构成，总共有巴比特加巴比特，

再加巴比特。其中中间这个部分就是我们刚才提到的所谓的令牌号。令牌号表示，当前网络当中是几号节点获得了令牌的使用权。好，那除此之外，首尾的这呃八个比特分别是真定界的开始，定界符以及结束定界符。那关于真定界的知识，我们在之前介绍过。好，那这是令牌帧的构成，非常简单，那数据帧的构成呃长这个样子。首先呃，

它里边也包含令牌号AC这八个比特access control，这八个比特表示的就是令牌号。要说明当前获得令牌的结点编号等于多少。好，那除此之外SD这个部分和ED这个部分分别表示的呃，也是真定界的开始和结束，这样的两个信息。好，接下来da和sa呃分别是48比特，用于表示原地址和目的地址。sa是source address的缩写。而da是destination address的缩写分别表示，发送方和接收方。好，接下来中间这个部分最多可以包含4500×8呃，

这么多的比特。那这个部分就是真正要传递的数据部分。好，接下来后面这个地方也会有一个我们很熟悉的东西，叫做crc，也就是crc循环冗余校验。总共包含32比特的信息。用于实现差错控制。好，最后尾部这个位置fs这八个比特就是用来标记这个针是否已经被接收，也就是刚才我们提到的呃true和FALSE的这个字段可以这么理解。好，那这两张图展示了令牌帧和数据帧的呃样子，我们大致了解一下，可以让这个部分的呃知识变得更不抽象，

更具体一些。好，那接下来我们还要介绍一个东西呃，同样也是为了让这个部分更不抽象一些。我们画的这种令牌环网，它在现实当中长什么样子呢？其实并不是用一根环形的线把所有的节点连起来，不是这样的。令牌环网的构造需要使用到一种呃设备，叫做mau。比如这台设备就是IBM公司生产的呃mau。中文翻译叫多站接入单元。反正这个东西就是用来构造这种呃令牌环网的。那可以看到它的背后有一二三四五六七八有八个接口，每个接口可以连呃一台计算机。

那么，这种网络设备就是呃，用于集中控制令牌环网。需要这种设备的支持，才可以让令牌环网以及令牌传递协议呃，正常的工作。那一台mau还可以通过ri和ro这两个接口和其他的mau进行连接。那这样的话，我们就可以构造更大范围的令牌环望，比如说左边这张图是用一台mau构建的令牌环望。在这个令牌黄网当中，只有两个节点，两台计算机，那如果说局域网的范围很大，需要连接更多台计算机，

我们就可以用呃mau的这个呃入和出这两个接口。把多个mau串联起来，然后在每一台mau上呃连接多台计算机，那这样的话就构建了一个更大范围的令牌环网。好，那么这个视频中我们介绍了令牌传递协议，令牌传递协议是在令牌环网当中呃，实现这个介质访问控制的核心协议。理解这个协议的关键在于呃，理解令牌帧和数据帧是什么原理？在一个令牌帧的内部，需要指明当前获得令牌的这个节点编号。那节点的编号是由mau去分配的，比如说当我们只使用一台mau的时候，我们可以给一号口连接的计算机编号为一二号口连接的计算机编号为二。

好以此类推，那当然，如果有多台mau呃去连接的话，那么这些mau会用某一种方式给各个节点分配一个唯一的编号。反正各节点的编号肯定是不重复的好，所以通过节点编号这个信息就可以知道当前获得令牌的节点是哪一个。那与此同时，也就是确定了当前呃，可以往信道上发送数据的节点到底是哪一个？只有获得令牌的节点，才可以发送数据。那如果说一个节点，它获得了令牌，但是暂时没有数据发送。那它会直接释放令牌，

把令牌传递给下一个节点，那具体来说就是要把令牌当中的这个呃节点编号呃改成下个节点的编号。好，另外，如果获得令牌的节点，想要传输数据的话，那么它就会把这个令牌帧转换成数据帧。在这个数据帧的内部，需要指明发送方是谁，接收方是谁，以及需要有一个字段去标记这个数据帧是否已被接收。当然，数据帧的内部也需要指明啊，当前获得令牌的这个节点编号到底是多少？那么，

我们的数据帧会从源节点发出，按照一个固定的方向传递。整个数据帧绕了一圈之后，又会回到原节点，数据帧在传递一圈的这个过程当中，每一个收到数据帧的节点都会对比。这个数据帧的目的地址和自己的这个地址是否一致？如果一致，那么这个节点就会把数据复制一份。并且把数据帧标记为已接收。好，那么最后数据帧回到原节点之后，如果原节点发现了某一些异常情况，比如说是否已接收的这个字段标记为FALSE？那么就说明这个数据帧没有被顺利的接收，

此时源结点可以尝试重发。那如果说数据帧的传输没有发生异常情况，那这个源结点就会把令牌传递给下一个节点。也就是说，我们获得令牌的呃节点，每次只能发送一个数据帧，发完之后就要立即释放令牌，如果还有数据帧，想要发送的话，那么需要等到下一次获得令牌的时候再进行。传输好，另外在这个视频中，我们也介绍了呃，用于实现这个令牌环网的集中控制的一种网络设备，叫做mau。

有了这种设备的集中控制，才可以协调各个节点啊，去传递令牌，以及给各个节点分配，这个节点编号等等一系列的事情。好，那么由于这种令牌传递协议的特性，呃，同一时刻只会有一个节点，呃，允许访问信道。因此，使用这种协议的网络不会发生信道冲突，那不会发生冲突就意味着数据帧的重发概率很低。因此，

在这种负载很高的网络当中，它的效率是很高的。那相比之下，在总线型以太网当中，如果使用csmacd协议去实现这种介质访问控制。那么，当这个网络中呃负载很高的时候，冲突的可能性就会大幅增加。因此csmacd协议不太适用于负载高的网络。好，那么大家可以再结合这个思维导图进行回顾，那以上就是这视频的全部内容。
