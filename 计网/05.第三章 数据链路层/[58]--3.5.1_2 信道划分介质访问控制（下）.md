好的，继续来看使用信道划分的方法啊，去实现介质访问控制，在这视频中我们重点探讨码分复用技术。英文缩写叫cdm那先介绍马芬复用技术的这个呃，现实背景啊，在曾经的2G3g移动网络时代。也就是零几年到一零年差不多，我上高中那会儿呃，那个时候我记得我们的手机可以选择网络的格式。GSM或者CDMA。那GSM和CDMA就是两种网络通信的技术，其中CDMA技术在后来的3g时代。也曾经被广泛的使用CDMA技术，可以用于实现节点和节点之间的通信。

那CDMA技术的呃背后，它的底层原理其实就是码分复用技术。英文缩写叫cdm好，为了方便理解这个马芬复用技术，接下来我们引入一个模型。我们假设有这样的三个通信基站，分别叫A站，B站，c站。这几个节点之间使用CDMA技术进行通信。那在这种技术的支持下，A站和B站可以同时给c站发送信号。当然，如果有更多的这个呃站点，比如说d站e站想要呃同时发送数据，

这个也OK都没问题。比如说A站发出了一零一B站发出了一一零这样的三个比特，那两个站点发出的信号。在传输的过程当中，会相互影响，相互叠加。c站会接收到呃，相互叠加的这个信号。但是在马芬复用技术的这个支持下，c站有办法可以把来自各个节点的信号值给分离出来。好，那具体是怎么做的呢？接下来我们展开来探讨一下，那在这给大家做了一个码分复用技术的总结。接下来，

我会结合这张导图去呃介绍码分复用技术，每一步需要做什么好？首先第一件事，我们需要给各个节点分配一个专属的码片序列。那一个码片序列会包含m个码片，也就是包含m个信号值，我们可以理解为m为的向量。同时，这个m为向量的分量通常为了方便计算，会取一或者负一好听起来很抽象，对不对？没关系，我们直接来看例子。假设现在我们用四维的码片序列啊，去做这样的设计给a这个节点分配一个码片序列。

那这个码片序列当中包含四个分量，分别是一一一每一个分量就是一个所谓的码片。四个分量结合起来就是所谓的码片序列，我们可以用线性代数的向量去理解这个所谓的m为码片序列。其实就是m维的向量对吧，而这个向量的每一个分量啊，我们要么取一，要么取负一。那至于为什么要这么取，我们一会儿再来填这个坑好，这是A站点的专属码片序列，那同样的道理B站点和c站点也会有。呃，各自专属的码片序列，并且各个站点的码片序列是不同的。

好，接下来看一下这个码片序列有什么作用？假设现在A站要给c站发送一零一这样的三个比特，那么a这个节点如果想要发送二进制的一。就需要呃发出四个信号值一一一，也就是和它的码片序列保持一致。如果要发送二进制的零，那么就会发出四个信号值，负一负一负一，也就是和它的码片序列啊，刚好完全相反。好，总之我们会用m个信号值去表示一个比特。那对于其他站点也是一样的，假如B站点要给c站点发送一个二进制的一，

那么它应该发出四个信号值一负一一负一。如果要发送二进制零，那么它发出的四个信号值就应该是呃全部取反负一一负一一。如果用线性代数来解释的话，相当于二进制一对应的是m为的这个向量，二进制零对应的是负的m为向量。好，那么在这个例子中，一零一这几个二进制对应的呃信号值就应该是这样的三组。那接下来c站收到了这样的呃三组信号值，每一组有四个分量组成。那在码分复用技术当中，各个站点会提前知道彼此的这个码片序列。也就是说c站点他是知道A站点的码片序列是多少，也知道B站点的码片序列是多少，

那么当c站点收到这样的一些信号值的时候，哎，经过对比发现。一一一，这个是A站点的码片序列，而负一负一负一负一，这个是A站点码片序列的呃取法。所以他就知道啊，这三组信号肯定是A站点发过来的，而且它对应的是一零一这样的三个比特。好，那么回到这张导图，刚才我们说相互通信的各个节点，会知道彼此的码片序列等于多少？好，

另外一点需要补充的是呃，在cdm技术当中，我们要求给各个节点分配的啊，这个码片序列，也就是每个节点的m为向量必须。相互正交。在线性代数当中，我们学过如果a和b两个向量相互正交，那么这两个向量的内积应该是等于零，对吧？好，所以在刚才这个例子当中，大家可以自己暂停来验算一下，就是ABC这几个向量两两内积，结果都是零，

这就意味着这三个向量。相互正交。那为什么要保证这些码片序列要相互正交呢？一会儿你会感受到呃它的作用。好，我们让故事继续刚才这个例子中，只有a正在给c发送数据，接下来我们让情况变得复杂一点。让B站点也同时发送数据，假设在某个时刻a正在给c发送比特1b，正在给c发送比特零。那么B站点发出的四个信号呃值就应该等于向量负b。而A站点发出的四个信号值，应该对应的就是向量a。好，

那么此时对于c站点来说，它会收到两个信号的叠加。也就是要把这两个向量进行一个加和加起来。所以c站此时收到的信号值就是零二零二，这组信号值的数学本质就是向量a加上负的向量b。好，那么接下来。c站点如何从这个叠加信号当中，把a和b发过来的数据给分离出来呢？那这就需要使用到一些数学手段。好，我们先尝试着把节点a发过来的呃，这个比特给分离出来，我们可以这么做。用节点a的。

这个码片序列和我这个接收方收到的这个叠加信号进行一个内积。然后再乘以m分之一，这的m等于四。因为是四维向量嘛。那我们可以把这个运算称为规格化内积好，那么结合线性代数的知识，我们知道啊，由于向量a和向量b。相互正交。所以这个式子应该等于m分之一a向量和a向量的内积。再加上m分之一a向量和负b向量的内积，那刚才说AB两个向量是正交的对吧？所以后面这部分就应该等于零。那这部分去掉就只剩下式子的前半部分。就应该等于m分之一乘以a向量和它自身的内积。

那由于a向量的每一个分量都是一。所以它和它自己的内积应该是等于一+1+1+1。也就等于m，对吧？那再乘以一个m分之一，就等于一。好，如果规格化内积的结果等于一，那么站点c就可以确定节点a给我发过来的，这个比特应该是一。好，接下来看节点b做法也是一样的，我们可以用节点b的这个呃向量去乘以。节点c收到的这个叠加信号。那同样的道理b和a相互正交，

所以二者相乘应该等于零，那最后就应该得到这样的一个式子。那b和负b进行内积，由于向量b的每个分量，要么是一，要么是负一，所以。这个内积的结果肯定是负的m，那再乘以前面的这个m分之一运算的结果就应该等于负一。那计算结果等于负一，意味着节点b发过来的这个比特应该是零。好，我们再整体梳理一遍A站要给c站发送一个比特一，那么它需要发出的信号值就应该呃等于。a这个向量B站要给c站发送比特零，

那么它发出的信号值就应该等于负b这个向量。那a和负b这两个向量，或者说这两组信号值在传输的过程当中相互叠加，那c站收到的信号值。叠加的信号值从数学的角度看，就应该是等于a向量，加上负b这个向量。接下来c站需要从这个叠加信号当中，把a和b两个节点呃发过来的这个数据进行分离。那分离出a节点发来的数据的方法就是用a节点的码片序列和我收到的这个信号值。进行规格化内积运算的结果等于一表示a节点给我发了一个比特一。那同样的道理，要把b节点发过来的，这个数据分离出来，那么c站点需要做的事情就是。

把c站点呃，收到的这个叠加信号和B站点的这个码片序列进行规格化内积。运算的结果等于负一表示B站点发来了一个比特零。好，那没跟上的同学可以暂停消化一下啊，接下来我们再举个例子，如果a和b同时发送三个比特啊。那么A站发出的信号就是向量a，然后负的向量a。再来一个向量AB站点发出的信号，就是向量b。向量b负的向量b。那c站点这边收到的信号就应该是这样的，大家可以自己加一下第一组向量，对应的是a+b第二组，

对应的是负a+b第三组，对应的是a+-b。接下来c站点要从这几组信号值中分辨出节点a发来的是什么数据，那么它会用节点a的码片序列。和这三组向量分别做规格化内积，那第一组和向量a规格化内积等于一。表示这是比特一第二组和向量a规格化内积等于负一表示比特零第三组和向量a进行规格化内积。结果等于一表示，这是比特一，所以c站点就可以确定A站点给我发送了三个比特，分别是一零一。好那么，分离B站点发来的这个数据也是完全一样的道理，这就不再赘述。好的，

那刚才讲解的这个例子其实是来自于二零一四年的真题，第37题。说站点ABC通过CDMA共享链路ABC的码片序列分别是呃，这样这样这样，这些数据和我刚才举的例子一模一样哈。然后c站点从链路上收到的这个序列是不拉不拉不拉，反正就是有四组信号，每组信号有四个分量组成。好题目问我们c站点收到A站点发送的数据是多少？那大家可以代入具体的数值去算一下。用这四组信号值。分别和A站点的这个码片序列做规格化内积，第一组运算得到的结果是一表示比特一。第二组信号得到结果是负一表示，比特零第三组得到结果是一表示，

比特一所以a发过来的数据就是一零一，这个题目选择b选项。那作为训练，大家也可以把B站点发过来的数据尝试着分离一下，反正就是和B站点的这个码片序列做规格化内积嘛。相比于线性代数，要考察的那些内容，这个计算简单多了，对吧？好的，那么在这个小节中，我们介绍了用信道划分的方式去实现。介质访问控制。首先，我们学习了十分复用技术，

十分复用技术，就是会把信道的使用时间拆分成。固定大小的时间片，然后各个节点依次分配一个时间片，轮流去使用。时分复用技术对于这个时间片的分配是不灵活的，因此我们提出了统计时分复用技术会在时分复用的基础上啊，给各个节点按需分配时间片。你的传输需求大，那么我就给你多分配几个时间片，那采用这样的策略就可以大幅提升这个信道的利用率。好紧接着，我们介绍了评分复用技术。评分复用技术的思想，就是会把一个呃信道的总平带拆分成几条互不相交的子平带。

并且子频带和子频带之间会加入隔离，频带用于防止这个子频带之间相互干扰。那每一个发送节点呃，会被分配一条子频带。那类似的原理，当我们对光信号呃进行这种评分复用的时候，我们可以把它称为波分复用技术。因为本质上光的这个波长和光的频率是有关的。所以光的波分复用本质上就是光的评分复用原理和上面这种技术是类似的。好，最后我们介绍了码分复用技术，英文缩写叫cdm，在cdm技术当中，首先我们需要给各个节点分配一个专属的。唯一的这个码片序列，

一个码片序列会包含m个码片，也就是m个信号值。我们可以把码片序列看作是一个MV的向量，那为了方便对这个向量进行处理，也就是为了方便做这个规格化内积。所以我们会规定，这个m为向量的每个分量，要么取一，要么取负一好，另一方面，为了从叠加信号当中分离出。呃，每个发送方呃发来的这个数据，所以我们要求。给各个节点分配的码片序列，

或者说给各个节点分配的这个m为向量，必须相互正交。那这是由向量的数学特性决定的。相互通信的各个节点需要提前知道呃，彼此的这个码片序列。那每个节点有了自己专属的码片序列之后，它就可以开始发送数据，如果要发送比特一，那么就发送和码片序列呃。相同的这个m个信号值，如果是要发送比特零，那么就要发出和码片序列相反的m个信号值。如果多个节点同时发送信号值，那么这些信号会在传输过程当中相互叠加。那信号值的相互叠加，

它的数学本质就是m为向量的加法。好，最后接收方收到这个叠加信号之后，要从叠加信号当中分离出各个发送方发来的数据。那这就需要使用一些数学手段，接收方会用它收到的这个叠加信号与某一个发送方的码片序列。进行规格化内积。如果规格化内积的结果为一表示它受到的是比特一。如果规格化内积结果为负一，那么表示它收到的是一个比特零。好，那么大家可以再结合这页PPT进行回顾，那以上就是这小节的全部内容。
