各位同学大家好，在这个视频中，我们要探讨VLAN虚拟局域网技术啊，我们知道len是局域网的一个英文缩写。然后这儿的v是virtual对应的，就是虚拟。好虚拟局域网在大纲当中处于这个位置，我们的王道书是按照大纲的这些标题顺序来编排的，所以在王道书上以太网的后面。呃，跟的是这个I triple e八零二点幺幺无线局域网，但是我们现在先跳过无线局域网这个部分。因为VLAN虚拟局域网和以太网是呃一脉相承的东西，所以我们把这两个部分的内容连起来学，效果会更好一些。

那VLAN这个部分是二零二二年才加入考纲的，所以属于一个还比较新的考点，那在I trip oe八零二委员会当中。呃VLAN这项技术主要由八零二点一q这个工作组去负责，所以和VLAN相关的呃很多技术标准都是以八零二点一q去冠名的。好，那么在探讨这个虚拟局域网之前，我们先来看一下呃，一个传统的这种大型局域网可能会面临什么问题？我们以大家熟悉的校园网为例，很多学校的校园网就是一整个大型的局域网，在校园网内部会通过呃很多交换机。去把各种节点连接起来，比如说学生的电脑以及学校的数据中心，还有校园网，

服务器等等。那通过之前的学习，我们知道通过交换机连接的各个节点，它们属于同一个广播域，对吧？那这就意味着，在这个网络内，任何一个节点发送一个广播帧。这个广播帧都会被交换机广播到与之相连的所有节点，那经过逐层的广播之后。最终，整个校园网，整个局域网内部的所有节点都会收到这个广播帧好。现在问题出现了，如果这个局域网。

规模比较大，连接的节点数比较多，那是不是意味着在这个局域网内广播帧出现的可能性很高？而一旦广播帧变多，那么整个网络的这个数据传输的负载，以及每个节点接收并且处理广播帧的这种啊，给节点带来的负载都会变得很高。甚至在某些情况下，可能出现大规模的广播风暴，就是整个网络的负载，可能会被广播帧给占满。好，这是一个大型局域网面临的第一个问题。接下来第二个方面也会有安全性的问题，可以看到一个校园网内部可能会有一些节点，

它是高敏感的节点，比如说校园网的服务器。以及学校的数据中心，这些高敏感的节点会暴露在整个局域，网上会被局域网内的其他节点呃探测到。并且很可能受到黑客的攻击。好，这是一个大型局域网面临的第二个问题，就是安全性。那怎么解决这些问题呢？I tripoli八零二点一q工作组提出了VLAN虚拟局域网的这种解决方案。我们可以把一个局域网内部的节点。重新进行分组，把这些节点归为不同的虚拟局域网。接下来，

为了简化探讨，我们引入这样的一个模型。两台交换机连接了多个节点，交换机和交换机之间用一条干线链路进行连接。干线链路通常是呃，更快速的网线，而节点和交换机之间连接的这种普通链路啊，我们用普通的网线就行了。好，那么在现在这个模型当中，所有的节点都属于同一个局域网，对吧？也就是说，所有的节点都处于同一个广播域。并且局域网内各个节点之间是可以相互发现的。

好，那么接下来我们引入虚拟局域网，我们不妨把所有的这些节点分成两个分组。VLAN 10这几个节点把它们划分为同一个虚拟局域网VLAN 20这几个节点，划分为另一个呃虚拟局域网。那这样的话，每个VLAN，每个虚拟局域网就是一个独立的广播域，并且刚才我们提到的VLAN 10 VLAN 20啊。十和20呃，这个参数我们把它定义为vid，也就是虚拟局域网的ID。好，那么在这种情况下，假设a节点发送一个广播帧。

那么，这个广播帧最终只会被呃从属于同一个虚拟局域网的节点所接收，也就是bcd还有hi。这几个节点可以收到a节点发送的广播帧，但是vid=20的这个虚拟局域网，这几个节点就接收不到呃a节点发送的广播帧。不知道大家有没有关注过WINDOWS电脑的一个功能，叫做网上邻居，你在你的电脑上可以看到你的网上邻居，而所谓的网上邻居就是呃，和你从属于同一个局域网的。并且没有隐身的那些设备。那么，划分了VLAN之后，比如说对于a节点而言bcdhi是它的网上邻居，

也就是说a节点可以探测到。啊bcdhi这几个节点的存在。但是efgjkl这几个节点是a节点探测不到的。那这样的话，其实就可以提升一个局域网的呃安全性，对吧？比如说使用VLAN技术，我们可以把一些敏感的节点。划分为同一个VLAN，而其他的这些节点再划分更多的VLAN，只要这些终端节点和敏感节点不属于同一个VLAN。那么，这些终端节点对敏感节点的访问就是受到限制的。好，所以划分VLAN之后，

我们可以把一个很大的广播域拆分成若干个更小的广播域。同时，不同的VLAN是相互独立的一个节点，只能探测到呃和自己从属于同一个VLAN的节点，而不能探测到其他VLAN的节点。从而就提升了网络的安全性。那值得一提的是，呃划分VLAN这个事情需要支持VLAN功能的以太网交换机来实现。普通的以太网交换机是不支持微量功能的。比如，这是一台支持VLAN功能的交换机，它总共有八个接口。交换机的管理员可以去配置如何划分VLAN？好，那接下来我们要展开探讨，

划分VLAN的三种方式，第一种方式我们可以基于接口去划分VLAN。比如，对于刚才这个例子来说，我们可以在交换机一和交换机二的后台进行配置。这是管理员做的事情，做什么样的配置呢？它可以设置两个vid，分别是十和20，也就是说要划分为两个呃VLAN。那对于交换机一而言，vid 10对应的接口号是一二三四。也就是说，只要连接了一二三四这几个接口的呃节点。我会把它划分为VLAN 10。

好，那除了一二三四之外，交换机二这边也会有VLAN 10相关的节点。那么，对于交换机一而言，它是通过八号接口和交换机二进行连接的。所以这儿也需要把八号接口和vid 10进行一个绑定。这么做是有好处的，比如说a节点发送一个广播帧。交换机一收到这个广播针，并且这个广播针是从一号接口进来的。那么，检查vid和接口号的这种映射关系之后，交换机一就能够知道。和一号接口从属于同一个VLAN的节点分别连接在二号接口，

三号接口，四号接口和八号接口。因此，它会从二号，三号，四号和八号这几个接口去转发。a节点发来的广播帧。好，那么对于交换机二来说也是一样的，它也需要绑定vid和接口号的，这种映射关系。当交换机二从一号接口收到一个vid=10的，这种广播帧的时候，它就知道。这个广播帧需要从二号接口和三号接口再转发出去，

那这样的话就实现了广播帧的传输。好，那细心的同学刚才可能会产生这样的一个疑问，一个广播帧从交换机一。传输给交换机二，那交换机二怎么去判断这个广播帧到底是发送给哪一个VLAN的呢？这个问题我们一会会来呃探讨。好，总之我们可以基于接口去划分VLAN交换机的管理员，需要把vid和vid对应的接口号。进行绑定。那么，在这种方案中，如果一台计算机它换了一个连接的接口，比如说I这台计算机，

它不通过呃三号接口连接。而是连到了八号接口。那么I这台计算机就不再属于呃VLAN 10。因为在交换机的后台，它是根据这个接口号去呃划分VLAN的，所以只要连接的接口一变。那么，这个节点它从属于哪个VLAN就有可能发生改变？那这样其实不太方便，对吧？那能不能让一个节点，即便换了接口也可以不改变它从属的VLAN呢？那接下来我们提出另一种VLAN的划分方式，可以基于MAC地址。如果采用这种方式，

那么交换机的管理员在后台配置的就是vid和。各个设备的MAC地址之间的这种映射关系。每个节点都会有自己呃，独一无二的MAC地址。那在交换机一的后台，我们可以把VLAN 10呃的这几个节点，它们的MAC地址。和vid 10进行一个绑定。那这样的话，即便其中的某一个节点，它改了一个接口，只要它的MAC地址不变。那么，这个节点依然从属于同一个VLAN。好在这种情况下，

交换机是怎么工作的呢？比如a节点，现在发送一个广播帧。那么，交换机会检查这个广播帧的发送方是谁？根据发送方的MAC地址去判断这个节点，它从属于哪一个VLAN？好，那既然a节点属于VLAN 10，因此a节点发送的广播帧就应该转发给BC。dhi这几个节点交换机能够知道这些节点分别对应哪一个接口。比如说h和I这两个节点，它们对应八号接口，所以a节点发送的广播帧会通过干线链路传给交换机二。那交换机二又会把这个广播帧转发给h和I，

这两个节点。好，那这是第二种VLAN的划分方式，基于MAC地址。接下来，第三种划分方式基于IP地址。在一个局域，网内部每一个节点都会有自己的IP地址。当然IP地址是属于网络层的概念。那么，和MAC地址类似，我们也可以通过IP地址和vid进行绑定，用这样的方式去实现VLAN的划分。那由于涉及到IP地址，因此这种划分方式需要呃网络层功能的支持，

同时这种划分方式甚至可以让呃VLAN的范围跨越路由器。让多个局域网的主机组成一个VLAN。什么意思呢？比如说交换机二，它连接了一个路由器，而路由器的另一端又连接了交换机，以及连接了多个节点。那之前我们说过路由器的呃，每一个接口对应的应该是不同的局域网对吧，也就是左边这部分。属于同一个局域网右边这个呃小部分又属于一个局域网，那如果我们使用IP地址去划分VLAN的话，我们可以做到说呃，比如说把。右边这个局域网里边的呃某几个节点。

也划分为VLAN 10。那这样的话，我们就把两个局域网内的呃，这些节点组成了一个虚拟局域网。这就是可以跨越路由器的意思。如果要基于IP地址划分VLAN，那么这些交换机它不仅是要实现物理层和数据链路层的功能。还需要实现一部分网络层的功能，就会使得这个交换机变得更复杂好，这是第三种方式。接下来，我们来填一个坑。之前我们说a节点发送一个广播帧，之后交换机会把这个广播帧转发给与之直接相连的bcd，这几个节点。

与此同时，也会把这个广播帧发给交换机二，那么交换机二如何去判断这个广播帧？到底是呃，从属于哪一个VLAN呢？那关键就在这个地方。交换机和交换机之间呃传输的这个帧，它的格式并不是标准，以太网帧的格式，而是八零二点一q帧的格式。八零二点EQ针是在标准以太网针的基础上演变而来的，它会在标准以太网针的中间这个位置。插入四个字节的信息，而这四个字节当中包含了vid的信息，也就是说，

假如现在a节点。呃，发送了一个广播帧，那么交换机一先判断a节点，它的vid等于多少？好知道这个vid=10之后，由于交换机二这边也涉及到呃VLAN 10，所以交换机一。会在这个标准以太网帧的内部插入四个字节，其中包含了vid=10这个信息。把这个针交给交换机二，那么交换机二收到这个针之后。就可以把这四个字节拆出去。同时，去检查vid等于多少，

那只要确定了vid交换机二，接下来就知道。自己应该从哪几个端口去转发这个广播帧了？好，所以主机和交换机之间还是以之前我们学过的那种标准，以太网帧的格式去进行传输。而交换机和交换机之间的这种传输，为了让对方能够知道我这个帧到底从属于哪一个VLAN。所以需要在其中插入关键的vid的信息。因此，交换机和交换机之间的传输是以八零二点EQ这种格式的帧去进行的。好，那么接下来我们不妨呃拆开来看一下这个八零二点一q帧它的构成。标准以太网针的构成是六六二n四收发斜数验，而八零二点EQ针会在标准以太网针的基础上。

增加四个字节在中间。那编了一个新的口诀，六六四二n四收发威斜书验。新增的这四个字节，我们把它称为VLAN标签。因此，在标准以太网帧的这个原地址字段的后面，插入VLAN标签之后，就可以得到八零二点EQ帧。那对于这四个字节来说，它又由这样的几个部分组成。首先，最前面的这两个字节，它是固定死的。这个固定值是指明这个帧的类型是八零二点一q帧。

好，这是前两个字节，在后两个字节当中，前面的四位没有任何意义，写什么值都行。比如说我们可以填入四个零或者四个一都可以，反正它毫无意义，那末尾的这12位关键的信息来了，这12位就是vid。要指明这个帧的发送方，它所属的VLAN vid等于多少？怎么判断发送方所属的vid等于多少呢？这就可以回到之前介绍的VLAN划分方式。在每一台交换机的内部，他肯定能够通过呃自己的这些内部的数据结构去判断他所收到的一个呃数据帧。

发送方到底从属于哪一个vid？好，那这就是八零二点一q帧的构成。主机和交换机之间以标准的以太网帧格式呃进行这个数据的传输，而交换机和交换机之间。会把这个标准以太网帧内部再插入四个字节，那显然。新加入这四个字节之后，末尾的这个crc循环冗余校验码就需要重新生成，对吧？因为循环冗余校验是针对前面这一整个部分去计算的校验嘛，对吧？好的，那么在这个视频中，我们探讨了虚拟局域网在一个传统的大型局域网啊当中，

我们会面临啊，比如说广播风暴以及安全性等等一系列的挑战。所以我们有必要把一个大型局域网内部划分成更小的虚拟局域网。那么，虚拟局域网有这样的一些特点，首先，每一个虚拟局域网构成了一个独立的广播域。同时，我们需要使用呃，支持虚拟局域网的以太网交换机去。进行配置。另外，每一个虚拟局域网都会对应一个vid。那我们介绍了三种划分VLAN的方法，如果要基于接口划分VLAN，

那么在交换机内部就需要记录vid和接口号的一个映射关系。如果要基于MAC地址，那么就是记录vid和MAC地址的映射关系，而如果基于IP地址的话，也是一样的vid和IP地址的映射关系，就需要被记录下来。那最后这种呃VLAN的划分方式可以允许让VLAN跨越路由器，让多个局域网内的主机组成一个VLAN。那最后我们还介绍了八零二点一q帧主机和交换机之间，还是以标准的以太网帧去进行数据的传输，标准以太网帧的构成是六六二n四收发谐数验。而交换机和交换机之间的传输，也就是在干线链路上的传输。我们需要以八零二点EQ帧的格式去进行。需要在六六的后面加四个字节就是VLAN标签的信息，

而VLAN标签的构成是这样的。四个字节VLAN标签总共对应32比特，这32比特被拆分成了16，42这样的三个部分。最开始的16个比特是固定的，写死的，然后中间四个比特没有任何意义，随便写什么都行，最后的12比特。最重要，它表示的是这个帧的发送方属于哪一个vid好的，那以上就是VLAN这个考点。
