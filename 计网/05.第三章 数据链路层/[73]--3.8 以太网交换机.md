各位同学大家好，在这个视频中，我们要探讨以太网交换机的呃功能原理，那以太网交换机属于数据链路层的设备。在大纲当中，处于这个位置。我个人建议，以太网交换机这个部分可以放在以太网与I triple e还有呃VLAN及其基本原理放在这两个小节的后面。就是紧接着来学习，因为它们都是一脉相承的东西。所以把这些内容啊，连起来学效果会更好一些，好那么在二零二二年以前数据链路层的设备。这个部分啊，其实大纲要求我们了解两种设备，

一种叫做网桥，一种叫做交换机。那上面这张图是网桥的样子，可以看到网桥有两个接口，而交换机有多个接口。那交换机本质上就是多端口的网桥。从二零二二年以后，网桥这种设备已经从啊四零八的大纲里边删除了，因为你只要理解了交换机，那肯定也能理解网桥。网桥比交换机反而更简单，那对于一台交换机插网线的这种，这种口子我们可以把它称为端口。也可以把它称为接口，这两种说法不做严格的区分好，

那么这节课中我们重点要了解的是以太网交换机的呃自学习功能。因为有自学习功能，所以以太网交换机支持，即插即用，那这个是考试的重点，也是理解交换机工作原理的一个关键。那除此之外，我们还要了解啊，交换机的两种交换方式，有两类交换机啊，一类是直通式的交换机。另一类是存储转发式的交换机，那一会儿我们也会展开来探讨。那在之前的学习中，我们已经体会到交换机的一个作用了，

它工作在数据链路层为节点转发帧。并且可以根据一个真的目的MAC地址去呃进行相应的转发。以及交换机的每个端口啊，通常都支持全双工通信，除非连接的是集线器，那当交换机的某一个端口和集线器连接的时候。就只能工作在半双工模式下，那这些知识我们在之前的学习中啊，都有介绍过，这儿就不再赘述，那接下来我们直接进入啊，交换机的重点就是自学习功能。交换机是如何根据一个真的目的MAC地址去选择？应该从哪个端口转发出去的呢？那这就涉及到一个很重要的呃，

内部数据结构叫做交换表。每台交换机都会维护呃，至少一个交换表初始为空交换表，记录的是MAC地址和端口号的一个对应关系。好，我们直接结合这样的例子来理解，这有两台交换机。每台交换机内部都会维护自己的交换表。每个交换表记录的是MAC地址和端口号的对应关系。好，我们下面这呃有很多终端节点，有的终端节点直接连接了交换机，而有的终端节点是先连接集线器，再通过集线器去连接交换机。那我们知道呃，

一个标准的以太网帧，它的格式是六六二n四收发写书页。前面两个六分别指的是接收方的MAC地址和发送方的MAC地址，那每个MAC地址是六个字节。也就是48比特那为了让下面这个例子更具体一些，我们不妨假设ABC一直到a2这些节点的MAC地址分别呃，如这儿所示。MAC地址通常会用这种16进制的方式来表示，比如说f8 ff，这些都是16进制。那我们知道每一个16进制位会对应四个比特的二进制，那大家可以自己数一下这些16进制刚好对应48比特的二进制。好，那么所有的这些节点呃，我们假设他们的MAC地址前面的这些16进制都是一样的。

只有最后一个16进制位啊，有所区别。那方便起见，接下来当我在呃书写每一个节点的这个MAC地址的时候，我只记录末尾的这两个16进制位。比如说a节点，我只会写9a。d节点我只会写9d。j节点我只会写九四。好，接下来看这个例子，两台交换机，它们的交换表刚开始都为空。我们假设这些节点，它们之间知道彼此的MAC地址。

关于节点之间如何知道彼此的MAC地址，这个我们会在呃后面的学习当中了解到，反正现在我们先假设这样的一个前提条件就是各个节点知道彼此的MAC地址。好，那么第一个帧是a发给c的。那在这个帧里边就包含了接收方的MAC地址和发送方的MAC地址。发送方是a节点，接收方是c节点。好，这个针首先传给交换机，那由于这个针是从一号端口这进来的。所以这台交换机就可以知道。这个发送方a它肯定是连接在端口一这儿的。因此，交换机收到这个帧之后，

它就可以把呃这个帧的发送方地址。也就是a节点的mac地址，什么什么9a这一串更新到这个交换表上。并且记录它进来的端口号一。好，接下来由于这台交换机暂时不知道c节点。它的这个MAC地址到底对应哪个端口？所以没办法，它只能把这个帧从啊，除了入口之外的，其他的所有端口都转发出去。相当于把它当一个广播帧去转发。好，因此bcd还有和集线器相连的efg。甚至交换机二这边的hi。

以及jkl都会收到。一这个针。而只有c节点检查这个接收方的地址和自己能够对得上，所以只有c节点会接收这个帧。其它节点会直接把针丢弃。好，那刚才这个过程当中，这个一号针也会呃广播给交换机二对吧？那么对于交换机二来说。它同样会检查这个针到底是谁发送的。并且把这个发送方的MAC地址和它进来的这个端口号。给更新到交换表，这所以交换机二收到这个帧的时候，它也会把a这个节点的MAC地址什么什么9a。就这串和进来的端口号一。

把这个信息进行登记。好，这是一号帧的传输。接下来，二号针是b给c发送的，那么二号针是从二号接口这进来的。所以交换机一就可以知道二号帧的发送方，也就是b这个节点，它肯定连接的是二号端口。所以，根据二号帧的发送方的MAC地址，把它和端口号二进行一个记录。好，另外二号帧的接收方是c这个节点，而c这个节点的MAC地址此时还没有登记到交换表上，

也就是说交换机一现在还是不知道c到底在哪？那没办法，它就只能用啊，广播帧的那种形式去处理，就是把二号帧转发到所有的与之相连的这些端口上。因此，所有的这些节点都会收到二号帧，但是只有c节点会接收二号帧。好，那么刚才这个过程当中，交换机二也收到了二号帧，同时在二号帧内部记录了发送方，也就是b节点的这个MAC地址。所以交换机二也能够知道b这个节点肯定是连接在端口一这边的，于是登记b和端口号的对应关系。

好，接下来看三号针，三号针是c传给a的，那么三号针会通过三号端口啊，传给交换机一。发送方是c节点，接收方是a节点。好，那交换机每收到一个帧就会把这个帧的发送方的MAC地址和这个帧进来的端口。进行一个登记绑定，那这一次发送方式c节点，它的MAC地址是什么？什么9c？进来的端口是三号端口。好登记，

好信息之后，交换机一检查接收方的MAC地址。那由于接收方式a节点。这个MAC地址在我的交换表当中，是可以找到的，对吧？所以交换机一就可以非常精准的。只从一号端口这转发帧。好，那么a节点收到帧之后可以顺利的接收。好，这是三号针。接下来看四号针h发给a的好，四号针会通过交换机二的二号端口啊。发进来发送方是h，

接收方是a，那这台交换机先登记这个发送方的MAC地址和他进来的这个端口号。那我们看这个h的MAC地址是什么什么什么九二那它进来的端口是二号端口？好，接下来交换机检查接收方是谁？那么由于接收方的这个MAC地址在交换表当中可以找到。所以交换机二会精准的从一号端口把这个帧转发出去。那么，一号端口转发给交换机一之后，那交换机一同样的，它首先登记这个帧的发送方。的MAC地址，还有它进来的这个端口号。好，那么发送方式h它的MAC地址是九二。

进来的端口是八号端口。接下来检查接收方，接收方的MAC地址在自己的这个交换表当中，可以查到那接下来交换机一就可以从端口一这。把这个色号帧转发出去，那这样a节点就可以收到色号帧。好，这是四号帧的传输。接下来，五号针h要给c发送。五号针从二号端口进入交换机，那交换机先检查五号针的发送方是h。接收方是c。由于发送方h它的这个地址和端口号的信息，我已经登记过了，

所以就不需要再重复的登记。那接下来交换机二检查接收方的地址，由于c的这个MAC地址在这个交换表当中找不到。所以交换机二只能够把呃，这个五号帧转发到除了入口之外的，其他所有的接口。因此ijkl这几个节点会收到这个mac帧好，那与此同时，五号帧从一号接口这出去。被交换机一所接收。检查五号帧的发送方h，这个h在交换表一当中也已经有登记，所以就不用再重复登记。紧接着再检查五号针的接收方MAC地址，那可以找到接收方连接的是三号端口。

所以五号帧会被交换机一从三后端口这转发出去，那c节点就可以顺利的收到这个帧。当然ijk这几个节点会啊，直接把帧丢弃。好，这是五号针的传输。接下来，六号针c发给h的那六号针从三号端口进入交换机一，由于发送方c的地址已经登记过，所以不用再重复登记。接下来检查六号针的接收方h，那这个MAC地址呃，能够找到它对应的是端口号八。所以六号针会从八号端口这。转发出去。

好，那交换机二收到这个帧之后，首先检查这个帧的发送方，那发送方的MAC地址。还暂时没有被登记到交换表二上面，所以交换机二会把这个地址也就是c节点的这个地址。给登记上去，它是从一号端口进入的。好登记好了之后再检查接收方的MAC地址，那由于接收方的MAC地址能够找到它的端口是二。所以这个帧会从二号端口这被精准的转化出去，于是h节点收到了六号帧。好，接下来看七号针，我们假设七号针是呃节点j发送的一个广播针。

所以七号帧的发送方啊，对应的是节点j的MAC地址。而接收方的这个地址是权一表示，这是一个广播帧。好，那么七号针是这么传输的，首先七号针给到集线器。那集线器会无脑的，把七号针转发到各个接口。于是k节点l节点都可以收到七号针好，那对于交换机二来说，由于这个针。是从五号口进来的，所以它首先登记这个针的发送方和端口号的一个呃对应关系。那发送方的MAC地址是啊，

九四结尾的好，所以把这个发送方的MAC地址什么什么九四？登记进来，它是从五号端口进入的。接下来，交换机检查这个帧的目的，地址权一表示，这是一个广播帧，所以它会把这个帧。转发到除了入口之外的其他所有口。好于是h和j能够顺利的收到这个广播针。那与此同时，七号针通过这条线传给交换机一。交换机一先检查七号帧的发送方，它的MAC地址需要登记到交换表上。

那它的MAC地址是什么？什么九四？它是从八号端口进入的。接下来，交换机检查目的地址，发现它是一个广播帧，于是交换机一会把这个帧从。除了入口之外的其他所有端口转发出去。于是abcd可以顺利的收到这个广播针，与集线器相连的efg也可以收到这个广播针。好，那这是七号针的传输。最后再来看一个例子，八号帧k给j发送的。八号针首先传给集线器，

那么集线器会无脑的转发到各个呃端口。那j和l都会收到八号帧。只有j这个节点会接收八号帧，因为目的地址和自己的MAC地址能够对得上。因此j节点接收l节点把帧丢弃。好，现在对于交换机二来说，它先检查发送方的MAC地址，那这个MAC地址还没有被登记到交换表上。所以需要做一个登记。那可以看到k节点的MAC地址是九五结尾的，所以把什么什么什么九五登记上去，同时这个MAC地址是从五号端口进来的。好，接下来交换机二检查这个帧的呃目的地址。

这个目的地址对应的是五号端口，也就是说这个帧的发送方和接收方连接的都是我的五号端口。因此，交换机二就不需要把这个帧再从五号口重新转发出去，多此一举了，对吧？好，所以八号帧的传输到这一步就结束了，交换机二不会再转发这个帧。那经过这一系列的传输，两个交换表就是这个样子。好，接下来我们思考这样的一个问题，假设现在h这个节点，它把网线拔掉。

连到了交换机一，这边也就变成这个样子，那这样的调整会导致什么问题呢？显然，这个节点的位置改变之后。交换表二的这个表项。以及交换表一的这个表项。它就是错误的，对吧？好，所以如果此时a要给h发送一个帧，那么这个帧给到交换机一之后。它检查接收方对应的是八号端口，于是。这个帧会从八号端口这转发出去。

那对于交换机二来说，他检查发现接收方呃对应二号端口，所以他会尝试从二号端口把这个针转发出去，但是二号端口的。后面已经空了，对吧？因此此时会导致h节点无法顺利的接收这个帧。那如何解决这个问题呢？在以太网交换机当中，呃，每一个表象其实会设置一个呃，有效期。有效期过了之后就自动作废。比如h这个节点，它对应的表象过期作废了，

那么同样的情况，如果a再给h发一个呃帧。那么，这个帧先给到交换机一，由于在交换机一的内部找不到呃接收方的MAC地址对应的端口号。找不到有效的信息，所以交换机一会把这个呃a给h发送的这个帧。从它连接的所有的这些端口转发出去。于是乎h这个节点就可以顺利的收到这个帧。那对于交换机二也是一样的，当它收到这个帧的时候，由于接收方的MAC地址在交换表当中找不到。所以交换机二也会把这个帧从各个端口转发出去。好，总之我们给每个表象设置一个有效期，

过期自动作废，这种机制可以防止某些节点呃突然离开，或者说换接口这种事情发生。那接下来只要h节点啊，它发出了某一个帧，那么交换机一就可以把h节点的这个新位置的信息给重新呃登记到这个交换表这。那对于交换机二也是一样的，什么时候他收到了一个h发出的帧，那么他就可以把h的新位置的信息给更新到自己的交换表这。好，那这是以太网交换机的自学习功能，每个交换机的内部都会维护一个交换表。初始为空。交换机每收到一个帧，就会把发送方这个帧的发送方呃的MAC地址和这个帧进来的端口号。

进行登记更新到交换表里边。那收到的帧如何转发出去呢？就是先查交换表，如果在交换表当中啊，查不到也就不知道接收方到底对应的是哪个端口，那么就把这个帧当做广播帧去处理。就是广播道除了入口之外的，其他所有端口。而如果能够查到接收方对应的是哪个端口，那么就可以精准的去转发到某一个端口。另外，为了防止某些节点啊，拔线跑路，所以交换表当中每个表项都会设置一个有效时间。过期之后自动的作废。

好，那么有了自学习功能的支持交换机就可以支持即插即用。我们可以动态的拔掉一些节点，也可以动态的插入一些新节点，反正经过自学习的过程之后，交换机总能知道每个节点的正确位置。好，这是自学习功能，也是考试的重点，接下来我们还要介绍交换机的两种交换方式，一种叫直通交换，一种叫存储转发交换。来看一下二者的区别，首先我们观察这个以太网mac帧。六六二n四收发协数页。

一个真应该发给谁？其实只需要检查开始的这六个字节就可以了，对吧？好，那么这种思路就引出了直通式的交换方式。这是一个完整的针六六二n四。只有开头的六个字，节会被交换机接收，并且去检查它的目的地址。那根据目的地址再决定这个针。到底应该从哪个端口转发出去？这就有点像早期电话网络当中呃，就是电路交换的那种特点了，对吧？一旦我确定这个转发的端口之后，

我就直接用内部的一根线呃，让整个帧的数据就是快速的通过。那显然，这种方式的转发时延会很低，因为对于交换机来说，它只需要接收并且处理目的地址六个字节的部分。好，这是直通交换的方式，那另一种交换方式存储转发交换。他的思路就是交换机，首先把这个针完整的接收，并且放到自己的高速缓存里边。然后检查这个帧的呃目的地址决定到底应该从哪个端口把它转发出去。与此同时，还可以对这个针进行差错控制等等，

一系列更复杂的处理。那处理完这个针之后，再把这个针。从呃相应的这个出口给它转发出去，那这就是存储转发交换方式，显然要把整个帧都保存下来，然后再转发出去。这种方式的转发时延会比直通方式要更高。那显然，对于直通交换方式来说，它不适用于需要速率匹配的这种场景。比如说有个节点a。它以100 mbps的这个速度。呃，发送帧。

然后出口这边连接的节点b，它只能以10 mbps的速度去接收这个针。那如果采用直通交换方式的话，那是不是意味着这个交换机的主体？它一旦确定了目的地址，对应的呃出口之后。在它的内部，是直接用一根线，可以这么理解，直接用一根线，把二者连通起来了，对吧？也就是说。a以多快的速度发送数据过来，那么这些数据会以同样的速度从出口这出去。

那对于b节点来说，由于它的接收能力不行，所以a节点给他发过来的这些数据。就无法被顺利的接收，我受不了这么快的速度，对吧好，所以这种方式它不适用于需要速率匹配的这种呃场景。那同样的故事，如果采用存储转发式，那么假设一个呃速度比较快的节点，以100 mbps的速度。发送这个帧那么这个帧首先会被交换机啊，完整的接收放到自己的高速缓存当中。好，接下来把这个帧从二号口转发给b节点。

那么，交换机可以根据b节点接收数据的这种能力去做一个速率的匹配。所以，存储转发式就适用于需要速率匹配的场景好，那么再回到直通方式，直通方式不支持协议转换。什么意思呢？呃，大家不妨思考一下，我们之前学过的VLAN。在虚拟局域网当中。主机给交换机发送的这个数据帧格式是标准的mac帧，对吧？标准的mac帧格式。而交换机和交换机之间的数据传输。

是八零二点一q帧的格式。那么，如果中间这个交换机，它采用的是直通方式，那是不是意味着主机给我发送什么格式的帧？我就只能让这个帧原模原样的就是呃。转发到下一个节点，对吧？也就是不支持这种协议的转换。那相比之下，如果采用后面这种策略，那我这个交换机先把标准的mac帧。完整的接收到自己的高速缓存当中，然后再把这个标准的mac帧。转一个格式转成八零二点EQ帧，

然后再把这个帧转交给下一个相邻的节点。那这样的话就可以完成呃，所谓的协议转换。好，所以后面这种方式它适用于呃，需要协议转换的这种线路。那最后呃存储转发这种方式也可以支持差错检测，你把整个帧放在高速缓存里，然后根据什么crc循环冗余校验的那一套规则去检查它有没有差错，对吧？那相比之下，直通式它肯定就没办法进行差错检测。好，所以这是两种交换方式的区别，好，

那接下来我们看一个真题，一三年的38题。这道题考的就是直通交换方式。一个以太网交换机，它的速率是100 mbps。采用直通交换方式去转发一个以太网针那题目，告诉我们呃，这个以太网针不考虑前导码。问，我们引起的转发时延至少是多少？好，那刚才我们说过直通交换的这种方式，交换机只需要接收，并且处理一个真的开头六个字节。六六二n四收发协数验开头的六个字节，

表示的就是接收方的呃MAC地址，对吧？那么，接收并且处理六个字节所需要的时间就是啊，把这六个字节也就是48比特。除以这个交换机的速率，根据题目给的条件接收六个字节，至少需要零点四八微秒。那根据题目给的这些条件，我们没办法去判断什么查找交换表啊等等呃，其他的那些时间开销对吧？所以这个转发时延至少就应该是零点四八位秒，因此这个题目选择b选项。好的，那么在这个视频中，

我们介绍了以太网交换机的工作原理啊，最重要的是这个自学习功能是考试的重点。与此同时，我们也介绍了交换机的两种交换方式，分别是直通交换和存储转发交换。如果采用直通交换方式，那么交换机只检查目的MAC地址，也就是一个以太网帧的前六个字节。通过前六个字节就可以决定呃，应该把这个帧转发到哪个端口？那直通交换方式有点类似于电话网络当中的电路交换那种方式，优点是转发时延低。但与此同时，也带来了一些缺点。那为了解决这些缺点，

我们可以采用存储转发式的交换。也就是交换机会，把整个针完整的接收下来，然后对这个针进行差错检测，还有协议转换等等，一系列有必要的处理。最后，再根据交换表决定到底要从哪个端口把这个帧转化出去。那采用存储转发的方式呃，就可以解决直通交换方式不支持的这些特性。不过，与此同时，带来的代价就是转发时延提高了。好的，那么大家可以再结合这个思维导图进行回顾，

那以上就是这视频的全部内容。
