各位同学大家好，在这个视频中，我们要学习ppp协议中文译名叫点对点协议point to point。protocol ppp协议，它属于数据链路层，在之前的学习中，我们详细探讨过以太网相关的技术，那么在一个以太网当中。数据链路层会以以太网mac帧呃作为pdu来进行数据的传输，那么以太网mac帧的内部可能携带的是一个IP分组。那类似的道理ppp协议，它作为数据链路层的协议，也需要定义ppp帧，它的格式是什么样？那ppp这种协议通常被用于点对点的链路，

比如说两台路由器之间可能有一条点对点的线路。那么，这两台路由器之间的数据链路层就可以使用ppp帧来传输数据，那和以太网mac真类似？ppp帧的内部也可以携带呃，更高层的这个协议数据，比如说携带IP分组。那关于这个协议最重要的东西，我都总结在了这张思维导图里边，接下来我们会基于这张导图来复习。首先ppp协议，它有三个重要的组成部分，分别是链路控制协议，英文缩写lcp。网络控制协议，

英文缩写ncp这两个协议，它属于ppp协议的子协议。关于这两个子协议有什么作用呃，我们放在之后再来探讨。好，除了这二者之外，ppp协议还需要定义ppp帧的格式，那我们知道数据链路层的帧。可以用于携带网络层的呃，协议数据，比如说携带IP数据报。那通过之后的学习，你也会看到ppp帧，它的内部也可以携带lcp协议的数据以及ncp协议的数据。好，

那么它定义的帧格是长这个样子，首先可以看到ppp帧的呃，最开始这一个字节以及最末尾的这一个字节。它们的值都固定为七亿。展开来看，就是零幺幺幺零那么开头的这个字节，以及末尾的这个字节。这种特殊的比特组合就是作为ppp帧的帧定界，符用于标记帧的开始和结束。好，接下来看地址域和控制域。对应ppp帧的第二个字节和第三个字节，那这两个字节固定为ff和零三。反正这两个字节啊，值是固定的，

没有特殊的含义，好接下来看协议字段占两个字节。这个字段指明了ppp协议的信息部分，它具体携带的是哪一个协议的数据？比如说如果携带的是IPV 4的这个数据的话，那么这个协议字段没记错的话，应该是啊零零二幺。这样的一个纸。那相应的，如果这个地方携带的是比如说lcp协议的数据或者ncp协议的数据，那协议字段就需要有对应的修改。那可以看到ppp针的信息部分，它的长度是可变的。通常不超过1500字节。好，

再往后看fcs。这个字段占两个字节，它主要用于存储crc循环冗余校验码，那这个我们很熟悉了。就是让ppp针的接收方根据这个校验码来检测整个ppp针有没有发生比特错误。如果发生了比特错误，那么直接把ppp针给丢弃。处理的方式和以太网针是类似的，直接把错误的针丢弃，但是并不会给发送方返回任何的呃信息。好，那么关于ppp协议的各个字段分别是什么含义？我已经在这个地方给大家梳理了这种记忆性的考点，考前回顾一下即可。好，

接下来我们探讨ppp帧如何实现透明传输，通过刚才学习我们知道。一个ppp帧，它是用零幺幺幺零作为帧的开头和结尾的定界符。那么显然，零幺幺幺零这种特殊的比特组合也有可能会出现在信息部分，对吧？那么，如何确保信息部分出现的这种特殊的比特组合不被误解为是真的开始或者结束？这个就是透明传输要解决的问题，那ppp协议支持字符填充和零比特填充。这两种方式去实现透明传输，其中字节填充会用于异步传输，而零比特填充会用于同步传输。所谓的异步传输，

就是两个点对点相连的节点之间，没有统一的时钟信号。而同步传输是指两个点对点相连的节点之间有统一的时钟信号。对于两个采用异步传输的节点来说，它们之间的这个ppp帧的传输是以字符为单位传送的。也就是巴比特为一组来传送。而如果两个节点之间采用同步传输，那么对ppp帧的传输就是一连串的比特，连续的传送。好，那么假如按照巴比特为一组的这种字符来传输的话，那信息部分有可能会包含。和真定界符完全一样的这种巴比特字符对吧？如果遇到这种特殊的字符，那么在这个字符之前。

加上一个转译字符。ppp协议定义的转义字符是啊7d。零幺幺幺零幺。那对于数据的接收方而言，一旦收到一个转移字符，就知道转移字符的后面即便跟的是这种特殊的字节。那也不能把这种字节呃解读为真定界符，就这个意思。那关于字节填充法实现透明传输的原理，我们在之前有过详细的探讨，这就不再赘述好，接下来再来看。如果采用同步传输。也就是把ppp帧的各个比特连续的进行传输，那么除了首尾的真定界符可能会出现连续的六个一之外。

在这个信息部分，只要发现有五个连续的一，我们就立即填入一个零。这是ppp真的发送方要做的事情，而接收方要做的是相反的事情，一旦检测到五个连续的一。就应该把呃后面紧跟的那个零给去掉那关于零比特填充法，实现透明传输的原理，我们也在之前有过详细的探讨。这我们只是简单回顾一下好，总之ppp协议支持两种实现透明传输的方式，分别是字符填充和零比特填充。分别用于逐字节的传送和一连串比特连续传送这两种场景好，那么最后我们总结一下这个协议的特点。首先，

它是链路层的协议，并且只支持点对点全双工的这种链路。在总线型的链路当中，不可以使用ppp协议作为链路层协议好，另外之前我们说过ppp真的接收方。如果检测出这个差错，那么会直接把ppp针给丢弃。所以只要能够被接收方顺利收下的这个针，它肯定是无差错的。但另一方面，接收方并不会给发送方返回类似于ack啊这种确认机制，因此用ppp协议实现的这个链路层。它只能保证无差错，但是不可靠。好，

另外从ppp帧的协议字段可以看到，只要修改协议字段，那么信息部分就可以对应的去携带呃各种各样的。高层协议数据，所以我们说ppp协议，它可以支持多种网络层的协议，比如说可以用ppb帧携带IPV 4分组。IPV 6分组或者像苹果公司，它定义的一个呃协议叫做appletalk，这个协议的数据也可以用ppp协议去运输。好，最后值得一提的是ppp针，它的长度一定是字节的整数倍，这个特点记住就行，不用问为什么。

这是设计者规定的好，那么关于这个协议的基本内容，我们先介绍到这儿，在下节课我们再来展开介绍lcp和ncp这两个子协议有什么作用？
